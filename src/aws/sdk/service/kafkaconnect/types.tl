-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    enum ConnectorOperationStepState
        "PENDING"
        "IN_PROGRESS"
        "COMPLETED"
        "FAILED"
        "CANCELLED"
    end

    enum ConnectorOperationStepType
        "INITIALIZE_UPDATE"
        "FINALIZE_UPDATE"
        "UPDATE_WORKER_SETTING"
        "UPDATE_CONNECTOR_CONFIGURATION"
        "VALIDATE_UPDATE"
    end

    -- Details of a step that is involved in a connector's operation.
    record ConnectorOperationStep
        -- The step type of the operation.
        stepType: string
        -- The step state of the operation.
        stepState: string
    end

    enum ConnectorOperationState
        "PENDING"
        "UPDATE_IN_PROGRESS"
        "UPDATE_COMPLETE"
        "UPDATE_FAILED"
        "ROLLBACK_IN_PROGRESS"
        "ROLLBACK_FAILED"
        "ROLLBACK_COMPLETE"
    end

    enum ConnectorOperationType
        "UPDATE_WORKER_SETTING"
        "UPDATE_CONNECTOR_CONFIGURATION"
        "ISOLATE_CONNECTOR"
        "RESTORE_CONNECTOR"
    end

    -- Summary of a connector operation.
    record ConnectorOperationSummary
        -- The Amazon Resource Name (ARN) of the connector operation.
        connectorOperationArn: string
        -- The type of connector operation performed.
        connectorOperationType: string
        -- The state of the connector operation.
        connectorOperationState: string
        -- The time when operation was created.
        creationTime: number
        -- The time when operation ended.
        endTime: number
    end

    -- The description of the scale-in policy for the connector.
    record ScaleInPolicyDescription
        -- Specifies the CPU utilization percentage threshold at which you want connector scale in to be triggered.
        cpuUtilizationPercentage: number
    end

    -- The description of the scale-out policy for the connector.
    record ScaleOutPolicyDescription
        -- The CPU utilization percentage threshold at which you want connector scale out to be triggered.
        cpuUtilizationPercentage: number
    end

    -- Information about the auto scaling parameters for the connector.
    record AutoScalingDescription
        -- The maximum number of workers allocated to the connector.
        maxWorkerCount: number
        -- The number of microcontroller units (MCUs) allocated to each connector worker. The valid values are 1,2,4,8.
        mcuCount: number
        -- The minimum number of workers allocated to the connector.
        minWorkerCount: number
        -- The scale-in policy for the connector.
        scaleInPolicy: ScaleInPolicyDescription
        -- The scale-out policy for the connector.
        scaleOutPolicy: ScaleOutPolicyDescription
        -- The maximum number of tasks allocated to the connector during autoscaling operations. Must be at least equal to maxWorkerCount.
        maxAutoscalingTaskCount: number
    end

    -- The description of a connector's provisioned capacity.
    record ProvisionedCapacityDescription
        -- The number of microcontroller units (MCUs) allocated to each connector worker. The valid values are 1,2,4,8.
        mcuCount: number
        -- The number of workers that are allocated to the connector.
        workerCount: number
    end

    -- A description of the connector's capacity.
    record CapacityDescription
        -- Describes the connector's auto scaling capacity.
        autoScaling: AutoScalingDescription
        -- Describes a connector's provisioned capacity.
        provisionedCapacity: ProvisionedCapacityDescription
    end

    enum ConnectorState
        "RUNNING"
        "CREATING"
        "UPDATING"
        "DELETING"
        "FAILED"
    end

    -- The description of the VPC in which the connector resides.
    record VpcDescription
        -- The security groups for the connector.
        securityGroups: {string}
        -- The subnets for the connector.
        subnets: {string}
    end

    -- The description of the Apache Kafka cluster to which the connector is connected.
    record ApacheKafkaClusterDescription
        -- The bootstrap servers of the cluster.
        bootstrapServers: string
        -- Details of an Amazon VPC which has network connectivity to the Apache Kafka cluster.
        vpc: VpcDescription
    end

    -- Details of how to connect to the Apache Kafka cluster.
    record KafkaClusterDescription
        -- The Apache Kafka cluster to which the connector is connected.
        apacheKafkaCluster: ApacheKafkaClusterDescription
    end

    enum KafkaClusterClientAuthenticationType
        "NONE"
        "IAM"
    end

    -- The client authentication information used in order to authenticate with the Apache Kafka cluster.
    record KafkaClusterClientAuthenticationDescription
        -- The type of client authentication used to connect to the Apache Kafka cluster. Value NONE means that no client authentication is used.
        authenticationType: string
    end

    enum KafkaClusterEncryptionInTransitType
        "PLAINTEXT"
        "TLS"
    end

    -- The description of the encryption in transit to the Apache Kafka cluster.
    record KafkaClusterEncryptionInTransitDescription
        -- The type of encryption in transit to the Apache Kafka cluster.
        encryptionType: string
    end

    -- A description of the log delivery settings.
    record CloudWatchLogsLogDeliveryDescription
        -- Whether log delivery to Amazon CloudWatch Logs is enabled.
        enabled: boolean
        -- The name of the CloudWatch log group that is the destination for log delivery.
        logGroup: string
    end

    -- A description of the settings for delivering logs to Amazon Kinesis Data Firehose.
    record FirehoseLogDeliveryDescription
        -- The name of the Kinesis Data Firehose delivery stream that is the destination for log delivery.
        deliveryStream: string
        -- Specifies whether connector logs get delivered to Amazon Kinesis Data Firehose.
        enabled: boolean
    end

    -- The description of the details about delivering logs to Amazon S3.
    record S3LogDeliveryDescription
        -- The name of the S3 bucket that is the destination for log delivery.
        bucket: string
        -- Specifies whether connector logs get sent to the specified Amazon S3 destination.
        enabled: boolean
        -- The S3 prefix that is the destination for log delivery.
        prefix: string
    end

    -- Workers can send worker logs to different destination types. This configuration specifies the details of these destinations.
    record WorkerLogDeliveryDescription
        -- Details about delivering logs to Amazon CloudWatch Logs.
        cloudWatchLogs: CloudWatchLogsLogDeliveryDescription
        -- Details about delivering logs to Amazon Kinesis Data Firehose.
        firehose: FirehoseLogDeliveryDescription
        -- Details about delivering logs to Amazon S3.
        s3: S3LogDeliveryDescription
    end

    -- The description of the log delivery settings.
    record LogDeliveryDescription
        -- The workers can send worker logs to different destination types. This configuration specifies the details of these destinations.
        workerLogDelivery: WorkerLogDeliveryDescription
    end

    enum NetworkType
        "IPV4"
        "DUAL"
    end

    -- Details about a custom plugin.
    record CustomPluginDescription
        -- The Amazon Resource Name (ARN) of the custom plugin.
        customPluginArn: string
        -- The revision of the custom plugin.
        revision: number
    end

    -- The description of the plugin.
    record PluginDescription
        -- Details about a custom plugin.
        customPlugin: CustomPluginDescription
    end

    -- The description of the worker configuration.
    record WorkerConfigurationDescription
        -- The revision of the worker configuration.
        revision: number
        -- The Amazon Resource Name (ARN) of the worker configuration.
        workerConfigurationArn: string
    end

    -- Summary of a connector.
    record ConnectorSummary
        -- The connector's compute capacity settings.
        capacity: CapacityDescription
        -- The Amazon Resource Name (ARN) of the connector.
        connectorArn: string
        -- The description of the connector.
        connectorDescription: string
        -- The name of the connector.
        connectorName: string
        -- The state of the connector.
        connectorState: string
        -- The time that the connector was created.
        creationTime: number
        -- The current version of the connector.
        currentVersion: string
        -- The details of the Apache Kafka cluster to which the connector is connected.
        kafkaCluster: KafkaClusterDescription
        -- The type of client authentication used to connect to the Apache Kafka cluster. The value is NONE when no client authentication is used.
        kafkaClusterClientAuthentication: KafkaClusterClientAuthenticationDescription
        -- Details of encryption in transit to the Apache Kafka cluster.
        kafkaClusterEncryptionInTransit: KafkaClusterEncryptionInTransitDescription
        -- The version of Kafka Connect. It has to be compatible with both the Apache Kafka cluster's version and the plugins.
        kafkaConnectVersion: string
        -- The settings for delivering connector logs to Amazon CloudWatch Logs.
        logDelivery: LogDeliveryDescription
        -- The network type of the connector. It gives connectors connectivity to either IPv4 (IPV4) or IPv4 and IPv6 (DUAL) destinations. Defaults to IPV4.
        networkType: string
        -- Specifies which plugins were used for this connector.
        plugins: {PluginDescription}
        -- The Amazon Resource Name (ARN) of the IAM role used by the connector to access Amazon Web Services resources.
        serviceExecutionRoleArn: string
        -- The worker configurations that are in use with the connector.
        workerConfiguration: WorkerConfigurationDescription
    end

    enum CustomPluginState
        "CREATING"
        "CREATE_FAILED"
        "ACTIVE"
        "UPDATING"
        "UPDATE_FAILED"
        "DELETING"
    end

    enum CustomPluginContentType
        "JAR"
        "ZIP"
    end

    -- Details about a custom plugin file.
    record CustomPluginFileDescription
        -- The hex-encoded MD5 checksum of the custom plugin file. You can use it to validate the file.
        fileMd5: string
        -- The size in bytes of the custom plugin file. You can use it to validate the file.
        fileSize: number
    end

    -- The description of the location of an object in Amazon S3.
    record S3LocationDescription
        -- The Amazon Resource Name (ARN) of an S3 bucket.
        bucketArn: string
        -- The file key for an object in an S3 bucket.
        fileKey: string
        -- The version of an object in an S3 bucket.
        objectVersion: string
    end

    -- Information about the location of a custom plugin.
    record CustomPluginLocationDescription
        -- The S3 bucket Amazon Resource Name (ARN), file key, and object version of the plugin file stored in Amazon S3.
        s3Location: S3LocationDescription
    end

    -- Details about the revision of a custom plugin.
    record CustomPluginRevisionSummary
        -- The format of the plugin file.
        contentType: string
        -- The time that the custom plugin was created.
        creationTime: number
        -- The description of the custom plugin.
        description: string
        -- Details about the custom plugin file.
        fileDescription: CustomPluginFileDescription
        -- Information about the location of the custom plugin.
        location: CustomPluginLocationDescription
        -- The revision of the custom plugin.
        revision: number
    end

    -- A summary of the custom plugin.
    record CustomPluginSummary
        -- The time that the custom plugin was created.
        creationTime: number
        -- The Amazon Resource Name (ARN) of the custom plugin.
        customPluginArn: string
        -- The state of the custom plugin.
        customPluginState: string
        -- A description of the custom plugin.
        description: string
        -- The latest revision of the custom plugin.
        latestRevision: CustomPluginRevisionSummary
        -- The name of the custom plugin.
        name: string
    end

    -- A plugin is an Amazon Web Services resource that contains the code that defines a connector's logic.
    record CustomPlugin
        -- The Amazon Resource Name (ARN) of the custom plugin.
        customPluginArn: string
        -- The revision of the custom plugin.
        revision: number
    end

    -- A plugin is an Amazon Web Services resource that contains the code that defines your connector logic.
    record Plugin
        -- Details about a custom plugin.
        customPlugin: CustomPlugin
    end

    -- The summary of a worker configuration revision.
    record WorkerConfigurationRevisionSummary
        -- The time that a worker configuration revision was created.
        creationTime: number
        -- The description of a worker configuration revision.
        description: string
        -- The revision of a worker configuration.
        revision: number
    end

    enum WorkerConfigurationState
        "ACTIVE"
        "DELETING"
    end

    -- The summary of a worker configuration.
    record WorkerConfigurationSummary
        -- The time that a worker configuration was created.
        creationTime: number
        -- The description of a worker configuration.
        description: string
        -- The latest revision of a worker configuration.
        latestRevision: WorkerConfigurationRevisionSummary
        -- The name of the worker configuration.
        name: string
        -- The Amazon Resource Name (ARN) of the worker configuration.
        workerConfigurationArn: string
        -- The state of the worker configuration.
        workerConfigurationState: string
    end

    -- Information about the VPC in which the connector resides.
    record Vpc
        -- The security groups for the connector.
        securityGroups: {string}
        -- The subnets for the connector.
        subnets: {string}
    end

    -- The details of the Apache Kafka cluster to which the connector is connected.
    record ApacheKafkaCluster
        -- The bootstrap servers of the cluster.
        bootstrapServers: string
        -- Details of an Amazon VPC which has network connectivity to the Apache Kafka cluster.
        vpc: Vpc
    end

    -- The scale-in policy for the connector.
    record ScaleInPolicy
        -- Specifies the CPU utilization percentage threshold at which you want connector scale in to be triggered.
        cpuUtilizationPercentage: number
    end

    -- The scale-out policy for the connector.
    record ScaleOutPolicy
        -- The CPU utilization percentage threshold at which you want connector scale out to be triggered.
        cpuUtilizationPercentage: number
    end

    -- Specifies how the connector scales.
    record AutoScaling
        -- The maximum number of workers allocated to the connector.
        maxWorkerCount: number
        -- The number of microcontroller units (MCUs) allocated to each connector worker. The valid values are 1,2,4,8.
        mcuCount: number
        -- The minimum number of workers allocated to the connector.
        minWorkerCount: number
        -- The scale-in policy for the connector.
        scaleInPolicy: ScaleInPolicy
        -- The scale-out policy for the connector.
        scaleOutPolicy: ScaleOutPolicy
        -- The maximum number of tasks allocated to the connector during autoscaling operations. Must be at least equal to maxWorkerCount.
        maxAutoscalingTaskCount: number
    end

    -- An update to the connector's scale-in policy.
    record ScaleInPolicyUpdate
        -- The target CPU utilization percentage threshold at which you want connector scale in to be triggered.
        cpuUtilizationPercentage: number
    end

    -- An update to the connector's scale-out policy.
    record ScaleOutPolicyUpdate
        -- The target CPU utilization percentage threshold at which you want connector scale out to be triggered.
        cpuUtilizationPercentage: number
    end

    -- The updates to the auto scaling parameters for the connector.
    record AutoScalingUpdate
        -- The target maximum number of workers allocated to the connector.
        maxWorkerCount: number
        -- The target number of microcontroller units (MCUs) allocated to each connector worker. The valid values are 1,2,4,8.
        mcuCount: number
        -- The target minimum number of workers allocated to the connector.
        minWorkerCount: number
        -- The target scale-in policy for the connector.
        scaleInPolicy: ScaleInPolicyUpdate
        -- The target scale-out policy for the connector.
        scaleOutPolicy: ScaleOutPolicyUpdate
        -- The maximum number of tasks allocated to the connector during autoscaling operations. Must be at least equal to maxWorkerCount.
        maxAutoscalingTaskCount: number
    end

    -- HTTP Status Code 400: Bad request due to incorrect input. Correct your request and then retry it.
    record BadRequestException
        message: string
    end

    -- Details about a connector's provisioned capacity.
    record ProvisionedCapacity
        -- The number of microcontroller units (MCUs) allocated to each connector worker. The valid values are 1,2,4,8.
        mcuCount: number
        -- The number of workers that are allocated to the connector.
        workerCount: number
    end

    -- Information about the capacity of the connector, whether it is auto scaled or provisioned.
    record Capacity
        -- Information about the auto scaling parameters for the connector.
        autoScaling: AutoScaling
        -- Details about a fixed capacity allocated to a connector.
        provisionedCapacity: ProvisionedCapacity
    end

    -- An update to a connector's fixed capacity.
    record ProvisionedCapacityUpdate
        -- The number of microcontroller units (MCUs) allocated to each connector worker. The valid values are 1,2,4,8.
        mcuCount: number
        -- The number of workers that are allocated to the connector.
        workerCount: number
    end

    -- The target capacity for the connector. The capacity can be auto scaled or provisioned.
    record CapacityUpdate
        -- The target auto scaling setting.
        autoScaling: AutoScalingUpdate
        -- The target settings for provisioned capacity.
        provisionedCapacity: ProvisionedCapacityUpdate
    end

    -- The settings for delivering connector logs to Amazon CloudWatch Logs.
    record CloudWatchLogsLogDelivery
        -- Whether log delivery to Amazon CloudWatch Logs is enabled.
        enabled: boolean
        -- The name of the CloudWatch log group that is the destination for log delivery.
        logGroup: string
    end

    -- HTTP Status Code 409: Conflict. A resource with this name already exists. Retry your request with another name.
    record ConflictException
        message: string
    end

    -- The details of the Apache Kafka cluster to which the connector is connected.
    record KafkaCluster
        -- The Apache Kafka cluster to which the connector is connected.
        apacheKafkaCluster: ApacheKafkaCluster
    end

    -- The client authentication information used in order to authenticate with the Apache Kafka cluster.
    record KafkaClusterClientAuthentication
        -- The type of client authentication used to connect to the Apache Kafka cluster. Value NONE means that no client authentication is used.
        authenticationType: string
    end

    -- Details of encryption in transit to the Apache Kafka cluster.
    record KafkaClusterEncryptionInTransit
        -- The type of encryption in transit to the Apache Kafka cluster.
        encryptionType: string
    end

    -- The settings for delivering logs to Amazon Kinesis Data Firehose.
    record FirehoseLogDelivery
        -- The name of the Kinesis Data Firehose delivery stream that is the destination for log delivery.
        deliveryStream: string
        -- Specifies whether connector logs get delivered to Amazon Kinesis Data Firehose.
        enabled: boolean
    end

    -- Details about delivering logs to Amazon S3.
    record S3LogDelivery
        -- The name of the S3 bucket that is the destination for log delivery.
        bucket: string
        -- Specifies whether connector logs get sent to the specified Amazon S3 destination.
        enabled: boolean
        -- The S3 prefix that is the destination for log delivery.
        prefix: string
    end

    -- Workers can send worker logs to different destination types. This configuration specifies the details of these destinations.
    record WorkerLogDelivery
        -- Details about delivering logs to Amazon CloudWatch Logs.
        cloudWatchLogs: CloudWatchLogsLogDelivery
        -- Details about delivering logs to Amazon Kinesis Data Firehose.
        firehose: FirehoseLogDelivery
        -- Details about delivering logs to Amazon S3.
        s3: S3LogDelivery
    end

    -- Details about log delivery.
    record LogDelivery
        -- The workers can send worker logs to different destination types. This configuration specifies the details of these destinations.
        workerLogDelivery: WorkerLogDelivery
    end

    -- The configuration of the workers, which are the processes that run the connector logic.
    record WorkerConfiguration
        -- The revision of the worker configuration.
        revision: number
        -- The Amazon Resource Name (ARN) of the worker configuration.
        workerConfigurationArn: string
    end

    record CreateConnectorInput
        -- Information about the capacity allocated to the connector. Exactly one of the two properties must be specified.
        capacity: Capacity
        -- A map of keys to values that represent the configuration for the connector.
        connectorConfiguration: {string : string}
        -- A summary description of the connector.
        connectorDescription: string
        -- The name of the connector.
        connectorName: string
        -- Specifies which Apache Kafka cluster to connect to.
        kafkaCluster: KafkaCluster
        -- Details of the client authentication used by the Apache Kafka cluster.
        kafkaClusterClientAuthentication: KafkaClusterClientAuthentication
        -- Details of encryption in transit to the Apache Kafka cluster.
        kafkaClusterEncryptionInTransit: KafkaClusterEncryptionInTransit
        -- The version of Kafka Connect. It has to be compatible with both the Apache Kafka cluster's version and the plugins.
        kafkaConnectVersion: string
        -- Details about log delivery.
        logDelivery: LogDelivery
        -- The network type of the connector. It gives connectors connectivity to either IPv4 (IPV4) or IPv4 and IPv6 (DUAL) destinations. Defaults to IPV4.
        networkType: string
        -- Amazon MSK Connect does not currently support specifying multiple plugins as a list. To use more than one plugin for your connector, you can create a single custom plugin using a ZIP file that bundles multiple plugins together. Specifies which plugin to use for the connector. You must specify a single-element list containing one customPlugin object.
        plugins: {Plugin}
        -- The Amazon Resource Name (ARN) of the IAM role used by the connector to access the Amazon Web Services resources that it needs. The types of resources depends on the logic of the connector. For example, a connector that has Amazon S3 as a destination must have permissions that allow it to write to the S3 destination bucket.
        serviceExecutionRoleArn: string
        -- Specifies which worker configuration to use with the connector.
        workerConfiguration: WorkerConfiguration
        -- The tags you want to attach to the connector.
        tags: {string : string}
    end

    record CreateConnectorOutput
        -- The Amazon Resource Name (ARN) that Amazon assigned to the connector.
        connectorArn: string
        -- The name of the connector.
        connectorName: string
        -- The state of the connector.
        connectorState: string
    end

    -- HTTP Status Code 403: Access forbidden. Correct your credentials and then retry your request.
    record ForbiddenException
        message: string
    end

    -- HTTP Status Code 500: Unexpected internal server error. Retrying your request might resolve the issue.
    record InternalServerErrorException
        message: string
    end

    -- HTTP Status Code 404: Resource not found due to incorrect input. Correct your request and then retry it.
    record NotFoundException
        message: string
    end

    -- HTTP Status Code 503: Service Unavailable. Retrying your request in some time might resolve the issue.
    record ServiceUnavailableException
        message: string
    end

    -- HTTP Status Code 429: Limit exceeded. Resource limit reached.
    record TooManyRequestsException
        message: string
    end

    -- HTTP Status Code 401: Unauthorized request. The provided credentials couldn't be validated.
    record UnauthorizedException
        message: string
    end

    -- The location of an object in Amazon S3.
    record S3Location
        -- The Amazon Resource Name (ARN) of an S3 bucket.
        bucketArn: string
        -- The file key for an object in an S3 bucket.
        fileKey: string
        -- The version of an object in an S3 bucket.
        objectVersion: string
    end

    -- Information about the location of a custom plugin.
    record CustomPluginLocation
        -- The S3 bucket Amazon Resource Name (ARN), file key, and object version of the plugin file stored in Amazon S3.
        s3Location: S3Location
    end

    record CreateCustomPluginInput
        -- The type of the plugin file.
        contentType: string
        -- A summary description of the custom plugin.
        description: string
        -- Information about the location of a custom plugin.
        location: CustomPluginLocation
        -- The name of the custom plugin.
        name: string
        -- The tags you want to attach to the custom plugin.
        tags: {string : string}
    end

    record CreateCustomPluginOutput
        -- The Amazon Resource Name (ARN) that Amazon assigned to the custom plugin.
        customPluginArn: string
        -- The state of the custom plugin.
        customPluginState: string
        -- The name of the custom plugin.
        name: string
        -- The revision of the custom plugin.
        revision: number
    end

    record CreateWorkerConfigurationInput
        -- A summary description of the worker configuration.
        description: string
        -- The name of the worker configuration.
        name: string
        -- Base64 encoded contents of connect-distributed.properties file.
        propertiesFileContent: string
        -- The tags you want to attach to the worker configuration.
        tags: {string : string}
    end

    record CreateWorkerConfigurationOutput
        -- The time that the worker configuration was created.
        creationTime: number
        -- The latest revision of the worker configuration.
        latestRevision: WorkerConfigurationRevisionSummary
        -- The name of the worker configuration.
        name: string
        -- The Amazon Resource Name (ARN) that Amazon assigned to the worker configuration.
        workerConfigurationArn: string
        -- The state of the worker configuration.
        workerConfigurationState: string
    end

    record DeleteConnectorInput
        -- The Amazon Resource Name (ARN) of the connector that you want to delete.
        connectorArn: string
        -- The current version of the connector that you want to delete.
        currentVersion: string
    end

    record DeleteConnectorOutput
        -- The Amazon Resource Name (ARN) of the connector that you requested to delete.
        connectorArn: string
        -- The state of the connector that you requested to delete.
        connectorState: string
    end

    record DeleteCustomPluginInput
        -- The Amazon Resource Name (ARN) of the custom plugin that you want to delete.
        customPluginArn: string
    end

    record DeleteCustomPluginOutput
        -- The Amazon Resource Name (ARN) of the custom plugin that you requested to delete.
        customPluginArn: string
        -- The state of the custom plugin.
        customPluginState: string
    end

    record DeleteWorkerConfigurationInput
        -- The Amazon Resource Name (ARN) of the worker configuration that you want to delete.
        workerConfigurationArn: string
    end

    record DeleteWorkerConfigurationOutput
        -- The Amazon Resource Name (ARN) of the worker configuration that you requested to delete.
        workerConfigurationArn: string
        -- The state of the worker configuration.
        workerConfigurationState: string
    end

    record DescribeConnectorInput
        -- The Amazon Resource Name (ARN) of the connector that you want to describe.
        connectorArn: string
    end

    -- Details about the state of a resource.
    record StateDescription
        -- A code that describes the state of a resource.
        code: string
        -- A message that describes the state of a resource.
        message: string
    end

    record DescribeConnectorOutput
        -- Information about the capacity of the connector, whether it is auto scaled or provisioned.
        capacity: CapacityDescription
        -- The Amazon Resource Name (ARN) of the connector.
        connectorArn: string
        -- A map of keys to values that represent the configuration for the connector.
        connectorConfiguration: {string : string}
        -- A summary description of the connector.
        connectorDescription: string
        -- The name of the connector.
        connectorName: string
        -- The state of the connector.
        connectorState: string
        -- The time the connector was created.
        creationTime: number
        -- The current version of the connector.
        currentVersion: string
        -- The Apache Kafka cluster that the connector is connected to.
        kafkaCluster: KafkaClusterDescription
        -- The type of client authentication used to connect to the Apache Kafka cluster. The value is NONE when no client authentication is used.
        kafkaClusterClientAuthentication: KafkaClusterClientAuthenticationDescription
        -- Details of encryption in transit to the Apache Kafka cluster.
        kafkaClusterEncryptionInTransit: KafkaClusterEncryptionInTransitDescription
        -- The version of Kafka Connect. It has to be compatible with both the Apache Kafka cluster's version and the plugins.
        kafkaConnectVersion: string
        -- Details about delivering logs to Amazon CloudWatch Logs.
        logDelivery: LogDeliveryDescription
        -- The network type of the connector. It gives connectors connectivity to either IPv4 (IPV4) or IPv4 and IPv6 (DUAL) destinations. Defaults to IPV4.
        networkType: string
        -- Specifies which plugins were used for this connector.
        plugins: {PluginDescription}
        -- The Amazon Resource Name (ARN) of the IAM role used by the connector to access Amazon Web Services resources.
        serviceExecutionRoleArn: string
        -- Specifies which worker configuration was used for the connector.
        workerConfiguration: WorkerConfigurationDescription
        -- Details about the state of a connector.
        stateDescription: StateDescription
    end

    record DescribeConnectorOperationInput
        -- ARN of the connector operation to be described.
        connectorOperationArn: string
    end

    -- Details about worker setting of a connector
    record WorkerSetting
        capacity: CapacityDescription
    end

    record DescribeConnectorOperationOutput
        -- The Amazon Resource Name (ARN) of the connector.
        connectorArn: string
        -- The Amazon Resource Name (ARN) of the connector operation.
        connectorOperationArn: string
        -- The state of the connector operation.
        connectorOperationState: string
        -- The type of connector operation performed.
        connectorOperationType: string
        -- The array of operation steps taken.
        operationSteps: {ConnectorOperationStep}
        -- The origin worker setting.
        originWorkerSetting: WorkerSetting
        -- The origin connector configuration.
        originConnectorConfiguration: {string : string}
        -- The target worker setting.
        targetWorkerSetting: WorkerSetting
        -- The target connector configuration.
        targetConnectorConfiguration: {string : string}
        errorInfo: StateDescription
        -- The time when the operation was created.
        creationTime: number
        -- The time when the operation ended.
        endTime: number
    end

    record DescribeCustomPluginInput
        -- Returns information about a custom plugin.
        customPluginArn: string
    end

    record DescribeCustomPluginOutput
        -- The time that the custom plugin was created.
        creationTime: number
        -- The Amazon Resource Name (ARN) of the custom plugin.
        customPluginArn: string
        -- The state of the custom plugin.
        customPluginState: string
        -- The description of the custom plugin.
        description: string
        -- The latest successfully created revision of the custom plugin. If there are no successfully created revisions, this field will be absent.
        latestRevision: CustomPluginRevisionSummary
        -- The name of the custom plugin.
        name: string
        -- Details about the state of a custom plugin.
        stateDescription: StateDescription
    end

    record DescribeWorkerConfigurationInput
        -- The Amazon Resource Name (ARN) of the worker configuration that you want to get information about.
        workerConfigurationArn: string
    end

    -- The description of the worker configuration revision.
    record WorkerConfigurationRevisionDescription
        -- The time that the worker configuration was created.
        creationTime: number
        -- The description of the worker configuration revision.
        description: string
        -- Base64 encoded contents of the connect-distributed.properties file.
        propertiesFileContent: string
        -- The description of a revision of the worker configuration.
        revision: number
    end

    record DescribeWorkerConfigurationOutput
        -- The time that the worker configuration was created.
        creationTime: number
        -- The description of the worker configuration.
        description: string
        -- The latest revision of the custom configuration.
        latestRevision: WorkerConfigurationRevisionDescription
        -- The name of the worker configuration.
        name: string
        -- The Amazon Resource Name (ARN) of the custom configuration.
        workerConfigurationArn: string
        -- The state of the worker configuration.
        workerConfigurationState: string
    end

    record ListConnectorOperationsInput
        -- The Amazon Resource Name (ARN) of the connector for which to list operations.
        connectorArn: string
        -- Maximum number of connector operations to fetch in one get request.
        maxResults: number
        -- If the response is truncated, it includes a NextToken. Send this NextToken in a subsequent request to continue listing from where it left off.
        nextToken: string
    end

    record ListConnectorOperationsOutput
        -- An array of connector operation descriptions.
        connectorOperations: {ConnectorOperationSummary}
        -- If the response is truncated, it includes a NextToken. Send this NextToken in a subsequent request to continue listing from where it left off.
        nextToken: string
    end

    record ListConnectorsInput
        -- The name prefix that you want to use to search for and list connectors.
        connectorNamePrefix: string
        -- The maximum number of connectors to list in one response.
        maxResults: number
        -- If the response of a ListConnectors operation is truncated, it will include a NextToken. Send this NextToken in a subsequent request to continue listing from where the previous operation left off.
        nextToken: string
    end

    record ListConnectorsOutput
        -- An array of connector descriptions.
        connectors: {ConnectorSummary}
        -- If the response of a ListConnectors operation is truncated, it will include a NextToken. Send this NextToken in a subsequent request to continue listing from where it left off.
        nextToken: string
    end

    record ListCustomPluginsInput
        -- The maximum number of custom plugins to list in one response.
        maxResults: number
        -- If the response of a ListCustomPlugins operation is truncated, it will include a NextToken. Send this NextToken in a subsequent request to continue listing from where the previous operation left off.
        nextToken: string
        -- Lists custom plugin names that start with the specified text string.
        namePrefix: string
    end

    record ListCustomPluginsOutput
        -- An array of custom plugin descriptions.
        customPlugins: {CustomPluginSummary}
        -- If the response of a ListCustomPlugins operation is truncated, it will include a NextToken. Send this NextToken in a subsequent request to continue listing from where the previous operation left off.
        nextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource for which you want to list all attached tags.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- Lists the tags attached to the specified resource in the corresponding request.
        tags: {string : string}
    end

    record ListWorkerConfigurationsInput
        -- The maximum number of worker configurations to list in one response.
        maxResults: number
        -- If the response of a ListWorkerConfigurations operation is truncated, it will include a NextToken. Send this NextToken in a subsequent request to continue listing from where the previous operation left off.
        nextToken: string
        -- Lists worker configuration names that start with the specified text string.
        namePrefix: string
    end

    record ListWorkerConfigurationsOutput
        -- If the response of a ListWorkerConfigurations operation is truncated, it will include a NextToken. Send this NextToken in a subsequent request to continue listing from where the previous operation left off.
        nextToken: string
        -- An array of worker configuration descriptions.
        workerConfigurations: {WorkerConfigurationSummary}
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource to which you want to attach tags.
        resourceArn: string
        -- The tags that you want to attach to the resource.
        tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource from which you want to remove tags.
        resourceArn: string
        -- The keys of the tags that you want to remove from the resource.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateConnectorInput
        -- The target capacity.
        capacity: CapacityUpdate
        -- A map of keys to values that represent the configuration for the connector.
        connectorConfiguration: {string : string}
        -- The Amazon Resource Name (ARN) of the connector that you want to update.
        connectorArn: string
        -- The current version of the connector that you want to update.
        currentVersion: string
    end

    record UpdateConnectorOutput
        -- The Amazon Resource Name (ARN) of the connector.
        connectorArn: string
        -- The state of the connector.
        connectorState: string
        -- The Amazon Resource Name (ARN) of the connector operation.
        connectorOperationArn: string
    end

end

return M
