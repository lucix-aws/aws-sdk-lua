-- Code generated by lua-client-codegen. DO NOT EDIT.

local paginator = require("smithy.paginator")

local M = {}

--- Returns a page iterator for listManagedMicrovmImageVersions.
function M.pages_list_managed_microvm_image_versions(client, input)
    return paginator.pages(client, "listManagedMicrovmImageVersions", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns an item iterator for listManagedMicrovmImageVersions.
function M.items_list_managed_microvm_image_versions(client, input)
    return paginator.items(client, "listManagedMicrovmImageVersions", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns a page iterator for listManagedMicrovmImages.
function M.pages_list_managed_microvm_images(client, input)
    return paginator.pages(client, "listManagedMicrovmImages", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns an item iterator for listManagedMicrovmImages.
function M.items_list_managed_microvm_images(client, input)
    return paginator.items(client, "listManagedMicrovmImages", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns a page iterator for listMicrovmImageBuilds.
function M.pages_list_microvm_image_builds(client, input)
    return paginator.pages(client, "listMicrovmImageBuilds", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns an item iterator for listMicrovmImageBuilds.
function M.items_list_microvm_image_builds(client, input)
    return paginator.items(client, "listMicrovmImageBuilds", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns a page iterator for listMicrovmImageVersions.
function M.pages_list_microvm_image_versions(client, input)
    return paginator.pages(client, "listMicrovmImageVersions", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns an item iterator for listMicrovmImageVersions.
function M.items_list_microvm_image_versions(client, input)
    return paginator.items(client, "listMicrovmImageVersions", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns a page iterator for listMicrovmImages.
function M.pages_list_microvm_images(client, input)
    return paginator.pages(client, "listMicrovmImages", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns an item iterator for listMicrovmImages.
function M.items_list_microvm_images(client, input)
    return paginator.items(client, "listMicrovmImages", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns a page iterator for listMicrovms.
function M.pages_list_microvms(client, input)
    return paginator.pages(client, "listMicrovms", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

--- Returns an item iterator for listMicrovms.
function M.items_list_microvms(client, input)
    return paginator.items(client, "listMicrovms", input, {
        input_token = "nextToken",
        output_token = "nextToken",
        items = "items",
    })
end

return M
