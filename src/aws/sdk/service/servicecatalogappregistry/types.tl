-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- Represents a Amazon Web Services Service Catalog AppRegistry application that is the top-level node in a hierarchy of related
    -- cloud resource abstractions.
    record Application
        -- The identifier of the application.
        id: string
        -- The Amazon resource name (ARN) that specifies the application across services.
        arn: string
        -- The name of the application. The name must be unique in the region in which you are creating the application.
        name: string
        -- The description of the application.
        description: string
        -- The ISO-8601 formatted timestamp of the moment when the application was created.
        creationTime: number
        -- The ISO-8601 formatted timestamp of the moment when the application was last updated.
        lastUpdateTime: number
        -- Key-value pairs you can use to associate with the application.
        tags: {string : string}
        -- A key-value pair that identifies an associated resource.
        applicationTag: {string : string}
    end

    -- Summary of a Amazon Web Services Service Catalog AppRegistry application.
    record ApplicationSummary
        -- The identifier of the application.
        id: string
        -- The Amazon resource name (ARN) that specifies the application across services.
        arn: string
        -- The name of the application. The name must be unique in the region in which you are creating the application.
        name: string
        -- The description of the application.
        description: string
        -- The ISO-8601 formatted timestamp of the moment when the application was created.
        creationTime: number
        -- The ISO-8601 formatted timestamp of the moment when the application was last updated.
        lastUpdateTime: number
    end

    enum ApplicationTagStatus
        "IN_PROGRESS"
        "SUCCESS"
        "FAILURE"
    end

    -- The resource in a list of resources.
    record ResourcesListItem
        -- The Amazon resource name (ARN) of the resource.
        resourceArn: string
        -- The message returned if the call fails.
        errorMessage: string
        -- The status of the list item.
        status: string
        -- Provides information about the AppRegistry resource type.
        resourceType: string
    end

    -- The result of the application tag that's applied to a resource.
    record ApplicationTagResult
        -- The application tag is in the process of being applied to a resource, was successfully applied to a resource, or failed to apply to a resource.
        applicationTagStatus: string
        -- The message returned if the call fails.
        errorMessage: string
        -- The resources associated with an application
        resources: {ResourcesListItem}
        -- A unique pagination token for each page of results. Make the call again with the returned token to retrieve the next page of results.
        nextToken: string
    end

    -- The
    -- definition
    -- of tagQuery.
    -- Specifies which resources are associated with an
    -- application.
    record TagQueryConfiguration
        -- Condition
        -- in the IAM policy
        -- that associates resources
        -- to an application.
        tagKey: string
    end

    -- Includes all
    -- of the AppRegistry settings.
    record AppRegistryConfiguration
        -- Includes the definition
        -- of a tagQuery.
        tagQueryConfiguration: TagQueryConfiguration
    end

    record AssociateAttributeGroupInput
        -- The name, ID, or ARN
        -- of the application.
        application: string
        -- The name, ID, or ARN
        -- of the attribute group
        -- that holds the attributes
        -- to describe the application.
        attributeGroup: string
    end

    record AssociateAttributeGroupOutput
        -- The Amazon resource name (ARN) of the application that was augmented with attributes.
        applicationArn: string
        -- The Amazon resource name (ARN) of the attribute group that contains the application's new attributes.
        attributeGroupArn: string
    end

    -- There was a conflict when processing the request (for example, a resource with the given
    -- name already exists within the account).
    record ConflictException
        message: string
    end

    -- The service is experiencing internal problems.
    record InternalServerException
        message: string
    end

    -- The specified resource does not exist.
    record ResourceNotFoundException
        message: string
    end

    -- The maximum number
    -- of resources per account
    -- has been reached.
    record ServiceQuotaExceededException
        message: string
    end

    -- The request has invalid or missing parameters.
    record ValidationException
        message: string
    end

    enum AssociationOption
        "APPLY_APPLICATION_TAG"
        "SKIP_APPLICATION_TAG"
    end

    enum ResourceType
        "CFN_STACK"
        "RESOURCE_TAG_VALUE"
    end

    record AssociateResourceInput
        -- The name, ID, or ARN
        -- of the application.
        application: string
        -- The type of resource of which the application will be associated.
        resourceType: string
        -- The name or ID of the resource of which the application will be associated.
        resource: string
        -- Determines whether an application tag is applied or skipped.
        options: {string}
    end

    record AssociateResourceOutput
        -- The Amazon resource name (ARN) of the application that was augmented with attributes.
        applicationArn: string
        -- The Amazon resource name (ARN) that specifies the resource.
        resourceArn: string
        -- Determines whether an application tag is applied or skipped.
        options: {string}
    end

    -- The maximum number
    -- of API requests
    -- has been exceeded.
    record ThrottlingException
        -- A message associated with the Throttling exception.
        message: string
        -- The originating service code.
        serviceCode: string
    end

    -- Represents a Amazon Web Services Service Catalog AppRegistry attribute group that is rich metadata which describes an application and its components.
    record AttributeGroup
        -- The globally unique attribute group identifier of the attribute group.
        id: string
        -- The Amazon resource name (ARN) that specifies the attribute group across services.
        arn: string
        -- The name of the attribute group.
        name: string
        -- The description of the attribute group that the user provides.
        description: string
        -- The ISO-8601 formatted timestamp of the moment the attribute group was created.
        creationTime: number
        -- The ISO-8601 formatted timestamp of the moment the attribute group was last updated. This time is the same as the creationTime for a newly created attribute group.
        lastUpdateTime: number
        -- Key-value pairs you can use to associate with the attribute group.
        tags: {string : string}
    end

    -- The details related to a specific AttributeGroup.
    record AttributeGroupDetails
        -- The unique identifier of the attribute group.
        id: string
        -- The Amazon resource name (ARN) that specifies the attribute group.
        arn: string
        -- This field is no longer supported.
        -- We recommend
        -- you don't use the field when using ListAttributeGroupsForApplication.
        -- The name of the attribute group.
        name: string
        -- The service principal that created the attribute group.
        createdBy: string
    end

    -- Summary of a Amazon Web Services Service Catalog AppRegistry attribute group.
    record AttributeGroupSummary
        -- The globally unique attribute group identifier of the attribute group.
        id: string
        -- The Amazon resource name (ARN) that specifies the attribute group across services.
        arn: string
        -- The name of the attribute group.
        name: string
        -- The description of the attribute group that the user provides.
        description: string
        -- The ISO-8601 formatted timestamp of the moment the attribute group was created.
        creationTime: number
        -- The ISO-8601 formatted timestamp of the moment the attribute group was last updated. This time is the same as the creationTime for a newly created attribute group.
        lastUpdateTime: number
        -- The service principal that created the attribute group.
        createdBy: string
    end

    record CreateApplicationInput
        -- The name of the application. The name must be unique in the region in which you are creating the application.
        name: string
        -- The description of the application.
        description: string
        -- Key-value pairs you can use to associate with the application.
        tags: {string : string}
        -- A unique identifier that you provide to ensure idempotency. If you retry a request that
        -- completed successfully using the same client token and the same parameters, the retry succeeds
        -- without performing any further actions. If you retry a successful request using the same
        -- client token, but one or more of the parameters are different, the retry fails.
        clientToken: string
    end

    record CreateApplicationOutput
        -- Information about the application.
        application: Application
    end

    record CreateAttributeGroupInput
        -- The name of the attribute group.
        name: string
        -- The description of the attribute group that the user provides.
        description: string
        -- A JSON string in the form of nested key-value pairs that represent the attributes in the group and describes an application and its components.
        attributes: string
        -- Key-value pairs you can use to associate with the attribute group.
        tags: {string : string}
        -- A unique identifier that you provide to ensure idempotency. If you retry a request that
        -- completed successfully using the same client token and the same parameters, the retry succeeds
        -- without performing any further actions. If you retry a successful request using the same
        -- client token, but one or more of the parameters are different, the retry fails.
        clientToken: string
    end

    record CreateAttributeGroupOutput
        -- Information about the attribute group.
        attributeGroup: AttributeGroup
    end

    record DeleteApplicationInput
        -- The name, ID, or ARN
        -- of the application.
        application: string
    end

    record DeleteApplicationOutput
        -- Information about the deleted application.
        application: ApplicationSummary
    end

    record DeleteAttributeGroupInput
        -- The name, ID, or ARN
        -- of the attribute group
        -- that holds the attributes
        -- to describe the application.
        attributeGroup: string
    end

    record DeleteAttributeGroupOutput
        -- Information about the deleted attribute group.
        attributeGroup: AttributeGroupSummary
    end

    record DisassociateAttributeGroupInput
        -- The name, ID, or ARN
        -- of the application.
        application: string
        -- The name, ID, or ARN
        -- of the attribute group
        -- that holds the attributes
        -- to describe the application.
        attributeGroup: string
    end

    record DisassociateAttributeGroupOutput
        -- The Amazon resource name (ARN) that specifies the application.
        applicationArn: string
        -- The Amazon resource name (ARN) that specifies the attribute group.
        attributeGroupArn: string
    end

    record DisassociateResourceInput
        -- The name or ID of the application.
        application: string
        -- The type of the resource that is being disassociated.
        resourceType: string
        -- The name or ID of the resource.
        resource: string
    end

    record DisassociateResourceOutput
        -- The Amazon resource name (ARN) that specifies the application.
        applicationArn: string
        -- The Amazon resource name (ARN) that specifies the resource.
        resourceArn: string
    end

    record GetApplicationInput
        -- The name, ID, or ARN
        -- of the application.
        application: string
    end

    enum ResourceGroupState
        "CREATING"
        "CREATE_COMPLETE"
        "CREATE_FAILED"
        "UPDATING"
        "UPDATE_COMPLETE"
        "UPDATE_FAILED"
    end

    -- The information about the resource group integration.
    record ResourceGroup
        -- The state of the propagation process for the resource group. The states includes:
        -- CREATING if the resource group is in the process of being created.
        -- CREATE_COMPLETE if the resource group was created successfully.
        -- CREATE_FAILED if the resource group failed to be created.
        -- UPDATING if the resource group is in the process of being updated.
        -- UPDATE_COMPLETE if the resource group updated successfully.
        -- UPDATE_FAILED if the resource group could not update successfully.
        state: string
        -- The Amazon resource name (ARN) of the resource group.
        arn: string
        -- The error message that generates when the propagation process for the resource group fails.
        errorMessage: string
    end

    -- The information about the service integration.
    record Integrations
        -- The information about the resource group integration.
        resourceGroup: ResourceGroup
        applicationTagResourceGroup: ResourceGroup
    end

    record GetApplicationOutput
        -- The identifier of the application.
        id: string
        -- The Amazon resource name (ARN) that specifies the application across services.
        arn: string
        -- The name of the application. The name must be unique in the region in which you are creating the application.
        name: string
        -- The description of the application.
        description: string
        -- The ISO-8601 formatted timestamp of the moment when the application was created.
        creationTime: number
        -- The ISO-8601 formatted timestamp of the moment when the application was last updated.
        lastUpdateTime: number
        -- The number of top-level resources that were registered as part of this application.
        associatedResourceCount: number
        -- Key-value pairs associated with the application.
        tags: {string : string}
        -- The information
        -- about the integration
        -- of the application
        -- with other services,
        -- such as
        -- Resource Groups.
        integrations: Integrations
        -- A key-value pair that identifies an associated resource.
        applicationTag: {string : string}
    end

    enum ResourceItemStatus
        "SUCCESS"
        "FAILED"
        "IN_PROGRESS"
        "SKIPPED"
    end

    record GetAssociatedResourceInput
        -- The name, ID, or ARN
        -- of the application.
        application: string
        -- The type of resource associated with the application.
        resourceType: string
        -- The name or ID of the resource associated with the application.
        resource: string
        -- A unique pagination token for each page of results.
        -- Make the call again with the returned token to retrieve the next page of results.
        nextToken: string
        -- States whether an application tag is applied, not applied, in the process of being applied, or skipped.
        resourceTagStatus: {string}
        -- The maximum number of results to return. If the parameter is omitted, it defaults to 25. The value is optional.
        maxResults: number
    end

    -- The service integration information about the resource.
    record ResourceIntegrations
        -- The information about the integration of Resource Groups.
        resourceGroup: ResourceGroup
    end

    -- The information about the resource.
    record Resource
        -- The name of the resource.
        name: string
        -- The Amazon resource name (ARN) of the resource.
        arn: string
        -- The time the resource was associated with the application.
        associationTime: number
        -- The service integration information about the resource.
        integrations: ResourceIntegrations
    end

    record GetAssociatedResourceOutput
        -- The resource associated with the application.
        resource: Resource
        -- Determines whether an application tag is applied or skipped.
        options: {string}
        -- The result of the application that's tag applied to a resource.
        applicationTagResult: ApplicationTagResult
    end

    record GetAttributeGroupInput
        -- The name, ID, or ARN
        -- of the attribute group
        -- that holds the attributes
        -- to describe the application.
        attributeGroup: string
    end

    record GetAttributeGroupOutput
        -- The identifier of the attribute group.
        id: string
        -- The Amazon resource name (ARN) that specifies the attribute group across services.
        arn: string
        -- The name of the attribute group.
        name: string
        -- The description of the attribute group that the user provides.
        description: string
        -- A JSON string in the form of nested key-value pairs that represent the attributes in the group and describes an application and its components.
        attributes: string
        -- The ISO-8601 formatted timestamp of the moment the attribute group was created.
        creationTime: number
        -- The ISO-8601 formatted timestamp of the moment the attribute group was last updated. This time is the same as the creationTime for a newly created attribute group.
        lastUpdateTime: number
        -- Key-value pairs associated with the attribute group.
        tags: {string : string}
        -- The service principal that created the attribute group.
        createdBy: string
    end

    record GetConfigurationInput
    end

    record GetConfigurationOutput
        -- Retrieves TagKey configuration
        -- from an account.
        configuration: AppRegistryConfiguration
    end

    record ListApplicationsInput
        -- The token to use to get the next page of results after a previous API call.
        nextToken: string
        -- The upper bound of the number of results to return (cannot exceed 25). If this parameter is omitted, it defaults to 25. This value is optional.
        maxResults: number
    end

    record ListApplicationsOutput
        -- This list of applications.
        applications: {ApplicationSummary}
        -- The token to use to get the next page of results after a previous API call.
        nextToken: string
    end

    record ListAssociatedAttributeGroupsInput
        -- The name or ID of the application.
        application: string
        -- The token to use to get the next page of results after a previous API call.
        nextToken: string
        -- The upper bound of the number of results to return (cannot exceed 25). If this parameter is omitted, it defaults to 25. This value is optional.
        maxResults: number
    end

    record ListAssociatedAttributeGroupsOutput
        -- A list of attribute group IDs.
        attributeGroups: {string}
        -- The token to use to get the next page of results after a previous API call.
        nextToken: string
    end

    record ListAssociatedResourcesInput
        -- The name, ID, or ARN
        -- of the application.
        application: string
        -- The token to use to get the next page of results after a previous API call.
        nextToken: string
        -- The upper bound of the number of results to return (cannot exceed 25). If this parameter is omitted, it defaults to 25. This value is optional.
        maxResults: number
    end

    -- The details
    -- related
    -- to the
    -- resource.
    record ResourceDetails
        -- The value of the tag.
        tagValue: string
    end

    -- The information about the resource.
    record ResourceInfo
        -- The name of the resource.
        name: string
        -- The Amazon resource name (ARN) that specifies the resource across services.
        arn: string
        -- Provides information
        -- about the Service Catalog App Registry resource type.
        resourceType: string
        -- The details related
        -- to
        -- the resource.
        resourceDetails: ResourceDetails
        -- Determines whether an application tag is applied or skipped.
        options: {string}
    end

    record ListAssociatedResourcesOutput
        -- Information about the resources.
        resources: {ResourceInfo}
        -- The token to use to get the next page of results after a previous API call.
        nextToken: string
    end

    record ListAttributeGroupsInput
        -- The token to use to get the next page of results after a previous API call.
        nextToken: string
        -- The upper bound of the number of results to return (cannot exceed 25). If this parameter is omitted, it defaults to 25. This value is optional.
        maxResults: number
    end

    record ListAttributeGroupsOutput
        -- This list of attribute groups.
        attributeGroups: {AttributeGroupSummary}
        -- The token to use to get the next page of results after a previous API call.
        nextToken: string
    end

    record ListAttributeGroupsForApplicationInput
        -- The name or ID of the application.
        application: string
        -- This token retrieves the next page of results after a previous API call.
        nextToken: string
        -- The upper bound of the number of results to return. The value cannot exceed 25. If you omit this parameter, it defaults to 25. This value is optional.
        maxResults: number
    end

    record ListAttributeGroupsForApplicationOutput
        -- The details related to a specific attribute group.
        attributeGroupsDetails: {AttributeGroupDetails}
        -- The token to use to get the next page of results after a previous API call.
        nextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon resource name (ARN) that specifies the resource.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- The tags on the resource.
        tags: {string : string}
    end

    record PutConfigurationInput
        -- Associates a TagKey configuration
        -- to an account.
        configuration: AppRegistryConfiguration
    end

    record PutConfigurationOutput
    end

    record SyncResourceInput
        -- The type of resource of which the application will be associated.
        resourceType: string
        -- An entity you can work with and specify with a name or ID. Examples include an Amazon EC2 instance, an Amazon Web Services CloudFormation stack, or an Amazon S3 bucket.
        resource: string
    end

    enum SyncAction
        "START_SYNC"
        "NO_ACTION"
    end

    record SyncResourceOutput
        -- The Amazon resource name (ARN) that specifies the application.
        applicationArn: string
        -- The Amazon resource name (ARN) that specifies the resource.
        resourceArn: string
        -- The results of the output if an application is associated with an ARN value, which could be syncStarted or None.
        actionTaken: string
    end

    record TagResourceInput
        -- The Amazon resource name (ARN) that specifies the resource.
        resourceArn: string
        -- The new or modified tags for the resource.
        tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon resource name (ARN) that specifies the resource.
        resourceArn: string
        -- A list of the tag keys to remove from the specified resource.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateApplicationInput
        -- The name, ID, or ARN
        -- of the application
        -- that will be updated.
        application: string
        -- Deprecated: The new name of the application. The name must be unique in the region in which you are
        -- updating the application. Please do not use this field as we have stopped supporting name updates.
        name: string
        -- The new description of the application.
        description: string
    end

    record UpdateApplicationOutput
        -- The updated information of the application.
        application: Application
    end

    record UpdateAttributeGroupInput
        -- The name, ID, or ARN
        -- of the attribute group
        -- that holds the attributes
        -- to describe the application.
        attributeGroup: string
        -- Deprecated: The new name of the attribute group. The name must be unique in the region in which you are
        -- updating the attribute group. Please do not use this field as we have stopped supporting name updates.
        name: string
        -- The description of the attribute group that the user provides.
        description: string
        -- A JSON string in the form of nested key-value pairs that represent the attributes in the group and describes an application and its components.
        attributes: string
    end

    record UpdateAttributeGroupOutput
        -- The updated information of the attribute group.
        attributeGroup: AttributeGroup
    end

end

return M
