-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- Preferences related to AWS Chatbot usage in the calling AWS account.
    record AccountPreferences
        -- Enables use of a user role requirement in your chat configuration.
        UserAuthorizationRequired: boolean
        -- Turns on training data collection.
        -- This helps improve the AWS Chatbot experience by allowing AWS Chatbot to store and use your customer information, such as AWS Chatbot configurations, notifications, user inputs, AWS Chatbot generated responses, and interaction data. This data helps us to continuously improve and develop Artificial Intelligence (AI) technologies. Your data is not shared with any third parties and is protected using sophisticated controls to prevent unauthorized access and misuse. AWS Chatbot does not store or use interactions in chat channels with Amazon Q for training AI technologies for AWS Chatbot.
        TrainingDataCollectionEnabled: boolean
    end

    record AssociateToConfigurationInput
        -- The resource Amazon Resource Name (ARN) to link.
        Resource: string
        -- The channel configuration to associate with the resource.
        ChatConfiguration: string
    end

    record AssociateToConfigurationOutput
    end

    -- Unexpected error during processing of request.
    record InternalServiceError
        Message: string
    end

    -- Your request input doesn't meet the constraints required by AWS Chatbot.
    record InvalidRequestException
        message: string
    end

    -- The request was rejected because it doesn't have valid credentials for the target resource.
    record UnauthorizedException
        message: string
    end

    -- A listing of an association with a channel configuration.
    record AssociationListing
        -- The Amazon Resource Name (ARN) of the resource (for example, a custom action).
        Resource: string
    end

    -- A key-value pair. A tag consists of a tag key and a tag value. Tag keys and tag values are both required, but tag values can be empty (null) strings.
    -- Do not include confidential or sensitive information in this field.
    -- For more information, see User-Defined Tag Restrictions in the AWS Billing and Cost Management User Guide.
    record Tag
        -- The key of the tag.
        TagKey: string
        -- The value of the tag.
        TagValue: string
    end

    -- An AWS Chatbot configuration for Amazon Chime.
    record ChimeWebhookConfiguration
        -- A description of the webhook. We recommend using the convention RoomName/WebhookName.
        -- For more information, see Tutorial: Get started with Amazon Chime in the AWS Chatbot Administrator Guide.
        WebhookDescription: string
        -- The Amazon Resource Name (ARN) of the ChimeWebhookConfiguration.
        ChatConfigurationArn: string
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- The Amazon Resource Names (ARNs) of the SNS topics that deliver notifications to AWS Chatbot.
        SnsTopicArns: {string}
        -- The name of the configuration.
        ConfigurationName: string
        -- Logging levels include ERROR, INFO, or NONE.
        LoggingLevel: string
        -- A map of tags assigned to a resource. A tag is a string-to-string map of key-value pairs.
        Tags: {Tag}
        -- Either ENABLED or DISABLED. The resource returns DISABLED if the organization's AWS Chatbot policy has explicitly denied that configuration.
        -- For example, if Amazon Chime is disabled.
        State: string
        -- Provided if State is DISABLED. Provides context as to why the resource is disabled.
        StateReason: string
    end

    -- A Microsoft Teams team that is authorized with AWS Chatbot.
    record ConfiguredTeam
        -- The ID of the Microsoft Teams tenant.
        TenantId: string
        -- The ID of the Microsoft Teams authorized with AWS Chatbot.
        -- To get the team ID, you must perform the initial authorization flow with Microsoft Teams in the AWS Chatbot console. Then you can copy and paste the team ID from the console. For more information, see Step 1: Configure a Microsoft Teams client in the AWS Chatbot Administrator Guide.
        TeamId: string
        -- The name of the Microsoft Teams Team.
        TeamName: string
        -- Either ENABLED or DISABLED. The resource returns DISABLED if the organization's AWS Chatbot policy has explicitly denied that configuration.
        -- For example, if Amazon Chime is disabled.
        State: string
        -- Provided if State is DISABLED. Provides context as to why the resource is disabled.
        StateReason: string
    end

    -- There was an issue processing your request.
    record ConflictException
        message: string
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record CreateChimeWebhookConfigurationException
        Message: string
    end

    record CreateChimeWebhookConfigurationInput
        -- A description of the webhook. We recommend using the convention RoomName/WebhookName.
        -- For more information, see Tutorial: Get started with Amazon Chime in the AWS Chatbot Administrator Guide.
        WebhookDescription: string
        -- The URL for the Amazon Chime webhook.
        WebhookUrl: string
        -- The Amazon Resource Names (ARNs) of the SNS topics that deliver notifications to AWS Chatbot.
        SnsTopicArns: {string}
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- The name of the configuration.
        ConfigurationName: string
        -- Logging levels include ERROR, INFO, or NONE.
        LoggingLevel: string
        -- A map of tags assigned to a resource. A tag is a string-to-string map of key-value pairs.
        Tags: {Tag}
    end

    record CreateChimeWebhookConfigurationOutput
        -- An Amazon Chime webhook configuration.
        WebhookConfiguration: ChimeWebhookConfiguration
    end

    -- Your request input doesn't meet the constraints required by AWS Chatbot.
    record InvalidParameterException
        message: string
    end

    -- You have exceeded a service limit for AWS Chatbot.
    record LimitExceededException
        message: string
    end

    enum CustomActionAttachmentCriteriaOperator
        "HAS_VALUE"
        "EQUALS"
    end

    -- A criteria for when a button should be shown based on values in the notification
    record CustomActionAttachmentCriteria
        -- The operation to perform on the named variable.
        Operator: string
        -- The name of the variable to operate on.
        VariableName: string
        -- A value that is compared with the actual value of the variable based on the behavior of the operator.
        Value: string
    end

    -- Defines when a custom action button should be attached to a notification.
    record CustomActionAttachment
        -- The type of notification that the custom action should be attached to.
        NotificationType: string
        -- The text of the button that appears on the notification.
        ButtonText: string
        -- The criteria for when a button should be shown based on values in the notification.
        Criteria: {CustomActionAttachmentCriteria}
        -- The variables to extract from the notification.
        Variables: {string : string}
    end

    -- The definition of the command to run when invoked as an alias or as an action button.
    record CustomActionDefinition
        -- The command string to run which may include variables by prefixing with a dollar sign ($).
        CommandText: string
    end

    record CreateCustomActionInput
        -- The definition of the command to run when invoked as an alias or as an action button.
        Definition: CustomActionDefinition
        -- The name used to invoke this action in a chat channel. For example, @aws run my-alias.
        AliasName: string
        -- Defines when this custom action button should be attached to a notification.
        Attachments: {CustomActionAttachment}
        -- A map of tags assigned to a resource. A tag is a string-to-string map of key-value pairs.
        Tags: {Tag}
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, subsequent retries with the same client token returns the result from the original successful request.
        -- If you do not specify a client token, one is automatically generated by the SDK.
        ClientToken: string
        -- The name of the custom action. This name is included in the Amazon Resource Name (ARN).
        ActionName: string
    end

    record CreateCustomActionOutput
        -- The fully defined ARN of the custom action.
        CustomActionArn: string
    end

    record CreateMicrosoftTeamsChannelConfigurationInput
        -- The ID of the Microsoft Teams channel.
        ChannelId: string
        -- The name of the Microsoft Teams channel.
        ChannelName: string
        -- The ID of the Microsoft Teams authorized with AWS Chatbot.
        -- To get the team ID, you must perform the initial authorization flow with Microsoft Teams in the AWS Chatbot console. Then you can copy and paste the team ID from the console. For more information, see Step 1: Configure a Microsoft Teams client in the AWS Chatbot Administrator Guide.
        TeamId: string
        -- The name of the Microsoft Teams Team.
        TeamName: string
        -- The ID of the Microsoft Teams tenant.
        TenantId: string
        -- The Amazon Resource Names (ARNs) of the SNS topics that deliver notifications to AWS Chatbot.
        SnsTopicArns: {string}
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- The name of the configuration.
        ConfigurationName: string
        -- Logging levels include ERROR, INFO, or NONE.
        LoggingLevel: string
        -- The list of IAM policy ARNs that are applied as channel guardrails. The AWS managed AdministratorAccess policy is applied by default if this is not set.
        GuardrailPolicyArns: {string}
        -- Enables use of a user role requirement in your chat configuration.
        UserAuthorizationRequired: boolean
        -- A map of tags assigned to a resource. A tag is a string-to-string map of key-value pairs.
        Tags: {Tag}
    end

    -- An AWS Chatbot configuration for Microsoft Teams.
    record TeamsChannelConfiguration
        -- The ID of the Microsoft Teams channel.
        ChannelId: string
        -- The name of the Microsoft Teams channel.
        ChannelName: string
        -- The ID of the Microsoft Teams authorized with AWS Chatbot.
        -- To get the team ID, you must perform the initial authorization flow with Microsoft Teams in the AWS Chatbot console. Then you can copy and paste the team ID from the console. For more information, see Step 1: Configure a Microsoft Teams client in the AWS Chatbot Administrator Guide.
        TeamId: string
        -- The name of the Microsoft Teams Team.
        TeamName: string
        -- The ID of the Microsoft Teams tenant.
        TenantId: string
        -- The Amazon Resource Name (ARN) of the MicrosoftTeamsChannelConfiguration associated with the user identity to delete.
        ChatConfigurationArn: string
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- The Amazon Resource Names (ARNs) of the SNS topics that deliver notifications to AWS Chatbot.
        SnsTopicArns: {string}
        -- The name of the configuration.
        ConfigurationName: string
        -- Logging levels include ERROR, INFO, or NONE.
        LoggingLevel: string
        -- The list of IAM policy ARNs that are applied as channel guardrails. The AWS managed AdministratorAccess policy is applied by default if this is not set.
        GuardrailPolicyArns: {string}
        -- Enables use of a user role requirement in your chat configuration.
        UserAuthorizationRequired: boolean
        -- A map of tags assigned to a resource. A tag is a string-to-string map of key-value pairs.
        Tags: {Tag}
        -- Either ENABLED or DISABLED. The resource returns DISABLED if the organization's AWS Chatbot policy has explicitly denied that configuration.
        -- For example, if Amazon Chime is disabled.
        State: string
        -- Provided if State is DISABLED. Provides context as to why the resource is disabled.
        StateReason: string
    end

    record CreateMicrosoftTeamsChannelConfigurationOutput
        -- The configuration for a Microsoft Teams channel configured with AWS Chatbot.
        ChannelConfiguration: TeamsChannelConfiguration
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record CreateTeamsChannelConfigurationException
        Message: string
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record CreateSlackChannelConfigurationException
        Message: string
    end

    record CreateSlackChannelConfigurationInput
        -- The ID of the Slack workspace authorized with AWS Chatbot.
        SlackTeamId: string
        -- The ID of the Slack channel.
        -- To get this ID, open Slack, right click on the channel name in the left pane, then choose Copy Link. The channel ID is the 9-character string at the end of the URL. For example, ABCBBLZZZ.
        SlackChannelId: string
        -- The name of the Slack channel.
        SlackChannelName: string
        -- The Amazon Resource Names (ARNs) of the SNS topics that deliver notifications to AWS Chatbot.
        SnsTopicArns: {string}
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- The name of the configuration.
        ConfigurationName: string
        -- Logging levels include ERROR, INFO, or NONE.
        LoggingLevel: string
        -- The list of IAM policy ARNs that are applied as channel guardrails. The AWS managed AdministratorAccess policy is applied by default if this is not set.
        GuardrailPolicyArns: {string}
        -- Enables use of a user role requirement in your chat configuration.
        UserAuthorizationRequired: boolean
        -- A map of tags assigned to a resource. A tag is a string-to-string map of key-value pairs.
        Tags: {Tag}
    end

    -- An AWS Chatbot configuration for Slack.
    record SlackChannelConfiguration
        -- Name of the Slack workspace.
        SlackTeamName: string
        -- The ID of the Slack workspace authorized with Amazon Chime.
        SlackTeamId: string
        -- The ID of the Slack channel.
        -- To get this ID, open Slack, right click on the channel name in the left pane, then choose Copy Link. The channel ID is the 9-character string at the end of the URL. For example, ABCBBLZZZ.
        SlackChannelId: string
        -- The name of the Slack channel.
        SlackChannelName: string
        -- The Amazon Resource Name (ARN) of the SlackChannelConfiguration.
        ChatConfigurationArn: string
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- The ARNs of the SNS topics that deliver notifications to AWS Chatbot.
        SnsTopicArns: {string}
        -- The name of the configuration.
        ConfigurationName: string
        -- Logging levels include ERROR, INFO, or NONE.
        LoggingLevel: string
        -- The list of IAM policy ARNs that are applied as channel guardrails. The AWS managed AdministratorAccess policy is applied by default if this is not set.
        GuardrailPolicyArns: {string}
        -- Enables use of a user role requirement in your chat configuration.
        UserAuthorizationRequired: boolean
        -- A map of tags assigned to a resource. A tag is a string-to-string map of key-value pairs.
        Tags: {Tag}
        -- Either ENABLED or DISABLED. The resource returns DISABLED if the organization's AWS Chatbot policy has explicitly denied that configuration.
        -- For example, if Amazon Chime is disabled.
        State: string
        -- Provided if State is DISABLED. Provides context as to why the resource is disabled.
        StateReason: string
    end

    record CreateSlackChannelConfigurationOutput
        -- The configuration for a Slack channel configured with AWS Chatbot.
        ChannelConfiguration: SlackChannelConfiguration
    end

    -- Represents a parameterized command that can be invoked as an alias or as a notification button in the chat client.
    record CustomAction
        -- The fully defined Amazon Resource Name (ARN) of the custom action.
        CustomActionArn: string
        -- The definition of the command to run when invoked an alias or as an action button.
        Definition: CustomActionDefinition
        -- The name used to invoke this action in the chat channel. For example, @aws run my-alias.
        AliasName: string
        -- Defines when this custom action button should be attached to a notification.
        Attachments: {CustomActionAttachment}
        -- The name of the custom action that is included in the ARN.
        ActionName: string
    end

    record DeleteCustomActionInput
        -- The fully defined ARN of the custom action.
        CustomActionArn: string
    end

    record DeleteCustomActionOutput
    end

    -- We were unable to find the resource for your request
    record ResourceNotFoundException
        Message: string
    end

    record GetCustomActionInput
        -- Returns the fully defined Amazon Resource Name (ARN) of the custom action.
        CustomActionArn: string
    end

    record GetCustomActionOutput
        -- Returns the custom action.
        CustomAction: CustomAction
    end

    record ListCustomActionsInput
        -- The maximum number of results to include in the response. If more results exist than the specified MaxResults value, a token is included in the response so that the remaining results can be retrieved.
        MaxResults: number
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
    end

    record ListCustomActionsOutput
        -- A list of custom actions.
        CustomActions: {string}
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
    end

    record UpdateCustomActionInput
        -- The fully defined Amazon Resource Name (ARN) of the custom action.
        CustomActionArn: string
        -- The definition of the command to run when invoked as an alias or as an action button.
        Definition: CustomActionDefinition
        -- The name used to invoke this action in the chat channel. For example, @aws run my-alias.
        AliasName: string
        -- Defines when this custom action button should be attached to a notification.
        Attachments: {CustomActionAttachment}
    end

    record UpdateCustomActionOutput
        -- The fully defined ARN of the custom action.
        CustomActionArn: string
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record DeleteChimeWebhookConfigurationException
        Message: string
    end

    record DeleteChimeWebhookConfigurationInput
        -- The Amazon Resource Name (ARN) of the ChimeWebhookConfiguration to delete.
        ChatConfigurationArn: string
    end

    record DeleteChimeWebhookConfigurationOutput
    end

    record DeleteMicrosoftTeamsChannelConfigurationInput
        -- The Amazon Resource Name (ARN) of the MicrosoftTeamsChannelConfiguration associated with the user identity to delete.
        ChatConfigurationArn: string
    end

    record DeleteMicrosoftTeamsChannelConfigurationOutput
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record DeleteTeamsChannelConfigurationException
        Message: string
    end

    record DeleteMicrosoftTeamsConfiguredTeamInput
        -- The ID of the Microsoft Teams team authorized with AWS Chatbot.
        -- To get the team ID, you must perform the initial authorization flow with Microsoft Teams in the AWS Chatbot console. Then you can copy and paste the team ID from the console. For more information, see Step 1: Configure a Microsoft Teams client in the AWS Chatbot Administrator Guide.
        TeamId: string
    end

    record DeleteMicrosoftTeamsConfiguredTeamOutput
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record DeleteTeamsConfiguredTeamException
        Message: string
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record DeleteMicrosoftTeamsUserIdentityException
        Message: string
    end

    record DeleteMicrosoftTeamsUserIdentityInput
        -- The ARN of the MicrosoftTeamsChannelConfiguration associated with the user identity to delete.
        ChatConfigurationArn: string
        -- The Microsoft Teams user ID.
        UserId: string
    end

    record DeleteMicrosoftTeamsUserIdentityOutput
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record DeleteSlackChannelConfigurationException
        Message: string
    end

    record DeleteSlackChannelConfigurationInput
        -- The Amazon Resource Name (ARN) of the SlackChannelConfiguration to delete.
        ChatConfigurationArn: string
    end

    record DeleteSlackChannelConfigurationOutput
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record DeleteSlackUserIdentityException
        Message: string
    end

    record DeleteSlackUserIdentityInput
        -- The ARN of the SlackChannelConfiguration associated with the user identity to delete.
        ChatConfigurationArn: string
        -- The ID of the Slack workspace authorized with AWS Chatbot.
        SlackTeamId: string
        -- The ID of the user in Slack
        SlackUserId: string
    end

    record DeleteSlackUserIdentityOutput
    end

    -- There was an issue deleting your Slack workspace.
    record DeleteSlackWorkspaceAuthorizationFault
        Message: string
    end

    record DeleteSlackWorkspaceAuthorizationInput
        -- The ID of the Slack workspace authorized with AWS Chatbot.
        SlackTeamId: string
    end

    record DeleteSlackWorkspaceAuthorizationOutput
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record DescribeChimeWebhookConfigurationsException
        Message: string
    end

    record DescribeChimeWebhookConfigurationsInput
        -- The maximum number of results to include in the response. If more results exist than the specified MaxResults value, a token is included in the response so that the remaining results can be retrieved.
        MaxResults: number
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
        -- An optional Amazon Resource Name (ARN) of a ChimeWebhookConfiguration to describe.
        ChatConfigurationArn: string
    end

    record DescribeChimeWebhookConfigurationsOutput
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
        -- A list of Amazon Chime webhooks associated with the account.
        WebhookConfigurations: {ChimeWebhookConfiguration}
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record DescribeSlackChannelConfigurationsException
        Message: string
    end

    record DescribeSlackChannelConfigurationsInput
        -- The maximum number of results to include in the response. If more results exist than the specified MaxResults value, a token is included in the response so that the remaining results can be retrieved.
        MaxResults: number
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
        -- An optional Amazon Resource Name (ARN) of a SlackChannelConfiguration to describe.
        ChatConfigurationArn: string
    end

    record DescribeSlackChannelConfigurationsOutput
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
        -- A list of Slack channel configurations.
        SlackChannelConfigurations: {SlackChannelConfiguration}
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record DescribeSlackUserIdentitiesException
        Message: string
    end

    record DescribeSlackUserIdentitiesInput
        -- The Amazon Resource Name (ARN) of the SlackChannelConfiguration associated with the user identities to describe.
        ChatConfigurationArn: string
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
        -- The maximum number of results to include in the response. If more results exist than the specified MaxResults value, a token is included in the response so that the remaining results can be retrieved.
        MaxResults: number
    end

    -- Identifes a user level permission for a channel configuration.
    record SlackUserIdentity
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- The Amazon Resource Name (ARN) of the SlackChannelConfiguration associated with the user identity to delete.
        ChatConfigurationArn: string
        -- The ID of the Slack workspace authorized with AWS Chatbot.
        SlackTeamId: string
        -- The ID of the user in Slack
        SlackUserId: string
        -- The AWS user identity ARN used to associate a Slack user ID with an IAM Role.
        AwsUserIdentity: string
    end

    record DescribeSlackUserIdentitiesOutput
        -- A list of Slack User Identities.
        SlackUserIdentities: {SlackUserIdentity}
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record DescribeSlackWorkspacesException
        Message: string
    end

    record DescribeSlackWorkspacesInput
        -- The maximum number of results to include in the response. If more results exist than the specified MaxResults value, a token is included in the response so that the remaining results can be retrieved.
        MaxResults: number
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
    end

    -- A Slack workspace.
    record SlackWorkspace
        -- The ID of the Slack workspace authorized with AWS Chatbot.
        SlackTeamId: string
        -- The name of the Slack workspace.
        SlackTeamName: string
        -- Either ENABLED or DISABLED. The resource returns DISABLED if the organization's AWS Chatbot policy has explicitly denied that configuration.
        -- For example, if Amazon Chime is disabled.
        State: string
        -- Provided if State is DISABLED. Provides context as to why the resource is disabled.
        StateReason: string
    end

    record DescribeSlackWorkspacesOutput
        -- A list of Slack workspaces registered with AWS Chatbot.
        SlackWorkspaces: {SlackWorkspace}
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
    end

    record DisassociateFromConfigurationInput
        -- The resource (for example, a custom action) Amazon Resource Name (ARN) to unlink.
        Resource: string
        -- The channel configuration the resource is being disassociated from.
        ChatConfiguration: string
    end

    record DisassociateFromConfigurationOutput
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record GetAccountPreferencesException
        Message: string
    end

    record GetAccountPreferencesInput
    end

    record GetAccountPreferencesOutput
        -- The preferences related to AWS Chatbot usage in the calling AWS account.
        AccountPreferences: AccountPreferences
    end

    record GetMicrosoftTeamsChannelConfigurationInput
        -- The Amazon Resource Name (ARN) of the MicrosoftTeamsChannelConfiguration to retrieve.
        ChatConfigurationArn: string
    end

    record GetMicrosoftTeamsChannelConfigurationOutput
        -- The configuration for a Microsoft Teams channel configured with AWS Chatbot.
        ChannelConfiguration: TeamsChannelConfiguration
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record GetTeamsChannelConfigurationException
        Message: string
    end

    record ListAssociationsInput
        -- The channel configuration to list associations for.
        ChatConfiguration: string
        -- The maximum number of results to include in the response. If more results exist than the specified MaxResults value, a token is included in the response so that the remaining results can be retrieved.
        MaxResults: number
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
    end

    record ListAssociationsOutput
        -- The resources associated with this channel configuration.
        Associations: {AssociationListing}
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
    end

    record ListMicrosoftTeamsChannelConfigurationsInput
        -- The maximum number of results to include in the response. If more results exist than the specified MaxResults value, a token is included in the response so that the remaining results can be retrieved.
        MaxResults: number
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
        -- The ID of the Microsoft Teams authorized with AWS Chatbot.
        -- To get the team ID, you must perform the initial authorization flow with Microsoft Teams in the AWS Chatbot console. Then you can copy and paste the team ID from the console. For more information, see Step 1: Configure a Microsoft Teams client in the AWS Chatbot Administrator Guide.
        TeamId: string
    end

    record ListMicrosoftTeamsChannelConfigurationsOutput
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
        -- A list of AWS Chatbot channel configurations for Microsoft Teams.
        TeamChannelConfigurations: {TeamsChannelConfiguration}
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record ListTeamsChannelConfigurationsException
        Message: string
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record ListMicrosoftTeamsConfiguredTeamsException
        Message: string
    end

    record ListMicrosoftTeamsConfiguredTeamsInput
        -- The maximum number of results to include in the response. If more results exist than the specified MaxResults value, a token is included in the response so that the remaining results can be retrieved.
        MaxResults: number
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
    end

    record ListMicrosoftTeamsConfiguredTeamsOutput
        -- A list of teams in Microsoft Teams that are configured with AWS Chatbot.
        ConfiguredTeams: {ConfiguredTeam}
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record ListMicrosoftTeamsUserIdentitiesException
        Message: string
    end

    record ListMicrosoftTeamsUserIdentitiesInput
        -- The Amazon Resource Name (ARN) of the MicrosoftTeamsChannelConfiguration associated with the user identities to list.
        ChatConfigurationArn: string
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
        -- The maximum number of results to include in the response. If more results exist than the specified MaxResults value, a token is included in the response so that the remaining results can be retrieved.
        MaxResults: number
    end

    -- Identifes a user level permission for a channel configuration.
    record TeamsUserIdentity
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- The Amazon Resource Name (ARN) of the MicrosoftTeamsChannelConfiguration associated with the user identity to delete.
        ChatConfigurationArn: string
        -- The ID of the Microsoft Teams authorized with AWS Chatbot.
        -- To get the team ID, you must perform the initial authorization flow with Microsoft Teams in the AWS Chatbot console. Then you can copy and paste the team ID from the console. For more information, see Step 1: Configure a Microsoft Teams client in the AWS Chatbot Administrator Guide.
        TeamId: string
        -- The Microsoft Teams user ID.
        UserId: string
        -- The AWS user identity ARN used to associate a Microsoft Teams user Identity with an IAM Role.
        AwsUserIdentity: string
        -- The ID of the Microsoft Teams channel.
        TeamsChannelId: string
        -- The ID of the Microsoft Teams tenant.
        TeamsTenantId: string
    end

    record ListMicrosoftTeamsUserIdentitiesOutput
        -- User level permissions associated to a channel configuration.
        TeamsUserIdentities: {TeamsUserIdentity}
        -- An optional token returned from a prior request. Use this token for pagination of results from this action. If this parameter is specified, the response includes only results beyond the token, up to the value specified by MaxResults.
        NextToken: string
    end

    record ListTagsForResourceInput
        -- The ARN of the resource to list tags for.
        ResourceARN: string
    end

    record ListTagsForResourceOutput
        -- Key-value pairs that are assigned to a resource, usually for the purpose of grouping and searching for items. Tags are metadata that you define.
        Tags: {Tag}
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record ServiceUnavailableException
        message: string
    end

    record TagResourceInput
        -- The ARN of the configuration.
        ResourceARN: string
        -- A list of tags to apply to the configuration.
        Tags: {Tag}
    end

    record TagResourceOutput
    end

    -- The supplied list of tags contains too many tags.
    record TooManyTagsException
        message: string
    end

    record UntagResourceInput
        -- The value of the resource that will have the tag removed. An Amazon Resource Name (ARN) is an identifier for a specific AWS resource, such as a server, user, or role.
        ResourceARN: string
        -- TagKeys are key-value pairs assigned to ARNs that can be used to group and search for resources by type. This metadata can be attached to resources for any purpose.
        TagKeys: {string}
    end

    record UntagResourceOutput
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record UpdateAccountPreferencesException
        Message: string
    end

    record UpdateAccountPreferencesInput
        -- Enables use of a user role requirement in your chat configuration.
        UserAuthorizationRequired: boolean
        -- Turns on training data collection.
        -- This helps improve the AWS Chatbot experience by allowing AWS Chatbot to store and use your customer information, such as AWS Chatbot configurations, notifications, user inputs, AWS Chatbot generated responses, and interaction data. This data helps us to continuously improve and develop Artificial Intelligence (AI) technologies. Your data is not shared with any third parties and is protected using sophisticated controls to prevent unauthorized access and misuse. AWS Chatbot does not store or use interactions in chat channels with Amazon Q for training AI technologies for AWS Chatbot.
        TrainingDataCollectionEnabled: boolean
    end

    record UpdateAccountPreferencesOutput
        -- Preferences related to AWS Chatbot usage in the calling AWS account.
        AccountPreferences: AccountPreferences
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record UpdateChimeWebhookConfigurationException
        Message: string
    end

    record UpdateChimeWebhookConfigurationInput
        -- The Amazon Resource Name (ARN) of the ChimeWebhookConfiguration to update.
        ChatConfigurationArn: string
        -- A description of the webhook. We recommend using the convention RoomName/WebhookName.
        -- For more information, see Tutorial: Get started with Amazon Chime in the AWS Chatbot Administrator Guide.
        WebhookDescription: string
        -- The URL for the Amazon Chime webhook.
        WebhookUrl: string
        -- The ARNs of the SNS topics that deliver notifications to AWS Chatbot.
        SnsTopicArns: {string}
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- Logging levels include ERROR, INFO, or NONE.
        LoggingLevel: string
    end

    record UpdateChimeWebhookConfigurationOutput
        -- A Amazon Chime webhook configuration.
        WebhookConfiguration: ChimeWebhookConfiguration
    end

    record UpdateMicrosoftTeamsChannelConfigurationInput
        -- The Amazon Resource Name (ARN) of the TeamsChannelConfiguration to update.
        ChatConfigurationArn: string
        -- The ID of the Microsoft Teams channel.
        ChannelId: string
        -- The name of the Microsoft Teams channel.
        ChannelName: string
        -- The Amazon Resource Names (ARNs) of the SNS topics that deliver notifications to AWS Chatbot.
        SnsTopicArns: {string}
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- Logging levels include ERROR, INFO, or NONE.
        LoggingLevel: string
        -- The list of IAM policy ARNs that are applied as channel guardrails. The AWS managed AdministratorAccess policy is applied by default if this is not set.
        GuardrailPolicyArns: {string}
        -- Enables use of a user role requirement in your chat configuration.
        UserAuthorizationRequired: boolean
    end

    record UpdateMicrosoftTeamsChannelConfigurationOutput
        -- The configuration for a Microsoft Teams channel configured with AWS Chatbot.
        ChannelConfiguration: TeamsChannelConfiguration
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record UpdateTeamsChannelConfigurationException
        Message: string
    end

    -- We can’t process your request right now because of a server issue. Try again later.
    record UpdateSlackChannelConfigurationException
        Message: string
    end

    record UpdateSlackChannelConfigurationInput
        -- The Amazon Resource Name (ARN) of the SlackChannelConfiguration to update.
        ChatConfigurationArn: string
        -- The ID of the Slack channel.
        -- To get this ID, open Slack, right click on the channel name in the left pane, then choose Copy Link. The channel ID is the 9-character string at the end of the URL. For example, ABCBBLZZZ.
        SlackChannelId: string
        -- The name of the Slack channel.
        SlackChannelName: string
        -- The Amazon Resource Names (ARNs) of the SNS topics that deliver notifications to AWS Chatbot.
        SnsTopicArns: {string}
        -- A user-defined role that AWS Chatbot assumes. This is not the service-linked role.
        -- For more information, see IAM policies for AWS Chatbot in the AWS Chatbot Administrator Guide.
        IamRoleArn: string
        -- Logging levels include ERROR, INFO, or NONE.
        LoggingLevel: string
        -- The list of IAM policy ARNs that are applied as channel guardrails. The AWS managed AdministratorAccess policy is applied by default if this is not set.
        GuardrailPolicyArns: {string}
        -- Enables use of a user role requirement in your chat configuration.
        UserAuthorizationRequired: boolean
    end

    record UpdateSlackChannelConfigurationOutput
        -- The configuration for a Slack channel configured with AWS Chatbot.
        ChannelConfiguration: SlackChannelConfiguration
    end

end

return M
