-- Code generated by lua-client-codegen. DO NOT EDIT.

local waiter = require("smithy.waiter")

local M = {}

--- Wait until FailureModeAssessmentSuccess.
function M.wait_until_failure_mode_assessment_success(client, input, options)
    return waiter.wait(client, "listFailureModeAssessments", input, {
        min_delay = 30,
        max_delay = 120,
        acceptors = {
            {
                state = "retry",
                matcher = {
                    output = {
                        path = "length(assessmentSummaries[]) == `0`",
                        expected = "true",
                        comparator = "booleanEquals",
                    },
                },
            },
            {
                state = "success",
                matcher = {
                    output = {
                        path = "assessmentSummaries[].assessmentStatus",
                        expected = "SUCCESS",
                        comparator = "allStringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "assessmentSummaries[].assessmentStatus",
                        expected = "FAILED",
                        comparator = "anyStringEquals",
                    },
                },
            },
        },
    }, options)
end

--- Wait until ReportSucceeded.
function M.wait_until_report_succeeded(client, input, options)
    return waiter.wait(client, "listReports", input, {
        min_delay = 30,
        max_delay = 120,
        acceptors = {
            {
                state = "retry",
                matcher = {
                    output = {
                        path = "length(reportGenerationResults[]) == `0`",
                        expected = "true",
                        comparator = "booleanEquals",
                    },
                },
            },
            {
                state = "success",
                matcher = {
                    output = {
                        path = "reportGenerationResults[].status",
                        expected = "SUCCEEDED",
                        comparator = "allStringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "reportGenerationResults[].status",
                        expected = "FAILED",
                        comparator = "anyStringEquals",
                    },
                },
            },
        },
    }, options)
end

--- Wait until ServiceAssessmentCompleted.
function M.wait_until_service_assessment_completed(client, input, options)
    return waiter.wait(client, "getService", input, {
        min_delay = 30,
        max_delay = 120,
        acceptors = {
            {
                state = "success",
                matcher = {
                    output = {
                        path = "service.assessmentStatus",
                        expected = "SUCCESS",
                        comparator = "stringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "service.assessmentStatus",
                        expected = "FAILED",
                        comparator = "stringEquals",
                    },
                },
            },
        },
    }, options)
end

--- Wait until ServiceResourceDiscoveryCompleted.
function M.wait_until_service_resource_discovery_completed(client, input, options)
    return waiter.wait(client, "getService", input, {
        min_delay = 30,
        max_delay = 120,
        acceptors = {
            {
                state = "success",
                matcher = {
                    output = {
                        path = "service.resourceDiscovery.status",
                        expected = "SUCCEEDED",
                        comparator = "stringEquals",
                    },
                },
            },
            {
                state = "success",
                matcher = {
                    output = {
                        path = "service.resourceDiscovery.status",
                        expected = "COMPLETED_WITH_FAILURES",
                        comparator = "stringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "service.resourceDiscovery.status",
                        expected = "FAILED",
                        comparator = "stringEquals",
                    },
                },
            },
        },
    }, options)
end

return M
