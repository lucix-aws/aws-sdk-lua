-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    enum AllowMessages
        "ALL"
        "NONE"
    end

    -- The details of an AppInstance, an instance of an Amazon Chime SDK messaging
    -- application.
    record AppInstance
        -- The ARN of the messaging instance.
        AppInstanceArn: string
        -- The name of an AppInstance.
        Name: string
        -- The time at which an AppInstance was created. In epoch milliseconds.
        CreatedTimestamp: number
        -- The time an AppInstance was last updated. In epoch milliseconds.
        LastUpdatedTimestamp: number
        -- The metadata of an AppInstance.
        Metadata: string
    end

    -- The details of a user or bot.
    record Identity
        -- The ARN in an Identity.
        Arn: string
        -- The name in an Identity.
        Name: string
    end

    -- The name and ARN of the admin for the AppInstance.
    record AppInstanceAdmin
        -- The AppInstanceAdmin data.
        Admin: Identity
        -- The ARN of the AppInstance for which the user is an administrator.
        AppInstanceArn: string
        -- The time at which an administrator was created.
        CreatedTimestamp: number
    end

    -- Summary of the details of an AppInstanceAdmin.
    record AppInstanceAdminSummary
        -- The details of the AppInstanceAdmin.
        Admin: Identity
    end

    enum StandardMessages
        "AUTO"
        "ALL"
        "MENTIONS"
        "NONE"
    end

    enum TargetedMessages
        "ALL"
        "NONE"
    end

    -- Specifies the type of message that triggers a bot.
    record InvokedBy
        -- Sets standard messages as the bot trigger. For standard messages:
        -- ALL: The bot processes all standard messages.
        -- AUTO: The bot responds to ALL messages when the channel has one other non-hidden member, and responds to MENTIONS when the
        -- channel has more than one other non-hidden member.
        -- MENTIONS: The bot processes all standard messages that have a message attribute with CHIME.mentions and a
        -- value of the bot ARN.
        -- NONE: The bot processes no standard messages.
        StandardMessages: string
        -- Sets targeted messages as the bot trigger. For targeted messages:
        -- ALL: The bot processes all TargetedMessages sent to it. The bot then responds with a targeted message back to the sender.
        -- NONE: The bot processes no targeted messages.
        TargetedMessages: string
    end

    enum RespondsTo
        "STANDARD_MESSAGES"
    end

    -- The configuration for an Amazon Lex V2 bot.
    record LexConfiguration
        -- Deprecated. Use InvokedBy instead.
        -- Determines whether the Amazon Lex V2 bot responds to all standard messages. Control messages are not supported.
        RespondsTo: string
        -- Specifies the type of message that triggers a bot.
        InvokedBy: InvokedBy
        -- The ARN of the Amazon Lex V2 bot's alias. The ARN uses this format:
        -- arn:aws:lex:REGION:ACCOUNT:bot-alias/MYBOTID/MYBOTALIAS
        LexBotAliasArn: string
        -- Identifies the Amazon Lex V2 bot's language and locale. The string must match one of the
        -- supported locales in Amazon Lex V2. All of the intents, slot types, and slots used in the bot must have the same
        -- locale. For more information, see Supported languages in the Amazon Lex V2 Developer Guide.
        LocaleId: string
        -- The name of the welcome intent configured in the Amazon Lex V2 bot.
        WelcomeIntent: string
    end

    -- A structure that contains configuration data.
    record Configuration
        -- The configuration for an Amazon Lex V2 bot.
        Lex: LexConfiguration
    end

    -- An Amazon Lex V2 chat bot created under an AppInstance.
    record AppInstanceBot
        -- The ARN of the AppInstanceBot.
        AppInstanceBotArn: string
        -- The name of the AppInstanceBot.
        Name: string
        -- The data processing instructions for an AppInstanceBot.
        Configuration: Configuration
        -- The time at which the AppInstanceBot was created.
        CreatedTimestamp: number
        -- The time at which the AppInstanceBot was last updated.
        LastUpdatedTimestamp: number
        -- The metadata for an AppInstanceBot.
        Metadata: string
    end

    -- High-level information about an AppInstanceBot.
    record AppInstanceBotSummary
        -- The ARN of the AppInstanceBot.
        AppInstanceBotArn: string
        -- The name of the AppInstanceBox.
        Name: string
        -- The metadata of the AppInstanceBot.
        Metadata: string
    end

    -- Summary of the data for an AppInstance.
    record AppInstanceSummary
        -- The AppInstance ARN.
        AppInstanceArn: string
        -- The name of the AppInstance.
        Name: string
        -- The metadata of the AppInstance.
        Metadata: string
    end

    -- The details of the retention settings for a channel.
    record ChannelRetentionSettings
        -- The time in days to retain the messages in a channel.
        RetentionDays: number
    end

    -- The details of the data-retention settings for an AppInstance.
    record AppInstanceRetentionSettings
        -- The length of time in days to retain the messages in a channel.
        ChannelRetentionSettings: ChannelRetentionSettings
    end

    enum ExpirationCriterion
        "CREATED_TIMESTAMP"
    end

    -- Determines the interval after which an AppInstanceUser is automatically deleted.
    record ExpirationSettings
        -- The period in days after which an AppInstanceUser will be automatically deleted.
        ExpirationDays: number
        -- Specifies the conditions under which an AppInstanceUser will expire.
        ExpirationCriterion: string
    end

    -- The details of an AppInstanceUser.
    record AppInstanceUser
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The name of the AppInstanceUser.
        Name: string
        -- The metadata of the AppInstanceUser.
        Metadata: string
        -- The time at which the AppInstanceUser was created.
        CreatedTimestamp: number
        -- The time at which the AppInstanceUser was last updated.
        LastUpdatedTimestamp: number
        -- The interval after which an AppInstanceUser is automatically deleted.
        ExpirationSettings: ExpirationSettings
    end

    -- The attributes of an Endpoint.
    record EndpointAttributes
        -- The device token for the GCM, APNS, and APNS_SANDBOX endpoint types.
        DeviceToken: string
        -- The VOIP device token for the APNS and APNS_SANDBOX endpoint types.
        VoipDeviceToken: string
    end

    enum EndpointStatus
        "ACTIVE"
        "INACTIVE"
    end

    enum EndpointStatusReason
        "INVALID_DEVICE_TOKEN"
        "INVALID_PINPOINT_ARN"
    end

    -- A read-only field that represents the state of an AppInstanceUserEndpoint. Supported values:
    -- ACTIVE: The AppInstanceUserEndpoint is active and able to receive messages. When ACTIVE, the EndpointStatusReason remains empty.
    -- INACTIVE: The AppInstanceUserEndpoint is inactive and can't receive
    -- message. When INACTIVE, the corresponding reason will be conveyed through
    -- EndpointStatusReason.
    -- INVALID_DEVICE_TOKEN indicates that an AppInstanceUserEndpoint is INACTIVE due to invalid device token
    -- INVALID_PINPOINT_ARN indicates that an AppInstanceUserEndpoint is INACTIVE due to an invalid pinpoint ARN that was input through the ResourceArn field.
    record EndpointState
        -- Enum that indicates the Status of an AppInstanceUserEndpoint.
        Status: string
        -- The reason for the EndpointStatus.
        StatusReason: string
    end

    enum AppInstanceUserEndpointType
        "APNS"
        "APNS_SANDBOX"
        "GCM"
    end

    -- An endpoint under an Amazon Chime AppInstanceUser that receives messages for a user. For push notifications, the endpoint is a mobile device used to receive mobile push notifications for a user.
    record AppInstanceUserEndpoint
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The unique identifier of the AppInstanceUserEndpoint.
        EndpointId: string
        -- The name of the AppInstanceUserEndpoint.
        Name: string
        -- The type of the AppInstanceUserEndpoint.
        Type: string
        -- The ARN of the resource to which the endpoint belongs.
        ResourceArn: string
        -- The attributes of an Endpoint.
        EndpointAttributes: EndpointAttributes
        -- The time at which an AppInstanceUserEndpoint was created.
        CreatedTimestamp: number
        -- The time at which an AppInstanceUserEndpoint was last updated.
        LastUpdatedTimestamp: number
        -- Boolean that controls whether the AppInstanceUserEndpoint is opted in to receive messages. ALL indicates the endpoint will receive all messages.
        -- NONE indicates the endpoint will receive no messages.
        AllowMessages: string
        -- A read-only field that represents the state of an AppInstanceUserEndpoint. Supported values:
        -- ACTIVE: The AppInstanceUserEndpoint is active and able to receive messages. When ACTIVE, the EndpointStatusReason remains empty.
        -- INACTIVE: The AppInstanceUserEndpoint is inactive and can't receive message. When INACTIVE, the corresponding reason will be
        -- conveyed through EndpointStatusReason.
        -- INVALID_DEVICE_TOKEN indicates that an AppInstanceUserEndpoint is INACTIVE due to invalid device token
        -- INVALID_PINPOINT_ARN indicates that an AppInstanceUserEndpoint is INACTIVE due to an invalid pinpoint ARN that was input
        -- through the ResourceArn field.
        EndpointState: EndpointState
    end

    -- Summary of the details of an AppInstanceUserEndpoint.
    record AppInstanceUserEndpointSummary
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The unique identifier of the AppInstanceUserEndpoint.
        EndpointId: string
        -- The name of the AppInstanceUserEndpoint.
        Name: string
        -- The type of the AppInstanceUserEndpoint.
        Type: string
        -- BBoolean that controls whether the AppInstanceUserEndpoint is opted in to receive messages. ALL indicates the endpoint will receive all messages.
        -- NONE indicates the endpoint will receive no messages.
        AllowMessages: string
        -- A read-only field that represent the state of an AppInstanceUserEndpoint.
        EndpointState: EndpointState
    end

    -- Summary of the details of an AppInstanceUser.
    record AppInstanceUserSummary
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The name of an AppInstanceUser.
        Name: string
        -- The metadata of the AppInstanceUser.
        Metadata: string
    end

    enum ErrorCode
        "BadRequest"
        "Conflict"
        "Forbidden"
        "NotFound"
        "PreconditionFailed"
        "ResourceLimitExceeded"
        "ServiceFailure"
        "AccessDenied"
        "ServiceUnavailable"
        "Throttled"
        "Throttling"
        "Unauthorized"
        "Unprocessable"
        "VoiceConnectorGroupAssociationsExist"
        "PhoneNumberAssociationsExist"
    end

    -- The input parameters don't match the service's restrictions.
    record BadRequestException
        Code: string
        Message: string
    end

    -- The request could not be processed because of conflict in the current state of the
    -- resource.
    record ConflictException
        Code: string
        Message: string
    end

    -- A tag object containing a key-value pair.
    record Tag
        -- The key in a tag.
        Key: string
        -- The value in a tag.
        Value: string
    end

    record CreateAppInstanceInput
        -- The name of the AppInstance.
        Name: string
        -- The metadata of the AppInstance. Limited to a 1KB string in UTF-8.
        Metadata: string
        -- The unique ID of the request. Use different tokens to create different AppInstances.
        ClientRequestToken: string
        -- Tags assigned to the AppInstance.
        Tags: {Tag}
    end

    record CreateAppInstanceOutput
        -- The Amazon Resource Number (ARN) of the AppInstance.
        AppInstanceArn: string
    end

    -- The client is permanently forbidden from making the request.
    record ForbiddenException
        Code: string
        Message: string
    end

    -- The request exceeds the resource limit.
    record ResourceLimitExceededException
        Code: string
        Message: string
    end

    -- The service encountered an unexpected error.
    record ServiceFailureException
        Code: string
        Message: string
    end

    -- The service is currently unavailable.
    record ServiceUnavailableException
        Code: string
        Message: string
    end

    -- The client exceeded its request rate limit.
    record ThrottledClientException
        Code: string
        Message: string
    end

    -- The client is not currently authorized to make the request.
    record UnauthorizedClientException
        Code: string
        Message: string
    end

    record CreateAppInstanceAdminInput
        -- The ARN of the administrator of the current AppInstance.
        AppInstanceAdminArn: string
        -- The ARN of the AppInstance.
        AppInstanceArn: string
    end

    record CreateAppInstanceAdminOutput
        -- The ARN and name of the administrator, the ARN of the AppInstance, and the created and
        -- last-updated timestamps. All timestamps use epoch milliseconds.
        AppInstanceAdmin: Identity
        -- The ARN of the of the admin for the AppInstance.
        AppInstanceArn: string
    end

    record CreateAppInstanceBotInput
        -- The ARN of the AppInstance request.
        AppInstanceArn: string
        -- The user's name.
        Name: string
        -- The request metadata. Limited to a 1KB string in UTF-8.
        Metadata: string
        -- The unique ID for the client making the request. Use different tokens for different AppInstanceBots.
        ClientRequestToken: string
        -- The tags assigned to the AppInstanceBot.
        Tags: {Tag}
        -- Configuration information about the Amazon Lex V2 V2 bot.
        Configuration: Configuration
    end

    record CreateAppInstanceBotOutput
        -- The ARN of the AppinstanceBot.
        AppInstanceBotArn: string
    end

    record CreateAppInstanceUserInput
        -- The ARN of the AppInstance request.
        AppInstanceArn: string
        -- The user ID of the AppInstance.
        AppInstanceUserId: string
        -- The user's name.
        Name: string
        -- The request's metadata. Limited to a 1KB string in UTF-8.
        Metadata: string
        -- The unique ID of the request. Use different tokens to request additional AppInstances.
        ClientRequestToken: string
        -- Tags assigned to the AppInstanceUser.
        Tags: {Tag}
        -- Settings that control the interval after which the AppInstanceUser is automatically deleted.
        ExpirationSettings: ExpirationSettings
    end

    record CreateAppInstanceUserOutput
        -- The user's ARN.
        AppInstanceUserArn: string
    end

    record DeleteAppInstanceInput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
    end

    record DeleteAppInstanceOutput
    end

    record DeleteAppInstanceAdminInput
        -- The ARN of the AppInstance's administrator.
        AppInstanceAdminArn: string
        -- The ARN of the AppInstance.
        AppInstanceArn: string
    end

    record DeleteAppInstanceAdminOutput
    end

    record DeleteAppInstanceBotInput
        -- The ARN of the AppInstanceBot being deleted.
        AppInstanceBotArn: string
    end

    record DeleteAppInstanceBotOutput
    end

    record DeleteAppInstanceUserInput
        -- The ARN of the user request being deleted.
        AppInstanceUserArn: string
    end

    record DeleteAppInstanceUserOutput
    end

    record DeregisterAppInstanceUserEndpointInput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The unique identifier of the AppInstanceUserEndpoint.
        EndpointId: string
    end

    record DeregisterAppInstanceUserEndpointOutput
    end

    record DescribeAppInstanceInput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
    end

    record DescribeAppInstanceOutput
        -- The ARN, metadata, created and last-updated timestamps, and the name of the
        -- AppInstance. All timestamps use epoch milliseconds.
        AppInstance: AppInstance
    end

    record DescribeAppInstanceAdminInput
        -- The ARN of the AppInstanceAdmin.
        AppInstanceAdminArn: string
        -- The ARN of the AppInstance.
        AppInstanceArn: string
    end

    record DescribeAppInstanceAdminOutput
        -- The ARN and name of the AppInstanceUser, the ARN of the
        -- AppInstance, and the created and last-updated timestamps. All timestamps
        -- use epoch milliseconds.
        AppInstanceAdmin: AppInstanceAdmin
    end

    record DescribeAppInstanceBotInput
        -- The ARN of the AppInstanceBot.
        AppInstanceBotArn: string
    end

    record DescribeAppInstanceBotOutput
        -- The detials of the AppInstanceBot.
        AppInstanceBot: AppInstanceBot
    end

    -- One or more of the resources in the request does not exist in the system.
    record NotFoundException
        Code: string
        Message: string
    end

    record DescribeAppInstanceUserInput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
    end

    record DescribeAppInstanceUserOutput
        -- The name of the AppInstanceUser.
        AppInstanceUser: AppInstanceUser
    end

    record DescribeAppInstanceUserEndpointInput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The unique identifier of the AppInstanceUserEndpoint.
        EndpointId: string
    end

    record DescribeAppInstanceUserEndpointOutput
        -- The full details of an AppInstanceUserEndpoint: the AppInstanceUserArn, ID, name, type, resource ARN, attributes,
        -- allow messages, state, and created and last updated timestamps. All timestamps use epoch milliseconds.
        AppInstanceUserEndpoint: AppInstanceUserEndpoint
    end

    record GetAppInstanceRetentionSettingsInput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
    end

    record GetAppInstanceRetentionSettingsOutput
        -- The retention settings for the AppInstance.
        AppInstanceRetentionSettings: AppInstanceRetentionSettings
        -- The timestamp representing the time at which the specified items are retained, in Epoch
        -- Seconds.
        InitiateDeletionTimestamp: number
    end

    record ListAppInstanceAdminsInput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
        -- The maximum number of administrators that you want to return.
        MaxResults: number
        -- The token returned from previous API requests until the number of administrators is
        -- reached.
        NextToken: string
    end

    record ListAppInstanceAdminsOutput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
        -- The information for each administrator.
        AppInstanceAdmins: {AppInstanceAdminSummary}
        -- The token returned from previous API requests until the number of administrators is
        -- reached.
        NextToken: string
    end

    record ListAppInstanceBotsInput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
        -- The maximum number of requests to return.
        MaxResults: number
        -- The token passed by previous API calls until all requested bots are returned.
        NextToken: string
    end

    record ListAppInstanceBotsOutput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
        -- The information for each requested AppInstanceBot.
        AppInstanceBots: {AppInstanceBotSummary}
        -- The token passed by previous API calls until all requested bots are returned.
        NextToken: string
    end

    record ListAppInstancesInput
        -- The maximum number of AppInstances that you want to return.
        MaxResults: number
        -- The token passed by previous API requests until you reach the maximum number of
        -- AppInstances.
        NextToken: string
    end

    record ListAppInstancesOutput
        -- The information for each AppInstance.
        AppInstances: {AppInstanceSummary}
        -- The token passed by previous API requests until the maximum number of
        -- AppInstances is reached.
        NextToken: string
    end

    record ListAppInstanceUserEndpointsInput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The maximum number of endpoints that you want to return.
        MaxResults: number
        -- The token passed by previous API calls until all requested endpoints are returned.
        NextToken: string
    end

    record ListAppInstanceUserEndpointsOutput
        -- The information for each requested AppInstanceUserEndpoint.
        AppInstanceUserEndpoints: {AppInstanceUserEndpointSummary}
        -- The token passed by previous API calls until all requested endpoints are returned.
        NextToken: string
    end

    record ListAppInstanceUsersInput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
        -- The maximum number of requests that you want returned.
        MaxResults: number
        -- The token passed by previous API calls until all requested users are returned.
        NextToken: string
    end

    record ListAppInstanceUsersOutput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
        -- The information for each requested AppInstanceUser.
        AppInstanceUsers: {AppInstanceUserSummary}
        -- The token passed by previous API calls until all requested users are returned.
        NextToken: string
    end

    record ListTagsForResourceInput
        -- The ARN of the resource.
        ResourceARN: string
    end

    record ListTagsForResourceOutput
        -- The tag key-value pairs.
        Tags: {Tag}
    end

    record PutAppInstanceRetentionSettingsInput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
        -- The time in days to retain data. Data type: number.
        AppInstanceRetentionSettings: AppInstanceRetentionSettings
    end

    record PutAppInstanceRetentionSettingsOutput
        -- The time in days to retain data. Data type: number.
        AppInstanceRetentionSettings: AppInstanceRetentionSettings
        -- The time at which the API deletes data.
        InitiateDeletionTimestamp: number
    end

    record PutAppInstanceUserExpirationSettingsInput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- Settings that control the interval after which an AppInstanceUser is automatically deleted.
        ExpirationSettings: ExpirationSettings
    end

    record PutAppInstanceUserExpirationSettingsOutput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- Settings that control the interval after which an AppInstanceUser is automatically deleted.
        ExpirationSettings: ExpirationSettings
    end

    record RegisterAppInstanceUserEndpointInput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The name of the AppInstanceUserEndpoint.
        Name: string
        -- The type of the AppInstanceUserEndpoint. Supported types:
        -- APNS: The mobile notification service for an Apple device.
        -- APNS_SANDBOX: The sandbox environment of the mobile notification service for an Apple device.
        -- GCM: The mobile notification service for an Android device.
        -- Populate the ResourceArn value of each type as PinpointAppArn.
        Type: string
        -- The ARN of the resource to which the endpoint belongs.
        ResourceArn: string
        -- The attributes of an Endpoint.
        EndpointAttributes: EndpointAttributes
        -- The unique ID assigned to the request. Use different tokens to register other endpoints.
        ClientRequestToken: string
        -- Boolean that controls whether the AppInstanceUserEndpoint is opted in to receive messages. ALL indicates the endpoint receives all messages.
        -- NONE indicates the endpoint receives no messages.
        AllowMessages: string
    end

    record RegisterAppInstanceUserEndpointOutput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The unique identifier of the AppInstanceUserEndpoint.
        EndpointId: string
    end

    record TagResourceInput
        -- The resource ARN.
        ResourceARN: string
        -- The tag key-value pairs.
        Tags: {Tag}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The resource ARN.
        ResourceARN: string
        -- The tag keys.
        TagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateAppInstanceInput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
        -- The name that you want to change.
        Name: string
        -- The metadata that you want to change.
        Metadata: string
    end

    record UpdateAppInstanceOutput
        -- The ARN of the AppInstance.
        AppInstanceArn: string
    end

    record UpdateAppInstanceBotInput
        -- The ARN of the AppInstanceBot.
        AppInstanceBotArn: string
        -- The name of the AppInstanceBot.
        Name: string
        -- The metadata of the AppInstanceBot.
        Metadata: string
        -- The configuration for the bot update.
        Configuration: Configuration
    end

    record UpdateAppInstanceBotOutput
        -- The ARN of the AppInstanceBot.
        AppInstanceBotArn: string
    end

    record UpdateAppInstanceUserInput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The name of the AppInstanceUser.
        Name: string
        -- The metadata of the AppInstanceUser.
        Metadata: string
    end

    record UpdateAppInstanceUserOutput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
    end

    record UpdateAppInstanceUserEndpointInput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The unique identifier of the AppInstanceUserEndpoint.
        EndpointId: string
        -- The name of the AppInstanceUserEndpoint.
        Name: string
        -- Boolean that controls whether the AppInstanceUserEndpoint is opted in to receive messages. ALL indicates the endpoint will receive all messages.
        -- NONE indicates the endpoint will receive no messages.
        AllowMessages: string
    end

    record UpdateAppInstanceUserEndpointOutput
        -- The ARN of the AppInstanceUser.
        AppInstanceUserArn: string
        -- The unique identifier of the AppInstanceUserEndpoint.
        EndpointId: string
    end

end

return M
