-- Code generated by lua-client-codegen. DO NOT EDIT.

local id = require("smithy.shape_id")
local schema = require("smithy.schema")
local prelude = require("smithy.prelude")
local traits = require("smithy.traits")
local smithy = require("smithy")

local _N = "com.amazonaws.iamtoolbox"

local M: {string:any} = {}

M.AuthorizationContext = schema.new({ type = "map", map_key = prelude.String, map_value = prelude.Document })

M.Evaluations = schema.new({ type = "list", list_member = M.Evaluation })

M.PolicyInfoList = schema.new({ type = "list", list_member = M.PolicyInfo })

M.MatchedPolicyList = schema.new({ type = "list", list_member = M.MatchedPolicy })

M.AttachedToList = schema.new({ type = "list", list_member = M.AttachedTo })

M.MatchedStatementList = schema.new({ type = "list", list_member = M.MatchedStatement })

M.AccessDeniedException = schema.new({
    id = id.from(_N, "AccessDeniedException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        message = schema.new({
            id = id.from(_N, "AccessDeniedException", "message"),
            type = "string",
            name = "message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.AttachedTo = schema.new({
    id = id.from(_N, "AttachedTo"),
    type = "structure",
    members = {
        arn = schema.new({
            id = id.from(_N, "AttachedTo", "arn"),
            type = "string",
            name = "arn",
            target_id = prelude.String.id,
        }),
    },
})

M.GetRequestAuthorizationDetailsInput = schema.new({
    id = id.from(_N, "GetRequestAuthorizationDetailsInput"),
    type = "structure",
    members = {
        authorizationId = schema.new({
            id = id.from(_N, "GetRequestAuthorizationDetailsInput", "authorizationId"),
            type = "string",
            name = "authorizationId",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_LABEL] = {},
            },
        }),
        nextToken = schema.new({
            id = id.from(_N, "GetRequestAuthorizationDetailsInput", "nextToken"),
            type = "string",
            name = "nextToken",
            target_id = prelude.String.id,
            traits = {
                [traits.HTTP_QUERY] = { name = "nextToken" },
            },
        }),
    },
})

M.MatchedStatement = schema.new({
    id = id.from(_N, "MatchedStatement"),
    type = "structure",
    members = {
        sid = schema.new({
            id = id.from(_N, "MatchedStatement", "sid"),
            type = "string",
            name = "sid",
            target_id = prelude.String.id,
        }),
        evaluatedEffect = schema.new({
            id = id.from(_N, "MatchedStatement", "evaluatedEffect"),
            type = "string",
            name = "evaluatedEffect",
            target_id = prelude.String.id,
        }),
    },
})

M.MatchedPolicy = schema.new({
    id = id.from(_N, "MatchedPolicy"),
    type = "structure",
    members = {
        uri = schema.new({
            id = id.from(_N, "MatchedPolicy", "uri"),
            type = "string",
            name = "uri",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        matchedStatements = schema.new({
            id = id.from(_N, "MatchedPolicy", "matchedStatements"),
            type = "list",
            name = "matchedStatements",
            target_id = prelude.Document.id,
            list_member = M.MatchedStatement,
        }),
    },
})

M.Evaluation = schema.new({
    id = id.from(_N, "Evaluation"),
    type = "structure",
    members = {
        action = schema.new({
            id = id.from(_N, "Evaluation", "action"),
            type = "string",
            name = "action",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        resource = schema.new({
            id = id.from(_N, "Evaluation", "resource"),
            type = "string",
            name = "resource",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        context = schema.new({
            id = id.from(_N, "Evaluation", "context"),
            type = "map",
            name = "context",
            target_id = prelude.Document.id,
            map_key = prelude.String,
            map_value = prelude.Document,
        }),
        evaluatedEffect = schema.new({
            id = id.from(_N, "Evaluation", "evaluatedEffect"),
            type = "string",
            name = "evaluatedEffect",
            target_id = prelude.String.id,
        }),
        matchedPolicies = schema.new({
            id = id.from(_N, "Evaluation", "matchedPolicies"),
            type = "list",
            name = "matchedPolicies",
            target_id = prelude.Document.id,
            list_member = M.MatchedPolicy,
        }),
    },
})

M.PolicyInfo = schema.new({
    id = id.from(_N, "PolicyInfo"),
    type = "structure",
    members = {
        type = schema.new({
            id = id.from(_N, "PolicyInfo", "type"),
            type = "string",
            name = "type",
            target_id = prelude.String.id,
        }),
        inline = schema.new({
            id = id.from(_N, "PolicyInfo", "inline"),
            type = "boolean",
            name = "inline",
            target_id = prelude.Boolean.id,
        }),
        uri = schema.new({
            id = id.from(_N, "PolicyInfo", "uri"),
            type = "string",
            name = "uri",
            target_id = prelude.String.id,
        }),
        attachedTo = schema.new({
            id = id.from(_N, "PolicyInfo", "attachedTo"),
            type = "list",
            name = "attachedTo",
            target_id = prelude.Document.id,
            list_member = M.AttachedTo,
        }),
    },
})

M.GetRequestAuthorizationDetailsOutput = schema.new({
    id = id.from(_N, "GetRequestAuthorizationDetailsOutput"),
    type = "structure",
    members = {
        requestContext = schema.new({
            id = id.from(_N, "GetRequestAuthorizationDetailsOutput", "requestContext"),
            type = "map",
            name = "requestContext",
            target_id = prelude.Document.id,
            map_key = prelude.String,
            map_value = prelude.Document,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        evaluations = schema.new({
            id = id.from(_N, "GetRequestAuthorizationDetailsOutput", "evaluations"),
            type = "list",
            name = "evaluations",
            target_id = prelude.Document.id,
            list_member = M.Evaluation,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        policies = schema.new({
            id = id.from(_N, "GetRequestAuthorizationDetailsOutput", "policies"),
            type = "list",
            name = "policies",
            target_id = prelude.Document.id,
            list_member = M.PolicyInfo,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        nextToken = schema.new({
            id = id.from(_N, "GetRequestAuthorizationDetailsOutput", "nextToken"),
            type = "string",
            name = "nextToken",
            target_id = prelude.String.id,
        }),
    },
})

M.InternalServerException = schema.new({
    id = id.from(_N, "InternalServerException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "server" },
    },
    members = {
        message = schema.new({
            id = id.from(_N, "InternalServerException", "message"),
            type = "string",
            name = "message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.ResourceNotFoundException = schema.new({
    id = id.from(_N, "ResourceNotFoundException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        message = schema.new({
            id = id.from(_N, "ResourceNotFoundException", "message"),
            type = "string",
            name = "message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.ValidationException = schema.new({
    id = id.from(_N, "ValidationException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        message = schema.new({
            id = id.from(_N, "ValidationException", "message"),
            type = "string",
            name = "message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

-- Fix forward references for recursive schemas
for _, _s in pairs(M) do
    local s = _s as {string:any}
    if type(s) == "table" and (s.type == "structure" or s.type == "union") then
        local members = rawget(s, "_members") as {string:{string:any}}
        if members then
            for _, ms in pairs(members) do
                if (ms.type == "structure" or ms.type == "union") and not rawget(ms as {string:any}, "_target") and ms.target_id then
                    rawset(ms as {string:any}, "_target", M[(ms.target_id as {string:string}).name])
                end
            end
        end
    end
end

M.Service = schema.service({
    id = id.from("com.amazonaws.iamtoolbox", "AuthRequestService"),
    version = "2018-05-10",
    traits = {
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

M.GetRequestAuthorizationDetails = schema.operation({
    id = id.from("com.amazonaws.iamtoolbox", "GetRequestAuthorizationDetails"),
    input = M.GetRequestAuthorizationDetailsInput,
    output = M.GetRequestAuthorizationDetailsOutput,
    traits = {
        [traits.HTTP] = { method = "GET", path = "/authorization-details/{authorizationId}" },
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

return M
