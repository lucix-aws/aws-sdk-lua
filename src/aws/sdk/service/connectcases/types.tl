-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- You do not have sufficient access to perform this action.
    record AccessDeniedException
        message: string
    end

    -- The requested operation would cause a conflict with the current state of a service resource associated with the request. Resolve the conflict before retrying this request. See the accompanying error message for details.
    record ConflictException
        message: string
    end

    -- An empty value. You cannot set EmptyFieldValue on a field that is required on a case template. This structure will never have any data members. It signifies an empty value on a case field.
    record EmptyFieldValue
    end

    -- Object to store union of Field values. The Summary system field accepts up to 3000 characters, while all other fields accept up to 4100 characters. If you use multi-byte characters, the effective character limit may be lower.
    record FieldValueUnion
        -- String value type.
        stringValue: string
        -- Can be either null, or have a Double number value type. Only one value can be provided.
        doubleValue: number
        -- Can be either null, or have a Boolean value type. Only one value can be provided.
        booleanValue: boolean
        -- An empty value.
        emptyValue: EmptyFieldValue
        -- Represents the user that performed the audit.
        userArnValue: string
    end

    -- Object for case field values.
    record FieldValue
        -- Unique identifier of a field.
        id: string
        -- Union of potential field value types.
        value: FieldValueUnion
    end

    -- Represents the entity that performed the action.
    record UserUnion
        -- Represents the Amazon Connect ARN of the user.
        userArn: string
        -- Any provided entity.
        customEntity: string
    end

    record CreateCaseInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier of a template.
        templateId: string
        -- An array of objects with field ID (matching ListFields/DescribeField) and value union data.
        fields: {FieldValue}
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
        clientToken: string
        performedBy: UserUnion
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
    end

    record CreateCaseOutput
        -- A unique identifier of the case.
        caseId: string
        -- The Amazon Resource Name (ARN) of the case.
        caseArn: string
    end

    -- We couldn't process your request because of an issue with the server. Try again later.
    record InternalServerException
        message: string
        -- Advice to clients on when the call can be safely retried.
        retryAfterSeconds: number
    end

    -- We couldn't find the requested resource. Check that your resources exists and were created in the same Amazon Web Services Region as your request, and try your request again.
    record ResourceNotFoundException
        message: string
        -- Unique identifier of the resource affected.
        resourceId: string
        -- Type of the resource affected.
        resourceType: string
    end

    -- The rate has been exceeded for this API. Please try again after a few minutes.
    record ThrottlingException
        message: string
    end

    -- The request isn't valid. Check the syntax and try again.
    record ValidationException
        message: string
    end

    record DeleteCaseInput
        -- A unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier of the case.
        caseId: string
    end

    record DeleteCaseOutput
    end

    -- Object for unique identifier of a field.
    record FieldIdentifier
        -- Unique identifier of a field.
        id: string
    end

    record GetCaseInput
        -- A unique identifier of the case.
        caseId: string
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A list of unique field identifiers.
        fields: {FieldIdentifier}
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
    end

    record GetCaseOutput
        -- A list of detailed field information.
        fields: {FieldValue}
        -- A unique identifier of a template.
        templateId: string
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
    end

    record GetCaseAuditEventsInput
        -- A unique identifier of the case.
        caseId: string
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The maximum number of audit events to return. When no value is provided, 25 is the default.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
    end

    -- Object to store union of Field values.
    record AuditEventFieldValueUnion
        -- Can be either null, or have a String value type. Only one value can be provided.
        stringValue: string
        -- Can be either null, or have a Double value type. Only one value can be provided.
        doubleValue: number
        -- Can be either null, or have a Boolean value type. Only one value can be provided.
        booleanValue: boolean
        emptyValue: EmptyFieldValue
        -- Can be either null, or have a String value type formatted as an ARN. Only one value can be provided.
        userArnValue: string
    end

    -- Fields for audit event.
    record AuditEventField
        -- Unique identifier of field in an Audit History entry.
        eventFieldId: string
        -- Union of potential field value types.
        oldValue: AuditEventFieldValueUnion
        -- Union of potential field value types.
        newValue: AuditEventFieldValueUnion
    end

    -- Information of the user which performed the audit.
    record AuditEventPerformedBy
        user: UserUnion
        -- Unique identifier of an IAM role.
        iamPrincipalArn: string
    end

    enum RelatedItemType
        "Contact"
        "Comment"
        "File"
        "Sla"
        "ConnectCase"
        "Custom"
    end

    enum AuditEventType
        "Case.Created"
        "Case.Updated"
        "RelatedItem.Created"
        "RelatedItem.Deleted"
        "RelatedItem.Updated"
    end

    -- Represents the content of a particular audit event.
    record AuditEvent
        -- Unique identifier of a case audit history event.
        eventId: string
        -- The Type of the related item.
        relatedItemType: string
        -- Time at which an Audit History event took place.
        performedTime: number
        -- A list of Case Audit History event fields.
        fields: {AuditEventField}
        -- Information of the user which performed the audit.
        performedBy: AuditEventPerformedBy
    end

    record GetCaseAuditEventsOutput
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
        -- A list of case audits where each represents a particular edit of the case.
        auditEvents: {AuditEvent}
    end

    record ListCasesForContactInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier of a contact in Amazon Connect.
        contactArn: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
    end

    -- Case summary information.
    record CaseSummary
        -- A unique identifier of the case.
        caseId: string
        -- A unique identifier of a template.
        templateId: string
    end

    record ListCasesForContactOutput
        -- A list of Case summary information.
        cases: {CaseSummary}
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
    end

    enum CommentBodyTextType
        "Text/Plain"
    end

    -- Represents the content of a Comment to be returned to agents.
    record CommentContent
        -- Text in the body of a Comment on a case.
        body: string
        -- Type of the text in the box of a Comment on a case.
        contentType: string
    end

    -- Represents the content of a ConnectCase related item.
    record ConnectCaseInputContent
        -- A unique identifier of the case.
        caseId: string
    end

    -- An object that represents an Amazon Connect contact object.
    record Contact
        -- A unique identifier of a contact in Amazon Connect.
        contactArn: string
    end

    -- Represents the content of a Custom related item.
    record CustomInputContent
        -- List of field values for the Custom related item.
        fields: {FieldValue}
    end

    -- An object that represents a content of an Amazon Connect file object.
    record FileContent
        -- The Amazon Resource Name (ARN) of a File in Amazon Connect.
        fileArn: string
    end

    enum SlaType
        "CaseField"
    end

    -- Represents the input configuration of an SLA being created.
    record SlaInputConfiguration
        -- Name of an SLA.
        name: string
        -- Unique identifier of a field.
        fieldId: string
        -- Represents a list of target field values for the fieldId specified in SlaInputConfiguration. The SLA is considered met if any one of these target field values matches the actual field value.
        targetFieldValues: {FieldValueUnion}
        -- Target duration in minutes within which an SLA should be completed.
        targetSlaMinutes: number
    end

    -- Represents the content of an SLA.
    record SlaInputContent
        -- Represents an input SLA configuration.
        slaInputConfiguration: SlaInputConfiguration
    end

    -- Represents the content of a related item to be created.
    record RelatedItemInputContent
        -- Object representing a contact in Amazon Connect as an API request field.
        contact: Contact
        -- Represents the content of a comment to be returned to agents.
        comment: CommentContent
        -- A file of related items.
        file: FileContent
        -- Represents the content of an SLA to be created.
        sla: SlaInputContent
        -- Represents the Amazon Connect case to be created as a related item.
        connectCase: ConnectCaseInputContent
        -- Represents the content of a Custom type related item.
        custom: CustomInputContent
    end

    record CreateRelatedItemInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier of the case.
        caseId: string
        -- The content of a related item to be created.
        content: RelatedItemInputContent
        -- Represents the creator of the related item.
        performedBy: UserUnion
    end

    record CreateRelatedItemOutput
        -- The unique identifier of the related item.
        relatedItemId: string
        -- The Amazon Resource Name (ARN) of the related item.
        relatedItemArn: string
    end

    -- The service quota has been exceeded. For a list of service quotas, see Amazon Connect Service Quotas in the Amazon Connect Administrator Guide.
    record ServiceQuotaExceededException
        message: string
    end

    record DeleteRelatedItemInput
        -- A unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier of the case.
        caseId: string
        -- A unique identifier of a related item.
        relatedItemId: string
    end

    record DeleteRelatedItemOutput
    end

    -- A filter for related items of type Comment.
    record CommentFilter
    end

    -- A filter for related items of type ConnectCase.
    record ConnectCaseFilter
        -- A unique identifier of the case.
        caseId: string
    end

    -- A filter for related items of type Contact.
    record ContactFilter
        -- A list of channels to filter on for related items of type Contact.
        channel: {string}
        -- A unique identifier of a contact in Amazon Connect.
        contactArn: string
    end

    -- A filter for fields. Only one value can be provided.
    record FieldFilter
        -- Object containing field identifier and value information.
        equalTo: FieldValue
        -- Object containing field identifier and value information.
        contains: FieldValue
        -- Object containing field identifier and value information.
        greaterThan: FieldValue
        -- Object containing field identifier and value information.
        greaterThanOrEqualTo: FieldValue
        -- Object containing field identifier and value information.
        lessThan: FieldValue
        -- Object containing field identifier and value information.
        lessThanOrEqualTo: FieldValue
    end

    -- A filter for related items of type File.
    record FileFilter
        -- The Amazon Resource Name (ARN) of the file.
        fileArn: string
    end

    enum SlaStatus
        "Active"
        "Overdue"
        "Met"
        "NotMet"
    end

    -- A filter for related items of type SLA.
    record SlaFilter
        -- Name of an SLA.
        name: string
        -- Status of an SLA.
        status: string
    end

    -- Represents the content of a ConnectCase type related item.
    record ConnectCaseContent
        -- A unique identifier of the case.
        caseId: string
    end

    -- An object that represents a content of an Amazon Connect contact object.
    record ContactContent
        -- A unique identifier of a contact in Amazon Connect.
        contactArn: string
        -- A list of channels to filter on for related items of type Contact.
        channel: string
        -- The difference between the InitiationTimestamp and the DisconnectTimestamp of the contact.
        connectedToSystemTime: number
    end

    -- Represents the content of a Custom type related item.
    record CustomContent
        -- List of field values for the Custom related item.
        fields: {FieldValue}
    end

    -- Represents an SLA configuration.
    record SlaConfiguration
        -- Name of an SLA.
        name: string
        -- Status of an SLA.
        status: string
        -- Unique identifier of a field.
        fieldId: string
        -- Represents a list of target field values for the fieldId specified in SlaConfiguration.
        targetFieldValues: {FieldValueUnion}
        -- Target time by which an SLA should be completed.
        targetTime: number
        -- Time at which an SLA was completed.
        completionTime: number
    end

    -- Represents the content of an SLA to be returned to agents.
    record SlaContent
        -- Represents an SLA configuration.
        slaConfiguration: SlaConfiguration
    end

    -- Represents the content of a particular type of related item.
    record RelatedItemContent
        -- Represents the content of a contact to be returned to agents.
        contact: ContactContent
        -- Represents the content of a comment to be returned to agents.
        comment: CommentContent
        -- Represents the content of a File to be returned to agents.
        file: FileContent
        -- Represents the content of an SLA to be returned to agents.
        sla: SlaContent
        -- Represents the Amazon Connect case to be created as a related item.
        connectCase: ConnectCaseContent
        -- Represents the content of a Custom type related item.
        custom: CustomContent
    end

    -- A list of items that represent RelatedItems.
    record SearchRelatedItemsResponseItem
        -- Unique identifier of a related item.
        relatedItemId: string
        -- Time at which a related item was associated with a case.
        associationTime: number
        -- Represents the content of a particular type of related item.
        content: RelatedItemContent
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
        -- Represents the creator of the related item.
        performedBy: UserUnion
    end

    record SearchRelatedItemsOutput
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
        -- A list of items related to a case.
        relatedItems: {SearchRelatedItemsResponseItem}
    end

    -- Represents the updated content of a Comment related item.
    record CommentUpdateContent
        -- Updated text in the body of a Comment on a case.
        body: string
        -- Type of the text in the box of a Comment on a case.
        contentType: string
    end

    -- Represents the updated content of a Custom related item.
    record CustomUpdateContent
        -- List of updated field values for the Custom related item. All existing and new fields, and their associated values should be included. Fields not included as part of this request will be removed.
        fields: {FieldValue}
    end

    -- Represents the content of a related item to be updated. This is a union type that can contain either comment content or custom content.
    record RelatedItemUpdateContent
        comment: CommentUpdateContent
        custom: CustomUpdateContent
    end

    record UpdateRelatedItemInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier of the case.
        caseId: string
        -- Unique identifier of a related item.
        relatedItemId: string
        -- The content of a related item to be updated.
        content: RelatedItemUpdateContent
        -- Represents the user who performed the update of the related item.
        performedBy: UserUnion
    end

    record UpdateRelatedItemOutput
        -- The unique identifier of the updated related item.
        relatedItemId: string
        -- The Amazon Resource Name (ARN) of the updated related item.
        relatedItemArn: string
        -- Represents the content of the updated related item.
        content: RelatedItemContent
        -- Time at which the related item was associated with the case.
        associationTime: number
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
        -- Represents the last user that updated the related item.
        lastUpdatedUser: UserUnion
        -- Represents the creator of the related item.
        createdBy: UserUnion
    end

    -- Object for case tag filter values.
    record TagValue
        -- The tag key in the tag filter value.
        key: string
        -- The tag value in the tag filter value.
        value: string
    end

    -- A filter for tags. Only one value can be provided.
    record TagFilter
        -- Object containing tag key and value information.
        equalTo: TagValue
    end

    enum Order
        "Asc"
        "Desc"
    end

    -- A structured set of sort terms.
    record Sort
        -- Unique identifier of a field.
        fieldId: string
        -- A structured set of sort terms
        sortOrder: string
    end

    -- A list of items that represent cases.
    record SearchCasesResponseItem
        -- A unique identifier of the case.
        caseId: string
        -- A unique identifier of a template.
        templateId: string
        -- List of case field values.
        fields: {FieldValue}
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
    end

    record SearchCasesOutput
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
        -- A list of case documents where each case contains the properties CaseId and Fields where each field is a complex union structure.
        cases: {SearchCasesResponseItem}
        -- The total number of cases that matched the search criteria.
        totalCount: number
    end

    record UpdateCaseInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier of the case.
        caseId: string
        -- An array of objects with fieldId (matching ListFields/DescribeField) and value union data, structured identical to CreateCase.
        fields: {FieldValue}
        performedBy: UserUnion
    end

    record UpdateCaseOutput
    end

    -- Object containing case rule identifier information.
    record CaseRuleIdentifier
        -- Unique identifier of a case rule.
        id: string
    end

    record BatchGetCaseRuleInput
        -- Unique identifier of a Cases domain.
        domainId: string
        -- A list of case rule identifiers.
        caseRules: {CaseRuleIdentifier}
    end

    -- A mapping between a parent field option value and child field option values.
    record ParentChildFieldOptionsMapping
        -- The value in the parent field.
        parentFieldOptionValue: string
        -- A list of allowed values in the child field.
        childFieldOptionValues: {string}
    end

    -- Rules that control which options are available in a child field based on the selected value in a parent field.
    record FieldOptionsCaseRule
        -- The identifier of the parent field that controls options.
        parentFieldId: string
        -- The identifier of the child field whose options are controlled.
        childFieldId: string
        -- A mapping between a parent field option value and child field option values.
        parentChildFieldOptionsMappings: {ParentChildFieldOptionsMapping}
    end

    -- Represents the left hand operand in the condition. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record OperandOne
        -- The field ID that this operand should take the value of.
        fieldId: string
    end

    -- Represents an empty operand value. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record EmptyOperandValue
    end

    -- Represents the right hand operand in the condition. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record OperandTwo
        -- String value type.
        stringValue: string
        -- Boolean value type.
        booleanValue: boolean
        -- Double value type.
        doubleValue: number
        -- Empty value type.
        emptyValue: EmptyOperandValue
    end

    -- Boolean operands for a condition. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record BooleanOperands
        -- Represents the left hand operand in the condition.
        operandOne: OperandOne
        -- Represents the right hand operand in the condition.
        operandTwo: OperandTwo
        -- The value of the outer rule if the condition evaluates to true.
        result: boolean
    end

    -- Error for batch describe case rules API failure. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record CaseRuleError
        -- The case rule identifier that caused the error.
        id: string
        -- Error code from getting a case rule.
        errorCode: string
        -- Error message from getting a case rule.
        message: string
    end

    record CreateCaseRuleOutput
        -- Unique identifier of a case rule.
        caseRuleId: string
        -- The Amazon Resource Name (ARN) of a case rule.
        caseRuleArn: string
    end

    record DeleteCaseRuleInput
        -- Unique identifier of a Cases domain.
        domainId: string
        -- Unique identifier of a case rule.
        caseRuleId: string
    end

    record DeleteCaseRuleOutput
    end

    record ListCaseRulesInput
        -- Unique identifier of a Cases domain.
        domainId: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
    end

    enum RuleType
        "Required"
        "Hidden"
        "FieldOptions"
    end

    -- Summary information of this case rule. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record CaseRuleSummary
        -- Unique identifier of a case rule.
        caseRuleId: string
        -- Name of the case rule.
        name: string
        -- The Amazon Resource Name (ARN) of the case rule.
        caseRuleArn: string
        -- Possible types for a rule.
        ruleType: string
        -- Description of a case rule.
        description: string
    end

    record ListCaseRulesOutput
        -- A list of field summary objects.
        caseRules: {CaseRuleSummary}
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
    end

    record UpdateCaseRuleOutput
    end

    record CreateDomainInput
        -- The name for your Cases domain. It must be unique for your Amazon Web Services account.
        name: string
    end

    enum DomainStatus
        "Active"
        "CreationInProgress"
        "CreationFailed"
    end

    record CreateDomainOutput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The Amazon Resource Name (ARN) for the Cases domain.
        domainArn: string
        -- The status of the domain.
        domainStatus: string
    end

    record DeleteDomainInput
        -- The unique identifier of the Cases domain.
        domainId: string
    end

    record DeleteDomainOutput
    end

    record GetCaseEventConfigurationInput
        -- The unique identifier of the Cases domain.
        domainId: string
    end

    -- Details of what case data is published through the case event stream.
    record CaseEventIncludedData
        -- List of field identifiers.
        fields: {FieldIdentifier}
    end

    -- Details of what related item data is published through the case event stream.
    record RelatedItemEventIncludedData
        -- Details of what related item data is published through the case event stream.
        includeContent: boolean
    end

    -- Details of what case and related item data is published through the case event stream.
    record EventIncludedData
        -- Details of what case data is published through the case event stream.
        caseData: CaseEventIncludedData
        -- Details of what related item data is published through the case event stream.
        relatedItemData: RelatedItemEventIncludedData
    end

    -- Configuration to enable EventBridge case event delivery and determine what data is delivered.
    record EventBridgeConfiguration
        -- Indicates whether the to broadcast case event data to the customer.
        enabled: boolean
        -- Details of what case and related item data is published through the case event stream.
        includedData: EventIncludedData
    end

    record GetCaseEventConfigurationOutput
        -- Configuration to enable EventBridge case event delivery and determine what data is delivered.
        eventBridge: EventBridgeConfiguration
    end

    record GetDomainInput
        -- The unique identifier of the Cases domain.
        domainId: string
    end

    record GetDomainOutput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The Amazon Resource Name (ARN) for the Cases domain.
        domainArn: string
        -- The name of the Cases domain.
        name: string
        -- The timestamp when the Cases domain was created.
        createdTime: number
        -- The status of the Cases domain.
        domainStatus: string
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
    end

    record ListDomainsInput
        -- The maximum number of results to return per page.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
    end

    -- Object for the summarized details of the domain.
    record DomainSummary
        -- The unique identifier of the domain.
        domainId: string
        -- The Amazon Resource Name (ARN) of the domain.
        domainArn: string
        -- The name of the domain.
        name: string
    end

    record ListDomainsOutput
        -- The Cases domain.
        domains: {DomainSummary}
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
    end

    record PutCaseEventConfigurationInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- Configuration to enable EventBridge case event delivery and determine what data is delivered.
        eventBridge: EventBridgeConfiguration
    end

    record PutCaseEventConfigurationOutput
    end

    enum SearchAllRelatedItemsSortProperty
        "AssociationTime"
        "CaseId"
    end

    -- The order in which all returned related items should be sorted.
    record SearchAllRelatedItemsSort
        -- Whether related items should be sorted in ascending or descending order.
        sortProperty: string
        -- Whether related items should be sorted by association time or case ID.
        sortOrder: string
    end

    -- A list of items that represent RelatedItems. This data type is similar to SearchRelatedItemsResponseItem except SearchAllRelatedItemsResponseItem has a caseId field.
    record SearchAllRelatedItemsResponseItem
        -- Unique identifier of a related item.
        relatedItemId: string
        -- A unique identifier of the case.
        caseId: string
        -- Time at which a related item was associated with a case.
        associationTime: number
        content: RelatedItemContent
        performedBy: UserUnion
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
    end

    record SearchAllRelatedItemsOutput
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
        -- A list of items related to a case.
        relatedItems: {SearchAllRelatedItemsResponseItem}
    end

    record BatchGetFieldInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A list of unique field identifiers.
        fields: {FieldIdentifier}
    end

    -- Object for errors on fields.
    record FieldError
        -- The field identifier that caused the error.
        id: string
        -- The error code from getting a field.
        errorCode: string
        -- The error message from getting a field.
        message: string
    end

    -- Field attributes for Text field type.
    record TextAttributes
        -- Attribute that defines rendering component and validation.
        isMultiline: boolean
    end

    -- Union of field attributes.
    record FieldAttributes
        -- Field attributes for Text field type.
        text: TextAttributes
    end

    enum FieldNamespace
        "System"
        "Custom"
    end

    enum FieldType
        "Text"
        "Number"
        "Boolean"
        "DateTime"
        "SingleSelect"
        "Url"
        "User"
    end

    -- Object to store detailed field information.
    record GetFieldResponse
        -- Unique identifier of the field.
        fieldId: string
        -- Name of the field.
        name: string
        -- The Amazon Resource Name (ARN) of the field.
        fieldArn: string
        -- Description of the field.
        description: string
        -- Namespace of the field.
        namespace: string
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
        -- Denotes whether or not the resource has been deleted.
        deleted: boolean
        -- Timestamp at which the resource was created.
        createdTime: number
        -- Timestamp at which the resource was created or last modified.
        lastModifiedTime: number
        -- Union of field attributes.
        attributes: FieldAttributes
    end

    record BatchGetFieldOutput
        -- A list of detailed field information.
        fields: {GetFieldResponse}
        -- A list of field errors.
        errors: {FieldError}
    end

    -- Object for field Options information.
    record FieldOption
        -- FieldOptionName has max length 100 and disallows trailing spaces.
        name: string
        -- FieldOptionValue has max length 100 and must be alphanumeric with hyphens and underscores.
        value: string
        -- Describes whether the FieldOption is active (displayed) or inactive.
        active: boolean
    end

    record BatchPutFieldOptionsInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The unique identifier of a field.
        fieldId: string
        -- A list of FieldOption objects.
        options: {FieldOption}
    end

    -- Object for field Options errors.
    record FieldOptionError
        -- Error message from creating or updating field option.
        message: string
        -- Error code from creating or updating field option.
        errorCode: string
        -- The field option value that caused the error.
        value: string
    end

    record BatchPutFieldOptionsOutput
        -- A list of field errors.
        errors: {FieldOptionError}
    end

    record CreateFieldInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The name of the field.
        name: string
        -- The description of the field.
        description: string
        -- Union of field attributes.
        attributes: FieldAttributes
    end

    record CreateFieldOutput
        -- The unique identifier of a field.
        fieldId: string
        -- The Amazon Resource Name (ARN) of the field.
        fieldArn: string
    end

    record DeleteFieldInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- Unique identifier of the field.
        fieldId: string
    end

    record DeleteFieldOutput
    end

    record ListFieldOptionsInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The unique identifier of a field.
        fieldId: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- A list of FieldOption values to filter on for ListFieldOptions.
        values: {string}
    end

    record ListFieldOptionsOutput
        -- A list of FieldOption objects.
        options: {FieldOption}
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
    end

    record ListFieldsInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
    end

    -- Object for the summarized details of the field.
    record FieldSummary
        -- The unique identifier of a field.
        fieldId: string
        -- The Amazon Resource Name (ARN) of the field.
        fieldArn: string
        -- Name of the field.
        name: string
        -- The namespace of a field.
        namespace: string
        -- Union of field attributes.
        attributes: FieldAttributes
    end

    record ListFieldsOutput
        -- List of detailed field information.
        fields: {FieldSummary}
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
    end

    record UpdateFieldInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The unique identifier of a field.
        fieldId: string
        -- The name of the field.
        name: string
        -- The description of a field.
        description: string
        -- Union of field attributes.
        attributes: FieldAttributes
    end

    record UpdateFieldOutput
    end

    -- Object for field related information.
    record FieldItem
        -- Unique identifier of a field.
        id: string
    end

    -- Object for a group of fields and associated properties.
    record FieldGroup
        -- Name of the field group.
        name: string
        -- Represents an ordered list containing field related information.
        fields: {FieldItem}
    end

    -- This represents a sections within a panel or tab of the page layout.
    record Section
        -- Consists of a group of fields and associated properties.
        fieldGroup: FieldGroup
    end

    -- Ordered list containing different kinds of sections that can be added. A LayoutSections object can only contain one section.
    record LayoutSections
        sections: {Section}
    end

    -- Content specific to BasicLayout type. It configures fields in the top panel and More Info tab of agent application.
    record BasicLayout
        -- This represents sections in a panel of the page layout.
        topPanel: LayoutSections
        -- This represents sections in a tab of the page layout.
        moreInfo: LayoutSections
    end

    -- Object to store union of different versions of layout content.
    record LayoutContent
        -- Content specific to BasicLayout type. It configures fields in the top panel and More Info tab of Cases user interface.
        basic: BasicLayout
    end

    record CreateLayoutInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The name of the layout. It must be unique for the Cases domain.
        name: string
        -- Information about which fields will be present in the layout, and information about the order of the fields.
        content: LayoutContent
    end

    record CreateLayoutOutput
        -- The unique identifier of the layout.
        layoutId: string
        -- The Amazon Resource Name (ARN) of the newly created layout.
        layoutArn: string
    end

    record DeleteLayoutInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The unique identifier of the layout.
        layoutId: string
    end

    record DeleteLayoutOutput
    end

    record GetLayoutInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The unique identifier of the layout.
        layoutId: string
    end

    record GetLayoutOutput
        -- The unique identifier of the layout.
        layoutId: string
        -- The Amazon Resource Name (ARN) of the newly created layout.
        layoutArn: string
        -- The name of the layout. It must be unique.
        name: string
        -- Information about which fields will be present in the layout, the order of the fields, and read-only attribute of the field.
        content: LayoutContent
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
        -- Denotes whether or not the resource has been deleted.
        deleted: boolean
        -- Timestamp at which the resource was created.
        createdTime: number
        -- Timestamp at which the resource was created or last modified.
        lastModifiedTime: number
    end

    record ListLayoutsInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
    end

    -- Object for the summarized details of the layout.
    record LayoutSummary
        -- The unique identifier for of the layout.
        layoutId: string
        -- The Amazon Resource Name (ARN) of the layout.
        layoutArn: string
        -- The name of the layout.
        name: string
    end

    record ListLayoutsOutput
        -- The layouts for the domain.
        layouts: {LayoutSummary}
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
    end

    record UpdateLayoutInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The unique identifier of the layout.
        layoutId: string
        -- The name of the layout. It must be unique per domain.
        name: string
        -- Information about which fields will be present in the layout, the order of the fields.
        content: LayoutContent
    end

    record UpdateLayoutOutput
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN)
        arn: string
    end

    record ListTagsForResourceOutput
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN)
        arn: string
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
    end

    record TagResourceOutput
    end

    -- Object to store configuration of layouts associated to the template.
    record LayoutConfiguration
        -- Unique identifier of a layout.
        defaultLayout: string
    end

    -- List of fields that must have a value provided to create a case.
    record RequiredField
        -- Unique identifier of a field.
        fieldId: string
    end

    -- An association representing a case rule acting upon a field. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record TemplateRule
        -- Unique identifier of a case rule.
        caseRuleId: string
        -- Unique identifier of a field.
        fieldId: string
    end

    enum TemplateStatus
        "Active"
        "Inactive"
    end

    enum TagPropagationResourceType
        "Cases"
    end

    -- Defines tag propagation configuration for resources created within a domain. Tags specified here will be automatically applied to resources being created for the specified resource type.
    record TagPropagationConfiguration
        -- Supported resource types for tag propagation. Determines which resources will receive automatically propagated tags.
        resourceType: string
        -- The tags that will be applied to the created resource.
        tagMap: {string : string}
    end

    record CreateTemplateInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A name for the template. It must be unique per domain.
        name: string
        -- A brief description of the template.
        description: string
        -- Configuration of layouts associated to the template.
        layoutConfiguration: LayoutConfiguration
        -- A list of fields that must contain a value for a case to be successfully created with this template.
        requiredFields: {RequiredField}
        -- The status of the template.
        status: string
        -- A list of case rules (also known as case field conditions) on a template.
        rules: {TemplateRule}
        -- Defines tag propagation configuration for resources created within a domain. Tags specified here will be automatically applied to resources being created for the specified resource type.
        tagPropagationConfigurations: {TagPropagationConfiguration}
    end

    record CreateTemplateOutput
        -- A unique identifier of a template.
        templateId: string
        -- The Amazon Resource Name (ARN) of the newly created template.
        templateArn: string
    end

    record DeleteTemplateInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier of a template.
        templateId: string
    end

    record DeleteTemplateOutput
    end

    record GetTemplateInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier of a template.
        templateId: string
    end

    record GetTemplateOutput
        -- A unique identifier of a template.
        templateId: string
        -- The Amazon Resource Name (ARN) of the template.
        templateArn: string
        -- The name of the template.
        name: string
        -- A brief description of the template.
        description: string
        -- Configuration of layouts associated to the template.
        layoutConfiguration: LayoutConfiguration
        -- A list of fields that must contain a value for a case to be successfully created with this template.
        requiredFields: {RequiredField}
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
        -- The status of the template.
        status: string
        -- Denotes whether or not the resource has been deleted.
        deleted: boolean
        -- Timestamp at which the resource was created.
        createdTime: number
        -- Timestamp at which the resource was created or last modified.
        lastModifiedTime: number
        -- A list of case rules (also known as case field conditions) on a template.
        rules: {TemplateRule}
        -- Defines tag propagation configuration for resources created within a domain. Tags specified here will be automatically applied to resources being created for the specified resource type.
        tagPropagationConfigurations: {TagPropagationConfiguration}
    end

    record ListTemplatesInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- A list of status values to filter on.
        status: {string}
    end

    -- Template summary information.
    record TemplateSummary
        -- The unique identifier for the template.
        templateId: string
        -- The Amazon Resource Name (ARN) of the template.
        templateArn: string
        -- The template name.
        name: string
        -- The status of the template.
        status: string
        -- Defines tag propagation configuration for resources created within a domain. Tags specified here will be automatically applied to resources being created for the specified resource type.
        tagPropagationConfigurations: {TagPropagationConfiguration}
    end

    record ListTemplatesOutput
        -- List of template summary objects.
        templates: {TemplateSummary}
        -- The token for the next set of results. This is null if there are no more results to return.
        nextToken: string
    end

    record UpdateTemplateInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier for the template.
        templateId: string
        -- The name of the template. It must be unique per domain.
        name: string
        -- A brief description of the template.
        description: string
        -- Configuration of layouts associated to the template.
        layoutConfiguration: LayoutConfiguration
        -- A list of fields that must contain a value for a case to be successfully created with this template.
        requiredFields: {RequiredField}
        -- The status of the template.
        status: string
        -- A list of case rules (also known as case field conditions) on a template.
        rules: {TemplateRule}
        -- Defines tag propagation configuration for resources created within a domain. Tags specified here will be automatically applied to resources being created for the specified resource type.
        tagPropagationConfigurations: {TagPropagationConfiguration}
    end

    record UpdateTemplateOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN)
        arn: string
        -- List of tag keys.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    -- A filter for cases. Only one value can be provided.
    record CaseFilter
        -- A list of fields to filter on.
        field: FieldFilter
        -- A list of tags to filter on.
        tag: TagFilter
        -- Provides "and all" filtering.
        andAll: {CaseFilter}
        -- Provides "or all" filtering.
        orAll: {CaseFilter}
    end

    -- A filter for fields in Custom type related items. Only one value can be provided.
    record CustomFieldsFilter
        field: FieldFilter
        -- Provides "and all" filtering.
        andAll: {CustomFieldsFilter}
        -- Provides "or all" filtering.
        orAll: {CustomFieldsFilter}
    end

    -- Boolean condition for a rule. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record BooleanCondition
        -- Tests that operandOne is equal to operandTwo.
        equalTo: BooleanOperands
        -- Tests that operandOne is not equal to operandTwo.
        notEqualTo: BooleanOperands
        -- Combines multiple conditions with AND operator. All conditions must be true for the compound condition to be true.
        andAll: CompoundCondition
        -- Combines multiple conditions with OR operator. At least one condition must be true for the compound condition to be true.
        orAll: CompoundCondition
    end

    -- A compound condition that combines multiple boolean conditions using logical operators. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record CompoundCondition
        -- The list of conditions to combine using the logical operator.
        conditions: {BooleanCondition}
    end

    -- A filter for related items of type Custom.
    record CustomFilter
        -- Filter conditions for custom fields.
        fields: CustomFieldsFilter
    end

    -- A rule that controls field visibility based on conditions. Fields can be shown or hidden dynamically based on values in other fields.
    record HiddenCaseRule
        -- Whether the field is hidden when no conditions match.
        defaultValue: boolean
        -- A list of conditions that determine field visibility.
        conditions: {BooleanCondition}
    end

    -- Required rule type, used to indicate whether a field is required. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record RequiredCaseRule
        -- The value of the rule (that is, whether the field is required) should none of the conditions evaluate to true.
        defaultValue: boolean
        -- List of conditions for the required rule; the first condition to evaluate to true dictates the value of the rule.
        conditions: {BooleanCondition}
    end

    record SearchCasesInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The maximum number of cases to return. When no value is provided, 25 is the default.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- A word or phrase used to perform a quick search.
        searchTerm: string
        -- A list of filter objects.
        filter: CaseFilter
        -- A list of sorts where each sort specifies a field and their sort order to be applied to the results.
        sorts: {Sort}
        -- The list of field identifiers to be returned as part of the response.
        fields: {FieldIdentifier}
    end

    -- The list of types of related items and their parameters to use for filtering.
    record RelatedItemTypeFilter
        -- A filter for related items of type Contact.
        contact: ContactFilter
        -- A filter for related items of type Comment.
        comment: CommentFilter
        -- A filter for related items of this type of File.
        file: FileFilter
        -- Filter for related items of type SLA.
        sla: SlaFilter
        -- Represents the Amazon Connect case to be created as a related item.
        connectCase: ConnectCaseFilter
        -- Represents the content of a Custom type related item.
        custom: CustomFilter
    end

    record SearchAllRelatedItemsInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The list of types of related items and their parameters to use for filtering. The filters work as an OR condition: caller gets back related items that match any of the specified filter types.
        filters: {RelatedItemTypeFilter}
        -- A structured set of sort terms to specify the order in which related items should be returned. Supports sorting by association time or case ID. The sorts work in the order specified: first sort term takes precedence over subsequent terms.
        sorts: {SearchAllRelatedItemsSort}
    end

    record SearchRelatedItemsInput
        -- The unique identifier of the Cases domain.
        domainId: string
        -- A unique identifier of the case.
        caseId: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The list of types of related items and their parameters to use for filtering.
        filters: {RelatedItemTypeFilter}
    end

    -- Represents what rule type should take place, under what conditions. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record CaseRuleDetails
        -- Required rule type, used to indicate whether a field is required.
        required: RequiredCaseRule
        -- Which options are available in a child field based on the selected value in a parent field.
        fieldOptions: FieldOptionsCaseRule
        -- Whether a field is visible, based on values in other fields.
        hidden: HiddenCaseRule
    end

    record CreateCaseRuleInput
        -- Unique identifier of a Cases domain.
        domainId: string
        -- Name of the case rule.
        name: string
        -- The description of a case rule.
        description: string
        -- Represents what rule type should take place, under what conditions.
        rule: CaseRuleDetails
    end

    -- Detailed case rule information. In the Amazon Connect admin website, case rules are known as case field conditions. For more information about case field conditions, see Add case field conditions to a case template.
    record GetCaseRuleResponse
        -- Unique identifier of a case rule.
        caseRuleId: string
        -- Name of the case rule.
        name: string
        -- The Amazon Resource Name (ARN) of the case rule.
        caseRuleArn: string
        -- Represents what rule type should take place, under what conditions.
        rule: CaseRuleDetails
        -- Description of a case rule.
        description: string
        -- Indicates whether the resource has been deleted.
        deleted: boolean
        -- Timestamp when the resource was created.
        createdTime: number
        -- Timestamp when the resource was created or last modified.
        lastModifiedTime: number
        -- A map of of key-value pairs that represent tags on a resource. Tags are used to organize, track, or control access for this resource.
        tags: {string : string}
    end

    record UpdateCaseRuleInput
        -- Unique identifier of a Cases domain.
        domainId: string
        -- Unique identifier of a case rule.
        caseRuleId: string
        -- Name of the case rule.
        name: string
        -- Description of a case rule.
        description: string
        -- Represents what rule type should take place, under what conditions.
        rule: CaseRuleDetails
    end

    record BatchGetCaseRuleOutput
        -- A list of detailed case rule information.
        caseRules: {GetCaseRuleResponse}
        -- A list of case rule errors.
        errors: {CaseRuleError}
        -- A list of unprocessed case rule identifiers.
        unprocessedCaseRules: {string}
    end

end

return M
