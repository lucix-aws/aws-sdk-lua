-- Code generated by lua-client-codegen. DO NOT EDIT.

local waiter = require("smithy.waiter")

local M = {}

--- Wait until ApplicationActive.
function M.wait_until_application_active(client, input, options)
    return waiter.wait(client, "getApplication", input, {
        min_delay = 5,
        max_delay = 120,
        acceptors = {
            {
                state = "success",
                matcher = {
                    output = {
                        path = "status",
                        expected = "ACTIVE",
                        comparator = "stringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "status",
                        expected = "CREATE_FAILED",
                        comparator = "stringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "status",
                        expected = "DELETE_FAILED",
                        comparator = "stringEquals",
                    },
                },
            },
        },
    }, options)
end

return M
