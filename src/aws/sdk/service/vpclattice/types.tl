-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- The user does not have sufficient access to perform this action.
    record AccessDeniedException
        message: string
    end

    -- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
    record ConflictException
        message: string
        -- The resource ID.
        resourceId: string
        -- The resource type.
        resourceType: string
    end

    enum ServiceNetworkLogType
        "SERVICE"
        "RESOURCE"
    end

    record CreateAccessLogSubscriptionInput
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The ID or ARN of the service network or service.
        resourceIdentifier: string
        -- The Amazon Resource Name (ARN) of the destination. The supported destination types are CloudWatch Log groups, Kinesis Data Firehose delivery streams, and Amazon S3 buckets.
        destinationArn: string
        -- The type of log that monitors your Amazon VPC Lattice service networks.
        serviceNetworkLogType: string
        -- The tags for the access log subscription.
        tags: {string : string}
    end

    record CreateAccessLogSubscriptionOutput
        -- The ID of the access log subscription.
        id: string
        -- The Amazon Resource Name (ARN) of the access log subscription.
        arn: string
        -- The ID of the service network or service.
        resourceId: string
        -- The Amazon Resource Name (ARN) of the service network or service.
        resourceArn: string
        -- The type of log that monitors your Amazon VPC Lattice service networks.
        serviceNetworkLogType: string
        -- The Amazon Resource Name (ARN) of the log destination.
        destinationArn: string
    end

    -- An unexpected error occurred while processing the request.
    record InternalServerException
        message: string
        -- The number of seconds to wait before retrying.
        retryAfterSeconds: number
    end

    -- The request references a resource that does not exist.
    record ResourceNotFoundException
        message: string
        -- The resource ID.
        resourceId: string
        -- The resource type.
        resourceType: string
    end

    -- The limit on the number of requests per second was exceeded.
    record ThrottlingException
        message: string
        -- The service code.
        serviceCode: string
        -- The ID of the service quota that was exceeded.
        quotaCode: string
        -- The number of seconds to wait before retrying.
        retryAfterSeconds: number
    end

    -- Describes a validation failure.
    record ValidationExceptionField
        -- The name of the validation exception.
        name: string
        -- Additional information about why the validation failed.
        message: string
    end

    enum ValidationExceptionReason
        "unknownOperation"
        "cannotParse"
        "fieldValidationFailed"
        "other"
    end

    -- The input does not satisfy the constraints specified by an Amazon Web Services service.
    record ValidationException
        message: string
        -- The reason.
        reason: string
        -- The fields that failed validation.
        fieldList: {ValidationExceptionField}
    end

    record DeleteAccessLogSubscriptionInput
        -- The ID or ARN of the access log subscription.
        accessLogSubscriptionIdentifier: string
    end

    record DeleteAccessLogSubscriptionOutput
    end

    record GetAccessLogSubscriptionInput
        -- The ID or ARN of the access log subscription.
        accessLogSubscriptionIdentifier: string
    end

    record GetAccessLogSubscriptionOutput
        -- The ID of the access log subscription.
        id: string
        -- The Amazon Resource Name (ARN) of the access log subscription.
        arn: string
        -- The ID of the service network or service.
        resourceId: string
        -- The Amazon Resource Name (ARN) of the service network or service.
        resourceArn: string
        -- The Amazon Resource Name (ARN) of the access log destination.
        destinationArn: string
        -- The log type for the service network.
        serviceNetworkLogType: string
        -- The date and time that the access log subscription was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the access log subscription was last updated, in ISO-8601 format.
        lastUpdatedAt: number
    end

    record ListAccessLogSubscriptionsInput
        -- The ID or ARN of the service network or service.
        resourceIdentifier: string
        -- The maximum number of results to return.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about an access log subscription.
    record AccessLogSubscriptionSummary
        -- The ID of the access log subscription.
        id: string
        -- The Amazon Resource Name (ARN) of the access log subscription
        arn: string
        -- The ID of the service or service network.
        resourceId: string
        -- The Amazon Resource Name (ARN) of the service or service network.
        resourceArn: string
        -- The Amazon Resource Name (ARN) of the destination.
        destinationArn: string
        -- Log type of the service network.
        serviceNetworkLogType: string
        -- The date and time that the access log subscription was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the access log subscription was last updated, in ISO-8601 format.
        lastUpdatedAt: number
    end

    record ListAccessLogSubscriptionsOutput
        -- Information about the access log subscriptions.
        items: {AccessLogSubscriptionSummary}
        -- A pagination token for the next page of results.
        nextToken: string
    end

    record UpdateAccessLogSubscriptionInput
        -- The ID or ARN of the access log subscription.
        accessLogSubscriptionIdentifier: string
        -- The Amazon Resource Name (ARN) of the access log destination.
        destinationArn: string
    end

    record UpdateAccessLogSubscriptionOutput
        -- The ID of the access log subscription.
        id: string
        -- The Amazon Resource Name (ARN) of the access log subscription.
        arn: string
        -- The ID of the resource.
        resourceId: string
        -- The Amazon Resource Name (ARN) of the access log subscription.
        resourceArn: string
        -- The Amazon Resource Name (ARN) of the access log destination.
        destinationArn: string
    end

    -- The Amazon Resource Name (ARN) of the resource.
    record ArnResource
        -- The Amazon Resource Name (ARN) of the resource.
        arn: string
    end

    enum AuthPolicyState
        "Active"
        "Inactive"
    end

    enum AuthType
        "NONE"
        "AWS_IAM"
    end

    -- Describes an action that returns a custom HTTP response.
    record FixedResponseAction
        -- The HTTP response code. Only 404 and 500 status codes are supported.
        statusCode: number
    end

    -- Describes the weight of a target group.
    record WeightedTargetGroup
        -- The ID or ARN of the target group.
        targetGroupIdentifier: string
        -- Only required if you specify multiple target groups for a forward action. The weight determines how requests are distributed to the target group. For example, if you specify two target groups, each with a weight of 10, each target group receives half the requests. If you specify two target groups, one with a weight of 10 and the other with a weight of 20, the target group with a weight of 20 receives twice as many requests as the other target group. If there's only one target group specified, then the default value is 100.
        weight: number
    end

    -- Describes a forward action. You can use forward actions to route requests to one or more target groups.
    record ForwardAction
        -- The target groups. Traffic matching the rule is forwarded to the specified target groups. With forward actions, you can assign a weight that controls the prioritization and selection of each target group. This means that requests are distributed to individual target groups based on their weights. For example, if two target groups have the same weight, each target group receives half of the traffic. The default value is 1. This means that if only one target group is provided, there is no need to set the weight; 100% of the traffic goes to that target group.
        targetGroups: {WeightedTargetGroup}
    end

    -- Describes the action for a rule.
    record RuleAction
        -- The forward action. Traffic that matches the rule is forwarded to the specified target groups.
        forward: ForwardAction
        -- The fixed response action. The rule returns a custom HTTP response.
        fixedResponse: FixedResponseAction
    end

    -- Describes a header match type.
    record HeaderMatchType
        -- An exact type match.
        exact: string
        -- A prefix type match. Matches the value with the prefix.
        prefix: string
        -- A contains type match.
        contains: string
    end

    -- Describes the constraints for a header match. Matches incoming requests with rule based on request header value before applying rule action.
    record HeaderMatch
        -- The name of the header.
        name: string
        -- The header match type.
        match: HeaderMatchType
        -- Indicates whether the match is case sensitive.
        caseSensitive: boolean
    end

    -- Describes a path match type. Each rule can include only one of the following types of paths.
    record PathMatchType
        -- An exact match of the path.
        exact: string
        -- A prefix match of the path.
        prefix: string
    end

    -- Describes the conditions that can be applied when matching a path for incoming requests.
    record PathMatch
        -- The type of path match.
        match: PathMatchType
        -- Indicates whether the match is case sensitive.
        caseSensitive: boolean
    end

    -- Describes criteria that can be applied to incoming requests.
    record HttpMatch
        -- The HTTP method type.
        method: string
        -- The path match.
        pathMatch: PathMatch
        -- The header matches. Matches incoming requests with rule based on request header value before applying rule action.
        headerMatches: {HeaderMatch}
    end

    -- Describes a rule match.
    record RuleMatch
        -- The HTTP criteria that a rule must match.
        httpMatch: HttpMatch
    end

    -- Describes a rule update.
    record RuleUpdate
        -- The ID or ARN of the rule.
        ruleIdentifier: string
        -- The rule match.
        match: RuleMatch
        -- The rule priority. A listener can't have multiple rules with the same priority.
        priority: number
        -- The rule action.
        action: RuleAction
    end

    record BatchUpdateRuleInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The ID or ARN of the listener.
        listenerIdentifier: string
        -- The rules for the specified listener.
        rules: {RuleUpdate}
    end

    -- Describes a successful rule update.
    record RuleUpdateSuccess
        -- The Amazon Resource Name (ARN) of the listener.
        arn: string
        -- The ID of the listener.
        id: string
        -- The name of the listener.
        name: string
        -- Indicates whether this is the default rule.
        isDefault: boolean
        -- The rule match.
        match: RuleMatch
        -- The rule priority.
        priority: number
        -- The action for the rule.
        action: RuleAction
    end

    -- Describes a rule update that failed.
    record RuleUpdateFailure
        -- The ID or ARN of the rule.
        ruleIdentifier: string
        -- The failure code.
        failureCode: string
        -- The failure message.
        failureMessage: string
    end

    record BatchUpdateRuleOutput
        -- The rules that were successfully updated.
        successful: {RuleUpdateSuccess}
        -- The rules that the operation couldn't update.
        unsuccessful: {RuleUpdateFailure}
    end

    enum ListenerProtocol
        "HTTP"
        "HTTPS"
        "TLS_PASSTHROUGH"
    end

    record CreateListenerInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The name of the listener. A listener name must be unique within a service. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
        name: string
        -- The listener protocol.
        protocol: string
        -- The listener port. You can specify a value from 1 to 65535. For HTTP, the default is 80. For HTTPS, the default is 443.
        port: number
        -- The action for the default rule. Each listener has a default rule. The default rule is used if no other rules match.
        defaultAction: RuleAction
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The tags for the listener.
        tags: {string : string}
    end

    record CreateListenerOutput
        -- The Amazon Resource Name (ARN) of the listener.
        arn: string
        -- The ID of the listener.
        id: string
        -- The name of the listener.
        name: string
        -- The protocol of the listener.
        protocol: string
        -- The port number of the listener.
        port: number
        -- The Amazon Resource Name (ARN) of the service.
        serviceArn: string
        -- The ID of the service.
        serviceId: string
        -- The action for the default rule.
        defaultAction: RuleAction
    end

    -- The request would cause a service quota to be exceeded.
    record ServiceQuotaExceededException
        message: string
        -- The resource ID.
        resourceId: string
        -- The resource type.
        resourceType: string
        -- The service code.
        serviceCode: string
        -- The ID of the service quota that was exceeded.
        quotaCode: string
    end

    enum ProtocolType
        "TCP"
    end

    enum ResourceConfigurationIpAddressType
        "IPV4"
        "IPV6"
        "DUALSTACK"
    end

    -- The DNS name of the resource.
    record DnsResource
        -- The domain name of the resource.
        domainName: string
        -- The type of IP address. Dualstack is currently not supported.
        ipAddressType: string
    end

    -- Describes an IP resource.
    record IpResource
        -- The IP address of the IP resource.
        ipAddress: string
    end

    -- Describes a resource configuration.
    record ResourceConfigurationDefinition
        -- The DNS name of the resource.
        dnsResource: DnsResource
        -- The IP resource.
        ipResource: IpResource
        -- The Amazon Resource Name (ARN) of the resource.
        arnResource: ArnResource
    end

    enum ResourceConfigurationType
        "GROUP"
        "CHILD"
        "SINGLE"
        "ARN"
    end

    record CreateResourceConfigurationInput
        -- The name of the resource configuration. The name must be unique within the account. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
        name: string
        -- (SINGLE, GROUP, CHILD) The TCP port ranges that a consumer can use to access a resource configuration (for example: 1-65535). You can separate port ranges using commas (for example: 1,2,22-30).
        portRanges: {string}
        -- (SINGLE, GROUP) The protocol accepted by the resource configuration.
        protocol: string
        -- (SINGLE, GROUP, ARN) The ID or ARN of the resource gateway used to connect to the resource configuration. For a child resource configuration, this value is inherited from the parent resource configuration.
        resourceGatewayIdentifier: string
        -- (CHILD) The ID or ARN of the parent resource configuration of type GROUP. This is used to associate a child resource configuration with a group resource configuration.
        resourceConfigurationGroupIdentifier: string
        -- Identifies the resource configuration in one of the following ways: Amazon Resource Name (ARN) - Supported resource-types that are provisioned by Amazon Web Services services, such as RDS databases, can be identified by their ARN. Domain name - Any domain name that is publicly resolvable. IP address - For IPv4 and IPv6, only IP addresses in the VPC are supported.
        resourceConfigurationDefinition: ResourceConfigurationDefinition
        -- (SINGLE, GROUP, ARN) Specifies whether the resource configuration can be associated with a sharable service network. The default is false.
        allowAssociationToShareableServiceNetwork: boolean
        -- A custom domain name for your resource configuration. Additionally, provide a DomainVerificationID to prove your ownership of a domain.
        customDomainName: string
        -- (GROUP) The group domain for a group resource configuration. Any domains that you create for the child resource are subdomains of the group domain. Child resources inherit the verification status of the domain.
        groupDomain: string
        -- The domain verification ID of your verified custom domain name. If you don't provide an ID, you must configure the DNS settings yourself.
        domainVerificationIdentifier: string
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The tags for the resource configuration.
        tags: {string : string}
    end

    enum ResourceConfigurationStatus
        "ACTIVE"
        "CREATE_IN_PROGRESS"
        "UPDATE_IN_PROGRESS"
        "DELETE_IN_PROGRESS"
        "CREATE_FAILED"
        "UPDATE_FAILED"
        "DELETE_FAILED"
    end

    record CreateResourceConfigurationOutput
        -- The ID of the resource configuration.
        id: string
        -- The name of the resource configuration.
        name: string
        -- The Amazon Resource Name (ARN) of the resource configuration.
        arn: string
        -- The ID of the resource gateway associated with the resource configuration.
        resourceGatewayId: string
        -- The ID of the parent resource configuration of type GROUP.
        resourceConfigurationGroupId: string
        -- The port range.
        portRanges: {string}
        -- The protocol.
        protocol: string
        -- The current status of the resource configuration.
        status: string
        -- Identifies the resource configuration in one of the following ways: Amazon Resource Name (ARN) - Supported resource-types that are provisioned by Amazon Web Services services, such as RDS databases, can be identified by their ARN. Domain name - Any domain name that is publicly resolvable. IP address - For IPv4 and IPv6, only IP addresses in the VPC are supported.
        resourceConfigurationDefinition: ResourceConfigurationDefinition
        -- Specifies whether the resource configuration can be associated with a sharable service network.
        allowAssociationToShareableServiceNetwork: boolean
        -- The date and time that the resource configuration was created, in ISO-8601 format.
        createdAt: number
        -- The reason that the request failed.
        failureReason: string
        -- The custom domain name for your resource configuration.
        customDomainName: string
        -- The domain name verification ID.
        domainVerificationId: string
        -- (GROUP) The group domain for a group resource configuration. Any domains that you create for the child resource are subdomains of the group domain. Child resources inherit the verification status of the domain.
        groupDomain: string
        -- The verification ID ARN
        domainVerificationArn: string
    end

    enum ResourceGatewayIpAddressType
        "IPV4"
        "IPV6"
        "DUALSTACK"
    end

    enum ResourceConfigDnsResolution
        "IN_VPC"
        "PUBLIC"
    end

    record CreateResourceGatewayInput
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The name of the resource gateway.
        name: string
        -- The ID of the VPC for the resource gateway.
        vpcIdentifier: string
        -- The IDs of the VPC subnets in which to create the resource gateway.
        subnetIds: {string}
        -- The IDs of the security groups to apply to the resource gateway. The security groups must be in the same VPC.
        securityGroupIds: {string}
        -- A resource gateway can have IPv4, IPv6 or dualstack addresses. The IP address type of a resource gateway must be compatible with the subnets of the resource gateway and the IP address type of the resource, as described here: IPv4Assign IPv4 addresses to your resource gateway network interfaces. This option is supported only if all selected subnets have IPv4 address ranges, and the resource also has an IPv4 address. IPv6Assign IPv6 addresses to your resource gateway network interfaces. This option is supported only if all selected subnets are IPv6 only subnets, and the resource also has an IPv6 address. DualstackAssign both IPv4 and IPv6 addresses to your resource gateway network interfaces. This option is supported only if all selected subnets have both IPv4 and IPv6 address ranges, and the resource either has an IPv4 or IPv6 address. The IP address type of the resource gateway is independent of the IP address type of the client or the VPC endpoint through which the resource is accessed.
        ipAddressType: string
        -- The number of IPv4 addresses in each ENI for the resource gateway.
        ipv4AddressesPerEni: number
        -- Indicates how DNS is resolved for resource configurations associated to this resource gateway. ResourceConfigDnsResolution is set at creation time and cannot be changed. IN_VPC - DNS resolution occurs privately within the resource gateway's VPC. DNS queries for resources behind this resource gateway resolve using the DNS resolvers defined in the VPC's DHCP option sets. Use this when your resource domain names are hosted in private Route 53 hosted zones or on-premises DNS servers reachable from the VPC. PUBLIC - DNS resolution occurs against public DNS resolvers. DNS queries for resources behind this resource gateway resolve using standard public DNS. Use this when your resource domain names are publicly resolvable.
        resourceConfigDnsResolution: string
        -- The tags for the resource gateway.
        tags: {string : string}
    end

    enum ResourceGatewayStatus
        "ACTIVE"
        "CREATE_IN_PROGRESS"
        "UPDATE_IN_PROGRESS"
        "DELETE_IN_PROGRESS"
        "CREATE_FAILED"
        "UPDATE_FAILED"
        "DELETE_FAILED"
    end

    record CreateResourceGatewayOutput
        -- The name of the resource gateway.
        name: string
        -- The ID of the resource gateway.
        id: string
        -- The Amazon Resource Name (ARN) of the resource gateway.
        arn: string
        -- The status of the resource gateway.
        status: string
        -- The ID of the VPC.
        vpcIdentifier: string
        -- The IDs of the resource gateway subnets.
        subnetIds: {string}
        -- The IDs of the security groups for the resource gateway.
        securityGroupIds: {string}
        -- The type of IP address for the resource gateway.
        ipAddressType: string
        -- The number of IPv4 addresses in each ENI for the resource gateway.
        ipv4AddressesPerEni: number
        -- The DNS resolution type for resource configurations that are associated with this resource gateway.
        resourceConfigDnsResolution: string
    end

    record CreateRuleInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The ID or ARN of the listener.
        listenerIdentifier: string
        -- The name of the rule. The name must be unique within the listener. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
        name: string
        -- The rule match.
        match: RuleMatch
        -- The priority assigned to the rule. Each rule for a specific listener must have a unique priority. The lower the priority number the higher the priority.
        priority: number
        -- The action for the default rule.
        action: RuleAction
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The tags for the rule.
        tags: {string : string}
    end

    record CreateRuleOutput
        -- The Amazon Resource Name (ARN) of the rule.
        arn: string
        -- The ID of the rule.
        id: string
        -- The name of the rule.
        name: string
        -- The rule match. The RuleMatch must be an HttpMatch. This means that the rule should be an exact match on HTTP constraints which are made up of the HTTP method, path, and header.
        match: RuleMatch
        -- The priority assigned to the rule. The lower the priority number the higher the priority.
        priority: number
        -- The rule action.
        action: RuleAction
    end

    record CreateServiceInput
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The name of the service. The name must be unique within the account. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
        name: string
        -- The tags for the service.
        tags: {string : string}
        -- The custom domain name of the service.
        customDomainName: string
        -- The Amazon Resource Name (ARN) of the certificate.
        certificateArn: string
        -- The type of IAM policy. NONE: The resource does not use an IAM policy. This is the default. AWS_IAM: The resource uses an IAM policy. When this type is used, auth is enabled and an auth policy is required.
        authType: string
        -- The amount of time, in seconds, that a connection can remain idle (no data sent) before VPC Lattice closes it. The valid range is 60 to 600 seconds. If you don't specify a value, the default is 60 seconds. This setting does not change the maximum connection duration of 10 minutes; connections are still closed when they reach that limit.
        idleTimeoutSeconds: number
    end

    -- Describes the DNS information of a service.
    record DnsEntry
        -- The domain name of the service.
        domainName: string
        -- The ID of the hosted zone.
        hostedZoneId: string
    end

    enum ServiceStatus
        "ACTIVE"
        "CREATE_IN_PROGRESS"
        "DELETE_IN_PROGRESS"
        "CREATE_FAILED"
        "DELETE_FAILED"
    end

    record CreateServiceOutput
        -- The ID of the service.
        id: string
        -- The Amazon Resource Name (ARN) of the service.
        arn: string
        -- The name of the service.
        name: string
        -- The custom domain name of the service.
        customDomainName: string
        -- The Amazon Resource Name (ARN) of the certificate.
        certificateArn: string
        -- The status. If the status is CREATE_FAILED, you must delete and recreate the service.
        status: string
        -- The type of IAM policy.
        authType: string
        -- The amount of time, in seconds, that a connection can remain idle before VPC Lattice closes it.
        idleTimeoutSeconds: number
        -- The public DNS name of the service.
        dnsEntry: DnsEntry
    end

    -- Specifies if the service network should be enabled for sharing.
    record SharingConfig
        -- Specifies if the service network is enabled for sharing.
        enabled: boolean
    end

    record CreateServiceNetworkInput
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The name of the service network. The name must be unique to the account. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
        name: string
        -- The type of IAM policy. NONE: The resource does not use an IAM policy. This is the default. AWS_IAM: The resource uses an IAM policy. When this type is used, auth is enabled and an auth policy is required.
        authType: string
        -- The tags for the service network.
        tags: {string : string}
        -- Specify if the service network should be enabled for sharing.
        sharingConfig: SharingConfig
    end

    record CreateServiceNetworkOutput
        -- The ID of the service network.
        id: string
        -- The name of the service network.
        name: string
        -- The Amazon Resource Name (ARN) of the service network.
        arn: string
        -- Specifies if the service network is enabled for sharing.
        sharingConfig: SharingConfig
        -- The type of IAM policy.
        authType: string
    end

    record CreateServiceNetworkResourceAssociationInput
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The ID of the resource configuration to associate with the service network.
        resourceConfigurationIdentifier: string
        -- The ID of the service network to associate with the resource configuration.
        serviceNetworkIdentifier: string
        -- Indicates if private DNS is enabled for the service network resource association.
        privateDnsEnabled: boolean
        -- A key-value pair to associate with a resource.
        tags: {string : string}
    end

    enum ServiceNetworkResourceAssociationStatus
        "CREATE_IN_PROGRESS"
        "ACTIVE"
        "PARTIAL"
        "DELETE_IN_PROGRESS"
        "CREATE_FAILED"
        "DELETE_FAILED"
    end

    record CreateServiceNetworkResourceAssociationOutput
        -- The ID of the association.
        id: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The status of the association.
        status: string
        -- The ID of the account that created the association.
        createdBy: string
        -- Indicates if private DNS is is enabled for the service network resource association.
        privateDnsEnabled: boolean
    end

    record CreateServiceNetworkServiceAssociationInput
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The ID or ARN of the service network. You must use an ARN if the resources are in different accounts.
        serviceNetworkIdentifier: string
        -- The tags for the association.
        tags: {string : string}
    end

    enum ServiceNetworkServiceAssociationStatus
        "CREATE_IN_PROGRESS"
        "ACTIVE"
        "DELETE_IN_PROGRESS"
        "CREATE_FAILED"
        "DELETE_FAILED"
    end

    record CreateServiceNetworkServiceAssociationOutput
        -- The ID of the association.
        id: string
        -- The association status.
        status: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The account that created the association.
        createdBy: string
        -- The custom domain name of the service.
        customDomainName: string
        -- The DNS name of the service.
        dnsEntry: DnsEntry
    end

    enum PrivateDnsPreference
        "VERIFIED_DOMAINS_ONLY"
        "ALL_DOMAINS"
        "VERIFIED_DOMAINS_AND_SPECIFIED_DOMAINS"
        "SPECIFIED_DOMAINS_ONLY"
    end

    -- The DNS configuration options.
    record DnsOptions
        -- The preference for which private domains have a private hosted zone created for and associated with the specified VPC. Only supported when private DNS is enabled and when the VPC endpoint type is ServiceNetwork or Resource. ALL_DOMAINS - VPC Lattice provisions private hosted zones for all custom domain names. VERIFIED_DOMAINS_ONLY - VPC Lattice provisions a private hosted zone only if custom domain name has been verified by the provider. VERIFIED_DOMAINS_AND_SPECIFIED_DOMAINS - VPC Lattice provisions private hosted zones for all verified custom domain names and other domain names that the resource consumer specifies. The resource consumer specifies the domain names in the privateDnsSpecifiedDomains parameter. SPECIFIED_DOMAINS_ONLY - VPC Lattice provisions a private hosted zone for domain names specified by the resource consumer. The resource consumer specifies the domain names in the privateDnsSpecifiedDomains parameter.
        privateDnsPreference: string
        -- Indicates which of the private domains to create private hosted zones for and associate with the specified VPC. Only supported when private DNS is enabled and the private DNS preference is VERIFIED_DOMAINS_AND_SPECIFIED_DOMAINS or SPECIFIED_DOMAINS_ONLY.
        privateDnsSpecifiedDomains: {string}
    end

    record CreateServiceNetworkVpcAssociationInput
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The ID or ARN of the service network. You must use an ARN if the resources are in different accounts.
        serviceNetworkIdentifier: string
        -- The ID of the VPC.
        vpcIdentifier: string
        -- Indicates if private DNS is enabled for the VPC association.
        privateDnsEnabled: boolean
        -- The IDs of the security groups. Security groups aren't added by default. You can add a security group to apply network level controls to control which resources in a VPC are allowed to access the service network and its services. For more information, see Control traffic to resources using security groups in the Amazon VPC User Guide.
        securityGroupIds: {string}
        -- The tags for the association.
        tags: {string : string}
        -- DNS options for the service network VPC association.
        dnsOptions: DnsOptions
    end

    enum ServiceNetworkVpcAssociationStatus
        "CREATE_IN_PROGRESS"
        "ACTIVE"
        "UPDATE_IN_PROGRESS"
        "DELETE_IN_PROGRESS"
        "CREATE_FAILED"
        "DELETE_FAILED"
        "UPDATE_FAILED"
    end

    record CreateServiceNetworkVpcAssociationOutput
        -- The ID of the association.
        id: string
        -- The association status.
        status: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The account that created the association.
        createdBy: string
        -- The IDs of the security groups.
        securityGroupIds: {string}
        -- Indicates if private DNS is enabled for the VPC association.
        privateDnsEnabled: boolean
        dnsOptions: DnsOptions
    end

    -- Describes the codes to use when checking for a successful response from a target for health checks.
    record Matcher
        -- The HTTP code to use when checking for a successful response from a target.
        httpCode: string
    end

    enum TargetGroupProtocol
        "HTTP"
        "HTTPS"
        "TCP"
    end

    enum HealthCheckProtocolVersion
        "HTTP1"
        "HTTP2"
    end

    -- Describes the health check configuration of a target group. Health check configurations aren't used for target groups of type LAMBDA or ALB.
    record HealthCheckConfig
        -- Indicates whether health checking is enabled.
        enabled: boolean
        -- The protocol used when performing health checks on targets. The possible protocols are HTTP and HTTPS. The default is HTTP.
        protocol: string
        -- The protocol version used when performing health checks on targets. The possible protocol versions are HTTP1 and HTTP2.
        protocolVersion: string
        -- The port used when performing health checks on targets. The default setting is the port that a target receives traffic on.
        port: number
        -- The destination for health checks on the targets. If the protocol version is HTTP/1.1 or HTTP/2, specify a valid URI (for example, /path?query). The default path is /. Health checks are not supported if the protocol version is gRPC, however, you can choose HTTP/1.1 or HTTP/2 and specify a valid URI.
        path: string
        -- The approximate amount of time, in seconds, between health checks of an individual target. The range is 5–300 seconds. The default is 30 seconds.
        healthCheckIntervalSeconds: number
        -- The amount of time, in seconds, to wait before reporting a target as unhealthy. The range is 1–120 seconds. The default is 5 seconds.
        healthCheckTimeoutSeconds: number
        -- The number of consecutive successful health checks required before considering an unhealthy target healthy. The range is 2–10. The default is 5.
        healthyThresholdCount: number
        -- The number of consecutive failed health checks required before considering a target unhealthy. The range is 2–10. The default is 2.
        unhealthyThresholdCount: number
        -- The codes to use when checking for a successful response from a target.
        matcher: Matcher
    end

    enum IpAddressType
        "IPV4"
        "IPV6"
    end

    enum LambdaEventStructureVersion
        "V1"
        "V2"
    end

    enum TargetGroupProtocolVersion
        "HTTP1"
        "HTTP2"
        "GRPC"
    end

    -- Describes the configuration of a target group. For more information, see Target groups in the Amazon VPC Lattice User Guide.
    record TargetGroupConfig
        -- The port on which the targets are listening. For HTTP, the default is 80. For HTTPS, the default is 443. Not supported if the target group type is LAMBDA.
        port: number
        -- The protocol to use for routing traffic to the targets. The default is the protocol of the target group. Not supported if the target group type is LAMBDA.
        protocol: string
        -- The protocol version. The default is HTTP1. Not supported if the target group type is LAMBDA.
        protocolVersion: string
        -- The type of IP address used for the target group. Supported only if the target group type is IP. The default is IPV4.
        ipAddressType: string
        -- The ID of the VPC. Not supported if the target group type is LAMBDA.
        vpcIdentifier: string
        -- The health check configuration. Not supported if the target group type is LAMBDA or ALB.
        healthCheck: HealthCheckConfig
        -- The version of the event structure that your Lambda function receives. Supported only if the target group type is LAMBDA. The default is V1.
        lambdaEventStructureVersion: string
    end

    enum TargetGroupType
        "IP"
        "LAMBDA"
        "INSTANCE"
        "ALB"
    end

    record CreateTargetGroupInput
        -- The name of the target group. The name must be unique within the account. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
        name: string
        -- The target group configuration.
        config: TargetGroupConfig
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The tags for the target group.
        tags: {string : string}
    end

    enum TargetGroupStatus
        "CREATE_IN_PROGRESS"
        "ACTIVE"
        "DELETE_IN_PROGRESS"
        "CREATE_FAILED"
        "DELETE_FAILED"
    end

    record CreateTargetGroupOutput
        -- The ID of the target group.
        id: string
        -- The Amazon Resource Name (ARN) of the target group.
        arn: string
        -- The name of the target group.
        name: string
        -- The target group configuration.
        config: TargetGroupConfig
        -- The status. You can retry the operation if the status is CREATE_FAILED. However, if you retry it while the status is CREATE_IN_PROGRESS, there is no change in the status.
        status: string
    end

    record DeleteAuthPolicyInput
        -- The ID or ARN of the resource.
        resourceIdentifier: string
    end

    record DeleteAuthPolicyOutput
    end

    record DeleteDomainVerificationInput
        -- The ID of the domain verification to delete.
        domainVerificationIdentifier: string
    end

    record DeleteDomainVerificationOutput
    end

    record DeleteListenerInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The ID or ARN of the listener.
        listenerIdentifier: string
    end

    record DeleteListenerOutput
    end

    record DeleteResourceConfigurationInput
        -- The ID or ARN of the resource configuration.
        resourceConfigurationIdentifier: string
    end

    record DeleteResourceConfigurationOutput
    end

    record DeleteResourceEndpointAssociationInput
        -- The ID or ARN of the association.
        resourceEndpointAssociationIdentifier: string
    end

    record DeleteResourceEndpointAssociationOutput
        -- The ID of the association.
        id: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The ID of the resource configuration.
        resourceConfigurationId: string
        -- The Amazon Resource Name (ARN) of the resource configuration associated with the VPC endpoint of type resource.
        resourceConfigurationArn: string
        -- The ID of the resource VPC endpoint that is associated with the resource configuration.
        vpcEndpointId: string
    end

    record DeleteResourceGatewayInput
        -- The ID or ARN of the resource gateway.
        resourceGatewayIdentifier: string
    end

    record DeleteResourceGatewayOutput
        -- The ID of the resource gateway.
        id: string
        -- The Amazon Resource Name (ARN) of the resource gateway.
        arn: string
        -- The name of the resource gateway.
        name: string
        -- The status of the resource gateway.
        status: string
    end

    record DeleteResourcePolicyInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
    end

    record DeleteResourcePolicyOutput
    end

    record DeleteRuleInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The ID or ARN of the listener.
        listenerIdentifier: string
        -- The ID or ARN of the rule.
        ruleIdentifier: string
    end

    record DeleteRuleOutput
    end

    record DeleteServiceInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
    end

    record DeleteServiceOutput
        -- The ID of the service.
        id: string
        -- The Amazon Resource Name (ARN) of the service.
        arn: string
        -- The name of the service.
        name: string
        -- The status. You can retry the operation if the status is DELETE_FAILED. However, if you retry it while the status is DELETE_IN_PROGRESS, the status doesn't change.
        status: string
    end

    record DeleteServiceNetworkInput
        -- The ID or ARN of the service network.
        serviceNetworkIdentifier: string
    end

    record DeleteServiceNetworkOutput
    end

    record DeleteServiceNetworkResourceAssociationInput
        -- The ID of the association.
        serviceNetworkResourceAssociationIdentifier: string
    end

    record DeleteServiceNetworkResourceAssociationOutput
        -- The ID of the association.
        id: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The status of the association.
        status: string
    end

    record DeleteServiceNetworkServiceAssociationInput
        -- The ID or ARN of the association.
        serviceNetworkServiceAssociationIdentifier: string
    end

    record DeleteServiceNetworkServiceAssociationOutput
        -- The ID of the association.
        id: string
        -- The status. You can retry the operation if the status is DELETE_FAILED. However, if you retry it when the status is DELETE_IN_PROGRESS, there is no change in the status.
        status: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
    end

    record DeleteServiceNetworkVpcAssociationInput
        -- The ID or ARN of the association.
        serviceNetworkVpcAssociationIdentifier: string
    end

    record DeleteServiceNetworkVpcAssociationOutput
        -- The ID of the association.
        id: string
        -- The status. You can retry the operation if the status is DELETE_FAILED. However, if you retry it while the status is DELETE_IN_PROGRESS, there is no change in the status.
        status: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
    end

    record DeleteTargetGroupInput
        -- The ID or ARN of the target group.
        targetGroupIdentifier: string
    end

    record DeleteTargetGroupOutput
        -- The ID of the target group.
        id: string
        -- The Amazon Resource Name (ARN) of the target group.
        arn: string
        -- The status. You can retry the operation if the status is DELETE_FAILED. However, if you retry it while the status is DELETE_IN_PROGRESS, the status doesn't change.
        status: string
    end

    -- Describes a target.
    record Target
        -- The ID of the target. If the target group type is INSTANCE, this is an instance ID. If the target group type is IP, this is an IP address. If the target group type is LAMBDA, this is the ARN of a Lambda function. If the target group type is ALB, this is the ARN of an Application Load Balancer.
        id: string
        -- The port on which the target is listening. For HTTP, the default is 80. For HTTPS, the default is 443.
        port: number
    end

    record DeregisterTargetsInput
        -- The ID or ARN of the target group.
        targetGroupIdentifier: string
        -- The targets to deregister.
        targets: {Target}
    end

    -- Describes a target failure.
    record TargetFailure
        -- The ID of the target. If the target group type is INSTANCE, this is an instance ID. If the target group type is IP, this is an IP address. If the target group type is LAMBDA, this is the ARN of a Lambda function. If the target group type is ALB, this is the ARN of an Application Load Balancer.
        id: string
        -- The port on which the target is listening. This parameter doesn't apply if the target is a Lambda function.
        port: number
        -- The failure code.
        failureCode: string
        -- The failure message.
        failureMessage: string
    end

    record DeregisterTargetsOutput
        -- The targets that were successfully deregistered.
        successful: {Target}
        -- The targets that the operation couldn't deregister.
        unsuccessful: {TargetFailure}
    end

    record GetDomainVerificationInput
        -- The ID or ARN of the domain verification to retrieve.
        domainVerificationIdentifier: string
    end

    enum VerificationStatus
        "VERIFIED"
        "PENDING"
        "VERIFICATION_TIMED_OUT"
    end

    -- Configuration for TXT record-based domain verification method.
    record TxtMethodConfig
        -- The value that must be added to the TXT record for domain verification.
        value: string
        -- The name of the TXT record that must be created for domain verification.
        name: string
    end

    record GetDomainVerificationOutput
        -- The ID of the domain verification.
        id: string
        -- The Amazon Resource Name (ARN) of the domain verification.
        arn: string
        -- The domain name being verified.
        domainName: string
        -- The current status of the domain verification process.
        status: string
        -- The TXT record configuration used for domain verification.
        txtMethodConfig: TxtMethodConfig
        -- The date and time that the domain verification was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the domain was last successfully verified, in ISO-8601 format.
        lastVerifiedTime: number
        -- The tags associated with the domain verification.
        tags: {string : string}
    end

    record ListDomainVerificationsInput
        -- The maximum number of results to return.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about a domain verification.
    record DomainVerificationSummary
        -- The ID of the domain verification.
        id: string
        -- The Amazon Resource Name (ARN) of the domain verification.
        arn: string
        -- The domain name being verified.
        domainName: string
        -- The current status of the domain verification process.
        status: string
        -- The TXT record configuration used for domain verification.
        txtMethodConfig: TxtMethodConfig
        -- The date and time that the domain verification was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the domain was last successfully verified, in ISO-8601 format.
        lastVerifiedTime: number
        -- The tags associated with the domain verification.
        tags: {string : string}
    end

    record ListDomainVerificationsOutput
        -- Information about the domain verifications.
        items: {DomainVerificationSummary}
        -- A pagination token for the next page of results.
        nextToken: string
    end

    record StartDomainVerificationInput
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
        clientToken: string
        -- The domain name to verify ownership for.
        domainName: string
        -- The tags for the domain verification.
        tags: {string : string}
    end

    record StartDomainVerificationOutput
        -- The ID of the domain verification.
        id: string
        -- The Amazon Resource Name (ARN) of the domain verification.
        arn: string
        -- The domain name being verified.
        domainName: string
        -- The current status of the domain verification process.
        status: string
        -- The TXT record configuration used for domain verification.
        txtMethodConfig: TxtMethodConfig
    end

    record GetAuthPolicyInput
        -- The ID or ARN of the service network or service.
        resourceIdentifier: string
    end

    record GetAuthPolicyOutput
        -- The auth policy.
        policy: string
        -- The state of the auth policy. The auth policy is only active when the auth type is set to AWS_IAM. If you provide a policy, then authentication and authorization decisions are made based on this policy and the client's IAM policy. If the auth type is NONE, then any auth policy that you provide remains inactive. For more information, see Create a service network in the Amazon VPC Lattice User Guide.
        state: string
        -- The date and time that the auth policy was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the auth policy was last updated, in ISO-8601 format.
        lastUpdatedAt: number
    end

    record GetListenerInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The ID or ARN of the listener.
        listenerIdentifier: string
    end

    record GetListenerOutput
        -- The Amazon Resource Name (ARN) of the listener.
        arn: string
        -- The ID of the listener.
        id: string
        -- The name of the listener.
        name: string
        -- The listener protocol.
        protocol: string
        -- The listener port.
        port: number
        -- The Amazon Resource Name (ARN) of the service.
        serviceArn: string
        -- The ID of the service.
        serviceId: string
        -- The actions for the default listener rule.
        defaultAction: RuleAction
        -- The date and time that the listener was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the listener was last updated, in ISO-8601 format.
        lastUpdatedAt: number
    end

    record GetResourceConfigurationInput
        -- The ID of the resource configuration.
        resourceConfigurationIdentifier: string
    end

    record GetResourceConfigurationOutput
        -- The ID of the resource configuration.
        id: string
        -- The name of the resource configuration.
        name: string
        -- The Amazon Resource Name (ARN) of the resource configuration.
        arn: string
        -- The ID of the resource gateway used to connect to the resource configuration in a given VPC. You can specify the resource gateway identifier only for resource configurations with type SINGLE, GROUP, or ARN.
        resourceGatewayId: string
        -- The ID of the group resource configuration.
        resourceConfigurationGroupId: string
        -- Specifies whether the resource configuration is associated with a sharable service network.
        allowAssociationToShareableServiceNetwork: boolean
        -- The TCP port ranges that a consumer can use to access a resource configuration. You can separate port ranges with a comma. Example: 1-65535 or 1,2,22-30
        portRanges: {string}
        -- The TCP protocol accepted by the specified resource configuration.
        protocol: string
        -- The custom domain name of the resource configuration.
        customDomainName: string
        -- The status of the resource configuration.
        status: string
        -- The resource configuration.
        resourceConfigurationDefinition: ResourceConfigurationDefinition
        -- The date and time that the resource configuration was created, in ISO-8601 format.
        createdAt: number
        -- Indicates whether the resource configuration was created and is managed by Amazon.
        amazonManaged: boolean
        -- The reason the create-resource-configuration request failed.
        failureReason: string
        -- The most recent date and time that the resource configuration was updated, in ISO-8601 format.
        lastUpdatedAt: number
        -- The domain verification ID.
        domainVerificationId: string
        -- The ARN of the domain verification.
        domainVerificationArn: string
        -- The domain verification status.
        domainVerificationStatus: string
        -- (GROUP) The group domain for a group resource configuration. Any domains that you create for the child resource are subdomains of the group domain. Child resources inherit the verification status of the domain.
        groupDomain: string
    end

    record GetResourceGatewayInput
        -- The ID of the resource gateway.
        resourceGatewayIdentifier: string
    end

    record GetResourceGatewayOutput
        -- The name of the resource gateway.
        name: string
        -- The ID of the resource gateway.
        id: string
        -- The Amazon Resource Name (ARN) of the resource gateway.
        arn: string
        -- The status for the resource gateway.
        status: string
        -- The ID of the VPC for the resource gateway.
        vpcId: string
        -- The IDs of the VPC subnets for resource gateway.
        subnetIds: {string}
        -- Indicates whether the resource gateway is managed by an Amazon Web Services service.
        serviceManaged: boolean
        -- The Amazon Web Services service that manages the resource gateway.
        managedBy: string
        -- The security group IDs associated with the resource gateway.
        securityGroupIds: {string}
        -- The type of IP address for the resource gateway.
        ipAddressType: string
        -- The number of IPv4 addresses in each ENI for the resource gateway.
        ipv4AddressesPerEni: number
        -- The DNS resolution type for resource configurations that are associated with this resource gateway.
        resourceConfigDnsResolution: string
        -- The date and time that the resource gateway was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the resource gateway was last updated, in ISO-8601 format.
        lastUpdatedAt: number
    end

    record GetResourcePolicyInput
        -- The Amazon Resource Name (ARN) of the service network or service.
        resourceArn: string
    end

    record GetResourcePolicyOutput
        -- An IAM policy.
        policy: string
    end

    record GetRuleInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The ID or ARN of the listener.
        listenerIdentifier: string
        -- The ID or ARN of the listener rule.
        ruleIdentifier: string
    end

    record GetRuleOutput
        -- The Amazon Resource Name (ARN) of the listener.
        arn: string
        -- The ID of the listener.
        id: string
        -- The name of the listener.
        name: string
        -- Indicates whether this is the default rule.
        isDefault: boolean
        -- The rule match.
        match: RuleMatch
        -- The priority level for the specified rule.
        priority: number
        -- The action for the default rule.
        action: RuleAction
        -- The date and time that the listener rule was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the listener rule was last updated, in ISO-8601 format.
        lastUpdatedAt: number
    end

    record GetServiceInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
    end

    record GetServiceOutput
        -- The ID of the service.
        id: string
        -- The name of the service.
        name: string
        -- The Amazon Resource Name (ARN) of the service.
        arn: string
        -- The date and time that the service was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the service was last updated, in ISO-8601 format.
        lastUpdatedAt: number
        -- The DNS name of the service.
        dnsEntry: DnsEntry
        -- The custom domain name of the service.
        customDomainName: string
        -- The Amazon Resource Name (ARN) of the certificate.
        certificateArn: string
        -- The status of the service.
        status: string
        -- The type of IAM policy.
        authType: string
        -- The amount of time, in seconds, that a connection can remain idle before VPC Lattice closes it.
        idleTimeoutSeconds: number
        -- The failure code.
        failureCode: string
        -- The failure message.
        failureMessage: string
    end

    record GetServiceNetworkInput
        -- The ID or ARN of the service network.
        serviceNetworkIdentifier: string
    end

    record GetServiceNetworkOutput
        -- The ID of the service network.
        id: string
        -- The name of the service network.
        name: string
        -- The date and time that the service network was created, in ISO-8601 format.
        createdAt: number
        -- The date and time of the last update, in ISO-8601 format.
        lastUpdatedAt: number
        -- The Amazon Resource Name (ARN) of the service network.
        arn: string
        -- The type of IAM policy.
        authType: string
        -- Specifies if the service network is enabled for sharing.
        sharingConfig: SharingConfig
        -- The number of VPCs associated with the service network.
        numberOfAssociatedVPCs: number
        -- The number of services associated with the service network.
        numberOfAssociatedServices: number
    end

    record GetServiceNetworkResourceAssociationInput
        -- The ID of the association.
        serviceNetworkResourceAssociationIdentifier: string
    end

    record GetServiceNetworkResourceAssociationOutput
        -- The ID of the association.
        id: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The status of the association.
        status: string
        -- The account that created the association.
        createdBy: string
        -- The date and time that the association was created, in ISO-8601 format.
        createdAt: number
        -- The ID of the resource configuration that is associated with the service network.
        resourceConfigurationId: string
        -- The Amazon Resource Name (ARN) of the association.
        resourceConfigurationArn: string
        -- The name of the resource configuration that is associated with the service network.
        resourceConfigurationName: string
        -- The ID of the service network that is associated with the resource configuration.
        serviceNetworkId: string
        -- The Amazon Resource Name (ARN) of the service network that is associated with the resource configuration.
        serviceNetworkArn: string
        -- The name of the service network that is associated with the resource configuration.
        serviceNetworkName: string
        -- The reason the association request failed.
        failureReason: string
        -- The failure code.
        failureCode: string
        -- The most recent date and time that the association was updated, in ISO-8601 format.
        lastUpdatedAt: number
        -- The private DNS entry for the service.
        privateDnsEntry: DnsEntry
        -- Indicates if private DNS is enabled in the service network resource association.
        privateDnsEnabled: boolean
        -- The DNS entry for the service.
        dnsEntry: DnsEntry
        -- Indicates whether the association is managed by Amazon.
        isManagedAssociation: boolean
        -- The domain verification status in the service network resource association.
        domainVerificationStatus: string
    end

    record GetServiceNetworkServiceAssociationInput
        -- The ID or ARN of the association.
        serviceNetworkServiceAssociationIdentifier: string
    end

    record GetServiceNetworkServiceAssociationOutput
        -- The ID of the service network and service association.
        id: string
        -- The status of the association.
        status: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The account that created the association.
        createdBy: string
        -- The date and time that the association was created, in ISO-8601 format.
        createdAt: number
        -- The ID of the service.
        serviceId: string
        -- The name of the service.
        serviceName: string
        -- The Amazon Resource Name (ARN) of the service.
        serviceArn: string
        -- The ID of the service network.
        serviceNetworkId: string
        -- The name of the service network.
        serviceNetworkName: string
        -- The Amazon Resource Name (ARN) of the service network.
        serviceNetworkArn: string
        -- The DNS name of the service.
        dnsEntry: DnsEntry
        -- The custom domain name of the service.
        customDomainName: string
        -- The failure message.
        failureMessage: string
        -- The failure code.
        failureCode: string
    end

    record GetServiceNetworkVpcAssociationInput
        -- The ID or ARN of the association.
        serviceNetworkVpcAssociationIdentifier: string
    end

    record GetServiceNetworkVpcAssociationOutput
        -- The ID of the association.
        id: string
        -- The status of the association.
        status: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The account that created the association.
        createdBy: string
        -- The date and time that the association was created, in ISO-8601 format.
        createdAt: number
        -- The ID of the service network.
        serviceNetworkId: string
        -- The name of the service network.
        serviceNetworkName: string
        -- The Amazon Resource Name (ARN) of the service network.
        serviceNetworkArn: string
        -- The ID of the VPC.
        vpcId: string
        -- The IDs of the security groups.
        securityGroupIds: {string}
        -- Indicates if private DNS is enabled in the VPC association.
        privateDnsEnabled: boolean
        -- The failure message.
        failureMessage: string
        -- The failure code.
        failureCode: string
        -- The date and time that the association was last updated, in ISO-8601 format.
        lastUpdatedAt: number
        -- DNS options for the service network VPC association.
        dnsOptions: DnsOptions
    end

    record GetTargetGroupInput
        -- The ID or ARN of the target group.
        targetGroupIdentifier: string
    end

    record GetTargetGroupOutput
        -- The ID of the target group.
        id: string
        -- The Amazon Resource Name (ARN) of the target group.
        arn: string
        -- The name of the target group.
        name: string
        -- The target group configuration.
        config: TargetGroupConfig
        -- The date and time that the target group was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the target group was last updated, in ISO-8601 format.
        lastUpdatedAt: number
        -- The status.
        status: string
        -- The Amazon Resource Names (ARNs) of the service.
        serviceArns: {string}
        -- The failure message.
        failureMessage: string
        -- The failure code.
        failureCode: string
    end

    record ListListenersInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The maximum number of results to return.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about a listener.
    record ListenerSummary
        -- The Amazon Resource Name (ARN) of the listener.
        arn: string
        -- The ID of the listener.
        id: string
        -- The name of the listener.
        name: string
        -- The listener protocol.
        protocol: string
        -- The listener port.
        port: number
        -- The date and time that the listener was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the listener was last updated, in ISO-8601 format.
        lastUpdatedAt: number
    end

    record ListListenersOutput
        -- Information about the listeners.
        items: {ListenerSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record UpdateListenerInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The ID or ARN of the listener.
        listenerIdentifier: string
        -- The action for the default rule.
        defaultAction: RuleAction
    end

    record UpdateListenerOutput
        -- The Amazon Resource Name (ARN) of the listener.
        arn: string
        -- The ID of the listener.
        id: string
        -- The name of the listener.
        name: string
        -- The protocol of the listener.
        protocol: string
        -- The listener port.
        port: number
        -- The Amazon Resource Name (ARN) of the service.
        serviceArn: string
        -- The ID of the service.
        serviceId: string
        -- The action for the default rule.
        defaultAction: RuleAction
    end

    record ListResourceConfigurationsInput
        -- The ID of the resource gateway for the resource configuration.
        resourceGatewayIdentifier: string
        -- The ID of the resource configuration of type Group.
        resourceConfigurationGroupIdentifier: string
        -- The domain verification ID.
        domainVerificationIdentifier: string
        -- The maximum page size.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about a resource configuration.
    record ResourceConfigurationSummary
        -- The ID of the resource configuration.
        id: string
        -- The name of the resource configuration.
        name: string
        -- The Amazon Resource Name (ARN) of the resource configuration.
        arn: string
        -- The ID of the resource gateway.
        resourceGatewayId: string
        -- The ID of the group resource configuration.
        resourceConfigurationGroupId: string
        -- The status of the resource configuration.
        status: string
        -- Indicates whether the resource configuration was created and is managed by Amazon.
        amazonManaged: boolean
        -- The date and time that the resource configuration was created, in ISO-8601 format.
        createdAt: number
        -- The most recent date and time that the resource configuration was updated, in ISO-8601 format.
        lastUpdatedAt: number
        -- The custom domain name.
        customDomainName: string
        -- The domain verification ID.
        domainVerificationId: string
        -- (GROUP) The group domain for a group resource configuration. Any domains that you create for the child resource are subdomains of the group domain. Child resources inherit the verification status of the domain.
        groupDomain: string
    end

    record ListResourceConfigurationsOutput
        -- Information about the resource configurations.
        items: {ResourceConfigurationSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListResourceEndpointAssociationsInput
        -- The ID for the resource configuration associated with the VPC endpoint.
        resourceConfigurationIdentifier: string
        -- The ID of the association.
        resourceEndpointAssociationIdentifier: string
        -- The ID of the VPC endpoint in the association.
        vpcEndpointId: string
        -- The owner of the VPC endpoint in the association.
        vpcEndpointOwner: string
        -- The maximum page size.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about a VPC endpoint association.
    record ResourceEndpointAssociationSummary
        -- The ID of the VPC endpoint association.
        id: string
        -- The Amazon Resource Name (ARN) of the VPC endpoint association.
        arn: string
        -- The ID of the resource configuration.
        resourceConfigurationId: string
        -- The Amazon Resource Name (ARN) of the resource configuration.
        resourceConfigurationArn: string
        -- The name of the resource configuration.
        resourceConfigurationName: string
        -- The ID of the VPC endpoint.
        vpcEndpointId: string
        -- The owner of the VPC endpoint.
        vpcEndpointOwner: string
        -- The account that created the association.
        createdBy: string
        -- The date and time that the VPC endpoint association was created, in ISO-8601 format.
        createdAt: number
    end

    record ListResourceEndpointAssociationsOutput
        -- Information about the VPC endpoint associations.
        items: {ResourceEndpointAssociationSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListResourceGatewaysInput
        -- The maximum page size.
        maxResults: number
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about a resource gateway.
    record ResourceGatewaySummary
        -- The name of the resource gateway.
        name: string
        -- The ID of the resource gateway.
        id: string
        -- The Amazon Resource Name (ARN) of the resource gateway.
        arn: string
        -- The name of the resource gateway.
        status: string
        -- The ID of the VPC for the resource gateway.
        vpcIdentifier: string
        -- The IDs of the VPC subnets for the resource gateway.
        subnetIds: {string}
        -- The IDs of the security groups applied to the resource gateway.
        securityGroupIds: {string}
        -- The type of IP address used by the resource gateway.
        ipAddressType: string
        -- The number of IPv4 addresses in each ENI for the resource gateway.
        ipv4AddressesPerEni: number
        -- The DNS resolution type for resource configurations that are associated with this resource gateway.
        resourceConfigDnsResolution: string
        -- The date and time that the VPC endpoint association was created, in ISO-8601 format.
        createdAt: number
        -- The most recent date and time that the resource gateway was updated, in ISO-8601 format.
        lastUpdatedAt: number
    end

    record ListResourceGatewaysOutput
        -- Information about the resource gateways.
        items: {ResourceGatewaySummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListRulesInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The ID or ARN of the listener.
        listenerIdentifier: string
        -- The maximum number of results to return.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about a listener rule.
    record RuleSummary
        -- The Amazon Resource Name (ARN) of the rule.
        arn: string
        -- The ID of the rule.
        id: string
        -- The name of the rule.
        name: string
        -- Indicates whether this is the default listener rule.
        isDefault: boolean
        -- The priority of the rule.
        priority: number
        -- The date and time that the listener rule was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the listener rule was last updated, in ISO-8601 format.
        lastUpdatedAt: number
    end

    record ListRulesOutput
        -- Information about the rules.
        items: {RuleSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListServiceNetworkResourceAssociationsInput
        -- The ID of the service network.
        serviceNetworkIdentifier: string
        -- The ID of the resource configuration.
        resourceConfigurationIdentifier: string
        -- The maximum page size.
        maxResults: number
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
        -- Include service network resource associations of the child resource configuration with the grouped resource configuration. The type is boolean and the default value is false.
        includeChildren: boolean
    end

    -- Summary information about an association between a service network and a resource configuration.
    record ServiceNetworkResourceAssociationSummary
        -- The ID of the association between the service network and resource configuration.
        id: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The status of the service network’s association with the resource configuration. If the deletion fails, try to delete again.
        status: string
        -- The account that created the association.
        createdBy: string
        -- The date and time that the association was created, in ISO-8601 format.
        createdAt: number
        -- The ID of the resource configuration associated with the service network.
        resourceConfigurationId: string
        -- The Amazon Resource Name (ARN) of the association.
        resourceConfigurationArn: string
        -- The name of the resource configuration associated with the service network.
        resourceConfigurationName: string
        -- The ID of the service network associated with the resource configuration.
        serviceNetworkId: string
        -- The Amazon Resource Name (ARN) of the service network associated with the resource configuration.
        serviceNetworkArn: string
        -- The name of the service network associated with the resource configuration.
        serviceNetworkName: string
        -- The DNS entry for the service.
        dnsEntry: DnsEntry
        -- The private DNS entry for the service.
        privateDnsEntry: DnsEntry
        -- Specifies whether the association is managed by Amazon.
        isManagedAssociation: boolean
        -- The failure code.
        failureCode: string
        -- Indicates if private DNS is enabled for the service network resource association.
        privateDnsEnabled: boolean
    end

    record ListServiceNetworkResourceAssociationsOutput
        -- Information about the associations.
        items: {ServiceNetworkResourceAssociationSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListServiceNetworksInput
        -- The maximum number of results to return.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about a service network.
    record ServiceNetworkSummary
        -- The ID of the service network.
        id: string
        -- The name of the service network.
        name: string
        -- The Amazon Resource Name (ARN) of the service network.
        arn: string
        -- The date and time that the service network was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the service network was last updated, in ISO-8601 format.
        lastUpdatedAt: number
        -- The number of VPCs associated with the service network.
        numberOfAssociatedVPCs: number
        -- The number of services associated with the service network.
        numberOfAssociatedServices: number
        -- The number of resource configurations associated with a service network.
        numberOfAssociatedResourceConfigurations: number
    end

    record ListServiceNetworksOutput
        -- Information about the service networks.
        items: {ServiceNetworkSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListServiceNetworkServiceAssociationsInput
        -- The ID or ARN of the service network.
        serviceNetworkIdentifier: string
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The maximum number of results to return.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about an association between a service network and a service.
    record ServiceNetworkServiceAssociationSummary
        -- The ID of the association.
        id: string
        -- The status of the service network’s association with the service. If the deletion fails, try to delete again.
        status: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The account that created the association.
        createdBy: string
        -- The date and time that the association was created, in ISO-8601 format.
        createdAt: number
        -- The ID of the service.
        serviceId: string
        -- The name of the service.
        serviceName: string
        -- The Amazon Resource Name (ARN) of the service.
        serviceArn: string
        -- The ID of the service network.
        serviceNetworkId: string
        -- The name of the service network.
        serviceNetworkName: string
        -- The Amazon Resource Name (ARN) of the service network.
        serviceNetworkArn: string
        -- The DNS information.
        dnsEntry: DnsEntry
        -- The custom domain name of the service.
        customDomainName: string
    end

    record ListServiceNetworkServiceAssociationsOutput
        -- Information about the associations.
        items: {ServiceNetworkServiceAssociationSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListServiceNetworkVpcAssociationsInput
        -- The ID or ARN of the service network.
        serviceNetworkIdentifier: string
        -- The ID or ARN of the VPC.
        vpcIdentifier: string
        -- The maximum number of results to return.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about an association between a service network and a VPC.
    record ServiceNetworkVpcAssociationSummary
        -- The ID of the association.
        id: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The status.
        status: string
        -- The account that created the association.
        createdBy: string
        -- The date and time that the association was created, in ISO-8601 format.
        createdAt: number
        -- The ID of the service network.
        serviceNetworkId: string
        -- The name of the service network.
        serviceNetworkName: string
        -- The Amazon Resource Name (ARN) of the service network.
        serviceNetworkArn: string
        -- Indicates if private DNS is enabled for the service network VPC association.
        privateDnsEnabled: boolean
        -- The DNS options for the service network VPC association.
        dnsOptions: DnsOptions
        -- The ID of the VPC.
        vpcId: string
        -- The date and time that the association was last updated, in ISO-8601 format.
        lastUpdatedAt: number
    end

    record ListServiceNetworkVpcAssociationsOutput
        -- Information about the associations.
        items: {ServiceNetworkVpcAssociationSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListServiceNetworkVpcEndpointAssociationsInput
        -- The ID of the service network associated with the VPC endpoint.
        serviceNetworkIdentifier: string
        -- The maximum page size.
        maxResults: number
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    -- Describes the association between a service network and a VPC endpoint.
    record ServiceNetworkEndpointAssociation
        -- The ID of the VPC endpoint associated with the service network.
        vpcEndpointId: string
        -- The ID of the VPC for the association.
        vpcId: string
        -- The owner of the VPC endpoint associated with the service network.
        vpcEndpointOwnerId: string
        -- The ID of the association.
        id: string
        -- The state of the association.
        state: string
        -- The Amazon Resource Name (ARN) of the service network.
        serviceNetworkArn: string
        -- The date and time that the association was created, in ISO-8601 format.
        createdAt: number
    end

    record ListServiceNetworkVpcEndpointAssociationsOutput
        -- Information about the association between the VPC endpoint and service network.
        items: {ServiceNetworkEndpointAssociation}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListServicesInput
        -- The maximum number of results to return.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
    end

    -- Summary information about a service.
    record ServiceSummary
        -- The ID of the service.
        id: string
        -- The name of the service.
        name: string
        -- The Amazon Resource Name (ARN) of the service.
        arn: string
        -- The date and time that the service was created, in ISO-8601 format.
        createdAt: number
        -- The date and time that the service was last updated, in ISO-8601 format.
        lastUpdatedAt: number
        -- The DNS information.
        dnsEntry: DnsEntry
        -- The custom domain name of the service.
        customDomainName: string
        -- The status.
        status: string
    end

    record ListServicesOutput
        -- Information about the services.
        items: {ServiceSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- Information about the tags.
        tags: {string : string}
    end

    record ListTargetGroupsInput
        -- The maximum number of results to return.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
        -- The ID or ARN of the VPC.
        vpcIdentifier: string
        -- The target group type.
        targetGroupType: string
    end

    -- Summary information about a target group. For more information, see Target groups in the Amazon VPC Lattice User Guide.
    record TargetGroupSummary
        -- The ID of the target group.
        id: string
        -- The ARN (Amazon Resource Name) of the target group.
        arn: string
        -- The name of the target group.
        name: string
        -- The date and time that the target group was created, in ISO-8601 format.
        createdAt: number
        -- The port of the target group.
        port: number
        -- The protocol of the target group.
        protocol: string
        -- The type of IP address used for the target group. The possible values are IPV4 and IPV6. This is an optional parameter. If not specified, the default is IPV4.
        ipAddressType: string
        -- The ID of the VPC of the target group.
        vpcIdentifier: string
        -- The date and time that the target group was last updated, in ISO-8601 format.
        lastUpdatedAt: number
        -- The status.
        status: string
        -- The Amazon Resource Names (ARNs) of the service.
        serviceArns: {string}
        -- The version of the event structure that your Lambda function receives. Supported only if the target group type is LAMBDA.
        lambdaEventStructureVersion: string
    end

    record ListTargetGroupsOutput
        -- Information about the target groups.
        items: {TargetGroupSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record ListTargetsInput
        -- The ID or ARN of the target group.
        targetGroupIdentifier: string
        -- The maximum number of results to return.
        maxResults: number
        -- A pagination token for the next page of results.
        nextToken: string
        -- The targets.
        targets: {Target}
    end

    enum TargetStatus
        "DRAINING"
        "UNAVAILABLE"
        "HEALTHY"
        "UNHEALTHY"
        "INITIAL"
        "UNUSED"
    end

    -- Summary information about a target.
    record TargetSummary
        -- The ID of the target. If the target group type is INSTANCE, this is an instance ID. If the target group type is IP, this is an IP address. If the target group type is LAMBDA, this is the ARN of a Lambda function. If the target type is ALB, this is the ARN of an Application Load Balancer.
        id: string
        -- The port on which the target is listening.
        port: number
        -- The status of the target. DRAINING: The target is being deregistered. No new connections are sent to this target while current connections are being drained. The default draining time is 1 minute. UNAVAILABLE: Health checks are unavailable for the target group. HEALTHY: The target is healthy. UNHEALTHY: The target is unhealthy. INITIAL: Initial health checks on the target are being performed. UNUSED: Target group is not used in a service.
        status: string
        -- The code for why the target status is what it is.
        reasonCode: string
    end

    record ListTargetsOutput
        -- Information about the targets.
        items: {TargetSummary}
        -- If there are additional results, a pagination token for the next page of results.
        nextToken: string
    end

    record PutAuthPolicyInput
        -- The ID or ARN of the service network or service for which the policy is created.
        resourceIdentifier: string
        -- The auth policy. The policy string in JSON must not contain newlines or blank lines.
        policy: string
    end

    record PutAuthPolicyOutput
        -- The auth policy. The policy string in JSON must not contain newlines or blank lines.
        policy: string
        -- The state of the auth policy. The auth policy is only active when the auth type is set to AWS_IAM. If you provide a policy, then authentication and authorization decisions are made based on this policy and the client's IAM policy. If the Auth type is NONE, then, any auth policy that you provide remains inactive. For more information, see Create a service network in the Amazon VPC Lattice User Guide.
        state: string
    end

    record PutResourcePolicyInput
        -- The ID or ARN of the service network or service for which the policy is created.
        resourceArn: string
        -- An IAM policy. The policy string in JSON must not contain newlines or blank lines.
        policy: string
    end

    record PutResourcePolicyOutput
    end

    record UpdateResourceConfigurationInput
        -- The ID of the resource configuration.
        resourceConfigurationIdentifier: string
        -- Identifies the resource configuration in one of the following ways: Amazon Resource Name (ARN) - Supported resource-types that are provisioned by Amazon Web Services services, such as RDS databases, can be identified by their ARN. Domain name - Any domain name that is publicly resolvable. IP address - For IPv4 and IPv6, only IP addresses in the VPC are supported.
        resourceConfigurationDefinition: ResourceConfigurationDefinition
        -- Indicates whether to add the resource configuration to service networks that are shared with other accounts.
        allowAssociationToShareableServiceNetwork: boolean
        -- The TCP port ranges that a consumer can use to access a resource configuration. You can separate port ranges with a comma. Example: 1-65535 or 1,2,22-30
        portRanges: {string}
    end

    record UpdateResourceConfigurationOutput
        -- The ID of the resource configuration.
        id: string
        -- The name of the resource configuration.
        name: string
        -- The Amazon Resource Name (ARN) of the resource configuration.
        arn: string
        -- The ID of the resource gateway associated with the resource configuration.
        resourceGatewayId: string
        -- The ID of the group resource configuration.
        resourceConfigurationGroupId: string
        -- The TCP port ranges that a consumer can use to access a resource configuration. You can separate port ranges with a comma. Example: 1-65535 or 1,2,22-30
        portRanges: {string}
        -- Indicates whether to add the resource configuration to service networks that are shared with other accounts.
        allowAssociationToShareableServiceNetwork: boolean
        -- The TCP protocol accepted by the specified resource configuration.
        protocol: string
        -- The status of the resource configuration.
        status: string
        -- The resource configuration.
        resourceConfigurationDefinition: ResourceConfigurationDefinition
    end

    record UpdateResourceGatewayInput
        -- The ID or ARN of the resource gateway.
        resourceGatewayIdentifier: string
        -- The IDs of the security groups associated with the resource gateway.
        securityGroupIds: {string}
    end

    record UpdateResourceGatewayOutput
        -- The name of the resource gateway.
        name: string
        -- The ID of the resource gateway.
        id: string
        -- The Amazon Resource Name (ARN) of the resource gateway.
        arn: string
        -- The status of the resource gateway.
        status: string
        -- The ID of the VPC for the resource gateway.
        vpcId: string
        -- The IDs of the VPC subnets for the resource gateway.
        subnetIds: {string}
        -- The IDs of the security groups associated with the resource gateway.
        securityGroupIds: {string}
        -- The type of IP address used by the resource gateway.
        ipAddressType: string
    end

    record UpdateRuleInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The ID or ARN of the listener.
        listenerIdentifier: string
        -- The ID or ARN of the rule.
        ruleIdentifier: string
        -- The rule match.
        match: RuleMatch
        -- The rule priority. A listener can't have multiple rules with the same priority.
        priority: number
        -- Information about the action for the specified listener rule.
        action: RuleAction
    end

    record UpdateRuleOutput
        -- The Amazon Resource Name (ARN) of the listener.
        arn: string
        -- The ID of the listener.
        id: string
        -- The name of the listener.
        name: string
        -- Indicates whether this is the default rule.
        isDefault: boolean
        -- The rule match.
        match: RuleMatch
        -- The rule priority.
        priority: number
        -- Information about the action for the specified listener rule.
        action: RuleAction
    end

    record UpdateServiceInput
        -- The ID or ARN of the service.
        serviceIdentifier: string
        -- The Amazon Resource Name (ARN) of the certificate.
        certificateArn: string
        -- The type of IAM policy. NONE: The resource does not use an IAM policy. This is the default. AWS_IAM: The resource uses an IAM policy. When this type is used, auth is enabled and an auth policy is required.
        authType: string
        -- The amount of time, in seconds, that a connection can remain idle (no data sent) before VPC Lattice closes it. The valid range is 60 to 600 seconds. If you don't specify a value, the default is 60 seconds. This setting does not change the maximum connection duration of 10 minutes; connections are still closed when they reach that limit.
        idleTimeoutSeconds: number
    end

    record UpdateServiceOutput
        -- The ID of the service.
        id: string
        -- The Amazon Resource Name (ARN) of the service.
        arn: string
        -- The name of the service.
        name: string
        -- The custom domain name of the service.
        customDomainName: string
        -- The Amazon Resource Name (ARN) of the certificate.
        certificateArn: string
        -- The type of IAM policy.
        authType: string
        -- The amount of time, in seconds, that a connection can remain idle before VPC Lattice closes it.
        idleTimeoutSeconds: number
    end

    record UpdateServiceNetworkInput
        -- The ID or ARN of the service network.
        serviceNetworkIdentifier: string
        -- The type of IAM policy. NONE: The resource does not use an IAM policy. This is the default. AWS_IAM: The resource uses an IAM policy. When this type is used, auth is enabled and an auth policy is required.
        authType: string
    end

    record UpdateServiceNetworkOutput
        -- The ID of the service network.
        id: string
        -- The name of the service network.
        name: string
        -- The Amazon Resource Name (ARN) of the service network.
        arn: string
        -- The type of IAM policy.
        authType: string
    end

    record UpdateServiceNetworkVpcAssociationInput
        -- The ID or ARN of the association.
        serviceNetworkVpcAssociationIdentifier: string
        -- The IDs of the security groups.
        securityGroupIds: {string}
    end

    record UpdateServiceNetworkVpcAssociationOutput
        -- The ID of the association.
        id: string
        -- The Amazon Resource Name (ARN) of the association.
        arn: string
        -- The status. You can retry the operation if the status is DELETE_FAILED. However, if you retry it while the status is DELETE_IN_PROGRESS, there is no change in the status.
        status: string
        -- The account that created the association.
        createdBy: string
        -- The IDs of the security groups.
        securityGroupIds: {string}
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
        -- The tags for the resource.
        tags: {string : string}
    end

    record TagResourceOutput
    end

    record RegisterTargetsInput
        -- The ID or ARN of the target group.
        targetGroupIdentifier: string
        -- The targets.
        targets: {Target}
    end

    record RegisterTargetsOutput
        -- The targets that were successfully registered.
        successful: {Target}
        -- The targets that were not registered.
        unsuccessful: {TargetFailure}
    end

    record UpdateTargetGroupInput
        -- The ID or ARN of the target group.
        targetGroupIdentifier: string
        -- The health check configuration.
        healthCheck: HealthCheckConfig
    end

    record UpdateTargetGroupOutput
        -- The ID of the target group.
        id: string
        -- The Amazon Resource Name (ARN) of the target group.
        arn: string
        -- The name of the target group.
        name: string
        -- The target group configuration.
        config: TargetGroupConfig
        -- The status.
        status: string
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
        -- The tag keys of the tags to remove.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

end

return M
