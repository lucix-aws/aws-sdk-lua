-- Code generated by lua-client-codegen. DO NOT EDIT.

local id = require("smithy.shape_id")
local schema = require("smithy.schema")
local prelude = require("smithy.prelude")
local traits = require("smithy.traits")

local _N = "com.amazonaws.sagemakeredge"

local M: {string:any} = {}

M.EdgeDeployments = schema.new({ type = "list", list_member = M.EdgeDeployment })

M.EdgeMetrics = schema.new({ type = "list", list_member = M.EdgeMetric })

M.Models = schema.new({ type = "list", list_member = M.Model })

M.DeploymentModels = schema.new({ type = "list", list_member = M.DeploymentModel })

M.Definitions = schema.new({ type = "list", list_member = M.Definition })

M.GetDeploymentsInput = schema.new({
    id = id.from(_N, "GetDeploymentsRequest"),
    type = "structure",
    members = {
        DeviceName = schema.new({
            id = id.from(_N, "GetDeploymentsInput", "DeviceName"),
            type = "string",
            name = "DeviceName",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        DeviceFleetName = schema.new({
            id = id.from(_N, "GetDeploymentsInput", "DeviceFleetName"),
            type = "string",
            name = "DeviceFleetName",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.Checksum = schema.new({
    id = id.from(_N, "Checksum"),
    type = "structure",
    members = {
        Type = schema.new({
            id = id.from(_N, "Checksum", "Type"),
            type = "string",
            name = "Type",
            target_id = prelude.String.id,
        }),
        Sum = schema.new({
            id = id.from(_N, "Checksum", "Sum"),
            type = "string",
            name = "Sum",
            target_id = prelude.String.id,
        }),
    },
})

M.Definition = schema.new({
    id = id.from(_N, "Definition"),
    type = "structure",
    members = {
        ModelHandle = schema.new({
            id = id.from(_N, "Definition", "ModelHandle"),
            type = "string",
            name = "ModelHandle",
            target_id = prelude.String.id,
        }),
        S3Url = schema.new({
            id = id.from(_N, "Definition", "S3Url"),
            type = "string",
            name = "S3Url",
            target_id = prelude.String.id,
        }),
        Checksum = schema.new({
            id = id.from(_N, "Definition", "Checksum"),
            type = "structure",
            name = "Checksum",
            target_id = id.from(_N, "Checksum"),
            target = M.Checksum,
        }),
        State = schema.new({
            id = id.from(_N, "Definition", "State"),
            type = "string",
            name = "State",
            target_id = prelude.String.id,
        }),
    },
})

M.EdgeDeployment = schema.new({
    id = id.from(_N, "EdgeDeployment"),
    type = "structure",
    members = {
        DeploymentName = schema.new({
            id = id.from(_N, "EdgeDeployment", "DeploymentName"),
            type = "string",
            name = "DeploymentName",
            target_id = prelude.String.id,
        }),
        Type = schema.new({
            id = id.from(_N, "EdgeDeployment", "Type"),
            type = "string",
            name = "Type",
            target_id = prelude.String.id,
        }),
        FailureHandlingPolicy = schema.new({
            id = id.from(_N, "EdgeDeployment", "FailureHandlingPolicy"),
            type = "string",
            name = "FailureHandlingPolicy",
            target_id = prelude.String.id,
        }),
        Definitions = schema.new({
            id = id.from(_N, "EdgeDeployment", "Definitions"),
            type = "list",
            name = "Definitions",
            target_id = prelude.Document.id,
            list_member = M.Definition,
        }),
    },
})

M.GetDeploymentsOutput = schema.new({
    id = id.from(_N, "GetDeploymentsResult"),
    type = "structure",
    members = {
        Deployments = schema.new({
            id = id.from(_N, "GetDeploymentsOutput", "Deployments"),
            type = "list",
            name = "Deployments",
            target_id = prelude.Document.id,
            list_member = M.EdgeDeployment,
        }),
    },
})

M.InternalServiceException = schema.new({
    id = id.from(_N, "InternalServiceException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "InternalServiceException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
        }),
    },
})

M.GetDeviceRegistrationInput = schema.new({
    id = id.from(_N, "GetDeviceRegistrationRequest"),
    type = "structure",
    members = {
        DeviceName = schema.new({
            id = id.from(_N, "GetDeviceRegistrationInput", "DeviceName"),
            type = "string",
            name = "DeviceName",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        DeviceFleetName = schema.new({
            id = id.from(_N, "GetDeviceRegistrationInput", "DeviceFleetName"),
            type = "string",
            name = "DeviceFleetName",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.GetDeviceRegistrationOutput = schema.new({
    id = id.from(_N, "GetDeviceRegistrationResult"),
    type = "structure",
    members = {
        DeviceRegistration = schema.new({
            id = id.from(_N, "GetDeviceRegistrationOutput", "DeviceRegistration"),
            type = "string",
            name = "DeviceRegistration",
            target_id = prelude.String.id,
        }),
        CacheTTL = schema.new({
            id = id.from(_N, "GetDeviceRegistrationOutput", "CacheTTL"),
            type = "string",
            name = "CacheTTL",
            target_id = prelude.String.id,
        }),
    },
})

M.EdgeMetric = schema.new({
    id = id.from(_N, "EdgeMetric"),
    type = "structure",
    members = {
        Dimension = schema.new({
            id = id.from(_N, "EdgeMetric", "Dimension"),
            type = "string",
            name = "Dimension",
            target_id = prelude.String.id,
        }),
        MetricName = schema.new({
            id = id.from(_N, "EdgeMetric", "MetricName"),
            type = "string",
            name = "MetricName",
            target_id = prelude.String.id,
        }),
        Value = schema.new({
            id = id.from(_N, "EdgeMetric", "Value"),
            type = "double",
            name = "Value",
            target_id = prelude.Double.id,
        }),
        Timestamp = schema.new({
            id = id.from(_N, "EdgeMetric", "Timestamp"),
            type = "timestamp",
            name = "Timestamp",
            target_id = prelude.Timestamp.id,
        }),
    },
})

M.DeploymentModel = schema.new({
    id = id.from(_N, "DeploymentModel"),
    type = "structure",
    members = {
        ModelHandle = schema.new({
            id = id.from(_N, "DeploymentModel", "ModelHandle"),
            type = "string",
            name = "ModelHandle",
            target_id = prelude.String.id,
        }),
        ModelName = schema.new({
            id = id.from(_N, "DeploymentModel", "ModelName"),
            type = "string",
            name = "ModelName",
            target_id = prelude.String.id,
        }),
        ModelVersion = schema.new({
            id = id.from(_N, "DeploymentModel", "ModelVersion"),
            type = "string",
            name = "ModelVersion",
            target_id = prelude.String.id,
        }),
        DesiredState = schema.new({
            id = id.from(_N, "DeploymentModel", "DesiredState"),
            type = "string",
            name = "DesiredState",
            target_id = prelude.String.id,
        }),
        State = schema.new({
            id = id.from(_N, "DeploymentModel", "State"),
            type = "string",
            name = "State",
            target_id = prelude.String.id,
        }),
        Status = schema.new({
            id = id.from(_N, "DeploymentModel", "Status"),
            type = "string",
            name = "Status",
            target_id = prelude.String.id,
        }),
        StatusReason = schema.new({
            id = id.from(_N, "DeploymentModel", "StatusReason"),
            type = "string",
            name = "StatusReason",
            target_id = prelude.String.id,
        }),
        RollbackFailureReason = schema.new({
            id = id.from(_N, "DeploymentModel", "RollbackFailureReason"),
            type = "string",
            name = "RollbackFailureReason",
            target_id = prelude.String.id,
        }),
    },
})

M.DeploymentResult = schema.new({
    id = id.from(_N, "DeploymentResult"),
    type = "structure",
    members = {
        DeploymentName = schema.new({
            id = id.from(_N, "DeploymentResult", "DeploymentName"),
            type = "string",
            name = "DeploymentName",
            target_id = prelude.String.id,
        }),
        DeploymentStatus = schema.new({
            id = id.from(_N, "DeploymentResult", "DeploymentStatus"),
            type = "string",
            name = "DeploymentStatus",
            target_id = prelude.String.id,
        }),
        DeploymentStatusMessage = schema.new({
            id = id.from(_N, "DeploymentResult", "DeploymentStatusMessage"),
            type = "string",
            name = "DeploymentStatusMessage",
            target_id = prelude.String.id,
        }),
        DeploymentStartTime = schema.new({
            id = id.from(_N, "DeploymentResult", "DeploymentStartTime"),
            type = "timestamp",
            name = "DeploymentStartTime",
            target_id = prelude.Timestamp.id,
        }),
        DeploymentEndTime = schema.new({
            id = id.from(_N, "DeploymentResult", "DeploymentEndTime"),
            type = "timestamp",
            name = "DeploymentEndTime",
            target_id = prelude.Timestamp.id,
        }),
        DeploymentModels = schema.new({
            id = id.from(_N, "DeploymentResult", "DeploymentModels"),
            type = "list",
            name = "DeploymentModels",
            target_id = prelude.Document.id,
            list_member = M.DeploymentModel,
        }),
    },
})

M.Model = schema.new({
    id = id.from(_N, "Model"),
    type = "structure",
    members = {
        ModelName = schema.new({
            id = id.from(_N, "Model", "ModelName"),
            type = "string",
            name = "ModelName",
            target_id = prelude.String.id,
        }),
        ModelVersion = schema.new({
            id = id.from(_N, "Model", "ModelVersion"),
            type = "string",
            name = "ModelVersion",
            target_id = prelude.String.id,
        }),
        LatestSampleTime = schema.new({
            id = id.from(_N, "Model", "LatestSampleTime"),
            type = "timestamp",
            name = "LatestSampleTime",
            target_id = prelude.Timestamp.id,
        }),
        LatestInference = schema.new({
            id = id.from(_N, "Model", "LatestInference"),
            type = "timestamp",
            name = "LatestInference",
            target_id = prelude.Timestamp.id,
        }),
        ModelMetrics = schema.new({
            id = id.from(_N, "Model", "ModelMetrics"),
            type = "list",
            name = "ModelMetrics",
            target_id = prelude.Document.id,
            list_member = M.EdgeMetric,
        }),
    },
})

M.SendHeartbeatInput = schema.new({
    id = id.from(_N, "SendHeartbeatRequest"),
    type = "structure",
    members = {
        AgentMetrics = schema.new({
            id = id.from(_N, "SendHeartbeatInput", "AgentMetrics"),
            type = "list",
            name = "AgentMetrics",
            target_id = prelude.Document.id,
            list_member = M.EdgeMetric,
        }),
        Models = schema.new({
            id = id.from(_N, "SendHeartbeatInput", "Models"),
            type = "list",
            name = "Models",
            target_id = prelude.Document.id,
            list_member = M.Model,
        }),
        AgentVersion = schema.new({
            id = id.from(_N, "SendHeartbeatInput", "AgentVersion"),
            type = "string",
            name = "AgentVersion",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        DeviceName = schema.new({
            id = id.from(_N, "SendHeartbeatInput", "DeviceName"),
            type = "string",
            name = "DeviceName",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        DeviceFleetName = schema.new({
            id = id.from(_N, "SendHeartbeatInput", "DeviceFleetName"),
            type = "string",
            name = "DeviceFleetName",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        DeploymentResult = schema.new({
            id = id.from(_N, "SendHeartbeatInput", "DeploymentResult"),
            type = "structure",
            name = "DeploymentResult",
            target_id = id.from(_N, "DeploymentResult"),
            target = M.DeploymentResult,
        }),
    },
})

M.SendHeartbeatOutput = prelude.Unit

-- Fix forward references for recursive schemas
for _, _s in pairs(M) do
    local s = _s as {string:any}
    if type(s) == "table" and (s.type == "structure" or s.type == "union") then
        local members = rawget(s, "_members") as {string:{string:any}}
        if members then
            for _, ms in pairs(members) do
                if (ms.type == "structure" or ms.type == "union") and not rawget(ms as {string:any}, "_target") and ms.target_id then
                    rawset(ms as {string:any}, "_target", M[(ms.target_id as {string:string}).name])
                end
            end
        end
    end
end

M.Service = schema.service({
    id = id.from("com.amazonaws.sagemakeredge", "AmazonSageMakerEdge"),
    version = "2020-09-23",
    traits = {
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

M.GetDeployments = schema.operation({
    id = id.from("com.amazonaws.sagemakeredge", "GetDeployments"),
    input = M.GetDeploymentsInput,
    output = M.GetDeploymentsOutput,
    traits = {
        [traits.HTTP] = { method = "POST", path = "/GetDeployments" },
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

M.GetDeviceRegistration = schema.operation({
    id = id.from("com.amazonaws.sagemakeredge", "GetDeviceRegistration"),
    input = M.GetDeviceRegistrationInput,
    output = M.GetDeviceRegistrationOutput,
    traits = {
        [traits.HTTP] = { method = "POST", path = "/GetDeviceRegistration" },
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

M.SendHeartbeat = schema.operation({
    id = id.from("com.amazonaws.sagemakeredge", "SendHeartbeat"),
    input = M.SendHeartbeatInput,
    output = M.SendHeartbeatOutput,
    traits = {
        [traits.HTTP] = { method = "POST", path = "/SendHeartbeat" },
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

return M
