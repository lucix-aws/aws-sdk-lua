-- Code generated by lua-client-codegen. DO NOT EDIT.

local id = require("smithy.shape_id")
local schema = require("smithy.schema")
local prelude = require("smithy.prelude")
local traits = require("smithy.traits")
local smithy = require("smithy")

local _N = "com.amazonaws.kinesisvideomedia"

local M: {string:any} = {}

M.ClientLimitExceededException = schema.new({
    id = id.from(_N, "ClientLimitExceededException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "ClientLimitExceededException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
        }),
    },
})

M.ConnectionLimitExceededException = schema.new({
    id = id.from(_N, "ConnectionLimitExceededException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "ConnectionLimitExceededException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
        }),
    },
})

M.StartSelector = schema.new({
    id = id.from(_N, "StartSelector"),
    type = "structure",
    members = {
        StartSelectorType = schema.new({
            id = id.from(_N, "StartSelector", "StartSelectorType"),
            type = "string",
            name = "StartSelectorType",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        AfterFragmentNumber = schema.new({
            id = id.from(_N, "StartSelector", "AfterFragmentNumber"),
            type = "string",
            name = "AfterFragmentNumber",
            target_id = prelude.String.id,
        }),
        StartTimestamp = schema.new({
            id = id.from(_N, "StartSelector", "StartTimestamp"),
            type = "timestamp",
            name = "StartTimestamp",
            target_id = prelude.Timestamp.id,
        }),
        ContinuationToken = schema.new({
            id = id.from(_N, "StartSelector", "ContinuationToken"),
            type = "string",
            name = "ContinuationToken",
            target_id = prelude.String.id,
        }),
    },
})

M.GetMediaInput = schema.new({
    id = id.from(_N, "GetMediaInput"),
    type = "structure",
    members = {
        StreamName = schema.new({
            id = id.from(_N, "GetMediaInput", "StreamName"),
            type = "string",
            name = "StreamName",
            target_id = prelude.String.id,
        }),
        StreamARN = schema.new({
            id = id.from(_N, "GetMediaInput", "StreamARN"),
            type = "string",
            name = "StreamARN",
            target_id = prelude.String.id,
        }),
        StartSelector = schema.new({
            id = id.from(_N, "GetMediaInput", "StartSelector"),
            type = "structure",
            name = "StartSelector",
            target_id = id.from(_N, "StartSelector"),
            target = M.StartSelector,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.GetMediaOutput = schema.new({
    id = id.from(_N, "GetMediaOutput"),
    type = "structure",
    members = {
        ContentType = schema.new({
            id = id.from(_N, "GetMediaOutput", "ContentType"),
            type = "string",
            name = "ContentType",
            target_id = prelude.String.id,
            traits = {
                [traits.HTTP_HEADER] = { name = "Content-Type" },
            },
        }),
        Payload = schema.new({
            id = id.from(_N, "GetMediaOutput", "Payload"),
            type = "blob",
            name = "Payload",
            target_id = prelude.Blob.id,
            traits = {
                [traits.DEFAULT] = { value = "" },
                [traits.HTTP_PAYLOAD] = {},
                [traits.STREAMING] = {},
            },
            direct_traits = {
                [traits.DEFAULT] = { value = "" },
                [traits.HTTP_PAYLOAD] = {},
            },
        }),
    },
})

M.InvalidArgumentException = schema.new({
    id = id.from(_N, "InvalidArgumentException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "InvalidArgumentException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
        }),
    },
})

M.InvalidEndpointException = schema.new({
    id = id.from(_N, "InvalidEndpointException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "InvalidEndpointException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
        }),
    },
})

M.NotAuthorizedException = schema.new({
    id = id.from(_N, "NotAuthorizedException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "NotAuthorizedException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
        }),
    },
})

M.ResourceNotFoundException = schema.new({
    id = id.from(_N, "ResourceNotFoundException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "ResourceNotFoundException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
        }),
    },
})

-- Fix forward references for recursive schemas
for _, _s in pairs(M) do
    local s = _s as {string:any}
    if type(s) == "table" and (s.type == "structure" or s.type == "union") then
        local members = rawget(s, "_members") as {string:{string:any}}
        if members then
            for _, ms in pairs(members) do
                if (ms.type == "structure" or ms.type == "union") and not rawget(ms as {string:any}, "_target") and ms.target_id then
                    rawset(ms as {string:any}, "_target", M[(ms.target_id as {string:string}).name])
                end
            end
        end
    end
end

M.Service = schema.service({
    id = id.from("com.amazonaws.kinesisvideomedia", "AWSAcuityInletService"),
    version = "2017-09-30",
    traits = {
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

M.GetMedia = schema.operation({
    id = id.from("com.amazonaws.kinesisvideomedia", "GetMedia"),
    input = M.GetMediaInput,
    output = M.GetMediaOutput,
    traits = {
        [traits.HTTP] = { method = "POST", path = "/getMedia" },
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

return M
