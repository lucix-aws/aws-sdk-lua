-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- You are not authorized to perform this operation.
    record AccessDeniedException
        message: string
    end

    -- Contains API key credential information.
    record ApiKeyCredential
        -- An API key for an application.
        apiKey: string
    end

    enum AuthType
        "oauth2"
        "apiKey"
    end

    enum Persona
        "admin"
        "endUser"
    end

    enum AppAuthorizationStatus
        "PendingConnect"
        "Connected"
        "ConnectionValidationFailed"
        "TokenAutoRotationFailed"
    end

    -- Contains information about an application tenant.
    record Tenant
        -- The ID of the application tenant.
        tenantIdentifier: string
        -- The display name of the tenant.
        tenantDisplayName: string
    end

    -- Contains information about an app authorization.
    record AppAuthorization
        -- The Amazon Resource Name (ARN) of the app authorization.
        appAuthorizationArn: string
        -- The Amazon Resource Name (ARN) of the app bundle for the app authorization.
        appBundleArn: string
        -- The name of the application.
        app: string
        -- Contains information about an application tenant, such as the application display name
        -- and identifier.
        tenant: Tenant
        -- The authorization type.
        authType: string
        -- The state of the app authorization.
        -- The following states are possible:
        -- PendingConnect: The initial state of the app authorization. The app
        -- authorization is created but not yet connected.
        -- Connected: The app authorization is connected to the application, and
        -- is ready to be used.
        -- ConnectionValidationFailed: The app authorization received a
        -- validation exception when trying to connect to the application. If the app
        -- authorization is in this state, you should verify the configured credentials and try
        -- to connect the app authorization again.
        -- TokenAutoRotationFailed: AppFabric failed to refresh the access token. If
        -- the app authorization is in this state, you should try to reconnect the app
        -- authorization.
        status: string
        -- The timestamp of when the app authorization was created.
        createdAt: number
        -- The timestamp of when the app authorization was last updated.
        updatedAt: number
        -- The user persona of the app authorization.
        -- This field should always be admin.
        persona: string
        -- The application URL for the OAuth flow.
        authUrl: string
    end

    -- Contains a summary of an app authorization.
    record AppAuthorizationSummary
        -- The Amazon Resource Name (ARN) of the app authorization.
        appAuthorizationArn: string
        -- The Amazon Resource Name (ARN) of the app bundle for the app authorization.
        appBundleArn: string
        -- The name of the application.
        app: string
        -- Contains information about an application tenant, such as the application display name
        -- and identifier.
        tenant: Tenant
        -- The state of the app authorization.
        -- The following states are possible:
        -- PendingConnect: The initial state of the app authorization. The app
        -- authorization is created but not yet connected.
        -- Connected: The app authorization is connected to the application, and
        -- is ready to be used.
        -- ConnectionValidationFailed: The app authorization received a
        -- validation exception when trying to connect to the application. If the app
        -- authorization is in this state, you should verify the configured credentials and try
        -- to connect the app authorization again.
        -- TokenAutoRotationFailed: AppFabric failed to refresh the access token. If
        -- the app authorization is in this state, you should try to reconnect the app
        -- authorization.
        status: string
        -- Timestamp for when the app authorization was last updated.
        updatedAt: number
    end

    -- Contains information about an app bundle.
    record AppBundle
        -- The Amazon Resource Name (ARN) of the app bundle.
        arn: string
        -- The Amazon Resource Name (ARN) of the Key Management Service (KMS) key
        -- used to encrypt the application data.
        customerManagedKeyArn: string
    end

    -- Contains a summary of an app bundle.
    record AppBundleSummary
        -- The Amazon Resource Name (ARN) of the app bundle.
        arn: string
    end

    -- Contains information about an Amazon Kinesis Data Firehose delivery stream.
    record FirehoseStream
        -- The name of the Amazon Kinesis Data Firehose delivery stream.
        streamName: string
    end

    -- Contains information about an Amazon S3 bucket.
    record S3Bucket
        -- The name of the Amazon S3 bucket.
        bucketName: string
        -- The object key to use.
        prefix: string
    end

    -- Contains information about an audit log destination.
    record Destination
        -- Contains information about an Amazon S3 bucket.
        s3Bucket: S3Bucket
        -- Contains information about an Amazon Kinesis Data Firehose delivery stream.
        firehoseStream: FirehoseStream
    end

    -- Contains information about an audit log destination configuration.
    record AuditLogDestinationConfiguration
        -- Contains information about an audit log destination.
        destination: Destination
    end

    enum Format
        "json"
        "parquet"
    end

    enum Schema
        "ocsf"
        "raw"
    end

    -- Contains information about an audit log processing configuration.
    record AuditLogProcessingConfiguration
        -- The event schema in which the audit logs need to be formatted.
        schema: string
        -- The format in which the audit logs need to be formatted.
        format: string
    end

    -- Contains authorization request information, which is required for Amazon Web Services AppFabric to get the
    -- OAuth2 access token for an application.
    record AuthRequest
        -- The redirect URL that is specified in the AuthURL and the application client.
        redirectUri: string
        -- The authorization code returned by the application after permission is granted in the
        -- application OAuth page (after clicking on the AuthURL).
        code: string
    end

    record BatchGetUserAccessTasksInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The tasks IDs to use for the request.
        taskIdList: {string}
    end

    enum ResultStatus
        "IN_PROGRESS"
        "COMPLETED"
        "FAILED"
        "EXPIRED"
    end

    -- Contains information about an error returned from a user access task.
    record TaskError
        -- The code of the error.
        errorCode: string
        -- The message of the error.
        errorMessage: string
    end

    -- Contains information about a user's access to an application.
    record UserAccessResultItem
        -- The name of the application.
        app: string
        -- The ID of the application tenant.
        tenantId: string
        -- The display name of the tenant.
        tenantDisplayName: string
        -- The unique ID of the task.
        taskId: string
        -- The status of the user access result item.
        -- The following states are possible:
        -- IN_PROGRESS: The user access task is in progress.
        -- COMPLETED: The user access task completed successfully.
        -- FAILED: The user access task failed.
        -- EXPIRED: The user access task expired.
        resultStatus: string
        -- The email address of the target user.
        email: string
        -- The unique ID of user.
        userId: string
        -- The full name of the user.
        userFullName: string
        -- The first name of the user.
        userFirstName: string
        -- The last name of the user.
        userLastName: string
        -- The status of the user returned by the application.
        userStatus: string
        -- Contains information about an error returned from a user access task.
        taskError: TaskError
    end

    record BatchGetUserAccessTasksOutput
        -- Contains a list of user access results.
        userAccessResultsList: {UserAccessResultItem}
    end

    -- The request processing has failed because of an unknown error, exception, or failure
    -- with an internal server.
    record InternalServerException
        message: string
        -- The period of time after which you should retry your request.
        retryAfterSeconds: number
    end

    -- The specified resource does not exist.
    record ResourceNotFoundException
        message: string
        -- The resource ID.
        resourceId: string
        -- The resource type.
        resourceType: string
    end

    -- The request rate exceeds the limit.
    record ThrottlingException
        message: string
        -- The code of the service.
        serviceCode: string
        -- The code for the quota exceeded.
        quotaCode: string
        -- The period of time after which you should retry your request.
        retryAfterSeconds: number
    end

    -- The input failed to meet the constraints specified by the Amazon Web Services service in
    -- a specified field.
    record ValidationExceptionField
        -- The field name where the invalid entry was detected.
        name: string
        -- A message about the validation exception.
        message: string
    end

    enum ValidationExceptionReason
        "unknownOperation"
        "cannotParse"
        "fieldValidationFailed"
        "other"
    end

    -- The request has invalid or missing parameters.
    record ValidationException
        message: string
        -- The reason for the exception.
        reason: string
        -- The field list.
        fieldList: {ValidationExceptionField}
    end

    -- The request has created a conflict. Check the request parameters and try again.
    record ConflictException
        message: string
        -- The resource ID.
        resourceId: string
        -- The resource type.
        resourceType: string
    end

    record ConnectAppAuthorizationInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- that contains the app authorization to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app
        -- authorization to use for the request.
        appAuthorizationIdentifier: string
        -- Contains OAuth2 authorization information.
        -- This is required if the app authorization for the request is configured with an OAuth2
        -- (oauth2) authorization type.
        authRequest: AuthRequest
    end

    record ConnectAppAuthorizationOutput
        -- Contains a summary of the app authorization.
        appAuthorizationSummary: AppAuthorizationSummary
    end

    -- Contains OAuth2 client credential information.
    record Oauth2Credential
        -- The client ID of the client application.
        clientId: string
        -- The client secret of the client application.
        clientSecret: string
    end

    -- Contains credential information for an application.
    record Credential
        -- Contains OAuth2 client credential information.
        oauth2Credential: Oauth2Credential
        -- Contains API key credential information.
        apiKeyCredential: ApiKeyCredential
    end

    -- The key or keys of the key-value pairs for the tag or tags assigned to a
    -- resource.
    record Tag
        -- Tag key.
        key: string
        -- Tag value.
        value: string
    end

    record CreateAppAuthorizationInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The name of the application.
        -- Valid values are:
        -- SLACK
        -- ASANA
        -- JIRA
        -- M365
        -- M365AUDITLOGS
        -- ZOOM
        -- ZENDESK
        -- OKTA
        -- GOOGLE
        -- DROPBOX
        -- SMARTSHEET
        -- CISCO
        app: string
        -- Contains credentials for the application, such as an API key or OAuth2 client ID and
        -- secret.
        -- Specify credentials that match the authorization type for your request. For example, if
        -- the authorization type for your request is OAuth2 (oauth2), then you should
        -- provide only the OAuth2 credentials.
        credential: Credential
        -- Contains information about an application tenant, such as the application display name
        -- and identifier.
        tenant: Tenant
        -- The authorization type for the app authorization.
        authType: string
        -- Specifies a unique, case-sensitive identifier that you provide to ensure the idempotency
        -- of the request. This lets you safely retry the request without accidentally performing the
        -- same operation a second time. Passing the same value to a later call to an operation
        -- requires that you also pass the same value for all other parameters. We recommend that you
        -- use a UUID type of
        -- value.
        -- If you don't provide this value, then Amazon Web Services generates a random one for
        -- you.
        -- If you retry the operation with the same ClientToken, but with different
        -- parameters, the retry fails with an IdempotentParameterMismatch error.
        clientToken: string
        -- A map of the key-value pairs of the tag or tags to assign to the resource.
        tags: {Tag}
    end

    record CreateAppAuthorizationOutput
        -- Contains information about an app authorization.
        appAuthorization: AppAuthorization
    end

    -- The request exceeds a service quota.
    record ServiceQuotaExceededException
        message: string
        -- The resource ID.
        resourceId: string
        -- The resource type.
        resourceType: string
        -- The code of the service.
        serviceCode: string
        -- The code for the quota exceeded.
        quotaCode: string
    end

    record CreateAppBundleInput
        -- Specifies a unique, case-sensitive identifier that you provide to ensure the idempotency
        -- of the request. This lets you safely retry the request without accidentally performing the
        -- same operation a second time. Passing the same value to a later call to an operation
        -- requires that you also pass the same value for all other parameters. We recommend that you
        -- use a UUID type of
        -- value.
        -- If you don't provide this value, then Amazon Web Services generates a random one for
        -- you.
        -- If you retry the operation with the same ClientToken, but with different
        -- parameters, the retry fails with an IdempotentParameterMismatch error.
        clientToken: string
        -- The Amazon Resource Name (ARN) of the Key Management Service (KMS) key to
        -- use to encrypt the application data. If this is not specified, an Amazon Web Services owned key is used for encryption.
        customerManagedKeyIdentifier: string
        -- A map of the key-value pairs of the tag or tags to assign to the resource.
        tags: {Tag}
    end

    record CreateAppBundleOutput
        -- Contains information about an app bundle.
        appBundle: AppBundle
    end

    enum IngestionType
        "auditLog"
    end

    record CreateIngestionInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The name of the application.
        -- Valid values are:
        -- SLACK
        -- ASANA
        -- JIRA
        -- M365
        -- M365AUDITLOGS
        -- ZOOM
        -- ZENDESK
        -- OKTA
        -- GOOGLE
        -- DROPBOX
        -- SMARTSHEET
        -- CISCO
        app: string
        -- The ID of the application tenant.
        tenantId: string
        -- The ingestion type.
        ingestionType: string
        -- Specifies a unique, case-sensitive identifier that you provide to ensure the idempotency
        -- of the request. This lets you safely retry the request without accidentally performing the
        -- same operation a second time. Passing the same value to a later call to an operation
        -- requires that you also pass the same value for all other parameters. We recommend that you
        -- use a UUID type of
        -- value.
        -- If you don't provide this value, then Amazon Web Services generates a random one for
        -- you.
        -- If you retry the operation with the same ClientToken, but with different
        -- parameters, the retry fails with an IdempotentParameterMismatch error.
        clientToken: string
        -- A map of the key-value pairs of the tag or tags to assign to the resource.
        tags: {Tag}
    end

    enum IngestionState
        "enabled"
        "disabled"
    end

    -- Contains information about an ingestion.
    record Ingestion
        -- The Amazon Resource Name (ARN) of the ingestion.
        arn: string
        -- The Amazon Resource Name (ARN) of the app bundle for the ingestion.
        appBundleArn: string
        -- The name of the application.
        app: string
        -- The ID of the application tenant.
        tenantId: string
        -- The timestamp of when the ingestion was created.
        createdAt: number
        -- The timestamp of when the ingestion was last updated.
        updatedAt: number
        -- The status of the ingestion.
        state: string
        -- The type of the ingestion.
        ingestionType: string
    end

    record CreateIngestionOutput
        -- Contains information about an ingestion.
        ingestion: Ingestion
    end

    -- Contains information about the destination of ingested data.
    record DestinationConfiguration
        -- Contains information about an audit log destination configuration.
        auditLog: AuditLogDestinationConfiguration
    end

    -- Contains information about how ingested data is processed.
    record ProcessingConfiguration
        -- Contains information about an audit log processing configuration.
        auditLog: AuditLogProcessingConfiguration
    end

    record CreateIngestionDestinationInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion to
        -- use for the request.
        ingestionIdentifier: string
        -- Contains information about how ingested data is processed.
        processingConfiguration: ProcessingConfiguration
        -- Contains information about the destination of ingested data.
        destinationConfiguration: DestinationConfiguration
        -- Specifies a unique, case-sensitive identifier that you provide to ensure the idempotency
        -- of the request. This lets you safely retry the request without accidentally performing the
        -- same operation a second time. Passing the same value to a later call to an operation
        -- requires that you also pass the same value for all other parameters. We recommend that you
        -- use a UUID type of
        -- value.
        -- If you don't provide this value, then Amazon Web Services generates a random one for
        -- you.
        -- If you retry the operation with the same ClientToken, but with different
        -- parameters, the retry fails with an IdempotentParameterMismatch error.
        clientToken: string
        -- A map of the key-value pairs of the tag or tags to assign to the resource.
        tags: {Tag}
    end

    enum IngestionDestinationStatus
        "Active"
        "Failed"
    end

    -- Contains information about an ingestion destination.
    record IngestionDestination
        -- The Amazon Resource Name (ARN) of the ingestion destination.
        arn: string
        -- The Amazon Resource Name (ARN) of the ingestion.
        ingestionArn: string
        -- Contains information about how ingested data is processed.
        processingConfiguration: ProcessingConfiguration
        -- Contains information about the destination of ingested data.
        destinationConfiguration: DestinationConfiguration
        -- The state of the ingestion destination.
        -- The following states are possible:
        -- Active: The ingestion destination is active and is ready to be
        -- used.
        -- Failed: The ingestion destination has failed. If the ingestion
        -- destination is in this state, you should verify the ingestion destination
        -- configuration and try again.
        status: string
        -- The reason for the current status of the ingestion destination.
        -- Only present when the status of ingestion destination is
        -- Failed.
        statusReason: string
        -- The timestamp of when the ingestion destination was created.
        createdAt: number
        -- The timestamp of when the ingestion destination was last updated.
        updatedAt: number
    end

    record CreateIngestionDestinationOutput
        -- Contains information about an ingestion destination.
        ingestionDestination: IngestionDestination
    end

    record DeleteAppAuthorizationInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app
        -- authorization to use for the request.
        appAuthorizationIdentifier: string
    end

    record DeleteAppAuthorizationOutput
    end

    record DeleteAppBundleInput
        -- The ID or Amazon Resource Name (ARN) of the app bundle that needs to be deleted.
        appBundleIdentifier: string
    end

    record DeleteAppBundleOutput
    end

    record DeleteIngestionInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion to
        -- use for the request.
        ingestionIdentifier: string
    end

    record DeleteIngestionOutput
    end

    record DeleteIngestionDestinationInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion to
        -- use for the request.
        ingestionIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion
        -- destination to use for the request.
        ingestionDestinationIdentifier: string
    end

    record DeleteIngestionDestinationOutput
    end

    record GetAppAuthorizationInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app
        -- authorization to use for the request.
        appAuthorizationIdentifier: string
    end

    record GetAppAuthorizationOutput
        -- Contains information about an app authorization.
        appAuthorization: AppAuthorization
    end

    record GetAppBundleInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
    end

    record GetAppBundleOutput
        -- Contains information about an app bundle.
        appBundle: AppBundle
    end

    record GetIngestionInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion to
        -- use for the request.
        ingestionIdentifier: string
    end

    record GetIngestionOutput
        -- Contains information about an ingestion.
        ingestion: Ingestion
    end

    record GetIngestionDestinationInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion to
        -- use for the request.
        ingestionIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion
        -- destination to use for the request.
        ingestionDestinationIdentifier: string
    end

    record GetIngestionDestinationOutput
        -- Contains information about an ingestion destination.
        ingestionDestination: IngestionDestination
    end

    record ListAppAuthorizationsInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The maximum number of results that are returned per call. You can use
        -- nextToken to obtain further pages of results.
        -- This is only an upper limit. The actual number of results returned per call might be
        -- fewer than the specified maximum.
        maxResults: number
        -- If nextToken is returned, there are more results available. The value of
        -- nextToken is a unique pagination token for each page. Make the call again
        -- using the returned token to retrieve the next page. Keep all other arguments unchanged.
        -- Each pagination token expires after 24 hours. Using an expired pagination token will return
        -- an HTTP 400 InvalidToken error.
        nextToken: string
    end

    record ListAppAuthorizationsOutput
        -- Contains a list of app authorization summaries.
        appAuthorizationSummaryList: {AppAuthorizationSummary}
        -- If nextToken is returned, there are more results available. The value of
        -- nextToken is a unique pagination token for each page. Make the call again
        -- using the returned token to retrieve the next page. Keep all other arguments unchanged.
        -- Each pagination token expires after 24 hours. Using an expired pagination token will return
        -- an HTTP 400 InvalidToken error.
        nextToken: string
    end

    record ListAppBundlesInput
        -- The maximum number of results that are returned per call. You can use
        -- nextToken to obtain further pages of results.
        -- This is only an upper limit. The actual number of results returned per call might be
        -- fewer than the specified maximum.
        maxResults: number
        -- If nextToken is returned, there are more results available. The value of
        -- nextToken is a unique pagination token for each page. Make the call again
        -- using the returned token to retrieve the next page. Keep all other arguments unchanged.
        -- Each pagination token expires after 24 hours. Using an expired pagination token will return
        -- an HTTP 400 InvalidToken error.
        nextToken: string
    end

    record ListAppBundlesOutput
        -- Contains a list of app bundle summaries.
        appBundleSummaryList: {AppBundleSummary}
        -- If nextToken is returned, there are more results available. The value of
        -- nextToken is a unique pagination token for each page. Make the call again
        -- using the returned token to retrieve the next page. Keep all other arguments unchanged.
        -- Each pagination token expires after 24 hours. Using an expired pagination token will return
        -- an HTTP 400 InvalidToken error.
        nextToken: string
    end

    record ListIngestionDestinationsInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion to
        -- use for the request.
        ingestionIdentifier: string
        -- The maximum number of results that are returned per call. You can use
        -- nextToken to obtain further pages of results.
        -- This is only an upper limit. The actual number of results returned per call might be
        -- fewer than the specified maximum.
        maxResults: number
        -- If nextToken is returned, there are more results available. The value of
        -- nextToken is a unique pagination token for each page. Make the call again
        -- using the returned token to retrieve the next page. Keep all other arguments unchanged.
        -- Each pagination token expires after 24 hours. Using an expired pagination token will return
        -- an HTTP 400 InvalidToken error.
        nextToken: string
    end

    -- Contains a summary of an ingestion destination.
    record IngestionDestinationSummary
        -- The Amazon Resource Name (ARN) of the ingestion destination.
        arn: string
    end

    record ListIngestionDestinationsOutput
        -- Contains a list of ingestion destination summaries.
        ingestionDestinations: {IngestionDestinationSummary}
        -- If nextToken is returned, there are more results available. The value of
        -- nextToken is a unique pagination token for each page. Make the call again
        -- using the returned token to retrieve the next page. Keep all other arguments unchanged.
        -- Each pagination token expires after 24 hours. Using an expired pagination token will return
        -- an HTTP 400 InvalidToken error.
        nextToken: string
    end

    record ListIngestionsInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The maximum number of results that are returned per call. You can use
        -- nextToken to obtain further pages of results.
        -- This is only an upper limit. The actual number of results returned per call might be
        -- fewer than the specified maximum.
        maxResults: number
        -- If nextToken is returned, there are more results available. The value of
        -- nextToken is a unique pagination token for each page. Make the call again
        -- using the returned token to retrieve the next page. Keep all other arguments unchanged.
        -- Each pagination token expires after 24 hours. Using an expired pagination token will return
        -- an HTTP 400 InvalidToken error.
        nextToken: string
    end

    -- Contains a summary of an ingestion.
    record IngestionSummary
        -- The Amazon Resource Name (ARN) of the ingestion.
        arn: string
        -- The name of the application.
        app: string
        -- The ID of the application tenant.
        tenantId: string
        -- The status of the ingestion.
        state: string
    end

    record ListIngestionsOutput
        -- Contains a list of ingestion summaries.
        ingestions: {IngestionSummary}
        -- If nextToken is returned, there are more results available. The value of
        -- nextToken is a unique pagination token for each page. Make the call again
        -- using the returned token to retrieve the next page. Keep all other arguments unchanged.
        -- Each pagination token expires after 24 hours. Using an expired pagination token will return
        -- an HTTP 400 InvalidToken error.
        nextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource for which you want to retrieve
        -- tags.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- A map of the key-value pairs for the tag or tags assigned to the specified resource.
        tags: {Tag}
    end

    record StartIngestionInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion to
        -- use for the request.
        ingestionIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
    end

    record StartIngestionOutput
    end

    record StartUserAccessTasksInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The email address of the target user.
        email: string
    end

    -- Contains information about a user access task.
    record UserAccessTaskItem
        -- The name of the application.
        app: string
        -- The ID of the application tenant.
        tenantId: string
        -- The unique ID of the task.
        taskId: string
        -- Error from the task, if any.
        error: TaskError
    end

    record StartUserAccessTasksOutput
        -- Contains a list of user access task information.
        userAccessTasksList: {UserAccessTaskItem}
    end

    record StopIngestionInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion to
        -- use for the request.
        ingestionIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
    end

    record StopIngestionOutput
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource that you want to tag.
        resourceArn: string
        -- A map of the key-value pairs of the tag or tags to assign to the resource.
        tags: {Tag}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource that you want to untag.
        resourceArn: string
        -- The keys of the key-value pairs for the tag or tags you want to remove from the
        -- specified resource.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateAppAuthorizationInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app
        -- authorization to use for the request.
        appAuthorizationIdentifier: string
        -- Contains credentials for the application, such as an API key or OAuth2 client ID and
        -- secret.
        -- Specify credentials that match the authorization type of the app authorization to
        -- update. For example, if the authorization type of the app authorization is OAuth2
        -- (oauth2), then you should provide only the OAuth2 credentials.
        credential: Credential
        -- Contains information about an application tenant, such as the application display name
        -- and identifier.
        tenant: Tenant
    end

    record UpdateAppAuthorizationOutput
        -- Contains information about an app authorization.
        appAuthorization: AppAuthorization
    end

    record UpdateIngestionDestinationInput
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the app bundle
        -- to use for the request.
        appBundleIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion to
        -- use for the request.
        ingestionIdentifier: string
        -- The Amazon Resource Name (ARN) or Universal Unique Identifier (UUID) of the ingestion
        -- destination to use for the request.
        ingestionDestinationIdentifier: string
        -- Contains information about the destination of ingested data.
        destinationConfiguration: DestinationConfiguration
    end

    record UpdateIngestionDestinationOutput
        -- Contains information about an ingestion destination.
        ingestionDestination: IngestionDestination
    end

end

return M
