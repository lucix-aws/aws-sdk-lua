-- Code generated by lua-client-codegen. DO NOT EDIT.

local waiter = require("smithy.waiter")

local M = {}

--- Wait until EventBusActive.
function M.wait_until_event_bus_active(client, input, options)
    return waiter.wait(client, "describeEventBus", input, {
        min_delay = 2,
        max_delay = 120,
        acceptors = {
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "State",
                        expected = "CREATE_FAILED",
                        comparator = "stringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "State",
                        expected = "UPDATE_FAILED",
                        comparator = "stringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "State",
                        expected = "DELETE_FAILED",
                        comparator = "stringEquals",
                    },
                },
            },
            {
                state = "success",
                matcher = {
                    output = {
                        path = "State",
                        expected = "ACTIVE",
                        comparator = "stringEquals",
                    },
                },
            },
        },
    }, options)
end

--- Wait until EventBusDeleted.
function M.wait_until_event_bus_deleted(client, input, options)
    return waiter.wait(client, "describeEventBus", input, {
        min_delay = 2,
        max_delay = 120,
        acceptors = {
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "State",
                        expected = "DELETE_FAILED",
                        comparator = "stringEquals",
                    },
                },
            },
            {
                state = "success",
                matcher = {
                    errorType = "ResourceNotFoundException",
                },
            },
        },
    }, options)
end

return M
