-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    enum AccessPolicyType
        "data"
    end

    -- When creating a resource, thrown when a resource with the same name already exists or is being created. When deleting a resource, thrown when the resource is not in the ACTIVE, FAILED, or UPDATE_FAILED state.
    record ConflictException
        message: string
    end

    record CreateAccessPolicyInput
        -- The name of the policy.
        name: string
        -- A description of the policy. Typically used to store information about the permissions defined in the policy.
        description: string
        -- The JSON policy document to use as the content for the policy.
        policy: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Details about an OpenSearch Serverless access policy.
    record AccessPolicyDetail
        -- The name of the policy.
        name: string
        -- The version of the policy.
        policyVersion: string
        -- The description of the policy.
        description: string
        -- The JSON policy document without any whitespaces.
        policy: any
        -- The date the policy was created.
        createdDate: number
        -- The timestamp of when the policy was last modified.
        lastModifiedDate: number
    end

    record CreateAccessPolicyOutput
        -- Details about the created access policy.
        accessPolicyDetail: AccessPolicyDetail
    end

    -- Thrown when an error internal to the service occurs while processing a request.
    record InternalServerException
        message: string
    end

    -- Thrown when you attempt to create more resources than the service allows based on service quotas.
    record ServiceQuotaExceededException
        -- Description of the error.
        message: string
        -- Identifier of the resource affected.
        resourceId: string
        -- Type of the resource affected.
        resourceType: string
        -- Service Quotas requirement to identify originating service.
        serviceCode: string
        -- Service Quotas requirement to identify originating quota.
        quotaCode: string
    end

    -- Thrown when the HTTP request contains invalid input or is missing required input.
    record ValidationException
        message: string
    end

    record DeleteAccessPolicyInput
        -- The name of the policy to delete.
        name: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    record DeleteAccessPolicyOutput
    end

    -- Thrown when accessing or deleting a resource that does not exist.
    record ResourceNotFoundException
        message: string
    end

    record GetAccessPolicyInput
        -- The name of the access policy.
        name: string
    end

    record GetAccessPolicyOutput
        -- Details about the requested access policy.
        accessPolicyDetail: AccessPolicyDetail
    end

    record ListAccessPoliciesInput
        -- Resource filters (can be collections or indexes) that policies can apply to.
        resource: {string}
        -- If your initial ListAccessPolicies operation returns a nextToken, you can include the returned nextToken in subsequent ListAccessPolicies operations, which returns results in the next page.
        nextToken: string
        -- An optional parameter that specifies the maximum number of results to return. You can use nextToken to get the next page of results. The default is 20.
        maxResults: number
    end

    -- A summary of the data access policy.
    record AccessPolicySummary
        -- The name of the access policy.
        name: string
        -- The version of the policy.
        policyVersion: string
        -- The description of the access policy.
        description: string
        -- The Epoch time when the access policy was created.
        createdDate: number
        -- The date and time when the collection was last modified.
        lastModifiedDate: number
    end

    record ListAccessPoliciesOutput
        -- Details about the requested access policies.
        accessPolicySummaries: {AccessPolicySummary}
        -- When nextToken is returned, there are more results available. The value of nextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page.
        nextToken: string
    end

    record UpdateAccessPolicyInput
        -- The name of the policy.
        name: string
        -- The version of the policy being updated.
        policyVersion: string
        -- A description of the policy. Typically used to store information about the permissions defined in the policy.
        description: string
        -- The JSON policy document to use as the content for the policy.
        policy: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    record UpdateAccessPolicyOutput
        -- Details about the updated access policy.
        accessPolicyDetail: AccessPolicyDetail
    end

    -- Statistics for an OpenSearch Serverless access policy.
    record AccessPolicyStats
        -- The number of data access policies in the current account.
        DataPolicyCount: number
    end

    -- The maximum capacity limits for all OpenSearch Serverless collections, in OpenSearch Compute Units (OCUs). These limits are used to scale your collections based on the current workload. For more information, see Managing capacity limits for Amazon OpenSearch Serverless.
    record CapacityLimits
        -- The maximum indexing capacity for collections.
        maxIndexingCapacityInOCU: number
        -- The maximum search capacity for collections.
        maxSearchCapacityInOCU: number
    end

    -- OpenSearch Serverless-related information for the current account.
    record AccountSettingsDetail
        capacityLimits: CapacityLimits
    end

    enum AutoscalingStatus
        "ACTION_SCALING_UP"
        "ACTION_SCALING_DOWN"
        "NO_ACTION"
    end

    record BatchGetCollectionInput
        -- A list of collection IDs. You can't provide names and IDs in the same request. The ID is part of the collection endpoint. You can also retrieve it using the ListCollections API.
        ids: {string}
        -- A list of collection names. You can't provide names and IDs in the same request.
        names: {string}
    end

    enum DeletionProtection
        "ENABLED"
        "DISABLED"
    end

    -- FIPS-compliant endpoint URLs for an OpenSearch Serverless collection. These endpoints ensure all data transmission uses FIPS 140-3 validated cryptographic implementations, meeting federal security requirements for government workloads.
    record FipsEndpoints
        -- FIPS-compliant collection endpoint used to submit index, search, and data upload requests to an OpenSearch Serverless collection. This endpoint uses FIPS 140-3 validated cryptography and is required for federal government workloads.
        collectionEndpoint: string
        -- FIPS-compliant endpoint used to access OpenSearch Dashboards. This endpoint uses FIPS 140-3 validated cryptography and is required for federal government workloads that need dashboard visualization capabilities.
        dashboardEndpoint: string
    end

    enum StandbyReplicas
        "ENABLED"
        "DISABLED"
    end

    enum CollectionStatus
        "CREATING"
        "UPDATING"
        "DELETING"
        "ACTIVE"
        "FAILED"
        "UPDATE_FAILED"
    end

    enum CollectionType
        "SEARCH"
        "TIMESERIES"
        "VECTORSEARCH"
    end

    enum ServerlessVectorAccelerationStatus
        "ENABLED"
        "DISABLED"
        "ALLOWED"
    end

    -- Configuration options for vector search capabilities in an OpenSearch Serverless collection.
    record VectorOptions
        -- Specifies whether serverless vector acceleration is enabled for the collection.
        ServerlessVectorAcceleration: string
    end

    -- Details about each OpenSearch Serverless collection, including the collection endpoint, the OpenSearch Dashboards endpoint, and FIPS-compliant endpoints for federal government workloads.
    record CollectionDetail
        -- A unique identifier for the collection.
        id: string
        -- The name of the collection.
        name: string
        -- The current status of the collection.
        status: string
        -- A description of the collection.
        description: string
        -- The Amazon Resource Name (ARN) of the collection.
        arn: string
        -- The ARN of the Amazon Web Services KMS key used to encrypt the collection.
        kmsKeyArn: string
        -- Details about an OpenSearch Serverless collection.
        standbyReplicas: string
        -- Indicates whether deletion protection is ENABLED or DISABLED for the collection.
        deletionProtection: string
        -- Configuration options for vector search capabilities in the collection.
        vectorOptions: VectorOptions
        -- The Epoch time when the collection was created.
        createdDate: number
        -- The date and time when the collection was last modified.
        lastModifiedDate: number
        -- Collection-specific endpoint used to submit index, search, and data upload requests to an OpenSearch Serverless collection.
        collectionEndpoint: string
        -- Collection-specific endpoint used to access OpenSearch Dashboards.
        dashboardEndpoint: string
        -- FIPS-compliant endpoints for the collection. These endpoints use FIPS 140-3 validated cryptographic modules and are required for federal government workloads that must comply with FedRAMP security standards.
        fipsEndpoints: FipsEndpoints
        -- A failure code associated with the request.
        failureCode: string
        -- A message associated with the failure code.
        failureMessage: string
        -- The name of the collection group that contains this collection.
        collectionGroupName: string
    end

    -- Error information for an OpenSearch Serverless request.
    record CollectionErrorDetail
        -- If the request contains collection IDs, the response includes the IDs provided in the request.
        id: string
        -- If the request contains collection names, the response includes the names provided in the request.
        name: string
        -- A description of the error. For example, The specified Collection is not found.
        errorMessage: string
        -- The error code for the request. For example, NOT_FOUND.
        errorCode: string
    end

    record BatchGetCollectionOutput
        -- Details about each collection.
        collectionDetails: {CollectionDetail}
        -- Error information for the request.
        collectionErrorDetails: {CollectionErrorDetail}
    end

    record BatchGetCollectionGroupInput
        -- A list of collection group IDs. You can't provide names and IDs in the same request.
        ids: {string}
        -- A list of collection group names. You can't provide names and IDs in the same request.
        names: {string}
    end

    -- Capacity limits for a collection group. These limits define the minimum and maximum OpenSearch Compute Units (OCUs) for indexing and search operations that can be used by collections in the group.
    record CollectionGroupCapacityLimits
        -- The maximum indexing capacity for collections in the group.
        maxIndexingCapacityInOCU: number
        -- The maximum search capacity for collections in the group.
        maxSearchCapacityInOCU: number
        -- The minimum indexing capacity for collections in the group.
        minIndexingCapacityInOCU: number
        -- The minimum search capacity for collections in the group.
        minSearchCapacityInOCU: number
    end

    -- Capacity details for an OpenSearch Serverless collection group, including the current capacity and autoscaling status.
    record CapacityDetails
        -- The current capacity in OpenSearch Compute Units (OCUs).
        capacityInOcu: number
        -- The current autoscaling status for the collection group.
        autoscalingStatus: string
    end

    -- Current search and indexing capacity for an OpenSearch Serverless collection group. Measured in OpenSearch Compute Units (OCUs).
    record CurrentCapacity
        -- The search capacity for the collection group.
        search: CapacityDetails
        -- The indexing capacity for the collection group.
        indexing: CapacityDetails
    end

    enum ServerlessGeneration
        "CLASSIC"
        "NEXTGEN"
    end

    -- A map of key-value pairs associated to an OpenSearch Serverless resource.
    record Tag
        -- The key to use in the tag.
        key: string
        -- The value of the tag.
        value: string
    end

    -- Details about a collection group.
    record CollectionGroupDetail
        -- The unique identifier of the collection group.
        id: string
        -- The Amazon Resource Name (ARN) of the collection group.
        arn: string
        -- The name of the collection group.
        name: string
        -- Indicates whether standby replicas are used for the collection group.
        standbyReplicas: string
        -- The description of the collection group.
        description: string
        -- A map of key-value pairs associated with the collection group.
        tags: {Tag}
        -- The Epoch time when the collection group was created.
        createdDate: number
        -- The capacity limits for the collection group, in OpenSearch Compute Units (OCUs).
        capacityLimits: CollectionGroupCapacityLimits
        -- Current search and indexing capacity for the collection group.
        currentCapacity: CurrentCapacity
        -- The number of collections associated with the collection group.
        numberOfCollections: number
        -- The generation of Amazon OpenSearch Serverless for the collection group.
        generation: string
    end

    -- Error details for a collection group operation.
    record CollectionGroupErrorDetail
        -- If the request contains collection group IDs, the response includes the IDs provided in the request.
        id: string
        -- If the request contains collection group names, the response includes the names provided in the request.
        name: string
        -- A description of the error. For example, The specified Collection Group is not found.
        errorMessage: string
        -- The error code for the request. For example, NOT_FOUND.
        errorCode: string
    end

    record BatchGetCollectionGroupOutput
        -- Details about each collection group.
        collectionGroupDetails: {CollectionGroupDetail}
        -- Error information for the request.
        collectionGroupErrorDetails: {CollectionGroupErrorDetail}
    end

    enum LifecyclePolicyType
        "retention"
    end

    -- The unique identifiers of policy types and resource names.
    record LifecyclePolicyResourceIdentifier
        -- The name of the OpenSearch Serverless ilndex resource.
        resource: string
    end

    record BatchGetEffectiveLifecyclePolicyInput
        -- The unique identifiers of policy types and resource names.
        resourceIdentifiers: {LifecyclePolicyResourceIdentifier}
    end

    enum ResourceType
        "index"
    end

    -- Error information for an OpenSearch Serverless request.
    record EffectiveLifecyclePolicyDetail
        -- The name of the OpenSearch Serverless index resource.
        resource: string
        -- The name of the lifecycle policy.
        policyName: string
        -- The type of OpenSearch Serverless resource. Currently, the only supported resource is index.
        resourceType: string
        -- The minimum number of index retention in days or hours. This is an optional parameter that will return only if it’s set.
        retentionPeriod: string
        -- The minimum number of index retention days set. That is an optional param that will return as true if the minimum number of days or hours is not set to a index resource.
        noMinRetentionPeriod: boolean
    end

    -- Error information for an OpenSearch Serverless request.
    record EffectiveLifecyclePolicyErrorDetail
        -- The name of OpenSearch Serverless index resource.
        resource: string
        -- A description of the error. For example, The specified Index resource is not found.
        errorMessage: string
        -- The error code for the request.
        errorCode: string
    end

    record BatchGetEffectiveLifecyclePolicyOutput
        -- A list of lifecycle policies applied to the OpenSearch Serverless indexes.
        effectiveLifecyclePolicyDetails: {EffectiveLifecyclePolicyDetail}
        -- A list of resources for which retrieval failed.
        effectiveLifecyclePolicyErrorDetails: {EffectiveLifecyclePolicyErrorDetail}
    end

    -- The unique identifiers of policy types and policy names.
    record LifecyclePolicyIdentifier
        -- The name of the lifecycle policy.
        name: string
    end

    record BatchGetLifecyclePolicyInput
        -- The unique identifiers of policy types and policy names.
        identifiers: {LifecyclePolicyIdentifier}
    end

    -- Details about an OpenSearch Serverless lifecycle policy.
    record LifecyclePolicyDetail
        -- The name of the lifecycle policy.
        name: string
        -- The version of the lifecycle policy.
        policyVersion: string
        -- The description of the lifecycle policy.
        description: string
        -- The JSON policy document without any whitespaces.
        policy: any
        -- The date the lifecycle policy was created.
        createdDate: number
        -- The timestamp of when the lifecycle policy was last modified.
        lastModifiedDate: number
    end

    -- Error information for an OpenSearch Serverless request.
    record LifecyclePolicyErrorDetail
        -- The name of the lifecycle policy.
        name: string
        -- A description of the error. For example, The specified Lifecycle Policy is not found.
        errorMessage: string
        -- The error code for the request. For example, NOT_FOUND.
        errorCode: string
    end

    record BatchGetLifecyclePolicyOutput
        -- A list of lifecycle policies matched to the input policy name and policy type.
        lifecyclePolicyDetails: {LifecyclePolicyDetail}
        -- A list of lifecycle policy names and policy types for which retrieval failed.
        lifecyclePolicyErrorDetails: {LifecyclePolicyErrorDetail}
    end

    record BatchGetVpcEndpointInput
        -- A list of VPC endpoint identifiers.
        ids: {string}
    end

    enum VpcEndpointStatus
        "PENDING"
        "DELETING"
        "ACTIVE"
        "FAILED"
    end

    -- Details about an OpenSearch Serverless-managed interface endpoint.
    record VpcEndpointDetail
        -- The unique identifier of the endpoint.
        id: string
        -- The name of the endpoint.
        name: string
        -- The ID of the VPC from which you access OpenSearch Serverless.
        vpcId: string
        -- The ID of the subnets from which you access OpenSearch Serverless.
        subnetIds: {string}
        -- The unique identifiers of the security groups that define the ports, protocols, and sources for inbound traffic that you are authorizing into your endpoint.
        securityGroupIds: {string}
        -- The current status of the endpoint.
        status: string
        -- The date the endpoint was created.
        createdDate: number
        -- A failure code associated with the request.
        failureCode: string
        -- A message associated with the failure code.
        failureMessage: string
    end

    -- Error information for a failed BatchGetVpcEndpoint request.
    record VpcEndpointErrorDetail
        -- The unique identifier of the VPC endpoint.
        id: string
        -- An error message describing the reason for the failure.
        errorMessage: string
        -- The error code for the failed request.
        errorCode: string
    end

    record BatchGetVpcEndpointOutput
        -- Details about the specified VPC endpoint.
        vpcEndpointDetails: {VpcEndpointDetail}
        -- Error information for a failed request.
        vpcEndpointErrorDetails: {VpcEndpointErrorDetail}
    end

    -- Encryption settings for a collection.
    record EncryptionConfig
        -- Indicates whether to use an Amazon Web Services-owned key for encryption.
        aWSOwnedKey: boolean
        -- The ARN of the Amazon Web Services Key Management Service key used to encrypt the collection.
        kmsKeyArn: string
    end

    record CreateCollectionInput
        -- Name of the collection.
        name: string
        -- Description of the collection.
        description: string
        -- An arbitrary set of tags (key–value pairs) to associate with the OpenSearch Serverless collection.
        tags: {Tag}
        -- Indicates whether standby replicas should be used for a collection.
        standbyReplicas: string
        -- Configuration options for vector search capabilities in the collection.
        vectorOptions: VectorOptions
        -- The name of the collection group to associate with the collection.
        collectionGroupName: string
        -- Encryption settings for the collection.
        encryptionConfig: EncryptionConfig
        -- Indicates whether to enable deletion protection for the collection. When set to ENABLED, the collection cannot be deleted.
        deletionProtection: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Details about the created OpenSearch Serverless collection.
    record CreateCollectionDetail
        -- The unique identifier of the collection.
        id: string
        -- The name of the collection.
        name: string
        -- The current status of the collection.
        status: string
        -- A description of the collection.
        description: string
        -- The Amazon Resource Name (ARN) of the collection.
        arn: string
        -- The Amazon Resource Name (ARN) of the KMS key with which to encrypt the collection.
        kmsKeyArn: string
        -- Creates details about an OpenSearch Serverless collection.
        standbyReplicas: string
        -- Indicates whether deletion protection is ENABLED or DISABLED for the collection.
        deletionProtection: string
        -- Configuration options for vector search capabilities in the collection.
        vectorOptions: VectorOptions
        -- The Epoch time when the collection was created.
        createdDate: number
        -- The date and time when the collection was last modified.
        lastModifiedDate: number
        -- The name of the collection group that contains this collection.
        collectionGroupName: string
    end

    record CreateCollectionOutput
        -- Details about the collection.
        createCollectionDetail: CreateCollectionDetail
    end

    -- Thrown when the collection you're attempting to create results in a number of search or indexing OCUs that exceeds the account limit.
    record OcuLimitExceededException
        -- Description of the error.
        message: string
    end

    record DeleteCollectionInput
        -- The unique identifier of the collection. For example, 1iu5usc406kd. The ID is part of the collection endpoint. You can also retrieve it using the ListCollections API.
        id: string
        -- A unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Details about a deleted OpenSearch Serverless collection.
    record DeleteCollectionDetail
        -- The unique identifier of the collection.
        id: string
        -- The name of the collection.
        name: string
        -- The current status of the collection.
        status: string
        -- Indicates whether deletion protection is ENABLED or DISABLED for the collection.
        deletionProtection: string
    end

    record DeleteCollectionOutput
        -- Details of the deleted collection.
        deleteCollectionDetail: DeleteCollectionDetail
    end

    -- A list of filter keys that you can use for LIST, UPDATE, and DELETE requests to OpenSearch Serverless collections.
    record CollectionFilters
        -- The name of the collection.
        name: string
        -- The current status of the collection.
        status: string
        -- The name of the collection group to filter by.
        collectionGroupName: string
    end

    record ListCollectionsInput
        -- A list of filter names and values that you can use for requests.
        collectionFilters: CollectionFilters
        -- If your initial ListCollections operation returns a nextToken, you can include the returned nextToken in subsequent ListCollections operations, which returns results in the next page.
        nextToken: string
        -- The maximum number of results to return. Default is 20. You can use nextToken to get the next page of results.
        maxResults: number
    end

    -- Details about each OpenSearch Serverless collection.
    record CollectionSummary
        -- The unique identifier of the collection.
        id: string
        -- The name of the collection.
        name: string
        -- The current status of the collection.
        status: string
        -- The Amazon Resource Name (ARN) of the collection.
        arn: string
        -- The ARN of the Amazon Web Services Key Management Service key used to encrypt the collection.
        kmsKeyArn: string
        -- The name of the collection group that contains this collection.
        collectionGroupName: string
    end

    record ListCollectionsOutput
        -- Details about each collection.
        collectionSummaries: {CollectionSummary}
        -- When nextToken is returned, there are more results available. The value of nextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page.
        nextToken: string
    end

    record UpdateCollectionInput
        -- The unique identifier of the collection.
        id: string
        -- A description of the collection.
        description: string
        -- Configuration options for vector search capabilities in the collection.
        vectorOptions: VectorOptions
        -- Indicates whether to enable or disable deletion protection for the collection. When set to ENABLED, the collection cannot be deleted.
        deletionProtection: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Details about an updated OpenSearch Serverless collection.
    record UpdateCollectionDetail
        -- The unique identifier of the collection.
        id: string
        -- The name of the collection.
        name: string
        -- The current status of the collection.
        status: string
        -- The description of the collection.
        description: string
        -- Configuration options for vector search capabilities in the collection.
        vectorOptions: VectorOptions
        -- The Amazon Resource Name (ARN) of the collection.
        arn: string
        -- The date and time when the collection was created.
        createdDate: number
        -- The date and time when the collection was last modified.
        lastModifiedDate: number
        -- Indicates whether deletion protection is ENABLED or DISABLED for the collection.
        deletionProtection: string
    end

    record UpdateCollectionOutput
        -- Details about the updated collection.
        updateCollectionDetail: UpdateCollectionDetail
    end

    record CreateCollectionGroupInput
        -- The name of the collection group.
        name: string
        -- Indicates whether standby replicas should be used for a collection group.
        standbyReplicas: string
        -- A description of the collection group.
        description: string
        -- An arbitrary set of tags (key–value pairs) to associate with the OpenSearch Serverless collection group.
        tags: {Tag}
        -- The capacity limits for the collection group, in OpenSearch Compute Units (OCUs). These limits control the maximum and minimum capacity for collections within the group.
        capacityLimits: CollectionGroupCapacityLimits
        -- The generation of Amazon OpenSearch Serverless for the collection group. Valid values are CLASSIC and NEXTGEN.
        generation: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Details about the created collection group.
    record CreateCollectionGroupDetail
        -- The unique identifier of the collection group.
        id: string
        -- The Amazon Resource Name (ARN) of the collection group.
        arn: string
        -- The name of the collection group.
        name: string
        -- Indicates whether standby replicas are used for the collection group.
        standbyReplicas: string
        -- The description of the collection group.
        description: string
        -- A map of key-value pairs associated with the collection group.
        tags: {Tag}
        -- The Epoch time when the collection group was created.
        createdDate: number
        -- The capacity limits for the collection group, in OpenSearch Compute Units (OCUs).
        capacityLimits: CollectionGroupCapacityLimits
        -- The generation of Amazon OpenSearch Serverless for the collection group.
        generation: string
    end

    record CreateCollectionGroupOutput
        -- Details about the created collection group.
        createCollectionGroupDetail: CreateCollectionGroupDetail
    end

    record DeleteCollectionGroupInput
        -- The unique identifier of the collection group to delete.
        id: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    record DeleteCollectionGroupOutput
    end

    record ListCollectionGroupsInput
        -- If your initial ListCollectionGroups operation returns a nextToken, you can include the returned nextToken in subsequent ListCollectionGroups operations, which returns results in the next page.
        nextToken: string
        -- The maximum number of results to return. Default is 20. You can use nextToken to get the next page of results.
        maxResults: number
    end

    -- Summary information about a collection group.
    record CollectionGroupSummary
        -- The unique identifier of the collection group.
        id: string
        -- The Amazon Resource Name (ARN) of the collection group.
        arn: string
        -- The name of the collection group.
        name: string
        -- The number of collections within the collection group.
        numberOfCollections: number
        -- The Epoch time when the collection group was created.
        createdDate: number
        capacityLimits: CollectionGroupCapacityLimits
        -- The generation of Amazon OpenSearch Serverless for the collection group.
        generation: string
    end

    record ListCollectionGroupsOutput
        -- Details about each collection group.
        collectionGroupSummaries: {CollectionGroupSummary}
        -- When nextToken is returned, there are more results available. The value of nextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page.
        nextToken: string
    end

    record UpdateCollectionGroupInput
        -- The unique identifier of the collection group to update.
        id: string
        -- A new description for the collection group.
        description: string
        -- Updated capacity limits for the collection group, in OpenSearch Compute Units (OCUs).
        capacityLimits: CollectionGroupCapacityLimits
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Details about the updated collection group.
    record UpdateCollectionGroupDetail
        -- The unique identifier of the collection group.
        id: string
        -- The Amazon Resource Name (ARN) of the collection group.
        arn: string
        -- The name of the collection group.
        name: string
        -- The description of the collection group.
        description: string
        -- The capacity limits for the collection group, in OpenSearch Compute Units (OCUs).
        capacityLimits: CollectionGroupCapacityLimits
        -- The Epoch time when the collection group was created.
        createdDate: number
        -- The date and time when the collection group was last modified.
        lastModifiedDate: number
        -- The generation of Amazon OpenSearch Serverless for the collection group.
        generation: string
    end

    record UpdateCollectionGroupOutput
        -- Details about the updated collection group.
        updateCollectionGroupDetail: UpdateCollectionGroupDetail
    end

    enum IamIdentityCenterGroupAttribute
        "GroupId"
        "GroupName"
    end

    enum IamIdentityCenterUserAttribute
        "UserId"
        "UserName"
        "Email"
    end

    -- Describes IAM Identity Center options for creating an OpenSearch Serverless security configuration in the form of a key-value map.
    record CreateIamIdentityCenterConfigOptions
        -- The ARN of the IAM Identity Center instance used to integrate with OpenSearch Serverless.
        instanceArn: string
        -- The user attribute for this IAM Identity Center integration. Defaults to UserId.
        userAttribute: string
        -- The group attribute for this IAM Identity Center integration. Defaults to GroupId.
        groupAttribute: string
    end

    record CreateIndexInput
        -- The unique identifier of the collection in which to create the index.
        id: string
        -- The name of the index to create. Index names must be lowercase and can't begin with underscores (_) or hyphens (-).
        indexName: string
        -- The JSON schema definition for the index, including field mappings and settings.
        indexSchema: any
    end

    record CreateIndexOutput
    end

    record CreateLifecyclePolicyInput
        -- The name of the lifecycle policy.
        name: string
        -- A description of the lifecycle policy.
        description: string
        -- The JSON policy document to use as the content for the lifecycle policy.
        policy: string
        -- A unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    record CreateLifecyclePolicyOutput
        -- Details about the created lifecycle policy.
        lifecyclePolicyDetail: LifecyclePolicyDetail
    end

    -- Describes IAM federation options for an OpenSearch Serverless security configuration in the form of a key-value map. These options define how OpenSearch Serverless integrates with external identity providers using federation.
    record IamFederationConfigOptions
        -- The group attribute for this IAM federation integration. This attribute is used to map identity provider groups to OpenSearch Serverless permissions.
        groupAttribute: string
        -- The user attribute for this IAM federation integration. This attribute is used to identify users in the federated authentication process.
        userAttribute: string
    end

    -- Describes SAML options for an OpenSearch Serverless security configuration in the form of a key-value map.
    record SamlConfigOptions
        -- The XML IdP metadata file generated from your identity provider.
        metadata: string
        -- A user attribute for this SAML integration.
        userAttribute: string
        -- The group attribute for this SAML integration.
        groupAttribute: string
        -- Custom entity ID attribute to override the default entity ID for this SAML integration.
        openSearchServerlessEntityId: string
        -- The session timeout, in minutes. Default is 60 minutes (12 hours).
        sessionTimeout: number
    end

    enum SecurityConfigType
        "saml"
        "iamidentitycenter"
        "iamfederation"
    end

    record CreateSecurityConfigInput
        -- The name of the security configuration.
        name: string
        -- A description of the security configuration.
        description: string
        -- Describes SAML options in the form of a key-value map. This field is required if you specify SAML for the type parameter.
        samlOptions: SamlConfigOptions
        -- Describes IAM Identity Center options in the form of a key-value map. This field is required if you specify iamidentitycenter for the type parameter.
        iamIdentityCenterOptions: CreateIamIdentityCenterConfigOptions
        -- Describes IAM federation options in the form of a key-value map. This field is required if you specify iamFederation for the type parameter.
        iamFederationOptions: IamFederationConfigOptions
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Describes IAM Identity Center options for an OpenSearch Serverless security configuration in the form of a key-value map.
    record IamIdentityCenterConfigOptions
        -- The ARN of the IAM Identity Center instance used to integrate with OpenSearch Serverless.
        instanceArn: string
        -- The ARN of the IAM Identity Center application used to integrate with OpenSearch Serverless.
        applicationArn: string
        -- The name of the IAM Identity Center application used to integrate with OpenSearch Serverless.
        applicationName: string
        -- The description of the IAM Identity Center application used to integrate with OpenSearch Serverless.
        applicationDescription: string
        -- The user attribute for this IAM Identity Center integration. Defaults to UserId
        userAttribute: string
        -- The group attribute for this IAM Identity Center integration. Defaults to GroupId.
        groupAttribute: string
    end

    -- Details about a security configuration for OpenSearch Serverless.
    record SecurityConfigDetail
        -- The unique identifier of the security configuration.
        id: string
        -- The version of the security configuration.
        configVersion: string
        -- The description of the security configuration.
        description: string
        -- SAML options for the security configuration in the form of a key-value map.
        samlOptions: SamlConfigOptions
        -- Describes IAM Identity Center options in the form of a key-value map.
        iamIdentityCenterOptions: IamIdentityCenterConfigOptions
        -- Describes IAM federation options in the form of a key-value map. Contains configuration details about how OpenSearch Serverless integrates with external identity providers through federation.
        iamFederationOptions: IamFederationConfigOptions
        -- The date the configuration was created.
        createdDate: number
        -- The timestamp of when the configuration was last modified.
        lastModifiedDate: number
    end

    record CreateSecurityConfigOutput
        -- Details about the created security configuration.
        securityConfigDetail: SecurityConfigDetail
    end

    enum SecurityPolicyType
        "encryption"
        "network"
    end

    record CreateSecurityPolicyInput
        -- The name of the policy.
        name: string
        -- A description of the policy. Typically used to store information about the permissions defined in the policy.
        description: string
        -- The JSON policy document to use as the content for the new policy.
        policy: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Details about an OpenSearch Serverless security policy.
    record SecurityPolicyDetail
        -- The name of the policy.
        name: string
        -- The version of the policy.
        policyVersion: string
        -- The description of the security policy.
        description: string
        -- The JSON policy document without any whitespaces.
        policy: any
        -- The date the policy was created.
        createdDate: number
        -- The timestamp of when the policy was last modified.
        lastModifiedDate: number
    end

    record CreateSecurityPolicyOutput
        -- Details about the created security policy.
        securityPolicyDetail: SecurityPolicyDetail
    end

    record CreateVpcEndpointInput
        -- The name of the interface endpoint.
        name: string
        -- The ID of the VPC from which you'll access OpenSearch Serverless.
        vpcId: string
        -- The ID of one or more subnets from which you'll access OpenSearch Serverless.
        subnetIds: {string}
        -- The unique identifiers of the security groups that define the ports, protocols, and sources for inbound traffic that you are authorizing into your endpoint.
        securityGroupIds: {string}
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Creation details for an OpenSearch Serverless-managed interface endpoint. For more information, see Access Amazon OpenSearch Serverless using an interface endpoint.
    record CreateVpcEndpointDetail
        -- The unique identifier of the endpoint.
        id: string
        -- The name of the endpoint.
        name: string
        -- The current status in the endpoint creation process.
        status: string
    end

    record CreateVpcEndpointOutput
        -- Details about the created interface VPC endpoint.
        createVpcEndpointDetail: CreateVpcEndpointDetail
    end

    record DeleteIndexInput
        -- The unique identifier of the collection containing the index to delete.
        id: string
        -- The name of the index to delete.
        indexName: string
    end

    record DeleteIndexOutput
    end

    record DeleteLifecyclePolicyInput
        -- The name of the policy to delete.
        name: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    record DeleteLifecyclePolicyOutput
    end

    record DeleteSecurityConfigInput
        -- The security configuration identifier. For SAML the ID will be saml/<accountId>/<idpProviderName>. For example, saml/123456789123/OKTADev.
        id: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    record DeleteSecurityConfigOutput
    end

    record DeleteSecurityPolicyInput
        -- The name of the policy to delete.
        name: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    record DeleteSecurityPolicyOutput
    end

    record DeleteVpcEndpointInput
        -- The VPC endpoint identifier.
        id: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Deletion details for an OpenSearch Serverless-managed interface endpoint.
    record DeleteVpcEndpointDetail
        -- The unique identifier of the endpoint.
        id: string
        -- The name of the endpoint.
        name: string
        -- The current status of the endpoint deletion process.
        status: string
    end

    record DeleteVpcEndpointOutput
        -- Details about the deleted endpoint.
        deleteVpcEndpointDetail: DeleteVpcEndpointDetail
    end

    record GetAccountSettingsInput
    end

    record GetAccountSettingsOutput
        -- OpenSearch Serverless-related details for the current account.
        accountSettingsDetail: AccountSettingsDetail
    end

    record GetIndexInput
        -- The unique identifier of the collection containing the index.
        id: string
        -- The name of the index to retrieve information about.
        indexName: string
    end

    record GetIndexOutput
        -- The JSON schema definition for the index, including field mappings and settings.
        indexSchema: any
    end

    record GetPoliciesStatsInput
    end

    -- Statistics for an OpenSearch Serverless lifecycle policy.
    record LifecyclePolicyStats
        -- The number of retention lifecycle policies in the current account.
        RetentionPolicyCount: number
    end

    -- Statistics for an OpenSearch Serverless security configuration.
    record SecurityConfigStats
        -- The number of security configurations in the current account.
        SamlConfigCount: number
    end

    -- Statistics for an OpenSearch Serverless security policy.
    record SecurityPolicyStats
        -- The number of encryption policies in the current account.
        EncryptionPolicyCount: number
        -- The number of network policies in the current account.
        NetworkPolicyCount: number
    end

    record GetPoliciesStatsOutput
        -- Information about the data access policies in your account.
        AccessPolicyStats: AccessPolicyStats
        -- Information about the security policies in your account.
        SecurityPolicyStats: SecurityPolicyStats
        -- Information about the security configurations in your account.
        SecurityConfigStats: SecurityConfigStats
        -- Information about the lifecycle policies in your account.
        LifecyclePolicyStats: LifecyclePolicyStats
        -- The total number of OpenSearch Serverless security policies and configurations in your account.
        TotalPolicyCount: number
    end

    record GetSecurityConfigInput
        -- The unique identifier of the security configuration.
        id: string
    end

    record GetSecurityConfigOutput
        -- Details of the requested security configuration.
        securityConfigDetail: SecurityConfigDetail
    end

    record GetSecurityPolicyInput
        -- The name of the security policy.
        name: string
    end

    record GetSecurityPolicyOutput
        -- Details about the requested security policy.
        securityPolicyDetail: SecurityPolicyDetail
    end

    record UpdateIndexInput
        -- The unique identifier of the collection containing the index to update.
        id: string
        -- The name of the index to update.
        indexName: string
        -- The updated JSON schema definition for the index, including field mappings and settings.
        indexSchema: any
    end

    record UpdateIndexOutput
    end

    record ListLifecyclePoliciesInput
        -- Resource filters that policies can apply to. Currently, the only supported resource type is index.
        resources: {string}
        -- If your initial ListLifecyclePolicies operation returns a nextToken, you can include the returned nextToken in subsequent ListLifecyclePolicies operations, which returns results in the next page.
        nextToken: string
        -- An optional parameter that specifies the maximum number of results to return. You can use use nextToken to get the next page of results. The default is 10.
        maxResults: number
    end

    -- A summary of the lifecycle policy.
    record LifecyclePolicySummary
        -- The name of the lifecycle policy.
        name: string
        -- The version of the lifecycle policy.
        policyVersion: string
        -- The description of the lifecycle policy.
        description: string
        -- The Epoch time when the lifecycle policy was created.
        createdDate: number
        -- The date and time when the lifecycle policy was last modified.
        lastModifiedDate: number
    end

    record ListLifecyclePoliciesOutput
        -- Details about the requested lifecycle policies.
        lifecyclePolicySummaries: {LifecyclePolicySummary}
        -- When nextToken is returned, there are more results available. The value of nextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page.
        nextToken: string
    end

    record UpdateLifecyclePolicyInput
        -- The name of the policy.
        name: string
        -- The version of the policy being updated.
        policyVersion: string
        -- A description of the lifecycle policy.
        description: string
        -- The JSON policy document to use as the content for the lifecycle policy.
        policy: string
        -- A unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    record UpdateLifecyclePolicyOutput
        -- Details about the updated lifecycle policy.
        lifecyclePolicyDetail: LifecyclePolicyDetail
    end

    record ListSecurityConfigsInput
        -- If your initial ListSecurityConfigs operation returns a nextToken, you can include the returned nextToken in subsequent ListSecurityConfigs operations, which returns results in the next page.
        nextToken: string
        -- An optional parameter that specifies the maximum number of results to return. You can use nextToken to get the next page of results. The default is 20.
        maxResults: number
    end

    -- A summary of a security configuration for OpenSearch Serverless.
    record SecurityConfigSummary
        -- The unique identifier of the security configuration.
        id: string
        -- The version of the security configuration.
        configVersion: string
        -- The description of the security configuration.
        description: string
        -- The Epoch time when the security configuration was created.
        createdDate: number
        -- The timestamp of when the configuration was last modified.
        lastModifiedDate: number
    end

    record ListSecurityConfigsOutput
        -- Details about the security configurations in your account.
        securityConfigSummaries: {SecurityConfigSummary}
        -- When nextToken is returned, there are more results available. The value of nextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page.
        nextToken: string
    end

    record ListSecurityPoliciesInput
        -- Resource filters (can be collection or indexes) that policies can apply to.
        resource: {string}
        -- If your initial ListSecurityPolicies operation returns a nextToken, you can include the returned nextToken in subsequent ListSecurityPolicies operations, which returns results in the next page.
        nextToken: string
        -- An optional parameter that specifies the maximum number of results to return. You can use nextToken to get the next page of results. The default is 20.
        maxResults: number
    end

    -- A summary of a security policy for OpenSearch Serverless.
    record SecurityPolicySummary
        -- The name of the policy.
        name: string
        -- The version of the policy.
        policyVersion: string
        -- The description of the security policy.
        description: string
        -- The date the policy was created.
        createdDate: number
        -- The timestamp of when the policy was last modified.
        lastModifiedDate: number
    end

    record ListSecurityPoliciesOutput
        -- Details about the security policies in your account.
        securityPolicySummaries: {SecurityPolicySummary}
        -- When nextToken is returned, there are more results available. The value of nextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page.
        nextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource. The resource must be active (not in the DELETING state), and must be owned by the account ID included in the request.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- The tags associated with the resource.
        tags: {Tag}
    end

    -- Filter the results of a ListVpcEndpoints request.
    record VpcEndpointFilters
        -- The current status of the endpoint.
        status: string
    end

    record ListVpcEndpointsInput
        -- Filter the results according to the current status of the VPC endpoint. Possible statuses are CREATING, DELETING, UPDATING, ACTIVE, and FAILED.
        vpcEndpointFilters: VpcEndpointFilters
        -- If your initial ListVpcEndpoints operation returns a nextToken, you can include the returned nextToken in subsequent ListVpcEndpoints operations, which returns results in the next page.
        nextToken: string
        -- An optional parameter that specifies the maximum number of results to return. You can use nextToken to get the next page of results. The default is 20.
        maxResults: number
    end

    -- The VPC endpoint object.
    record VpcEndpointSummary
        -- The unique identifier of the endpoint.
        id: string
        -- The name of the endpoint.
        name: string
        -- The current status of the endpoint.
        status: string
    end

    record ListVpcEndpointsOutput
        -- Details about each VPC endpoint, including the name and current status.
        vpcEndpointSummaries: {VpcEndpointSummary}
        -- When nextToken is returned, there are more results available. The value of nextToken is a unique pagination token for each page. Make the call again using the returned token to retrieve the next page.
        nextToken: string
    end

    -- Describes IAM Identity Center options for updating an OpenSearch Serverless security configuration in the form of a key-value map.
    record UpdateIamIdentityCenterConfigOptions
        -- The user attribute for this IAM Identity Center integration. Defaults to UserId.
        userAttribute: string
        -- The group attribute for this IAM Identity Center integration. Defaults to GroupId.
        groupAttribute: string
    end

    record UpdateSecurityConfigInput
        -- The security configuration identifier. For SAML the ID will be saml/<accountId>/<idpProviderName>. For example, saml/123456789123/OKTADev.
        id: string
        -- The version of the security configuration to be updated. You can find the most recent version of a security configuration using the GetSecurityPolicy command.
        configVersion: string
        -- A description of the security configuration.
        description: string
        -- SAML options in in the form of a key-value map.
        samlOptions: SamlConfigOptions
        -- Describes IAM Identity Center options in the form of a key-value map.
        iamIdentityCenterOptionsUpdates: UpdateIamIdentityCenterConfigOptions
        -- Describes IAM federation options in the form of a key-value map for updating an existing security configuration. Use this field to modify IAM federation settings for the security configuration.
        iamFederationOptions: IamFederationConfigOptions
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    record UpdateSecurityConfigOutput
        -- Details about the updated security configuration.
        securityConfigDetail: SecurityConfigDetail
    end

    record UpdateSecurityPolicyInput
        -- The name of the policy.
        name: string
        -- The version of the policy being updated.
        policyVersion: string
        -- A description of the policy. Typically used to store information about the permissions defined in the policy.
        description: string
        -- The JSON policy document to use as the content for the new policy.
        policy: string
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    record UpdateSecurityPolicyOutput
        -- Details about the updated security policy.
        securityPolicyDetail: SecurityPolicyDetail
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource. The resource must be active (not in the DELETING state), and must be owned by the account ID included in the request.
        resourceArn: string
        -- A list of tags (key-value pairs) to add to the resource. All tag keys in the request must be unique.
        tags: {Tag}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource to remove tags from. The resource must be active (not in the DELETING state), and must be owned by the account ID included in the request.
        resourceArn: string
        -- The tag or set of tags to remove from the resource. All tag keys in the request must be unique.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateAccountSettingsInput
        capacityLimits: CapacityLimits
    end

    record UpdateAccountSettingsOutput
        -- OpenSearch Serverless-related settings for the current Amazon Web Services account.
        accountSettingsDetail: AccountSettingsDetail
    end

    record UpdateVpcEndpointInput
        -- The unique identifier of the interface endpoint to update.
        id: string
        -- The ID of one or more subnets to add to the endpoint.
        addSubnetIds: {string}
        -- The unique identifiers of the subnets to remove from the endpoint.
        removeSubnetIds: {string}
        -- The unique identifiers of the security groups to add to the endpoint. Security groups define the ports, protocols, and sources for inbound traffic that you are authorizing into your endpoint.
        addSecurityGroupIds: {string}
        -- The unique identifiers of the security groups to remove from the endpoint.
        removeSecurityGroupIds: {string}
        -- Unique, case-sensitive identifier to ensure idempotency of the request.
        clientToken: string
    end

    -- Update details for an OpenSearch Serverless-managed interface endpoint.
    record UpdateVpcEndpointDetail
        -- The unique identifier of the endpoint.
        id: string
        -- The name of the endpoint.
        name: string
        -- The current status of the endpoint update process.
        status: string
        -- The ID of the subnets from which you access OpenSearch Serverless.
        subnetIds: {string}
        -- The unique identifiers of the security groups that define the ports, protocols, and sources for inbound traffic that you are authorizing into your endpoint.
        securityGroupIds: {string}
        -- The timestamp of when the endpoint was last modified.
        lastModifiedDate: number
    end

    record UpdateVpcEndpointOutput
        -- Details about the updated VPC endpoint.
        UpdateVpcEndpointDetail: UpdateVpcEndpointDetail
    end

end

return M
