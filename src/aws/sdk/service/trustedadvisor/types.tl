-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- Exception that access has been denied due to insufficient access
    record AccessDeniedException
        message: string
    end

    enum RecommendationLifecycleStage
        "in_progress"
        "pending_response"
        "dismissed"
        "resolved"
    end

    enum UpdateRecommendationLifecycleStageReasonCode
        "non_critical_account"
        "temporary_account"
        "valid_business_case"
        "other_methods_available"
        "low_priority"
        "not_applicable"
        "other"
    end

    -- Summary of an AccountRecommendationLifecycle for an Organization Recommendation
    record AccountRecommendationLifecycleSummary
        -- The AWS account ID
        accountId: string
        -- The Recommendation ARN
        accountRecommendationArn: string
        -- The lifecycle stage from AWS Trusted Advisor Priority
        lifecycleStage: string
        -- The person on whose behalf a Technical Account Manager (TAM) updated the recommendation. This information is only available when a Technical Account Manager takes an action on a recommendation managed by AWS Trusted Advisor Priority
        updatedOnBehalfOf: string
        -- The job title of the person on whose behalf a Technical Account Manager (TAM) updated the recommendation. This information is only available when a Technical Account Manager takes an action on a recommendation managed by AWS Trusted Advisor Priority
        updatedOnBehalfOfJobTitle: string
        -- Reason for the lifecycle stage change
        updateReason: string
        -- Reason code for the lifecycle state change
        updateReasonCode: string
        -- When the Recommendation was last updated
        lastUpdatedAt: number
    end

    -- The request entry for Recommendation Resource exclusion. Each entry is a combination of Recommendation Resource ARN and corresponding exclusion status
    record RecommendationResourceExclusion
        -- The ARN of the Recommendation Resource
        arn: string
        -- The exclusion status
        isExcluded: boolean
    end

    record BatchUpdateRecommendationResourceExclusionInput
        -- A list of recommendation resource ARNs and exclusion status to update
        recommendationResourceExclusions: {RecommendationResourceExclusion}
    end

    -- The error entry for Recommendation Resource exclusion. Each entry is a combination of Recommendation Resource ARN, error code and error message
    record UpdateRecommendationResourceExclusionError
        -- The ARN of the Recommendation Resource
        arn: string
        -- The error code
        errorCode: string
        -- The error message
        errorMessage: string
    end

    record BatchUpdateRecommendationResourceExclusionOutput
        -- A list of recommendation resource ARNs whose exclusion status failed to update, if any
        batchUpdateRecommendationResourceExclusionErrors: {UpdateRecommendationResourceExclusionError}
    end

    -- Exception that the request was denied due to conflictions in state
    record ConflictException
        message: string
    end

    -- Exception to notify that an unexpected internal error occurred during processing of the request
    record InternalServerException
        message: string
    end

    -- Exception to notify that requests are being throttled
    record ThrottlingException
        message: string
    end

    -- Exception that the request failed to satisfy service constraints
    record ValidationException
        message: string
    end

    enum RecommendationPillar
        "cost_optimizing"
        "performance"
        "security"
        "service_limits"
        "fault_tolerance"
        "operational_excellence"
    end

    enum RecommendationSource
        "aws_config"
        "compute_optimizer"
        "cost_explorer"
        "lse"
        "manual"
        "pse"
        "rds"
        "resilience"
        "resilience_hub"
        "security_hub"
        "stir"
        "ta_check"
        "well_architected"
        "cost_optimization_hub"
    end

    -- A summary of an AWS Trusted Advisor Check
    record CheckSummary
        -- The unique identifier of the AWS Trusted Advisor Check
        id: string
        -- The ARN of the AWS Trusted Advisor Check
        arn: string
        -- The name of the AWS Trusted Advisor Check
        name: string
        -- A description of what the AWS Trusted Advisor Check is monitoring
        description: string
        -- The Recommendation pillars that the AWS Trusted Advisor Check falls under
        pillars: {string}
        -- The AWS Services that the Check applies to
        awsServices: {string}
        -- The source of the Recommendation
        source: string
        -- The column headings for the metadata returned in the resource
        metadata: {string : string}
    end

    enum ExclusionStatus
        "excluded"
        "included"
    end

    record GetOrganizationRecommendationInput
        -- The Recommendation identifier
        organizationRecommendationIdentifier: string
    end

    -- Cost optimizing aggregates for a Recommendation
    record RecommendationCostOptimizingAggregates
        -- The estimated monthly savings
        estimatedMonthlySavings: number
        -- The estimated percently monthly savings
        estimatedPercentMonthlySavings: number
    end

    -- Recommendation pillar aggregates
    record RecommendationPillarSpecificAggregates
        -- Cost optimizing aggregates
        costOptimizing: RecommendationCostOptimizingAggregates
    end

    -- Aggregation of Recommendation Resources
    record RecommendationResourcesAggregates
        -- The number of AWS resources that were flagged to be OK according to the Trusted Advisor check
        okCount: number
        -- The number of AWS resources that were flagged to have warning according to the Trusted Advisor check
        warningCount: number
        -- The number of AWS resources that were flagged to have errors according to the Trusted Advisor check
        errorCount: number
        -- The number of AWS resources belonging to this Trusted Advisor check that were excluded by the customer
        excludedCount: number
    end

    enum RecommendationStatus
        "ok"
        "warning"
        "error"
    end

    enum RecommendationType
        "standard"
        "priority"
    end

    -- A Recommendation for accounts within an Organization
    record OrganizationRecommendation
        -- The ID which identifies where the Recommendation was produced
        id: string
        -- The AWS Trusted Advisor Check ARN that relates to the Recommendation
        checkArn: string
        -- The status of the Recommendation
        status: string
        -- The lifecycle stage from AWS Trusted Advisor Priority
        lifecycleStage: string
        -- The Pillars that the Recommendation is optimizing
        pillars: {string}
        -- The source of the Recommendation
        source: string
        -- The AWS Services that the Recommendation applies to
        awsServices: {string}
        -- The name of the AWS Trusted Advisor Recommendation
        name: string
        -- An aggregation of all resources
        resourcesAggregates: RecommendationResourcesAggregates
        -- The pillar aggregations for cost savings
        pillarSpecificAggregates: RecommendationPillarSpecificAggregates
        -- When the Recommendation was created, if created by AWS Trusted Advisor Priority
        createdAt: number
        -- When the Recommendation was last updated
        lastUpdatedAt: number
        -- The ARN of the Recommendation
        arn: string
        -- A description for AWS Trusted Advisor recommendations
        description: string
        -- The creator, if created by AWS Trusted Advisor Priority
        createdBy: string
        -- The person on whose behalf a Technical Account Manager (TAM) updated the recommendation. This information is only available when a Technical Account Manager takes an action on a recommendation managed by AWS Trusted Advisor Priority
        updatedOnBehalfOf: string
        -- The job title of the person on whose behalf a Technical Account Manager (TAM) updated the recommendation. This information is only available when a Technical Account Manager takes an action on a recommendation managed by AWS Trusted Advisor Priority
        updatedOnBehalfOfJobTitle: string
        -- Reason for the lifecycle stage change
        updateReason: string
        -- Reason code for the lifecycle state change
        updateReasonCode: string
        -- When the Recommendation was resolved
        resolvedAt: number
    end

    record GetOrganizationRecommendationOutput
        -- The Recommendation
        organizationRecommendation: OrganizationRecommendation
    end

    -- Exception that the requested resource has not been found
    record ResourceNotFoundException
        message: string
    end

    enum RecommendationLanguage
        "en"
        "ja"
        "zh"
        "fr"
        "de"
        "ko"
        "zh_TW"
        "it"
        "es"
        "pt_BR"
        "id"
    end

    record GetRecommendationInput
        -- The Recommendation identifier
        recommendationIdentifier: string
        -- The ISO 639-1 code for the language that you want your recommendations to appear in.
        language: string
    end

    enum StatusReason
        "no_data_ok"
    end

    -- A Recommendation for an Account
    record Recommendation
        -- The ID which identifies where the Recommendation was produced
        id: string
        -- The AWS Trusted Advisor Check ARN that relates to the Recommendation
        checkArn: string
        -- The status of the Recommendation
        status: string
        -- The lifecycle stage from AWS Trusted Advisor Priority
        lifecycleStage: string
        -- The Pillars that the Recommendation is optimizing
        pillars: {string}
        -- The source of the Recommendation
        source: string
        -- The AWS Services that the Recommendation applies to
        awsServices: {string}
        -- The name of the AWS Trusted Advisor Recommendation
        name: string
        -- An aggregation of all resources
        resourcesAggregates: RecommendationResourcesAggregates
        -- The pillar aggregations for cost savings
        pillarSpecificAggregates: RecommendationPillarSpecificAggregates
        -- When the Recommendation was created, if created by AWS Trusted Advisor Priority
        createdAt: number
        -- When the Recommendation was last updated
        lastUpdatedAt: number
        -- The ARN of the Recommendation
        arn: string
        -- This attribute provides additional details about potential discrepancies in check status determination.
        statusReason: string
        -- A description for AWS Trusted Advisor recommendations
        description: string
        -- The creator, if created by AWS Trusted Advisor Priority
        createdBy: string
        -- The person on whose behalf a Technical Account Manager (TAM) updated the recommendation. This information is only available when a Technical Account Manager takes an action on a recommendation managed by AWS Trusted Advisor Priority
        updatedOnBehalfOf: string
        -- The job title of the person on whose behalf a Technical Account Manager (TAM) updated the recommendation. This information is only available when a Technical Account Manager takes an action on a recommendation managed by AWS Trusted Advisor Priority
        updatedOnBehalfOfJobTitle: string
        -- Reason for the lifecycle stage change
        updateReason: string
        -- Reason code for the lifecycle state change
        updateReasonCode: string
        -- When the Recommendation was resolved
        resolvedAt: number
    end

    record GetRecommendationOutput
        -- The Recommendation
        recommendation: Recommendation
    end

    record ListChecksInput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The pillar of the check
        pillar: string
        -- The aws service associated with the check
        awsService: string
        -- The source of the check
        source: string
        -- The ISO 639-1 code for the language that you want your checks to appear in.
        language: string
    end

    record ListChecksOutput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The list of Checks
        checkSummaries: {CheckSummary}
    end

    record ListOrganizationRecommendationAccountsInput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The Recommendation identifier
        organizationRecommendationIdentifier: string
        -- An account affected by this organization recommendation
        affectedAccountId: string
    end

    record ListOrganizationRecommendationAccountsOutput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The account recommendations lifecycles that are applicable to the Recommendation
        accountRecommendationLifecycleSummaries: {AccountRecommendationLifecycleSummary}
    end

    enum ResourceStatus
        "ok"
        "warning"
        "error"
    end

    record ListOrganizationRecommendationResourcesInput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The status of the resource
        status: string
        -- The exclusion status of the resource
        exclusionStatus: string
        -- The AWS Region code of the resource
        regionCode: string
        -- The AWS Organization organization's Recommendation identifier
        organizationRecommendationIdentifier: string
        -- An account affected by this organization recommendation
        affectedAccountId: string
    end

    -- Organization Recommendation Resource Summary
    record OrganizationRecommendationResourceSummary
        -- The ID of the Recommendation Resource
        id: string
        -- The ARN of the Recommendation Resource
        arn: string
        -- The AWS resource identifier. There are certain checks that generate recommendation resources without an awsResourceId.
        awsResourceId: string
        -- The AWS Region code that the Recommendation Resource is in
        regionCode: string
        -- The current status of the Recommendation Resource
        status: string
        -- Metadata associated with the Recommendation Resource
        metadata: {string : string}
        -- When the Recommendation Resource was last updated
        lastUpdatedAt: number
        -- The exclusion status of the Recommendation Resource
        exclusionStatus: string
        -- The AWS account ID
        accountId: string
        -- The Recommendation ARN
        recommendationArn: string
    end

    record ListOrganizationRecommendationResourcesOutput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- A list of Recommendation Resources
        organizationRecommendationResourceSummaries: {OrganizationRecommendationResourceSummary}
    end

    record ListOrganizationRecommendationsInput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The status of the Recommendation
        status: string
        -- The pillar of the Recommendation
        pillar: string
        -- The aws service associated with the Recommendation
        awsService: string
        -- The source of the Recommendation
        source: string
        -- The check identifier of the Recommendation
        checkIdentifier: string
        -- After the last update of the Recommendation
        afterLastUpdatedAt: number
        -- Before the last update of the Recommendation
        beforeLastUpdatedAt: number
    end

    -- Summary of recommendation for accounts within an Organization
    record OrganizationRecommendationSummary
        -- The ID which identifies where the Recommendation was produced
        id: string
        -- The AWS Trusted Advisor Check ARN that relates to the Recommendation
        checkArn: string
        -- The status of the Recommendation
        status: string
        -- The lifecycle stage from AWS Trusted Advisor Priority
        lifecycleStage: string
        -- The Pillars that the Recommendation is optimizing
        pillars: {string}
        -- The source of the Recommendation
        source: string
        -- The AWS Services that the Recommendation applies to
        awsServices: {string}
        -- The name of the AWS Trusted Advisor Recommendation
        name: string
        -- An aggregation of all resources
        resourcesAggregates: RecommendationResourcesAggregates
        -- The pillar aggregations for cost savings
        pillarSpecificAggregates: RecommendationPillarSpecificAggregates
        -- When the Recommendation was created, if created by AWS Trusted Advisor Priority
        createdAt: number
        -- When the Recommendation was last updated
        lastUpdatedAt: number
        -- The ARN of the Recommendation
        arn: string
    end

    record ListOrganizationRecommendationsOutput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The list of Recommendations
        organizationRecommendationSummaries: {OrganizationRecommendationSummary}
    end

    record ListRecommendationResourcesInput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The status of the resource
        status: string
        -- The exclusion status of the resource
        exclusionStatus: string
        -- The AWS Region code of the resource
        regionCode: string
        -- The Recommendation identifier
        recommendationIdentifier: string
        -- The ISO 639-1 code for the language that you want your recommendations to appear in.
        language: string
    end

    -- Summary of a Recommendation Resource
    record RecommendationResourceSummary
        -- The ID of the Recommendation Resource
        id: string
        -- The ARN of the Recommendation Resource
        arn: string
        -- The AWS resource identifier. There are certain checks that generate recommendation resources without an awsResourceId.
        awsResourceId: string
        -- The AWS Region code that the Recommendation Resource is in
        regionCode: string
        -- The current status of the Recommendation Resource
        status: string
        -- Metadata associated with the Recommendation Resource
        metadata: {string : string}
        -- When the Recommendation Resource was last updated
        lastUpdatedAt: number
        -- The exclusion status of the Recommendation Resource
        exclusionStatus: string
        -- The Recommendation ARN
        recommendationArn: string
    end

    record ListRecommendationResourcesOutput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- A list of Recommendation Resources
        recommendationResourceSummaries: {RecommendationResourceSummary}
    end

    record ListRecommendationsInput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The maximum number of results to return per page.
        maxResults: number
        -- The status of the Recommendation
        status: string
        -- The pillar of the Recommendation
        pillar: string
        -- The aws service associated with the Recommendation
        awsService: string
        -- The source of the Recommendation
        source: string
        -- The check identifier of the Recommendation
        checkIdentifier: string
        -- After the last update of the Recommendation
        afterLastUpdatedAt: number
        -- Before the last update of the Recommendation
        beforeLastUpdatedAt: number
        -- The ISO 639-1 code for the language that you want your recommendations to appear in.
        language: string
    end

    -- Summary of Recommendation for an Account
    record RecommendationSummary
        -- The ID which identifies where the Recommendation was produced
        id: string
        -- The AWS Trusted Advisor Check ARN that relates to the Recommendation
        checkArn: string
        -- The status of the Recommendation
        status: string
        -- The lifecycle stage from AWS Trusted Advisor Priority
        lifecycleStage: string
        -- The Pillars that the Recommendation is optimizing
        pillars: {string}
        -- The source of the Recommendation
        source: string
        -- The AWS Services that the Recommendation applies to
        awsServices: {string}
        -- The name of the AWS Trusted Advisor Recommendation
        name: string
        -- An aggregation of all resources
        resourcesAggregates: RecommendationResourcesAggregates
        -- The pillar aggregations for cost savings
        pillarSpecificAggregates: RecommendationPillarSpecificAggregates
        -- When the Recommendation was created, if created by AWS Trusted Advisor Priority
        createdAt: number
        -- When the Recommendation was last updated
        lastUpdatedAt: number
        -- The ARN of the Recommendation
        arn: string
        -- This attribute provides additional details about potential discrepancies in check status determination.
        statusReason: string
    end

    record ListRecommendationsOutput
        -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
        nextToken: string
        -- The list of Recommendations
        recommendationSummaries: {RecommendationSummary}
    end

    enum UpdateRecommendationLifecycleStage
        "pending_response"
        "in_progress"
        "dismissed"
        "resolved"
    end

    record UpdateOrganizationRecommendationLifecycleInput
        -- The new lifecycle stage
        lifecycleStage: string
        -- Reason for the lifecycle stage change
        updateReason: string
        -- Reason code for the lifecycle state change
        updateReasonCode: string
        -- The Recommendation identifier for AWS Trusted Advisor Priority recommendations
        organizationRecommendationIdentifier: string
    end

    record UpdateOrganizationRecommendationLifecycleOutput
    end

    record UpdateRecommendationLifecycleInput
        -- The new lifecycle stage
        lifecycleStage: string
        -- Reason for the lifecycle stage change
        updateReason: string
        -- Reason code for the lifecycle state change
        updateReasonCode: string
        -- The Recommendation identifier for AWS Trusted Advisor Priority recommendations
        recommendationIdentifier: string
    end

    record UpdateRecommendationLifecycleOutput
    end

end

return M
