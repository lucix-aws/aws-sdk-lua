-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- Either your AWS credentials are not valid or you do not have access to the EC2 instance.
    record AuthException
        Message: string
    end

    -- The specified instance was not found.
    record EC2InstanceNotFoundException
        Message: string
    end

    -- Unable to connect because the instance is not in a valid state.
    -- Connecting to a stopped or terminated instance is not supported. If the instance is stopped,
    -- start your instance, and try to connect again.
    record EC2InstanceStateInvalidException
        Message: string
    end

    -- The instance type is not supported for connecting via the serial console. Only Nitro
    -- instance types are currently supported.
    record EC2InstanceTypeInvalidException
        Message: string
    end

    -- The instance is currently unavailable. Wait a few minutes and try again.
    record EC2InstanceUnavailableException
        Message: string
    end

    -- One of the parameters is not valid.
    record InvalidArgsException
        Message: string
    end

    record SendSerialConsoleSSHPublicKeyInput
        -- The ID of the EC2 instance.
        InstanceId: string
        -- The serial port of the EC2 instance. Currently only port 0 is supported.
        -- Default: 0
        SerialPort: number
        -- The public key material. To use the public key, you must have the matching private
        -- key. For information about the supported key formats and lengths, see Requirements for key pairs in the Amazon EC2 User
        -- Guide.
        SSHPublicKey: string
    end

    record SendSerialConsoleSSHPublicKeyOutput
        -- The ID of the request. Please provide this ID when contacting AWS Support for assistance.
        RequestId: string
        -- Is true if the request succeeds and an error otherwise.
        Success: boolean
    end

    -- Your account is not authorized to use the EC2 Serial Console. To authorize your
    -- account, run the EnableSerialConsoleAccess API. For more information, see
    -- EnableSerialConsoleAccess in the Amazon EC2 API Reference.
    record SerialConsoleAccessDisabledException
        Message: string
    end

    -- The instance currently has 1 active serial console session. Only 1 session is supported at a time.
    record SerialConsoleSessionLimitExceededException
        Message: string
    end

    -- Unable to start a serial console session. Please try again.
    record SerialConsoleSessionUnavailableException
        Message: string
    end

    -- Your instance's BIOS version is unsupported for serial console connection. Reboot your instance to update its BIOS, and then try again to connect.
    record SerialConsoleSessionUnsupportedException
        Message: string
    end

    -- The service encountered an error. Follow the instructions in the error message and try again.
    record ServiceException
        Message: string
    end

    -- The requests were made too frequently and have been throttled. Wait a while and try again.
    -- To increase the limit on your request frequency, contact AWS Support.
    record ThrottlingException
        Message: string
    end

    record SendSSHPublicKeyInput
        -- The ID of the EC2 instance.
        InstanceId: string
        -- The OS user on the EC2 instance for whom the key can be used to authenticate.
        InstanceOSUser: string
        -- The public key material. To use the public key, you must have the matching private key.
        SSHPublicKey: string
        -- The Availability Zone in which the EC2 instance was launched.
        AvailabilityZone: string
    end

    record SendSSHPublicKeyOutput
        -- The ID of the request. Please provide this ID when contacting AWS Support for assistance.
        RequestId: string
        -- Is true if the request succeeds and an error otherwise.
        Success: boolean
    end

end

return M
