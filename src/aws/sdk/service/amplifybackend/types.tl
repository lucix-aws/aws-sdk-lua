-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    enum AdditionalConstraintsElement
        "REQUIRE_DIGIT"
        "REQUIRE_LOWERCASE"
        "REQUIRE_SYMBOL"
        "REQUIRE_UPPERCASE"
    end

    -- An error returned if a request is not formed properly.
    record BadRequestException
        -- An error message to inform that the request failed.
        Message: string
    end

    -- The request body for CloneBackend.
    record CloneBackendInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The name of the destination backend environment to be created.
        TargetEnvironmentName: string
    end

    record CloneBackendOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    -- An error returned if there's a temporary issue with the service.
    record GatewayTimeoutException
        -- An error message to inform that the request failed.
        Message: string
    end

    -- An error returned when a specific resource type is not found.
    record NotFoundException
        -- An error message to inform that the request has failed.
        Message: string
        -- The type of resource that is not found.
        ResourceType: string
    end

    -- An error that is returned when a limit of a specific type has been exceeded.
    record TooManyRequestsException
        -- The type of limit that was exceeded.
        LimitType: string
        -- An error message to inform that the request has failed.
        Message: string
    end

    -- Defines the resource configuration for the data model in your Amplify project.
    record ResourceConfig
    end

    -- The request body for CreateBackend.
    record CreateBackendInput
        -- The app ID.
        AppId: string
        -- The name of the app.
        AppName: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The resource configuration for creating a backend.
        ResourceConfig: ResourceConfig
        -- The name of the resource.
        ResourceName: string
    end

    record CreateBackendOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    enum Mode
        "API_KEY"
        "AWS_IAM"
        "AMAZON_COGNITO_USER_POOLS"
        "OPENID_CONNECT"
    end

    -- The authentication settings for accessing provisioned data models in your Amplify project.
    record BackendAPIAppSyncAuthSettings
        -- The Amazon Cognito user pool ID, if Amazon Cognito was used as an authentication setting to access your data models.
        CognitoUserPoolId: string
        -- The API key description for API_KEY, if it was used as an authentication mechanism to access your data models.
        Description: string
        -- The API key expiration time for API_KEY, if it was used as an authentication mechanism to access your data models.
        ExpirationTime: number
        -- The expiry time for the OpenID authentication mechanism.
        OpenIDAuthTTL: string
        -- The clientID for openID, if openID was used as an authentication setting to access your data models.
        OpenIDClientId: string
        -- The expiry time for the OpenID authentication mechanism.
        OpenIDIatTTL: string
        -- The openID issuer URL, if openID was used as an authentication setting to access your data models.
        OpenIDIssueURL: string
        -- The OpenID provider name, if OpenID was used as an authentication mechanism to access your data models.
        OpenIDProviderName: string
    end

    -- Describes the auth types for your configured data models.
    record BackendAPIAuthType
        -- Describes the authentication mode.
        Mode: string
        -- Describes settings for the authentication mode.
        Settings: BackendAPIAppSyncAuthSettings
    end

    enum ResolutionStrategy
        "OPTIMISTIC_CONCURRENCY"
        "LAMBDA"
        "AUTOMERGE"
        "NONE"
    end

    -- Describes the conflict resolution configuration for your data model configured in your Amplify project.
    record BackendAPIConflictResolution
        -- The strategy for conflict resolution.
        ResolutionStrategy: string
    end

    -- The resource config for the data model, configured as a part of the Amplify project.
    record BackendAPIResourceConfig
        -- Additional authentication methods used to interact with your data models.
        AdditionalAuthTypes: {BackendAPIAuthType}
        -- The API name used to interact with the data model, configured as a part of your Amplify project.
        ApiName: string
        -- The conflict resolution strategy for your data stored in the data models.
        ConflictResolution: BackendAPIConflictResolution
        -- The default authentication type for interacting with the configured data models in your Amplify project.
        DefaultAuthType: BackendAPIAuthType
        -- The service used to provision and interact with the data model.
        Service: string
        -- The definition of the data model in the annotated transform of the GraphQL schema.
        TransformSchema: string
    end

    -- The request body for CreateBackendAPI.
    record CreateBackendAPIInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The resource configuration for this request.
        ResourceConfig: BackendAPIResourceConfig
        -- The name of this resource.
        ResourceName: string
    end

    record CreateBackendAPIOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    enum AuthResources
        "USER_POOL_ONLY"
        "IDENTITY_POOL_AND_USER_POOL"
    end

    -- Describes authorization configurations for the auth resources, configured as a part of your Amplify project.
    record CreateBackendAuthIdentityPoolConfig
        -- Name of the Amazon Cognito identity pool used for authorization.
        IdentityPoolName: string
        -- Set to true or false based on whether you want to enable guest authorization to your Amplify app.
        UnauthenticatedLogin: boolean
    end

    enum Service
        "COGNITO"
    end

    enum DeliveryMethod
        "EMAIL"
        "SMS"
    end

    -- The configuration for the email sent when an app user forgets their password.
    record EmailSettings
        -- The contents of the email message.
        EmailMessage: string
        -- The contents of the subject line of the email message.
        EmailSubject: string
    end

    -- SMS settings for authentication.
    record SmsSettings
        -- The contents of the SMS message.
        SmsMessage: string
    end

    -- (DEPRECATED) Describes the forgot password policy for authenticating into the Amplify app.
    record CreateBackendAuthForgotPasswordConfig
        -- (DEPRECATED) Describes which mode to use (either SMS or email) to deliver messages to app users who want to recover their password.
        DeliveryMethod: string
        -- (DEPRECATED) The configuration for the email sent when an app user forgets their password.
        EmailSettings: EmailSettings
        -- (DEPRECATED) The configuration for the SMS message sent when an app user forgets their password.
        SmsSettings: SmsSettings
    end

    enum MFAMode
        "ON"
        "OFF"
        "OPTIONAL"
    end

    enum MfaTypesElement
        "SMS"
        "TOTP"
    end

    -- The settings of your MFA configuration for the backend of your Amplify project.
    record Settings
        -- The supported MFA types.
        MfaTypes: {string}
        -- The body of the SMS message.
        SmsMessage: string
    end

    -- Describes whether to apply multi-factor authentication policies for your Amazon Cognito user pool configured as a part of your Amplify project.
    record CreateBackendAuthMFAConfig
        -- Describes whether MFA should be [ON, OFF, or OPTIONAL] for authentication in your Amplify project.
        MFAMode: string
        -- Describes the configuration settings and methods for your Amplify app users to use MFA.
        Settings: Settings
    end

    enum OAuthGrantType
        "CODE"
        "IMPLICIT"
    end

    enum OAuthScopesElement
        "PHONE"
        "EMAIL"
        "OPENID"
        "PROFILE"
        "AWS_COGNITO_SIGNIN_USER_ADMIN"
    end

    -- Describes third-party social federation configurations for allowing your app users to sign in using OAuth.
    record BackendAuthSocialProviderConfig
        -- Describes the client_id, which can be obtained from the third-party social federation provider.
        ClientId: string
        -- Describes the client_secret, which can be obtained from third-party social federation providers.
        ClientSecret: string
    end

    -- Describes Apple social federation configurations for allowing your app users to sign in using OAuth.
    record BackendAuthAppleProviderConfig
        -- Describes the client_id (also called Services ID) that comes from Apple.
        ClientId: string
        -- Describes the key_id that comes from Apple.
        KeyId: string
        -- Describes the private_key that comes from Apple.
        PrivateKey: string
        -- Describes the team_id that comes from Apple.
        TeamId: string
    end

    -- The settings for using the social identity providers for access to your Amplify app.
    record SocialProviderSettings
        Facebook: BackendAuthSocialProviderConfig
        Google: BackendAuthSocialProviderConfig
        LoginWithAmazon: BackendAuthSocialProviderConfig
        SignInWithApple: BackendAuthAppleProviderConfig
    end

    -- Creates the OAuth configuration for your Amplify project.
    record CreateBackendAuthOAuthConfig
        -- The domain prefix for your Amplify app.
        DomainPrefix: string
        -- The OAuth grant type that you use to allow app users to authenticate from your Amplify app.
        OAuthGrantType: string
        -- List of OAuth-related flows used to allow your app users to authenticate from your Amplify app.
        OAuthScopes: {string}
        -- The redirected URI for signing in to your Amplify app.
        RedirectSignInURIs: {string}
        -- Redirect URLs that OAuth uses when a user signs out of an Amplify app.
        RedirectSignOutURIs: {string}
        -- The settings for using social providers to access your Amplify app.
        SocialProviderSettings: SocialProviderSettings
    end

    -- The password policy configuration for the backend to your Amplify project.
    record CreateBackendAuthPasswordPolicyConfig
        -- Additional constraints for the password used to access the backend of your Amplify project.
        AdditionalConstraints: {string}
        -- The minimum length of the password used to access the backend of your Amplify project.
        MinimumLength: number
    end

    enum RequiredSignUpAttributesElement
        "ADDRESS"
        "BIRTHDATE"
        "EMAIL"
        "FAMILY_NAME"
        "GENDER"
        "GIVEN_NAME"
        "LOCALE"
        "MIDDLE_NAME"
        "NAME"
        "NICKNAME"
        "PHONE_NUMBER"
        "PICTURE"
        "PREFERRED_USERNAME"
        "PROFILE"
        "UPDATED_AT"
        "WEBSITE"
        "ZONE_INFO"
    end

    enum SignInMethod
        "EMAIL"
        "EMAIL_AND_PHONE_NUMBER"
        "PHONE_NUMBER"
        "USERNAME"
    end

    -- Creates an email or SMS verification message for the auth resource configured for your Amplify project.
    record CreateBackendAuthVerificationMessageConfig
        -- The type of verification message to send.
        DeliveryMethod: string
        -- The settings for the email message.
        EmailSettings: EmailSettings
        -- The settings for the SMS message.
        SmsSettings: SmsSettings
    end

    -- Describes the Amazon Cognito user pool configuration for the auth resource to be configured for your Amplify project.
    record CreateBackendAuthUserPoolConfig
        -- (DEPRECATED) Describes the forgotten password policy for your Amazon Cognito user pool, configured as a part of your Amplify project.
        ForgotPassword: CreateBackendAuthForgotPasswordConfig
        -- Describes whether to apply multi-factor authentication policies for your Amazon Cognito user pool configured as a part of your Amplify project.
        Mfa: CreateBackendAuthMFAConfig
        -- Describes the OAuth policy and rules for your Amazon Cognito user pool, configured as a part of your Amplify project.
        OAuth: CreateBackendAuthOAuthConfig
        -- Describes the password policy for your Amazon Cognito user pool, configured as a part of your Amplify project.
        PasswordPolicy: CreateBackendAuthPasswordPolicyConfig
        -- The required attributes to sign up new users in the user pool.
        RequiredSignUpAttributes: {string}
        -- Describes the sign-in methods that your Amplify app users use to log in using the Amazon Cognito user pool, configured as a part of your Amplify project.
        SignInMethod: string
        -- The Amazon Cognito user pool name.
        UserPoolName: string
        -- Describes the email or SMS verification message for your Amazon Cognito user pool, configured as a part of your Amplify project.
        VerificationMessage: CreateBackendAuthVerificationMessageConfig
    end

    -- Defines the resource configuration when creating an auth resource in your Amplify project.
    record CreateBackendAuthResourceConfig
        -- Defines whether you want to configure only authentication or both authentication and authorization settings.
        AuthResources: string
        -- Describes the authorization configuration for the Amazon Cognito identity pool, provisioned as a part of your auth resource in the Amplify project.
        IdentityPoolConfigs: CreateBackendAuthIdentityPoolConfig
        -- Defines the service name to use when configuring an authentication resource in your Amplify project.
        Service: string
        -- Describes authentication configuration for the Amazon Cognito user pool, provisioned as a part of your auth resource in the Amplify project.
        UserPoolConfigs: CreateBackendAuthUserPoolConfig
    end

    -- The request body for CreateBackendAuth.
    record CreateBackendAuthInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The resource configuration for this request object.
        ResourceConfig: CreateBackendAuthResourceConfig
        -- The name of this resource.
        ResourceName: string
    end

    record CreateBackendAuthOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    -- The request body for CreateBackendConfig.
    record CreateBackendConfigInput
        -- The app ID.
        AppId: string
        -- The app ID for the backend manager.
        BackendManagerAppId: string
    end

    record CreateBackendConfigOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The ID for the job.
        JobId: string
        -- The current status of the request.
        Status: string
    end

    enum AuthenticatedElement
        "READ"
        "CREATE_AND_UPDATE"
        "DELETE"
    end

    enum UnAuthenticatedElement
        "READ"
        "CREATE_AND_UPDATE"
        "DELETE"
    end

    -- Describes the read, write, and delete permissions users have against your storage S3 bucket.
    record BackendStoragePermissions
        -- Lists all authenticated user read, write, and delete permissions for your S3 bucket.
        Authenticated: {string}
        -- Lists all unauthenticated user read, write, and delete permissions for your S3 bucket.
        UnAuthenticated: {string}
    end

    enum ServiceName
        "S3"
    end

    -- The resource configuration for creating backend storage.
    record CreateBackendStorageResourceConfig
        -- The name of the S3 bucket.
        BucketName: string
        -- The authorization configuration for the storage S3 bucket.
        Permissions: BackendStoragePermissions
        -- The name of the storage service.
        ServiceName: string
    end

    -- The request body for CreateBackendStorage.
    record CreateBackendStorageInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The resource configuration for creating backend storage.
        ResourceConfig: CreateBackendStorageResourceConfig
        -- The name of the storage resource.
        ResourceName: string
    end

    record CreateBackendStorageOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The ID for the job.
        JobId: string
        -- The current status of the request.
        Status: string
    end

    record CreateTokenInput
        -- The app ID.
        AppId: string
    end

    record CreateTokenOutput
        -- The app ID.
        AppId: string
        -- One-time challenge code for authenticating into the Amplify Admin UI.
        ChallengeCode: string
        -- A unique ID provided when creating a new challenge token.
        SessionId: string
        -- The expiry time for the one-time generated token code.
        Ttl: string
    end

    record DeleteBackendInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
    end

    record DeleteBackendOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    -- The request body for DeleteBackendAPI.
    record DeleteBackendAPIInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- Defines the resource configuration for the data model in your Amplify project.
        ResourceConfig: BackendAPIResourceConfig
        -- The name of this resource.
        ResourceName: string
    end

    record DeleteBackendAPIOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    -- The request body for DeleteBackendAuth.
    record DeleteBackendAuthInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The name of this resource.
        ResourceName: string
    end

    record DeleteBackendAuthOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    -- The request body for DeleteBackendStorage.
    record DeleteBackendStorageInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The name of the storage resource.
        ResourceName: string
        -- The name of the storage service.
        ServiceName: string
    end

    record DeleteBackendStorageOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The ID for the job.
        JobId: string
        -- The current status of the request.
        Status: string
    end

    record DeleteTokenInput
        -- The app ID.
        AppId: string
        -- The session ID.
        SessionId: string
    end

    record DeleteTokenOutput
        -- Indicates whether the request succeeded or failed.
        IsSuccess: boolean
    end

    -- The request body for GenerateBackendAPIModels.
    record GenerateBackendAPIModelsInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The name of this resource.
        ResourceName: string
    end

    record GenerateBackendAPIModelsOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    -- The request body for GetBackend.
    record GetBackendInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
    end

    record GetBackendOutput
        -- A stringified version of the cli.json file for your Amplify project.
        AmplifyFeatureFlags: string
        -- A stringified version of the current configs for your Amplify project.
        AmplifyMetaConfig: string
        -- The app ID.
        AppId: string
        -- The name of the app.
        AppName: string
        -- A list of backend environments in an array.
        BackendEnvironmentList: {string}
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request failed, this is the returned error.
        Error: string
    end

    -- The request body for GetBackendAPI.
    record GetBackendAPIInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- Defines the resource configuration for the data model in your Amplify project.
        ResourceConfig: BackendAPIResourceConfig
        -- The name of this resource.
        ResourceName: string
    end

    record GetBackendAPIOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The resource configuration for this response object.
        ResourceConfig: BackendAPIResourceConfig
        -- The name of this resource.
        ResourceName: string
    end

    -- The request body for GetBackendAPIModels.
    record GetBackendAPIModelsInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The name of this resource.
        ResourceName: string
    end

    enum Status
        "LATEST"
        "STALE"
    end

    record GetBackendAPIModelsOutput
        -- Stringified JSON of the datastore model.
        Models: string
        -- The current status of the request.
        Status: string
        -- Stringified JSON of the model introspection schema for an existing backend API resource.
        ModelIntrospectionSchema: string
    end

    -- The request body for GetBackendAuth.
    record GetBackendAuthInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The name of this resource.
        ResourceName: string
    end

    record GetBackendAuthOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The resource configuration for authorization requests to the backend of your Amplify project.
        ResourceConfig: CreateBackendAuthResourceConfig
        -- The name of this resource.
        ResourceName: string
    end

    record GetBackendJobInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The ID for the job.
        JobId: string
    end

    record GetBackendJobOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The time when the job was created.
        CreateTime: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
        -- The time when the job was last updated.
        UpdateTime: string
    end

    -- The request body for GetBackendStorage.
    record GetBackendStorageInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The name of the storage resource.
        ResourceName: string
    end

    -- The details for a backend storage resource.
    record GetBackendStorageResourceConfig
        -- The name of the S3 bucket.
        BucketName: string
        -- Returns True if the storage resource has been imported.
        Imported: boolean
        -- The authorization configuration for the storage S3 bucket.
        Permissions: BackendStoragePermissions
        -- The name of the storage service.
        ServiceName: string
    end

    record GetBackendStorageOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The resource configuration for the backend storage resource.
        ResourceConfig: GetBackendStorageResourceConfig
        -- The name of the storage resource.
        ResourceName: string
    end

    record GetTokenInput
        -- The app ID.
        AppId: string
        -- The session ID.
        SessionId: string
    end

    record GetTokenOutput
        -- The app ID.
        AppId: string
        -- The one-time challenge code for authenticating into the Amplify Admin UI.
        ChallengeCode: string
        -- A unique ID provided when creating a new challenge token.
        SessionId: string
        -- The expiry time for the one-time generated token code.
        Ttl: string
    end

    -- The request body for ImportBackendAuth.
    record ImportBackendAuthInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The ID of the Amazon Cognito identity pool.
        IdentityPoolId: string
        -- The ID of the Amazon Cognito native client.
        NativeClientId: string
        -- The ID of the Amazon Cognito user pool.
        UserPoolId: string
        -- The ID of the Amazon Cognito web client.
        WebClientId: string
    end

    record ImportBackendAuthOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    -- The request body for ImportBackendStorage.
    record ImportBackendStorageInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The name of the S3 bucket.
        BucketName: string
        -- The name of the storage service.
        ServiceName: string
    end

    record ImportBackendStorageOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The ID for the job.
        JobId: string
        -- The current status of the request.
        Status: string
    end

    -- The request body for ListBackendJobs.
    record ListBackendJobsInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The ID for the job.
        JobId: string
        -- The maximum number of results that you want in the response.
        MaxResults: number
        -- The token for the next set of results.
        NextToken: string
        -- Filters the list of response objects to include only those with the specified operation name.
        Operation: string
        -- Filters the list of response objects to include only those with the specified status.
        Status: string
    end

    -- The response object for this operation.
    record BackendJobRespObj
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The time when the job was created.
        CreateTime: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
        -- The time when the job was last updated.
        UpdateTime: string
    end

    record ListBackendJobsOutput
        -- An array of jobs and their properties.
        Jobs: {BackendJobRespObj}
        -- The token for the next set of results.
        NextToken: string
    end

    -- The request body for S3Buckets.
    record ListS3BucketsInput
        -- Reserved for future use.
        NextToken: string
    end

    -- Describes the metadata of the S3 bucket.
    record S3BucketInfo
        -- The creation date of the S3 bucket.
        CreationDate: string
        -- The name of the S3 bucket.
        Name: string
    end

    record ListS3BucketsOutput
        -- The list of S3 buckets.
        Buckets: {S3BucketInfo}
        -- Reserved for future use.
        NextToken: string
    end

    -- The request body for RemoveAllBackends.
    record RemoveAllBackendsInput
        -- The app ID.
        AppId: string
        -- Cleans up the Amplify Console app if this value is set to true.
        CleanAmplifyApp: boolean
    end

    record RemoveAllBackendsOutput
        -- The app ID.
        AppId: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    record RemoveBackendConfigInput
        -- The app ID.
        AppId: string
    end

    record RemoveBackendConfigOutput
        -- If the request fails, this error is returned.
        Error: string
    end

    -- The request body for UpdateBackendAPI.
    record UpdateBackendAPIInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- Defines the resource configuration for the data model in your Amplify project.
        ResourceConfig: BackendAPIResourceConfig
        -- The name of this resource.
        ResourceName: string
    end

    record UpdateBackendAPIOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    -- Describes the authorization configuration for the Amazon Cognito identity pool, provisioned as a part of your auth resource in the Amplify project.
    record UpdateBackendAuthIdentityPoolConfig
        -- A boolean value that can be set to allow or disallow guest-level authorization into your Amplify app.
        UnauthenticatedLogin: boolean
    end

    -- (DEPRECATED) Describes the forgot password policy for authenticating into the Amplify app.
    record UpdateBackendAuthForgotPasswordConfig
        -- (DEPRECATED) Describes which mode to use (either SMS or email) to deliver messages to app users that want to recover their password.
        DeliveryMethod: string
        -- (DEPRECATED) The configuration for the email sent when an app user forgets their password.
        EmailSettings: EmailSettings
        -- (DEPRECATED) The configuration for the SMS message sent when an Amplify app user forgets their password.
        SmsSettings: SmsSettings
    end

    -- Updates the multi-factor authentication (MFA) configuration for the backend of your Amplify project.
    record UpdateBackendAuthMFAConfig
        -- The MFA mode for the backend of your Amplify project.
        MFAMode: string
        -- The settings of your MFA configuration for the backend of your Amplify project.
        Settings: Settings
    end

    -- The OAuth configurations for authenticating users into your Amplify app.
    record UpdateBackendAuthOAuthConfig
        -- The Amazon Cognito domain prefix used to create a hosted UI for authentication.
        DomainPrefix: string
        -- The OAuth grant type to allow app users to authenticate from your Amplify app.
        OAuthGrantType: string
        -- The list of OAuth-related flows that can allow users to authenticate from your Amplify app.
        OAuthScopes: {string}
        -- Redirect URLs that OAuth uses when a user signs in to an Amplify app.
        RedirectSignInURIs: {string}
        -- Redirect URLs that OAuth uses when a user signs out of an Amplify app.
        RedirectSignOutURIs: {string}
        -- Describes third-party social federation configurations for allowing your users to sign in with OAuth.
        SocialProviderSettings: SocialProviderSettings
    end

    -- Describes the password policy for your Amazon Cognito user pool configured as a part of your Amplify project.
    record UpdateBackendAuthPasswordPolicyConfig
        -- Describes additional constraints on password requirements to sign in to the auth resource, configured as a part of your Amplify project.
        AdditionalConstraints: {string}
        -- Describes the minimum length of the password required to sign in to the auth resource, configured as a part of your Amplify project.
        MinimumLength: number
    end

    -- Updates the configuration of the email or SMS message for the auth resource configured for your Amplify project.
    record UpdateBackendAuthVerificationMessageConfig
        -- The type of verification message to send.
        DeliveryMethod: string
        -- The settings for the email message.
        EmailSettings: EmailSettings
        -- The settings for the SMS message.
        SmsSettings: SmsSettings
    end

    -- Describes the Amazon Cognito user pool configuration for the authorization resource to be configured for your Amplify project on an update.
    record UpdateBackendAuthUserPoolConfig
        -- (DEPRECATED) Describes the forgot password policy for your Amazon Cognito user pool, configured as a part of your Amplify project.
        ForgotPassword: UpdateBackendAuthForgotPasswordConfig
        -- Describes whether to apply multi-factor authentication policies for your Amazon Cognito user pool configured as a part of your Amplify project.
        Mfa: UpdateBackendAuthMFAConfig
        -- Describes the OAuth policy and rules for your Amazon Cognito user pool, configured as a part of your Amplify project.
        OAuth: UpdateBackendAuthOAuthConfig
        -- Describes the password policy for your Amazon Cognito user pool, configured as a part of your Amplify project.
        PasswordPolicy: UpdateBackendAuthPasswordPolicyConfig
        -- Describes the email or SMS verification message for your Amazon Cognito user pool, configured as a part of your Amplify project.
        VerificationMessage: UpdateBackendAuthVerificationMessageConfig
    end

    -- Defines the resource configuration when updating an authentication resource in your Amplify project.
    record UpdateBackendAuthResourceConfig
        -- Defines the service name to use when configuring an authentication resource in your Amplify project.
        AuthResources: string
        -- Describes the authorization configuration for the Amazon Cognito identity pool, provisioned as a part of your auth resource in the Amplify project.
        IdentityPoolConfigs: UpdateBackendAuthIdentityPoolConfig
        -- Defines the service name to use when configuring an authentication resource in your Amplify project.
        Service: string
        -- Describes the authentication configuration for the Amazon Cognito user pool, provisioned as a part of your auth resource in the Amplify project.
        UserPoolConfigs: UpdateBackendAuthUserPoolConfig
    end

    -- The request body for UpdateBackendAuth.
    record UpdateBackendAuthInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The resource configuration for this request object.
        ResourceConfig: UpdateBackendAuthResourceConfig
        -- The name of this resource.
        ResourceName: string
    end

    record UpdateBackendAuthOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
    end

    -- The request object for this operation.
    record LoginAuthConfigReqObj
        -- The Amazon Cognito identity pool ID used for the Amplify Admin UI login authorization.
        AwsCognitoIdentityPoolId: string
        -- The AWS Region for the Amplify Admin UI login.
        AwsCognitoRegion: string
        -- The Amazon Cognito user pool ID used for Amplify Admin UI login authentication.
        AwsUserPoolsId: string
        -- The web client ID for the Amazon Cognito user pools.
        AwsUserPoolsWebClientId: string
    end

    -- The request body for UpdateBackendConfig.
    record UpdateBackendConfigInput
        -- The app ID.
        AppId: string
        -- Describes the Amazon Cognito configuration for Admin UI access.
        LoginAuthConfig: LoginAuthConfigReqObj
    end

    record UpdateBackendConfigOutput
        -- The app ID.
        AppId: string
        -- The app ID for the backend manager.
        BackendManagerAppId: string
        -- If the request fails, this error is returned.
        Error: string
        -- Describes the Amazon Cognito configurations for the Admin UI auth resource to log in with.
        LoginAuthConfig: LoginAuthConfigReqObj
    end

    -- The request body for GetBackendJob.
    record UpdateBackendJobInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The ID for the job.
        JobId: string
        -- Filters the list of response objects to include only those with the specified operation name.
        Operation: string
        -- Filters the list of response objects to include only those with the specified status.
        Status: string
    end

    record UpdateBackendJobOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The time when the job was created.
        CreateTime: string
        -- If the request fails, this error is returned.
        Error: string
        -- The ID for the job.
        JobId: string
        -- The name of the operation.
        Operation: string
        -- The current status of the request.
        Status: string
        -- The time when the job was last updated.
        UpdateTime: string
    end

    -- The resource configuration for updating backend storage.
    record UpdateBackendStorageResourceConfig
        -- The authorization configuration for the storage S3 bucket.
        Permissions: BackendStoragePermissions
        -- The name of the storage service.
        ServiceName: string
    end

    -- The request body for UpdateBackendStorage.
    record UpdateBackendStorageInput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The resource configuration for updating backend storage.
        ResourceConfig: UpdateBackendStorageResourceConfig
        -- The name of the storage resource.
        ResourceName: string
    end

    record UpdateBackendStorageOutput
        -- The app ID.
        AppId: string
        -- The name of the backend environment.
        BackendEnvironmentName: string
        -- The ID for the job.
        JobId: string
        -- The current status of the request.
        Status: string
    end

end

return M
