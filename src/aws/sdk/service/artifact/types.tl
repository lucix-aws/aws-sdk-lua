-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    enum AcceptanceType
        "PASSTHROUGH"
        "EXPLICIT"
    end

    -- User does not have sufficient access to perform this action.
    record AccessDeniedException
        message: string
    end

    enum NotificationSubscriptionStatus
        "SUBSCRIBED"
        "NOT_SUBSCRIBED"
    end

    -- Account settings for the customer.
    record AccountSettings
        -- Notification subscription status of the customer.
        notificationSubscriptionStatus: string
    end

    -- Request to create/modify content would result in a conflict.
    record ConflictException
        message: string
        -- Identifier of the affected resource.
        resourceId: string
        -- Type of the affected resource.
        resourceType: string
    end

    record GetAccountSettingsInput
    end

    record GetAccountSettingsOutput
        accountSettings: AccountSettings
    end

    -- An unknown server exception has occurred.
    record InternalServerException
        message: string
        -- Number of seconds in which the caller can retry the request.
        retryAfterSeconds: number
    end

    -- Request references a resource which does not exist.
    record ResourceNotFoundException
        message: string
        -- Identifier of the affected resource.
        resourceId: string
        -- Type of the affected resource.
        resourceType: string
    end

    -- Request would cause a service quota to be exceeded.
    record ServiceQuotaExceededException
        message: string
        -- Identifier of the affected resource.
        resourceId: string
        -- Type of the affected resource.
        resourceType: string
        -- Code for the affected service.
        serviceCode: string
        -- Code for the affected quota.
        quotaCode: string
    end

    -- Request was denied due to request throttling.
    record ThrottlingException
        message: string
        -- Code for the affected service.
        serviceCode: string
        -- Code for the affected quota.
        quotaCode: string
        -- Number of seconds in which the caller can retry the request.
        retryAfterSeconds: number
    end

    -- Validation exception message and name.
    record ValidationExceptionField
        -- Name of validation exception.
        name: string
        -- Message describing why the field failed validation.
        message: string
    end

    enum ValidationExceptionReason
        "unknownOperation"
        "cannotParse"
        "fieldValidationFailed"
        "invalidToken"
        "other"
    end

    -- Request fails to satisfy the constraints specified by an AWS service.
    record ValidationException
        message: string
        -- Reason the request failed validation.
        reason: string
        -- The field that caused the error, if applicable.
        fieldList: {ValidationExceptionField}
    end

    record PutAccountSettingsInput
        -- Desired notification subscription status.
        notificationSubscriptionStatus: string
    end

    record PutAccountSettingsOutput
        accountSettings: AccountSettings
    end

    enum AgreementType
        "CUSTOM"
        "DEFAULT"
        "MODIFIED"
    end

    -- File content structure for compliance inquiry uploads.
    record InquiryFileContent
        -- List of file sections/sheets to process.
        fileSections: {string}
        -- Binary content of the uploaded file.
        content: string
    end

    -- Content for creating a compliance inquiry - either a single query or file content.
    record InquiryContent
        -- Single text query for AI-generated answer.
        query: string
        -- File content with multiple questions.
        fileContent: InquiryFileContent
    end

    enum InquirySupportMode
        "AI_ONLY"
        "FULL_SUPPORT"
    end

    record CreateComplianceInquiryInput
        -- Title of the inquiry.
        name: string
        -- Content for creating a compliance inquiry - either a single query or file content.
        inquiryContent: InquiryContent
        -- Idempotency token for the request.
        clientToken: string
        -- Support mode for inquiry processing. Only supported for file upload mode. Defaults to AI_ONLY if not specified.
        supportMode: string
        -- Tags to associate with the compliance inquiry resource.
        tags: {string : string}
    end

    enum InputSource
        "TEXT"
        "FILE"
    end

    enum InquiryStatus
        "PROCESSING"
        "HUMAN_REVIEW"
        "COMPLETED"
        "FAILED"
    end

    enum InquiryStatusMessage
        "Compliance inquiry processing is complete."
        "Malware was detected on the file. Provide a new file and try again."
        "Compliance inquiry processing is in-progress."
        "An internal error occurred while processing the inquiry. Try again at a later time."
        "Human review is in progress."
        "Compliance inquiry processing is complete. One or more queries encountered errors during processing."
    end

    -- Summary information about a compliance inquiry.
    record InquirySummary
        -- ARN of the compliance inquiry resource.
        arn: string
        -- Title of the inquiry.
        name: string
        -- Unique resource ID for the compliance inquiry.
        id: string
        -- Current processing status of the inquiry.
        status: string
        -- Status message providing additional context.
        statusMessage: string
        -- Type of inquiry content (text or file).
        inputSource: string
        -- Timestamp indicating when the resource was created.
        createdAt: number
    end

    record CreateComplianceInquiryOutput
        -- Summary information about the created compliance inquiry.
        complianceInquirySummary: InquirySummary
        -- Tags associated with the compliance inquiry resource.
        tags: {string : string}
    end

    record ExportComplianceInquiryInput
        -- Unique resource ID for the compliance inquiry.
        complianceInquiryId: string
        -- List of query identifiers to include in the export.
        queryIdentifiers: {number}
        -- When true, include citations in the exported document.
        includeCitations: boolean
    end

    record ExportComplianceInquiryOutput
        -- Presigned S3 URL to access the exported compliance inquiry report.
        documentPresignedUrl: string
        -- Tags associated with the compliance inquiry resource.
        tags: {string : string}
    end

    record GetComplianceInquiryMetadataInput
        -- Unique resource ID for the compliance inquiry.
        complianceInquiryId: string
    end

    -- Detailed information about a compliance inquiry.
    record InquiryDetail
        -- ARN of the compliance inquiry resource.
        arn: string
        -- Title of the inquiry.
        name: string
        -- Unique resource ID for the compliance inquiry.
        id: string
        -- Current processing status of the inquiry.
        status: string
        -- Status message providing additional context.
        statusMessage: string
        -- Type of inquiry content (text or file).
        inputSource: string
        -- Timestamp indicating when the resource was created.
        createdAt: number
        -- Timestamp indicating when the resource was last modified.
        updatedAt: number
        -- Support mode for this inquiry. AI_ONLY provides AI-generated responses. FULL_SUPPORT includes human expert review.
        supportMode: string
    end

    record GetComplianceInquiryMetadataOutput
        -- Detailed information about the compliance inquiry.
        complianceInquiryDetail: InquiryDetail
        -- Tags associated with the compliance inquiry resource.
        tags: {string : string}
    end

    record ListComplianceInquiriesInput
        -- Maximum number of resources to return in the paginated response.
        maxResults: number
        -- Pagination token to request the next page of resources.
        nextToken: string
    end

    record ListComplianceInquiriesOutput
        -- List of compliance inquiry resources.
        complianceInquiries: {InquirySummary}
        -- Pagination token to request the next page of resources.
        nextToken: string
    end

    record ListComplianceInquiryQueriesInput
        -- Unique resource ID for the compliance inquiry.
        complianceInquiryId: string
        -- Maximum number of resources to return in the paginated response.
        maxResults: number
        -- Pagination token to request the next page of resources.
        nextToken: string
    end

    -- Citation information for AI-generated responses.
    record Citation
        -- Label identifying the compliance source.
        sourceLabel: string
        -- Content text from the compliance source.
        sourceContent: string
        -- Link to the compliance source.
        sourceLink: string
    end

    enum ReviewType
        "HUMAN"
        "AI"
    end

    enum QueryStatus
        "PROCESSING"
        "COMPLETED"
        "FAILED"
    end

    enum QueryStatusMessage
        "Query processing is complete."
        "Query processing is in-progress."
        "An internal error occurred while processing the query. Try again at a later time."
        "Query is pending human review."
        "Query contains restricted or unsupported content."
    end

    -- A versioned snapshot of a response edit.
    record ResponseVersion
        -- The response text for this version.
        responseText: string
        -- ISO 8601 timestamp of when this edit was made.
        timestamp: number
    end

    -- Summary information about a single query within a compliance inquiry.
    record QuerySummary
        -- Sequential identifier of the query within the inquiry.
        queryIdentifier: number
        -- The actual query text.
        query: string
        -- Generated response to the query.
        response: string
        -- Type of review for the response.
        reviewType: string
        -- Supporting citations for the response.
        citations: {Citation}
        -- Current processing status of the query.
        status: string
        -- Descriptive status message.
        statusMessage: string
        -- Timestamp when the query was created.
        createdAt: number
        -- Ordered list of response version history entries, oldest first.
        updatedResponseVersions: {ResponseVersion}
    end

    record ListComplianceInquiryQueriesOutput
        -- List of compliance query summaries.
        queries: {QuerySummary}
        -- Pagination token to request the next page of resources.
        nextToken: string
    end

    record ListCustomerAgreementsInput
        -- Maximum number of resources to return in the paginated response.
        maxResults: number
        -- Pagination token to request the next page of resources.
        nextToken: string
    end

    enum CustomerAgreementState
        "ACTIVE"
        "CUSTOMER_TERMINATED"
        "AWS_TERMINATED"
    end

    -- Summary for customer-agreement resource.
    record CustomerAgreementSummary
        -- Name of the customer-agreement resource.
        name: string
        -- ARN of the customer-agreement resource.
        arn: string
        -- Identifier of the customer-agreement resource.
        id: string
        -- ARN of the agreement resource the customer-agreement resource represents.
        agreementArn: string
        -- AWS account Id that owns the resource.
        awsAccountId: string
        -- ARN of the organization that owns the resource.
        organizationArn: string
        -- Timestamp indicating when the agreement became effective.
        effectiveStart: number
        -- Timestamp indicating when the agreement was terminated.
        effectiveEnd: number
        -- State of the resource.
        state: string
        -- Description of the resource.
        description: string
        -- Terms required to accept the agreement resource.
        acceptanceTerms: {string}
        -- Terms required to terminate the customer-agreement resource.
        terminateTerms: {string}
    end

    record ListCustomerAgreementsOutput
        -- List of customer-agreement resources.
        customerAgreements: {CustomerAgreementSummary}
        -- Pagination token to request the next page of resources.
        nextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- Tags associated with the resource.
        tags: {string : string}
    end

    record GetReportInput
        -- Unique resource ID for the report resource.
        reportId: string
        -- Version for the report resource.
        reportVersion: number
        -- Unique download token provided by GetTermForReport API.
        termToken: string
    end

    record GetReportOutput
        -- Presigned S3 url to access the report content.
        documentPresignedUrl: string
    end

    record GetReportMetadataInput
        -- Unique resource ID for the report resource.
        reportId: string
        -- Version for the report resource.
        reportVersion: number
    end

    enum PublishedState
        "PUBLISHED"
        "UNPUBLISHED"
    end

    enum UploadState
        "PROCESSING"
        "COMPLETE"
        "FAILED"
        "FAULT"
    end

    -- Full detail for report resource metadata.
    record ReportDetail
        -- Unique resource ID for the report resource.
        id: string
        -- Name for the report resource.
        name: string
        -- Description for the report resource.
        description: string
        -- Timestamp indicating the report resource effective start.
        periodStart: number
        -- Timestamp indicating the report resource effective end.
        periodEnd: number
        -- Timestamp indicating when the report resource was created.
        createdAt: number
        -- Timestamp indicating when the report resource was last modified.
        lastModifiedAt: number
        -- Timestamp indicating when the report resource was deleted.
        deletedAt: number
        -- Current state of the report resource
        state: string
        -- ARN for the report resource.
        arn: string
        -- Series for the report resource.
        series: string
        -- Category for the report resource.
        category: string
        -- Associated company name for the report resource.
        companyName: string
        -- Associated product name for the report resource.
        productName: string
        -- Unique resource ARN for term resource.
        termArn: string
        -- Version for the report resource.
        version: number
        -- Acceptance type for report.
        acceptanceType: string
        -- Sequence number to enforce optimistic locking.
        sequenceNumber: number
        -- The current state of the document upload.
        uploadState: string
        -- The message associated with the current upload state.
        statusMessage: string
    end

    record GetReportMetadataOutput
        -- Report resource detail.
        reportDetails: ReportDetail
    end

    record GetTermForReportInput
        -- Unique resource ID for the report resource.
        reportId: string
        -- Version for the report resource.
        reportVersion: number
    end

    record GetTermForReportOutput
        -- Presigned S3 url to access the term content.
        documentPresignedUrl: string
        -- Unique token representing this request event.
        termToken: string
    end

    record ListReportsInput
        -- Maximum number of resources to return in the paginated response.
        maxResults: number
        -- Pagination token to request the next page of resources.
        nextToken: string
    end

    -- Summary for report resource.
    record ReportSummary
        -- Unique resource ID for the report resource.
        id: string
        -- Name for the report resource.
        name: string
        -- Current state of the report resource.
        state: string
        -- ARN for the report resource.
        arn: string
        -- Version for the report resource.
        version: number
        -- The current state of the document upload.
        uploadState: string
        -- Description for the report resource.
        description: string
        -- Timestamp indicating the report resource effective start.
        periodStart: number
        -- Timestamp indicating the report resource effective end.
        periodEnd: number
        -- Series for the report resource.
        series: string
        -- Category for the report resource.
        category: string
        -- Associated company name for the report resource.
        companyName: string
        -- Associated product name for the report resource.
        productName: string
        -- The message associated with the current upload state.
        statusMessage: string
        -- Acceptance type for report.
        acceptanceType: string
    end

    record ListReportsOutput
        -- List of report resources.
        reports: {ReportSummary}
        -- Pagination token to request the next page of resources.
        nextToken: string
    end

    record ListReportVersionsInput
        -- Unique resource ID for the report resource.
        reportId: string
        -- Maximum number of resources to return in the paginated response.
        maxResults: number
        -- Pagination token to request the next page of resources.
        nextToken: string
    end

    record ListReportVersionsOutput
        -- List of report resources.
        reports: {ReportSummary}
        -- Pagination token to request the next page of resources.
        nextToken: string
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
        -- Tags to add to the resource.
        tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
        -- Tag keys to remove from the resource.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

end

return M
