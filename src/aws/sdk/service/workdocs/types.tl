-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    record AbortDocumentVersionUploadInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
        -- The ID of the version.
        VersionId: string
    end

    record AbortDocumentVersionUploadOutput
    end

    -- The resource hierarchy is changing.
    record ConcurrentModificationException
        Message: string
    end

    -- The resource does not exist.
    record EntityNotExistsException
        Message: string
        -- The IDs of the non-existent resources.
        EntityIds: {string}
    end

    -- The Directory Service cannot reach an on-premises instance. Or a dependency
    -- under the control of the organization is failing, such as a connected Active
    -- Directory.
    record FailedDependencyException
        Message: string
    end

    -- The specified document version is not in the INITIALIZED state.
    record ProhibitedStateException
        Message: string
    end

    -- One or more of the dependencies is unavailable.
    record ServiceUnavailableException
        Message: string
    end

    -- The operation is not permitted.
    record UnauthorizedOperationException
        Message: string
        Code: string
    end

    -- The caller does not have access to perform the action on the resource.
    record UnauthorizedResourceAccessException
        Message: string
    end

    record ActivateUserInput
        -- The ID of the user.
        UserId: string
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
    end

    enum LocaleType
        "en"
        "fr"
        "ko"
        "de"
        "es"
        "ja"
        "ru"
        "zh_CN"
        "zh_TW"
        "pt_BR"
        "default"
    end

    enum UserStatusType
        "ACTIVE"
        "INACTIVE"
        "PENDING"
    end

    enum StorageType
        "UNLIMITED"
        "QUOTA"
    end

    -- Describes the storage for a user.
    record StorageRuleType
        -- The amount of storage allocated, in bytes.
        StorageAllocatedInBytes: number
        -- The type of storage.
        StorageType: string
    end

    -- Describes the storage for a user.
    record UserStorageMetadata
        -- The amount of storage used, in bytes.
        StorageUtilizedInBytes: number
        -- The storage for a user.
        StorageRule: StorageRuleType
    end

    enum UserType
        "USER"
        "ADMIN"
        "POWERUSER"
        "MINIMALUSER"
        "WORKSPACESUSER"
    end

    -- Describes a user.
    record User
        -- The ID of the user.
        Id: string
        -- The login name of the user.
        Username: string
        -- The email address of the user.
        EmailAddress: string
        -- The given name of the user.
        GivenName: string
        -- The surname of the user.
        Surname: string
        -- The ID of the organization.
        OrganizationId: string
        -- The ID of the root folder.
        RootFolderId: string
        -- The ID of the recycle bin folder.
        RecycleBinFolderId: string
        -- The status of the user.
        Status: string
        -- The type of user.
        Type: string
        -- The time when the user was created.
        CreatedTimestamp: number
        -- The time when the user was modified.
        ModifiedTimestamp: number
        -- The time zone ID of the user.
        TimeZoneId: string
        -- The locale of the user.
        Locale: string
        -- The storage for the user.
        Storage: UserStorageMetadata
    end

    record ActivateUserOutput
        -- The user information.
        User: User
    end

    enum CommentStatusType
        "DRAFT"
        "PUBLISHED"
        "DELETED"
    end

    -- Describes the metadata of a comment.
    record CommentMetadata
        -- The ID of the comment.
        CommentId: string
        -- The user who made the comment.
        Contributor: User
        -- The timestamp that the comment was created.
        CreatedTimestamp: number
        -- The status of the comment.
        CommentStatus: string
        -- The ID of the user being replied to.
        RecipientId: string
        -- The ID of the user who made the comment.
        ContributorId: string
    end

    -- Describes the metadata of the user.
    record UserMetadata
        -- The ID of the user.
        Id: string
        -- The name of the user.
        Username: string
        -- The given name of the user before a rename operation.
        GivenName: string
        -- The surname of the user.
        Surname: string
        -- The email address of the user.
        EmailAddress: string
    end

    enum ResourceType
        "FOLDER"
        "DOCUMENT"
    end

    -- Describes the metadata of a resource.
    record ResourceMetadata
        -- The type of resource.
        Type: string
        -- The name of the resource.
        Name: string
        -- The original name of the resource before a rename operation.
        OriginalName: string
        -- The ID of the resource.
        Id: string
        -- The version ID of the resource. This is an optional field and is filled for action
        -- on document version.
        VersionId: string
        -- The owner of the resource.
        Owner: UserMetadata
        -- The parent ID of the resource before a rename operation.
        ParentId: string
    end

    -- Describes the metadata of a user group.
    record GroupMetadata
        -- The ID of the user group.
        Id: string
        -- The name of the group.
        Name: string
    end

    -- Describes the users or user groups.
    record Participants
        -- The list of users.
        Users: {UserMetadata}
        -- The list of user groups.
        Groups: {GroupMetadata}
    end

    enum ActivityType
        "DOCUMENT_CHECKED_IN"
        "DOCUMENT_CHECKED_OUT"
        "DOCUMENT_RENAMED"
        "DOCUMENT_VERSION_UPLOADED"
        "DOCUMENT_VERSION_DELETED"
        "DOCUMENT_VERSION_VIEWED"
        "DOCUMENT_VERSION_DOWNLOADED"
        "DOCUMENT_RECYCLED"
        "DOCUMENT_RESTORED"
        "DOCUMENT_REVERTED"
        "DOCUMENT_SHARED"
        "DOCUMENT_UNSHARED"
        "DOCUMENT_SHARE_PERMISSION_CHANGED"
        "DOCUMENT_SHAREABLE_LINK_CREATED"
        "DOCUMENT_SHAREABLE_LINK_REMOVED"
        "DOCUMENT_SHAREABLE_LINK_PERMISSION_CHANGED"
        "DOCUMENT_MOVED"
        "DOCUMENT_COMMENT_ADDED"
        "DOCUMENT_COMMENT_DELETED"
        "DOCUMENT_ANNOTATION_ADDED"
        "DOCUMENT_ANNOTATION_DELETED"
        "FOLDER_CREATED"
        "FOLDER_DELETED"
        "FOLDER_RENAMED"
        "FOLDER_RECYCLED"
        "FOLDER_RESTORED"
        "FOLDER_SHARED"
        "FOLDER_UNSHARED"
        "FOLDER_SHARE_PERMISSION_CHANGED"
        "FOLDER_SHAREABLE_LINK_CREATED"
        "FOLDER_SHAREABLE_LINK_REMOVED"
        "FOLDER_SHAREABLE_LINK_PERMISSION_CHANGED"
        "FOLDER_MOVED"
    end

    -- Describes the activity information.
    record Activity
        -- The activity type.
        Type: string
        -- The timestamp when the action was performed.
        TimeStamp: number
        -- Indicates whether an activity is indirect or direct. An indirect activity results
        -- from a direct activity performed on a parent resource. For example, sharing a parent
        -- folder (the direct activity) shares all of the subfolders and documents within the
        -- parent folder (the indirect activity).
        IsIndirectActivity: boolean
        -- The ID of the organization.
        OrganizationId: string
        -- The user who performed the action.
        Initiator: UserMetadata
        -- The list of users or groups impacted by this action. This is an optional field and
        -- is filled for the following sharing activities: DOCUMENT_SHARED, DOCUMENT_SHARED,
        -- DOCUMENT_UNSHARED, FOLDER_SHARED, FOLDER_UNSHARED.
        Participants: Participants
        -- The metadata of the resource involved in the user action.
        ResourceMetadata: ResourceMetadata
        -- The original parent of the resource. This is an optional field and is filled for
        -- move activities.
        OriginalParent: ResourceMetadata
        -- Metadata of the commenting activity. This is an optional field and is filled for
        -- commenting activities.
        CommentMetadata: CommentMetadata
    end

    enum AdditionalResponseFieldType
        "WEBURL"
    end

    -- Set of options which defines notification preferences of given action.
    record NotificationOptions
        -- Boolean value to indicate an email notification should be sent to the
        -- recipients.
        SendEmail: boolean
        -- Text value to be included in the email body.
        EmailMessage: string
    end

    enum RoleType
        "VIEWER"
        "CONTRIBUTOR"
        "OWNER"
        "COOWNER"
    end

    enum PrincipalType
        "USER"
        "GROUP"
        "INVITE"
        "ANONYMOUS"
        "ORGANIZATION"
    end

    -- Describes the recipient type and ID, if available.
    record SharePrincipal
        -- The ID of the recipient.
        Id: string
        -- The type of the recipient.
        Type: string
        -- The role of the recipient.
        Role: string
    end

    record AddResourcePermissionsInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the resource.
        ResourceId: string
        -- The users, groups, or organization being granted permission.
        Principals: {SharePrincipal}
        -- The notification options.
        NotificationOptions: NotificationOptions
    end

    enum ShareStatusType
        "SUCCESS"
        "FAILURE"
    end

    -- Describes the share results of a resource.
    record ShareResult
        -- The ID of the principal.
        PrincipalId: string
        -- The ID of the invited user.
        InviteePrincipalId: string
        -- The role.
        Role: string
        -- The status.
        Status: string
        -- The ID of the resource that was shared.
        ShareId: string
        -- The status message.
        StatusMessage: string
    end

    record AddResourcePermissionsOutput
        -- The share results.
        ShareResults: {ShareResult}
    end

    enum CommentVisibilityType
        "PUBLIC"
        "PRIVATE"
    end

    record CreateCommentInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
        -- The ID of the document version.
        VersionId: string
        -- The ID of the parent comment.
        ParentId: string
        -- The ID of the root comment in the thread.
        ThreadId: string
        -- The text of the comment.
        Text: string
        -- The visibility of the comment. Options are either PRIVATE, where the comment is
        -- visible only to the comment author and document owner and co-owners, or PUBLIC, where
        -- the comment is visible to document owners, co-owners, and contributors.
        Visibility: string
        -- Set this parameter to TRUE to send an email out to the document collaborators after
        -- the comment is created.
        NotifyCollaborators: boolean
    end

    -- Describes a comment.
    record Comment
        -- The ID of the comment.
        CommentId: string
        -- The ID of the parent comment.
        ParentId: string
        -- The ID of the root comment in the thread.
        ThreadId: string
        -- The text of the comment.
        Text: string
        -- The details of the user who made the comment.
        Contributor: User
        -- The time that the comment was created.
        CreatedTimestamp: number
        -- The status of the comment.
        Status: string
        -- The visibility of the comment. Options are either PRIVATE, where the comment is
        -- visible only to the comment author and document owner and co-owners, or PUBLIC, where
        -- the comment is visible to document owners, co-owners, and contributors.
        Visibility: string
        -- If the comment is a reply to another user's comment, this field contains the user
        -- ID of the user being replied to.
        RecipientId: string
    end

    record CreateCommentOutput
        -- The comment that has been created.
        Comment: Comment
    end

    -- This exception is thrown when the document is locked for comments and user tries to
    -- create or delete a comment on that document.
    record DocumentLockedForCommentsException
        Message: string
    end

    -- The requested operation is not allowed on the specified comment object.
    record InvalidCommentOperationException
        Message: string
    end

    record CreateCustomMetadataInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the resource.
        ResourceId: string
        -- The ID of the version, if the custom metadata is being added to a document
        -- version.
        VersionId: string
        -- Custom metadata in the form of name-value pairs.
        CustomMetadata: {string : string}
    end

    record CreateCustomMetadataOutput
    end

    -- The limit has been reached on the number of custom properties for the specified
    -- resource.
    record CustomMetadataLimitExceededException
        Message: string
    end

    -- Another operation is in progress on the resource that conflicts with the current operation.
    record ConflictingOperationException
        Message: string
    end

    record CreateFolderInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The name of the new folder.
        Name: string
        -- The ID of the parent folder.
        ParentFolderId: string
    end

    enum ResourceStateType
        "ACTIVE"
        "RESTORING"
        "RECYCLING"
        "RECYCLED"
    end

    -- Describes a folder.
    record FolderMetadata
        -- The ID of the folder.
        Id: string
        -- The name of the folder.
        Name: string
        -- The ID of the creator.
        CreatorId: string
        -- The ID of the parent folder.
        ParentFolderId: string
        -- The time when the folder was created.
        CreatedTimestamp: number
        -- The time when the folder was updated.
        ModifiedTimestamp: number
        -- The resource state of the folder.
        ResourceState: string
        -- The unique identifier created from the subfolders and documents of the
        -- folder.
        Signature: string
        -- List of labels on the folder.
        Labels: {string}
        -- The size of the folder metadata.
        Size: number
        -- The size of the latest version of the folder metadata.
        LatestVersionSize: number
    end

    record CreateFolderOutput
        -- The metadata of the folder.
        Metadata: FolderMetadata
    end

    -- The resource already exists.
    record EntityAlreadyExistsException
        Message: string
    end

    -- The maximum of 100,000 files and folders under the parent folder has been exceeded.
    record LimitExceededException
        Message: string
    end

    record CreateLabelsInput
        -- The ID of the resource.
        ResourceId: string
        -- List of labels to add to the resource.
        Labels: {string}
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
    end

    record CreateLabelsOutput
    end

    -- The limit has been reached on the number of labels for the specified
    -- resource.
    record TooManyLabelsException
        Message: string
    end

    enum SubscriptionProtocolType
        "HTTPS"
        "SQS"
    end

    enum SubscriptionType
        "ALL"
    end

    record CreateNotificationSubscriptionInput
        -- The ID of the organization.
        OrganizationId: string
        -- The endpoint to receive the notifications. If the protocol is HTTPS, the endpoint
        -- is a URL that begins with https.
        Endpoint: string
        -- The protocol to use. The supported value is https, which delivers JSON-encoded
        -- messages using HTTPS POST.
        Protocol: string
        -- The notification type.
        SubscriptionType: string
    end

    -- Describes a subscription.
    record Subscription
        -- The ID of the subscription.
        SubscriptionId: string
        -- The endpoint of the subscription.
        EndPoint: string
        -- The protocol of the subscription.
        Protocol: string
    end

    record CreateNotificationSubscriptionOutput
        -- The subscription.
        Subscription: Subscription
    end

    -- The pagination marker or limit fields are not valid.
    record InvalidArgumentException
        Message: string
    end

    -- You've reached the limit on the number of subscriptions for the WorkDocs
    -- instance.
    record TooManySubscriptionsException
        Message: string
    end

    record CreateUserInput
        -- The ID of the organization.
        OrganizationId: string
        -- The login name of the user.
        Username: string
        -- The email address of the user.
        EmailAddress: string
        -- The given name of the user.
        GivenName: string
        -- The surname of the user.
        Surname: string
        -- The password of the user.
        Password: string
        -- The time zone ID of the user.
        TimeZoneId: string
        -- The amount of storage for the user.
        StorageRule: StorageRuleType
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
    end

    record CreateUserOutput
        -- The user information.
        User: User
    end

    record DeactivateUserInput
        -- The ID of the user.
        UserId: string
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
    end

    record DeactivateUserOutput
    end

    record DeleteCommentInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
        -- The ID of the document version.
        VersionId: string
        -- The ID of the comment.
        CommentId: string
    end

    record DeleteCommentOutput
    end

    record DeleteCustomMetadataInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the resource, either a document or folder.
        ResourceId: string
        -- The ID of the version, if the custom metadata is being deleted from a document
        -- version.
        VersionId: string
        -- List of properties to remove.
        Keys: {string}
        -- Flag to indicate removal of all custom metadata properties from the specified
        -- resource.
        DeleteAll: boolean
    end

    record DeleteCustomMetadataOutput
    end

    record DeleteDocumentInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
    end

    record DeleteDocumentOutput
    end

    record DeleteDocumentVersionInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document associated with the version being deleted.
        DocumentId: string
        -- The ID of the version being deleted.
        VersionId: string
        -- Deletes all versions of a document prior to the current version.
        DeletePriorVersions: boolean
    end

    record DeleteDocumentVersionOutput
    end

    -- The operation is invalid.
    record InvalidOperationException
        Message: string
    end

    record DeleteFolderInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the folder.
        FolderId: string
    end

    record DeleteFolderOutput
    end

    record DeleteFolderContentsInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the folder.
        FolderId: string
    end

    record DeleteFolderContentsOutput
    end

    record DeleteLabelsInput
        -- The ID of the resource.
        ResourceId: string
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- List of labels to delete from the resource.
        Labels: {string}
        -- Flag to request removal of all labels from the specified resource.
        DeleteAll: boolean
    end

    record DeleteLabelsOutput
    end

    record DeleteNotificationSubscriptionInput
        -- The ID of the subscription.
        SubscriptionId: string
        -- The ID of the organization.
        OrganizationId: string
    end

    record DeleteNotificationSubscriptionOutput
    end

    record DeleteUserInput
        -- Amazon WorkDocs authentication token. Do not set this field when using
        -- administrative API actions, as in accessing the API using Amazon Web Services credentials.
        AuthenticationToken: string
        -- The ID of the user.
        UserId: string
    end

    record DeleteUserOutput
    end

    record DescribeActivitiesInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The timestamp that determines the starting time of the activities. The response
        -- includes the activities performed after the specified timestamp.
        StartTime: number
        -- The timestamp that determines the end time of the activities. The response includes
        -- the activities performed before the specified timestamp.
        EndTime: number
        -- The ID of the organization. This is a mandatory parameter when using administrative
        -- API (SigV4) requests.
        OrganizationId: string
        -- Specifies which activity types to include in the response. If this field is left
        -- empty, all activity types are returned.
        ActivityTypes: string
        -- The document or folder ID for which to describe activity types.
        ResourceId: string
        -- The ID of the user who performed the action. The response includes activities
        -- pertaining to this user. This is an optional parameter and is only applicable for
        -- administrative API (SigV4) requests.
        UserId: string
        -- Includes indirect activities. An indirect activity results from a direct activity
        -- performed on a parent resource. For example, sharing a parent folder (the direct
        -- activity) shares all of the subfolders and documents within the parent folder (the
        -- indirect activity).
        IncludeIndirectActivities: boolean
        -- The maximum number of items to return.
        Limit: number
        -- The marker for the next set of results.
        Marker: string
    end

    record DescribeActivitiesOutput
        -- The list of activities for the specified user and time period.
        UserActivities: {Activity}
        -- The marker for the next set of results.
        Marker: string
    end

    record DescribeCommentsInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
        -- The ID of the document version.
        VersionId: string
        -- The maximum number of items to return.
        Limit: number
        -- The marker for the next set of results. This marker was received from a previous
        -- call.
        Marker: string
    end

    record DescribeCommentsOutput
        -- The list of comments for the specified document version.
        Comments: {Comment}
        -- The marker for the next set of results. This marker was received from a previous
        -- call.
        Marker: string
    end

    record DescribeDocumentVersionsInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
        -- The marker for the next set of results. (You received this marker from a previous
        -- call.)
        Marker: string
        -- The maximum number of versions to return with this call.
        Limit: number
        -- A comma-separated list of values. Specify "INITIALIZED" to include incomplete
        -- versions.
        Include: string
        -- Specify "SOURCE" to include initialized versions and a URL for the source
        -- document.
        Fields: string
    end

    enum DocumentSourceType
        "ORIGINAL"
        "WITH_COMMENTS"
    end

    enum DocumentStatusType
        "INITIALIZED"
        "ACTIVE"
    end

    enum DocumentThumbnailType
        "SMALL"
        "SMALL_HQ"
        "LARGE"
    end

    -- Describes a version of a document.
    record DocumentVersionMetadata
        -- The ID of the version.
        Id: string
        -- The name of the version.
        Name: string
        -- The content type of the document.
        ContentType: string
        -- The size of the document, in bytes.
        Size: number
        -- The signature of the document.
        Signature: string
        -- The status of the document.
        Status: string
        -- The timestamp when the document was first uploaded.
        CreatedTimestamp: number
        -- The timestamp when the document was last uploaded.
        ModifiedTimestamp: number
        -- The timestamp when the content of the document was originally created.
        ContentCreatedTimestamp: number
        -- The timestamp when the content of the document was modified.
        ContentModifiedTimestamp: number
        -- The ID of the creator.
        CreatorId: string
        -- The thumbnail of the document.
        Thumbnail: {string : string}
        -- The source of the document.
        Source: {string : string}
    end

    record DescribeDocumentVersionsOutput
        -- The document versions.
        DocumentVersions: {DocumentVersionMetadata}
        -- The marker to use when requesting the next set of results. If there are no
        -- additional results, the string is empty.
        Marker: string
    end

    -- The password is invalid.
    record InvalidPasswordException
        Message: string
    end

    enum OrderType
        "ASCENDING"
        "DESCENDING"
    end

    enum ResourceSortType
        "DATE"
        "NAME"
    end

    enum FolderContentType
        "ALL"
        "DOCUMENT"
        "FOLDER"
    end

    record DescribeFolderContentsInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the folder.
        FolderId: string
        -- The sorting criteria.
        Sort: string
        -- The order for the contents of the folder.
        Order: string
        -- The maximum number of items to return with this call.
        Limit: number
        -- The marker for the next set of results. This marker was received from a previous
        -- call.
        Marker: string
        -- The type of items.
        Type: string
        -- The contents to include. Specify "INITIALIZED" to include initialized
        -- documents.
        Include: string
    end

    -- Describes the document.
    record DocumentMetadata
        -- The ID of the document.
        Id: string
        -- The ID of the creator.
        CreatorId: string
        -- The ID of the parent folder.
        ParentFolderId: string
        -- The time when the document was created.
        CreatedTimestamp: number
        -- The time when the document was updated.
        ModifiedTimestamp: number
        -- The latest version of the document.
        LatestVersionMetadata: DocumentVersionMetadata
        -- The resource state.
        ResourceState: string
        -- List of labels on the document.
        Labels: {string}
    end

    record DescribeFolderContentsOutput
        -- The subfolders in the specified folder.
        Folders: {FolderMetadata}
        -- The documents in the specified folder.
        Documents: {DocumentMetadata}
        -- The marker to use when requesting the next set of results. If there are no
        -- additional results, the string is empty.
        Marker: string
    end

    record DescribeGroupsInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- A query to describe groups by group name.
        SearchQuery: string
        -- The ID of the organization.
        OrganizationId: string
        -- The marker for the next set of results. (You received this marker from a previous
        -- call.)
        Marker: string
        -- The maximum number of items to return with this call.
        Limit: number
    end

    record DescribeGroupsOutput
        -- The list of groups.
        Groups: {GroupMetadata}
        -- The marker to use when requesting the next set of results. If there are no additional
        -- results, the string is empty.
        Marker: string
    end

    record DescribeNotificationSubscriptionsInput
        -- The ID of the organization.
        OrganizationId: string
        -- The marker for the next set of results. (You received this marker from a previous
        -- call.)
        Marker: string
        -- The maximum number of items to return with this call.
        Limit: number
    end

    record DescribeNotificationSubscriptionsOutput
        -- The subscriptions.
        Subscriptions: {Subscription}
        -- The marker to use when requesting the next set of results. If there are no
        -- additional results, the string is empty.
        Marker: string
    end

    record DescribeResourcePermissionsInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the resource.
        ResourceId: string
        -- The ID of the principal to filter permissions by.
        PrincipalId: string
        -- The maximum number of items to return with this call.
        Limit: number
        -- The marker for the next set of results. (You received this marker from a previous
        -- call)
        Marker: string
    end

    enum RolePermissionType
        "DIRECT"
        "INHERITED"
    end

    -- Describes the permissions.
    record PermissionInfo
        -- The role of the user.
        Role: string
        -- The type of permissions.
        Type: string
    end

    -- Describes a resource.
    record Principal
        -- The ID of the resource.
        Id: string
        -- The type of resource.
        Type: string
        -- The permission information for the resource.
        Roles: {PermissionInfo}
    end

    record DescribeResourcePermissionsOutput
        -- The principals.
        Principals: {Principal}
        -- The marker to use when requesting the next set of results. If there are no
        -- additional results, the string is empty.
        Marker: string
    end

    record DescribeRootFoldersInput
        -- Amazon WorkDocs authentication token.
        AuthenticationToken: string
        -- The maximum number of items to return.
        Limit: number
        -- The marker for the next set of results. (You received this marker from a previous
        -- call.)
        Marker: string
    end

    record DescribeRootFoldersOutput
        -- The user's special folders.
        Folders: {FolderMetadata}
        -- The marker for the next set of results.
        Marker: string
    end

    enum UserFilterType
        "ALL"
        "ACTIVE_PENDING"
    end

    enum UserSortType
        "USER_NAME"
        "FULL_NAME"
        "STORAGE_LIMIT"
        "USER_STATUS"
        "STORAGE_USED"
    end

    record DescribeUsersInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the organization.
        OrganizationId: string
        -- The IDs of the users.
        UserIds: string
        -- A query to filter users by user name. Remember the following about the Userids and Query parameters:
        -- If you don't use either parameter, the API returns a paginated list of all users on the site.
        -- If you use both parameters, the API ignores the Query parameter.
        -- The Userid parameter only returns user names that match a corresponding user ID.
        -- The Query parameter runs a "prefix" search for users by the GivenName, SurName, or UserName fields included in a
        -- CreateUser API call. For example, querying on
        -- Ma returns Márcia Oliveira, María García, and Mateo Jackson. If you use multiple characters, the API only returns data that matches all characters. For example, querying on Ma J only
        -- returns Mateo Jackson.
        Query: string
        -- The state of the users. Specify "ALL" to include inactive users.
        Include: string
        -- The order for the results.
        Order: string
        -- The sorting criteria.
        Sort: string
        -- The marker for the next set of results. (You received this marker from a previous
        -- call.)
        Marker: string
        -- The maximum number of items to return.
        Limit: number
        -- A comma-separated list of values. Specify "STORAGE_METADATA" to include the user
        -- storage quota and utilization information.
        Fields: string
    end

    record DescribeUsersOutput
        -- The users.
        Users: {User}
        -- The total number of users included in the results.
        TotalNumberOfUsers: number
        -- The marker to use when requesting the next set of results. If there are no
        -- additional results, the string is empty.
        Marker: string
    end

    -- The response is too large to return. The request must include a filter to reduce the size of the response.
    record RequestedEntityTooLargeException
        Message: string
    end

    record GetCurrentUserInput
        -- Amazon WorkDocs authentication token.
        AuthenticationToken: string
    end

    record GetCurrentUserOutput
        -- Metadata of the user.
        User: User
    end

    record GetDocumentInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
        -- Set this to TRUE to include custom metadata in the response.
        IncludeCustomMetadata: boolean
    end

    record GetDocumentOutput
        -- The metadata details of the document.
        Metadata: DocumentMetadata
        -- The custom metadata on the document.
        CustomMetadata: {string : string}
    end

    record GetDocumentPathInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
        -- The maximum number of levels in the hierarchy to return.
        Limit: number
        -- A comma-separated list of values. Specify NAME to include the names of
        -- the parent folders.
        Fields: string
        -- This value is not supported.
        Marker: string
    end

    -- Describes the resource path.
    record ResourcePathComponent
        -- The ID of the resource path.
        Id: string
        -- The name of the resource path.
        Name: string
    end

    -- Describes the path information of a resource.
    record ResourcePath
        -- The components of the resource path.
        Components: {ResourcePathComponent}
    end

    record GetDocumentPathOutput
        -- The path information.
        Path: ResourcePath
    end

    record GetDocumentVersionInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
        -- The version ID of the document.
        VersionId: string
        -- A comma-separated list of values. Specify "SOURCE" to include a URL for the source
        -- document.
        Fields: string
        -- Set this to TRUE to include custom metadata in the response.
        IncludeCustomMetadata: boolean
    end

    record GetDocumentVersionOutput
        -- The version metadata.
        Metadata: DocumentVersionMetadata
        -- The custom metadata on the document version.
        CustomMetadata: {string : string}
    end

    record GetFolderInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the folder.
        FolderId: string
        -- Set to TRUE to include custom metadata in the response.
        IncludeCustomMetadata: boolean
    end

    record GetFolderOutput
        -- The metadata of the folder.
        Metadata: FolderMetadata
        -- The custom metadata on the folder.
        CustomMetadata: {string : string}
    end

    record GetFolderPathInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the folder.
        FolderId: string
        -- The maximum number of levels in the hierarchy to return.
        Limit: number
        -- A comma-separated list of values. Specify "NAME" to include the names of the parent
        -- folders.
        Fields: string
        -- This value is not supported.
        Marker: string
    end

    record GetFolderPathOutput
        -- The path information.
        Path: ResourcePath
    end

    enum ResourceCollectionType
        "SHARED_WITH_ME"
    end

    record GetResourcesInput
        -- The Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The user ID for the resource collection. This is a required field for accessing the
        -- API operation using IAM credentials.
        UserId: string
        -- The collection type.
        CollectionType: string
        -- The maximum number of resources to return.
        Limit: number
        -- The marker for the next set of results. This marker was received from a previous call.
        Marker: string
    end

    record GetResourcesOutput
        -- The folders in the specified folder.
        Folders: {FolderMetadata}
        -- The documents in the specified collection.
        Documents: {DocumentMetadata}
        -- The marker to use when requesting the next set of results. If there are no additional results, the string is empty.
        Marker: string
    end

    -- This exception is thrown when a valid checkout ID is not presented on document
    -- version upload calls for a document that has been checked out from Web client.
    record DraftUploadOutOfSyncException
        Message: string
    end

    record InitiateDocumentVersionUploadInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        Id: string
        -- The name of the document.
        Name: string
        -- The timestamp when the content of the document was originally created.
        ContentCreatedTimestamp: number
        -- The timestamp when the content of the document was modified.
        ContentModifiedTimestamp: number
        -- The content type of the document.
        ContentType: string
        -- The size of the document, in bytes.
        DocumentSizeInBytes: number
        -- The ID of the parent folder.
        ParentFolderId: string
    end

    -- Describes the upload.
    record UploadMetadata
        -- The URL of the upload.
        UploadUrl: string
        -- The signed headers.
        SignedHeaders: {string : string}
    end

    record InitiateDocumentVersionUploadOutput
        -- The document metadata.
        Metadata: DocumentMetadata
        -- The upload metadata.
        UploadMetadata: UploadMetadata
    end

    -- The resource is already checked out.
    record ResourceAlreadyCheckedOutException
        Message: string
    end

    -- The storage limit has been exceeded.
    record StorageLimitExceededException
        Message: string
    end

    -- The storage limit will be exceeded.
    record StorageLimitWillExceedException
        Message: string
    end

    record RemoveAllResourcePermissionsInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the resource.
        ResourceId: string
    end

    record RemoveAllResourcePermissionsOutput
    end

    record RemoveResourcePermissionInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the resource.
        ResourceId: string
        -- The principal ID of the resource.
        PrincipalId: string
        -- The principal type of the resource.
        PrincipalType: string
    end

    record RemoveResourcePermissionOutput
    end

    record RestoreDocumentVersionsInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
    end

    record RestoreDocumentVersionsOutput
    end

    enum ContentCategoryType
        "IMAGE"
        "DOCUMENT"
        "PDF"
        "SPREADSHEET"
        "PRESENTATION"
        "AUDIO"
        "VIDEO"
        "SOURCE_CODE"
        "OTHER"
    end

    -- Filters results based on timestamp range (in epochs).
    record DateRangeType
        -- Timestamp range start value (in epochs)
        StartValue: number
        -- Timestamp range end value (in epochs).
        EndValue: number
    end

    enum PrincipalRoleType
        "VIEWER"
        "CONTRIBUTOR"
        "OWNER"
        "COOWNER"
    end

    -- Filter based on UserIds or GroupIds.
    record SearchPrincipalType
        -- UserIds or GroupIds.
        Id: string
        -- The Role of a User or Group.
        Roles: {string}
    end

    enum SearchResourceType
        "FOLDER"
        "DOCUMENT"
        "COMMENT"
        "DOCUMENT_VERSION"
    end

    enum SearchCollectionType
        "OWNED"
        "SHARED_WITH_ME"
    end

    -- Filter based on size (in bytes).
    record LongRangeType
        -- The size start range (in bytes).
        StartValue: number
        -- The size end range (in bytes).
        EndValue: number
    end

    enum LanguageCodeType
        "AR"
        "BG"
        "BN"
        "DA"
        "DE"
        "CS"
        "EL"
        "EN"
        "ES"
        "FA"
        "FI"
        "FR"
        "HI"
        "HU"
        "ID"
        "IT"
        "JA"
        "KO"
        "LT"
        "LV"
        "NL"
        "NO"
        "PT"
        "RO"
        "RU"
        "SV"
        "SW"
        "TH"
        "TR"
        "ZH"
        "DEFAULT"
    end

    -- Filters results based on entity metadata.
    record Filters
        -- Filters by the locale of the content or comment.
        TextLocales: {string}
        -- Filters by content category.
        ContentCategories: {string}
        -- Filters based on entity type.
        ResourceTypes: {string}
        -- Filter by labels using exact match.
        Labels: {string}
        -- Filter based on UserIds or GroupIds.
        Principals: {SearchPrincipalType}
        -- Filter based on resource’s path.
        AncestorIds: {string}
        -- Filter based on file groupings.
        SearchCollectionTypes: {string}
        -- Filter based on size (in bytes).
        SizeRange: LongRangeType
        -- Filter based on resource’s creation timestamp.
        CreatedRange: DateRangeType
        -- Filter based on resource’s modified timestamp.
        ModifiedRange: DateRangeType
    end

    enum OrderByFieldType
        "RELEVANCE"
        "NAME"
        "SIZE"
        "CREATED_TIMESTAMP"
        "MODIFIED_TIMESTAMP"
    end

    enum SortOrder
        "ASC"
        "DESC"
    end

    -- The result of the sort operation.
    record SearchSortResult
        -- Sort search results based on this field name.
        Field: string
        -- Sort direction.
        Order: string
    end

    enum SearchQueryScopeType
        "NAME"
        "CONTENT"
    end

    record SearchResourcesInput
        -- WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The String to search for. Searches across different text fields based on request parameters. Use double quotes around the query string for exact phrase matches.
        QueryText: string
        -- Filter based on the text field type. A Folder has only a name and no content. A Comment has only content and no name. A Document or Document Version has a name and content
        QueryScopes: {string}
        -- Filters based on the resource owner OrgId. This is a mandatory parameter when using Admin SigV4 credentials.
        OrganizationId: string
        -- A list of attributes to include in the response. Used to request fields that are not normally
        -- returned in a standard response.
        AdditionalResponseFields: {string}
        -- Filters results based on entity metadata.
        Filters: Filters
        -- Order by results in one or more categories.
        OrderBy: {SearchSortResult}
        -- Max results count per page.
        Limit: number
        -- The marker for the next set of results.
        Marker: string
    end

    enum ResponseItemType
        "DOCUMENT"
        "FOLDER"
        "COMMENT"
        "DOCUMENT_VERSION"
    end

    -- List of Documents, Folders, Comments, and Document Versions matching the query.
    record ResponseItem
        -- The type of item being returned.
        ResourceType: string
        -- The webUrl of the item being returned.
        WebUrl: string
        -- The document that matches the query.
        DocumentMetadata: DocumentMetadata
        -- The folder that matches the query.
        FolderMetadata: FolderMetadata
        -- The comment that matches the query.
        CommentMetadata: CommentMetadata
        -- The document version that matches the metadata.
        DocumentVersionMetadata: DocumentVersionMetadata
    end

    record SearchResourcesOutput
        -- List of Documents, Folders, Comments, and Document Versions matching the query.
        Items: {ResponseItem}
        -- The marker to use when requesting the next set of results. If there are no additional results, the string is empty.
        Marker: string
    end

    record UpdateDocumentInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
        -- The name of the document.
        Name: string
        -- The ID of the parent folder.
        ParentFolderId: string
        -- The resource state of the document. Only ACTIVE and RECYCLED are
        -- supported.
        ResourceState: string
    end

    record UpdateDocumentOutput
    end

    enum DocumentVersionStatus
        "ACTIVE"
    end

    record UpdateDocumentVersionInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the document.
        DocumentId: string
        -- The version ID of the document.
        VersionId: string
        -- The status of the version.
        VersionStatus: string
    end

    record UpdateDocumentVersionOutput
    end

    record UpdateFolderInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the folder.
        FolderId: string
        -- The name of the folder.
        Name: string
        -- The ID of the parent folder.
        ParentFolderId: string
        -- The resource state of the folder. Only ACTIVE and RECYCLED are accepted values from
        -- the API.
        ResourceState: string
    end

    record UpdateFolderOutput
    end

    -- The last user in the organization is being deactivated.
    record DeactivatingLastSystemUserException
        Message: string
        Code: string
    end

    -- The user is undergoing transfer of ownership.
    record IllegalUserStateException
        Message: string
    end

    enum BooleanEnumType
        "TRUE"
        "FALSE"
    end

    record UpdateUserInput
        -- Amazon WorkDocs authentication token. Not required when using Amazon Web Services administrator credentials to access the API.
        AuthenticationToken: string
        -- The ID of the user.
        UserId: string
        -- The given name of the user.
        GivenName: string
        -- The surname of the user.
        Surname: string
        -- The type of the user.
        Type: string
        -- The amount of storage for the user.
        StorageRule: StorageRuleType
        -- The time zone ID of the user.
        TimeZoneId: string
        -- The locale of the user.
        Locale: string
        -- Boolean value to determine whether the user is granted Power user privileges.
        GrantPoweruserPrivileges: string
    end

    record UpdateUserOutput
        -- The user information.
        User: User
    end

end

return M
