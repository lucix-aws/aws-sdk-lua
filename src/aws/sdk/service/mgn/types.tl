-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- Operation denied due to a file permission or access check error.
    record AccessDeniedException
        message: string
        code: string
    end

    enum ActionCategory
        "DISASTER_RECOVERY"
        "OPERATING_SYSTEM"
        "LICENSE_AND_SUBSCRIPTION"
        "VALIDATION"
        "OBSERVABILITY"
        "REFACTORING"
        "SECURITY"
        "NETWORKING"
        "CONFIGURATION"
        "BACKUP"
        "OTHER"
    end

    enum AnalyzerType
        "REACHABILITY_ANALYZER"
    end

    enum ApplicationHealthStatus
        "HEALTHY"
        "LAGGING"
        "ERROR"
    end

    enum ApplicationProgressStatus
        "NOT_STARTED"
        "IN_PROGRESS"
        "COMPLETED"
    end

    -- Application aggregated status.
    record ApplicationAggregatedStatus
        -- Application aggregated status last update dateTime.
        lastUpdateDateTime: string
        -- Application aggregated status health status.
        healthStatus: string
        -- Application aggregated status progress status.
        progressStatus: string
        -- Application aggregated status total source servers amount.
        totalSourceServers: number
    end

    record Application
        -- Application ID.
        applicationID: string
        -- Application ARN.
        arn: string
        -- Application name.
        name: string
        -- Application description.
        description: string
        -- Application archival status.
        isArchived: boolean
        -- Application aggregated status.
        applicationAggregatedStatus: ApplicationAggregatedStatus
        -- Application creation dateTime.
        creationDateTime: string
        -- Application last modified dateTime.
        lastModifiedDateTime: string
        -- Application tags.
        tags: {string : string}
        -- Application wave ID.
        waveID: string
    end

    record ArchiveApplicationInput
        -- Application ID.
        applicationID: string
        -- Account ID.
        accountID: string
    end

    record ArchiveApplicationOutput
        -- Application ID.
        applicationID: string
        -- Application ARN.
        arn: string
        -- Application name.
        name: string
        -- Application description.
        description: string
        -- Application archival status.
        isArchived: boolean
        -- Application aggregated status.
        applicationAggregatedStatus: ApplicationAggregatedStatus
        -- Application creation dateTime.
        creationDateTime: string
        -- Application last modified dateTime.
        lastModifiedDateTime: string
        -- Application tags.
        tags: {string : string}
        -- Application wave ID.
        waveID: string
    end

    -- Error details.
    record ErrorDetails
        -- Error details message.
        message: string
        -- Error details code.
        code: string
        -- Error details resourceId.
        resourceId: string
        -- Error details resourceType.
        resourceType: string
    end

    -- The request could not be completed due to a conflict with the current state of the target resource.
    record ConflictException
        message: string
        code: string
        -- A conflict occurred when prompting for the Resource ID.
        resourceId: string
        -- A conflict occurred when prompting for resource type.
        resourceType: string
        -- Conflict Exception specific errors.
        errors: {ErrorDetails}
    end

    -- Resource not found exception.
    record ResourceNotFoundException
        message: string
        code: string
        -- Resource ID not found error.
        resourceId: string
        -- Resource type not found error.
        resourceType: string
    end

    -- The request could not be completed because it exceeded the service quota.
    record ServiceQuotaExceededException
        message: string
        code: string
        -- Exceeded the service quota resource ID.
        resourceId: string
        -- Exceeded the service quota resource type.
        resourceType: string
        -- Exceeded the service quota service code.
        serviceCode: string
        -- Exceeded the service quota code.
        quotaCode: string
        -- Exceeded the service quota value.
        quotaValue: number
    end

    -- Uninitialized account exception.
    record UninitializedAccountException
        message: string
        code: string
    end

    record AssociateSourceServersInput
        -- Application ID.
        applicationID: string
        -- Source server IDs list.
        sourceServerIDs: {string}
        -- Account ID.
        accountID: string
    end

    record AssociateSourceServersOutput
    end

    record CreateApplicationInput
        -- Application name.
        name: string
        -- Application description.
        description: string
        -- Application tags.
        tags: {string : string}
        -- Account ID.
        accountID: string
    end

    record CreateApplicationOutput
        -- Application ID.
        applicationID: string
        -- Application ARN.
        arn: string
        -- Application name.
        name: string
        -- Application description.
        description: string
        -- Application archival status.
        isArchived: boolean
        -- Application aggregated status.
        applicationAggregatedStatus: ApplicationAggregatedStatus
        -- Application creation dateTime.
        creationDateTime: string
        -- Application last modified dateTime.
        lastModifiedDateTime: string
        -- Application tags.
        tags: {string : string}
        -- Application wave ID.
        waveID: string
    end

    record DeleteApplicationInput
        -- Application ID.
        applicationID: string
        -- Account ID.
        accountID: string
    end

    record DeleteApplicationOutput
    end

    record DisassociateSourceServersInput
        -- Application ID.
        applicationID: string
        -- Source server IDs list.
        sourceServerIDs: {string}
        -- Account ID.
        accountID: string
    end

    record DisassociateSourceServersOutput
    end

    -- Applications list filters.
    record ListApplicationsRequestFilters
        -- Filter applications list by application ID.
        applicationIDs: {string}
        -- Filter applications list by archival status.
        isArchived: boolean
        -- Filter applications list by wave ID.
        waveIDs: {string}
    end

    record ListApplicationsInput
        -- Applications list filters.
        filters: ListApplicationsRequestFilters
        -- Maximum results to return when listing applications.
        maxResults: number
        -- Request next token.
        nextToken: string
        -- Applications list Account ID.
        accountID: string
    end

    record ListApplicationsOutput
        -- Applications list.
        items: {Application}
        -- Response next token.
        nextToken: string
    end

    record UnarchiveApplicationInput
        -- Application ID.
        applicationID: string
        -- Account ID.
        accountID: string
    end

    record UnarchiveApplicationOutput
        -- Application ID.
        applicationID: string
        -- Application ARN.
        arn: string
        -- Application name.
        name: string
        -- Application description.
        description: string
        -- Application archival status.
        isArchived: boolean
        -- Application aggregated status.
        applicationAggregatedStatus: ApplicationAggregatedStatus
        -- Application creation dateTime.
        creationDateTime: string
        -- Application last modified dateTime.
        lastModifiedDateTime: string
        -- Application tags.
        tags: {string : string}
        -- Application wave ID.
        waveID: string
    end

    record UpdateApplicationInput
        -- Application ID.
        applicationID: string
        -- Application name.
        name: string
        -- Application description.
        description: string
        -- Account ID.
        accountID: string
    end

    record UpdateApplicationOutput
        -- Application ID.
        applicationID: string
        -- Application ARN.
        arn: string
        -- Application name.
        name: string
        -- Application description.
        description: string
        -- Application archival status.
        isArchived: boolean
        -- Application aggregated status.
        applicationAggregatedStatus: ApplicationAggregatedStatus
        -- Application creation dateTime.
        creationDateTime: string
        -- Application last modified dateTime.
        lastModifiedDateTime: string
        -- Application tags.
        tags: {string : string}
        -- Application wave ID.
        waveID: string
    end

    -- Connector SSM command config.
    record ConnectorSsmCommandConfig
        -- Connector SSM command config S3 output enabled.
        s3OutputEnabled: boolean
        -- Connector SSM command config output S3 bucket name.
        outputS3BucketName: string
        -- Connector SSM command config CloudWatch output enabled.
        cloudWatchOutputEnabled: boolean
        -- Connector SSM command config CloudWatch log group name.
        cloudWatchLogGroupName: string
    end

    record CreateConnectorInput
        -- Create Connector request name.
        name: string
        -- Create Connector request SSM instance ID.
        ssmInstanceID: string
        -- Create Connector request tags.
        tags: {string : string}
        -- Create Connector request SSM command config.
        ssmCommandConfig: ConnectorSsmCommandConfig
    end

    record CreateConnectorOutput
        -- Connector ID.
        connectorID: string
        -- Connector name.
        name: string
        -- Connector SSM instance ID.
        ssmInstanceID: string
        -- Connector arn.
        arn: string
        -- Connector tags.
        tags: {string : string}
        -- Connector SSM command config.
        ssmCommandConfig: ConnectorSsmCommandConfig
    end

    -- Validate exception field.
    record ValidationExceptionField
        -- Validate exception field name.
        name: string
        -- Validate exception field message.
        message: string
    end

    enum ValidationExceptionReason
        "unknownOperation"
        "cannotParse"
        "fieldValidationFailed"
        "other"
    end

    -- Validate exception.
    record ValidationException
        message: string
        code: string
        -- Validate exception reason.
        reason: string
        -- Validate exception field list.
        fieldList: {ValidationExceptionField}
    end

    record DeleteConnectorInput
        -- Delete Connector request connector ID.
        connectorID: string
    end

    record DeleteConnectorOutput
    end

    -- List Connectors Request Filters.
    record ListConnectorsRequestFilters
        -- List Connectors Request Filters connector IDs.
        connectorIDs: {string}
    end

    record ListConnectorsInput
        -- List Connectors Request filters.
        filters: ListConnectorsRequestFilters
        -- List Connectors Request max results.
        maxResults: number
        -- List Connectors Request next token.
        nextToken: string
    end

    record Connector
        -- Connector ID.
        connectorID: string
        -- Connector name.
        name: string
        -- Connector SSM instance ID.
        ssmInstanceID: string
        -- Connector arn.
        arn: string
        -- Connector tags.
        tags: {string : string}
        -- Connector SSM command config.
        ssmCommandConfig: ConnectorSsmCommandConfig
    end

    record ListConnectorsOutput
        -- List connectors response items.
        items: {Connector}
        -- List connectors response next token.
        nextToken: string
    end

    record UpdateConnectorInput
        -- Update Connector request connector ID.
        connectorID: string
        -- Update Connector request name.
        name: string
        -- Update Connector request SSM command config.
        ssmCommandConfig: ConnectorSsmCommandConfig
    end

    record UpdateConnectorOutput
        -- Connector ID.
        connectorID: string
        -- Connector name.
        name: string
        -- Connector SSM instance ID.
        ssmInstanceID: string
        -- Connector arn.
        arn: string
        -- Connector tags.
        tags: {string : string}
        -- Connector SSM command config.
        ssmCommandConfig: ConnectorSsmCommandConfig
    end

    -- List export errors request.
    record ListExportErrorsInput
        -- List export errors request export id.
        exportID: string
        -- List export errors request max results.
        maxResults: number
        -- List export errors request next token.
        nextToken: string
    end

    -- Export errors data.
    record ExportErrorData
        -- Export errors data raw error.
        rawError: string
    end

    -- Export task error.
    record ExportTaskError
        -- Export task error datetime.
        errorDateTime: string
        -- Export task error data.
        errorData: ExportErrorData
    end

    -- List export errors response.
    record ListExportErrorsOutput
        -- List export errors response items.
        items: {ExportTaskError}
        -- List export errors response next token.
        nextToken: string
    end

    -- List exports request filters.
    record ListExportsRequestFilters
        -- List exports request filters export ids.
        exportIDs: {string}
    end

    -- List export request.
    record ListExportsInput
        filters: ListExportsRequestFilters
        -- List export request max results.
        maxResults: number
        -- List export request next token.
        nextToken: string
    end

    enum ExportStatus
        "PENDING"
        "STARTED"
        "FAILED"
        "SUCCEEDED"
    end

    -- Export task summary.
    record ExportTaskSummary
        -- Export task summary servers count.
        serversCount: number
        -- Export task summary applications count.
        applicationsCount: number
        -- Export task summary waves count.
        wavesCount: number
    end

    -- Export task.
    record ExportTask
        -- Export task id.
        exportID: string
        -- ExportTask arn.
        arn: string
        -- Export task s3 bucket.
        s3Bucket: string
        -- Export task s3 key.
        s3Key: string
        -- Export task s3 bucket owner.
        s3BucketOwner: string
        -- Export task creation datetime.
        creationDateTime: string
        -- Export task end datetime.
        endDateTime: string
        -- Export task status.
        status: string
        -- Export task progress percentage.
        progressPercentage: number
        -- Export task summary.
        summary: ExportTaskSummary
        -- Export task tags.
        tags: {string : string}
    end

    -- List export response.
    record ListExportsOutput
        -- List export response items.
        items: {ExportTask}
        -- List export response next token.
        nextToken: string
    end

    -- Start export request.
    record StartExportInput
        -- Start export request s3 bucket.
        s3Bucket: string
        -- Start export request s3key.
        s3Key: string
        -- Start export request s3 bucket owner.
        s3BucketOwner: string
        -- Start export request tags.
        tags: {string : string}
    end

    -- Start export response.
    record StartExportOutput
        -- Start export response export task.
        exportTask: ExportTask
    end

    -- List import errors request.
    record ListImportErrorsInput
        -- List import errors request import id.
        importID: string
        -- List import errors request max results.
        maxResults: number
        -- List import errors request next token.
        nextToken: string
    end

    -- Import error data.
    record ImportErrorData
        -- Import error data source server ID.
        sourceServerID: string
        -- Import error data application ID.
        applicationID: string
        -- Import error data wave id.
        waveID: string
        -- Import error data ec2 LaunchTemplate ID.
        ec2LaunchTemplateID: string
        -- Import error data row number.
        rowNumber: number
        -- Import error data raw error.
        rawError: string
        -- Import error data source account ID.
        accountID: string
    end

    enum ImportErrorType
        "VALIDATION_ERROR"
        "PROCESSING_ERROR"
    end

    -- Import task error.
    record ImportTaskError
        -- Import task error datetime.
        errorDateTime: string
        -- Import task error type.
        errorType: string
        -- Import task error data.
        errorData: ImportErrorData
    end

    -- List imports errors response.
    record ListImportErrorsOutput
        -- List imports errors response items.
        items: {ImportTaskError}
        -- List imports errors response next token.
        nextToken: string
    end

    -- List imports request filters.
    record ListImportsRequestFilters
        -- List imports request filters import IDs.
        importIDs: {string}
    end

    -- List imports request.
    record ListImportsInput
        -- List imports request filters.
        filters: ListImportsRequestFilters
        -- List imports request max results.
        maxResults: number
        -- List imports request next token.
        nextToken: string
    end

    -- S3 bucket source.
    record S3BucketSource
        -- S3 bucket source s3 bucket.
        s3Bucket: string
        -- S3 bucket source s3 key.
        s3Key: string
        -- S3 bucket source s3 bucket owner.
        s3BucketOwner: string
    end

    enum ImportStatus
        "PENDING"
        "STARTED"
        "FAILED"
        "SUCCEEDED"
    end

    -- Import task summary applications.
    record ImportTaskSummaryApplications
        -- Import task summary applications created count.
        createdCount: number
        -- Import task summary applications modified count.
        modifiedCount: number
    end

    -- Import task summary servers.
    record ImportTaskSummaryServers
        -- Import task summary servers created count.
        createdCount: number
        -- Import task summary servers modified count.
        modifiedCount: number
    end

    -- Import task summary waves.
    record ImportTaskSummaryWaves
        -- Import task summary waves created count.
        createdCount: number
        -- Import task summary waves modified count.
        modifiedCount: number
    end

    -- Import task summary.
    record ImportTaskSummary
        -- Import task summary waves.
        waves: ImportTaskSummaryWaves
        -- Import task summary applications.
        applications: ImportTaskSummaryApplications
        -- Import task summary servers.
        servers: ImportTaskSummaryServers
    end

    -- Import task.
    record ImportTask
        -- Import task id.
        importID: string
        -- ImportTask arn.
        arn: string
        -- Import task s3 bucket source.
        s3BucketSource: S3BucketSource
        -- Import task creation datetime.
        creationDateTime: string
        -- Import task end datetime.
        endDateTime: string
        -- Import task status.
        status: string
        -- Import task progress percentage.
        progressPercentage: number
        -- Import task summary.
        summary: ImportTaskSummary
        -- Import task tags.
        tags: {string : string}
    end

    -- List import response.
    record ListImportsOutput
        -- List import response items.
        items: {ImportTask}
        -- List import response next token.
        nextToken: string
    end

    -- Start import request.
    record StartImportInput
        -- Start import request client token.
        clientToken: string
        -- Start import request s3 bucket source.
        s3BucketSource: S3BucketSource
        -- Start import request tags.
        tags: {string : string}
    end

    -- Start import response.
    record StartImportOutput
        -- Start import response import task.
        importTask: ImportTask
    end

    record InitializeServiceInput
    end

    record InitializeServiceOutput
    end

    record DeleteJobInput
        -- Request to delete Job from service by Job ID.
        jobID: string
        -- Request to delete Job from service by Account ID.
        accountID: string
    end

    record DeleteJobOutput
    end

    record DescribeJobLogItemsInput
        -- Request to describe Job log job ID.
        jobID: string
        -- Request to describe Job log item maximum results.
        maxResults: number
        -- Request to describe Job log next token.
        nextToken: string
        -- Request to describe Job log Account ID.
        accountID: string
    end

    enum JobLogEvent
        "JOB_START"
        "SERVER_SKIPPED"
        "CLEANUP_START"
        "CLEANUP_END"
        "CLEANUP_FAIL"
        "SNAPSHOT_START"
        "SNAPSHOT_END"
        "SNAPSHOT_FAIL"
        "USING_PREVIOUS_SNAPSHOT"
        "CONVERSION_START"
        "CONVERSION_END"
        "CONVERSION_FAIL"
        "LAUNCH_START"
        "LAUNCH_FAILED"
        "JOB_CANCEL"
        "JOB_END"
    end

    -- Job log data
    record JobLogEventData
        -- Job Event Source Server ID.
        sourceServerID: string
        -- Job Event conversion Server ID.
        conversionServerID: string
        -- Job Event Target instance ID.
        targetInstanceID: string
        -- Job error.
        rawError: string
        -- Retries for this operation.
        attemptCount: number
        -- The maximum number of retries that will be attempted if this operation failed.
        maxAttemptsCount: number
    end

    -- Job log.
    record JobLog
        -- Job log event date and time.
        logDateTime: string
        -- Job log event.
        event: string
        -- Job event data
        eventData: JobLogEventData
    end

    record DescribeJobLogItemsOutput
        -- Request to describe Job log response items.
        items: {JobLog}
        -- Request to describe Job log response next token.
        nextToken: string
    end

    -- Request to describe Job log filters.
    record DescribeJobsRequestFilters
        -- Request to describe Job log filters by job ID.
        jobIDs: {string}
        -- Request to describe Job log filters by date.
        fromDate: string
        -- Request to describe job log items by last date.
        toDate: string
    end

    record DescribeJobsInput
        -- Request to describe Job log filters.
        filters: DescribeJobsRequestFilters
        -- Request to describe job log items by max results.
        maxResults: number
        -- Request to describe job log items by next token.
        nextToken: string
        -- Request to describe job log items by Account ID.
        accountID: string
    end

    enum InitiatedBy
        "START_TEST"
        "START_CUTOVER"
        "DIAGNOSTIC"
        "TERMINATE"
    end

    enum LaunchStatus
        "PENDING"
        "IN_PROGRESS"
        "LAUNCHED"
        "FAILED"
        "TERMINATED"
    end

    enum PostLaunchActionExecutionStatus
        "IN_PROGRESS"
        "SUCCESS"
        "FAILED"
    end

    -- AWS Systems Manager Document external parameter.
    record SsmExternalParameter
        -- AWS Systems Manager Document external parameters dynamic path.
        dynamicPath: string
    end

    enum SsmParameterStoreParameterType
        "STRING"
        "SECURE_STRING"
    end

    -- AWS Systems Manager Parameter Store parameter.
    record SsmParameterStoreParameter
        -- AWS Systems Manager Parameter Store parameter type.
        parameterType: string
        -- AWS Systems Manager Parameter Store parameter name.
        parameterName: string
    end

    -- AWS Systems Manager Document.
    record SsmDocument
        -- User-friendly name for the AWS Systems Manager Document.
        actionName: string
        -- AWS Systems Manager Document name or full ARN.
        ssmDocumentName: string
        -- AWS Systems Manager Document timeout seconds.
        timeoutSeconds: number
        -- If true, Cutover will not be enabled if the document has failed.
        mustSucceedForCutover: boolean
        -- AWS Systems Manager Document parameters.
        parameters: {string : {SsmParameterStoreParameter}}
        -- AWS Systems Manager Document external parameters.
        externalParameters: {string : SsmExternalParameter}
    end

    enum SsmDocumentType
        "AUTOMATION"
        "COMMAND"
    end

    -- Launch Status of the Job Post Launch Actions.
    record JobPostLaunchActionsLaunchStatus
        -- AWS Systems Manager's Document of the Job Post Launch Actions.
        ssmDocument: SsmDocument
        -- AWS Systems Manager Document type.
        ssmDocumentType: string
        -- AWS Systems Manager Document's execution ID of the Job Post Launch Actions.
        executionID: string
        -- AWS Systems Manager Document's execution status.
        executionStatus: string
        -- AWS Systems Manager Document's failure reason.
        failureReason: string
    end

    -- Status of the Post Launch Actions running on the Test or Cutover instance.
    record PostLaunchActionsStatus
        -- Time where the AWS Systems Manager was detected as running on the Test or Cutover instance.
        ssmAgentDiscoveryDatetime: string
        -- List of Post Launch Action status.
        postLaunchActionsLaunchStatusList: {JobPostLaunchActionsLaunchStatus}
    end

    -- Server participating in Job.
    record ParticipatingServer
        -- Participating server Source Server ID.
        sourceServerID: string
        -- Participating server launch status.
        launchStatus: string
        -- Participating server's launched ec2 instance ID.
        launchedEc2InstanceID: string
        -- Participating server's Post Launch Actions Status.
        postLaunchActionsStatus: PostLaunchActionsStatus
    end

    enum JobStatus
        "PENDING"
        "STARTED"
        "COMPLETED"
    end

    enum JobType
        "LAUNCH"
        "TERMINATE"
    end

    -- Job.
    record Job
        -- Job ID.
        jobID: string
        -- the ARN of the specific Job.
        arn: string
        -- Job initiated by field.
        initiatedBy: string
        -- Job creation time.
        creationDateTime: string
        -- Job end time.
        endDateTime: string
        -- Job status.
        status: string
        -- Servers participating in a specific Job.
        participatingServers: {ParticipatingServer}
        -- Tags associated with specific Job.
        tags: {string : string}
    end

    record DescribeJobsOutput
        -- Request to describe Job log items.
        items: {Job}
        -- Request to describe Job response by next token.
        nextToken: string
    end

    enum BootMode
        "LEGACY_BIOS"
        "UEFI"
        "USE_SOURCE"
    end

    enum VolumeType
        "io1"
        "io2"
        "gp3"
        "gp2"
        "st1"
        "sc1"
        "standard"
    end

    -- Launch template disk configuration.
    record LaunchTemplateDiskConf
        -- Launch template disk volume type configuration.
        volumeType: string
        -- Launch template disk iops configuration.
        iops: number
        -- Launch template disk throughput configuration.
        throughput: number
    end

    enum LaunchDisposition
        "STOPPED"
        "STARTED"
    end

    -- Configure Licensing.
    record Licensing
        -- Configure BYOL OS licensing.
        osByol: boolean
    end

    enum PostLaunchActionsDeploymentType
        "TEST_AND_CUTOVER"
        "CUTOVER_ONLY"
        "TEST_ONLY"
    end

    -- Post Launch Actions to be executed on the Test or Cutover instance.
    record PostLaunchActions
        -- Deployment type in which AWS Systems Manager Documents will be executed.
        deployment: string
        -- AWS Systems Manager Command's logs S3 log bucket.
        s3LogBucket: string
        -- AWS Systems Manager Command's logs S3 output key prefix.
        s3OutputKeyPrefix: string
        -- AWS Systems Manager Command's CloudWatch log group name.
        cloudWatchLogGroupName: string
        -- AWS Systems Manager Documents.
        ssmDocuments: {SsmDocument}
    end

    enum TargetInstanceTypeRightSizingMethod
        "NONE"
        "BASIC"
    end

    record CreateLaunchConfigurationTemplateInput
        -- Launch configuration template post launch actions.
        postLaunchActions: PostLaunchActions
        -- Enable map auto tagging.
        enableMapAutoTagging: boolean
        -- Launch configuration template map auto tagging MPE ID.
        mapAutoTaggingMpeID: string
        -- Request to associate tags during creation of a Launch Configuration Template.
        tags: {string : string}
        -- Launch disposition.
        launchDisposition: string
        -- Target instance type right-sizing method.
        targetInstanceTypeRightSizingMethod: string
        -- Copy private Ip.
        copyPrivateIp: boolean
        -- Associate public Ip address.
        associatePublicIpAddress: boolean
        -- Copy tags.
        copyTags: boolean
        licensing: Licensing
        -- Launch configuration template boot mode.
        bootMode: string
        -- Small volume maximum size.
        smallVolumeMaxSize: number
        -- Small volume config.
        smallVolumeConf: LaunchTemplateDiskConf
        -- Large volume config.
        largeVolumeConf: LaunchTemplateDiskConf
        -- Enable parameters encryption.
        enableParametersEncryption: boolean
        -- Parameters encryption key.
        parametersEncryptionKey: string
    end

    record CreateLaunchConfigurationTemplateOutput
        -- ID of the Launch Configuration Template.
        launchConfigurationTemplateID: string
        -- ARN of the Launch Configuration Template.
        arn: string
        -- Post Launch Actions of the Launch Configuration Template.
        postLaunchActions: PostLaunchActions
        -- Enable map auto tagging.
        enableMapAutoTagging: boolean
        -- Launch configuration template map auto tagging MPE ID.
        mapAutoTaggingMpeID: string
        -- Tags of the Launch Configuration Template.
        tags: {string : string}
        -- EC2 launch template ID.
        ec2LaunchTemplateID: string
        -- Launch disposition.
        launchDisposition: string
        -- Target instance type right-sizing method.
        targetInstanceTypeRightSizingMethod: string
        -- Copy private Ip.
        copyPrivateIp: boolean
        -- Associate public Ip address.
        associatePublicIpAddress: boolean
        -- Copy tags.
        copyTags: boolean
        licensing: Licensing
        -- Launch configuration template boot mode.
        bootMode: string
        -- Small volume maximum size.
        smallVolumeMaxSize: number
        -- Small volume config.
        smallVolumeConf: LaunchTemplateDiskConf
        -- Large volume config.
        largeVolumeConf: LaunchTemplateDiskConf
        -- Enable parameters encryption.
        enableParametersEncryption: boolean
        -- Parameters encryption key.
        parametersEncryptionKey: string
    end

    record DeleteLaunchConfigurationTemplateInput
        -- ID of resource to be deleted.
        launchConfigurationTemplateID: string
    end

    record DeleteLaunchConfigurationTemplateOutput
    end

    record DescribeLaunchConfigurationTemplatesInput
        -- Request to filter Launch Configuration Templates list by Launch Configuration Template ID.
        launchConfigurationTemplateIDs: {string}
        -- Maximum results to be returned in DescribeLaunchConfigurationTemplates.
        maxResults: number
        -- Next pagination token returned from DescribeLaunchConfigurationTemplates.
        nextToken: string
    end

    record LaunchConfigurationTemplate
        -- ID of the Launch Configuration Template.
        launchConfigurationTemplateID: string
        -- ARN of the Launch Configuration Template.
        arn: string
        -- Post Launch Actions of the Launch Configuration Template.
        postLaunchActions: PostLaunchActions
        -- Enable map auto tagging.
        enableMapAutoTagging: boolean
        -- Launch configuration template map auto tagging MPE ID.
        mapAutoTaggingMpeID: string
        -- Tags of the Launch Configuration Template.
        tags: {string : string}
        -- EC2 launch template ID.
        ec2LaunchTemplateID: string
        -- Launch disposition.
        launchDisposition: string
        -- Target instance type right-sizing method.
        targetInstanceTypeRightSizingMethod: string
        -- Copy private Ip.
        copyPrivateIp: boolean
        -- Associate public Ip address.
        associatePublicIpAddress: boolean
        -- Copy tags.
        copyTags: boolean
        licensing: Licensing
        -- Launch configuration template boot mode.
        bootMode: string
        -- Small volume maximum size.
        smallVolumeMaxSize: number
        -- Small volume config.
        smallVolumeConf: LaunchTemplateDiskConf
        -- Large volume config.
        largeVolumeConf: LaunchTemplateDiskConf
        -- Enable parameters encryption.
        enableParametersEncryption: boolean
        -- Parameters encryption key.
        parametersEncryptionKey: string
    end

    record DescribeLaunchConfigurationTemplatesOutput
        -- List of items returned by DescribeLaunchConfigurationTemplates.
        items: {LaunchConfigurationTemplate}
        -- Next pagination token returned from DescribeLaunchConfigurationTemplates.
        nextToken: string
    end

    -- Template post migration custom action filters.
    record TemplateActionsRequestFilters
        -- Action IDs to filter template post migration custom actions by.
        actionIDs: {string}
    end

    record ListTemplateActionsInput
        -- Launch configuration template ID.
        launchConfigurationTemplateID: string
        -- Filters to apply when listing template post migration custom actions.
        filters: TemplateActionsRequestFilters
        -- Maximum amount of items to return when listing template post migration custom actions.
        maxResults: number
        -- Next token to use when listing template post migration custom actions.
        nextToken: string
    end

    record TemplateActionDocument
        -- Template post migration custom action ID.
        actionID: string
        -- Template post migration custom action name.
        actionName: string
        -- Template post migration custom action document identifier.
        documentIdentifier: string
        -- Template post migration custom action order.
        order: number
        -- Template post migration custom action document version.
        documentVersion: string
        -- Template post migration custom action active status.
        active: boolean
        -- Template post migration custom action timeout in seconds.
        timeoutSeconds: number
        -- Template post migration custom action must succeed for cutover.
        mustSucceedForCutover: boolean
        -- Template post migration custom action parameters.
        parameters: {string : {SsmParameterStoreParameter}}
        -- Operating system eligible for this template post migration custom action.
        operatingSystem: string
        -- Template post migration custom action external parameters.
        externalParameters: {string : SsmExternalParameter}
        -- Template post migration custom action description.
        description: string
        -- Template post migration custom action category.
        category: string
    end

    record ListTemplateActionsOutput
        -- List of template post migration custom actions.
        items: {TemplateActionDocument}
        -- Next token returned when listing template post migration custom actions.
        nextToken: string
    end

    record PutTemplateActionInput
        -- Launch configuration template ID.
        launchConfigurationTemplateID: string
        -- Template post migration custom action name.
        actionName: string
        -- Template post migration custom action document identifier.
        documentIdentifier: string
        -- Template post migration custom action order.
        order: number
        -- Template post migration custom action ID.
        actionID: string
        -- Template post migration custom action document version.
        documentVersion: string
        -- Template post migration custom action active status.
        active: boolean
        -- Template post migration custom action timeout in seconds.
        timeoutSeconds: number
        -- Template post migration custom action must succeed for cutover.
        mustSucceedForCutover: boolean
        -- Template post migration custom action parameters.
        parameters: {string : {SsmParameterStoreParameter}}
        -- Operating system eligible for this template post migration custom action.
        operatingSystem: string
        -- Template post migration custom action external parameters.
        externalParameters: {string : SsmExternalParameter}
        -- Template post migration custom action description.
        description: string
        -- Template post migration custom action category.
        category: string
    end

    record PutTemplateActionOutput
        -- Template post migration custom action ID.
        actionID: string
        -- Template post migration custom action name.
        actionName: string
        -- Template post migration custom action document identifier.
        documentIdentifier: string
        -- Template post migration custom action order.
        order: number
        -- Template post migration custom action document version.
        documentVersion: string
        -- Template post migration custom action active status.
        active: boolean
        -- Template post migration custom action timeout in seconds.
        timeoutSeconds: number
        -- Template post migration custom action must succeed for cutover.
        mustSucceedForCutover: boolean
        -- Template post migration custom action parameters.
        parameters: {string : {SsmParameterStoreParameter}}
        -- Operating system eligible for this template post migration custom action.
        operatingSystem: string
        -- Template post migration custom action external parameters.
        externalParameters: {string : SsmExternalParameter}
        -- Template post migration custom action description.
        description: string
        -- Template post migration custom action category.
        category: string
    end

    record RemoveTemplateActionInput
        -- Launch configuration template ID of the post migration custom action to remove.
        launchConfigurationTemplateID: string
        -- Template post migration custom action ID to remove.
        actionID: string
    end

    record RemoveTemplateActionOutput
    end

    record UpdateLaunchConfigurationTemplateInput
        -- Launch Configuration Template ID.
        launchConfigurationTemplateID: string
        -- Post Launch Action to execute on the Test or Cutover instance.
        postLaunchActions: PostLaunchActions
        -- Enable map auto tagging.
        enableMapAutoTagging: boolean
        -- Launch configuration template map auto tagging MPE ID.
        mapAutoTaggingMpeID: string
        -- Launch disposition.
        launchDisposition: string
        -- Target instance type right-sizing method.
        targetInstanceTypeRightSizingMethod: string
        -- Copy private Ip.
        copyPrivateIp: boolean
        -- Associate public Ip address.
        associatePublicIpAddress: boolean
        -- Copy tags.
        copyTags: boolean
        licensing: Licensing
        -- Launch configuration template boot mode.
        bootMode: string
        -- Small volume maximum size.
        smallVolumeMaxSize: number
        -- Small volume config.
        smallVolumeConf: LaunchTemplateDiskConf
        -- Large volume config.
        largeVolumeConf: LaunchTemplateDiskConf
        -- Enable parameters encryption.
        enableParametersEncryption: boolean
        -- Parameters encryption key.
        parametersEncryptionKey: string
    end

    record UpdateLaunchConfigurationTemplateOutput
        -- ID of the Launch Configuration Template.
        launchConfigurationTemplateID: string
        -- ARN of the Launch Configuration Template.
        arn: string
        -- Post Launch Actions of the Launch Configuration Template.
        postLaunchActions: PostLaunchActions
        -- Enable map auto tagging.
        enableMapAutoTagging: boolean
        -- Launch configuration template map auto tagging MPE ID.
        mapAutoTaggingMpeID: string
        -- Tags of the Launch Configuration Template.
        tags: {string : string}
        -- EC2 launch template ID.
        ec2LaunchTemplateID: string
        -- Launch disposition.
        launchDisposition: string
        -- Target instance type right-sizing method.
        targetInstanceTypeRightSizingMethod: string
        -- Copy private Ip.
        copyPrivateIp: boolean
        -- Associate public Ip address.
        associatePublicIpAddress: boolean
        -- Copy tags.
        copyTags: boolean
        licensing: Licensing
        -- Launch configuration template boot mode.
        bootMode: string
        -- Small volume maximum size.
        smallVolumeMaxSize: number
        -- Small volume config.
        smallVolumeConf: LaunchTemplateDiskConf
        -- Large volume config.
        largeVolumeConf: LaunchTemplateDiskConf
        -- Enable parameters encryption.
        enableParametersEncryption: boolean
        -- Parameters encryption key.
        parametersEncryptionKey: string
    end

    -- Filters for listing import file enrichment jobs.
    record ListImportFileEnrichmentsFilters
        -- A list of job IDs to filter by.
        jobIDs: {string}
    end

    record ListImportFileEnrichmentsInput
        -- Filters to apply when listing import file enrichment jobs.
        filters: ListImportFileEnrichmentsFilters
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    enum EncryptionAlgorithm
        "SHA256"
    end

    -- A checksum structure used to verify data integrity.
    record Checksum
        -- The encryption algorithm used to generate the checksum.
        encryptionAlgorithm: string
        -- The hash value of the checksum.
        hash: string
    end

    -- S3 configuration for storing the enriched import file.
    record EnrichmentTargetS3Configuration
        -- The name of the S3 bucket where the enriched import file will be stored.
        s3Bucket: string
        -- The AWS account ID of the target S3 bucket owner.
        s3BucketOwner: string
        -- The S3 key (path) where the enriched import file will be stored.
        s3Key: string
    end

    enum ImportFileEnrichmentStatus
        "PENDING"
        "STARTED"
        "FAILED"
        "SUCCEEDED"
        "SUCCEEDED_WITH_WARNINGS"
    end

    -- Details about an import file enrichment job.
    record ImportFileEnrichment
        -- The unique identifier of the import file enrichment job.
        jobID: string
        -- The timestamp when the enrichment job was created.
        createdAt: number
        -- The timestamp when the enrichment job completed or failed.
        endedAt: number
        -- The current status of the import file enrichment job.
        status: string
        -- Detailed status information about the enrichment job.
        statusDetails: string
        -- The checksum of the enriched file for integrity verification.
        checksum: Checksum
        -- The target S3 configuration for the enriched import file.
        s3BucketTarget: EnrichmentTargetS3Configuration
    end

    record ListImportFileEnrichmentsOutput
        -- A list of import file enrichment jobs.
        items: {ImportFileEnrichment}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- List managed accounts request.
    record ListManagedAccountsInput
        -- List managed accounts request max results.
        maxResults: number
        -- List managed accounts request next token.
        nextToken: string
    end

    -- Managed account.
    record ManagedAccount
        -- Managed account, account ID.
        accountId: string
    end

    -- List managed accounts response.
    record ListManagedAccountsOutput
        -- List managed accounts response items.
        items: {ManagedAccount}
        -- List managed accounts response next token.
        nextToken: string
    end

    -- The server encountered an unexpected condition that prevented it from fulfilling the request.
    record InternalServerException
        message: string
        -- The server encountered an unexpected condition that prevented it from fulfilling the request. The request will be retried again after x seconds.
        retryAfterSeconds: number
    end

    record ListTagsForResourceInput
        -- List tags for resource request by ARN.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- List tags for resource response.
        tags: {string : string}
    end

    -- Reached throttling quota exception.
    record ThrottlingException
        message: string
        -- Reached throttling quota exception service code.
        serviceCode: string
        -- Reached throttling quota exception.
        quotaCode: string
        -- Reached throttling quota exception will retry after x seconds.
        retryAfterSeconds: string
    end

    -- Maps a source CIDR range to the corresponding target CIDR range to use in the target network.
    record CidrMapping
        -- The original CIDR range in the source network.
        originalCidr: string
        -- The updated CIDR range to use in the target network.
        updatedCidr: string
    end

    enum SourceEnvironment
        "NSX"
        "VSPHERE"
        "FORTIGATE_FIREWALL"
        "PALO_ALTO_FIREWALL"
        "CISCO_ACI"
        "LOGICAL_MODEL"
        "MODELIZE_IT"
        "AWS_DISCOVERY_COLLECTOR"
    end

    -- S3 configuration for source network data.
    record SourceS3Configuration
        -- The name of the S3 bucket containing source data.
        s3Bucket: string
        -- The AWS account ID of the S3 bucket owner.
        s3BucketOwner: string
        -- The S3 key (path) for the source data.
        s3Key: string
    end

    -- Configuration for a migration source environment.
    record SourceConfiguration
        -- The source environment type.
        sourceEnvironment: string
        -- The S3 configuration for the source data.
        sourceS3Configuration: SourceS3Configuration
    end

    enum TargetDeployment
        "SINGLE_ACCOUNT"
        "MULTI_ACCOUNT"
    end

    enum TargetNetworkTopology
        "ISOLATED_VPC"
        "HUB_AND_SPOKE"
    end

    -- Configuration for the target network topology and addressing.
    record TargetNetwork
        -- The network topology type for the target environment.
        topology: string
        -- The CIDR block for inbound traffic in the target network.
        inboundCidr: string
        -- The CIDR block for outbound traffic in the target network.
        outboundCidr: string
        -- The CIDR block for inspection traffic in the target network.
        inspectionCidr: string
    end

    -- S3 configuration for storing target network artifacts.
    record TargetS3Configuration
        -- The name of the S3 bucket for target artifacts.
        s3Bucket: string
        -- The AWS account ID of the S3 bucket owner.
        s3BucketOwner: string
    end

    enum VpcProvisioningStrategy
        "CREATE_NEW"
        "USE_EXISTING"
    end

    record CreateNetworkMigrationDefinitionInput
        -- The name of the network migration definition.
        name: string
        -- A description of the network migration definition.
        description: string
        -- A list of source configurations for the network migration.
        sourceConfigurations: {SourceConfiguration}
        -- The S3 configuration for storing the target network artifacts.
        targetS3Configuration: TargetS3Configuration
        -- The target network configuration including topology and CIDR ranges.
        targetNetwork: TargetNetwork
        -- The target deployment configuration for the migrated network.
        targetDeployment: string
        -- Specifies whether to create new target VPCs or use existing ones. Set to CREATE_NEW to provision new target VPCs as part of the migration, or USE_EXISTING to migrate into existing VPCs in the target account.
        vpcProvisioningStrategy: string
        -- A list of CIDR mappings that map original source CIDR ranges to updated target CIDR ranges. CIDR mappings can be provided only when vpcProvisioningStrategy is set to USE_EXISTING.
        cidrMappings: {CidrMapping}
        -- Tags to assign to the network migration definition.
        tags: {string : string}
        -- Scope tags for the network migration definition to control access and organization.
        scopeTags: {string : string}
    end

    record CreateNetworkMigrationDefinitionOutput
        -- The Amazon Resource Name (ARN) of the network migration definition.
        arn: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The name of the network migration definition.
        name: string
        -- A description of the network migration definition.
        description: string
        -- A list of source configurations for the network migration.
        sourceConfigurations: {SourceConfiguration}
        -- The S3 configuration for storing the target network artifacts.
        targetS3Configuration: TargetS3Configuration
        -- The target network configuration including topology and CIDR ranges.
        targetNetwork: TargetNetwork
        -- The target deployment configuration for the migrated network.
        targetDeployment: string
        -- Indicates whether the migration creates new target VPCs or uses existing ones. CREATE_NEW provisions new target VPCs; USE_EXISTING migrates into existing VPCs in the target account.
        vpcProvisioningStrategy: string
        -- A list of CIDR mappings that map original source CIDR ranges to updated target CIDR ranges. CIDR mappings apply only when vpcProvisioningStrategy is set to USE_EXISTING.
        cidrMappings: {CidrMapping}
        -- The timestamp when the network migration definition was created.
        createdAt: number
        -- The timestamp when the network migration definition was last updated.
        updatedAt: number
        -- Tags assigned to the network migration definition.
        tags: {string : string}
        -- Scope tags for the network migration definition.
        scopeTags: {string : string}
    end

    record DeleteNetworkMigrationDefinitionInput
        -- The unique identifier of the network migration definition to delete.
        networkMigrationDefinitionID: string
    end

    record DeleteNetworkMigrationDefinitionOutput
    end

    record GetNetworkMigrationDefinitionInput
        -- The unique identifier of the network migration definition to retrieve.
        networkMigrationDefinitionID: string
    end

    record GetNetworkMigrationDefinitionOutput
        -- The Amazon Resource Name (ARN) of the network migration definition.
        arn: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The name of the network migration definition.
        name: string
        -- A description of the network migration definition.
        description: string
        -- A list of source configurations for the network migration.
        sourceConfigurations: {SourceConfiguration}
        -- The S3 configuration for storing the target network artifacts.
        targetS3Configuration: TargetS3Configuration
        -- The target network configuration including topology and CIDR ranges.
        targetNetwork: TargetNetwork
        -- The target deployment configuration for the migrated network.
        targetDeployment: string
        -- Indicates whether the migration creates new target VPCs or uses existing ones. CREATE_NEW provisions new target VPCs; USE_EXISTING migrates into existing VPCs in the target account.
        vpcProvisioningStrategy: string
        -- A list of CIDR mappings that map original source CIDR ranges to updated target CIDR ranges. CIDR mappings apply only when vpcProvisioningStrategy is set to USE_EXISTING.
        cidrMappings: {CidrMapping}
        -- The timestamp when the network migration definition was created.
        createdAt: number
        -- The timestamp when the network migration definition was last updated.
        updatedAt: number
        -- Tags assigned to the network migration definition.
        tags: {string : string}
        -- Scope tags for the network migration definition.
        scopeTags: {string : string}
    end

    record GetNetworkMigrationMapperSegmentConstructInput
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the mapper segment.
        segmentID: string
        -- The unique identifier of the construct within the segment.
        constructID: string
    end

    -- Represents a construct within a mapper segment. A construct is an individual infrastructure component such as a VPC, subnet, security group, route table, or other network resource that is part of the network migration.
    record NetworkMigrationMapperSegmentConstruct
        -- The unique identifier of the construct.
        constructID: string
        -- The type of the construct, such as VPC, subnet, security group, or route table.
        constructType: string
        -- The name of the construct.
        name: string
        -- A description of the construct.
        description: string
        -- The logical identifier for the construct in the infrastructure code.
        logicalID: string
        -- Whether this construct is excluded from the migration.
        excluded: boolean
        -- The timestamp when the construct was created.
        createdAt: number
        -- The timestamp when the construct was last updated.
        updatedAt: number
        -- The properties and configuration of the construct.
        properties: {string : string}
    end

    record GetNetworkMigrationMapperSegmentConstructOutput
        -- The construct metadata including type, name, and configuration.
        construct: NetworkMigrationMapperSegmentConstruct
    end

    -- Filters for listing network migration analysis jobs.
    record ListNetworkMigrationAnalysesFilters
        -- A list of job IDs to filter by.
        jobIDs: {string}
    end

    record ListNetworkMigrationAnalysesInput
        -- The unique identifier of the network migration execution to list analyses for.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- Filters to apply when listing analysis jobs.
        filters: ListNetworkMigrationAnalysesFilters
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    enum NetworkMigrationJobStatus
        "PENDING"
        "STARTED"
        "SUCCEEDED"
        "FAILED"
    end

    -- Details about a network migration analysis job.
    record NetworkMigrationAnalysisJobDetails
        -- The unique identifier of the analysis job.
        jobID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The timestamp when the job was created.
        createdAt: number
        -- The timestamp when the job completed or failed.
        endedAt: number
        -- The current status of the analysis job.
        status: string
        -- Detailed status information about the job.
        statusDetails: string
    end

    record ListNetworkMigrationAnalysesOutput
        -- A list of network migration analysis job details.
        items: {NetworkMigrationAnalysisJobDetails}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- Filters for listing network migration analysis results.
    record ListNetworkMigrationAnalysisResultsFilters
        -- A list of VPC IDs to filter results by.
        vpcIDs: {string}
    end

    record ListNetworkMigrationAnalysisResultsInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- Filters to apply when listing analysis results, such as VPC IDs.
        filters: ListNetworkMigrationAnalysisResultsFilters
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    -- The source resource information for an analysis result.
    record NetworkMigrationAnalysisResultSource
        -- The VPC ID of the source resource.
        vpcID: string
        -- The subnet ID of the source resource.
        subnetID: string
    end

    enum NetworkMigrationAnalysisResultStatus
        "PENDING"
        "STARTED"
        "SUCCEEDED"
        "FAILED"
    end

    -- The target resource information for an analysis result.
    record NetworkMigrationAnalysisResultTarget
        -- The VPC ID of the target resource.
        vpcID: string
        -- The subnet ID of the target resource.
        subnetID: string
    end

    -- The result of a network migration analysis operation.
    record NetworkMigrationAnalysisResult
        -- The unique identifier of the analysis job that generated this result.
        jobID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The type of analyzer that generated this result.
        analyzerType: string
        -- The source resource that was analyzed.
        source: NetworkMigrationAnalysisResultSource
        -- The target resource in the analysis.
        target: NetworkMigrationAnalysisResultTarget
        -- The status of the analysis result.
        status: string
        -- The detailed analysis findings and recommendations.
        analysisResult: string
    end

    record ListNetworkMigrationAnalysisResultsOutput
        -- A list of network migration analysis results.
        items: {NetworkMigrationAnalysisResult}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- Filters for listing code generation jobs.
    record ListNetworkMigrationCodeGenerationsFilters
        -- A list of job IDs to filter by.
        jobIDs: {string}
    end

    record ListNetworkMigrationCodeGenerationsInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- Filters to apply when listing code generation jobs.
        filters: ListNetworkMigrationCodeGenerationsFilters
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    enum CodeGenerationOutputFormatType
        "CDK_L1"
        "CDK_L2"
        "TERRAFORM"
        "LZA"
    end

    enum CodeGenerationOutputFormatStatus
        "SUCCEEDED"
        "FAILED"
    end

    -- Status details for a specific code generation output format.
    record CodeGenerationOutputFormatStatusDetails
        -- The status of the code generation for this output format.
        status: string
        -- A list of detailed status information for the code generation.
        statusDetailList: string
    end

    -- Details about a network migration code generation job.
    record NetworkMigrationCodeGenerationJobDetails
        -- The unique identifier of the code generation job.
        jobID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The timestamp when the job was created.
        createdAt: number
        -- The timestamp when the job completed or failed.
        endedAt: number
        -- The current status of the code generation job.
        status: string
        -- Detailed status information about the job.
        statusDetails: string
        -- A map of output format types to their status details.
        codeGenerationOutputFormatStatusDetailsMap: {string : CodeGenerationOutputFormatStatusDetails}
    end

    record ListNetworkMigrationCodeGenerationsOutput
        -- A list of network migration code generation job details.
        items: {NetworkMigrationCodeGenerationJobDetails}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- Filters for listing code generation segments.
    record ListNetworkMigrationCodeGenerationSegmentsFilters
        -- A list of segment IDs to filter by.
        segmentIDs: {string}
    end

    record ListNetworkMigrationCodeGenerationSegmentsInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- Filters to apply when listing code generation segments.
        filters: ListNetworkMigrationCodeGenerationSegmentsFilters
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    enum NetworkMigrationCodeGenerationArtifactSubType
        "APPLICATION"
        "NESTED_STACK"
        "STACK"
    end

    enum NetworkMigrationCodeGenerationArtifactType
        "CDK_TYPESCRIPT"
        "CLOUDFORMATION_TEMPLATE"
        "CDKTF_TYPESCRIPT"
        "TERRAFORM_CONFIGURATION"
        "CDK_L2_TYPESCRIPT"
    end

    -- Configuration for an S3 location.
    record S3Configuration
        -- The name of the S3 bucket.
        s3Bucket: string
        -- The AWS account ID of the S3 bucket owner.
        s3BucketOwner: string
        -- The S3 key (path) for the object.
        s3Key: string
    end

    -- An artifact generated during code generation, such as a CloudFormation template or Terraform file.
    record NetworkMigrationCodeGenerationArtifact
        -- The unique identifier of the artifact.
        artifactID: string
        -- The type of the generated artifact.
        artifactType: string
        -- The sub-type of the artifact for further classification.
        artifactSubType: string
        -- The logical identifier for the artifact.
        logicalID: string
        -- The S3 location where the artifact is stored.
        outputS3Configuration: S3Configuration
        -- The checksum of the artifact for integrity verification.
        checksum: Checksum
        -- The timestamp when the artifact was created.
        createdAt: number
    end

    enum NetworkMigrationCodeGenerationSegmentType
        "WORKLOAD"
        "APPLIANCE"
        "NETWORK"
    end

    -- A segment of generated code representing a logical grouping of infrastructure resources.
    record NetworkMigrationCodeGenerationSegment
        -- The unique identifier of the code generation job.
        jobID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The unique identifier of the segment.
        segmentID: string
        -- The type of the segment.
        segmentType: string
        -- The logical identifier for the segment.
        logicalID: string
        -- The ID of the mapper segment that this code generation segment was created from.
        mapperSegmentID: string
        -- A list of artifacts generated for this segment.
        artifacts: {NetworkMigrationCodeGenerationArtifact}
        -- A list of other segments that this segment depends on or references.
        referencedSegments: {string}
        -- The timestamp when the segment was created.
        createdAt: number
    end

    record ListNetworkMigrationCodeGenerationSegmentsOutput
        -- A list of network migration code generation segments.
        items: {NetworkMigrationCodeGenerationSegment}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- Filters for listing network migration definitions.
    record ListNetworkMigrationDefinitionsRequestFilters
        -- A list of definition IDs to filter by.
        networkMigrationDefinitionIDs: {string}
    end

    record ListNetworkMigrationDefinitionsInput
        -- Filters to apply when listing network migration definitions.
        filters: ListNetworkMigrationDefinitionsRequestFilters
        -- The token for the next page of results.
        nextToken: string
        -- The maximum number of results to return in a single call.
        maxResults: number
    end

    -- A summary of a network migration definition.
    record NetworkMigrationDefinitionSummary
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The name of the network migration definition.
        name: string
        -- The source environment configuration.
        sourceEnvironment: string
        -- The Amazon Resource Name (ARN) of the network migration definition.
        arn: string
        -- Tags assigned to the network migration definition.
        tags: {string : string}
        -- Scope tags for the network migration definition.
        scopeTags: {string : string}
    end

    record ListNetworkMigrationDefinitionsOutput
        -- A list of network migration definition summaries.
        items: {NetworkMigrationDefinitionSummary}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    record ListNetworkMigrationDeployedStacksInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    enum NetworkMigrationFailedResourceStatus
        "CREATE_FAILED"
        "DELETE_FAILED"
    end

    -- Details about a resource that failed to deploy.
    record NetworkMigrationFailedResourceDetails
        -- The logical ID of the failed resource.
        logicalID: string
        -- The status of the failed resource.
        status: string
        -- The reason why the resource failed.
        statusReason: string
    end

    enum NetworkMigrationDeployedStackStatus
        "CREATE_COMPLETE"
        "CREATE_FAILED"
        "CREATE_STARTED"
        "DELETE_COMPLETE"
        "DELETE_FAILED"
        "DELETE_STARTED"
    end

    -- Details about a CloudFormation stack that has been deployed as part of the network migration.
    record NetworkMigrationDeployedStackDetails
        -- The current status of the deployed stack.
        status: string
        -- The physical ID of the CloudFormation stack.
        stackPhysicalID: string
        -- The logical ID of the stack.
        stackLogicalID: string
        -- The ID of the segment that this stack was deployed for.
        segmentID: string
        -- The target AWS account where the stack was deployed.
        targetAccount: string
        -- A list of resources that failed to deploy.
        failedResources: {NetworkMigrationFailedResourceDetails}
    end

    record ListNetworkMigrationDeployedStacksOutput
        -- A list of deployed stack details including status and resources.
        items: {NetworkMigrationDeployedStackDetails}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- Filters for listing deployer jobs.
    record ListNetworkMigrationDeployerJobFilters
        -- A list of job IDs to filter by.
        jobIDs: {string}
    end

    record ListNetworkMigrationDeploymentsInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- Filters to apply when listing deployment jobs.
        filters: ListNetworkMigrationDeployerJobFilters
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    -- Details about a network migration deployer job.
    record NetworkMigrationDeployerJobDetails
        -- The unique identifier of the deployer job.
        jobID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The timestamp when the job was created.
        createdAt: number
        -- The timestamp when the job completed or failed.
        endedAt: number
        -- The current status of the deployer job.
        status: string
        -- Detailed status information about the job.
        statusDetails: string
    end

    record ListNetworkMigrationDeploymentsOutput
        -- A list of deployer job details.
        items: {NetworkMigrationDeployerJobDetails}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    enum ExecutionStatus
        "PENDING"
        "STARTED"
        "SUCCEEDED"
        "FAILED"
    end

    -- Filters for listing network migration executions.
    record ListNetworkMigrationExecutionRequestFilters
        -- A list of execution IDs to filter by.
        networkMigrationExecutionIDs: {string}
        -- A list of execution statuses to filter by.
        networkMigrationExecutionStatuses: {string}
    end

    record ListNetworkMigrationExecutionsInput
        -- The unique identifier of the network migration definition to list executions for.
        networkMigrationDefinitionID: string
        -- Filters to apply when listing executions, such as status or execution ID.
        filters: ListNetworkMigrationExecutionRequestFilters
        -- The token for the next page of results.
        nextToken: string
        -- The maximum number of results to return in a single call.
        maxResults: number
    end

    enum ExecutionStageActivity
        "MAPPING"
        "MAPPING_UPDATE"
        "CODE_GENERATION"
        "DEPLOY"
        "DEPLOYED_STACKS_DELETION"
        "ANALYZE"
    end

    enum ExecutionStage
        "MAPPING"
        "CODE_GENERATION"
        "DEPLOY"
        "DEPLOYED_STACKS_DELETION"
        "ANALYZE"
    end

    -- Details about a network migration execution instance.
    record NetworkMigrationExecution
        -- The unique identifier of the network migration definition used by this execution.
        networkMigrationDefinitionID: string
        -- The unique identifier of the execution.
        networkMigrationExecutionID: string
        -- The current status of the execution.
        status: string
        -- The current stage of the execution in the migration workflow.
        stage: string
        -- The current activity being performed in the execution.
        activity: string
        -- The timestamp when the execution was created.
        createdAt: number
        -- The timestamp when the execution was last updated.
        updatedAt: number
        -- Tags assigned to the execution.
        tags: {string : string}
    end

    record ListNetworkMigrationExecutionsOutput
        -- A list of network migration execution details.
        items: {NetworkMigrationExecution}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- Filters for listing mapper segment constructs.
    record ListNetworkMigrationMapperSegmentConstructsFilters
        -- A list of construct IDs to filter by.
        constructIDs: {string}
        -- A list of construct types to filter by.
        constructTypes: {string}
    end

    record ListNetworkMigrationMapperSegmentConstructsInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The unique identifier of the segment to list constructs for.
        segmentID: string
        -- Filters to apply when listing constructs, such as construct type or ID.
        filters: ListNetworkMigrationMapperSegmentConstructsFilters
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    record ListNetworkMigrationMapperSegmentConstructsOutput
        -- A list of mapper segment constructs.
        items: {NetworkMigrationMapperSegmentConstruct}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- Filters for listing mapper segments.
    record ListNetworkMigrationMapperSegmentsFilters
        -- A list of segment IDs to filter by.
        segmentIDs: {string}
    end

    record ListNetworkMigrationMapperSegmentsInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- Filters to apply when listing segments.
        filters: ListNetworkMigrationMapperSegmentsFilters
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    enum NetworkMigrationMapperSegmentType
        "WORKLOAD"
        "APPLIANCE"
    end

    record NetworkMigrationMapperSegment
        -- The unique identifier of the job that created this segment.
        jobID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The unique identifier of the segment.
        segmentID: string
        -- The category of the network migration segment. A segment groups the network constructs (such as VPCs, subnets, and security groups) that are migrated together. Valid values: WORKLOAD, APPLIANCE.
        segmentType: string
        -- The name of the segment.
        name: string
        -- A description of the segment.
        description: string
        -- The logical identifier for the segment in the infrastructure code.
        logicalID: string
        -- The checksum of the segment data for integrity verification.
        checksum: Checksum
        -- The S3 location where segment artifacts are stored.
        outputS3Configuration: S3Configuration
        -- The timestamp when the segment was created.
        createdAt: number
        -- The timestamp when the segment was last updated.
        updatedAt: number
        -- Scope tags for the segment.
        scopeTags: {string : string}
        -- The target AWS account where this segment will be deployed.
        targetAccount: string
        -- A list of other segments that this segment depends on or references.
        referencedSegments: {string}
    end

    record ListNetworkMigrationMapperSegmentsOutput
        -- A list of mapper segments.
        items: {NetworkMigrationMapperSegment}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- Filters for listing network migration mapping jobs.
    record ListNetworkMigrationMappingsFilters
        -- A list of job IDs to filter by.
        jobIDs: {string}
    end

    record ListNetworkMigrationMappingsInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- Filters to apply when listing mapping jobs.
        filters: ListNetworkMigrationMappingsFilters
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    -- Details about a network migration mapping job.
    record NetworkMigrationMappingJobDetails
        -- The unique identifier of the mapping job.
        jobID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The timestamp when the job was created.
        createdAt: number
        -- The timestamp when the job completed or failed.
        endedAt: number
        -- The current status of the mapping job.
        status: string
        -- Detailed status information about the job.
        statusDetails: string
    end

    record ListNetworkMigrationMappingsOutput
        -- A list of network migration mapping job details.
        items: {NetworkMigrationMappingJobDetails}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- Filters for listing mapping update jobs.
    record ListNetworkMigrationMappingUpdatesFilters
        -- A list of job IDs to filter by.
        jobIDs: {string}
    end

    record ListNetworkMigrationMappingUpdatesInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- Filters to apply when listing mapping update jobs.
        filters: ListNetworkMigrationMappingUpdatesFilters
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    -- Details about a network migration mapping update job.
    record NetworkMigrationMappingUpdateJobDetails
        -- The unique identifier of the mapping update job.
        jobID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The timestamp when the job was created.
        createdAt: number
        -- The timestamp when the job completed or failed.
        endedAt: number
        -- The current status of the mapping update job.
        status: string
        -- Detailed status information about the job.
        statusDetails: string
    end

    record ListNetworkMigrationMappingUpdatesOutput
        -- A list of mapping update job details.
        items: {NetworkMigrationMappingUpdateJobDetails}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    record StartNetworkMigrationAnalysisInput
        -- The unique identifier of the network migration execution to analyze.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
    end

    record StartNetworkMigrationAnalysisOutput
        -- The unique identifier of the analysis job that was started.
        jobID: string
    end

    record StartNetworkMigrationCodeGenerationInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The output format types for code generation, such as CloudFormation or Terraform.
        codeGenerationOutputFormatTypes: {string}
    end

    record StartNetworkMigrationCodeGenerationOutput
        -- The unique identifier of the code generation job that was started.
        jobID: string
    end

    record StartNetworkMigrationDeploymentInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
    end

    record StartNetworkMigrationDeploymentOutput
        -- The unique identifier of the deployer job that was started.
        jobID: string
    end

    enum SecurityGroupMappingStrategy
        "MAP"
        "SKIP"
        "MAP_DHCP"
    end

    record StartNetworkMigrationMappingInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The security group mapping strategy to use.
        securityGroupMappingStrategy: string
    end

    record StartNetworkMigrationMappingOutput
        -- The unique identifier of the mapping job that was started.
        jobID: string
    end

    -- An operation that deletes a construct from the mapping.
    record DeleteOperation
    end

    -- A construct reference specifying the source segment and construct to merge.
    record MergeConstruct
        -- The segment ID of the construct to merge.
        segmentID: string
        -- The construct ID to merge.
        constructID: string
    end

    -- An operation that merges constructs from different segments into the target construct.
    record MergeOperation
        -- The list of constructs to merge into the target.
        mergeConstructs: {MergeConstruct}
    end

    -- A split target specifying the CIDR block for the new construct.
    record SplitConstruct
        -- The CIDR block for the split construct.
        cidrBlock: string
    end

    -- An operation that splits a construct into multiple constructs with different CIDR blocks.
    record SplitOperation
        -- The list of split targets with their CIDR blocks.
        splitConstructs: {SplitConstruct}
    end

    -- An operation that updates the properties of a construct.
    record UpdateOperation
        -- The updated name for the construct.
        name: string
        -- Whether to exclude this construct from the migration.
        excluded: boolean
        -- The properties to update on the construct.
        properties: {string : string}
    end

    -- A union type representing the operation to perform on a construct during a mapping update.
    record OperationUnion
        -- A merge operation to combine constructs from different segments.
        merge: MergeOperation
        -- A split operation to divide a construct into multiple constructs with specified CIDR blocks.
        split: SplitOperation
        -- A delete operation to remove a construct from the mapping.
        delete: DeleteOperation
        -- An update operation to modify construct properties.
        update: UpdateOperation
    end

    -- A construct update to apply during a mapping update operation.
    record StartNetworkMigrationMappingUpdateConstruct
        -- The ID of the segment containing the construct.
        segmentID: string
        -- The ID of the construct to update.
        constructID: string
        -- The type of the construct.
        constructType: string
        -- The operation to perform on the construct.
        operation: OperationUnion
    end

    -- A segment update to apply during a mapping update operation.
    record StartNetworkMigrationMappingUpdateSegment
        -- The ID of the segment to update.
        segmentID: string
        -- The updated target AWS account for the segment.
        targetAccount: string
        -- The updated scope tags for the segment.
        scopeTags: {string : string}
    end

    record StartNetworkMigrationMappingUpdateInput
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- A list of construct updates to apply.
        constructs: {StartNetworkMigrationMappingUpdateConstruct}
        -- A list of segment updates to apply.
        segments: {StartNetworkMigrationMappingUpdateSegment}
    end

    record StartNetworkMigrationMappingUpdateOutput
        -- The unique identifier of the mapping update job that was started.
        jobID: string
    end

    -- Updated configuration for the target network topology and addressing.
    record TargetNetworkUpdate
        -- The updated network topology type.
        topology: string
        -- The updated CIDR block for inbound traffic.
        inboundCidr: string
        -- The updated CIDR block for outbound traffic.
        outboundCidr: string
        -- The updated CIDR block for inspection traffic.
        inspectionCidr: string
    end

    -- Updated S3 configuration for storing target network artifacts.
    record TargetS3ConfigurationUpdate
        -- The updated name of the S3 bucket.
        s3Bucket: string
        -- The updated AWS account ID of the S3 bucket owner.
        s3BucketOwner: string
    end

    record UpdateNetworkMigrationDefinitionInput
        -- The unique identifier of the network migration definition to update.
        networkMigrationDefinitionID: string
        -- The updated name of the network migration definition.
        name: string
        -- The updated description of the network migration definition.
        description: string
        -- The updated list of source configurations.
        sourceConfigurations: {SourceConfiguration}
        -- The updated S3 configuration for storing the target network artifacts.
        targetS3Configuration: TargetS3ConfigurationUpdate
        -- The updated target network configuration.
        targetNetwork: TargetNetworkUpdate
        -- The updated target deployment configuration.
        targetDeployment: string
        -- Updates whether the migration creates new target VPCs or uses existing ones. Set to USE_EXISTING to migrate into existing VPCs in the target account, or to CREATE_NEW to provision new target VPCs.
        vpcProvisioningStrategy: string
        -- The updated list of CIDR mappings that map original source CIDR ranges to updated target CIDR ranges. CIDR mappings can be provided only when vpcProvisioningStrategy is set to USE_EXISTING.
        cidrMappings: {CidrMapping}
        -- The updated scope tags for the network migration definition.
        scopeTags: {string : string}
    end

    record UpdateNetworkMigrationDefinitionOutput
        -- The Amazon Resource Name (ARN) of the network migration definition.
        arn: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The name of the network migration definition.
        name: string
        -- A description of the network migration definition.
        description: string
        -- A list of source configurations for the network migration.
        sourceConfigurations: {SourceConfiguration}
        -- The S3 configuration for storing the target network artifacts.
        targetS3Configuration: TargetS3Configuration
        -- The target network configuration including topology and CIDR ranges.
        targetNetwork: TargetNetwork
        -- The target deployment configuration for the migrated network.
        targetDeployment: string
        -- Indicates whether the migration creates new target VPCs or uses existing ones. CREATE_NEW provisions new target VPCs; USE_EXISTING migrates into existing VPCs in the target account.
        vpcProvisioningStrategy: string
        -- A list of CIDR mappings that map original source CIDR ranges to updated target CIDR ranges. CIDR mappings apply only when vpcProvisioningStrategy is set to USE_EXISTING.
        cidrMappings: {CidrMapping}
        -- The timestamp when the network migration definition was created.
        createdAt: number
        -- The timestamp when the network migration definition was last updated.
        updatedAt: number
        -- Tags assigned to the network migration definition.
        tags: {string : string}
        -- Scope tags for the network migration definition.
        scopeTags: {string : string}
    end

    record UpdateNetworkMigrationMapperSegmentInput
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the segment to update.
        segmentID: string
        -- The updated scope tags for the segment.
        scopeTags: {string : string}
    end

    record UpdateNetworkMigrationMapperSegmentOutput
        -- The unique identifier of the job that created this segment.
        jobID: string
        -- The unique identifier of the network migration execution.
        networkMigrationExecutionID: string
        -- The unique identifier of the network migration definition.
        networkMigrationDefinitionID: string
        -- The unique identifier of the segment.
        segmentID: string
        -- The category of the network migration segment. A segment groups the network constructs (such as VPCs, subnets, and security groups) that are migrated together. Valid values: WORKLOAD, APPLIANCE.
        segmentType: string
        -- The name of the segment.
        name: string
        -- A description of the segment.
        description: string
        -- The logical identifier for the segment in the infrastructure code.
        logicalID: string
        -- The checksum of the segment data for integrity verification.
        checksum: Checksum
        -- The S3 location where segment artifacts are stored.
        outputS3Configuration: S3Configuration
        -- The timestamp when the segment was created.
        createdAt: number
        -- The timestamp when the segment was last updated.
        updatedAt: number
        -- Scope tags for the segment.
        scopeTags: {string : string}
        -- The target AWS account where this segment will be deployed.
        targetAccount: string
        -- A list of other segments that this segment depends on or references.
        referencedSegments: {string}
    end

    enum ReplicationConfigurationDataPlaneRouting
        "PRIVATE_IP"
        "PUBLIC_IP"
    end

    enum ReplicationConfigurationDefaultLargeStagingDiskType
        "GP2"
        "ST1"
        "GP3"
    end

    enum ReplicationConfigurationEbsEncryption
        "DEFAULT"
        "CUSTOM"
    end

    enum InternetProtocol
        "IPV4"
        "IPV6"
    end

    -- FSx for ONTAP storage configuration.
    record FsxOntapConfiguration
        -- FSx ONTAP configuration storage virtual machine ID.
        storageVirtualMachineId: string
        -- FSx ONTAP configuration credentials secret ARN.
        credentialsSecretArn: string
    end

    enum StorageType
        "EBS"
        "FSX_ONTAP"
    end

    -- Storage configuration for replication.
    record StorageConfiguration
        -- Storage configuration storage type.
        storageType: string
        -- Storage configuration FSx ONTAP configuration.
        fsxOntapConfiguration: FsxOntapConfiguration
    end

    record CreateReplicationConfigurationTemplateInput
        -- Request to configure the Staging Area subnet ID during Replication Settings template creation.
        stagingAreaSubnetId: string
        -- Request to associate the default Application Migration Service Security group with the Replication Settings template.
        associateDefaultSecurityGroup: boolean
        -- Request to configure the Replication Server Security group ID during Replication Settings template creation.
        replicationServersSecurityGroupsIDs: {string}
        -- Request to configure the Replication Server instance type during Replication Settings template creation.
        replicationServerInstanceType: string
        -- Request to use Dedicated Replication Servers during Replication Settings template creation.
        useDedicatedReplicationServer: boolean
        -- Request to configure the default large staging disk EBS volume type during Replication Settings template creation.
        defaultLargeStagingDiskType: string
        -- Request to configure EBS encryption during Replication Settings template creation.
        ebsEncryption: string
        -- Request to configure an EBS encryption key during Replication Settings template creation.
        ebsEncryptionKeyArn: string
        -- Request to configure bandwidth throttling during Replication Settings template creation.
        bandwidthThrottling: number
        -- Request to configure data plane routing during Replication Settings template creation.
        dataPlaneRouting: string
        -- Request to create Public IP during Replication Settings template creation.
        createPublicIP: boolean
        -- Request to configure Staging Area tags during Replication Settings template creation.
        stagingAreaTags: {string : string}
        -- Request to use Fips Endpoint during Replication Settings template creation.
        useFipsEndpoint: boolean
        -- Request to configure tags during Replication Settings template creation.
        tags: {string : string}
        -- Request to configure the internet protocol to IPv4 or IPv6.
        internetProtocol: string
        -- Request to store snapshot on local zone during Replication Settings template creation.
        storeSnapshotOnLocalZone: boolean
        -- Request to configure storage during Replication Settings template creation.
        storageConfiguration: StorageConfiguration
    end

    record CreateReplicationConfigurationTemplateOutput
        -- Replication Configuration template ID.
        replicationConfigurationTemplateID: string
        -- Replication Configuration template ARN.
        arn: string
        -- Replication Configuration template Staging Area subnet ID.
        stagingAreaSubnetId: string
        -- Replication Configuration template associate default Application Migration Service Security group.
        associateDefaultSecurityGroup: boolean
        -- Replication Configuration template server Security Groups IDs.
        replicationServersSecurityGroupsIDs: {string}
        -- Replication Configuration template server instance type.
        replicationServerInstanceType: string
        -- Replication Configuration template use Dedicated Replication Server.
        useDedicatedReplicationServer: boolean
        -- Replication Configuration template use default large Staging Disk type.
        defaultLargeStagingDiskType: string
        -- Replication Configuration template EBS encryption.
        ebsEncryption: string
        -- Replication Configuration template EBS encryption key ARN.
        ebsEncryptionKeyArn: string
        -- Replication Configuration template bandwidth throttling.
        bandwidthThrottling: number
        -- Replication Configuration template data plane routing.
        dataPlaneRouting: string
        -- Replication Configuration template create Public IP.
        createPublicIP: boolean
        -- Replication Configuration template Staging Area Tags.
        stagingAreaTags: {string : string}
        -- Replication Configuration template use Fips Endpoint.
        useFipsEndpoint: boolean
        -- Replication Configuration template Tags.
        tags: {string : string}
        -- Replication Configuration template internet protocol.
        internetProtocol: string
        -- Replication Configuration template store snapshot on local zone.
        storeSnapshotOnLocalZone: boolean
        -- Replication Configuration template storage configuration.
        storageConfiguration: StorageConfiguration
    end

    record DeleteReplicationConfigurationTemplateInput
        -- Request to delete Replication Configuration Template from service by Replication Configuration Template ID.
        replicationConfigurationTemplateID: string
    end

    record DeleteReplicationConfigurationTemplateOutput
    end

    record DescribeReplicationConfigurationTemplatesInput
        -- Request to describe Replication Configuration template by template IDs.
        replicationConfigurationTemplateIDs: {string}
        -- Request to describe Replication Configuration template by max results.
        maxResults: number
        -- Request to describe Replication Configuration template by next token.
        nextToken: string
    end

    record ReplicationConfigurationTemplate
        -- Replication Configuration template ID.
        replicationConfigurationTemplateID: string
        -- Replication Configuration template ARN.
        arn: string
        -- Replication Configuration template Staging Area subnet ID.
        stagingAreaSubnetId: string
        -- Replication Configuration template associate default Application Migration Service Security group.
        associateDefaultSecurityGroup: boolean
        -- Replication Configuration template server Security Groups IDs.
        replicationServersSecurityGroupsIDs: {string}
        -- Replication Configuration template server instance type.
        replicationServerInstanceType: string
        -- Replication Configuration template use Dedicated Replication Server.
        useDedicatedReplicationServer: boolean
        -- Replication Configuration template use default large Staging Disk type.
        defaultLargeStagingDiskType: string
        -- Replication Configuration template EBS encryption.
        ebsEncryption: string
        -- Replication Configuration template EBS encryption key ARN.
        ebsEncryptionKeyArn: string
        -- Replication Configuration template bandwidth throttling.
        bandwidthThrottling: number
        -- Replication Configuration template data plane routing.
        dataPlaneRouting: string
        -- Replication Configuration template create Public IP.
        createPublicIP: boolean
        -- Replication Configuration template Staging Area Tags.
        stagingAreaTags: {string : string}
        -- Replication Configuration template use Fips Endpoint.
        useFipsEndpoint: boolean
        -- Replication Configuration template Tags.
        tags: {string : string}
        -- Replication Configuration template internet protocol.
        internetProtocol: string
        -- Replication Configuration template store snapshot on local zone.
        storeSnapshotOnLocalZone: boolean
        -- Replication Configuration template storage configuration.
        storageConfiguration: StorageConfiguration
    end

    record DescribeReplicationConfigurationTemplatesOutput
        -- Request to describe Replication Configuration template by items.
        items: {ReplicationConfigurationTemplate}
        -- Request to describe Replication Configuration template by next token.
        nextToken: string
    end

    record UpdateReplicationConfigurationTemplateInput
        -- Update replication configuration template template ID request.
        replicationConfigurationTemplateID: string
        -- Update replication configuration template ARN request.
        arn: string
        -- Update replication configuration template Staging Area subnet ID request.
        stagingAreaSubnetId: string
        -- Update replication configuration template associate default Application Migration Service Security group request.
        associateDefaultSecurityGroup: boolean
        -- Update replication configuration template Replication Server Security groups IDs request.
        replicationServersSecurityGroupsIDs: {string}
        -- Update replication configuration template Replication Server instance type request.
        replicationServerInstanceType: string
        -- Update replication configuration template use dedicated Replication Server request.
        useDedicatedReplicationServer: boolean
        -- Update replication configuration template use default large Staging Disk type request.
        defaultLargeStagingDiskType: string
        -- Update replication configuration template EBS encryption request.
        ebsEncryption: string
        -- Update replication configuration template EBS encryption key ARN request.
        ebsEncryptionKeyArn: string
        -- Update replication configuration template bandwidth throttling request.
        bandwidthThrottling: number
        -- Update replication configuration template data plane routing request.
        dataPlaneRouting: string
        -- Update replication configuration template create Public IP request.
        createPublicIP: boolean
        -- Update replication configuration template Staging Area Tags request.
        stagingAreaTags: {string : string}
        -- Update replication configuration template use Fips Endpoint request.
        useFipsEndpoint: boolean
        -- Update replication configuration template internet protocol request.
        internetProtocol: string
        -- Update replication configuration template store snapshot on local zone request.
        storeSnapshotOnLocalZone: boolean
        -- Update replication configuration template storage configuration request.
        storageConfiguration: StorageConfiguration
    end

    record UpdateReplicationConfigurationTemplateOutput
        -- Replication Configuration template ID.
        replicationConfigurationTemplateID: string
        -- Replication Configuration template ARN.
        arn: string
        -- Replication Configuration template Staging Area subnet ID.
        stagingAreaSubnetId: string
        -- Replication Configuration template associate default Application Migration Service Security group.
        associateDefaultSecurityGroup: boolean
        -- Replication Configuration template server Security Groups IDs.
        replicationServersSecurityGroupsIDs: {string}
        -- Replication Configuration template server instance type.
        replicationServerInstanceType: string
        -- Replication Configuration template use Dedicated Replication Server.
        useDedicatedReplicationServer: boolean
        -- Replication Configuration template use default large Staging Disk type.
        defaultLargeStagingDiskType: string
        -- Replication Configuration template EBS encryption.
        ebsEncryption: string
        -- Replication Configuration template EBS encryption key ARN.
        ebsEncryptionKeyArn: string
        -- Replication Configuration template bandwidth throttling.
        bandwidthThrottling: number
        -- Replication Configuration template data plane routing.
        dataPlaneRouting: string
        -- Replication Configuration template create Public IP.
        createPublicIP: boolean
        -- Replication Configuration template Staging Area Tags.
        stagingAreaTags: {string : string}
        -- Replication Configuration template use Fips Endpoint.
        useFipsEndpoint: boolean
        -- Replication Configuration template Tags.
        tags: {string : string}
        -- Replication Configuration template internet protocol.
        internetProtocol: string
        -- Replication Configuration template store snapshot on local zone.
        storeSnapshotOnLocalZone: boolean
        -- Replication Configuration template storage configuration.
        storageConfiguration: StorageConfiguration
    end

    enum ChangeServerLifeCycleStateSourceServerLifecycleState
        "READY_FOR_TEST"
        "READY_FOR_CUTOVER"
        "CUTOVER"
    end

    -- The request to change the source server migration lifecycle state.
    record ChangeServerLifeCycleStateSourceServerLifecycle
        -- The request to change the source server migration lifecycle state.
        state: string
    end

    record ChangeServerLifeCycleStateInput
        -- The request to change the source server migration lifecycle state by source server ID.
        sourceServerID: string
        -- The request to change the source server migration lifecycle state.
        lifeCycle: ChangeServerLifeCycleStateSourceServerLifecycle
        -- The request to change the source server migration account ID.
        accountID: string
    end

    -- Source Server connector action.
    record SourceServerConnectorAction
        -- Source Server connector action credentials secret arn.
        credentialsSecretArn: string
        -- Source Server connector action connector arn.
        connectorArn: string
    end

    enum DataReplicationErrorString
        "AGENT_NOT_SEEN"
        "SNAPSHOTS_FAILURE"
        "NOT_CONVERGING"
        "UNSTABLE_NETWORK"
        "FAILED_TO_CREATE_SECURITY_GROUP"
        "FAILED_TO_LAUNCH_REPLICATION_SERVER"
        "FAILED_TO_BOOT_REPLICATION_SERVER"
        "FAILED_TO_AUTHENTICATE_WITH_SERVICE"
        "FAILED_TO_DOWNLOAD_REPLICATION_SOFTWARE"
        "FAILED_TO_CREATE_STAGING_DISKS"
        "FAILED_TO_ATTACH_STAGING_DISKS"
        "FAILED_TO_PAIR_REPLICATION_SERVER_WITH_AGENT"
        "FAILED_TO_CONNECT_AGENT_TO_REPLICATION_SERVER"
        "FAILED_TO_START_DATA_TRANSFER"
        "UNSUPPORTED_VM_CONFIGURATION"
        "LAST_SNAPSHOT_JOB_FAILED"
        "FAILED_TO_SETUP_FSX_PROXY"
        "FAILED_TO_CREATE_FSX_SNAPSHOT"
    end

    -- Error in data replication.
    record DataReplicationError
        -- Error in data replication.
        error: string
        -- Error in data replication.
        rawError: string
    end

    enum DataReplicationInitiationStepName
        "WAIT"
        "CREATE_SECURITY_GROUP"
        "LAUNCH_REPLICATION_SERVER"
        "BOOT_REPLICATION_SERVER"
        "AUTHENTICATE_WITH_SERVICE"
        "DOWNLOAD_REPLICATION_SOFTWARE"
        "CREATE_STAGING_DISKS"
        "ATTACH_STAGING_DISKS"
        "PAIR_REPLICATION_SERVER_WITH_AGENT"
        "CONNECT_AGENT_TO_REPLICATION_SERVER"
        "START_DATA_TRANSFER"
        "SETUP_FSX_PROXY"
    end

    enum DataReplicationInitiationStepStatus
        "NOT_STARTED"
        "IN_PROGRESS"
        "SUCCEEDED"
        "FAILED"
        "SKIPPED"
    end

    -- Data replication initiation step.
    record DataReplicationInitiationStep
        -- Request to query data initiation step name.
        name: string
        -- Request to query data initiation status.
        status: string
    end

    -- Data replication initiation.
    record DataReplicationInitiation
        -- Request to query data initiation start date and time.
        startDateTime: string
        -- Request to query next data initiation date and time.
        nextAttemptDateTime: string
        -- Request to query data initiation steps.
        steps: {DataReplicationInitiationStep}
    end

    enum DataReplicationState
        "STOPPED"
        "INITIATING"
        "INITIAL_SYNC"
        "BACKLOG"
        "CREATING_SNAPSHOT"
        "CONTINUOUS"
        "PAUSED"
        "RESCAN"
        "STALLED"
        "DISCONNECTED"
        "PENDING_SNAPSHOT_SHIPPING"
        "SHIPPING_SNAPSHOT"
    end

    -- Request to query disks replicated.
    record DataReplicationInfoReplicatedDisk
        -- Request to query device name.
        deviceName: string
        -- Request to query total amount of data replicated in bytes.
        totalStorageBytes: number
        -- Request to query amount of data replicated in bytes.
        replicatedStorageBytes: number
        -- Request to query amount of data rescanned in bytes.
        rescannedStorageBytes: number
        -- Request to query data replication backlog size in bytes.
        backloggedStorageBytes: number
    end

    -- Request data replication info.
    record DataReplicationInfo
        -- Request to query data replication lag duration.
        lagDuration: string
        -- Request to query the time when data replication will be complete.
        etaDateTime: string
        -- Request to query disks replicated.
        replicatedDisks: {DataReplicationInfoReplicatedDisk}
        -- Request to query the data replication state.
        dataReplicationState: string
        -- Request to query whether data replication has been initiated.
        dataReplicationInitiation: DataReplicationInitiation
        -- Error in obtaining data replication info.
        dataReplicationError: DataReplicationError
        -- Request to query data replication last snapshot time.
        lastSnapshotDateTime: string
        -- Replication server instance ID.
        replicatorId: string
    end

    enum FirstBoot
        "WAITING"
        "SUCCEEDED"
        "UNKNOWN"
        "STOPPED"
    end

    enum LastKnownCheckStatus
        "PASSED"
        "FAILED"
        "PENDING"
    end

    enum LastKnownCheckType
        "EC2"
        "FSx"
    end

    -- Last known check performed on a launched instance.
    record LastKnownCheck
        -- Last known check name.
        name: string
        -- Last known check status.
        status: string
        -- Last known check error.
        error: string
        -- Last known check timestamp.
        checkedAt: number
    end

    -- Launched instance.
    record LaunchedInstance
        -- Launched instance EC2 ID.
        ec2InstanceID: string
        -- Launched instance Job ID.
        jobID: string
        -- Launched instance first boot.
        firstBoot: string
        -- Launched instance last known checks.
        lastKnownChecks: {LastKnownCheck}
        -- Launched instance last known FSx checks status.
        lastKnownFsxChecksStatus: string
    end

    -- Lifecycle Cutover finalized
    record LifeCycleLastCutoverFinalized
        -- Lifecycle Cutover finalized date and time.
        apiCallDateTime: string
    end

    -- Lifecycle last Cutover initiated.
    record LifeCycleLastCutoverInitiated
        apiCallDateTime: string
        -- Lifecycle last Cutover initiated by Job ID.
        jobID: string
    end

    -- Lifecycle last Cutover reverted.
    record LifeCycleLastCutoverReverted
        -- Lifecycle last Cutover reverted API call date time.
        apiCallDateTime: string
    end

    -- Lifecycle last Cutover .
    record LifeCycleLastCutover
        -- Lifecycle last Cutover initiated.
        initiated: LifeCycleLastCutoverInitiated
        -- Lifecycle last Cutover reverted.
        reverted: LifeCycleLastCutoverReverted
        -- Lifecycle Cutover finalized date and time.
        finalized: LifeCycleLastCutoverFinalized
    end

    -- Lifecycle last Test finalized.
    record LifeCycleLastTestFinalized
        -- Lifecycle Test finalized API call date and time.
        apiCallDateTime: string
    end

    -- Lifecycle last Test initiated.
    record LifeCycleLastTestInitiated
        -- Lifecycle last Test initiated API call date and time.
        apiCallDateTime: string
        -- Lifecycle last Test initiated Job ID.
        jobID: string
    end

    -- Lifecycle last Test reverted.
    record LifeCycleLastTestReverted
        -- Lifecycle last Test reverted API call date and time.
        apiCallDateTime: string
    end

    -- Lifecycle last Test.
    record LifeCycleLastTest
        -- Lifecycle last Test initiated.
        initiated: LifeCycleLastTestInitiated
        -- Lifecycle last Test reverted.
        reverted: LifeCycleLastTestReverted
        -- Lifecycle last Test finalized.
        finalized: LifeCycleLastTestFinalized
    end

    enum LifeCycleState
        "STOPPED"
        "NOT_READY"
        "READY_FOR_TEST"
        "TESTING"
        "READY_FOR_CUTOVER"
        "CUTTING_OVER"
        "CUTOVER"
        "DISCONNECTED"
        "DISCOVERED"
        "PENDING_INSTALLATION"
    end

    -- Lifecycle.
    record LifeCycle
        -- Lifecycle added to service date and time.
        addedToServiceDateTime: string
        -- Lifecycle replication initiation date and time.
        firstByteDateTime: string
        -- Lifecycle elapsed time and duration.
        elapsedReplicationDuration: string
        -- Lifecycle last seen date and time.
        lastSeenByServiceDateTime: string
        -- Lifecycle last Test.
        lastTest: LifeCycleLastTest
        -- Lifecycle last Cutover.
        lastCutover: LifeCycleLastCutover
        -- Lifecycle state.
        state: string
    end

    enum ReplicationType
        "AGENT_BASED"
        "SNAPSHOT_SHIPPING"
    end

    -- Source server CPU information.
    record CPU
        -- The number of CPU cores on the source server.
        cores: number
        -- The source server's CPU model name.
        modelName: string
    end

    -- The disk identifier.
    record Disk
        -- The disk or device name.
        deviceName: string
        -- The amount of storage on the disk in bytes.
        bytes: number
    end

    -- Identification hints.
    record IdentificationHints
        -- FQDN address identification hint.
        fqdn: string
        -- Hostname identification hint.
        hostname: string
        -- vmWare UUID identification hint.
        vmWareUuid: string
        -- AWS Instance ID identification hint.
        awsInstanceID: string
        -- vCenter VM path identification hint.
        vmPath: string
    end

    -- Network interface.
    record NetworkInterface
        -- Network interface Mac address.
        macAddress: string
        -- Network interface IPs.
        ips: {string}
        -- Network interface primary IP.
        isPrimary: boolean
    end

    -- Operating System.
    record OS
        -- OS full string.
        fullString: string
    end

    -- Source server properties.
    record SourceProperties
        -- Source server last update date and time.
        lastUpdatedDateTime: string
        -- Source server recommended instance type.
        recommendedInstanceType: string
        -- Source server identification hints.
        identificationHints: IdentificationHints
        -- Source server network interfaces.
        networkInterfaces: {NetworkInterface}
        -- Source Server disks.
        disks: {Disk}
        -- Source Server CPUs.
        cpus: {CPU}
        -- Source server RAM in bytes.
        ramBytes: number
        -- Source server OS.
        os: OS
    end

    record ChangeServerLifeCycleStateOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record DeleteSourceServerInput
        -- Request to delete Source Server from service by Server ID.
        sourceServerID: string
        -- Request to delete Source Server from service by Account ID.
        accountID: string
    end

    record DeleteSourceServerOutput
    end

    -- Request to filter Source Servers list.
    record DescribeSourceServersRequestFilters
        -- Request to filter Source Servers list by Source Server ID.
        sourceServerIDs: {string}
        -- Request to filter Source Servers list by archived.
        isArchived: boolean
        -- Request to filter Source Servers list by replication type.
        replicationTypes: {string}
        -- Request to filter Source Servers list by life cycle states.
        lifeCycleStates: {string}
        -- Request to filter Source Servers list by application IDs.
        applicationIDs: {string}
    end

    record DescribeSourceServersInput
        -- Request to filter Source Servers list.
        filters: DescribeSourceServersRequestFilters
        -- Request to filter Source Servers list by maximum results.
        maxResults: number
        -- Request to filter Source Servers list by next token.
        nextToken: string
        -- Request to filter Source Servers list by Account ID.
        accountID: string
    end

    record SourceServer
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record DescribeSourceServersOutput
        -- The list of returned Source Servers.
        items: {SourceServer}
        -- The token of the next Source Server to retrieve.
        nextToken: string
    end

    record DisconnectFromServiceInput
        -- Request to disconnect Source Server from service by Server ID.
        sourceServerID: string
        -- Request to disconnect Source Server from service by Account ID.
        accountID: string
    end

    record DisconnectFromServiceOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record FinalizeCutoverInput
        -- Request to finalize Cutover by Source Server ID.
        sourceServerID: string
        -- Request to finalize Cutover by Source Account ID.
        accountID: string
    end

    record FinalizeCutoverOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record GetLaunchConfigurationInput
        -- Request to get Launch Configuration information by Source Server ID.
        sourceServerID: string
        -- Request to get Launch Configuration information by Account ID.
        accountID: string
    end

    record GetLaunchConfigurationOutput
        -- Launch configuration Source Server ID.
        sourceServerID: string
        -- Launch configuration name.
        name: string
        -- Launch configuration EC2 Launch template ID.
        ec2LaunchTemplateID: string
        -- Launch disposition for launch configuration.
        launchDisposition: string
        -- Launch configuration Target instance type right sizing method.
        targetInstanceTypeRightSizingMethod: string
        -- Copy Private IP during Launch Configuration.
        copyPrivateIp: boolean
        -- Copy Tags during Launch Configuration.
        copyTags: boolean
        -- Launch configuration OS licensing.
        licensing: Licensing
        -- Launch configuration boot mode.
        bootMode: string
        postLaunchActions: PostLaunchActions
        -- Enable map auto tagging.
        enableMapAutoTagging: boolean
        -- Map auto tagging MPE ID.
        mapAutoTaggingMpeID: string
    end

    record GetReplicationConfigurationInput
        -- Request to get Replication Configuration by Source Server ID.
        sourceServerID: string
        -- Request to get Replication Configuration by Account ID.
        accountID: string
    end

    enum ReplicationConfigurationReplicatedDiskStagingDiskType
        "AUTO"
        "GP2"
        "IO1"
        "SC1"
        "ST1"
        "STANDARD"
        "GP3"
        "IO2"
        "FSX_ONTAP"
    end

    -- Replication Configuration replicated disk.
    record ReplicationConfigurationReplicatedDisk
        -- Replication Configuration replicated disk device name.
        deviceName: string
        -- Replication Configuration replicated disk boot disk.
        isBootDisk: boolean
        -- Replication Configuration replicated disk staging disk type.
        stagingDiskType: string
        -- Replication Configuration replicated disk IOPs.
        iops: number
        -- Replication Configuration replicated disk throughput.
        throughput: number
    end

    record GetReplicationConfigurationOutput
        -- Replication Configuration Source Server ID.
        sourceServerID: string
        -- Replication Configuration name.
        name: string
        -- Replication Configuration Staging Area subnet ID.
        stagingAreaSubnetId: string
        -- Replication Configuration associate default Application Migration Service Security Group.
        associateDefaultSecurityGroup: boolean
        -- Replication Configuration Replication Server Security Group IDs.
        replicationServersSecurityGroupsIDs: {string}
        -- Replication Configuration Replication Server instance type.
        replicationServerInstanceType: string
        -- Replication Configuration use Dedicated Replication Server.
        useDedicatedReplicationServer: boolean
        -- Replication Configuration use default large Staging Disks.
        defaultLargeStagingDiskType: string
        -- Replication Configuration replicated disks.
        replicatedDisks: {ReplicationConfigurationReplicatedDisk}
        -- Replication Configuration EBS encryption.
        ebsEncryption: string
        -- Replication Configuration EBS encryption key ARN.
        ebsEncryptionKeyArn: string
        -- Replication Configuration set bandwidth throttling.
        bandwidthThrottling: number
        -- Replication Configuration data plane routing.
        dataPlaneRouting: string
        -- Replication Configuration create Public IP.
        createPublicIP: boolean
        -- Replication Configuration Staging Area tags.
        stagingAreaTags: {string : string}
        -- Replication Configuration use Fips Endpoint.
        useFipsEndpoint: boolean
        -- Replication Configuration internet protocol.
        internetProtocol: string
        -- Replication Configuration store snapshot on local zone.
        storeSnapshotOnLocalZone: boolean
        -- Replication Configuration storage configuration.
        storageConfiguration: StorageConfiguration
    end

    -- Source server post migration custom action filters.
    record SourceServerActionsRequestFilters
        -- Action IDs to filter source server post migration custom actions by.
        actionIDs: {string}
    end

    record ListSourceServerActionsInput
        -- Source server ID.
        sourceServerID: string
        -- Filters to apply when listing source server post migration custom actions.
        filters: SourceServerActionsRequestFilters
        -- Maximum amount of items to return when listing source server post migration custom actions.
        maxResults: number
        -- Next token to use when listing source server post migration custom actions.
        nextToken: string
        -- Account ID to return when listing source server post migration custom actions.
        accountID: string
    end

    record SourceServerActionDocument
        -- Source server post migration custom action ID.
        actionID: string
        -- Source server post migration custom action name.
        actionName: string
        -- Source server post migration custom action document identifier.
        documentIdentifier: string
        -- Source server post migration custom action order.
        order: number
        -- Source server post migration custom action document version.
        documentVersion: string
        -- Source server post migration custom action active status.
        active: boolean
        -- Source server post migration custom action timeout in seconds.
        timeoutSeconds: number
        -- Source server post migration custom action must succeed for cutover.
        mustSucceedForCutover: boolean
        -- Source server post migration custom action parameters.
        parameters: {string : {SsmParameterStoreParameter}}
        -- Source server post migration custom action external parameters.
        externalParameters: {string : SsmExternalParameter}
        -- Source server post migration custom action description.
        description: string
        -- Source server post migration custom action category.
        category: string
    end

    record ListSourceServerActionsOutput
        -- List of source server post migration custom actions.
        items: {SourceServerActionDocument}
        -- Next token returned when listing source server post migration custom actions.
        nextToken: string
    end

    record MarkAsArchivedInput
        -- Mark as archived by Source Server ID.
        sourceServerID: string
        -- Mark as archived by Account ID.
        accountID: string
    end

    record MarkAsArchivedOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record PauseReplicationInput
        -- Pause Replication Request source server ID.
        sourceServerID: string
        -- Pause Replication Request account ID.
        accountID: string
    end

    record PauseReplicationOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record PutSourceServerActionInput
        -- Source server ID.
        sourceServerID: string
        -- Source server post migration custom action name.
        actionName: string
        -- Source server post migration custom action document identifier.
        documentIdentifier: string
        -- Source server post migration custom action order.
        order: number
        -- Source server post migration custom action ID.
        actionID: string
        -- Source server post migration custom action document version.
        documentVersion: string
        -- Source server post migration custom action active status.
        active: boolean
        -- Source server post migration custom action timeout in seconds.
        timeoutSeconds: number
        -- Source server post migration custom action must succeed for cutover.
        mustSucceedForCutover: boolean
        -- Source server post migration custom action parameters.
        parameters: {string : {SsmParameterStoreParameter}}
        -- Source server post migration custom action external parameters.
        externalParameters: {string : SsmExternalParameter}
        -- Source server post migration custom action description.
        description: string
        -- Source server post migration custom action category.
        category: string
        -- Source server post migration custom account ID.
        accountID: string
    end

    record PutSourceServerActionOutput
        -- Source server post migration custom action ID.
        actionID: string
        -- Source server post migration custom action name.
        actionName: string
        -- Source server post migration custom action document identifier.
        documentIdentifier: string
        -- Source server post migration custom action order.
        order: number
        -- Source server post migration custom action document version.
        documentVersion: string
        -- Source server post migration custom action active status.
        active: boolean
        -- Source server post migration custom action timeout in seconds.
        timeoutSeconds: number
        -- Source server post migration custom action must succeed for cutover.
        mustSucceedForCutover: boolean
        -- Source server post migration custom action parameters.
        parameters: {string : {SsmParameterStoreParameter}}
        -- Source server post migration custom action external parameters.
        externalParameters: {string : SsmExternalParameter}
        -- Source server post migration custom action description.
        description: string
        -- Source server post migration custom action category.
        category: string
    end

    record RemoveSourceServerActionInput
        -- Source server ID of the post migration custom action to remove.
        sourceServerID: string
        -- Source server post migration custom action ID to remove.
        actionID: string
        -- Source server post migration account ID.
        accountID: string
    end

    record RemoveSourceServerActionOutput
    end

    record ResumeReplicationInput
        -- Resume Replication Request source server ID.
        sourceServerID: string
        -- Resume Replication Request account ID.
        accountID: string
    end

    record ResumeReplicationOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record RetryDataReplicationInput
        -- Retry data replication for Source Server ID.
        sourceServerID: string
        -- Retry data replication for Account ID.
        accountID: string
    end

    record RetryDataReplicationOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record StartCutoverInput
        -- Start Cutover by Source Server IDs.
        sourceServerIDs: {string}
        -- Start Cutover by Tags.
        tags: {string : string}
        -- Start Cutover by Account IDs
        accountID: string
    end

    record StartCutoverOutput
        -- Start Cutover Job response.
        job: Job
    end

    record StartReplicationInput
        -- ID of source server on which to start replication.
        sourceServerID: string
        -- Account ID on which to start replication.
        accountID: string
    end

    record StartReplicationOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record StartTestInput
        -- Start Test for Source Server IDs.
        sourceServerIDs: {string}
        -- Start Test by Tags.
        tags: {string : string}
        -- Start Test for Account ID.
        accountID: string
    end

    record StartTestOutput
        -- Start Test Job response.
        job: Job
    end

    record StopReplicationInput
        -- Stop Replication Request source server ID.
        sourceServerID: string
        -- Stop Replication Request account ID.
        accountID: string
    end

    record StopReplicationOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record TerminateTargetInstancesInput
        -- Terminate Target instance by Source Server IDs.
        sourceServerIDs: {string}
        -- Terminate Target instance by Tags.
        tags: {string : string}
        -- Terminate Target instance by Account ID
        accountID: string
    end

    record TerminateTargetInstancesOutput
        -- Terminate Target instance Job response.
        job: Job
    end

    record UpdateLaunchConfigurationInput
        -- Update Launch configuration by Source Server ID request.
        sourceServerID: string
        -- Update Launch configuration name request.
        name: string
        -- Update Launch configuration launch disposition request.
        launchDisposition: string
        -- Update Launch configuration Target instance right sizing request.
        targetInstanceTypeRightSizingMethod: string
        -- Update Launch configuration copy Private IP request.
        copyPrivateIp: boolean
        -- Update Launch configuration copy Tags request.
        copyTags: boolean
        -- Update Launch configuration licensing request.
        licensing: Licensing
        -- Update Launch configuration boot mode request.
        bootMode: string
        postLaunchActions: PostLaunchActions
        -- Enable map auto tagging.
        enableMapAutoTagging: boolean
        -- Launch configuration map auto tagging MPE ID.
        mapAutoTaggingMpeID: string
        -- Update Launch configuration Account ID.
        accountID: string
    end

    record UpdateLaunchConfigurationOutput
        -- Launch configuration Source Server ID.
        sourceServerID: string
        -- Launch configuration name.
        name: string
        -- Launch configuration EC2 Launch template ID.
        ec2LaunchTemplateID: string
        -- Launch disposition for launch configuration.
        launchDisposition: string
        -- Launch configuration Target instance type right sizing method.
        targetInstanceTypeRightSizingMethod: string
        -- Copy Private IP during Launch Configuration.
        copyPrivateIp: boolean
        -- Copy Tags during Launch Configuration.
        copyTags: boolean
        -- Launch configuration OS licensing.
        licensing: Licensing
        -- Launch configuration boot mode.
        bootMode: string
        postLaunchActions: PostLaunchActions
        -- Enable map auto tagging.
        enableMapAutoTagging: boolean
        -- Map auto tagging MPE ID.
        mapAutoTaggingMpeID: string
    end

    record UpdateReplicationConfigurationInput
        -- Update replication configuration Source Server ID request.
        sourceServerID: string
        -- Update replication configuration name request.
        name: string
        -- Update replication configuration Staging Area subnet request.
        stagingAreaSubnetId: string
        -- Update replication configuration associate default Application Migration Service Security group request.
        associateDefaultSecurityGroup: boolean
        -- Update replication configuration Replication Server Security Groups IDs request.
        replicationServersSecurityGroupsIDs: {string}
        -- Update replication configuration Replication Server instance type request.
        replicationServerInstanceType: string
        -- Update replication configuration use dedicated Replication Server request.
        useDedicatedReplicationServer: boolean
        -- Update replication configuration use default large Staging Disk type request.
        defaultLargeStagingDiskType: string
        -- Update replication configuration replicated disks request.
        replicatedDisks: {ReplicationConfigurationReplicatedDisk}
        -- Update replication configuration EBS encryption request.
        ebsEncryption: string
        -- Update replication configuration EBS encryption key ARN request.
        ebsEncryptionKeyArn: string
        -- Update replication configuration bandwidth throttling request.
        bandwidthThrottling: number
        -- Update replication configuration data plane routing request.
        dataPlaneRouting: string
        -- Update replication configuration create Public IP request.
        createPublicIP: boolean
        -- Update replication configuration Staging Area Tags request.
        stagingAreaTags: {string : string}
        -- Update replication configuration use Fips Endpoint.
        useFipsEndpoint: boolean
        -- Update replication configuration Account ID request.
        accountID: string
        -- Update replication configuration internet protocol.
        internetProtocol: string
        -- Update replication configuration store snapshot on local zone.
        storeSnapshotOnLocalZone: boolean
        -- Update replication configuration storage configuration.
        storageConfiguration: StorageConfiguration
    end

    record UpdateReplicationConfigurationOutput
        -- Replication Configuration Source Server ID.
        sourceServerID: string
        -- Replication Configuration name.
        name: string
        -- Replication Configuration Staging Area subnet ID.
        stagingAreaSubnetId: string
        -- Replication Configuration associate default Application Migration Service Security Group.
        associateDefaultSecurityGroup: boolean
        -- Replication Configuration Replication Server Security Group IDs.
        replicationServersSecurityGroupsIDs: {string}
        -- Replication Configuration Replication Server instance type.
        replicationServerInstanceType: string
        -- Replication Configuration use Dedicated Replication Server.
        useDedicatedReplicationServer: boolean
        -- Replication Configuration use default large Staging Disks.
        defaultLargeStagingDiskType: string
        -- Replication Configuration replicated disks.
        replicatedDisks: {ReplicationConfigurationReplicatedDisk}
        -- Replication Configuration EBS encryption.
        ebsEncryption: string
        -- Replication Configuration EBS encryption key ARN.
        ebsEncryptionKeyArn: string
        -- Replication Configuration set bandwidth throttling.
        bandwidthThrottling: number
        -- Replication Configuration data plane routing.
        dataPlaneRouting: string
        -- Replication Configuration create Public IP.
        createPublicIP: boolean
        -- Replication Configuration Staging Area tags.
        stagingAreaTags: {string : string}
        -- Replication Configuration use Fips Endpoint.
        useFipsEndpoint: boolean
        -- Replication Configuration internet protocol.
        internetProtocol: string
        -- Replication Configuration store snapshot on local zone.
        storeSnapshotOnLocalZone: boolean
        -- Replication Configuration storage configuration.
        storageConfiguration: StorageConfiguration
    end

    record UpdateSourceServerInput
        -- Update Source Server request account ID.
        accountID: string
        -- Update Source Server request source server ID.
        sourceServerID: string
        -- Update Source Server request connector action.
        connectorAction: SourceServerConnectorAction
        -- Update Source Server request user provided ID.
        userProvidedID: string
        -- Update Source Server request FQDN for action framework.
        fqdnForActionFramework: string
        -- Update Source Server request platform operating system.
        platform: string
    end

    record UpdateSourceServerOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    record UpdateSourceServerReplicationTypeInput
        -- ID of source server on which to update replication type.
        sourceServerID: string
        -- Replication type to which to update source server.
        replicationType: string
        -- Account ID on which to update replication type.
        accountID: string
    end

    record UpdateSourceServerReplicationTypeOutput
        -- Source server ID.
        sourceServerID: string
        -- Source server ARN.
        arn: string
        -- Source server archived status.
        isArchived: boolean
        -- Source server Tags.
        tags: {string : string}
        -- Source server launched instance.
        launchedInstance: LaunchedInstance
        -- Source server data replication info.
        dataReplicationInfo: DataReplicationInfo
        -- Source server lifecycle state.
        lifeCycle: LifeCycle
        -- Source server properties.
        sourceProperties: SourceProperties
        -- Source server replication type.
        replicationType: string
        -- Source server vCenter client id.
        vcenterClientID: string
        -- Source server application ID.
        applicationID: string
        -- Source server user provided ID.
        userProvidedID: string
        -- Source server fqdn for action framework.
        fqdnForActionFramework: string
        -- Source Server connector action.
        connectorAction: SourceServerConnectorAction
    end

    enum IpAssignmentStrategy
        "STATIC"
        "DYNAMIC"
    end

    -- S3 configuration for the source import file to be enriched.
    record EnrichmentSourceS3Configuration
        -- The name of the S3 bucket containing the source import file.
        s3Bucket: string
        -- The AWS account ID of the S3 bucket owner.
        s3BucketOwner: string
        -- The S3 key (path) for the source import file.
        s3Key: string
    end

    record StartImportFileEnrichmentInput
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
        clientToken: string
        -- The S3 configuration specifying the source location of the import file to be enriched.
        s3BucketSource: EnrichmentSourceS3Configuration
        -- The S3 configuration specifying the target location where the enriched import file will be stored.
        s3BucketTarget: EnrichmentTargetS3Configuration
        -- The IP assignment strategy to use when enriching the import file. Can be STATIC or DYNAMIC.
        ipAssignmentStrategy: string
    end

    record StartImportFileEnrichmentOutput
        -- The unique identifier of the import file enrichment job that was started.
        jobID: string
    end

    record TagResourceInput
        -- Tag resource by ARN.
        resourceArn: string
        -- Tag resource by Tags.
        tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- Untag resource by ARN.
        resourceArn: string
        -- Untag resource by Keys.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    record DeleteVcenterClientInput
        -- ID of resource to be deleted.
        vcenterClientID: string
    end

    record DeleteVcenterClientOutput
    end

    record DescribeVcenterClientsInput
        -- Maximum results to be returned in DescribeVcenterClients.
        maxResults: number
        -- Next pagination token to be provided for DescribeVcenterClients.
        nextToken: string
    end

    -- vCenter client.
    record VcenterClient
        -- ID of vCenter client.
        vcenterClientID: string
        -- Arn of vCenter client.
        arn: string
        -- Hostname of vCenter client .
        hostname: string
        -- Vcenter UUID of vCenter client.
        vcenterUUID: string
        -- Datacenter name of vCenter client.
        datacenterName: string
        -- Last seen time of vCenter client.
        lastSeenDatetime: string
        -- Tags for Source Server of vCenter client.
        sourceServerTags: {string : string}
        -- Tags for vCenter client.
        tags: {string : string}
    end

    record DescribeVcenterClientsOutput
        -- List of items returned by DescribeVcenterClients.
        items: {VcenterClient}
        -- Next pagination token returned from DescribeVcenterClients.
        nextToken: string
    end

    record ArchiveWaveInput
        -- Wave ID.
        waveID: string
        -- Account ID.
        accountID: string
    end

    enum WaveHealthStatus
        "HEALTHY"
        "LAGGING"
        "ERROR"
    end

    enum WaveProgressStatus
        "NOT_STARTED"
        "IN_PROGRESS"
        "COMPLETED"
    end

    -- Wave aggregated status.
    record WaveAggregatedStatus
        -- Wave aggregated status last update dateTime.
        lastUpdateDateTime: string
        -- DateTime marking when the first source server in the wave started replication.
        replicationStartedDateTime: string
        -- Wave aggregated status health status.
        healthStatus: string
        -- Wave aggregated status progress status.
        progressStatus: string
        -- Wave aggregated status total applications amount.
        totalApplications: number
    end

    record ArchiveWaveOutput
        -- Wave ID.
        waveID: string
        -- Wave ARN.
        arn: string
        -- Wave name.
        name: string
        -- Wave description.
        description: string
        -- Wave archival status.
        isArchived: boolean
        -- Wave aggregated status.
        waveAggregatedStatus: WaveAggregatedStatus
        -- Wave creation dateTime.
        creationDateTime: string
        -- Wave last modified dateTime.
        lastModifiedDateTime: string
        -- Wave tags.
        tags: {string : string}
    end

    record AssociateApplicationsInput
        -- Wave ID.
        waveID: string
        -- Application IDs list.
        applicationIDs: {string}
        -- Account ID.
        accountID: string
    end

    record AssociateApplicationsOutput
    end

    record CreateWaveInput
        -- Wave name.
        name: string
        -- Wave description.
        description: string
        -- Wave tags.
        tags: {string : string}
        -- Account ID.
        accountID: string
    end

    record CreateWaveOutput
        -- Wave ID.
        waveID: string
        -- Wave ARN.
        arn: string
        -- Wave name.
        name: string
        -- Wave description.
        description: string
        -- Wave archival status.
        isArchived: boolean
        -- Wave aggregated status.
        waveAggregatedStatus: WaveAggregatedStatus
        -- Wave creation dateTime.
        creationDateTime: string
        -- Wave last modified dateTime.
        lastModifiedDateTime: string
        -- Wave tags.
        tags: {string : string}
    end

    record DeleteWaveInput
        -- Wave ID.
        waveID: string
        -- Account ID.
        accountID: string
    end

    record DeleteWaveOutput
    end

    record DisassociateApplicationsInput
        -- Wave ID.
        waveID: string
        -- Application IDs list.
        applicationIDs: {string}
        -- Account ID.
        accountID: string
    end

    record DisassociateApplicationsOutput
    end

    -- Waves list filters.
    record ListWavesRequestFilters
        -- Filter waves list by wave ID.
        waveIDs: {string}
        -- Filter waves list by archival status.
        isArchived: boolean
    end

    record ListWavesInput
        -- Waves list filters.
        filters: ListWavesRequestFilters
        -- Maximum results to return when listing waves.
        maxResults: number
        -- Request next token.
        nextToken: string
        -- Request account ID.
        accountID: string
    end

    record Wave
        -- Wave ID.
        waveID: string
        -- Wave ARN.
        arn: string
        -- Wave name.
        name: string
        -- Wave description.
        description: string
        -- Wave archival status.
        isArchived: boolean
        -- Wave aggregated status.
        waveAggregatedStatus: WaveAggregatedStatus
        -- Wave creation dateTime.
        creationDateTime: string
        -- Wave last modified dateTime.
        lastModifiedDateTime: string
        -- Wave tags.
        tags: {string : string}
    end

    record ListWavesOutput
        -- Waves list.
        items: {Wave}
        -- Response next token.
        nextToken: string
    end

    record UnarchiveWaveInput
        -- Wave ID.
        waveID: string
        -- Account ID.
        accountID: string
    end

    record UnarchiveWaveOutput
        -- Wave ID.
        waveID: string
        -- Wave ARN.
        arn: string
        -- Wave name.
        name: string
        -- Wave description.
        description: string
        -- Wave archival status.
        isArchived: boolean
        -- Wave aggregated status.
        waveAggregatedStatus: WaveAggregatedStatus
        -- Wave creation dateTime.
        creationDateTime: string
        -- Wave last modified dateTime.
        lastModifiedDateTime: string
        -- Wave tags.
        tags: {string : string}
    end

    record UpdateWaveInput
        -- Wave ID.
        waveID: string
        -- Wave name.
        name: string
        -- Wave description.
        description: string
        -- Account ID.
        accountID: string
    end

    record UpdateWaveOutput
        -- Wave ID.
        waveID: string
        -- Wave ARN.
        arn: string
        -- Wave name.
        name: string
        -- Wave description.
        description: string
        -- Wave archival status.
        isArchived: boolean
        -- Wave aggregated status.
        waveAggregatedStatus: WaveAggregatedStatus
        -- Wave creation dateTime.
        creationDateTime: string
        -- Wave last modified dateTime.
        lastModifiedDateTime: string
        -- Wave tags.
        tags: {string : string}
    end

end

return M
