-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    record DeleteHumanLoopInput
        -- The name of the human loop that you want to delete.
        HumanLoopName: string
    end

    record DeleteHumanLoopOutput
    end

    -- We couldn't process your request because of an issue with the server. Try again
    -- later.
    record InternalServerException
        Message: string
    end

    -- We couldn't find the requested resource. Check that your resources exists and were created
    -- in the same AWS Region as your request, and try your request again.
    record ResourceNotFoundException
        Message: string
    end

    -- You exceeded
    -- the
    -- maximum number of requests.
    record ThrottlingException
        Message: string
    end

    -- The
    -- request isn't valid. Check the syntax and try again.
    record ValidationException
        Message: string
    end

    record DescribeHumanLoopInput
        -- The name of the human loop that you want information about.
        HumanLoopName: string
    end

    -- Information about where the human output will be stored.
    record HumanLoopOutput
        -- The location of the Amazon S3 object where Amazon Augmented AI stores your human loop output.
        OutputS3Uri: string
    end

    enum HumanLoopStatus
        "InProgress"
        "Failed"
        "Completed"
        "Stopped"
        "Stopping"
    end

    record DescribeHumanLoopOutput
        -- The creation time when Amazon Augmented AI created the human loop.
        CreationTime: number
        -- The reason why a human loop failed. The failure reason is returned when the status of the
        -- human loop is Failed.
        FailureReason: string
        -- A failure code that identifies the type of failure.
        -- Possible values: ValidationError, Expired,
        -- InternalError
        FailureCode: string
        -- The status of the human loop.
        HumanLoopStatus: string
        -- The name of the human loop. The name must be lowercase, unique within the Region in your
        -- account, and can have up to 63 characters. Valid characters: a-z, 0-9, and - (hyphen).
        HumanLoopName: string
        -- The Amazon Resource Name (ARN) of the human loop.
        HumanLoopArn: string
        -- The Amazon Resource Name (ARN) of the flow definition.
        FlowDefinitionArn: string
        -- An object that contains information about the output of the human loop.
        HumanLoopOutput: HumanLoopOutput
    end

    enum SortOrder
        "Ascending"
        "Descending"
    end

    record ListHumanLoopsInput
        -- (Optional) The timestamp of the date when you want the human loops to begin in ISO 8601 format. For example, 2020-02-24.
        CreationTimeAfter: number
        -- (Optional) The timestamp of the date before which you want the human loops to begin in ISO 8601 format. For example, 2020-02-24.
        CreationTimeBefore: number
        -- The Amazon Resource Name (ARN) of a flow definition.
        FlowDefinitionArn: string
        -- Optional. The order for displaying results. Valid values: Ascending and
        -- Descending.
        SortOrder: string
        -- A token to display the next page of results.
        NextToken: string
        -- The total number of items to return. If the total number of available items is more than
        -- the value specified in MaxResults, then a NextToken is returned in
        -- the output. You can use this token to display the next page of results.
        MaxResults: number
    end

    -- Summary information about the human loop.
    record HumanLoopSummary
        -- The name of the human loop.
        HumanLoopName: string
        -- The status of the human loop.
        HumanLoopStatus: string
        -- When Amazon Augmented AI created the human loop.
        CreationTime: number
        -- The reason why the human loop failed. A failure reason is returned when the status of the
        -- human loop is Failed.
        FailureReason: string
        -- The Amazon Resource Name (ARN) of the flow definition used to configure the human
        -- loop.
        FlowDefinitionArn: string
    end

    record ListHumanLoopsOutput
        -- An array of objects that contain information about the human loops.
        HumanLoopSummaries: {HumanLoopSummary}
        -- A token to display the next page of results.
        NextToken: string
    end

    -- Your request has the same name as another active human loop but has different input data. You cannot start two
    -- human loops with the same name and different input data.
    record ConflictException
        Message: string
    end

    -- You exceeded your service quota. Service quotas, also referred to as limits, are the
    -- maximum number of service resources or operations for your AWS account. For a list of
    -- Amazon A2I service quotes, see Amazon Augmented AI Service Quotes. Delete some resources or request an increase in your
    -- service quota. You can request a quota increase using Service Quotas or the AWS Support
    -- Center. To request an increase, see AWS Service Quotas in the
    -- AWS General Reference.
    record ServiceQuotaExceededException
        Message: string
    end

    enum ContentClassifier
        "FreeOfPersonallyIdentifiableInformation"
        "FreeOfAdultContent"
    end

    -- Attributes of the data specified by the customer. Use these to describe the data to be labeled.
    record HumanLoopDataAttributes
        -- Declares that your content is free of personally identifiable information or adult content.
        -- Amazon SageMaker can restrict the Amazon Mechanical Turk workers who can view your task based on this information.
        ContentClassifiers: {string}
    end

    -- An object containing the human loop input in JSON format.
    record HumanLoopInput
        -- Serialized input from the human loop. The input must be a string representation of a file in JSON format.
        InputContent: string
    end

    record StartHumanLoopInput
        -- The name of the human loop.
        HumanLoopName: string
        -- The Amazon Resource Name (ARN) of the flow definition associated with this human
        -- loop.
        FlowDefinitionArn: string
        -- An object that contains information about the human loop.
        HumanLoopInput: HumanLoopInput
        -- Attributes of the specified data. Use DataAttributes to specify if your data
        -- is free of personally identifiable information and/or free of adult content.
        DataAttributes: HumanLoopDataAttributes
    end

    record StartHumanLoopOutput
        -- The Amazon Resource Name (ARN) of the human loop.
        HumanLoopArn: string
    end

    record StopHumanLoopInput
        -- The name of the human loop that you want to stop.
        HumanLoopName: string
    end

    record StopHumanLoopOutput
    end

end

return M
