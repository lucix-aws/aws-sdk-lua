-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- You do not have sufficient access to perform this action.
    record AccessDeniedException
        message: string
        xAmzErrorType: string
    end

    enum AgentAction
        "DISCARD"
    end

    -- Agentless config
    record AgentlessConfig
    end

    -- The request could not be processed because of conflict in the current state of the resource.
    record ConflictException
        message: string
        xAmzErrorType: string
    end

    -- Default Email Outbound config
    record EmailOutboundConfig
        connectSourceEmailAddress: string
        sourceEmailAddressDisplayName: string
        wisdomTemplateArn: string
    end

    -- Email Outbound Mode
    record EmailOutboundMode
        agentless: AgentlessConfig
    end

    -- Email Channel Subtype config
    record EmailChannelSubtypeConfig
        capacity: number
        outboundMode: EmailOutboundMode
        defaultOutboundConfig: EmailOutboundConfig
    end

    -- Default SMS Outbound config
    record SmsOutboundConfig
        connectSourcePhoneNumberArn: string
        wisdomTemplateArn: string
    end

    -- SMS Outbound Mode
    record SmsOutboundMode
        agentless: AgentlessConfig
    end

    -- SMS Channel Subtype config
    record SmsChannelSubtypeConfig
        capacity: number
        outboundMode: SmsOutboundMode
        defaultOutboundConfig: SmsOutboundConfig
    end

    -- Answering Machine Detection config
    record AnswerMachineDetectionConfig
        -- Enable or disable answering machine detection
        enableAnswerMachineDetection: boolean
        -- Enable or disable await answer machine prompt
        awaitAnswerMachinePrompt: boolean
    end

    -- Default Telephony Outbound config
    record TelephonyOutboundConfig
        connectContactFlowId: string
        connectSourcePhoneNumber: string
        answerMachineDetectionConfig: AnswerMachineDetectionConfig
        ringTimeout: number
    end

    -- Predictive config
    record PredictiveConfig
        bandwidthAllocation: number
    end

    -- Timeout Config for preview contacts.
    record TimeoutConfig
        durationInSeconds: number
    end

    -- Preview config
    record PreviewConfig
        bandwidthAllocation: number
        timeoutConfig: TimeoutConfig
        agentActions: {string}
    end

    -- Progressive config
    record ProgressiveConfig
        bandwidthAllocation: number
    end

    -- Telephony Outbound Mode
    record TelephonyOutboundMode
        progressive: ProgressiveConfig
        predictive: PredictiveConfig
        agentless: AgentlessConfig
        preview: PreviewConfig
    end

    -- Telephony Channel Subtype config
    record TelephonyChannelSubtypeConfig
        capacity: number
        connectQueueId: string
        outboundMode: TelephonyOutboundMode
        defaultOutboundConfig: TelephonyOutboundConfig
    end

    -- Default WhatsApp Outbound config
    record WhatsAppOutboundConfig
        connectSourcePhoneNumberArn: string
        wisdomTemplateArn: string
    end

    -- WhatsApp Outbound Mode
    record WhatsAppOutboundMode
        agentless: AgentlessConfig
    end

    -- WhatsApp Channel Subtype config
    record WhatsAppChannelSubtypeConfig
        capacity: number
        outboundMode: WhatsAppOutboundMode
        defaultOutboundConfig: WhatsAppOutboundConfig
    end

    -- Campaign Channel Subtype config
    record ChannelSubtypeConfig
        telephony: TelephonyChannelSubtypeConfig
        sms: SmsChannelSubtypeConfig
        email: EmailChannelSubtypeConfig
        whatsApp: WhatsAppChannelSubtypeConfig
    end

    enum CommunicationLimitTimeUnit
        "DAY"
    end

    -- Communication Limit
    record CommunicationLimit
        -- Maximum number of contacts allowed for a given target within the given frequency.
        maxCountPerRecipient: number
        -- The number of days to consider with regards to this limit.
        frequency: number
        unit: string
    end

    -- Communication limits
    record CommunicationLimits
        communicationLimitsList: {CommunicationLimit}
    end

    enum InstanceLimitsHandling
        "OPT_IN"
        "OPT_OUT"
    end

    -- Communication limits config
    record CommunicationLimitsConfig
        allChannelSubtypes: CommunicationLimits
        instanceLimitsHandling: string
    end

    enum DayOfWeek
        "MONDAY"
        "TUESDAY"
        "WEDNESDAY"
        "THURSDAY"
        "FRIDAY"
        "SATURDAY"
        "SUNDAY"
    end

    -- Time range in 24 hour format
    record TimeRange
        startTime: string
        endTime: string
    end

    -- Open Hours config
    record OpenHours
        dailyHours: {string : {TimeRange}}
    end

    -- Restricted period
    record RestrictedPeriod
        name: string
        startDate: string
        endDate: string
    end

    -- Restricted period config
    record RestrictedPeriods
        restrictedPeriodList: {RestrictedPeriod}
    end

    -- Time window config
    record TimeWindow
        openHours: OpenHours
        restrictedPeriods: RestrictedPeriods
    end

    enum LocalTimeZoneDetectionType
        "ZIP_CODE"
        "AREA_CODE"
    end

    -- Local time zone config
    record LocalTimeZoneConfig
        defaultTimeZone: string
        localTimeZoneDetection: {string}
    end

    -- Campaign communication time config
    record CommunicationTimeConfig
        localTimeZoneConfig: LocalTimeZoneConfig
        telephony: TimeWindow
        sms: TimeWindow
        email: TimeWindow
        whatsApp: TimeWindow
    end

    -- Campaign entry limits config
    record EntryLimitsConfig
        -- Maximum number of times a participant can enter the campaign. A value of 0 indicates unlimited entries. Values of 1 or greater specify the exact number of entries allowed.
        maxEntryCount: number
        -- Minimum time interval that must pass before a participant can enter the campaign again.
        minEntryInterval: string
    end

    -- Campaign schedule
    record Schedule
        startTime: number
        endTime: number
        refreshFrequency: string
    end

    -- Event trigger of the campaign
    record EventTrigger
        customerProfilesDomainArn: string
    end

    -- Source of the campaign
    record Source
        customerProfilesSegmentArn: string
        eventTrigger: EventTrigger
    end

    enum ExternalCampaignType
        "MANAGED"
        "JOURNEY"
    end

    -- The request for CreateCampaign API.
    record CreateCampaignInput
        name: string
        connectInstanceId: string
        channelSubtypeConfig: ChannelSubtypeConfig
        source: Source
        connectCampaignFlowArn: string
        schedule: Schedule
        entryLimitsConfig: EntryLimitsConfig
        communicationTimeConfig: CommunicationTimeConfig
        communicationLimitsOverride: CommunicationLimitsConfig
        tags: {string : string}
    end

    -- The response for Create Campaign API
    record CreateCampaignOutput
        id: string
        arn: string
        tags: {string : string}
    end

    -- Request processing failed because of an error or failure with the service.
    record InternalServerException
        message: string
        xAmzErrorType: string
    end

    -- The specified resource was not found.
    record ResourceNotFoundException
        message: string
        xAmzErrorType: string
    end

    -- Request would cause a service quota to be exceeded.
    record ServiceQuotaExceededException
        message: string
        xAmzErrorType: string
    end

    -- The request was denied due to request throttling.
    record ThrottlingException
        message: string
        xAmzErrorType: string
    end

    -- The input fails to satisfy the constraints specified by an AWS service.
    record ValidationException
        message: string
        xAmzErrorType: string
    end

    -- The request for DeleteCampaign API.
    record DeleteCampaignInput
        id: string
    end

    record DeleteCampaignOutput
    end

    enum ChannelSubtype
        "TELEPHONY"
        "SMS"
        "EMAIL"
        "WHATSAPP"
    end

    -- The request for DeleteCampaignChannelSubtypeConfig API.
    record DeleteCampaignChannelSubtypeConfigInput
        id: string
        channelSubtype: string
    end

    record DeleteCampaignChannelSubtypeConfigOutput
    end

    enum CommunicationLimitsConfigType
        "ALL_CHANNEL_SUBTYPES"
    end

    -- The request for DeleteCampaignCommunicationLimits API.
    record DeleteCampaignCommunicationLimitsInput
        id: string
        config: string
    end

    record DeleteCampaignCommunicationLimitsOutput
    end

    enum CampaignState
        "Initialized"
        "Running"
        "Paused"
        "Stopped"
        "Failed"
        "Completed"
    end

    -- The request could not be processed because of conflict in the current state of the campaign.
    record InvalidCampaignStateException
        state: string
        message: string
        xAmzErrorType: string
    end

    enum CommunicationTimeConfigType
        "TELEPHONY"
        "SMS"
        "EMAIL"
        "WHATSAPP"
    end

    -- The request for DeleteCampaignCommunicationTime API.
    record DeleteCampaignCommunicationTimeInput
        id: string
        config: string
    end

    record DeleteCampaignCommunicationTimeOutput
    end

    -- The request for DeleteCampaignEntryLimits API.
    record DeleteCampaignEntryLimitsInput
        id: string
    end

    record DeleteCampaignEntryLimitsOutput
    end

    enum CampaignDeletionPolicy
        "RETAIN_ALL"
        "DELETE_ALL"
    end

    -- The request for DeleteConnectInstanceConfig API.
    record DeleteConnectInstanceConfigInput
        connectInstanceId: string
        campaignDeletionPolicy: string
    end

    record DeleteConnectInstanceConfigOutput
    end

    -- The request could not be processed because of conflict in the current state.
    record InvalidStateException
        message: string
        xAmzErrorType: string
    end

    -- Customer Profiles integration identifier
    record CustomerProfilesIntegrationIdentifier
        domainArn: string
    end

    -- Lambda integration identifier
    record LambdaIntegrationIdentifier
        functionArn: string
    end

    -- Q Connect integration identifier
    record QConnectIntegrationIdentifier
        knowledgeBaseArn: string
    end

    -- Integration identifier for Connect instance
    record IntegrationIdentifier
        customerProfiles: CustomerProfilesIntegrationIdentifier
        qConnect: QConnectIntegrationIdentifier
        lambda: LambdaIntegrationIdentifier
    end

    -- The request for DeleteConnectInstanceIntegration API.
    record DeleteConnectInstanceIntegrationInput
        connectInstanceId: string
        integrationIdentifier: IntegrationIdentifier
    end

    record DeleteConnectInstanceIntegrationOutput
    end

    -- The request for DeleteInstanceOnboardingJob API.
    record DeleteInstanceOnboardingJobInput
        connectInstanceId: string
    end

    record DeleteInstanceOnboardingJobOutput
    end

    -- The request for DescribeCampaign API.
    record DescribeCampaignInput
        id: string
    end

    -- An Amazon Connect campaign.
    record Campaign
        id: string
        arn: string
        name: string
        connectInstanceId: string
        channelSubtypeConfig: ChannelSubtypeConfig
        source: Source
        connectCampaignFlowArn: string
        schedule: Schedule
        entryLimitsConfig: EntryLimitsConfig
        communicationTimeConfig: CommunicationTimeConfig
        communicationLimitsOverride: CommunicationLimitsConfig
        tags: {string : string}
    end

    -- The response for DescribeCampaign API.
    record DescribeCampaignOutput
        campaign: Campaign
    end

    -- The request for GetCampaignState API.
    record GetCampaignStateInput
        id: string
    end

    -- The response for GetCampaignState API.
    record GetCampaignStateOutput
        state: string
    end

    -- The request for GetCampaignStateBatch API.
    record GetCampaignStateBatchInput
        campaignIds: {string}
    end

    enum GetCampaignStateBatchFailureCode
        "ResourceNotFound"
        "UnknownError"
    end

    -- Failed response of campaign state
    record FailedCampaignStateResponse
        campaignId: string
        failureCode: string
    end

    -- Successful response of campaign state
    record SuccessfulCampaignStateResponse
        campaignId: string
        state: string
    end

    -- The response for GetCampaignStateBatch API.
    record GetCampaignStateBatchOutput
        successfulRequests: {SuccessfulCampaignStateResponse}
        failedRequests: {FailedCampaignStateResponse}
    end

    -- The request for GetConnectInstanceConfig API.
    record GetConnectInstanceConfigInput
        connectInstanceId: string
    end

    enum EncryptionType
        "KMS"
    end

    -- Encryption config for Connect Instance. Note that sensitive data will always be encrypted.
    -- If disabled, service will perform encryption with its own key.
    -- If enabled, a KMS key id needs to be provided and KMS charges will apply.
    -- KMS is only type supported
    record EncryptionConfig
        enabled: boolean
        encryptionType: string
        keyArn: string
    end

    -- Instance config object
    record InstanceConfig
        connectInstanceId: string
        serviceLinkedRoleArn: string
        encryptionConfig: EncryptionConfig
    end

    -- The response for GetConnectInstanceConfig API.
    record GetConnectInstanceConfigOutput
        connectInstanceConfig: InstanceConfig
    end

    -- The request for GetInstanceCommunicationLimits API.
    record GetInstanceCommunicationLimitsInput
        connectInstanceId: string
    end

    -- Instance Communication limits config
    record InstanceCommunicationLimitsConfig
        allChannelSubtypes: CommunicationLimits
    end

    -- The response for GetInstanceCommunicationLimits API.
    record GetInstanceCommunicationLimitsOutput
        communicationLimitsConfig: InstanceCommunicationLimitsConfig
    end

    -- The request for GetInstanceOnboardingJobStatus API.
    record GetInstanceOnboardingJobStatusInput
        connectInstanceId: string
    end

    enum InstanceOnboardingJobFailureCode
        "EVENT_BRIDGE_ACCESS_DENIED"
        "EVENT_BRIDGE_MANAGED_RULE_LIMIT_EXCEEDED"
        "IAM_ACCESS_DENIED"
        "KMS_ACCESS_DENIED"
        "KMS_KEY_NOT_FOUND"
        "INTERNAL_FAILURE"
    end

    enum InstanceOnboardingJobStatusCode
        "IN_PROGRESS"
        "SUCCEEDED"
        "FAILED"
    end

    -- Instance onboarding job status object
    record InstanceOnboardingJobStatus
        connectInstanceId: string
        status: string
        failureCode: string
    end

    -- The response for GetInstanceOnboardingJobStatus API.
    record GetInstanceOnboardingJobStatusOutput
        connectInstanceOnboardingJobStatus: InstanceOnboardingJobStatus
    end

    enum InstanceIdFilterOperator
        "Eq"
    end

    -- Connect instance identifier filter
    record InstanceIdFilter
        value: string
        operator: string
    end

    -- Filter model by type
    record CampaignFilters
        instanceIdFilter: InstanceIdFilter
    end

    -- The request for ListCampaigns API.
    record ListCampaignsInput
        maxResults: number
        nextToken: string
        filters: CampaignFilters
    end

    -- An Amazon Connect campaign summary.
    record CampaignSummary
        id: string
        arn: string
        name: string
        connectInstanceId: string
        channelSubtypes: {string}
        schedule: Schedule
        entryLimitsConfig: EntryLimitsConfig
        connectCampaignFlowArn: string
    end

    -- The response for ListCampaigns API.
    record ListCampaignsOutput
        nextToken: string
        campaignSummaryList: {CampaignSummary}
    end

    -- The request for ListConnectInstanceIntegrations API.
    record ListConnectInstanceIntegrationsInput
        connectInstanceId: string
        maxResults: number
        nextToken: string
    end

    enum EventType
        "Campaign-Email"
        "Campaign-SMS"
        "Campaign-Telephony"
        "Campaign-Orchestration"
        "Campaign-WhatsApp"
    end

    -- Customer Profiles integration summary
    record CustomerProfilesIntegrationSummary
        domainArn: string
        objectTypeNames: {string : string}
    end

    -- Lambda integration summary
    record LambdaIntegrationSummary
        functionArn: string
    end

    -- Q Connect integration summary
    record QConnectIntegrationSummary
        knowledgeBaseArn: string
    end

    -- Integration summary for Connect instance.
    record IntegrationSummary
        customerProfiles: CustomerProfilesIntegrationSummary
        qConnect: QConnectIntegrationSummary
        lambda: LambdaIntegrationSummary
    end

    -- The response for ListConnectInstanceIntegrations API.
    record ListConnectInstanceIntegrationsOutput
        nextToken: string
        integrationSummaryList: {IntegrationSummary}
    end

    -- The request for ListTagsForResource API.
    record ListTagsForResourceInput
        arn: string
    end

    -- The request for ListTagsForResource API.
    record ListTagsForResourceOutput
        tags: {string : string}
    end

    -- The request for PauseCampaign API.
    record PauseCampaignInput
        id: string
    end

    record PauseCampaignOutput
    end

    -- Customer Profiles integration config
    record CustomerProfilesIntegrationConfig
        domainArn: string
        objectTypeNames: {string : string}
    end

    -- Lambda integration config
    record LambdaIntegrationConfig
        functionArn: string
    end

    -- Q Connect integration config
    record QConnectIntegrationConfig
        knowledgeBaseArn: string
    end

    -- Integration config for Connect Instance
    record IntegrationConfig
        customerProfiles: CustomerProfilesIntegrationConfig
        qConnect: QConnectIntegrationConfig
        lambda: LambdaIntegrationConfig
    end

    -- The request for PutConnectInstanceIntegration API.
    record PutConnectInstanceIntegrationInput
        connectInstanceId: string
        integrationConfig: IntegrationConfig
    end

    record PutConnectInstanceIntegrationOutput
    end

    -- The request for PutInstanceCommunicationLimits API.
    record PutInstanceCommunicationLimitsInput
        connectInstanceId: string
        communicationLimitsConfig: InstanceCommunicationLimitsConfig
    end

    record PutInstanceCommunicationLimitsOutput
    end

    -- Parameters for the Email Channel Subtype
    record EmailChannelSubtypeParameters
        destinationEmailAddress: string
        connectSourceEmailAddress: string
        templateArn: string
        templateParameters: {string : string}
    end

    -- Parameters for the SMS Channel Subtype
    record SmsChannelSubtypeParameters
        destinationPhoneNumber: string
        connectSourcePhoneNumberArn: string
        templateArn: string
        templateParameters: {string : string}
    end

    -- Parameters for the Telephony Channel Subtype
    record TelephonyChannelSubtypeParameters
        destinationPhoneNumber: string
        attributes: {string : string}
        connectSourcePhoneNumber: string
        answerMachineDetectionConfig: AnswerMachineDetectionConfig
        ringTimeout: number
    end

    -- Parameters for the WhatsApp Channel Subtype
    record WhatsAppChannelSubtypeParameters
        destinationPhoneNumber: string
        connectSourcePhoneNumberArn: string
        templateArn: string
        templateParameters: {string : string}
    end

    -- ChannelSubtypeParameters for an outbound request
    record ChannelSubtypeParameters
        telephony: TelephonyChannelSubtypeParameters
        sms: SmsChannelSubtypeParameters
        email: EmailChannelSubtypeParameters
        whatsApp: WhatsAppChannelSubtypeParameters
    end

    -- An outbound request for a campaign.
    record OutboundRequest
        clientToken: string
        expirationTime: number
        channelSubtypeParameters: ChannelSubtypeParameters
    end

    -- The request for PutOutboundRequestBatch API.
    record PutOutboundRequestBatchInput
        id: string
        outboundRequests: {OutboundRequest}
    end

    enum FailureCode
        "InvalidInput"
        "RequestThrottled"
        "UnknownError"
        "BufferLimitExceeded"
    end

    -- A failed request identified by the unique client token.
    record FailedRequest
        clientToken: string
        id: string
        failureCode: string
    end

    -- A successful request identified by the unique client token.
    record SuccessfulRequest
        clientToken: string
        id: string
    end

    -- The response for PutOutboundRequestBatch API.
    record PutOutboundRequestBatchOutput
        successfulRequests: {SuccessfulRequest}
        failedRequests: {FailedRequest}
    end

    -- Information about a profile outbound request
    record ProfileOutboundRequest
        clientToken: string
        profileId: string
        expirationTime: number
    end

    -- The request for PutProfileOutboundRequestBatch API
    record PutProfileOutboundRequestBatchInput
        id: string
        profileOutboundRequests: {ProfileOutboundRequest}
    end

    enum ProfileOutboundRequestFailureCode
        "UnknownError"
        "ResourceNotFound"
        "Conflict"
        "RequestThrottled"
        "InvalidInput"
    end

    -- Failure details for a profile outbound request
    record FailedProfileOutboundRequest
        clientToken: string
        id: string
        failureCode: string
    end

    -- Success details for a profile outbound request
    record SuccessfulProfileOutboundRequest
        clientToken: string
        id: string
    end

    -- The response for PutProfileOutboundRequestBatch API
    record PutProfileOutboundRequestBatchOutput
        successfulRequests: {SuccessfulProfileOutboundRequest}
        failedRequests: {FailedProfileOutboundRequest}
    end

    -- The request for ResumeCampaign API.
    record ResumeCampaignInput
        id: string
    end

    record ResumeCampaignOutput
    end

    -- The request for StartCampaign API.
    record StartCampaignInput
        id: string
    end

    record StartCampaignOutput
    end

    -- The request for StartInstanceOnboardingJob API.
    record StartInstanceOnboardingJobInput
        connectInstanceId: string
        encryptionConfig: EncryptionConfig
    end

    -- The response for StartInstanceOnboardingJob API.
    record StartInstanceOnboardingJobOutput
        connectInstanceOnboardingJobStatus: InstanceOnboardingJobStatus
    end

    -- The request for StopCampaign API.
    record StopCampaignInput
        id: string
    end

    record StopCampaignOutput
    end

    -- The request for TagResource API.
    record TagResourceInput
        arn: string
        tags: {string : string}
    end

    record TagResourceOutput
    end

    -- The request for UntagResource API.
    record UntagResourceInput
        arn: string
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    -- The request for UpdateCampaignChannelSubtypeConfig API.
    record UpdateCampaignChannelSubtypeConfigInput
        id: string
        channelSubtypeConfig: ChannelSubtypeConfig
    end

    record UpdateCampaignChannelSubtypeConfigOutput
    end

    -- The request for UpdateCampaignCommunicationLimits API.
    record UpdateCampaignCommunicationLimitsInput
        id: string
        communicationLimitsOverride: CommunicationLimitsConfig
    end

    record UpdateCampaignCommunicationLimitsOutput
    end

    -- The request for UpdateCampaignCommunicationTime API.
    record UpdateCampaignCommunicationTimeInput
        id: string
        communicationTimeConfig: CommunicationTimeConfig
    end

    record UpdateCampaignCommunicationTimeOutput
    end

    -- The request for UpdateCampaignEntryLimits API.
    record UpdateCampaignEntryLimitsInput
        id: string
        entryLimitsConfig: EntryLimitsConfig
    end

    record UpdateCampaignEntryLimitsOutput
    end

    -- The request for UpdateCampaignFlowAssociation API.
    record UpdateCampaignFlowAssociationInput
        id: string
        connectCampaignFlowArn: string
    end

    record UpdateCampaignFlowAssociationOutput
    end

    -- The request for UpdateCampaignName API.
    record UpdateCampaignNameInput
        id: string
        name: string
    end

    record UpdateCampaignNameOutput
    end

    -- The request for UpdateCampaignSchedule API.
    record UpdateCampaignScheduleInput
        id: string
        schedule: Schedule
    end

    record UpdateCampaignScheduleOutput
    end

    -- The request for UpdateCampaignSource API.
    record UpdateCampaignSourceInput
        id: string
        source: Source
    end

    record UpdateCampaignSourceOutput
    end

end

return M
