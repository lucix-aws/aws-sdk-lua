-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- You do not have permission to perform this operation.
    record AccessDeniedException
        Message: string
    end

    -- Information about an address.
    record Address
        -- The name of the contact.
        ContactName: string
        -- The phone number of the contact, including the country code (for example,
        -- +12065550100).
        ContactPhoneNumber: string
        -- The first line of the address.
        AddressLine1: string
        -- The second line of the address.
        AddressLine2: string
        -- The third line of the address.
        AddressLine3: string
        -- The city for the address.
        City: string
        -- The state for the address.
        StateOrRegion: string
        -- The district or county for the address.
        DistrictOrCounty: string
        -- The postal code for the address.
        PostalCode: string
        -- The ISO-3166 two-letter country code for the address.
        CountryCode: string
        -- The municipality for the address.
        Municipality: string
    end

    enum AddressType
        "SHIPPING_ADDRESS"
        "OPERATING_ADDRESS"
    end

    -- Information about the position of the asset in a rack.
    record AssetLocation
        -- The position of an asset in a rack measured in rack units.
        RackElevation: number
    end

    enum AssetType
        "COMPUTE"
        "STORAGE"
        "POWERSHELF"
        "SWITCH"
        "NETWORKING"
    end

    -- The capacity for each instance type.
    record AssetInstanceTypeCapacity
        -- The type of instance.
        InstanceType: string
        -- The number of each instance type.
        Count: number
    end

    enum ComputeAssetState
        "ACTIVE"
        "ISOLATED"
        "RETIRING"
        "INSTALLING"
    end

    -- Information about compute hardware assets.
    record ComputeAttributes
        -- The host ID of the Dedicated Host on the asset.
        HostId: string
        -- The state.
        -- ACTIVE - The asset is available and can provide capacity for new compute
        -- resources.
        -- ISOLATED - The asset is undergoing maintenance and can't provide capacity for new
        -- compute resources. Existing compute resources on the asset are not affected.
        -- RETIRING - The underlying hardware for the asset is degraded. Capacity for new compute
        -- resources is reduced. Amazon Web Services sends notifications for resources that must be stopped before
        -- the asset can be replaced.
        -- INSTALLING - The asset is being installed and can't yet provide capacity for new compute
        -- resources.
        State: string
        -- A list of the names of instance families that are currently associated with a given
        -- asset.
        InstanceFamilies: {string}
        -- The instance type capacities configured for this asset. This can be changed through a
        -- capacity task.
        InstanceTypeCapacities: {AssetInstanceTypeCapacity}
        -- The maximum number of vCPUs possible for the specified asset.
        MaxVcpus: number
    end

    -- Information about hardware assets.
    record AssetInfo
        -- The ID of the asset. An Outpost asset can be a single server within an Outposts rack or
        -- an Outposts server configuration.
        AssetId: string
        -- The rack ID of the asset.
        RackId: string
        -- The type of the asset.
        AssetType: string
        -- Information about compute hardware assets.
        ComputeAttributes: ComputeAttributes
        -- The position of an asset in a rack.
        AssetLocation: AssetLocation
    end

    enum AWSServiceName
        "AWS"
        "EC2"
        "EKS"
        "ELASTICACHE"
        "ELB"
        "RDS"
        "ROUTE53"
    end

    -- An Amazon EC2 instance.
    record AssetInstance
        -- The ID of the instance.
        InstanceId: string
        -- The type of instance.
        InstanceType: string
        -- The ID of the asset. An Outpost asset can be a single server within an Outposts rack or an
        -- Outposts server configuration.
        AssetId: string
        AccountId: string
        -- The Amazon Web Services service name of the instance.
        AwsServiceName: string
    end

    enum AssetState
        "ACTIVE"
        "RETIRING"
        "ISOLATED"
        "INSTALLING"
    end

    -- A running Amazon EC2 instance that can be stopped to free up capacity needed to run the
    -- capacity task.
    record BlockingInstance
        -- The ID of the blocking instance.
        InstanceId: string
        AccountId: string
        -- The Amazon Web Services service name that owns the specified blocking instance.
        AwsServiceName: string
    end

    enum BlockingResourceType
        "EC2_INSTANCE"
        "OUTPOST_RAM_SHARE"
        "LGW_ROUTING_DOMAIN"
        "LGW_ROUTE_TABLE"
        "LGW_VIRTUAL_INTERFACE_GROUP"
        "OUTPOST_ORDER_CANCELLABLE"
        "OUTPOST_ORDER_INTERVENTION_REQUIRED"
    end

    record CancelCapacityTaskInput
        -- ID of the capacity task that you want to cancel.
        CapacityTaskId: string
        -- ID or ARN of the Outpost associated with the capacity task that you want to cancel.
        OutpostIdentifier: string
    end

    record CancelCapacityTaskOutput
    end

    enum ResourceType
        "OUTPOST"
        "ORDER"
    end

    -- Updating or deleting this resource can cause an inconsistent state.
    record ConflictException
        Message: string
        -- The ID of the resource causing the conflict.
        ResourceId: string
        -- The type of the resource causing the conflict.
        ResourceType: string
    end

    -- An internal error has occurred.
    record InternalServerException
        Message: string
    end

    -- The specified request is not valid.
    record NotFoundException
        Message: string
    end

    -- A parameter is not valid.
    record ValidationException
        Message: string
    end

    record CancelOrderInput
        -- The ID of the order.
        OrderId: string
    end

    record CancelOrderOutput
    end

    enum QuoteCapacityType
        "EC2"
        "EBS"
        "S3"
    end

    -- A capacity requirement for a quote. Specifies the type of capacity, the unit, and the
    -- quantity.
    record QuoteCapacity
        -- The type of capacity. Valid values are EC2, EBS, and
        -- S3.
        QuoteCapacityType: string
        -- The unit of measurement for the capacity. For Amazon EC2, this is the instance type (for
        -- example, c5.24xlarge). For Amazon EBS and Amazon S3, this is the storage unit (for
        -- example, TiB for tebibytes).
        Unit: string
        -- The quantity of the specified capacity unit. For Amazon EC2, this is the number of
        -- additional instances to add to the Outpost. For Amazon EBS and Amazon S3, this is the total
        -- desired end-state capacity of the Outpost.
        Quantity: number
    end

    -- A summary of the capacity changes for a quote option.
    record CapacitySummary
        -- The existing capacities on the Outpost before the quote is fulfilled.
        ExistingCapacities: {QuoteCapacity}
        -- The final capacities on the Outpost after the quote is fulfilled.
        FinalCapacities: {QuoteCapacity}
        -- The change in capacity between the existing and final state.
        CapacityChange: {QuoteCapacity}
    end

    enum CapacityTaskFailureType
        "UNSUPPORTED_CAPACITY_CONFIGURATION"
        "UNEXPECTED_ASSET_STATE"
        "BLOCKING_INSTANCES_NOT_EVACUATED"
        "INTERNAL_SERVER_ERROR"
        "RESOURCE_NOT_FOUND"
    end

    -- The capacity tasks that failed.
    record CapacityTaskFailure
        -- The reason that the specified capacity task failed.
        Reason: string
        -- The type of failure.
        Type: string
    end

    enum CapacityTaskStatus
        "REQUESTED"
        "IN_PROGRESS"
        "FAILED"
        "COMPLETED"
        "WAITING_FOR_EVACUATION"
        "CANCELLATION_IN_PROGRESS"
        "CANCELLED"
    end

    -- The summary of the capacity task.
    record CapacityTaskSummary
        -- The ID of the specified capacity task.
        CapacityTaskId: string
        -- The ID of the Outpost associated with the specified capacity task.
        OutpostId: string
        -- The ID of the Amazon Web Services Outposts order of the host associated with the capacity task.
        OrderId: string
        -- The ID of the asset. An Outpost asset can be a single server within an Outposts rack or an
        -- Outposts server configuration.
        AssetId: string
        -- The status of the capacity task.
        CapacityTaskStatus: string
        -- The date that the specified capacity task was created.
        CreationDate: number
        -- The date that the specified capacity task successfully ran.
        CompletionDate: number
        -- The date that the specified capacity was last modified.
        LastModifiedDate: number
    end

    -- Information about EC2 capacity.
    record EC2Capacity
        -- The family of the EC2 capacity.
        Family: string
        -- The maximum size of the EC2 capacity.
        MaxSize: string
        -- The quantity of the EC2 capacity.
        Quantity: string
    end

    enum CatalogItemStatus
        "AVAILABLE"
        "DISCONTINUED"
    end

    enum SupportedStorageEnum
        "EBS"
        "S3"
    end

    -- Information about a catalog item.
    record CatalogItem
        -- The ID of the catalog item.
        CatalogItemId: string
        -- The status of a catalog item.
        ItemStatus: string
        -- Information about the EC2 capacity of an item.
        EC2Capacities: {EC2Capacity}
        -- Information about the power draw of an item.
        PowerKva: number
        -- The weight of the item in pounds.
        WeightLbs: number
        -- The uplink speed this catalog item requires for the connection to the Region.
        SupportedUplinkGbps: {number}
        -- The supported storage options for the catalog item.
        SupportedStorage: {string}
    end

    enum CatalogItemClass
        "RACK"
        "SERVER"
    end

    -- Information about a connection.
    record ConnectionDetails
        -- The public key of the client.
        ClientPublicKey: string
        -- The public key of the server.
        ServerPublicKey: string
        -- The endpoint for the server.
        ServerEndpoint: string
        -- The client tunnel address.
        ClientTunnelAddress: string
        -- The server tunnel address.
        ServerTunnelAddress: string
        -- The allowed IP addresses.
        AllowedIps: {string}
    end

    -- Information about a line item request.
    record LineItemRequest
        -- The ID of the catalog item.
        CatalogItemId: string
        -- The quantity of a line item request.
        Quantity: number
    end

    enum PaymentOption
        "ALL_UPFRONT"
        "NO_UPFRONT"
        "PARTIAL_UPFRONT"
    end

    enum PaymentTerm
        "THREE_YEARS"
        "ONE_YEAR"
        "FIVE_YEARS"
    end

    record CreateOrderInput
        -- The ID or the Amazon Resource Name (ARN) of the Outpost.
        OutpostIdentifier: string
        -- The ID of the quote to use for the order.
        QuoteIdentifier: string
        -- The ID of the quote option to use for the order.
        QuoteOptionIdentifier: string
        -- The line items that make up the order.
        LineItems: {LineItemRequest}
        -- The payment option.
        PaymentOption: string
        -- The payment terms.
        PaymentTerm: string
    end

    -- Information about a line item asset.
    record LineItemAssetInformation
        -- The ID of the asset. An Outpost asset can be a single server within an Outposts rack or
        -- an Outposts server configuration.
        AssetId: string
        -- The MAC addresses of the asset.
        MacAddressList: {string}
    end

    enum ShipmentCarrier
        "DHL"
        "DBS"
        "FEDEX"
        "UPS"
        "EXPEDITORS"
    end

    -- Information about a line item shipment.
    record ShipmentInformation
        -- The tracking number of the shipment.
        ShipmentTrackingNumber: string
        -- The carrier of the shipment.
        ShipmentCarrier: string
    end

    enum LineItemStatus
        "PREPARING"
        "BUILDING"
        "SHIPPED"
        "DELIVERED"
        "INSTALLING"
        "INSTALLED"
        "ERROR"
        "CANCELLED"
        "REPLACED"
    end

    -- Information about a line item.
    record LineItem
        -- The ID of the catalog item.
        CatalogItemId: string
        -- The ID of the line item.
        LineItemId: string
        -- The quantity of the line item.
        Quantity: number
        -- The status of the line item.
        Status: string
        -- Information about a line item shipment.
        ShipmentInformation: ShipmentInformation
        -- Information about assets.
        AssetInformationList: {LineItemAssetInformation}
        -- The ID of the previous line item.
        PreviousLineItemId: string
        -- The ID of the previous order.
        PreviousOrderId: string
    end

    enum OrderType
        "OUTPOST"
        "REPLACEMENT"
    end

    enum OrderStatus
        "RECEIVED"
        "PENDING"
        "PROCESSING"
        "INSTALLING"
        "FULFILLED"
        "CANCELLED"
        "PREPARING"
        "IN_PROGRESS"
        "DELIVERED"
        "COMPLETED"
        "ERROR"
    end

    -- Information about an order.
    record Order
        -- The ID of the Outpost in the order.
        OutpostId: string
        -- The ID of the quote associated with the order.
        QuoteIdentifier: string
        -- The ID of the quote option associated with the order.
        QuoteOptionIdentifier: string
        -- The ID of the order.
        OrderId: string
        -- The status of the order.
        -- PREPARING - Order is received and being prepared.
        -- IN_PROGRESS - Order is either being built or shipped. To get more
        -- details, see the line item status.
        -- DELIVERED - Order was delivered to the Outpost site.
        -- COMPLETED - Order is complete.
        -- CANCELLED - Order is cancelled.
        -- ERROR - Customer should contact support.
        -- The following status are deprecated: RECEIVED, PENDING,
        -- PROCESSING, INSTALLING, and FULFILLED.
        Status: string
        -- The line items for the order
        LineItems: {LineItem}
        -- The payment option for the order.
        PaymentOption: string
        -- The submission date for the order.
        OrderSubmissionDate: number
        -- The fulfillment date of the order.
        OrderFulfilledDate: number
        -- The payment term.
        PaymentTerm: string
        -- The type of order.
        OrderType: string
    end

    record CreateOrderOutput
        -- Information about this order.
        Order: Order
    end

    -- You have exceeded a service quota.
    record ServiceQuotaExceededException
        Message: string
    end

    enum SupportedHardwareType
        "RACK"
        "SERVER"
    end

    record CreateOutpostInput
        Name: string
        Description: string
        -- The ID or the Amazon Resource Name (ARN) of the site.
        SiteId: string
        AvailabilityZone: string
        AvailabilityZoneId: string
        -- The tags to apply to the Outpost.
        Tags: {string : string}
        -- The type of hardware for this Outpost.
        SupportedHardwareType: string
    end

    -- Information about an Outpost.
    record Outpost
        -- The ID of the Outpost.
        OutpostId: string
        OwnerId: string
        OutpostArn: string
        SiteId: string
        Name: string
        Description: string
        LifeCycleStatus: string
        AvailabilityZone: string
        AvailabilityZoneId: string
        -- The Outpost tags.
        Tags: {string : string}
        SiteArn: string
        -- The hardware type.
        SupportedHardwareType: string
    end

    record CreateOutpostOutput
        Outpost: Outpost
    end

    enum QuoteConstraintType
        "RACK_MAXIMUM"
        "RACK_MAX_POWER_KVA"
        "RACK_MAX_WEIGHT_LBS"
    end

    -- A physical constraint for a quote.
    record QuoteConstraint
        -- The type of constraint. Valid values are RACK_MAXIMUM,
        -- RACK_MAX_POWER_KVA, and RACK_MAX_WEIGHT_LBS.
        QuoteConstraintType: string
        -- The value of the constraint.
        Value: string
    end

    record CreateQuoteInput
        -- The ID or ARN of the Outpost to associate with the quote. If not specified, the quote is
        -- created without an Outpost association.
        OutpostIdentifier: string
        -- The country code for the Outpost site location.
        CountryCode: string
        -- The capacity requirements for the quote. Each entry specifies a capacity type (such as
        -- Amazon EC2), the unit, and the quantity. For Amazon EC2, the quantity is the number of additional
        -- instances to add to the Outpost. For Amazon EBS and Amazon S3, the quantity is the total desired
        -- end-state capacity of the Outpost.
        RequestedCapacities: {QuoteCapacity}
        -- The physical constraints for the quote, such as maximum number of racks, maximum power
        -- draw per rack, or maximum weight per rack.
        RequestedConstraints: {QuoteConstraint}
        -- The payment options to include in the quote pricing. If not specified, all available
        -- payment options are returned.
        RequestedPaymentOptions: {string}
        -- The payment terms to include in the quote pricing. If not specified, all available
        -- payment terms are returned.
        RequestedPaymentTerms: {string}
        -- A description for the quote.
        Description: string
    end

    enum OrderingRequirementType
        "OUTPOST_ACTIVE_CHECK_ERROR"
        "MAXIMUM_ALLOWED_ORDERS_CHECK_ERROR"
        "VALID_ZIP_CODE_CHECK_ERROR"
        "RACK_PHYSICAL_PROPERTIES_CHECK_ERROR"
        "OPERATING_ADDRESS_EXISTENCE_CHECK_ERROR"
        "SHIPPING_ADDRESS_EXISTENCE_CHECK_ERROR"
        "COUNTRY_CODE_MISMATCH_CHECK_ERROR"
        "OUTPOST_GENERATION_MISMATCH_ERROR"
        "UNSUPPORTED"
        "OUTPOST_ID_MISSING_ON_QUOTE_ERROR"
        "ENTERPRISE_SUPPORT_ERROR"
        "SHIPPING_ADDRESS_MISSING_CONTACT_NAME_ERROR"
        "SHIPPING_ADDRESS_MISSING_CONTACT_NUMBER_ERROR"
        "SHIPPING_ADDRESS_MISSING_CONTACT_INFO_ERROR"
        "OUTPOST_STATE_CHANGED_ERROR"
        "OUTPOST_NOT_FOUND_ERROR"
        "OUTPOST_RENEWAL_REQUIRED_ERROR"
    end

    enum OrderingRequirementStatus
        "PASS"
        "FAIL"
        "EXEMPT"
    end

    -- A requirement that must be met before an order can be submitted for a quote.
    record OrderingRequirement
        -- A message about the ordering requirement.
        StatusMessage: string
        -- The type of ordering requirement. Indicates which check failed or passed.
        -- OUTPOST_ACTIVE_CHECK_ERROR - The Outpost must be in
        -- an active state.
        -- MAXIMUM_ALLOWED_ORDERS_CHECK_ERROR - The maximum
        -- number of allowed orders has been reached.
        -- VALID_ZIP_CODE_CHECK_ERROR - The site address must
        -- have a valid zip code.
        -- RACK_PHYSICAL_PROPERTIES_CHECK_ERROR - The rack
        -- physical properties do not meet requirements.
        -- OPERATING_ADDRESS_EXISTENCE_CHECK_ERROR - The site
        -- must have an operating address.
        -- SHIPPING_ADDRESS_EXISTENCE_CHECK_ERROR - The site
        -- must have a shipping address.
        -- COUNTRY_CODE_MISMATCH_CHECK_ERROR - The country
        -- code on the quote does not match the Outpost site country.
        -- OUTPOST_GENERATION_MISMATCH_ERROR - The Outpost generation
        -- does not match the requested configuration.
        -- OUTPOST_ID_MISSING_ON_QUOTE_ERROR - The quote must
        -- be associated with an Outpost before submitting an order.
        -- ENTERPRISE_SUPPORT_ERROR - Enterprise Support is
        -- required.
        -- SHIPPING_ADDRESS_MISSING_CONTACT_NAME_ERROR - The
        -- shipping address must have a contact name.
        -- SHIPPING_ADDRESS_MISSING_CONTACT_NUMBER_ERROR - The
        -- shipping address must have a contact phone number.
        -- SHIPPING_ADDRESS_MISSING_CONTACT_INFO_ERROR - The
        -- shipping address must have contact information.
        -- OUTPOST_STATE_CHANGED_ERROR - The Outpost state has
        -- changed since the quote was created.
        -- OUTPOST_NOT_FOUND_ERROR - The Outpost associated
        -- with the quote was not found.
        -- OUTPOST_RENEWAL_REQUIRED_ERROR - The Outpost
        -- requires a renewal before a new order can be submitted.
        -- UNSUPPORTED - The requirement type is not
        -- recognized.
        OrderingRequirementType: string
        -- The status of the ordering requirement. Valid values are PASS,
        -- FAIL, and EXEMPT.
        Status: string
    end

    enum QuotePricingType
        "SUBSCRIPTION"
    end

    enum CurrencyCode
        "USD"
    end

    -- The pricing details for a subscription.
    record SubscriptionPricingDetails
        -- The payment option.
        PaymentOption: string
        -- The payment term.
        PaymentTerm: string
        -- The upfront price.
        UpfrontPrice: number
        -- The monthly recurring price.
        MonthlyRecurringPrice: number
        -- The currency of the price. Currently only USD is supported.
        Currency: string
    end

    -- A pricing option for the specified Outpost.
    record PricingOption
        -- The type of pricing model.
        PricingType: string
        -- The subscription pricing details for this pricing option.
        SubscriptionPricingDetails: SubscriptionPricingDetails
    end

    enum RackUnitHeight
        "HEIGHT_42U"
        "HEIGHT_2U"
        "HEIGHT_1U"
    end

    enum QuoteRackUseType
        "NETWORKING"
        "COMPUTE"
    end

    -- The physical specification details for a rack in a quote option.
    record RackSpecificationDetails
        -- The ID of the rack.
        RackId: string
        -- The use of the rack. Valid values are COMPUTE and
        -- NETWORKING.
        RackUse: string
        -- The maximum power draw of the rack in kVA.
        RackPowerDrawKva: number
        -- The weight of the rack in pounds.
        RackWeightLbs: number
        -- The height of the rack in inches.
        RackHeightInches: number
        -- The width of the rack in inches.
        RackWidthInches: number
        -- The depth of the rack in inches.
        RackDepthInches: number
        -- The rack unit height.
        -- HEIGHT_42U - 42 rack units.
        -- HEIGHT_2U - 2 rack units.
        -- HEIGHT_1U - 1 rack unit.
        RackUnitHeight: string
        -- The Amazon EC2 capacities for the rack.
        EC2Capacities: {EC2Capacity}
    end

    enum QuoteSpecificationType
        "UPDATED_RACK"
        "NEW_RACK"
        "EXISTING_RACK"
        "SERVER"
    end

    -- The physical specification details for a server in a quote option.
    record ServerSpecificationDetails
        -- The maximum power draw of the server in kVA.
        ServerPowerDrawKva: number
        -- The weight of the server in pounds.
        ServerWeightLbs: number
        -- The height of the server in inches.
        ServerHeightInches: number
        -- The width of the server in inches.
        ServerWidthInches: number
        -- The depth of the server in inches.
        ServerDepthInches: number
        -- The rack unit height of the server.
        -- HEIGHT_2U - 2 rack units.
        -- HEIGHT_1U - 1 rack unit.
        RackUnitHeight: string
        -- The Amazon EC2 capacities for the server.
        EC2Capacities: {EC2Capacity}
    end

    -- A physical specification for a quote option. Describes the rack or server configuration
    -- that would be deployed.
    record QuoteSpecification
        -- The type of specification. Valid values are NEW_RACK,
        -- UPDATED_RACK, EXISTING_RACK, and SERVER.
        QuoteSpecificationType: string
        -- The existing rack specification details, if the specification type is
        -- UPDATED_RACK or EXISTING_RACK.
        ExistingRackSpecificationDetails: RackSpecificationDetails
        -- The final rack specification details after the quote is fulfilled.
        FinalRackSpecificationDetails: RackSpecificationDetails
        -- The server specification details, if the specification type is
        -- SERVER.
        ServerSpecificationDetails: ServerSpecificationDetails
    end

    -- A configuration and pricing option for a quote. Each option includes the capacity
    -- breakdown, physical specifications for the racks or servers, and pricing details.
    record QuoteOption
        -- The ID of the quote option.
        QuoteOptionIdentifier: string
        -- The capacities included in this quote option.
        Capacities: {QuoteCapacity}
        -- A summary of the existing, final, and changed capacity for this quote option.
        CapacitySummary: CapacitySummary
        -- The physical specifications for the racks or servers in this quote option.
        Specifications: {QuoteSpecification}
        -- The pricing options for this quote option.
        PricingOptions: {PricingOption}
    end

    enum QuoteStatus
        "CREATED"
        "ORDER_SUBMITTED"
        "EXPIRED"
    end

    -- Information about a quote for an Outpost. A quote provides pricing and configuration
    -- options based on the requested capacity.
    record Quote
        -- The ID of the quote.
        QuoteId: string
        -- The ID of the account that owns the quote.
        AccountId: string
        -- The status of the quote.
        -- CREATED - The quote has been created and is
        -- available for review.
        -- ORDER_SUBMITTED - An order has been submitted for
        -- the quote.
        -- EXPIRED - The quote has expired and can no longer
        -- be used to submit an order.
        QuoteStatus: string
        -- A message about the status of the quote.
        StatusMessage: string
        -- The ARN of the Outpost associated with the quote.
        OutpostArn: string
        -- The country code for the Outpost site location.
        CountryCode: string
        -- The capacity requirements specified in the quote request.
        RequestedCapacities: {QuoteCapacity}
        -- The physical constraints specified in the quote request.
        RequestedConstraints: {QuoteConstraint}
        -- The payment options specified in the quote request.
        RequestedPaymentOptions: {string}
        -- The payment terms specified in the quote request.
        RequestedPaymentTerms: {string}
        -- The configuration and pricing options for the quote. Each option includes capacity
        -- details, physical specifications, and pricing information.
        QuoteOptions: {QuoteOption}
        -- The requirements that must be met before an order can be submitted for the quote.
        OrderingRequirements: {OrderingRequirement}
        -- The ID of the order submitted for the quote.
        SubmittedOrderId: string
        -- The date the quote was created.
        CreatedDate: number
        -- The date the quote expires.
        ExpirationDate: number
        -- The description of the quote.
        Description: string
    end

    record CreateQuoteOutput
        -- Information about the quote.
        Quote: Quote
    end

    record CreateRenewalInput
        -- The payment option.
        PaymentOption: string
        -- The payment term.
        PaymentTerm: string
        -- The ID or ARN of the Outpost.
        OutpostIdentifier: string
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the
        -- request.
        ClientToken: string
    end

    record CreateRenewalOutput
        -- The payment option.
        PaymentOption: string
        -- The payment term.
        PaymentTerm: string
        -- The ID of the Outpost.
        OutpostId: string
        -- The upfront price of the renewal.
        UpfrontPrice: number
        -- The monthly recurring price of the renewal.
        MonthlyRecurringPrice: number
        -- The currency of the renewal price.
        Currency: string
    end

    enum FiberOpticCableType
        "SINGLE_MODE"
        "MULTI_MODE"
    end

    enum MaximumSupportedWeightLbs
        "NO_LIMIT"
        "MAX_1400_LBS"
        "MAX_1600_LBS"
        "MAX_1800_LBS"
        "MAX_2000_LBS"
    end

    enum OpticalStandard
        "OPTIC_10GBASE_SR"
        "OPTIC_10GBASE_IR"
        "OPTIC_10GBASE_LR"
        "OPTIC_40GBASE_SR"
        "OPTIC_40GBASE_ESR"
        "OPTIC_40GBASE_IR4_LR4L"
        "OPTIC_40GBASE_LR4"
        "OPTIC_100GBASE_SR4"
        "OPTIC_100GBASE_CWDM4"
        "OPTIC_100GBASE_LR4"
        "OPTIC_100G_PSM4_MSA"
        "OPTIC_1000BASE_LX"
        "OPTIC_1000BASE_SX"
    end

    enum PowerConnector
        "L6_30P"
        "IEC309"
        "AH530P7W"
        "AH532P6W"
        "CS8365C"
    end

    enum PowerDrawKva
        "POWER_5_KVA"
        "POWER_10_KVA"
        "POWER_15_KVA"
        "POWER_30_KVA"
    end

    enum PowerFeedDrop
        "ABOVE_RACK"
        "BELOW_RACK"
    end

    enum PowerPhase
        "SINGLE_PHASE"
        "THREE_PHASE"
    end

    enum UplinkCount
        "UPLINK_COUNT_1"
        "UPLINK_COUNT_2"
        "UPLINK_COUNT_3"
        "UPLINK_COUNT_4"
        "UPLINK_COUNT_5"
        "UPLINK_COUNT_6"
        "UPLINK_COUNT_7"
        "UPLINK_COUNT_8"
        "UPLINK_COUNT_12"
        "UPLINK_COUNT_16"
    end

    enum UplinkGbps
        "UPLINK_1G"
        "UPLINK_10G"
        "UPLINK_40G"
        "UPLINK_100G"
    end

    -- Information about the physical and logistical details for racks at sites.
    -- For more information
    -- about hardware requirements for racks, see Network
    -- readiness checklist in the Amazon Web Services Outposts User Guide.
    record RackPhysicalProperties
        -- The power draw available at the hardware placement position for the rack.
        PowerDrawKva: string
        -- The power option that you can provide for hardware.
        PowerPhase: string
        -- The power connector for the hardware.
        PowerConnector: string
        -- The position of the power feed.
        PowerFeedDrop: string
        -- The uplink speed the rack supports for the connection to the Region.
        UplinkGbps: string
        -- The number of uplinks each Outpost network device.
        UplinkCount: string
        -- The type of fiber used to attach the Outpost to the network.
        FiberOpticCableType: string
        -- The type of optical standard used to attach the Outpost to the network. This field is
        -- dependent on uplink speed, fiber type, and distance to the upstream device.
        -- For more information
        -- about networking requirements for racks, see Network
        -- in the Amazon Web Services Outposts User Guide.
        OpticalStandard: string
        -- The maximum rack weight that this site can support. NO_LIMIT is over 2000 lbs
        -- (907 kg).
        MaximumSupportedWeightLbs: string
    end

    record CreateSiteInput
        Name: string
        Description: string
        -- Additional information that you provide about site access requirements, electrician
        -- scheduling, personal protective equipment, or regulation of equipment materials that could
        -- affect your installation process.
        Notes: string
        -- The tags to apply to a site.
        Tags: {string : string}
        -- The location to install and power on the hardware. This address might be different from
        -- the shipping address.
        OperatingAddress: Address
        -- The location to ship the hardware. This address might be different from the operating
        -- address.
        ShippingAddress: Address
        -- Information about the physical and logistical details for the rack at this site.
        -- For more information
        -- about hardware requirements for racks, see Network
        -- readiness checklist in the Amazon Web Services Outposts User Guide.
        RackPhysicalProperties: RackPhysicalProperties
    end

    -- Information about a site.
    record Site
        SiteId: string
        AccountId: string
        Name: string
        Description: string
        -- The site tags.
        Tags: {string : string}
        SiteArn: string
        -- Notes about a site.
        Notes: string
        -- The ISO-3166 two-letter country code where the hardware is installed and powered on.
        OperatingAddressCountryCode: string
        -- State or region where the hardware is installed and powered on.
        OperatingAddressStateOrRegion: string
        -- City where the hardware is installed and powered on.
        OperatingAddressCity: string
        -- Information about the physical and logistical details for a rack at the site.
        RackPhysicalProperties: RackPhysicalProperties
    end

    record CreateSiteOutput
        Site: Site
    end

    enum DecommissionRequestStatus
        "SKIPPED"
        "BLOCKED"
        "REQUESTED"
    end

    record DeleteOutpostInput
        -- The ID or ARN of the Outpost.
        OutpostId: string
    end

    record DeleteOutpostOutput
    end

    record DeleteQuoteInput
        -- The ID of the quote.
        QuoteIdentifier: string
    end

    record DeleteQuoteOutput
    end

    record DeleteSiteInput
        -- The ID or the Amazon Resource Name (ARN) of the site.
        SiteId: string
    end

    record DeleteSiteOutput
    end

    enum FormFactor
        "RACK"
        "SERVER"
    end

    enum OutpostGeneration
        "GENERATION_2"
        "GENERATION_1"
    end

    -- A supported form factor and Outpost generation configuration for an instance type.
    record FormFactorConfig
        -- The form factor. Valid values are RACK for rack-based Outposts and
        -- SERVER for server-based Outposts.
        FormFactor: string
        -- The Outpost generation. Valid values are GENERATION_1 for first-generation
        -- rack deployments and GENERATION_2 for second-generation rack deployments. This
        -- value is not set for server form factors.
        OutpostGeneration: string
    end

    -- Information about an instance type that can be ordered for an Outpost, including
    -- hardware specifications and supported form factors.
    record DetailedInstanceTypeItem
        -- The instance type.
        InstanceType: string
        -- The number of default VCPUs in the instance type.
        VCPUs: number
        -- The memory size of the instance type, in MiB.
        MemoryInMib: number
        -- The network performance of the instance type.
        NetworkPerformance: string
        -- The supported form factor and Outpost generation configurations for the instance
        -- type.
        FormFactorConfigs: {FormFactorConfig}
    end

    record GetCapacityTaskInput
        -- ID of the capacity task.
        CapacityTaskId: string
        -- ID or ARN of the Outpost associated with the specified capacity task.
        OutpostIdentifier: string
    end

    -- User-specified instances that must not be stopped. These instances will not appear in the
    -- list of instances that Amazon Web Services recommends to stop in order to free up capacity.
    record InstancesToExclude
        -- List of user-specified instances that must not be stopped.
        Instances: {string}
        -- IDs of the accounts that own each instance that must not be stopped.
        AccountIds: {string}
        -- Names of the services that own each instance that must not be stopped in order to free up
        -- the capacity needed to run the capacity task.
        Services: {string}
    end

    -- The instance type that you specify determines the combination of CPU, memory, storage, and
    -- networking capacity.
    record InstanceTypeCapacity
        -- The instance type of the hosts.
        InstanceType: string
        -- The number of instances for the specified instance type.
        Count: number
    end

    enum TaskActionOnBlockingInstances
        "WAIT_FOR_EVACUATION"
        "FAIL_TASK"
    end

    record GetCapacityTaskOutput
        -- ID of the capacity task.
        CapacityTaskId: string
        -- ID of the Outpost associated with the specified capacity task.
        OutpostId: string
        -- ID of the Amazon Web Services Outposts order associated with the specified capacity task.
        OrderId: string
        -- The ID of the Outpost asset. An Outpost asset can be a single server within an Outposts
        -- rack or an Outposts server configuration.
        AssetId: string
        -- List of instance pools requested in the capacity task.
        RequestedInstancePools: {InstanceTypeCapacity}
        -- Instances that the user specified they cannot stop in order to free up the capacity needed
        -- to run the capacity task.
        InstancesToExclude: InstancesToExclude
        -- Performs a dry run to determine if you are above or below instance capacity.
        DryRun: boolean
        -- Status of the capacity task.
        -- A capacity task can have one of the following statuses:
        -- REQUESTED - The capacity task was created and is awaiting the next step
        -- by Amazon Web Services Outposts.
        -- IN_PROGRESS - The capacity task is running and cannot be
        -- cancelled.
        -- FAILED - The capacity task could not be completed.
        -- COMPLETED - The capacity task has completed successfully.
        -- WAITING_FOR_EVACUATION - The capacity task requires capacity to run. You
        -- must stop the recommended EC2 running instances to free up capacity for the task to
        -- run.
        -- CANCELLATION_IN_PROGRESS - The capacity task has been cancelled and is in
        -- the process of cleaning up resources.
        -- CANCELLED - The capacity task is cancelled.
        CapacityTaskStatus: string
        -- Reason why the capacity task failed.
        Failed: CapacityTaskFailure
        -- The date the capacity task was created.
        CreationDate: number
        -- The date the capacity task ran successfully.
        CompletionDate: number
        -- The date the capacity task was last modified.
        LastModifiedDate: number
        -- User-specified option in case an instance is blocking the capacity task from running.
        -- Shows one of the following options:
        -- WAIT_FOR_EVACUATION - Checks every 10 minutes over 48 hours to determine
        -- if instances have stopped and capacity is available to complete the task.
        -- FAIL_TASK - The capacity task fails.
        TaskActionOnBlockingInstances: string
    end

    record GetCatalogItemInput
        -- The ID of the catalog item.
        CatalogItemId: string
    end

    record GetCatalogItemOutput
        -- Information about this catalog item.
        CatalogItem: CatalogItem
    end

    record GetConnectionInput
        -- The ID of the connection.
        ConnectionId: string
    end

    record GetConnectionOutput
        -- The ID of the connection.
        ConnectionId: string
        -- Information about the connection.
        ConnectionDetails: ConnectionDetails
    end

    record GetOrderInput
        -- The ID of the order.
        OrderId: string
    end

    record GetOrderOutput
        Order: Order
    end

    record GetOutpostInput
        -- The ID or ARN of the Outpost.
        OutpostId: string
    end

    record GetOutpostOutput
        Outpost: Outpost
    end

    record GetOutpostBillingInformationInput
        NextToken: string
        MaxResults: number
        -- The ID or ARN of the Outpost.
        OutpostIdentifier: string
    end

    enum SubscriptionStatus
        "ACTIVE"
        "PENDING"
        "INACTIVE"
        "CANCELLED"
    end

    enum SubscriptionType
        "ORIGINAL"
        "RENEWAL"
        "CAPACITY_INCREASE"
    end

    -- Provides information about your Amazon Web Services Outposts subscriptions.
    record Subscription
        -- The ID of the subscription that appears on the Amazon Web Services Billing Center console.
        SubscriptionId: string
        -- The type of subscription which can be one of the following:
        -- ORIGINAL - The first order on the Amazon Web Services Outposts.
        -- RENEWAL - Renewal requests, both month to month and
        -- longer term.
        -- CAPACITY_INCREASE - Capacity scaling orders.
        SubscriptionType: string
        -- The status of subscription which can be one of the following:
        -- INACTIVE - Subscription requests that are
        -- inactive.
        -- ACTIVE - Subscription requests that are in progress
        -- and have an end date in the future.
        -- PENDING - Subscription has been created but billing
        -- has not yet commenced because the subscription begin date has not been reached.
        -- CANCELLED - Subscription requests that are
        -- cancelled.
        SubscriptionStatus: string
        -- The order ID for your subscription.
        OrderIds: {string}
        -- The date your subscription starts.
        BeginDate: number
        -- The date your subscription ends.
        EndDate: number
        -- The currency of the subscription price. Currently only USD is
        -- supported.
        Currency: string
        -- The amount you are billed each month in the subscription period.
        MonthlyRecurringPrice: number
        -- The amount billed when the subscription is created. This is a one-time charge.
        UpfrontPrice: number
    end

    record GetOutpostBillingInformationOutput
        NextToken: string
        -- The subscription details for the specified Outpost.
        Subscriptions: {Subscription}
        -- The date the current contract term ends for the specified Outpost. You must start the
        -- renewal or decommission process at least 5 business days before the current term for your
        -- Amazon Web Services Outposts ends. Failing to complete these steps at least 5 business days before the current term
        -- ends might result in unanticipated charges.
        ContractEndDate: string
        -- The payment term.
        PaymentTerm: string
        -- The payment option.
        PaymentOption: string
    end

    record GetOutpostInstanceTypesInput
        -- The ID or ARN of the Outpost.
        OutpostId: string
        NextToken: string
        MaxResults: number
    end

    -- Information about an instance type.
    record InstanceTypeItem
        InstanceType: string
        -- The number of default VCPUs in an instance type.
        VCPUs: number
    end

    record GetOutpostInstanceTypesOutput
        InstanceTypes: {InstanceTypeItem}
        NextToken: string
        -- The ID of the Outpost.
        OutpostId: string
        OutpostArn: string
    end

    record GetOutpostSupportedInstanceTypesInput
        -- The ID or ARN of the Outpost.
        OutpostIdentifier: string
        -- The ID for the Amazon Web Services Outposts order.
        OrderId: string
        -- The ID of the Outpost asset. An Outpost asset can be a single server within an Outposts
        -- rack or an Outposts server configuration.
        AssetId: string
        MaxResults: number
        NextToken: string
    end

    record GetOutpostSupportedInstanceTypesOutput
        InstanceTypes: {InstanceTypeItem}
        NextToken: string
    end

    record GetQuoteInput
        -- The ID of the quote.
        QuoteIdentifier: string
    end

    record GetQuoteOutput
        -- Information about the quote.
        Quote: Quote
    end

    record GetRenewalPricingInput
        -- The ID or ARN of the Outpost.
        OutpostIdentifier: string
    end

    enum PricingResult
        "PRICED"
        "UNABLE_TO_PRICE"
    end

    record GetRenewalPricingOutput
        -- The result of the pricing request.
        PricingResult: string
        -- The pricing options for the specified Outpost.
        PricingOptions: {PricingOption}
    end

    record GetSiteInput
        -- The ID or the Amazon Resource Name (ARN) of the site.
        SiteId: string
    end

    record GetSiteOutput
        Site: Site
    end

    record GetSiteAddressInput
        -- The ID or the Amazon Resource Name (ARN) of the site.
        SiteId: string
        -- The type of the address you request.
        AddressType: string
    end

    record GetSiteAddressOutput
        SiteId: string
        -- The type of the address you receive.
        AddressType: string
        -- Information about the address.
        Address: Address
    end

    record ListAssetInstancesInput
        -- The ID of the Outpost.
        OutpostIdentifier: string
        -- Filters the results by asset ID.
        AssetIdFilter: {string}
        -- Filters the results by instance ID.
        InstanceTypeFilter: {string}
        -- Filters the results by account ID.
        AccountIdFilter: {string}
        -- Filters the results by Amazon Web Services service.
        AwsServiceFilter: {string}
        MaxResults: number
        NextToken: string
    end

    record ListAssetInstancesOutput
        -- List of instances owned by all accounts on the Outpost. Does not include Amazon EBS or Amazon S3
        -- instances.
        AssetInstances: {AssetInstance}
        NextToken: string
    end

    record ListAssetsInput
        -- The ID or the Amazon Resource Name (ARN) of the Outpost.
        OutpostIdentifier: string
        -- Filters the results by the host ID of a Dedicated Host.
        HostIdFilter: {string}
        MaxResults: number
        NextToken: string
        -- Filters the results by state.
        StatusFilter: {string}
        -- Filters the results by asset type.
        -- COMPUTE - Server asset used for customer compute
        -- STORAGE - Server asset used by storage services
        -- POWERSHELF - Powershelf assets
        -- SWITCH - Switch assets
        -- NETWORKING - Asset managed by Amazon Web Services for networking purposes
        AssetTypeFilter: {string}
    end

    record ListAssetsOutput
        -- Information about the hardware assets.
        Assets: {AssetInfo}
        NextToken: string
    end

    record ListBlockingInstancesForCapacityTaskInput
        -- The ID or ARN of the Outpost associated with the specified capacity task.
        OutpostIdentifier: string
        -- The ID of the capacity task.
        CapacityTaskId: string
        MaxResults: number
        NextToken: string
    end

    record ListBlockingInstancesForCapacityTaskOutput
        -- A list of all running Amazon EC2 instances on the Outpost. Stopping one or more of these
        -- instances can free up the capacity needed to run the capacity task.
        BlockingInstances: {BlockingInstance}
        NextToken: string
    end

    record ListCapacityTasksInput
        -- Filters the results by an Outpost ID or an Outpost ARN.
        OutpostIdentifierFilter: string
        MaxResults: number
        NextToken: string
        -- A list of statuses. For example, REQUESTED or
        -- WAITING_FOR_EVACUATION.
        CapacityTaskStatusFilter: {string}
    end

    record ListCapacityTasksOutput
        -- Lists all the capacity tasks.
        CapacityTasks: {CapacityTaskSummary}
        NextToken: string
    end

    record ListCatalogItemsInput
        NextToken: string
        MaxResults: number
        -- Filters the results by item class.
        ItemClassFilter: {string}
        -- Filters the results by storage option.
        SupportedStorageFilter: {string}
        -- Filters the results by EC2 family (for example, M5).
        EC2FamilyFilter: {string}
    end

    record ListCatalogItemsOutput
        -- Information about the catalog items.
        CatalogItems: {CatalogItem}
        NextToken: string
    end

    record ListOrderableInstanceTypesInput
        -- Filters the results by Outpost generation. Specify GENERATION_1 for
        -- first-generation rack deployments or GENERATION_2 for second-generation rack
        -- deployments.
        OutpostGenerationFilter: string
        -- The maximum page size.
        MaxResults: number
        -- The pagination token.
        NextToken: string
    end

    record ListOrderableInstanceTypesOutput
        -- Information about the instance types that can be ordered for the Outpost.
        InstanceTypes: {DetailedInstanceTypeItem}
        -- The pagination token.
        NextToken: string
    end

    record ListOrdersInput
        -- The ID or the Amazon Resource Name (ARN) of the Outpost.
        OutpostIdentifierFilter: string
        NextToken: string
        MaxResults: number
    end

    -- A summary of line items in your order.
    record OrderSummary
        -- The ID of the Outpost.
        OutpostId: string
        -- The ID of the order.
        OrderId: string
        -- The type of order.
        OrderType: string
        -- The status of the order.
        -- PREPARING - Order is received and is being prepared.
        -- IN_PROGRESS - Order is either being built, shipped, or installed. For
        -- more information, see the LineItem status.
        -- COMPLETED - Order is complete.
        -- CANCELLED - Order is cancelled.
        -- ERROR - Customer should contact support.
        -- The following statuses are deprecated: RECEIVED, PENDING,
        -- PROCESSING, INSTALLING, and FULFILLED.
        Status: string
        -- The status of all line items in the order.
        LineItemCountsByStatus: {string : number}
        -- The submission date for the order.
        OrderSubmissionDate: number
        -- The fulfilment date for the order.
        OrderFulfilledDate: number
    end

    record ListOrdersOutput
        -- Information about the orders.
        Orders: {OrderSummary}
        NextToken: string
    end

    record ListOutpostsInput
        NextToken: string
        MaxResults: number
        -- Filters the results by the lifecycle status.
        LifeCycleStatusFilter: {string}
        -- Filters the results by Availability Zone (for example, us-east-1a).
        AvailabilityZoneFilter: {string}
        -- Filters the results by AZ ID (for example, use1-az1).
        AvailabilityZoneIdFilter: {string}
    end

    record ListOutpostsOutput
        Outposts: {Outpost}
        NextToken: string
    end

    record ListQuotesInput
        -- The pagination token.
        NextToken: string
        -- The maximum page size.
        MaxResults: number
    end

    -- Summary information about a quote.
    record QuoteSummary
        -- The ID of the quote.
        QuoteId: string
        -- The ID of the account that owns the quote.
        AccountId: string
        -- The status of the quote.
        QuoteStatus: string
        -- A message about the status of the quote.
        StatusMessage: string
        -- The ARN of the Outpost associated with the quote.
        OutpostArn: string
        -- The country code for the Outpost site location.
        CountryCode: string
        -- The capacity requirements specified in the quote request.
        RequestedCapacities: {QuoteCapacity}
        -- The physical constraints specified in the quote request.
        RequestedConstraints: {QuoteConstraint}
        -- The payment options specified in the quote request.
        RequestedPaymentOptions: {string}
        -- The payment terms specified in the quote request.
        RequestedPaymentTerms: {string}
        -- The configuration and pricing options for the quote.
        QuoteOptions: {QuoteOption}
        -- The ID of the order submitted for the quote.
        SubmittedOrderId: string
        -- The date the quote was created.
        CreatedDate: number
        -- The date the quote expires.
        ExpirationDate: number
        -- The description of the quote.
        Description: string
    end

    record ListQuotesOutput
        -- Information about the quotes.
        Quotes: {QuoteSummary}
        -- The pagination token.
        NextToken: string
    end

    record ListSitesInput
        NextToken: string
        MaxResults: number
        -- Filters the results by country code.
        OperatingAddressCountryCodeFilter: {string}
        -- Filters the results by state or region.
        OperatingAddressStateOrRegionFilter: {string}
        -- Filters the results by city.
        OperatingAddressCityFilter: {string}
    end

    record ListSitesOutput
        Sites: {Site}
        NextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        ResourceArn: string
    end

    record ListTagsForResourceOutput
        -- The resource tags.
        Tags: {string : string}
    end

    record StartCapacityTaskInput
        -- The ID or ARN of the Outposts associated with the specified capacity task.
        OutpostIdentifier: string
        -- The ID of the Amazon Web Services Outposts order associated with the specified capacity task.
        OrderId: string
        -- The ID of the Outpost asset. An Outpost asset can be a single server within an Outposts
        -- rack or an Outposts server configuration.
        AssetId: string
        -- The instance pools specified in the capacity task.
        InstancePools: {InstanceTypeCapacity}
        -- List of user-specified running instances that must not be stopped in order to free up the
        -- capacity needed to run the capacity task.
        InstancesToExclude: InstancesToExclude
        -- You can request a dry run to determine if the instance type and instance size changes is
        -- above or below available instance capacity. Requesting a dry run does not make any changes to
        -- your plan.
        DryRun: boolean
        -- Specify one of the following options in case an instance is blocking the capacity task
        -- from running.
        -- WAIT_FOR_EVACUATION - Checks every 10 minutes over 48 hours to determine
        -- if instances have stopped and capacity is available to complete the task.
        -- FAIL_TASK - The capacity task fails.
        TaskActionOnBlockingInstances: string
    end

    record StartCapacityTaskOutput
        -- ID of the capacity task that you want to start.
        CapacityTaskId: string
        -- ID of the Outpost associated with the capacity task.
        OutpostId: string
        -- ID of the Amazon Web Services Outposts order of the host associated with the capacity task.
        OrderId: string
        -- The ID of the asset. An Outpost asset can be a single server within an Outposts rack or an
        -- Outposts server configuration.
        AssetId: string
        -- List of the instance pools requested in the specified capacity task.
        RequestedInstancePools: {InstanceTypeCapacity}
        -- User-specified instances that must not be stopped in order to free up the capacity needed
        -- to run the capacity task.
        InstancesToExclude: InstancesToExclude
        -- Results of the dry run showing if the specified capacity task is above or below the
        -- available instance capacity.
        DryRun: boolean
        -- Status of the specified capacity task.
        CapacityTaskStatus: string
        -- Reason that the specified capacity task failed.
        Failed: CapacityTaskFailure
        -- Date that the specified capacity task was created.
        CreationDate: number
        -- Date that the specified capacity task ran successfully.
        CompletionDate: number
        -- Date that the specified capacity task was last modified.
        LastModifiedDate: number
        -- User-specified option in case an instance is blocking the capacity task from
        -- running.
        -- WAIT_FOR_EVACUATION - Checks every 10 minutes over 48 hours to determine
        -- if instances have stopped and capacity is available to complete the task.
        -- FAIL_TASK - The capacity task fails.
        TaskActionOnBlockingInstances: string
    end

    record StartConnectionInput
        -- The serial number of the dongle.
        DeviceSerialNumber: string
        -- The ID of the Outpost server.
        AssetId: string
        -- The public key of the client.
        ClientPublicKey: string
        -- The device index of the network interface on the Outpost server.
        NetworkInterfaceDeviceIndex: number
    end

    record StartConnectionOutput
        -- The ID of the connection.
        ConnectionId: string
        -- The underlay IP address.
        UnderlayIpAddress: string
    end

    record StartOutpostDecommissionInput
        -- The ID or ARN of the Outpost that you want to decommission.
        OutpostIdentifier: string
        -- Validates the request without starting the decommission process.
        ValidateOnly: boolean
    end

    record StartOutpostDecommissionOutput
        -- The status of the decommission request.
        Status: string
        -- The resources still associated with the Outpost that you are decommissioning.
        BlockingResourceTypes: {string}
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        ResourceArn: string
        -- The tags to add to the resource.
        Tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        ResourceArn: string
        -- The tag keys.
        TagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateOutpostInput
        -- The ID or ARN of the Outpost.
        OutpostId: string
        Name: string
        Description: string
        -- The type of hardware for this Outpost.
        SupportedHardwareType: string
    end

    record UpdateOutpostOutput
        Outpost: Outpost
    end

    record UpdateQuoteInput
        -- The ID of the quote.
        QuoteIdentifier: string
        -- The ID or ARN of the Outpost to associate with the quote. Specify an empty string to
        -- remove the Outpost association.
        OutpostIdentifier: string
        -- The country code for the Outpost site location.
        CountryCode: string
        -- The updated capacity requirements for the quote.
        RequestedCapacities: {QuoteCapacity}
        -- The updated physical constraints for the quote.
        RequestedConstraints: {QuoteConstraint}
        -- The updated payment options to include in the quote pricing.
        RequestedPaymentOptions: {string}
        -- The updated payment terms to include in the quote pricing.
        RequestedPaymentTerms: {string}
        -- A description for the quote.
        Description: string
    end

    record UpdateQuoteOutput
        -- Information about the updated quote.
        Quote: Quote
    end

    record UpdateSiteInput
        -- The ID or the Amazon Resource Name (ARN) of the site.
        SiteId: string
        Name: string
        Description: string
        -- Notes about a site.
        Notes: string
    end

    record UpdateSiteOutput
        Site: Site
    end

    record UpdateSiteAddressInput
        -- The ID or the Amazon Resource Name (ARN) of the site.
        SiteId: string
        -- The type of the address.
        AddressType: string
        -- The address for the site.
        Address: Address
    end

    record UpdateSiteAddressOutput
        -- The type of the address.
        AddressType: string
        -- Information about an address.
        Address: Address
    end

    record UpdateSiteRackPhysicalPropertiesInput
        -- The ID or the Amazon Resource Name (ARN) of the site.
        SiteId: string
        -- The power draw, in kVA, available at the hardware placement position for the rack.
        PowerDrawKva: string
        -- The power option that you can provide for hardware.
        -- Single-phase AC feed: 200 V to 277 V, 50 Hz or 60 Hz
        -- Three-phase AC feed: 346 V to 480 V, 50 Hz or 60 Hz
        PowerPhase: string
        -- The power connector that Amazon Web Services should plan to provide for connections to the hardware.
        -- Note the correlation between PowerPhase and PowerConnector.
        -- Single-phase AC feed
        -- L6-30P – (common in US); 30A; single phase
        -- IEC309 (blue) – P+N+E, 6hr; 32 A; single
        -- phase
        -- Three-phase AC feed
        -- AH530P7W (red) – 3P+N+E, 7hr; 30A; three
        -- phase
        -- AH532P6W (red) – 3P+N+E, 6hr; 32A; three
        -- phase
        -- CS8365C – (common in US); 3P+E, 50A; three
        -- phase
        PowerConnector: string
        -- Indicates whether the power feed comes above or below the rack.
        PowerFeedDrop: string
        -- The uplink speed the rack should support for the connection to the Region.
        UplinkGbps: string
        -- Racks come with two Outpost network devices. Depending on the supported uplink speed at
        -- the site, the Outpost network devices provide a variable number of uplinks. Specify the number
        -- of uplinks for each Outpost network device that you intend to use to connect the rack to your
        -- network. Note the correlation between UplinkGbps and UplinkCount.
        -- 1Gbps - Uplinks available: 1, 2, 4, 6, 8
        -- 10Gbps - Uplinks available: 1, 2, 4, 8, 12, 16
        -- 40 and 100 Gbps- Uplinks available: 1, 2, 4
        UplinkCount: string
        -- The type of fiber that you will use to attach the Outpost to your network.
        FiberOpticCableType: string
        -- The type of optical standard that you will use to attach the Outpost to your network. This
        -- field is dependent on uplink speed, fiber type, and distance to the upstream device.
        -- For more information
        -- about networking requirements for racks, see Network
        -- in the Amazon Web Services Outposts User Guide.
        -- OPTIC_10GBASE_SR: 10GBASE-SR
        -- OPTIC_10GBASE_IR: 10GBASE-IR
        -- OPTIC_10GBASE_LR: 10GBASE-LR
        -- OPTIC_40GBASE_SR: 40GBASE-SR
        -- OPTIC_40GBASE_ESR: 40GBASE-ESR
        -- OPTIC_40GBASE_IR4_LR4L: 40GBASE-IR (LR4L)
        -- OPTIC_40GBASE_LR4: 40GBASE-LR4
        -- OPTIC_100GBASE_SR4: 100GBASE-SR4
        -- OPTIC_100GBASE_CWDM4: 100GBASE-CWDM4
        -- OPTIC_100GBASE_LR4: 100GBASE-LR4
        -- OPTIC_100G_PSM4_MSA: 100G PSM4 MSA
        -- OPTIC_1000BASE_LX: 1000Base-LX
        -- OPTIC_1000BASE_SX : 1000Base-SX
        OpticalStandard: string
        -- The maximum rack weight that this site can support. NO_LIMIT is over 2000lbs.
        MaximumSupportedWeightLbs: string
    end

    record UpdateSiteRackPhysicalPropertiesOutput
        Site: Site
    end

end

return M
