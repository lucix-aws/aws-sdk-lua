-- Code generated by lua-client-codegen. DO NOT EDIT.

local id = require("smithy.shape_id")
local schema = require("smithy.schema")
local prelude = require("smithy.prelude")
local traits = require("smithy.traits")
local smithy = require("smithy")

local _N = "com.amazonaws.sagemakerjobruntime"

local M: {string:any} = {}

M.DoubleList = schema.new({ type = "list", list_member = prelude.Double })

M.AccessDeniedException = schema.new({
    id = id.from(_N, "AccessDeniedException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "AccessDeniedException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.CompleteRolloutInput = schema.new({
    id = id.from(_N, "CompleteRolloutRequest"),
    type = "structure",
    members = {
        JobArn = schema.new({
            id = id.from(_N, "CompleteRolloutInput", "JobArn"),
            type = "string",
            name = "JobArn",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_HEADER] = { name = "X-Amzn-SageMaker-Job-Arn" },
            },
        }),
        TrajectoryId = schema.new({
            id = id.from(_N, "CompleteRolloutInput", "TrajectoryId"),
            type = "string",
            name = "TrajectoryId",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        Status = schema.new({
            id = id.from(_N, "CompleteRolloutInput", "Status"),
            type = "string",
            name = "Status",
            target_id = prelude.String.id,
        }),
        ClientToken = schema.new({
            id = id.from(_N, "CompleteRolloutInput", "ClientToken"),
            type = "string",
            name = "ClientToken",
            target_id = prelude.String.id,
            traits = {
                [traits.IDEMPOTENCY_TOKEN] = {},
            },
        }),
    },
})

M.CompleteRolloutOutput = schema.new({
    id = id.from(_N, "CompleteRolloutResponse"),
    type = "structure",
})

M.ConflictException = schema.new({
    id = id.from(_N, "ConflictException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "ConflictException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.InternalServiceError = schema.new({
    id = id.from(_N, "InternalServiceError"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "server" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "InternalServiceError", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.ResourceNotFoundException = schema.new({
    id = id.from(_N, "ResourceNotFoundException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "ResourceNotFoundException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.ServiceQuotaExceededException = schema.new({
    id = id.from(_N, "ServiceQuotaExceededException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "ServiceQuotaExceededException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.ThrottlingException = schema.new({
    id = id.from(_N, "ThrottlingException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "ThrottlingException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.ValidationException = schema.new({
    id = id.from(_N, "ValidationException"),
    type = "structure",
    traits = {
        [traits.ERROR] = { value = "client" },
    },
    members = {
        Message = schema.new({
            id = id.from(_N, "ValidationException", "Message"),
            type = "string",
            name = "Message",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
    },
})

M.SampleInput = schema.new({
    id = id.from(_N, "SampleRequest"),
    type = "structure",
    members = {
        JobArn = schema.new({
            id = id.from(_N, "SampleInput", "JobArn"),
            type = "string",
            name = "JobArn",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_HEADER] = { name = "X-Amzn-SageMaker-Job-Arn" },
            },
        }),
        TrajectoryId = schema.new({
            id = id.from(_N, "SampleInput", "TrajectoryId"),
            type = "string",
            name = "TrajectoryId",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_HEADER] = { name = "X-Amzn-SageMaker-Trajectory-Id" },
            },
        }),
        Body = schema.new({
            id = id.from(_N, "SampleInput", "Body"),
            type = "blob",
            name = "Body",
            target_id = prelude.Blob.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_PAYLOAD] = {},
            },
        }),
    },
})

M.SampleOutput = schema.new({
    id = id.from(_N, "SampleResponse"),
    type = "structure",
    members = {
        ContentType = schema.new({
            id = id.from(_N, "SampleOutput", "ContentType"),
            type = "string",
            name = "ContentType",
            target_id = prelude.String.id,
            traits = {
                [traits.HTTP_HEADER] = { name = "Content-Type" },
            },
        }),
        Body = schema.new({
            id = id.from(_N, "SampleOutput", "Body"),
            type = "blob",
            name = "Body",
            target_id = prelude.Blob.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_PAYLOAD] = {},
            },
        }),
    },
})

M.SampleWithResponseStreamInput = schema.new({
    id = id.from(_N, "SampleWithResponseStreamRequest"),
    type = "structure",
    members = {
        JobArn = schema.new({
            id = id.from(_N, "SampleWithResponseStreamInput", "JobArn"),
            type = "string",
            name = "JobArn",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_HEADER] = { name = "X-Amzn-SageMaker-Job-Arn" },
            },
        }),
        TrajectoryId = schema.new({
            id = id.from(_N, "SampleWithResponseStreamInput", "TrajectoryId"),
            type = "string",
            name = "TrajectoryId",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_HEADER] = { name = "X-Amzn-SageMaker-Trajectory-Id" },
            },
        }),
        Body = schema.new({
            id = id.from(_N, "SampleWithResponseStreamInput", "Body"),
            type = "blob",
            name = "Body",
            target_id = prelude.Blob.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_PAYLOAD] = {},
            },
        }),
    },
})

M.SampleWithResponseStreamOutput = schema.new({
    id = id.from(_N, "SampleWithResponseStreamResponse"),
    type = "structure",
    members = {
        ContentType = schema.new({
            id = id.from(_N, "SampleWithResponseStreamOutput", "ContentType"),
            type = "string",
            name = "ContentType",
            target_id = prelude.String.id,
            traits = {
                [traits.HTTP_HEADER] = { name = "Content-Type" },
            },
        }),
        Body = schema.new({
            id = id.from(_N, "SampleWithResponseStreamOutput", "Body"),
            type = "blob",
            name = "Body",
            target_id = prelude.Blob.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_PAYLOAD] = {},
                [traits.STREAMING] = {},
            },
            direct_traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_PAYLOAD] = {},
            },
        }),
    },
})

M.UpdateRewardInput = schema.new({
    id = id.from(_N, "UpdateRewardRequest"),
    type = "structure",
    members = {
        JobArn = schema.new({
            id = id.from(_N, "UpdateRewardInput", "JobArn"),
            type = "string",
            name = "JobArn",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
                [traits.HTTP_HEADER] = { name = "X-Amzn-SageMaker-Job-Arn" },
            },
        }),
        TrajectoryId = schema.new({
            id = id.from(_N, "UpdateRewardInput", "TrajectoryId"),
            type = "string",
            name = "TrajectoryId",
            target_id = prelude.String.id,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        Rewards = schema.new({
            id = id.from(_N, "UpdateRewardInput", "Rewards"),
            type = "list",
            name = "Rewards",
            target_id = prelude.Document.id,
            list_member = prelude.Double,
            traits = {
                [traits.REQUIRED] = {},
            },
        }),
        ClientToken = schema.new({
            id = id.from(_N, "UpdateRewardInput", "ClientToken"),
            type = "string",
            name = "ClientToken",
            target_id = prelude.String.id,
            traits = {
                [traits.IDEMPOTENCY_TOKEN] = {},
            },
        }),
    },
})

M.UpdateRewardOutput = schema.new({
    id = id.from(_N, "UpdateRewardResponse"),
    type = "structure",
})

-- Fix forward references for recursive schemas
for _, _s in pairs(M) do
    local s = _s as {string:any}
    if type(s) == "table" and (s.type == "structure" or s.type == "union") then
        local members = rawget(s, "_members") as {string:{string:any}}
        if members then
            for _, ms in pairs(members) do
                if (ms.type == "structure" or ms.type == "union") and not rawget(ms as {string:any}, "_target") and ms.target_id then
                    rawset(ms as {string:any}, "_target", M[(ms.target_id as {string:string}).name])
                end
            end
        end
    end
end

M.Service = schema.service({
    id = id.from("com.amazonaws.sagemakerjobruntime", "AgenticRFTRuntimeService"),
    version = "2026-02-01",
    traits = {
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

M.CompleteRollout = schema.operation({
    id = id.from("com.amazonaws.sagemakerjobruntime", "CompleteRollout"),
    input = M.CompleteRolloutInput,
    output = M.CompleteRolloutOutput,
    traits = {
        [traits.HTTP] = { method = "POST", path = "/complete-rollout" },
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

M.Sample = schema.operation({
    id = id.from("com.amazonaws.sagemakerjobruntime", "Sample"),
    input = M.SampleInput,
    output = M.SampleOutput,
    traits = {
        [traits.HTTP] = { method = "POST", path = "/sample" },
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

M.SampleWithResponseStream = schema.operation({
    id = id.from("com.amazonaws.sagemakerjobruntime", "SampleWithResponseStream"),
    input = M.SampleWithResponseStreamInput,
    output = M.SampleWithResponseStreamOutput,
    traits = {
        [traits.HTTP] = { method = "POST", path = "/sample-with-response-stream" },
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

M.UpdateReward = schema.operation({
    id = id.from("com.amazonaws.sagemakerjobruntime", "UpdateReward"),
    input = M.UpdateRewardInput,
    output = M.UpdateRewardOutput,
    traits = {
        [traits.HTTP] = { method = "POST", path = "/update-reward" },
        [traits.AUTH] = {
            { scheme_id = "aws.auth#sigv4" },
        },
    },
})

return M
