-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- You do not have sufficient access to perform this action.
    record AccessDeniedByMetaException
        message: string
    end

    -- You do not have sufficient access to perform this action.
    record AccessDeniedException
        message: string
    end

    -- The tag for a resource.
    record Tag
        -- The tag key.
        key: string
        -- The tag value.
        value: string
    end

    -- The registration details for a linked phone number.
    record WabaPhoneNumberSetupFinalization
        -- The unique identifier of the originating phone number associated with the media. Phone
        -- number identifiers are formatted as
        -- phone-number-id-01234567890123456789012345678901. Use the
        -- GetLinkedWhatsAppBusinessAccount
        -- API action to find a phone number's id.
        id: string
        -- The PIN to use for two-step verification. To reset your PIN follow the directions in
        -- Updating PIN in the WhatsApp Business Platform Cloud API
        -- Reference.
        twoFactorPin: string
        -- The two letter ISO region for the location of where Meta will store data.
        -- Asia–Pacific (APAC)
        -- Australia AU
        -- Indonesia ID
        -- India IN
        -- Japan JP
        -- Singapore SG
        -- South Korea KR
        -- Europe
        -- Germany DE
        -- Switzerland CH
        -- United Kingdom GB
        -- Latin America (LATAM)
        -- Brazil BR
        -- Middle East and Africa (MEA)
        -- Bahrain BH
        -- South Africa ZA
        -- United Arab Emirates AE
        -- North America (NORAM)
        -- Canada CA
        dataLocalizationRegion: string
        -- An array of key and value pair tags.
        tags: {Tag}
    end

    -- Contains information on the event destination.
    record WhatsAppBusinessAccountEventDestination
        -- The ARN of the event destination.
        eventDestinationArn: string
        -- The Amazon Resource Name (ARN) of an Identity and Access Management role
        -- that is able to import phone numbers and write events.
        roleArn: string
    end

    -- The registration details for a linked WhatsApp Business Account.
    record WabaSetupFinalization
        -- The ID of the linked WhatsApp Business Account, formatted as waba-01234567890123456789012345678901.
        id: string
        -- The event destinations for the linked WhatsApp Business Account.
        eventDestinations: {WhatsAppBusinessAccountEventDestination}
        -- An array of key and value pair tags.
        tags: {Tag}
    end

    -- The details of linking a WhatsApp Business Account to your Amazon Web Services account.
    record WhatsAppSetupFinalization
        -- An Amazon Web Services access token generated by WhatsAppSignupCallback and used by WhatsAppSetupFinalization.
        associateInProgressToken: string
        -- An array of WabaPhoneNumberSetupFinalization objects containing the details of each phone number associated with the WhatsApp Business Account.
        phoneNumbers: {WabaPhoneNumberSetupFinalization}
        -- Used to add a new phone number to an existing WhatsApp Business Account. This field can't be used when the waba field is present.
        phoneNumberParent: string
        -- Used to create a new WhatsApp Business Account and add a phone number. This field can't be used when the phoneNumberParent field is present.
        waba: WabaSetupFinalization
    end

    -- Contains the accessToken provided by Meta during signup.
    record WhatsAppSignupCallback
        -- The access token for your WhatsApp Business Account. The accessToken value is provided by Meta.
        accessToken: string
        -- The URL where WhatsApp will send callback notifications for this account.
        callbackUrl: string
    end

    record AssociateWhatsAppBusinessAccountInput
        -- Contains the callback access token.
        signupCallback: WhatsAppSignupCallback
        -- A JSON object that contains the phone numbers and WhatsApp Business Account to link to your account.
        setupFinalization: WhatsAppSetupFinalization
    end

    enum RegistrationStatus
        "COMPLETE"
        "INCOMPLETE"
    end

    -- The details of your WhatsApp phone number.
    record WhatsAppPhoneNumberDetail
        -- The ARN of the WhatsApp phone number.
        arn: string
        -- The phone number for sending WhatsApp.
        phoneNumber: string
        -- The phone number ID. Phone number identifiers are formatted as phone-number-id-01234567890123456789012345678901.
        phoneNumberId: string
        -- The phone number ID from Meta.
        metaPhoneNumberId: string
        -- The display name for this phone number.
        displayPhoneNumberName: string
        -- The phone number that appears in the recipients display.
        displayPhoneNumber: string
        -- The quality rating of the phone number.
        qualityRating: string
        -- The geographic region where the WhatsApp phone number's data is stored and processed.
        dataLocalizationRegion: string
    end

    -- Contains your WhatsApp registration status and details of any unregistered WhatsApp
    -- phone number.
    record LinkedWhatsAppBusinessAccountIdMetaData
        -- The name of your account.
        accountName: string
        -- The registration status of the linked WhatsApp Business Account.
        registrationStatus: string
        -- The details for unregistered WhatsApp phone numbers.
        unregisteredWhatsAppPhoneNumbers: {WhatsAppPhoneNumberDetail}
        -- The Amazon Resource Name (ARN) of the WhatsApp Business Account ID.
        wabaId: string
    end

    -- Contains the results of WhatsAppSignupCallback.
    record WhatsAppSignupCallbackResult
        -- An Amazon Web Services access token generated by WhatsAppSignupCallback and used by WhatsAppSetupFinalization.
        associateInProgressToken: string
        -- A LinkedWhatsAppBusinessAccountIdMetaData object map containing the details of any WhatsAppBusiness accounts that have incomplete setup.
        linkedAccountsWithIncompleteSetup: {string : LinkedWhatsAppBusinessAccountIdMetaData}
    end

    record AssociateWhatsAppBusinessAccountOutput
        -- Contains your WhatsApp registration status.
        signupCallbackResult: WhatsAppSignupCallbackResult
        -- The status code for the response.
        statusCode: number
        -- The ID of the WhatsApp Business Account that was linked to your Amazon Web Services account.
        linkedWhatsAppBusinessAccountId: string
    end

    -- Thrown when performing an action because a dependency would be broken.
    record DependencyException
        message: string
    end

    -- One or more parameters provided to the action are not valid.
    record InvalidParametersException
        message: string
    end

    -- The request was denied because it would exceed one or more service quotas or limits.
    record LimitExceededException
        message: string
    end

    -- The request was denied due to request throttling.
    record ThrottledRequestException
        message: string
    end

    -- The request contains an invalid parameter value.
    record ValidationException
        message: string
    end

    record CreateWhatsAppMessageTemplateInput
        -- The complete template definition as a JSON blob.
        templateDefinition: string
        -- The ID of the WhatsApp Business Account to associate with this template.
        id: string
    end

    record CreateWhatsAppMessageTemplateOutput
        -- The numeric ID assigned to the template by Meta.
        metaTemplateId: string
        -- The status of the created template, such as PENDING or APPROVED..
        templateStatus: string
        -- The category of the template, such as UTILITY or MARKETING.
        category: string
    end

    -- The request processing has failed because of an unknown error, exception, or
    -- failure.
    record InternalServiceException
        message: string
    end

    -- The resource was not found.
    record ResourceNotFoundException
        message: string
    end

    -- Configuration options for customizing the body content of a template from Meta's library.
    record LibraryTemplateBodyInputs
        -- When true, includes a contact number in the template body.
        addContactNumber: boolean
        -- When true, includes a "learn more" link in the template body.
        addLearnMoreLink: boolean
        -- When true, includes security recommendations in the template body.
        addSecurityRecommendation: boolean
        -- When true, includes a package tracking link in the template body.
        addTrackPackageLink: boolean
        -- The number of minutes until a verification code or OTP expires.
        codeExpirationMinutes: number
    end

    -- Configuration options for customizing buttons in a template from Meta's library.
    record LibraryTemplateButtonInput
        -- The phone number in E.164 format for CALL-type buttons.
        phoneNumber: string
        -- The URL with dynamic parameters for URL-type buttons.
        url: {string : string}
        -- The type of one-time password for OTP buttons.
        otpType: string
        -- When true, indicates acceptance of zero-tap terms for the button.
        zeroTapTermsAccepted: boolean
        -- List of supported applications for this button type.
        supportedApps: {{string : string}}
    end

    -- Represents a template from Meta's library with customization options.
    record MetaLibraryTemplate
        -- The name to assign to the template.
        templateName: string
        -- The name of the template in Meta's library.
        libraryTemplateName: string
        -- The category of the template (for example, UTILITY or MARKETING).
        templateCategory: string
        -- The language code for the template (for example, en_US).
        templateLanguage: string
        -- Button customizations for the template.
        libraryTemplateButtonInputs: {LibraryTemplateButtonInput}
        -- Body text customizations for the template.
        libraryTemplateBodyInputs: LibraryTemplateBodyInputs
    end

    record CreateWhatsAppMessageTemplateFromLibraryInput
        -- The template configuration from Meta's library, including customizations for buttons and body text.
        metaLibraryTemplate: MetaLibraryTemplate
        -- The ID of the WhatsApp Business Account to associate with this template.
        id: string
    end

    record CreateWhatsAppMessageTemplateFromLibraryOutput
        -- The numeric ID assigned to the template by Meta.
        metaTemplateId: string
        -- The status of the created template (for example, PENDING or APPROVED).
        templateStatus: string
        -- The category of the template (for example, UTILITY or MARKETING).
        category: string
    end

    -- Contains information for the S3 bucket that contains media files.
    record S3File
        -- The bucket name.
        bucketName: string
        -- The S3 key prefix that defines the storage location of your media files. The prefix works like a folder path in S3,
        -- and is combined with the WhatsApp mediaId to create the final file path.
        -- For example, if a media file's WhatsApp mediaId is 123.ogg, and the key is audio/example.ogg,
        -- the final file path is audio/example.ogg123.ogg.
        -- For the same mediaId, a key of audio/ results in the file path audio/123.ogg.
        key: string
    end

    record CreateWhatsAppMessageTemplateMediaInput
        -- The ID of the WhatsApp Business Account associated with this media upload.
        id: string
        sourceS3File: S3File
    end

    record CreateWhatsAppMessageTemplateMediaOutput
        -- The handle assigned to the uploaded media by Meta, used to reference the media in templates.
        metaHeaderHandle: string
    end

    record DeleteWhatsAppMessageMediaInput
        -- The unique identifier of the media file to delete. Use the mediaId returned from PostWhatsAppMessageMedia.
        mediaId: string
        -- The unique identifier of the originating phone number associated with the media. Phone
        -- number identifiers are formatted as
        -- phone-number-id-01234567890123456789012345678901. Use
        -- GetLinkedWhatsAppBusinessAccount to find a phone number's
        -- id.
        originationPhoneNumberId: string
    end

    record DeleteWhatsAppMessageMediaOutput
        -- Success indicator for deleting the media file.
        success: boolean
    end

    record DeleteWhatsAppMessageTemplateInput
        -- The numeric ID of the template assigned by Meta.
        metaTemplateId: string
        -- If true, deletes all language versions of the template.
        deleteAllLanguages: boolean
        -- The ID of the WhatsApp Business Account associated with this template.
        id: string
        -- The name of the template to delete.
        templateName: string
    end

    record DeleteWhatsAppMessageTemplateOutput
    end

    record DisassociateWhatsAppBusinessAccountInput
        -- The unique identifier of your WhatsApp Business Account. WABA identifiers are formatted as
        -- waba-01234567890123456789012345678901. Use
        -- ListLinkedWhatsAppBusinessAccounts to list all WABAs
        -- and their details.
        id: string
    end

    record DisassociateWhatsAppBusinessAccountOutput
    end

    record GetLinkedWhatsAppBusinessAccountInput
        -- The unique identifier, from Amazon Web Services, of the linked WhatsApp Business
        -- Account. WABA identifiers are formatted as
        -- waba-01234567890123456789012345678901. Use
        -- ListLinkedWhatsAppBusinessAccounts to list all WABAs and their details.
        id: string
    end

    -- The details of a linked phone number.
    record WhatsAppPhoneNumberSummary
        -- The full Amazon Resource Name (ARN) for the phone number.
        arn: string
        -- The phone number associated with the Linked WhatsApp Business Account.
        phoneNumber: string
        -- The phone number ID. Phone number identifiers are formatted as phone-number-id-01234567890123456789012345678901.
        phoneNumberId: string
        -- The phone number ID from Meta.
        metaPhoneNumberId: string
        -- The display name for this phone number.
        displayPhoneNumberName: string
        -- The phone number that appears in the recipients display.
        displayPhoneNumber: string
        -- The quality rating of the phone number. This is from Meta.
        qualityRating: string
        -- The geographic region where the WhatsApp phone number's data is stored and processed.
        dataLocalizationRegion: string
    end

    -- The details of your linked WhatsApp Business Account.
    record LinkedWhatsAppBusinessAccount
        -- The ARN of the linked WhatsApp Business Account.
        arn: string
        -- The ID of the linked WhatsApp Business Account, formatted as waba-01234567890123456789012345678901.
        id: string
        -- The WhatsApp Business Account ID from meta.
        wabaId: string
        -- The registration status of the linked WhatsApp Business Account.
        registrationStatus: string
        -- The date the WhatsApp Business Account was linked.
        linkDate: number
        -- The name of the linked WhatsApp Business Account.
        wabaName: string
        -- The event destinations for the linked WhatsApp Business Account.
        eventDestinations: {WhatsAppBusinessAccountEventDestination}
        -- The onboarding status for the Marketing Messages API. This value is fetched from Meta and indicates whether the WhatsApp Business Account is onboarded for Meta's Marketing Messages API.
        marketingMessagesOnboardingStatus: string
        -- The phone numbers associated with the Linked WhatsApp Business Account.
        phoneNumbers: {WhatsAppPhoneNumberSummary}
    end

    record GetLinkedWhatsAppBusinessAccountOutput
        -- The details of the linked WhatsApp Business Account.
        account: LinkedWhatsAppBusinessAccount
    end

    record GetLinkedWhatsAppBusinessAccountPhoneNumberInput
        -- The unique identifier of the phone number. Phone number
        -- identifiers are formatted as phone-number-id-01234567890123456789012345678901.
        -- Use GetLinkedWhatsAppBusinessAccount
        -- to find a phone number's id.
        id: string
    end

    record GetLinkedWhatsAppBusinessAccountPhoneNumberOutput
        phoneNumber: WhatsAppPhoneNumberDetail
        -- The WABA identifier linked to the phone number, formatted as
        -- waba-01234567890123456789012345678901.
        linkedWhatsAppBusinessAccountId: string
    end

    -- You can use presigned URLs to grant time-limited access to objects in Amazon S3 without updating your bucket policy. For more information, see Working with presigned URLs in the Amazon S3
    -- User Guide.
    record S3PresignedUrl
        -- The presign url to the object.
        url: string
        -- A map of headers and their values. You must specify the Content-Type header when using PostWhatsAppMessageMedia. For a list of common headers, see Common Request Headers in the Amazon S3
        -- API Reference
        headers: {string : string}
    end

    record GetWhatsAppMessageMediaInput
        -- The unique identifier for the media file.
        mediaId: string
        -- The unique identifier of the originating phone number for the WhatsApp message media.
        -- The phone number identifiers are formatted as
        -- phone-number-id-01234567890123456789012345678901. Use
        -- GetLinkedWhatsAppBusinessAccount to find a phone number's
        -- id.
        originationPhoneNumberId: string
        -- Set to True to get only the metadata for the file.
        metadataOnly: boolean
        -- The presign url of the media file.
        destinationS3PresignedUrl: S3PresignedUrl
        -- The bucketName and key of the S3 media file.
        destinationS3File: S3File
    end

    record GetWhatsAppMessageMediaOutput
        -- The MIME type of the media.
        mimeType: string
        -- The size of the media file, in KB.
        fileSize: number
    end

    record GetWhatsAppMessageTemplateInput
        -- The numeric ID of the template assigned by Meta.
        metaTemplateId: string
        -- The ID of the WhatsApp Business Account associated with this template.
        id: string
        -- The name of the message template. Use together with templateLanguageCode as an alternative to metaTemplateId to identify a template.
        templateName: string
        -- The language code of the message template (for example, en or en_US). Use together with templateName as an alternative to metaTemplateId to identify a template.
        templateLanguageCode: string
    end

    record GetWhatsAppMessageTemplateOutput
        -- The complete template definition as a JSON string (maximum 6000 characters).
        template: string
    end

    -- Defines a button in a template from Meta's library.
    record LibraryTemplateButtonList
        -- The text displayed on the button (maximum 40 characters).
        text: string
        -- The phone number in E.164 format for CALL-type buttons.
        phoneNumber: string
        -- The URL for URL-type buttons.
        url: string
        -- The type of one-time password for OTP buttons.
        otpType: string
        -- When true, indicates acceptance of zero-tap terms for the button.
        zeroTapTermsAccepted: boolean
        -- List of supported applications for this button type.
        supportedApps: {{string : string}}
    end

    record ListLinkedWhatsAppBusinessAccountsInput
        -- The next token for pagination.
        nextToken: string
        -- The maximum number of results to return.
        maxResults: number
    end

    -- The details of a linked WhatsApp Business Account.
    record LinkedWhatsAppBusinessAccountSummary
        -- The ARN of the linked WhatsApp Business Account.
        arn: string
        -- The ID of the linked WhatsApp Business Account, formatted as waba-01234567890123456789012345678901.
        id: string
        -- The WhatsApp Business Account ID provided by Meta.
        wabaId: string
        -- The registration status of the linked WhatsApp Business Account.
        registrationStatus: string
        -- The date the WhatsApp Business Account was linked.
        linkDate: number
        -- The name of the linked WhatsApp Business Account.
        wabaName: string
        -- The event destinations for the linked WhatsApp Business Account.
        eventDestinations: {WhatsAppBusinessAccountEventDestination}
        -- The onboarding status for the Marketing Messages API. This value is fetched from Meta and indicates whether the WhatsApp Business Account is onboarded for Meta's Marketing Messages API.
        marketingMessagesOnboardingStatus: string
    end

    record ListLinkedWhatsAppBusinessAccountsOutput
        -- A list of WhatsApp Business Accounts linked to your Amazon Web Services account.
        linkedAccounts: {LinkedWhatsAppBusinessAccountSummary}
        -- The next token for pagination.
        nextToken: string
    end

    record ListWhatsAppMessageTemplatesInput
        -- The ID of the WhatsApp Business Account to list templates for.
        id: string
        -- The token for the next page of results.
        nextToken: string
        -- The maximum number of results to return per page (1-100).
        maxResults: number
    end

    -- Provides a summary of a WhatsApp message template's key attributes.
    record TemplateSummary
        -- The name of the template.
        templateName: string
        -- The numeric ID assigned to the template by Meta.
        metaTemplateId: string
        -- The current status of the template (for example, APPROVED, PENDING, or REJECTED).
        templateStatus: string
        -- The quality score assigned to the template by Meta.
        templateQualityScore: string
        -- The language code of the template (for example, en_US).
        templateLanguage: string
        -- The category of the template (for example, UTILITY or MARKETING).
        templateCategory: string
    end

    record ListWhatsAppMessageTemplatesOutput
        -- A list of template summaries.
        templates: {TemplateSummary}
        -- The token to retrieve the next page of results, if any.
        nextToken: string
    end

    record ListWhatsAppTemplateLibraryInput
        -- The token for the next page of results.
        nextToken: string
        -- The maximum number of results to return per page (1-100).
        maxResults: number
        -- The ID of the WhatsApp Business Account to list library templates for.
        id: string
        -- Map of filters to apply (searchKey, topic, usecase, industry, language).
        filters: {string : string}
    end

    -- Defines the complete structure and content of a template in Meta's library.
    record MetaLibraryTemplateDefinition
        -- The name of the template.
        templateName: string
        -- The language code for the template (for example, en_US).
        templateLanguage: string
        -- The category of the template (for example, UTILITY or MARKETING).
        templateCategory: string
        -- The topic or subject matter of the template.
        templateTopic: string
        -- The intended use case for the template.
        templateUseCase: string
        -- The industries the template is designed for.
        templateIndustry: {string}
        -- The header text of the template.
        templateHeader: string
        -- The body text of the template.
        templateBody: string
        -- The buttons included in the template.
        templateButtons: {LibraryTemplateButtonList}
        -- The ID of the template in Meta's library.
        templateId: string
        -- Example parameter values for the template body, used to demonstrate how dynamic content appears in the template.
        templateBodyExampleParams: {string}
    end

    record ListWhatsAppTemplateLibraryOutput
        -- A list of templates from Meta's library.
        metaLibraryTemplates: {MetaLibraryTemplateDefinition}
        -- The token to retrieve the next page of results, if any.
        nextToken: string
    end

    record PutWhatsAppBusinessAccountEventDestinationsInput
        -- The unique identifier of your WhatsApp Business Account. WABA identifiers are formatted as
        -- waba-01234567890123456789012345678901. Use
        -- ListLinkedWhatsAppBusinessAccounts to list all WABAs and their details.
        id: string
        -- An array of WhatsAppBusinessAccountEventDestination event destinations.
        eventDestinations: {WhatsAppBusinessAccountEventDestination}
    end

    record PutWhatsAppBusinessAccountEventDestinationsOutput
    end

    record UpdateWhatsAppMessageTemplateInput
        -- The ID of the WhatsApp Business Account associated with this template.
        id: string
        -- The numeric ID of the template assigned by Meta.
        metaTemplateId: string
        -- The name of the message template. Use together with templateLanguageCode as an alternative to metaTemplateId to identify a template.
        templateName: string
        -- The language code of the message template (for example, en or en_US). Use together with templateName as an alternative to metaTemplateId to identify a template.
        templateLanguageCode: string
        -- The format specification for parameters in the template, this can be either 'named' or 'positional'.
        parameterFormat: string
        -- The new category for the template (for example, UTILITY or MARKETING).
        templateCategory: string
        -- The updated components of the template as a JSON blob (maximum 3000 characters).
        templateComponents: string
        -- When true, disables click tracking for call-to-action URL buttons in the template.
        ctaUrlLinkTrackingOptedOut: boolean
    end

    record UpdateWhatsAppMessageTemplateOutput
    end

    record PostWhatsAppMessageMediaInput
        -- The ID of the phone number to associate with the WhatsApp media file. The phone number
        -- identifiers are formatted as phone-number-id-01234567890123456789012345678901.
        -- Use GetLinkedWhatsAppBusinessAccount
        -- to find a phone number's id.
        originationPhoneNumberId: string
        -- The source presign url of the media file.
        sourceS3PresignedUrl: S3PresignedUrl
        -- The source S3 url for the media file.
        sourceS3File: S3File
    end

    record PostWhatsAppMessageMediaOutput
        -- The unique identifier of the posted WhatsApp message.
        mediaId: string
    end

    record SendWhatsAppMessageInput
        -- The ID of the phone number used to send the WhatsApp message. If you are sending a media
        -- file only the originationPhoneNumberId used to upload the file can be used.
        -- Phone number identifiers are formatted as
        -- phone-number-id-01234567890123456789012345678901. Use
        -- GetLinkedWhatsAppBusinessAccount to find a phone number's
        -- id.
        originationPhoneNumberId: string
        -- The message to send through WhatsApp. The length is in KB. The message field passes through a WhatsApp
        -- Message object, see Messages in the WhatsApp Business Platform Cloud API
        -- Reference.
        message: string
        -- The API version for the request formatted as v{VersionNumber}. For a list of supported API versions and Amazon Web Services Regions, see
        -- Amazon Web Services End User Messaging Social API Service Endpoints in the Amazon Web Services General Reference.
        metaApiVersion: string
    end

    record SendWhatsAppMessageOutput
        -- The unique identifier of the message.
        messageId: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource to retrieve the tags from.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- The status code of the response.
        statusCode: number
        -- The tags for the resource.
        tags: {Tag}
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource to tag.
        resourceArn: string
        -- The tags to add to the resource.
        tags: {Tag}
    end

    record TagResourceOutput
        -- The status code of the tag resource operation.
        statusCode: number
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource to remove tags from.
        resourceArn: string
        -- The keys of the tags to remove from the resource.
        tagKeys: {string}
    end

    record UntagResourceOutput
        -- The status code of the untag resource operation.
        statusCode: number
    end

end

return M
