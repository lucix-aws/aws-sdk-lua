-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- You do not have sufficient access to perform this action.
    record AccessDeniedException
        message: string
    end

    record GetBaselineOperationInput
        -- The operation ID returned from mutating asynchronous APIs (Enable, Disable, Update, Reset).
        operationIdentifier: string
    end

    enum BaselineOperationType
        "ENABLE_BASELINE"
        "DISABLE_BASELINE"
        "UPDATE_ENABLED_BASELINE"
        "RESET_ENABLED_BASELINE"
    end

    enum BaselineOperationStatus
        "SUCCEEDED"
        "FAILED"
        "IN_PROGRESS"
    end

    -- An object of shape BaselineOperation, returning details about the specified Baseline operation ID.
    record BaselineOperation
        -- The identifier of the specified operation.
        operationIdentifier: string
        -- An enumerated type (enum) with possible values of ENABLE_BASELINE, DISABLE_BASELINE, UPDATE_ENABLED_BASELINE, or RESET_ENABLED_BASELINE.
        operationType: string
        -- An enumerated type (enum) with possible values of SUCCEEDED, FAILED, or IN_PROGRESS.
        status: string
        -- The start time of the operation, in ISO 8601 format.
        startTime: number
        -- The end time of the operation (if applicable), in ISO 8601 format.
        endTime: number
        -- A status message that gives more information about the operation's status, if applicable.
        statusMessage: string
    end

    record GetBaselineOperationOutput
        -- A baselineOperation object that shows information about the specified operation ID.
        baselineOperation: BaselineOperation
    end

    -- An unexpected error occurred during processing of a request.
    record InternalServerException
        message: string
    end

    -- The request references a resource that does not exist.
    record ResourceNotFoundException
        message: string
    end

    -- The request was denied due to request throttling.
    record ThrottlingException
        message: string
        -- The ID of the service that is associated with the error.
        serviceCode: string
        -- The ID of the service quota that was exceeded.
        quotaCode: string
        -- The number of seconds the caller should wait before retrying.
        retryAfterSeconds: number
    end

    -- The input does not satisfy the constraints specified by an Amazon Web Services service.
    record ValidationException
        message: string
    end

    record GetBaselineInput
        -- The ARN of the Baseline resource to be retrieved.
        baselineIdentifier: string
    end

    record GetBaselineOutput
        -- The baseline ARN.
        arn: string
        -- A user-friendly name for the baseline.
        name: string
        -- A description of the baseline.
        description: string
    end

    record ListBaselinesInput
        -- A pagination token.
        nextToken: string
        -- The maximum number of results to be shown.
        maxResults: number
    end

    -- Returns a summary of information about a Baseline object.
    record BaselineSummary
        -- The full ARN of a Baseline.
        arn: string
        -- The human-readable name of a Baseline.
        name: string
        -- A summary description of a Baseline.
        description: string
    end

    record ListBaselinesOutput
        -- A list of Baseline object details.
        baselines: {BaselineSummary}
        -- A pagination token.
        nextToken: string
    end

    record GetControlOperationInput
        -- The ID of the asynchronous operation, which is used to track status. The operation is available for 90 days.
        operationIdentifier: string
    end

    enum ControlOperationType
        "ENABLE_CONTROL"
        "DISABLE_CONTROL"
        "UPDATE_ENABLED_CONTROL"
        "RESET_ENABLED_CONTROL"
    end

    enum ControlOperationStatus
        "SUCCEEDED"
        "FAILED"
        "IN_PROGRESS"
    end

    -- An operation performed by the control.
    record ControlOperation
        -- One of ENABLE_CONTROL or DISABLE_CONTROL.
        operationType: string
        -- The time that the operation began.
        startTime: number
        -- The time that the operation finished.
        endTime: number
        -- One of IN_PROGRESS, SUCEEDED, or FAILED.
        status: string
        -- If the operation result is FAILED, this string contains a message explaining why the operation failed.
        statusMessage: string
        -- The identifier of the specified operation.
        operationIdentifier: string
        -- The controlIdentifier of the control for the operation.
        controlIdentifier: string
        -- The target upon which the control operation is working.
        targetIdentifier: string
        -- The controlIdentifier of the enabled control.
        enabledControlIdentifier: string
    end

    record GetControlOperationOutput
        -- An operation performed by the control.
        controlOperation: ControlOperation
    end

    -- A filter object that lets you call ListControlOperations with a specific filter.
    record ControlOperationFilter
        -- The set of controlIdentifier returned by the filter.
        controlIdentifiers: {string}
        -- The set of targetIdentifier objects returned by the filter.
        targetIdentifiers: {string}
        -- The set controlIdentifier of enabled controls selected by the filter.
        enabledControlIdentifiers: {string}
        -- Lists the status of control operations.
        statuses: {string}
        -- The set of ControlOperation objects returned by the filter.
        controlOperationTypes: {string}
    end

    record ListControlOperationsInput
        -- An input filter for the ListControlOperations API that lets you select the types of control operations to view.
        filter: ControlOperationFilter
        -- A pagination token.
        nextToken: string
        -- The maximum number of results to be shown.
        maxResults: number
    end

    -- A summary of information about the specified control operation.
    record ControlOperationSummary
        -- The type of operation.
        operationType: string
        -- The time at which a control operation began.
        startTime: number
        -- The time at which the control operation was completed.
        endTime: number
        -- The status of the specified control operation.
        status: string
        -- A speficic message displayed as part of the control status.
        statusMessage: string
        -- The unique identifier of a control operation.
        operationIdentifier: string
        -- The controlIdentifier of a control.
        controlIdentifier: string
        -- The unique identifier of the target of a control operation.
        targetIdentifier: string
        -- The controlIdentifier of an enabled control.
        enabledControlIdentifier: string
    end

    record ListControlOperationsOutput
        -- Returns a list of output from control operations.
        controlOperations: {ControlOperationSummary}
        -- A pagination token.
        nextToken: string
    end

    -- Updating or deleting the resource can cause an inconsistent state.
    record ConflictException
        message: string
    end

    record DisableControlInput
        -- The ARN of the control. Only Strongly recommended and Elective controls are permitted, with the exception of the Region deny control. For information on how to find the controlIdentifier, see the overview page.
        controlIdentifier: string
        -- The ARN of the organizational unit. For information on how to find the targetIdentifier, see the overview page.
        targetIdentifier: string
        -- The ARN of the enabled control to be disabled, which uniquely identifies the control instance on the target organizational unit.
        enabledControlIdentifier: string
    end

    record DisableControlOutput
        -- The ID of the asynchronous operation, which is used to track status. The operation is available for 90 days.
        operationIdentifier: string
    end

    -- The request would cause a service quota to be exceeded. See Service quotas.
    record ServiceQuotaExceededException
        message: string
    end

    record DisableBaselineInput
        -- Identifier of the EnabledBaseline resource to be deactivated, in ARN format.
        enabledBaselineIdentifier: string
    end

    record DisableBaselineOutput
        -- The ID (in UUID format) of the asynchronous DisableBaseline operation. This operationIdentifier is used to track status through calls to the GetBaselineOperation API.
        operationIdentifier: string
    end

    -- A key-value parameter to an EnabledBaseline resource.
    record EnabledBaselineParameter
        -- A string denoting the parameter key.
        key: string
        -- A low-level Document object of any type (for example, a Java Object).
        value: any
    end

    record EnableBaselineInput
        -- The specific version to be enabled of the specified baseline.
        baselineVersion: string
        -- A list of key-value objects that specify enablement parameters, where key is a string and value is a document of any type.
        parameters: {EnabledBaselineParameter}
        -- The ARN of the baseline to be enabled.
        baselineIdentifier: string
        -- The ARN of the target on which the baseline will be enabled. Only OUs are supported as targets.
        targetIdentifier: string
        -- Tags associated with input to EnableBaseline.
        tags: {string : string}
    end

    record EnableBaselineOutput
        -- The ID (in UUID format) of the asynchronous EnableBaseline operation. This operationIdentifier is used to track status through calls to the GetBaselineOperation API.
        operationIdentifier: string
        -- The ARN of the EnabledBaseline resource.
        arn: string
    end

    record GetEnabledBaselineInput
        -- Identifier of the EnabledBaseline resource to be retrieved, in ARN format.
        enabledBaselineIdentifier: string
    end

    enum EnabledBaselineDriftStatus
        "IN_SYNC"
        "DRIFTED"
    end

    -- The inheritance drift summary for the enabled baseline. Inheritance drift occurs when any accounts in the target OU do not match the baseline configuration defined on that OU.
    record EnabledBaselineInheritanceDrift
        -- The inheritance drift status for enabled baselines.
        status: string
    end

    -- The types of drift that can be detected for an enabled baseline. Amazon Web Services Control Tower detects inheritance drift on the enabled baselines that target OUs: AWSControlTowerBaseline and BackupBaseline. Amazon Web Services Control Tower does not detect drift on the baselines that apply to your landing zone: IdentityCenterBaseline, AuditBaseline, LogArchiveBaseline, BackupCentralVaultBaseline, or BackupAdminBaseline. For more information, see Types of baselines. Baselines enabled on an OU are inherited by its member accounts as child EnabledBaseline resources. The baseline on the OU serves as the parent EnabledBaseline, which governs the configuration of each child EnabledBaseline. If the baseline configuration of a member account in an OU does not match the configuration of the parent OU, the parent and child baseline is in a state of inheritance drift. This drift could occur in the AWSControlTowerBaseline or the BackupBaseline related to that account.
    record EnabledBaselineDriftTypes
        -- At least one account within the target OU does not match the baseline configuration defined on that OU. An account is in inheritance drift when it does not match the configuration of a parent OU, possibly a new parent OU, if the account is moved.
        inheritance: EnabledBaselineInheritanceDrift
    end

    -- The drift summary of the enabled baseline. Amazon Web Services Control Tower reports inheritance drift when an enabled baseline configuration of a member account is different than the configuration that applies to the OU. Amazon Web Services Control Tower reports this type of drift for a parent or child enabled baseline. One way to repair this drift by resetting the parent enabled baseline, on the OU. For example, you may see this type of drift if you move accounts between OUs, but the accounts are not yet (re-)enrolled.
    record EnabledBaselineDriftStatusSummary
        -- The types of drift that can be detected for an enabled baseline. Amazon Web Services Control Tower detects inheritance drift on enabled baselines that apply at the OU level.
        types: EnabledBaselineDriftTypes
    end

    -- Summary of an applied parameter to an EnabledBaseline resource.
    record EnabledBaselineParameterSummary
        -- A string denoting the parameter key.
        key: string
        -- A low-level document object of any type (for example, a Java Object).
        value: any
    end

    enum EnablementStatus
        "SUCCEEDED"
        "FAILED"
        "UNDER_CHANGE"
    end

    -- The deployment summary of an EnabledControl or EnabledBaseline resource.
    record EnablementStatusSummary
        -- The deployment status of the enabled resource. Valid values: SUCCEEDED: The EnabledControl or EnabledBaseline configuration was deployed successfully. UNDER_CHANGE: The EnabledControl or EnabledBaseline configuration is changing. FAILED: The EnabledControl or EnabledBaseline configuration failed to deploy.
        status: string
        -- The last operation identifier for the enabled resource.
        lastOperationIdentifier: string
    end

    -- Details of the EnabledBaseline resource.
    record EnabledBaselineDetails
        -- The ARN of the EnabledBaseline resource.
        arn: string
        -- The specific Baseline enabled as part of the EnabledBaseline resource.
        baselineIdentifier: string
        -- The enabled version of the Baseline.
        baselineVersion: string
        -- The drift status of the enabled baseline.
        driftStatusSummary: EnabledBaselineDriftStatusSummary
        -- The target on which to enable the Baseline.
        targetIdentifier: string
        -- An ARN that represents the parent EnabledBaseline at the Organizational Unit (OU) level, from which the child EnabledBaseline inherits its configuration. The value is returned by GetEnabledBaseline.
        parentIdentifier: string
        statusSummary: EnablementStatusSummary
        -- Shows the parameters that are applied when enabling this Baseline.
        parameters: {EnabledBaselineParameterSummary}
    end

    record GetEnabledBaselineOutput
        -- Details of the EnabledBaseline resource.
        enabledBaselineDetails: EnabledBaselineDetails
    end

    -- A filter applied on the ListEnabledBaseline operation. Allowed filters are baselineIdentifiers and targetIdentifiers. The filter can be applied for either, or both.
    record EnabledBaselineFilter
        -- Identifiers for the targets of the Baseline filter operation.
        targetIdentifiers: {string}
        -- Identifiers for the Baseline objects returned as part of the filter operation.
        baselineIdentifiers: {string}
        -- An optional filter that sets up a list of parentIdentifiers to filter the results of the ListEnabledBaseline output.
        parentIdentifiers: {string}
        -- A list of EnablementStatus items.
        statuses: {string}
        -- A list of EnabledBaselineDriftStatus items for enabled baselines.
        inheritanceDriftStatuses: {string}
    end

    record ListEnabledBaselinesInput
        -- A filter applied on the ListEnabledBaseline operation. Allowed filters are baselineIdentifiers and targetIdentifiers. The filter can be applied for either, or both.
        filter: EnabledBaselineFilter
        -- A pagination token.
        nextToken: string
        -- The maximum number of results to be shown.
        maxResults: number
        -- A value that can be set to include the child enabled baselines in responses. The default value is false.
        includeChildren: boolean
    end

    -- Returns a summary of information about an EnabledBaseline object.
    record EnabledBaselineSummary
        -- The ARN of the EnabledBaseline resource
        arn: string
        -- The specific baseline that is enabled as part of the EnabledBaseline resource.
        baselineIdentifier: string
        -- The enabled version of the baseline.
        baselineVersion: string
        -- The drift status of the enabled baseline.
        driftStatusSummary: EnabledBaselineDriftStatusSummary
        -- The target upon which the baseline is enabled.
        targetIdentifier: string
        -- An ARN that represents an object returned by ListEnabledBaseline, to describe an enabled baseline.
        parentIdentifier: string
        statusSummary: EnablementStatusSummary
    end

    record ListEnabledBaselinesOutput
        -- Retuens a list of summaries of EnabledBaseline resources.
        enabledBaselines: {EnabledBaselineSummary}
        -- A pagination token.
        nextToken: string
    end

    record ResetEnabledBaselineInput
        -- Specifies the ID of the EnabledBaseline resource to be re-enabled, in ARN format.
        enabledBaselineIdentifier: string
    end

    record ResetEnabledBaselineOutput
        -- The ID (in UUID format) of the asynchronous ResetEnabledBaseline operation. This operationIdentifier is used to track status through calls to the GetBaselineOperation API.
        operationIdentifier: string
    end

    record UpdateEnabledBaselineInput
        -- Specifies the new Baseline version, to which the EnabledBaseline should be updated.
        baselineVersion: string
        -- Parameters to apply when making an update.
        parameters: {EnabledBaselineParameter}
        -- Specifies the EnabledBaseline resource to be updated.
        enabledBaselineIdentifier: string
    end

    record UpdateEnabledBaselineOutput
        -- The ID (in UUID format) of the asynchronous UpdateEnabledBaseline operation. This operationIdentifier is used to track status through calls to the GetBaselineOperation API.
        operationIdentifier: string
    end

    -- A key/value pair, where Key is of type String and Value is of type Document.
    record EnabledControlParameter
        -- The key of a key/value pair.
        key: string
        -- The value of a key/value pair.
        value: any
    end

    record EnableControlInput
        -- The ARN of the control. Only Strongly recommended and Elective controls are permitted, with the exception of the Region deny control. For information on how to find the controlIdentifier, see the overview page.
        controlIdentifier: string
        -- The ARN of the organizational unit. For information on how to find the targetIdentifier, see the overview page.
        targetIdentifier: string
        -- Tags to be applied to the EnabledControl resource.
        tags: {string : string}
        -- A list of input parameter values, which are specified to configure the control when you enable it.
        parameters: {EnabledControlParameter}
    end

    record EnableControlOutput
        -- The ID of the asynchronous operation, which is used to track status. The operation is available for 90 days.
        operationIdentifier: string
        -- The ARN of the EnabledControl resource.
        arn: string
    end

    record GetEnabledControlInput
        -- The controlIdentifier of the enabled control.
        enabledControlIdentifier: string
    end

    enum DriftStatus
        "DRIFTED"
        "IN_SYNC"
        "NOT_CHECKING"
        "UNKNOWN"
    end

    -- Represents drift information related to control inheritance between organizational units.
    record EnabledControlInheritanceDrift
        -- The status of inheritance drift for the enabled control, indicating whether inheritance configuration matches expectations.
        status: string
    end

    -- Represents drift information related to the underlying Amazon Web Services resources managed by the control.
    record EnabledControlResourceDrift
        -- The status of resource drift for the enabled control, indicating whether the underlying resources match the expected configuration.
        status: string
    end

    -- Defines the various categories of drift that can occur for an enabled control resource.
    record EnabledControlDriftTypes
        -- Indicates drift related to inheritance configuration between parent and child controls.
        inheritance: EnabledControlInheritanceDrift
        -- Indicates drift related to the underlying Amazon Web Services resources managed by the control.
        resource: EnabledControlResourceDrift
    end

    -- The drift summary of the enabled control. Amazon Web Services Control Tower expects the enabled control configuration to include all supported and governed Regions. If the enabled control differs from the expected configuration, it is defined to be in a state of drift. You can repair this drift by resetting the enabled control.
    record DriftStatusSummary
        -- The drift status of the enabled control. Valid values: DRIFTED: The enabledControl deployed in this configuration doesn’t match the configuration that Amazon Web Services Control Tower expected. IN_SYNC: The enabledControl deployed in this configuration matches the configuration that Amazon Web Services Control Tower expected. NOT_CHECKING: Amazon Web Services Control Tower does not check drift for this enabled control. Drift is not supported for the control type. UNKNOWN: Amazon Web Services Control Tower is not able to check the drift status for the enabled control.
        driftStatus: string
        -- An object that categorizes the different types of drift detected for the enabled control.
        types: EnabledControlDriftTypes
    end

    -- Returns a summary of information about the parameters of an enabled control.
    record EnabledControlParameterSummary
        -- The key of a key/value pair.
        key: string
        -- The value of a key/value pair.
        value: any
    end

    -- An Amazon Web Services Region in which Amazon Web Services Control Tower expects to find the control deployed. The expected Regions are based on the Regions that are governed by the landing zone. In certain cases, a control is not actually enabled in the Region as expected, such as during drift, or mixed governance.
    record Region
        -- The Amazon Web Services Region name.
        name: string
    end

    -- Information about the enabled control.
    record EnabledControlDetails
        -- The ARN of the enabled control.
        arn: string
        -- The control identifier of the enabled control. For information on how to find the controlIdentifier, see the overview page.
        controlIdentifier: string
        -- The ARN of the organizational unit. For information on how to find the targetIdentifier, see the overview page.
        targetIdentifier: string
        -- The deployment summary of the enabled control.
        statusSummary: EnablementStatusSummary
        -- The drift status of the enabled control.
        driftStatusSummary: DriftStatusSummary
        -- The ARN of the parent enabled control from which this control inherits its configuration, if applicable.
        parentIdentifier: string
        -- Target Amazon Web Services Regions for the enabled control.
        targetRegions: {Region}
        -- Array of EnabledControlParameter objects.
        parameters: {EnabledControlParameterSummary}
    end

    record GetEnabledControlOutput
        -- Information about the enabled control.
        enabledControlDetails: EnabledControlDetails
    end

    -- A structure that returns a set of control identifiers, the control status for each control in the set, and the drift status for each control in the set.
    record EnabledControlFilter
        -- The set of controlIdentifier returned by the filter.
        controlIdentifiers: {string}
        -- A list of EnablementStatus items.
        statuses: {string}
        -- A list of DriftStatus items.
        driftStatuses: {string}
        -- Filters enabled controls by their parent control identifiers, allowing you to find child controls of specific parent controls.
        parentIdentifiers: {string}
        -- Filters enabled controls by their inheritance drift status, allowing you to find controls with specific inheritance-related drift conditions.
        inheritanceDriftStatuses: {string}
        -- Filters enabled controls by their resource drift status, allowing you to find controls with specific resource-related drift conditions.
        resourceDriftStatuses: {string}
    end

    record ListEnabledControlsInput
        -- The ARN of the organizational unit. For information on how to find the targetIdentifier, see the overview page.
        targetIdentifier: string
        -- The token to continue the list from a previous API call with the same parameters.
        nextToken: string
        -- How many results to return per API call.
        maxResults: number
        -- An input filter for the ListEnabledControls API that lets you select the types of control operations to view.
        filter: EnabledControlFilter
        -- A boolean value that determines whether to include enabled controls from child organizational units in the response.
        includeChildren: boolean
    end

    -- Returns a summary of information about an enabled control.
    record EnabledControlSummary
        -- The ARN of the enabled control.
        arn: string
        -- The controlIdentifier of the enabled control.
        controlIdentifier: string
        -- The ARN of the organizational unit.
        targetIdentifier: string
        -- A short description of the status of the enabled control.
        statusSummary: EnablementStatusSummary
        -- The drift status of the enabled control.
        driftStatusSummary: DriftStatusSummary
        -- The ARN of the parent enabled control from which this control inherits its configuration, if applicable.
        parentIdentifier: string
    end

    record ListEnabledControlsOutput
        -- Lists the controls enabled by Amazon Web Services Control Tower on the specified organizational unit and the accounts it contains.
        enabledControls: {EnabledControlSummary}
        -- Retrieves the next page of results. If the string is empty, the response is the end of the results.
        nextToken: string
    end

    record ResetEnabledControlInput
        -- The ARN of the enabled control to be reset.
        enabledControlIdentifier: string
    end

    record ResetEnabledControlOutput
        -- The operation identifier for this ResetEnabledControl operation.
        operationIdentifier: string
    end

    record UpdateEnabledControlInput
        -- A key/value pair, where Key is of type String and Value is of type Document.
        parameters: {EnabledControlParameter}
        -- The ARN of the enabled control that will be updated.
        enabledControlIdentifier: string
    end

    record UpdateEnabledControlOutput
        -- The operation identifier for this UpdateEnabledControl operation.
        operationIdentifier: string
    end

    record GetLandingZoneOperationInput
        -- A unique identifier assigned to a landing zone operation.
        operationIdentifier: string
    end

    enum LandingZoneOperationType
        "DELETE"
        "CREATE"
        "UPDATE"
        "RESET"
    end

    enum LandingZoneOperationStatus
        "SUCCEEDED"
        "FAILED"
        "IN_PROGRESS"
    end

    -- Information about a landing zone operation.
    record LandingZoneOperationDetail
        -- The landing zone operation type. Valid values: DELETE: The DeleteLandingZone operation. CREATE: The CreateLandingZone operation. UPDATE: The UpdateLandingZone operation. RESET: The ResetLandingZone operation.
        operationType: string
        -- The operationIdentifier of the landing zone operation.
        operationIdentifier: string
        -- Valid values: SUCCEEDED: The landing zone operation succeeded. IN_PROGRESS: The landing zone operation is in progress. FAILED: The landing zone operation failed.
        status: string
        -- The landing zone operation start time.
        startTime: number
        -- The landing zone operation end time.
        endTime: number
        -- If the operation result is FAILED, this string contains a message explaining why the operation failed.
        statusMessage: string
    end

    record GetLandingZoneOperationOutput
        -- Details about a landing zone operation.
        operationDetails: LandingZoneOperationDetail
    end

    -- A filter object that lets you call ListLandingZoneOperations with a specific filter.
    record LandingZoneOperationFilter
        -- The set of landing zone operation types selected by the filter.
        types: {string}
        -- The statuses of the set of landing zone operations selected by the filter.
        statuses: {string}
    end

    record ListLandingZoneOperationsInput
        -- An input filter for the ListLandingZoneOperations API that lets you select the types of landing zone operations to view.
        filter: LandingZoneOperationFilter
        -- The token to continue the list from a previous API call with the same parameters.
        nextToken: string
        -- How many results to return per API call.
        maxResults: number
    end

    -- Returns a summary of information about a landing zone operation.
    record LandingZoneOperationSummary
        -- The type of the landing zone operation.
        operationType: string
        -- The operationIdentifier of the landing zone operation.
        operationIdentifier: string
        -- The status of the landing zone operation.
        status: string
    end

    record ListLandingZoneOperationsOutput
        -- Lists landing zone operations.
        landingZoneOperations: {LandingZoneOperationSummary}
        -- Retrieves the next page of results. If the string is empty, the response is the end of the results.
        nextToken: string
    end

    enum RemediationType
        "INHERITANCE_DRIFT"
    end

    record CreateLandingZoneInput
        -- The landing zone version, for example, 3.0.
        version: string
        -- Specifies the types of remediation actions to apply when creating the landing zone, such as automatic drift correction or compliance enforcement.
        remediationTypes: {string}
        -- Tags to be applied to the landing zone.
        tags: {string : string}
        -- The manifest JSON file is a text file that describes your Amazon Web Services resources. For examples, review Launch your landing zone.
        manifest: any
    end

    record CreateLandingZoneOutput
        -- The ARN of the landing zone resource.
        arn: string
        -- A unique identifier assigned to a CreateLandingZone operation. You can use this identifier as an input of GetLandingZoneOperation to check the operation's status.
        operationIdentifier: string
    end

    record DeleteLandingZoneInput
        -- The unique identifier of the landing zone.
        landingZoneIdentifier: string
    end

    record DeleteLandingZoneOutput
        -- >A unique identifier assigned to a DeleteLandingZone operation. You can use this identifier as an input parameter of GetLandingZoneOperation to check the operation's status.
        operationIdentifier: string
    end

    record GetLandingZoneInput
        -- The unique identifier of the landing zone.
        landingZoneIdentifier: string
    end

    enum LandingZoneDriftStatus
        "DRIFTED"
        "IN_SYNC"
    end

    -- The drift status summary of the landing zone. If the landing zone differs from the expected configuration, it is defined to be in a state of drift. You can repair this drift by resetting the landing zone.
    record LandingZoneDriftStatusSummary
        -- The drift status of the landing zone. Valid values: DRIFTED: The landing zone deployed in this configuration does not match the configuration that Amazon Web Services Control Tower expected. IN_SYNC: The landing zone deployed in this configuration matches the configuration that Amazon Web Services Control Tower expected.
        status: string
    end

    enum LandingZoneStatus
        "ACTIVE"
        "PROCESSING"
        "FAILED"
    end

    -- Information about the landing zone.
    record LandingZoneDetail
        -- The landing zone's current deployed version.
        version: string
        -- The types of remediation actions configured for the landing zone, such as automatic drift correction or compliance enforcement.
        remediationTypes: {string}
        -- The ARN of the landing zone.
        arn: string
        -- The landing zone deployment status. One of ACTIVE, PROCESSING, FAILED.
        status: string
        -- The latest available version of the landing zone.
        latestAvailableVersion: string
        -- The drift status of the landing zone.
        driftStatus: LandingZoneDriftStatusSummary
        -- The landing zone manifest JSON text file that specifies the landing zone configurations.
        manifest: any
    end

    record GetLandingZoneOutput
        -- Information about the landing zone.
        landingZone: LandingZoneDetail
    end

    record ListLandingZonesInput
        -- The token to continue the list from a previous API call with the same parameters.
        nextToken: string
        -- The maximum number of returned landing zone ARNs, which is one.
        maxResults: number
    end

    -- Returns a summary of information about a landing zone.
    record LandingZoneSummary
        -- The ARN of the landing zone.
        arn: string
    end

    record ListLandingZonesOutput
        -- The ARN of the landing zone.
        landingZones: {LandingZoneSummary}
        -- Retrieves the next page of results. If the string is empty, the response is the end of the results.
        nextToken: string
    end

    record ResetLandingZoneInput
        -- The unique identifier of the landing zone.
        landingZoneIdentifier: string
    end

    record ResetLandingZoneOutput
        -- A unique identifier assigned to a ResetLandingZone operation. You can use this identifier as an input parameter of GetLandingZoneOperation to check the operation's status.
        operationIdentifier: string
    end

    record UpdateLandingZoneInput
        -- The landing zone version, for example, 3.2.
        version: string
        -- Specifies the types of remediation actions to apply when updating the landing zone configuration.
        remediationTypes: {string}
        -- The unique identifier of the landing zone.
        landingZoneIdentifier: string
        -- The manifest file (JSON) is a text file that describes your Amazon Web Services resources. For an example, review Launch your landing zone. The example manifest file contains each of the available parameters. The schema for the landing zone's JSON manifest file is not published, by design.
        manifest: any
    end

    record UpdateLandingZoneOutput
        -- A unique identifier assigned to a UpdateLandingZone operation. You can use this identifier as an input of GetLandingZoneOperation to check the operation's status.
        operationIdentifier: string
    end

    record ListTagsForResourceInput
        -- The ARN of the resource.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- A list of tags, as key:value strings.
        tags: {string : string}
    end

    record TagResourceInput
        -- The ARN of the resource to be tagged.
        resourceArn: string
        -- Tags to be applied to the resource.
        tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The ARN of the resource.
        resourceArn: string
        -- Tag keys to be removed from the resource.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

end

return M
