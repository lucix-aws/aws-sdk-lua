-- Code generated by lua-client-codegen. DO NOT EDIT.

local waiter = require("smithy.waiter")

local M = {}

--- Wait until ConnectorConnected.
function M.wait_until_connector_connected(client, input, options)
    return waiter.wait(client, "listConnectors", input, {
        min_delay = 30,
        max_delay = 120,
        acceptors = {
            {
                state = "success",
                matcher = {
                    output = {
                        path = "items[].health.connectorStatus",
                        expected = "CONNECTED",
                        comparator = "allStringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "items[].health.connectorStatus",
                        expected = "FAILED_TO_CONNECT",
                        comparator = "anyStringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "items[].enablementStatus",
                        expected = "FAILED_TO_ENABLE",
                        comparator = "anyStringEquals",
                    },
                },
            },
        },
    }, options)
end

--- Wait until ConnectorDeleted.
function M.wait_until_connector_deleted(client, input, options)
    return waiter.wait(client, "listConnectors", input, {
        min_delay = 30,
        max_delay = 120,
        acceptors = {
            {
                state = "success",
                matcher = {
                    output = {
                        path = "length(items) == `0`",
                        expected = "true",
                        comparator = "booleanEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "items[].enablementStatus",
                        expected = "FAILED_TO_DELETE",
                        comparator = "anyStringEquals",
                    },
                },
            },
        },
    }, options)
end

--- Wait until ConnectorEnabled.
function M.wait_until_connector_enabled(client, input, options)
    return waiter.wait(client, "listConnectors", input, {
        min_delay = 30,
        max_delay = 120,
        acceptors = {
            {
                state = "success",
                matcher = {
                    output = {
                        path = "items[].enablementStatus",
                        expected = "ENABLED",
                        comparator = "allStringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "items[].enablementStatus",
                        expected = "FAILED_TO_ENABLE",
                        comparator = "anyStringEquals",
                    },
                },
            },
            {
                state = "failure",
                matcher = {
                    output = {
                        path = "items[].enablementStatus",
                        expected = "FAILED_TO_UPDATE",
                        comparator = "anyStringEquals",
                    },
                },
            },
        },
    }, options)
end

return M
