-- Code generated by lua-client-codegen. DO NOT EDIT.

-- Retroactively applies an archive rule.
local record ApplyArchiveRuleInput
    -- The Amazon resource name (ARN) of the analyzer.
    analyzerArn: string
    -- The name of the rule to apply.
    ruleName: string
    -- A client token.
    clientToken: string
end

local record ApplyArchiveRuleOutput
end

local record CancelPolicyGenerationInput
    -- The JobId that is returned by the StartPolicyGeneration operation. The JobId can be used with GetGeneratedPolicy to retrieve the generated policies or used with CancelPolicyGeneration to cancel the policy generation request.
    jobId: string
end

local record CancelPolicyGenerationOutput
end

local record CheckAccessNotGrantedInput
    -- The JSON policy document to use as the content for the policy.
    policyDocument: string
    -- An access object containing the permissions that shouldn't be granted by the specified policy. If only actions are specified, IAM Access Analyzer checks for access to peform at least one of the actions on any resource in the policy. If only resources are specified, then IAM Access Analyzer checks for access to perform any action on at least one of the resources. If both actions and resources are specified, IAM Access Analyzer checks for access to perform at least one of the specified actions on at least one of the specified resources.
    access: {Access}
    -- The type of policy. Identity policies grant permissions to IAM principals. Identity policies include managed and inline policies for IAM roles, users, and groups. Resource policies grant permissions on Amazon Web Services resources. Resource policies include trust policies for IAM roles and bucket policies for Amazon S3 buckets.
    policyType: string
end

local record CheckAccessNotGrantedOutput
    -- The result of the check for whether the access is allowed. If the result is PASS, the specified policy doesn't allow any of the specified permissions in the access object. If the result is FAIL, the specified policy might allow some or all of the permissions in the access object.
    result: string
    -- The message indicating whether the specified access is allowed.
    message: string
    -- A description of the reasoning of the result.
    reasons: {ReasonSummary}
end

local record CheckNoNewAccessInput
    -- The JSON policy document to use as the content for the updated policy.
    newPolicyDocument: string
    -- The JSON policy document to use as the content for the existing policy.
    existingPolicyDocument: string
    -- The type of policy to compare. Identity policies grant permissions to IAM principals. Identity policies include managed and inline policies for IAM roles, users, and groups. Resource policies grant permissions on Amazon Web Services resources. Resource policies include trust policies for IAM roles and bucket policies for Amazon S3 buckets. You can provide a generic input such as identity policy or resource policy or a specific input such as managed policy or Amazon S3 bucket policy.
    policyType: string
end

local record CheckNoNewAccessOutput
    -- The result of the check for new access. If the result is PASS, no new access is allowed by the updated policy. If the result is FAIL, the updated policy might allow new access.
    result: string
    -- The message indicating whether the updated policy allows new access.
    message: string
    -- A description of the reasoning of the result.
    reasons: {ReasonSummary}
end

local record CheckNoPublicAccessInput
    -- The JSON policy document to evaluate for public access.
    policyDocument: string
    -- The type of resource to evaluate for public access. For example, to check for public access to Amazon S3 buckets, you can choose AWS::S3::Bucket for the resource type. For resource types not supported as valid values, IAM Access Analyzer will return an error.
    resourceType: string
end

local record CheckNoPublicAccessOutput
    -- The result of the check for public access to the specified resource type. If the result is PASS, the policy doesn't allow public access to the specified resource type. If the result is FAIL, the policy might allow public access to the specified resource type.
    result: string
    -- The message indicating whether the specified policy allows public access to resources.
    message: string
    -- A list of reasons why the specified resource policy grants public access for the resource type.
    reasons: {ReasonSummary}
end

local record CreateAccessPreviewInput
    -- The ARN of the account analyzer used to generate the access preview. You can only create an access preview for analyzers with an Account type and Active status.
    analyzerArn: string
    -- Access control configuration for your resource that is used to generate the access preview. The access preview includes findings for external access allowed to the resource with the proposed access control configuration. The configuration must contain exactly one element.
    configurations: {string : Configuration}
    -- A client token.
    clientToken: string
end

local record CreateAccessPreviewOutput
    -- The unique ID for the access preview.
    id: string
end

-- Creates an analyzer.
local record CreateAnalyzerInput
    -- The name of the analyzer to create.
    analyzerName: string
    -- The type of analyzer to create. You can create only one analyzer per account per Region. You can create up to 5 analyzers per organization per Region.
    type: string
    -- Specifies the archive rules to add for the analyzer. Archive rules automatically archive findings that meet the criteria you define for the rule.
    archiveRules: {InlineArchiveRule}
    -- An array of key-value pairs to apply to the analyzer. You can use the set of Unicode letters, digits, whitespace, _, ., /, =, +, and -. For the tag key, you can specify a value that is 1 to 128 characters in length and cannot be prefixed with aws:. For the tag value, you can specify a value that is 0 to 256 characters in length.
    tags: {string : string}
    -- A client token.
    clientToken: string
    -- Specifies the configuration of the analyzer. If the analyzer is an unused access analyzer, the specified scope of unused access is used for the configuration. If the analyzer is an internal access analyzer, the specified internal access analysis rules are used for the configuration.
    configuration: AnalyzerConfiguration
end

-- The response to the request to create an analyzer.
local record CreateAnalyzerOutput
    -- The ARN of the analyzer that was created by the request.
    arn: string
end

-- Creates an archive rule.
local record CreateArchiveRuleInput
    -- The name of the created analyzer.
    analyzerName: string
    -- The name of the rule to create.
    ruleName: string
    -- The criteria for the rule.
    filter: {string : Criterion}
    -- A client token.
    clientToken: string
end

local record CreateArchiveRuleOutput
end

-- Deletes an analyzer.
local record DeleteAnalyzerInput
    -- The name of the analyzer to delete.
    analyzerName: string
    -- A client token.
    clientToken: string
end

local record DeleteAnalyzerOutput
end

-- Deletes an archive rule.
local record DeleteArchiveRuleInput
    -- The name of the analyzer that associated with the archive rule to delete.
    analyzerName: string
    -- The name of the rule to delete.
    ruleName: string
    -- A client token.
    clientToken: string
end

local record DeleteArchiveRuleOutput
end

local record GenerateFindingRecommendationInput
    -- The ARN of the analyzer used to generate the finding recommendation.
    analyzerArn: string
    -- The unique ID for the finding recommendation.
    id: string
end

local record GenerateFindingRecommendationOutput
end

local record GetAccessPreviewInput
    -- The unique ID for the access preview.
    accessPreviewId: string
    -- The ARN of the analyzer used to generate the access preview.
    analyzerArn: string
end

local record GetAccessPreviewOutput
    -- An object that contains information about the access preview.
    accessPreview: AccessPreview
end

-- Retrieves an analyzed resource.
local record GetAnalyzedResourceInput
    -- The ARN of the analyzer to retrieve information from.
    analyzerArn: string
    -- The ARN of the resource to retrieve information about.
    resourceArn: string
end

-- The response to the request.
local record GetAnalyzedResourceOutput
    -- An AnalyzedResource object that contains information that IAM Access Analyzer found when it analyzed the resource.
    resource: AnalyzedResource
end

-- Retrieves an analyzer.
local record GetAnalyzerInput
    -- The name of the analyzer retrieved.
    analyzerName: string
end

-- The response to the request.
local record GetAnalyzerOutput
    -- An AnalyzerSummary object that contains information about the analyzer.
    analyzer: AnalyzerSummary
end

-- Retrieves an archive rule.
local record GetArchiveRuleInput
    -- The name of the analyzer to retrieve rules from.
    analyzerName: string
    -- The name of the rule to retrieve.
    ruleName: string
end

-- The response to the request.
local record GetArchiveRuleOutput
    archiveRule: ArchiveRuleSummary
end

-- Retrieves a finding.
local record GetFindingInput
    -- The ARN of the analyzer that generated the finding.
    analyzerArn: string
    -- The ID of the finding to retrieve.
    id: string
end

-- The response to the request.
local record GetFindingOutput
    -- A finding object that contains finding details.
    finding: Finding
end

local record GetFindingRecommendationInput
    -- The ARN of the analyzer used to generate the finding recommendation.
    analyzerArn: string
    -- The unique ID for the finding recommendation.
    id: string
    -- The maximum number of results to return in the response.
    maxResults: number
    -- A token used for pagination of results returned.
    nextToken: string
end

local record GetFindingRecommendationOutput
    -- The time at which the retrieval of the finding recommendation was started.
    startedAt: number
    -- The time at which the retrieval of the finding recommendation was completed.
    completedAt: number
    -- A token used for pagination of results returned.
    nextToken: string
    -- Detailed information about the reason that the retrieval of a recommendation for the finding failed.
    error: RecommendationError
    -- The ARN of the resource of the finding.
    resourceArn: string
    -- A group of recommended steps for the finding.
    recommendedSteps: {RecommendedStep}
    -- The type of recommendation for the finding.
    recommendationType: string
    -- The status of the retrieval of the finding recommendation.
    status: string
end

local record GetFindingsStatisticsInput
    -- The ARN of the analyzer used to generate the statistics.
    analyzerArn: string
end

local record GetFindingsStatisticsOutput
    -- A group of external access or unused access findings statistics.
    findingsStatistics: {FindingsStatistics}
    -- The time at which the retrieval of the findings statistics was last updated. If the findings statistics have not been previously retrieved for the specified analyzer, this field will not be populated.
    lastUpdatedAt: number
end

local record GetFindingV2Input
    -- The ARN of the analyzer that generated the finding.
    analyzerArn: string
    -- The ID of the finding to retrieve.
    id: string
    -- The maximum number of results to return in the response.
    maxResults: number
    -- A token used for pagination of results returned.
    nextToken: string
end

local record GetFindingV2Output
    -- The time at which the resource-based policy or IAM entity that generated the finding was analyzed.
    analyzedAt: number
    -- The time at which the finding was created.
    createdAt: number
    -- An error.
    error: string
    -- The ID of the finding to retrieve.
    id: string
    -- A token used for pagination of results returned.
    nextToken: string
    -- The resource that generated the finding.
    resource: string
    -- The type of the resource identified in the finding.
    resourceType: string
    -- Tye Amazon Web Services account ID that owns the resource.
    resourceOwnerAccount: string
    -- The status of the finding.
    status: string
    -- The time at which the finding was updated.
    updatedAt: number
    -- A localized message that explains the finding and provides guidance on how to address it.
    findingDetails: {FindingDetails}
    -- The type of the finding. For external access analyzers, the type is ExternalAccess. For unused access analyzers, the type can be UnusedIAMRole, UnusedIAMUserAccessKey, UnusedIAMUserPassword, or UnusedPermission. For internal access analyzers, the type is InternalAccess.
    findingType: string
end

local record GetGeneratedPolicyInput
    -- The JobId that is returned by the StartPolicyGeneration operation. The JobId can be used with GetGeneratedPolicy to retrieve the generated policies or used with CancelPolicyGeneration to cancel the policy generation request.
    jobId: string
    -- The level of detail that you want to generate. You can specify whether to generate policies with placeholders for resource ARNs for actions that support resource level granularity in policies. For example, in the resource section of a policy, you can receive a placeholder such as "Resource":"arn:aws:s3:::${BucketName}" instead of "*".
    includeResourcePlaceholders: boolean
    -- The level of detail that you want to generate. You can specify whether to generate service-level policies. IAM Access Analyzer uses iam:servicelastaccessed to identify services that have been used recently to create this service-level template.
    includeServiceLevelTemplate: boolean
end

local record GetGeneratedPolicyOutput
    -- A GeneratedPolicyDetails object that contains details about the generated policy.
    jobDetails: JobDetails
    -- A GeneratedPolicyResult object that contains the generated policies and associated details.
    generatedPolicyResult: GeneratedPolicyResult
end

local record ListAccessPreviewFindingsInput
    -- The unique ID for the access preview.
    accessPreviewId: string
    -- The ARN of the analyzer used to generate the access.
    analyzerArn: string
    -- Criteria to filter the returned findings.
    filter: {string : Criterion}
    -- A token used for pagination of results returned.
    nextToken: string
    -- The maximum number of results to return in the response.
    maxResults: number
end

local record ListAccessPreviewFindingsOutput
    -- A list of access preview findings that match the specified filter criteria.
    findings: {AccessPreviewFinding}
    -- A token used for pagination of results returned.
    nextToken: string
end

local record ListAccessPreviewsInput
    -- The ARN of the analyzer used to generate the access preview.
    analyzerArn: string
    -- A token used for pagination of results returned.
    nextToken: string
    -- The maximum number of results to return in the response.
    maxResults: number
end

local record ListAccessPreviewsOutput
    -- A list of access previews retrieved for the analyzer.
    accessPreviews: {AccessPreviewSummary}
    -- A token used for pagination of results returned.
    nextToken: string
end

-- Retrieves a list of resources that have been analyzed.
local record ListAnalyzedResourcesInput
    -- The ARN of the analyzer to retrieve a list of analyzed resources from.
    analyzerArn: string
    -- The type of resource.
    resourceType: string
    -- A token used for pagination of results returned.
    nextToken: string
    -- The maximum number of results to return in the response.
    maxResults: number
end

-- The response to the request.
local record ListAnalyzedResourcesOutput
    -- A list of resources that were analyzed.
    analyzedResources: {AnalyzedResourceSummary}
    -- A token used for pagination of results returned.
    nextToken: string
end

-- Retrieves a list of analyzers.
local record ListAnalyzersInput
    -- A token used for pagination of results returned.
    nextToken: string
    -- The maximum number of results to return in the response.
    maxResults: number
    -- The type of analyzer.
    type: string
end

-- The response to the request.
local record ListAnalyzersOutput
    -- The analyzers retrieved.
    analyzers: {AnalyzerSummary}
    -- A token used for pagination of results returned.
    nextToken: string
end

-- Retrieves a list of archive rules created for the specified analyzer.
local record ListArchiveRulesInput
    -- The name of the analyzer to retrieve rules from.
    analyzerName: string
    -- A token used for pagination of results returned.
    nextToken: string
    -- The maximum number of results to return in the request.
    maxResults: number
end

-- The response to the request.
local record ListArchiveRulesOutput
    -- A list of archive rules created for the specified analyzer.
    archiveRules: {ArchiveRuleSummary}
    -- A token used for pagination of results returned.
    nextToken: string
end

-- Retrieves a list of findings generated by the specified analyzer.
local record ListFindingsInput
    -- The ARN of the analyzer to retrieve findings from.
    analyzerArn: string
    -- A filter to match for the findings to return.
    filter: {string : Criterion}
    -- The sort order for the findings returned.
    sort: SortCriteria
    -- A token used for pagination of results returned.
    nextToken: string
    -- The maximum number of results to return in the response.
    maxResults: number
end

-- The response to the request.
local record ListFindingsOutput
    -- A list of findings retrieved from the analyzer that match the filter criteria specified, if any.
    findings: {FindingSummary}
    -- A token used for pagination of results returned.
    nextToken: string
end

local record ListFindingsV2Input
    -- The ARN of the analyzer to retrieve findings from.
    analyzerArn: string
    -- A filter to match for the findings to return.
    filter: {string : Criterion}
    -- The maximum number of results to return in the response.
    maxResults: number
    -- A token used for pagination of results returned.
    nextToken: string
    sort: SortCriteria
end

local record ListFindingsV2Output
    -- A list of findings retrieved from the analyzer that match the filter criteria specified, if any.
    findings: {FindingSummaryV2}
    -- A token used for pagination of results returned.
    nextToken: string
end

local record ListPolicyGenerationsInput
    -- The ARN of the IAM entity (user or role) for which you are generating a policy. Use this with ListGeneratedPolicies to filter the results to only include results for a specific principal.
    principalArn: string
    -- The maximum number of results to return in the response.
    maxResults: number
    -- A token used for pagination of results returned.
    nextToken: string
end

local record ListPolicyGenerationsOutput
    -- A PolicyGeneration object that contains details about the generated policy.
    policyGenerations: {PolicyGeneration}
    -- A token used for pagination of results returned.
    nextToken: string
end

-- Retrieves a list of tags applied to the specified resource.
local record ListTagsForResourceInput
    -- The ARN of the resource to retrieve tags from.
    resourceArn: string
end

-- The response to the request.
local record ListTagsForResourceOutput
    -- The tags that are applied to the specified resource.
    tags: {string : string}
end

local record StartPolicyGenerationInput
    -- Contains the ARN of the IAM entity (user or role) for which you are generating a policy.
    policyGenerationDetails: PolicyGenerationDetails
    -- A CloudTrailDetails object that contains details about a Trail that you want to analyze to generate policies.
    cloudTrailDetails: CloudTrailDetails
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you do not specify a client token, one is automatically generated by the Amazon Web Services SDK.
    clientToken: string
end

local record StartPolicyGenerationOutput
    -- The JobId that is returned by the StartPolicyGeneration operation. The JobId can be used with GetGeneratedPolicy to retrieve the generated policies or used with CancelPolicyGeneration to cancel the policy generation request.
    jobId: string
end

-- Starts a scan of the policies applied to the specified resource.
local record StartResourceScanInput
    -- The ARN of the analyzer to use to scan the policies applied to the specified resource.
    analyzerArn: string
    -- The ARN of the resource to scan.
    resourceArn: string
    -- The Amazon Web Services account ID that owns the resource. For most Amazon Web Services resources, the owning account is the account in which the resource was created.
    resourceOwnerAccount: string
end

local record StartResourceScanOutput
end

-- Adds a tag to the specified resource.
local record TagResourceInput
    -- The ARN of the resource to add the tag to.
    resourceArn: string
    -- The tags to add to the resource.
    tags: {string : string}
end

-- The response to the request.
local record TagResourceOutput
end

-- Removes a tag from the specified resource.
local record UntagResourceInput
    -- The ARN of the resource to remove the tag from.
    resourceArn: string
    -- The key for the tag to add.
    tagKeys: {string}
end

-- The response to the request.
local record UntagResourceOutput
end

local record UpdateAnalyzerInput
    -- The name of the analyzer to modify.
    analyzerName: string
    configuration: AnalyzerConfiguration
end

local record UpdateAnalyzerOutput
    configuration: AnalyzerConfiguration
end

-- Updates the specified archive rule.
local record UpdateArchiveRuleInput
    -- The name of the analyzer to update the archive rules for.
    analyzerName: string
    -- The name of the rule to update.
    ruleName: string
    -- A filter to match for the rules to update. Only rules that match the filter are updated.
    filter: {string : Criterion}
    -- A client token.
    clientToken: string
end

local record UpdateArchiveRuleOutput
end

-- Updates findings with the new values provided in the request.
local record UpdateFindingsInput
    -- The ARN of the analyzer that generated the findings to update.
    analyzerArn: string
    -- The state represents the action to take to update the finding Status. Use ARCHIVE to change an Active finding to an Archived finding. Use ACTIVE to change an Archived finding to an Active finding.
    status: string
    -- The IDs of the findings to update.
    ids: {string}
    -- The ARN of the resource identified in the finding.
    resourceArn: string
    -- A client token.
    clientToken: string
end

local record UpdateFindingsOutput
end

local record ValidatePolicyInput
    -- The locale to use for localizing the findings.
    locale: string
    -- The maximum number of results to return in the response.
    maxResults: number
    -- A token used for pagination of results returned.
    nextToken: string
    -- The JSON policy document to use as the content for the policy.
    policyDocument: string
    -- The type of policy to validate. Identity policies grant permissions to IAM principals. Identity policies include managed and inline policies for IAM roles, users, and groups. Resource policies grant permissions on Amazon Web Services resources. Resource policies include trust policies for IAM roles and bucket policies for Amazon S3 buckets. You can provide a generic input such as identity policy or resource policy or a specific input such as managed policy or Amazon S3 bucket policy. Service control policies (SCPs) are a type of organization policy attached to an Amazon Web Services organization, organizational unit (OU), or an account.
    policyType: string
    -- The type of resource to attach to your resource policy. Specify a value for the policy validation resource type only if the policy type is RESOURCE_POLICY. For example, to validate a resource policy to attach to an Amazon S3 bucket, you can choose AWS::S3::Bucket for the policy validation resource type. For resource types not supported as valid values, IAM Access Analyzer runs policy checks that apply to all resource policies. For example, to validate a resource policy to attach to a KMS key, do not specify a value for the policy validation resource type and IAM Access Analyzer will run policy checks that apply to all resource policies.
    validatePolicyResourceType: string
end

local record ValidatePolicyOutput
    -- The list of findings in a policy returned by IAM Access Analyzer based on its suite of policy checks.
    findings: {ValidatePolicyFinding}
    -- A token used for pagination of results returned.
    nextToken: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified parameter is invalid.
local record InvalidParameterException
    message: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified entity could not be processed.
local record UnprocessableEntityException
    message: string
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified parameter is invalid.
local record InvalidParameterException
    message: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified entity could not be processed.
local record UnprocessableEntityException
    message: string
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified parameter is invalid.
local record InvalidParameterException
    message: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified entity could not be processed.
local record UnprocessableEntityException
    message: string
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception error.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Service quote met error.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception error.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Service quote met error.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception error.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Service quote met error.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception error.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception error.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Service quote met error.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception error.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- The specified resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of the resource.
    resourceType: string
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Internal server error.
local record InternalServerException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Throttling limit exceeded error.
local record ThrottlingException
    message: string
    -- The seconds to wait to retry.
    retryAfterSeconds: number
end

-- Validation exception error.
local record ValidationException
    message: string
    -- The reason for the exception.
    reason: string
    -- A list of fields that didn't validate.
    fieldList: {ValidationExceptionField}
end

local record M
    ApplyArchiveRuleInput: ApplyArchiveRuleInput
    ApplyArchiveRuleOutput: ApplyArchiveRuleOutput
    CancelPolicyGenerationInput: CancelPolicyGenerationInput
    CancelPolicyGenerationOutput: CancelPolicyGenerationOutput
    CheckAccessNotGrantedInput: CheckAccessNotGrantedInput
    CheckAccessNotGrantedOutput: CheckAccessNotGrantedOutput
    CheckNoNewAccessInput: CheckNoNewAccessInput
    CheckNoNewAccessOutput: CheckNoNewAccessOutput
    CheckNoPublicAccessInput: CheckNoPublicAccessInput
    CheckNoPublicAccessOutput: CheckNoPublicAccessOutput
    CreateAccessPreviewInput: CreateAccessPreviewInput
    CreateAccessPreviewOutput: CreateAccessPreviewOutput
    CreateAnalyzerInput: CreateAnalyzerInput
    CreateAnalyzerOutput: CreateAnalyzerOutput
    CreateArchiveRuleInput: CreateArchiveRuleInput
    CreateArchiveRuleOutput: CreateArchiveRuleOutput
    DeleteAnalyzerInput: DeleteAnalyzerInput
    DeleteAnalyzerOutput: DeleteAnalyzerOutput
    DeleteArchiveRuleInput: DeleteArchiveRuleInput
    DeleteArchiveRuleOutput: DeleteArchiveRuleOutput
    GenerateFindingRecommendationInput: GenerateFindingRecommendationInput
    GenerateFindingRecommendationOutput: GenerateFindingRecommendationOutput
    GetAccessPreviewInput: GetAccessPreviewInput
    GetAccessPreviewOutput: GetAccessPreviewOutput
    GetAnalyzedResourceInput: GetAnalyzedResourceInput
    GetAnalyzedResourceOutput: GetAnalyzedResourceOutput
    GetAnalyzerInput: GetAnalyzerInput
    GetAnalyzerOutput: GetAnalyzerOutput
    GetArchiveRuleInput: GetArchiveRuleInput
    GetArchiveRuleOutput: GetArchiveRuleOutput
    GetFindingInput: GetFindingInput
    GetFindingOutput: GetFindingOutput
    GetFindingRecommendationInput: GetFindingRecommendationInput
    GetFindingRecommendationOutput: GetFindingRecommendationOutput
    GetFindingsStatisticsInput: GetFindingsStatisticsInput
    GetFindingsStatisticsOutput: GetFindingsStatisticsOutput
    GetFindingV2Input: GetFindingV2Input
    GetFindingV2Output: GetFindingV2Output
    GetGeneratedPolicyInput: GetGeneratedPolicyInput
    GetGeneratedPolicyOutput: GetGeneratedPolicyOutput
    ListAccessPreviewFindingsInput: ListAccessPreviewFindingsInput
    ListAccessPreviewFindingsOutput: ListAccessPreviewFindingsOutput
    ListAccessPreviewsInput: ListAccessPreviewsInput
    ListAccessPreviewsOutput: ListAccessPreviewsOutput
    ListAnalyzedResourcesInput: ListAnalyzedResourcesInput
    ListAnalyzedResourcesOutput: ListAnalyzedResourcesOutput
    ListAnalyzersInput: ListAnalyzersInput
    ListAnalyzersOutput: ListAnalyzersOutput
    ListArchiveRulesInput: ListArchiveRulesInput
    ListArchiveRulesOutput: ListArchiveRulesOutput
    ListFindingsInput: ListFindingsInput
    ListFindingsOutput: ListFindingsOutput
    ListFindingsV2Input: ListFindingsV2Input
    ListFindingsV2Output: ListFindingsV2Output
    ListPolicyGenerationsInput: ListPolicyGenerationsInput
    ListPolicyGenerationsOutput: ListPolicyGenerationsOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    StartPolicyGenerationInput: StartPolicyGenerationInput
    StartPolicyGenerationOutput: StartPolicyGenerationOutput
    StartResourceScanInput: StartResourceScanInput
    StartResourceScanOutput: StartResourceScanOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateAnalyzerInput: UpdateAnalyzerInput
    UpdateAnalyzerOutput: UpdateAnalyzerOutput
    UpdateArchiveRuleInput: UpdateArchiveRuleInput
    UpdateArchiveRuleOutput: UpdateArchiveRuleOutput
    UpdateFindingsInput: UpdateFindingsInput
    UpdateFindingsOutput: UpdateFindingsOutput
    ValidatePolicyInput: ValidatePolicyInput
    ValidatePolicyOutput: ValidatePolicyOutput
end

return M
