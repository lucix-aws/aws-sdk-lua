-- Code generated by lua-client-codegen. DO NOT EDIT.

local record AssociateChannelInput
    -- The Amazon Resource Name (ARN) of the Channel to associate with the NotificationConfiguration. Supported ARNs include Amazon Q Developer in chat applications, the Console Mobile Application, and notifications-contacts.
    arn: string
    -- The ARN of the NotificationConfiguration to associate with the Channel.
    notificationConfigurationArn: string
end

local record AssociateChannelOutput
end

local record AssociateManagedNotificationAccountContactInput
    -- A unique value of an Account Contact Type to associate with the ManagedNotificationConfiguration.
    contactIdentifier: string
    -- The Amazon Resource Name (ARN) of the ManagedNotificationConfiguration to associate with the Account Contact.
    managedNotificationConfigurationArn: string
end

local record AssociateManagedNotificationAccountContactOutput
end

local record AssociateManagedNotificationAdditionalChannelInput
    -- The Amazon Resource Name (ARN) of the Channel to associate with the ManagedNotificationConfiguration. Supported ARNs include Amazon Q Developer in chat applications, the Console Mobile Application, and email (notifications-contacts).
    channelArn: string
    -- The Amazon Resource Name (ARN) of the ManagedNotificationConfiguration to associate with the additional Channel.
    managedNotificationConfigurationArn: string
end

local record AssociateManagedNotificationAdditionalChannelOutput
end

local record AssociateOrganizationalUnitInput
    -- The unique identifier of the organizational unit to associate.
    organizationalUnitId: string
    -- The Amazon Resource Name (ARN) of the notification configuration to associate with the organizational unit.
    notificationConfigurationArn: string
end

local record AssociateOrganizationalUnitOutput
end

local record CreateEventRuleInput
    -- The Amazon Resource Name (ARN) of the NotificationConfiguration associated with this EventRule.
    notificationConfigurationArn: string
    -- The matched event source. Must match one of the valid EventBridge sources. Only Amazon Web Services service sourced events are supported. For example, aws.ec2 and aws.cloudwatch. For more information, see Event delivery from Amazon Web Services services in the Amazon EventBridge User Guide.
    source: string
    -- The event type to match. Must match one of the valid Amazon EventBridge event types. For example, EC2 Instance State-change Notification and Amazon CloudWatch Alarm State Change. For more information, see Event delivery from Amazon Web Services services in the Amazon EventBridge User Guide.
    eventType: string
    -- An additional event pattern used to further filter the events this EventRule receives. For more information, see Amazon EventBridge event patterns in the Amazon EventBridge User Guide.
    eventPattern: string
    -- A list of Amazon Web Services Regions that send events to this EventRule.
    regions: {string}
end

local record CreateEventRuleOutput
    -- The ARN of the resource.
    arn: string
    -- The ARN of a NotificationConfiguration.
    notificationConfigurationArn: string
    -- A list of an EventRule's status by Region. Regions are mapped to EventRuleStatusSummary.
    statusSummaryByRegion: {string : EventRuleStatusSummary}
end

local record CreateNotificationConfigurationInput
    -- The name of the NotificationConfiguration. Supports RFC 3986's unreserved characters.
    name: string
    -- The description of the NotificationConfiguration.
    description: string
    -- The aggregation preference of the NotificationConfiguration. Values: LONG Aggregate notifications for long periods of time (12 hours). SHORT Aggregate notifications for short periods of time (5 minutes). NONE Don't aggregate notifications.
    aggregationDuration: string
    -- A map of tags assigned to a resource. A tag is a string-to-string map of key-value pairs.
    tags: {string : string}
end

local record CreateNotificationConfigurationOutput
    -- The Amazon Resource Name (ARN) of the NotificationConfiguration.
    arn: string
    -- The current status of this NotificationConfiguration.
    status: string
end

local record DeleteEventRuleInput
    -- The Amazon Resource Name (ARN) of the EventRule to delete.
    arn: string
end

local record DeleteEventRuleOutput
end

local record DeleteNotificationConfigurationInput
    -- The Amazon Resource Name (ARN) of the NotificationConfiguration to delete.
    arn: string
end

local record DeleteNotificationConfigurationOutput
end

local record DeregisterNotificationHubInput
    -- The NotificationConfiguration Region.
    notificationHubRegion: string
end

local record DeregisterNotificationHubOutput
    -- The NotificationConfiguration Region.
    notificationHubRegion: string
    -- NotificationConfiguration status information.
    statusSummary: NotificationHubStatusSummary
end

local record DisableNotificationsAccessForOrganizationInput
end

local record DisableNotificationsAccessForOrganizationOutput
end

local record DisassociateChannelInput
    -- The Amazon Resource Name (ARN) of the Channel to disassociate.
    arn: string
    -- The ARN of the NotificationConfiguration to disassociate.
    notificationConfigurationArn: string
end

local record DisassociateChannelOutput
end

local record DisassociateManagedNotificationAccountContactInput
    -- The unique value of an Account Contact Type to associate with the ManagedNotificationConfiguration.
    contactIdentifier: string
    -- The Amazon Resource Name (ARN) of the ManagedNotificationConfiguration to associate with the Account Contact.
    managedNotificationConfigurationArn: string
end

local record DisassociateManagedNotificationAccountContactOutput
end

local record DisassociateManagedNotificationAdditionalChannelInput
    -- The Amazon Resource Name (ARN) of the Channel to associate with the ManagedNotificationConfiguration.
    channelArn: string
    -- The Amazon Resource Name (ARN) of the Managed Notification Configuration to associate with the additional Channel.
    managedNotificationConfigurationArn: string
end

local record DisassociateManagedNotificationAdditionalChannelOutput
end

local record DisassociateOrganizationalUnitInput
    -- The unique identifier of the organizational unit to disassociate.
    organizationalUnitId: string
    -- The Amazon Resource Name (ARN) of the notification configuration to disassociate from the organizational unit.
    notificationConfigurationArn: string
end

local record DisassociateOrganizationalUnitOutput
end

local record EnableNotificationsAccessForOrganizationInput
end

local record EnableNotificationsAccessForOrganizationOutput
end

local record GetEventRuleInput
    -- The Amazon Resource Name (ARN) of the EventRule to return.
    arn: string
end

local record GetEventRuleOutput
    -- The ARN of the resource.
    arn: string
    -- The ARN of a NotificationConfiguration.
    notificationConfigurationArn: string
    -- The date when the EventRule was created.
    creationTime: number
    -- The matched event source. Must match one of the valid EventBridge sources. Only Amazon Web Services service sourced events are supported. For example, aws.ec2 and aws.cloudwatch. For more information, see Event delivery from Amazon Web Services services in the Amazon EventBridge User Guide.
    source: string
    -- The event type to match. Must match one of the valid Amazon EventBridge event types. For example, EC2 Instance State-change Notification and Amazon CloudWatch Alarm State Change. For more information, see Event delivery from Amazon Web Services services in the Amazon EventBridge User Guide.
    eventType: string
    -- An additional event pattern used to further filter the events this EventRule receives. For more information, see Amazon EventBridge event patterns in the Amazon EventBridge User Guide.
    eventPattern: string
    -- A list of Amazon Web Services Regions that send events to this EventRule.
    regions: {string}
    -- A list of managed rules from EventBridge that are associated with this EventRule. These are created by User Notifications within your account so this EventRule functions.
    managedRules: {string}
    -- A list of an EventRule's status by Region. Regions are mapped to EventRuleStatusSummary.
    statusSummaryByRegion: {string : EventRuleStatusSummary}
end

local record GetManagedNotificationChildEventInput
    -- The Amazon Resource Name (ARN) of the ManagedNotificationChildEvent to return.
    arn: string
    -- The locale code of the language used for the retrieved ManagedNotificationChildEvent. The default locale is English en_US.
    locale: string
end

local record GetManagedNotificationChildEventOutput
    -- The ARN of the resource.
    arn: string
    -- The Amazon Resource Name (ARN) of the ManagedNotificationConfiguration associated with the ManagedNotificationChildEvent.
    managedNotificationConfigurationArn: string
    -- The creation time of the ManagedNotificationChildEvent.
    creationTime: number
    -- The content of the ManagedNotificationChildEvent.
    content: ManagedNotificationChildEvent
end

local record GetManagedNotificationConfigurationInput
    -- The Amazon Resource Name (ARN) of the ManagedNotificationConfiguration to return.
    arn: string
end

local record GetManagedNotificationConfigurationOutput
    -- The ARN of the ManagedNotificationConfiguration resource.
    arn: string
    -- The name of the ManagedNotificationConfiguration.
    name: string
    -- The description of the ManagedNotificationConfiguration.
    description: string
    -- The category of the ManagedNotificationConfiguration.
    category: string
    -- The subCategory of the ManagedNotificationConfiguration.
    subCategory: string
end

local record GetManagedNotificationEventInput
    -- The Amazon Resource Name (ARN) of the ManagedNotificationEvent to return.
    arn: string
    -- The locale code of the language used for the retrieved ManagedNotificationEvent. The default locale is English (en_US).
    locale: string
end

local record GetManagedNotificationEventOutput
    -- The ARN of the resource.
    arn: string
    -- The ARN of the ManagedNotificationConfiguration.
    managedNotificationConfigurationArn: string
    -- The creation time of the ManagedNotificationEvent.
    creationTime: number
    -- The content of the ManagedNotificationEvent.
    content: ManagedNotificationEvent
end

local record GetNotificationConfigurationInput
    -- The Amazon Resource Name (ARN) of the NotificationConfiguration to return.
    arn: string
end

local record GetNotificationConfigurationOutput
    -- The ARN of the resource.
    arn: string
    -- The name of the NotificationConfiguration.
    name: string
    -- The description of the NotificationConfiguration.
    description: string
    -- The status of this NotificationConfiguration.
    status: string
    -- The creation time of the NotificationConfiguration.
    creationTime: number
    -- The aggregation preference of the NotificationConfiguration. Values: LONG Aggregate notifications for long periods of time (12 hours). SHORT Aggregate notifications for short periods of time (5 minutes). NONE Don't aggregate notifications.
    aggregationDuration: string
    -- The subtype of the notification configuration returned in the response.
    subtype: string
end

local record GetNotificationEventInput
    -- The Amazon Resource Name (ARN) of the NotificationEvent to return.
    arn: string
    -- The locale code of the language used for the retrieved NotificationEvent. The default locale is English en_US.
    locale: string
end

local record GetNotificationEventOutput
    -- The ARN of the resource.
    arn: string
    -- The ARN of the NotificationConfiguration.
    notificationConfigurationArn: string
    -- The creation time of the NotificationEvent.
    creationTime: number
    -- The content of the NotificationEvent.
    content: NotificationEventSchema
end

local record GetNotificationsAccessForOrganizationInput
end

local record GetNotificationsAccessForOrganizationOutput
    -- The AccessStatus of Service Trust Enablement for User Notifications to Amazon Web Services Organizations.
    notificationsAccessForOrganization: NotificationsAccessForOrganization
end

local record ListChannelsInput
    -- The Amazon Resource Name (ARN) of the NotificationConfiguration.
    notificationConfigurationArn: string
    -- The maximum number of results to be returned in this call. The default value is 20.
    maxResults: number
    -- The start token for paginated calls. Retrieved from the response of a previous ListNotificationEvents call. NextToken uses Base64 encoding.
    nextToken: string
end

local record ListChannelsOutput
    -- A pagination token. If a non-null pagination token is returned in a result, pass its value in another request to retrieve more entries.
    nextToken: string
    -- A list of Channels.
    channels: {string}
end

local record ListEventRulesInput
    -- The Amazon Resource Name (ARN) of the NotificationConfiguration.
    notificationConfigurationArn: string
    -- The maximum number of results to be returned in this call. The default value is 20.
    maxResults: number
    -- The start token for paginated calls. Retrieved from the response of a previous ListEventRules call. Next token uses Base64 encoding.
    nextToken: string
end

local record ListEventRulesOutput
    -- A pagination token. If a non-null pagination token is returned in a result, pass its value in another request to retrieve more entries.
    nextToken: string
    -- A list of EventRules.
    eventRules: {EventRuleStructure}
end

local record ListManagedNotificationChannelAssociationsInput
    -- The Amazon Resource Name (ARN) of the ManagedNotificationConfiguration to match.
    managedNotificationConfigurationArn: string
    -- The maximum number of results to be returned in this call. Defaults to 20.
    maxResults: number
    -- The start token for paginated calls. Retrieved from the response of a previous ListManagedNotificationChannelAssociations call.
    nextToken: string
end

local record ListManagedNotificationChannelAssociationsOutput
    -- A pagination token. If a non-null pagination token is returned in a result, pass its value in another request to retrieve more entries.
    nextToken: string
    -- A list that contains the following information about a channel association.
    channelAssociations: {ManagedNotificationChannelAssociationSummary}
end

local record ListManagedNotificationChildEventsInput
    -- The Amazon Resource Name (ARN) of the ManagedNotificationEvent.
    aggregateManagedNotificationEventArn: string
    -- The earliest time of events to return from this call.
    startTime: number
    -- Latest time of events to return from this call.
    endTime: number
    -- The locale code of the language used for the retrieved NotificationEvent. The default locale is English.en_US.
    locale: string
    -- The maximum number of results to be returned in this call. Defaults to 20.
    maxResults: number
    -- The Amazon Web Services account ID associated with the Managed Notification Child Events.
    relatedAccount: string
    -- The identifier of the Amazon Web Services Organizations organizational unit (OU) associated with the Managed Notification Child Events.
    organizationalUnitId: string
    -- The start token for paginated calls. Retrieved from the response of a previous ListManagedNotificationChannelAssociations call. Next token uses Base64 encoding.
    nextToken: string
end

local record ListManagedNotificationChildEventsOutput
    -- A pagination token. If a non-null pagination token is returned in a result, pass its value in another request to retrieve more entries.
    nextToken: string
    -- A pagination token. If a non-null pagination token is returned in a result, pass its value in another request to retrieve more entries.
    managedNotificationChildEvents: {ManagedNotificationChildEventOverview}
end

local record ListManagedNotificationConfigurationsInput
    -- The identifier or ARN of the notification channel to filter configurations by.
    channelIdentifier: string
    -- The maximum number of results to be returned in this call. Defaults to 20.
    maxResults: number
    -- The start token for paginated calls. Retrieved from the response of a previous ListManagedNotificationChannelAssociations call. Next token uses Base64 encoding.
    nextToken: string
end

local record ListManagedNotificationConfigurationsOutput
    -- A pagination token. If a non-null pagination token is returned in a result, pass its value in another request to retrieve more entries.
    nextToken: string
    -- A list of Managed Notification Configurations matching the request criteria.
    managedNotificationConfigurations: {ManagedNotificationConfigurationStructure}
end

local record ListManagedNotificationEventsInput
    -- The earliest time of events to return from this call.
    startTime: number
    -- Latest time of events to return from this call.
    endTime: number
    -- The locale code of the language used for the retrieved NotificationEvent. The default locale is English (en_US).
    locale: string
    -- The Amazon Web Services service the event originates from. For example aws.cloudwatch.
    source: string
    -- The maximum number of results to be returned in this call. Defaults to 20.
    maxResults: number
    -- The start token for paginated calls. Retrieved from the response of a previous ListManagedNotificationChannelAssociations call. Next token uses Base64 encoding.
    nextToken: string
    -- The Organizational Unit Id that an Amazon Web Services account belongs to.
    organizationalUnitId: string
    -- The Amazon Web Services account ID associated with the Managed Notification Events.
    relatedAccount: string
end

local record ListManagedNotificationEventsOutput
    -- A pagination token. If a non-null pagination token is returned in a result, pass its value in another request to retrieve more entries.
    nextToken: string
    -- A list of Managed Notification Events matching the request criteria.
    managedNotificationEvents: {ManagedNotificationEventOverview}
end

local record ListMemberAccountsInput
    -- The Amazon Resource Name (ARN) of the notification configuration used to filter the member accounts.
    notificationConfigurationArn: string
    -- The maximum number of results to return in a single call. Valid values are 1-100.
    maxResults: number
    -- The token for the next page of results. Use the value returned in the previous response.
    nextToken: string
    -- The member account identifier used to filter the results.
    memberAccount: string
    -- The status used to filter the member accounts.
    status: string
    -- The organizational unit ID used to filter the member accounts.
    organizationalUnitId: string
end

local record ListMemberAccountsOutput
    -- The list of member accounts that match the specified criteria.
    memberAccounts: {MemberAccount}
    -- The token to use for the next page of results.
    nextToken: string
end

local record ListNotificationConfigurationsInput
    -- The matched event source. Must match one of the valid EventBridge sources. Only Amazon Web Services service sourced events are supported. For example, aws.ec2 and aws.cloudwatch. For more information, see Event delivery from Amazon Web Services services in the Amazon EventBridge User Guide.
    eventRuleSource: string
    -- The Amazon Resource Name (ARN) of the Channel to match.
    channelArn: string
    -- The NotificationConfiguration status to match. Values: ACTIVE All EventRules are ACTIVE and any call can be run. PARTIALLY_ACTIVE Some EventRules are ACTIVE and some are INACTIVE. Any call can be run. Any call can be run. INACTIVE All EventRules are INACTIVE and any call can be run. DELETING This NotificationConfiguration is being deleted. Only GET and LIST calls can be run.
    status: string
    -- The subtype used to filter the notification configurations in the request.
    subtype: string
    -- The maximum number of results to be returned in this call. Defaults to 20.
    maxResults: number
    -- The start token for paginated calls. Retrieved from the response of a previous ListEventRules call. Next token uses Base64 encoding.
    nextToken: string
end

local record ListNotificationConfigurationsOutput
    -- A pagination token. If a non-null pagination token is returned in a result, pass its value in another request to retrieve more entries.
    nextToken: string
    -- The NotificationConfigurations in the account.
    notificationConfigurations: {NotificationConfigurationStructure}
end

local record ListNotificationEventsInput
    -- The earliest time of events to return from this call.
    startTime: number
    -- Latest time of events to return from this call.
    endTime: number
    -- The locale code of the language used for the retrieved NotificationEvent. The default locale is English (en_US).
    locale: string
    -- The matched event source. Must match one of the valid EventBridge sources. Only Amazon Web Services service sourced events are supported. For example, aws.ec2 and aws.cloudwatch. For more information, see Event delivery from Amazon Web Services services in the Amazon EventBridge User Guide.
    source: string
    -- Include aggregated child events in the result.
    includeChildEvents: boolean
    -- The Amazon Resource Name (ARN) of the aggregatedNotificationEventArn to match.
    aggregateNotificationEventArn: string
    -- The maximum number of results to be returned in this call. Defaults to 20.
    maxResults: number
    -- The start token for paginated calls. Retrieved from the response of a previous ListEventRules call. Next token uses Base64 encoding.
    nextToken: string
    -- The unique identifier of the organizational unit used to filter notification events.
    organizationalUnitId: string
end

local record ListNotificationEventsOutput
    -- A pagination token. If a non-null pagination token is returned in a result, pass its value in another request to retrieve more entries.
    nextToken: string
    -- The list of notification events.
    notificationEvents: {NotificationEventOverview}
end

local record ListNotificationHubsInput
    -- The maximum number of records to list in a single response.
    maxResults: number
    -- A pagination token. Set to null to start listing notification hubs from the start.
    nextToken: string
end

local record ListNotificationHubsOutput
    -- The NotificationHubs in the account.
    notificationHubs: {NotificationHubOverview}
    -- A pagination token. If a non-null pagination token is returned in a result, pass its value in another request to retrieve more entries.
    nextToken: string
end

local record ListOrganizationalUnitsInput
    -- The Amazon Resource Name (ARN) of the notification configuration used to filter the organizational units.
    notificationConfigurationArn: string
    -- The maximum number of organizational units to return in a single call. Valid values are 1-100.
    maxResults: number
    -- The token for the next page of results. Use the value returned in the previous response.
    nextToken: string
end

local record ListOrganizationalUnitsOutput
    -- The list of organizational units that match the specified criteria.
    organizationalUnits: {string}
    -- The token to use for the next page of results. If there are no additional results, this value is null.
    nextToken: string
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) to use to list tags.
    arn: string
end

local record ListTagsForResourceOutput
    -- A list of tags for the specified ARN.
    tags: {string : string}
end

local record RegisterNotificationHubInput
    -- The Region of the NotificationHub.
    notificationHubRegion: string
end

local record RegisterNotificationHubOutput
    -- The Region of the NotificationHub.
    notificationHubRegion: string
    -- Provides additional information about the current NotificationConfiguration status information.
    statusSummary: NotificationHubStatusSummary
    -- The date the resource was created.
    creationTime: number
    -- The date the resource was last activated.
    lastActivationTime: number
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) to use to tag a resource.
    arn: string
    -- A map of tags assigned to a resource. A tag is a string-to-string map of key-value pairs.
    tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) to use to untag a resource.
    arn: string
    -- The tag keys to use to untag a resource.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateEventRuleInput
    -- The Amazon Resource Name (ARN) to use to update the EventRule.
    arn: string
    -- An additional event pattern used to further filter the events this EventRule receives. For more information, see Amazon EventBridge event patterns in the Amazon EventBridge User Guide.
    eventPattern: string
    -- A list of Amazon Web Services Regions that sends events to this EventRule.
    regions: {string}
end

local record UpdateEventRuleOutput
    -- The Amazon Resource Name (ARN) to use to update the EventRule.
    arn: string
    -- The ARN of the NotificationConfiguration.
    notificationConfigurationArn: string
    -- The status of the action by Region.
    statusSummaryByRegion: {string : EventRuleStatusSummary}
end

local record UpdateNotificationConfigurationInput
    -- The Amazon Resource Name (ARN) used to update the NotificationConfiguration.
    arn: string
    -- The name of the NotificationConfiguration.
    name: string
    -- The description of the NotificationConfiguration.
    description: string
    -- The aggregation preference of the NotificationConfiguration. Values: LONG Aggregate notifications for long periods of time (12 hours). SHORT Aggregate notifications for short periods of time (5 minutes). NONE Don't aggregate notifications.
    aggregationDuration: string
end

local record UpdateNotificationConfigurationOutput
    -- The ARN used to update the NotificationConfiguration.
    arn: string
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The type of the resource that exceeds the service quota.
    resourceType: string
    -- The ID of the resource that exceeds the service quota.
    resourceId: string
    -- The code for the service quota exceeded in Service Quotas.
    serviceCode: string
    -- The code for the service quota in Service Quotas.
    quotaCode: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The type of the resource that exceeds the service quota.
    resourceType: string
    -- The ID of the resource that exceeds the service quota.
    resourceId: string
    -- The code for the service quota exceeded in Service Quotas.
    serviceCode: string
    -- The code for the service quota in Service Quotas.
    quotaCode: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The type of the resource that exceeds the service quota.
    resourceType: string
    -- The ID of the resource that exceeds the service quota.
    resourceId: string
    -- The code for the service quota exceeded in Service Quotas.
    serviceCode: string
    -- The code for the service quota in Service Quotas.
    quotaCode: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The type of the resource that exceeds the service quota.
    resourceType: string
    -- The ID of the resource that exceeds the service quota.
    resourceId: string
    -- The code for the service quota exceeded in Service Quotas.
    serviceCode: string
    -- The code for the service quota in Service Quotas.
    quotaCode: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The type of the resource that exceeds the service quota.
    resourceType: string
    -- The ID of the resource that exceeds the service quota.
    resourceId: string
    -- The code for the service quota exceeded in Service Quotas.
    serviceCode: string
    -- The code for the service quota in Service Quotas.
    quotaCode: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The type of the resource that exceeds the service quota.
    resourceType: string
    -- The ID of the resource that exceeds the service quota.
    resourceId: string
    -- The code for the service quota exceeded in Service Quotas.
    serviceCode: string
    -- The code for the service quota in Service Quotas.
    quotaCode: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The type of the resource that exceeds the service quota.
    resourceType: string
    -- The ID of the resource that exceeds the service quota.
    resourceId: string
    -- The code for the service quota exceeded in Service Quotas.
    serviceCode: string
    -- The code for the service quota in Service Quotas.
    quotaCode: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The type of the resource that exceeds the service quota.
    resourceType: string
    -- The ID of the resource that exceeds the service quota.
    resourceId: string
    -- The code for the service quota exceeded in Service Quotas.
    serviceCode: string
    -- The code for the service quota in Service Quotas.
    quotaCode: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The type of the resource that exceeds the service quota.
    resourceType: string
    -- The ID of the resource that exceeds the service quota.
    resourceId: string
    -- The code for the service quota exceeded in Service Quotas.
    serviceCode: string
    -- The code for the service quota in Service Quotas.
    quotaCode: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

-- User does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID that prompted the conflict error.
    resourceId: string
end

-- Unexpected error during processing of request.
local record InternalServerException
    message: string
end

-- Request references a resource which does not exist.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource that wasn't found.
    resourceId: string
end

-- Request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- Identifies the service being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- This exception is thrown when the notification event fails validation.
local record ValidationException
    message: string
    -- The reason why your input is considered invalid.
    reason: string
    -- The list of input fields that are invalid.
    fieldList: {ValidationExceptionField}
end

local record M
    AssociateChannelInput: AssociateChannelInput
    AssociateChannelOutput: AssociateChannelOutput
    AssociateManagedNotificationAccountContactInput: AssociateManagedNotificationAccountContactInput
    AssociateManagedNotificationAccountContactOutput: AssociateManagedNotificationAccountContactOutput
    AssociateManagedNotificationAdditionalChannelInput: AssociateManagedNotificationAdditionalChannelInput
    AssociateManagedNotificationAdditionalChannelOutput: AssociateManagedNotificationAdditionalChannelOutput
    AssociateOrganizationalUnitInput: AssociateOrganizationalUnitInput
    AssociateOrganizationalUnitOutput: AssociateOrganizationalUnitOutput
    CreateEventRuleInput: CreateEventRuleInput
    CreateEventRuleOutput: CreateEventRuleOutput
    CreateNotificationConfigurationInput: CreateNotificationConfigurationInput
    CreateNotificationConfigurationOutput: CreateNotificationConfigurationOutput
    DeleteEventRuleInput: DeleteEventRuleInput
    DeleteEventRuleOutput: DeleteEventRuleOutput
    DeleteNotificationConfigurationInput: DeleteNotificationConfigurationInput
    DeleteNotificationConfigurationOutput: DeleteNotificationConfigurationOutput
    DeregisterNotificationHubInput: DeregisterNotificationHubInput
    DeregisterNotificationHubOutput: DeregisterNotificationHubOutput
    DisableNotificationsAccessForOrganizationInput: DisableNotificationsAccessForOrganizationInput
    DisableNotificationsAccessForOrganizationOutput: DisableNotificationsAccessForOrganizationOutput
    DisassociateChannelInput: DisassociateChannelInput
    DisassociateChannelOutput: DisassociateChannelOutput
    DisassociateManagedNotificationAccountContactInput: DisassociateManagedNotificationAccountContactInput
    DisassociateManagedNotificationAccountContactOutput: DisassociateManagedNotificationAccountContactOutput
    DisassociateManagedNotificationAdditionalChannelInput: DisassociateManagedNotificationAdditionalChannelInput
    DisassociateManagedNotificationAdditionalChannelOutput: DisassociateManagedNotificationAdditionalChannelOutput
    DisassociateOrganizationalUnitInput: DisassociateOrganizationalUnitInput
    DisassociateOrganizationalUnitOutput: DisassociateOrganizationalUnitOutput
    EnableNotificationsAccessForOrganizationInput: EnableNotificationsAccessForOrganizationInput
    EnableNotificationsAccessForOrganizationOutput: EnableNotificationsAccessForOrganizationOutput
    GetEventRuleInput: GetEventRuleInput
    GetEventRuleOutput: GetEventRuleOutput
    GetManagedNotificationChildEventInput: GetManagedNotificationChildEventInput
    GetManagedNotificationChildEventOutput: GetManagedNotificationChildEventOutput
    GetManagedNotificationConfigurationInput: GetManagedNotificationConfigurationInput
    GetManagedNotificationConfigurationOutput: GetManagedNotificationConfigurationOutput
    GetManagedNotificationEventInput: GetManagedNotificationEventInput
    GetManagedNotificationEventOutput: GetManagedNotificationEventOutput
    GetNotificationConfigurationInput: GetNotificationConfigurationInput
    GetNotificationConfigurationOutput: GetNotificationConfigurationOutput
    GetNotificationEventInput: GetNotificationEventInput
    GetNotificationEventOutput: GetNotificationEventOutput
    GetNotificationsAccessForOrganizationInput: GetNotificationsAccessForOrganizationInput
    GetNotificationsAccessForOrganizationOutput: GetNotificationsAccessForOrganizationOutput
    ListChannelsInput: ListChannelsInput
    ListChannelsOutput: ListChannelsOutput
    ListEventRulesInput: ListEventRulesInput
    ListEventRulesOutput: ListEventRulesOutput
    ListManagedNotificationChannelAssociationsInput: ListManagedNotificationChannelAssociationsInput
    ListManagedNotificationChannelAssociationsOutput: ListManagedNotificationChannelAssociationsOutput
    ListManagedNotificationChildEventsInput: ListManagedNotificationChildEventsInput
    ListManagedNotificationChildEventsOutput: ListManagedNotificationChildEventsOutput
    ListManagedNotificationConfigurationsInput: ListManagedNotificationConfigurationsInput
    ListManagedNotificationConfigurationsOutput: ListManagedNotificationConfigurationsOutput
    ListManagedNotificationEventsInput: ListManagedNotificationEventsInput
    ListManagedNotificationEventsOutput: ListManagedNotificationEventsOutput
    ListMemberAccountsInput: ListMemberAccountsInput
    ListMemberAccountsOutput: ListMemberAccountsOutput
    ListNotificationConfigurationsInput: ListNotificationConfigurationsInput
    ListNotificationConfigurationsOutput: ListNotificationConfigurationsOutput
    ListNotificationEventsInput: ListNotificationEventsInput
    ListNotificationEventsOutput: ListNotificationEventsOutput
    ListNotificationHubsInput: ListNotificationHubsInput
    ListNotificationHubsOutput: ListNotificationHubsOutput
    ListOrganizationalUnitsInput: ListOrganizationalUnitsInput
    ListOrganizationalUnitsOutput: ListOrganizationalUnitsOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    RegisterNotificationHubInput: RegisterNotificationHubInput
    RegisterNotificationHubOutput: RegisterNotificationHubOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateEventRuleInput: UpdateEventRuleInput
    UpdateEventRuleOutput: UpdateEventRuleOutput
    UpdateNotificationConfigurationInput: UpdateNotificationConfigurationInput
    UpdateNotificationConfigurationOutput: UpdateNotificationConfigurationOutput
end

return M
