-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record AssociateEntityToThingInput
        -- The name of the thing to which the entity is to be associated.
        thingName: string
        -- The ID of the device to be associated with the thing.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:device:DEVICENAME
        entityId: string
        -- The version of the user's namespace. Defaults to the latest version of the user's namespace.
        namespaceVersion: number
    end

    record AssociateEntityToThingOutput
    end

    record CreateFlowTemplateInput
        -- The workflow DefinitionDocument.
        definition: DefinitionDocument
        -- The namespace version in which the workflow is to be created.
        -- If no value is specified, the latest version is used by default.
        compatibleNamespaceVersion: number
    end

    record CreateFlowTemplateOutput
        -- The summary object that describes the created workflow.
        summary: FlowTemplateSummary
    end

    record CreateSystemInstanceInput
        -- Metadata, consisting of key-value pairs, that can be used to categorize your system instances.
        tags: {Tag}
        definition: DefinitionDocument
        -- The target type of the deployment. Valid values are GREENGRASS and CLOUD.
        target: string
        -- The name of the Greengrass group where the system instance will be deployed. This value is required if
        -- the value of the target parameter is GREENGRASS.
        greengrassGroupName: string
        -- The name of the Amazon Simple Storage Service bucket that will be used to store and deploy the system instance's resource file. This value is required if
        -- the value of the target parameter is GREENGRASS.
        s3BucketName: string
        metricsConfiguration: MetricsConfiguration
        -- The ARN of the IAM role that AWS IoT Things Graph will assume when it executes the flow. This role must have
        -- read and write access to AWS Lambda and AWS IoT and any other AWS services that the flow uses when it executes. This
        -- value is required if the value of the target parameter is CLOUD.
        flowActionsRoleArn: string
    end

    record CreateSystemInstanceOutput
        -- The summary object that describes the new system instance.
        summary: SystemInstanceSummary
    end

    record CreateSystemTemplateInput
        -- The DefinitionDocument used to create the system.
        definition: DefinitionDocument
        -- The namespace version in which the system is to be created.
        -- If no value is specified, the latest version is used by default.
        compatibleNamespaceVersion: number
    end

    record CreateSystemTemplateOutput
        -- The summary object that describes the created system.
        summary: SystemTemplateSummary
    end

    record DeleteFlowTemplateInput
        -- The ID of the workflow to be deleted.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:workflow:WORKFLOWNAME
        id: string
    end

    record DeleteFlowTemplateOutput
    end

    record DeleteNamespaceInput
    end

    record DeleteNamespaceOutput
        -- The ARN of the namespace to be deleted.
        namespaceArn: string
        -- The name of the namespace to be deleted.
        namespaceName: string
    end

    record DeleteSystemInstanceInput
        -- The ID of the system instance to be deleted.
        id: string
    end

    record DeleteSystemInstanceOutput
    end

    record DeleteSystemTemplateInput
        -- The ID of the system to be deleted.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:system:SYSTEMNAME
        id: string
    end

    record DeleteSystemTemplateOutput
    end

    record DeploySystemInstanceInput
        -- The ID of the system instance. This value is returned by the CreateSystemInstance action.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:deployment:DEPLOYMENTNAME
        id: string
    end

    record DeploySystemInstanceOutput
        -- An object that contains summary information about a system instance that was deployed.
        summary: SystemInstanceSummary
        -- The ID of the Greengrass deployment used to deploy the system instance.
        greengrassDeploymentId: string
    end

    record DeprecateFlowTemplateInput
        -- The ID of the workflow to be deleted.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:workflow:WORKFLOWNAME
        id: string
    end

    record DeprecateFlowTemplateOutput
    end

    record DeprecateSystemTemplateInput
        -- The ID of the system to delete.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:system:SYSTEMNAME
        id: string
    end

    record DeprecateSystemTemplateOutput
    end

    record DescribeNamespaceInput
        -- The name of the user's namespace. Set this to aws to get the public namespace.
        namespaceName: string
    end

    record DescribeNamespaceOutput
        -- The ARN of the namespace.
        namespaceArn: string
        -- The name of the namespace.
        namespaceName: string
        -- The name of the public namespace that the latest namespace version is tracking.
        trackingNamespaceName: string
        -- The version of the public namespace that the latest version is tracking.
        trackingNamespaceVersion: number
        -- The version of the user's namespace to describe.
        namespaceVersion: number
    end

    record DissociateEntityFromThingInput
        -- The name of the thing to disassociate.
        thingName: string
        -- The entity type from which to disassociate the thing.
        entityType: string
    end

    record DissociateEntityFromThingOutput
    end

    record GetEntitiesInput
        -- An array of entity IDs.
        -- The IDs should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:device:DEVICENAME
        ids: {string}
        -- The version of the user's namespace. Defaults to the latest version of the user's namespace.
        namespaceVersion: number
    end

    record GetEntitiesOutput
        -- An array of descriptions for the specified entities.
        descriptions: {EntityDescription}
    end

    record GetFlowTemplateInput
        -- The ID of the workflow.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:workflow:WORKFLOWNAME
        id: string
        -- The number of the workflow revision to retrieve.
        revisionNumber: number
    end

    record GetFlowTemplateOutput
        -- The object that describes the specified workflow.
        description: FlowTemplateDescription
    end

    record GetFlowTemplateRevisionsInput
        -- The ID of the workflow.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:workflow:WORKFLOWNAME
        id: string
        -- The string that specifies the next page of results. Use this when you're paginating results.
        nextToken: string
        -- The maximum number of results to return in the response.
        maxResults: number
    end

    record GetFlowTemplateRevisionsOutput
        -- An array of objects that provide summary data about each revision.
        summaries: {FlowTemplateSummary}
        -- The string to specify as nextToken when you request the next page of results.
        nextToken: string
    end

    record GetNamespaceDeletionStatusInput
    end

    record GetNamespaceDeletionStatusOutput
        -- The ARN of the namespace that is being deleted.
        namespaceArn: string
        -- The name of the namespace that is being deleted.
        namespaceName: string
        -- The status of the deletion request.
        status: string
        -- An error code returned by the namespace deletion task.
        errorCode: string
        -- An error code returned by the namespace deletion task.
        errorMessage: string
    end

    record GetSystemInstanceInput
        -- The ID of the system deployment instance. This value is returned by CreateSystemInstance.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:deployment:DEPLOYMENTNAME
        id: string
    end

    record GetSystemInstanceOutput
        -- An object that describes the system instance.
        description: SystemInstanceDescription
    end

    record GetSystemTemplateInput
        -- The ID of the system to get. This ID must be in the user's namespace.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:system:SYSTEMNAME
        id: string
        -- The number that specifies the revision of the system to get.
        revisionNumber: number
    end

    record GetSystemTemplateOutput
        -- An object that contains summary data about the system.
        description: SystemTemplateDescription
    end

    record GetSystemTemplateRevisionsInput
        -- The ID of the system template.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:system:SYSTEMNAME
        id: string
        -- The string that specifies the next page of results. Use this when you're paginating results.
        nextToken: string
        -- The maximum number of results to return in the response.
        maxResults: number
    end

    record GetSystemTemplateRevisionsOutput
        -- An array of objects that contain summary data about the system template revisions.
        summaries: {SystemTemplateSummary}
        -- The string to specify as nextToken when you request the next page of results.
        nextToken: string
    end

    record GetUploadStatusInput
        -- The ID of the upload. This value is returned by the UploadEntityDefinitions action.
        uploadId: string
    end

    record GetUploadStatusOutput
        -- The ID of the upload.
        uploadId: string
        -- The status of the upload. The initial status is IN_PROGRESS. The response show all validation failures if the upload fails.
        uploadStatus: string
        -- The ARN of the upload.
        namespaceArn: string
        -- The name of the upload's namespace.
        namespaceName: string
        -- The version of the user's namespace. Defaults to the latest version of the user's namespace.
        namespaceVersion: number
        -- The reason for an upload failure.
        failureReason: {string}
        -- The date at which the upload was created.
        createdDate: number
    end

    record ListFlowExecutionMessagesInput
        -- The ID of the flow execution.
        flowExecutionId: string
        -- The string that specifies the next page of results. Use this when you're paginating results.
        nextToken: string
        -- The maximum number of results to return in the response.
        maxResults: number
    end

    record ListFlowExecutionMessagesOutput
        -- A list of objects that contain information about events in the specified flow execution.
        messages: {FlowExecutionMessage}
        -- The string to specify as nextToken when you request the next page of results.
        nextToken: string
    end

    record ListTagsForResourceInput
        -- The maximum number of tags to return.
        maxResults: number
        -- The Amazon Resource Name (ARN) of the resource whose tags are to be returned.
        resourceArn: string
        -- The token that specifies the next page of results to return.
        nextToken: string
    end

    record ListTagsForResourceOutput
        -- List of tags returned by the ListTagsForResource operation.
        tags: {Tag}
        -- The token that specifies the next page of results to return.
        nextToken: string
    end

    record SearchEntitiesInput
        -- The entity types for which to search.
        entityTypes: {string}
        -- Optional filter to apply to the search. Valid filters are NAME
        -- NAMESPACE, SEMANTIC_TYPE_PATH and REFERENCED_ENTITY_ID.
        -- REFERENCED_ENTITY_ID filters on entities that are used by the entity in the result set. For example,
        -- you can filter on the ID of a property that is used in a state.
        -- Multiple filters function as OR criteria in the query. Multiple values passed inside the filter function as AND criteria.
        filters: {EntityFilter}
        -- The string that specifies the next page of results. Use this when you're paginating results.
        nextToken: string
        -- The maximum number of results to return in the response.
        maxResults: number
        -- The version of the user's namespace. Defaults to the latest version of the user's namespace.
        namespaceVersion: number
    end

    record SearchEntitiesOutput
        -- An array of descriptions for each entity returned in the search result.
        descriptions: {EntityDescription}
        -- The string to specify as nextToken when you request the next page of results.
        nextToken: string
    end

    record SearchFlowExecutionsInput
        -- The ID of the system instance that contains the flow.
        systemInstanceId: string
        -- The ID of a flow execution.
        flowExecutionId: string
        -- The date and time of the earliest flow execution to return.
        startTime: number
        -- The date and time of the latest flow execution to return.
        endTime: number
        -- The string that specifies the next page of results. Use this when you're paginating results.
        nextToken: string
        -- The maximum number of results to return in the response.
        maxResults: number
    end

    record SearchFlowExecutionsOutput
        -- An array of objects that contain summary information about each workflow execution in the result set.
        summaries: {FlowExecutionSummary}
        -- The string to specify as nextToken when you request the next page of results.
        nextToken: string
    end

    record SearchFlowTemplatesInput
        -- An array of objects that limit the result set. The only valid filter is DEVICE_MODEL_ID.
        filters: {FlowTemplateFilter}
        -- The string that specifies the next page of results. Use this when you're paginating results.
        nextToken: string
        -- The maximum number of results to return in the response.
        maxResults: number
    end

    record SearchFlowTemplatesOutput
        -- An array of objects that contain summary information about each workflow in the result set.
        summaries: {FlowTemplateSummary}
        -- The string to specify as nextToken when you request the next page of results.
        nextToken: string
    end

    record SearchSystemInstancesInput
        -- Optional filter to apply to the search. Valid filters are SYSTEM_TEMPLATE_ID, STATUS, and
        -- GREENGRASS_GROUP_NAME.
        -- Multiple filters function as OR criteria in the query. Multiple values passed inside the filter function as AND criteria.
        filters: {SystemInstanceFilter}
        -- The string that specifies the next page of results. Use this when you're paginating results.
        nextToken: string
        -- The maximum number of results to return in the response.
        maxResults: number
    end

    record SearchSystemInstancesOutput
        -- An array of objects that contain summary data abour the system instances in the result set.
        summaries: {SystemInstanceSummary}
        -- The string to specify as nextToken when you request the next page of results.
        nextToken: string
    end

    record SearchSystemTemplatesInput
        -- An array of filters that limit the result set. The only valid filter is FLOW_TEMPLATE_ID.
        filters: {SystemTemplateFilter}
        -- The string that specifies the next page of results. Use this when you're paginating results.
        nextToken: string
        -- The maximum number of results to return in the response.
        maxResults: number
    end

    record SearchSystemTemplatesOutput
        -- An array of objects that contain summary information about each system deployment in the result set.
        summaries: {SystemTemplateSummary}
        -- The string to specify as nextToken when you request the next page of results.
        nextToken: string
    end

    record SearchThingsInput
        -- The ID of the entity to which the things are associated.
        -- The IDs should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:device:DEVICENAME
        entityId: string
        -- The string that specifies the next page of results. Use this when you're paginating results.
        nextToken: string
        -- The maximum number of results to return in the response.
        maxResults: number
        -- The version of the user's namespace. Defaults to the latest version of the user's namespace.
        namespaceVersion: number
    end

    record SearchThingsOutput
        -- An array of things in the result set.
        things: {Thing}
        -- The string to specify as nextToken when you request the next page of results.
        nextToken: string
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource whose tags are returned.
        resourceArn: string
        -- A list of tags to add to the resource.>
        tags: {Tag}
    end

    record TagResourceOutput
    end

    record UndeploySystemInstanceInput
        -- The ID of the system instance to remove from its target.
        id: string
    end

    record UndeploySystemInstanceOutput
        -- An object that contains summary information about the system instance that was removed from its target.
        summary: SystemInstanceSummary
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource whose tags are to be removed.
        resourceArn: string
        -- A list of tag key names to remove from the resource. You don't specify the value. Both the key and its associated value are removed.
        -- This parameter to the API requires a JSON text string argument. For information on how to format a JSON parameter for the various command line tool environments, see Using JSON for Parameters in the AWS CLI User Guide.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateFlowTemplateInput
        -- The ID of the workflow to be updated.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:workflow:WORKFLOWNAME
        id: string
        -- The DefinitionDocument that contains the updated workflow definition.
        definition: DefinitionDocument
        -- The version of the user's namespace.
        -- If no value is specified, the latest version is used by default. Use the GetFlowTemplateRevisions if you want to find earlier revisions of the flow
        -- to update.
        compatibleNamespaceVersion: number
    end

    record UpdateFlowTemplateOutput
        -- An object containing summary information about the updated workflow.
        summary: FlowTemplateSummary
    end

    record UpdateSystemTemplateInput
        -- The ID of the system to be updated.
        -- The ID should be in the following format.
        -- urn:tdm:REGION/ACCOUNT ID/default:system:SYSTEMNAME
        id: string
        -- The DefinitionDocument that contains the updated system definition.
        definition: DefinitionDocument
        -- The version of the user's namespace. Defaults to the latest version of the user's namespace.
        -- If no value is specified, the latest version is used by default.
        compatibleNamespaceVersion: number
    end

    record UpdateSystemTemplateOutput
        -- An object containing summary information about the updated system.
        summary: SystemTemplateSummary
    end

    record UploadEntityDefinitionsInput
        -- The DefinitionDocument that defines the updated entities.
        document: DefinitionDocument
        -- A Boolean that specifies whether to synchronize with the latest version of the public namespace. If set to true, the upload will create a new namespace version.
        syncWithPublicNamespace: boolean
        -- A Boolean that specifies whether to deprecate all entities in the latest version before uploading the new DefinitionDocument.
        -- If set to true, the upload will create a new namespace version.
        deprecateExistingEntities: boolean
    end

    record UploadEntityDefinitionsOutput
        -- The ID that specifies the upload action. You can use this to track the status of the upload.
        uploadId: string
    end

    record InternalFailureException
        message: string
    end

    record InvalidRequestException
        message: string
    end

    record ResourceNotFoundException
        message: string
    end

    record ThrottlingException
        message: string
    end

    record LimitExceededException
        message: string
    end

    record ResourceAlreadyExistsException
        message: string
    end

    record ResourceInUseException
        message: string
    end

    enum DeploymentTarget
        "GREENGRASS"
        "CLOUD"
    end

    enum EntityType
        "DEVICE"
        "SERVICE"
        "DEVICE_MODEL"
        "CAPABILITY"
        "STATE"
        "ACTION"
        "EVENT"
        "PROPERTY"
        "MAPPING"
        "ENUM"
    end

    enum NamespaceDeletionStatus
        "IN_PROGRESS"
        "SUCCEEDED"
        "FAILED"
    end

    enum NamespaceDeletionStatusErrorCodes
        "VALIDATION_FAILED"
    end

    enum UploadStatus
        "IN_PROGRESS"
        "SUCCEEDED"
        "FAILED"
    end

end

return M
