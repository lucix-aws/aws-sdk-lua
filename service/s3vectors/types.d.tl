-- Code generated by lua-client-codegen. DO NOT EDIT.

local record CreateIndexInput
    -- The name of the vector bucket to create the vector index in.
    vectorBucketName: string
    -- The Amazon Resource Name (ARN) of the vector bucket to create the vector index in.
    vectorBucketArn: string
    -- The name of the vector index to create.
    indexName: string
    -- The data type of the vectors to be inserted into the vector index.
    dataType: string
    -- The dimensions of the vectors to be inserted into the vector index.
    dimension: number
    -- The distance metric to be used for similarity search.
    distanceMetric: string
    -- The metadata configuration for the vector index.
    metadataConfiguration: MetadataConfiguration
    -- The encryption configuration for a vector index. By default, if you don't specify, all new vectors in the vector index will use the encryption configuration of the vector bucket.
    encryptionConfiguration: EncryptionConfiguration
    -- An array of user-defined tags that you would like to apply to the vector index that you are creating. A tag is a key-value pair that you apply to your resources. Tags can help you organize, track costs, and control access to resources. For more information, see Tagging for cost allocation or attribute-based access control (ABAC). You must have the s3vectors:TagResource permission in addition to s3vectors:CreateIndex permission to create a vector index with tags.
    tags: {string : string}
end

local record CreateIndexOutput
    -- The Amazon Resource Name (ARN) of the newly created vector index.
    indexArn: string
end

local record CreateVectorBucketInput
    -- The name of the vector bucket to create.
    vectorBucketName: string
    -- The encryption configuration for the vector bucket. By default, if you don't specify, all new vectors in Amazon S3 vector buckets use server-side encryption with Amazon S3 managed keys (SSE-S3), specifically AES256.
    encryptionConfiguration: EncryptionConfiguration
    -- An array of user-defined tags that you would like to apply to the vector bucket that you are creating. A tag is a key-value pair that you apply to your resources. Tags can help you organize and control access to resources. For more information, see Tagging for cost allocation or attribute-based access control (ABAC). You must have the s3vectors:TagResource permission in addition to s3vectors:CreateVectorBucket permission to create a vector bucket with tags.
    tags: {string : string}
end

local record CreateVectorBucketOutput
    -- The Amazon Resource Name (ARN) of the newly created vector bucket.
    vectorBucketArn: string
end

local record DeleteIndexInput
    -- The name of the vector bucket that contains the vector index.
    vectorBucketName: string
    -- The name of the vector index to delete.
    indexName: string
    -- The ARN of the vector index to delete.
    indexArn: string
end

local record DeleteIndexOutput
end

local record DeleteVectorBucketInput
    -- The name of the vector bucket to delete.
    vectorBucketName: string
    -- The ARN of the vector bucket to delete.
    vectorBucketArn: string
end

local record DeleteVectorBucketOutput
end

local record DeleteVectorBucketPolicyInput
    -- The name of the vector bucket to delete the policy from.
    vectorBucketName: string
    -- The ARN of the vector bucket to delete the policy from.
    vectorBucketArn: string
end

local record DeleteVectorBucketPolicyOutput
end

local record DeleteVectorsInput
    -- The name of the vector bucket that contains the vector index.
    vectorBucketName: string
    -- The name of the vector index that contains a vector you want to delete.
    indexName: string
    -- The ARN of the vector index that contains a vector you want to delete.
    indexArn: string
    -- The keys of the vectors to delete.
    keys: {string}
end

local record DeleteVectorsOutput
end

local record GetIndexInput
    -- The name of the vector bucket that contains the vector index.
    vectorBucketName: string
    -- The name of the vector index.
    indexName: string
    -- The ARN of the vector index.
    indexArn: string
end

local record GetIndexOutput
    -- The attributes of the vector index.
    index: Index
end

local record GetVectorBucketInput
    -- The name of the vector bucket to retrieve information about.
    vectorBucketName: string
    -- The ARN of the vector bucket to retrieve information about.
    vectorBucketArn: string
end

local record GetVectorBucketOutput
    -- The attributes of the vector bucket.
    vectorBucket: VectorBucket
end

local record GetVectorBucketPolicyInput
    -- The name of the vector bucket.
    vectorBucketName: string
    -- The ARN of the vector bucket.
    vectorBucketArn: string
end

local record GetVectorBucketPolicyOutput
    -- The JSON that defines the policy.
    policy: string
end

local record GetVectorsInput
    -- The name of the vector bucket that contains the vector index.
    vectorBucketName: string
    -- The name of the vector index.
    indexName: string
    -- The ARN of the vector index.
    indexArn: string
    -- The names of the vectors you want to return attributes for.
    keys: {string}
    -- Indicates whether to include the vector data in the response. The default value is false.
    returnData: boolean
    -- Indicates whether to include metadata in the response. The default value is false.
    returnMetadata: boolean
end

local record GetVectorsOutput
    -- The attributes of the vectors.
    vectors: {GetOutputVector}
end

local record ListIndexesInput
    -- The name of the vector bucket that contains the vector indexes.
    vectorBucketName: string
    -- The ARN of the vector bucket that contains the vector indexes.
    vectorBucketArn: string
    -- The maximum number of items to be returned in the response.
    maxResults: number
    -- The previous pagination token.
    nextToken: string
    -- Limits the response to vector indexes that begin with the specified prefix.
    prefix: string
end

local record ListIndexesOutput
    -- The next pagination token.
    nextToken: string
    -- The attributes of the vector indexes
    indexes: {IndexSummary}
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the Amazon S3 Vectors resource that you want to list tags for. The tagged resource can be a vector bucket or a vector index.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- The user-defined tags that are applied to the S3 Vectors resource. For more information, see Tagging for cost allocation or attribute-based access control (ABAC).
    tags: {string : string}
end

local record ListVectorBucketsInput
    -- The maximum number of vector buckets to be returned in the response.
    maxResults: number
    -- The previous pagination token.
    nextToken: string
    -- Limits the response to vector buckets that begin with the specified prefix.
    prefix: string
end

local record ListVectorBucketsOutput
    -- The element is included in the response when there are more buckets to be listed with pagination.
    nextToken: string
    -- The list of vector buckets owned by the requester.
    vectorBuckets: {VectorBucketSummary}
end

local record ListVectorsInput
    -- The name of the vector bucket.
    vectorBucketName: string
    -- The name of the vector index.
    indexName: string
    -- The Amazon resource Name (ARN) of the vector index.
    indexArn: string
    -- The maximum number of vectors to return on a page. If you don't specify maxResults, the ListVectors operation uses a default value of 500. If the processed dataset size exceeds 1 MB before reaching the maxResults value, the operation stops and returns the vectors that are retrieved up to that point, along with a nextToken that you can use in a subsequent request to retrieve the next set of results.
    maxResults: number
    -- Pagination token from a previous request. The value of this field is empty for an initial request.
    nextToken: string
    -- For a parallel ListVectors request, segmentCount represents the total number of vector segments into which the ListVectors operation will be divided. The value of segmentCount corresponds to the number of application workers that will perform the parallel ListVectors operation. For example, if you want to use four application threads to list vectors in a vector index, specify a segmentCount value of 4. If you specify a segmentCount value of 1, the ListVectors operation will be sequential rather than parallel. If you specify segmentCount, you must also specify segmentIndex.
    segmentCount: number
    -- For a parallel ListVectors request, segmentIndex is the index of the segment from which to list vectors in the current request. It identifies an individual segment to be listed by an application worker. Segment IDs are zero-based, so the first segment is always 0. For example, if you want to use four application threads to list vectors in a vector index, then the first thread specifies a segmentIndex value of 0, the second thread specifies 1, and so on. The value of segmentIndex must be less than the value provided for segmentCount. If you provide segmentIndex, you must also provide segmentCount.
    segmentIndex: number
    -- If true, the vector data of each vector will be included in the response. The default value is false.
    returnData: boolean
    -- If true, the metadata associated with each vector will be included in the response. The default value is false.
    returnMetadata: boolean
end

local record ListVectorsOutput
    -- Pagination token to be used in the subsequent request. The field is empty if no further pagination is required.
    nextToken: string
    -- Vectors in the current segment.
    vectors: {ListOutputVector}
end

local record PutVectorBucketPolicyInput
    -- The name of the vector bucket.
    vectorBucketName: string
    -- The Amazon Resource Name (ARN) of the vector bucket.
    vectorBucketArn: string
    -- The JSON that defines the policy. For more information about bucket policies for S3 Vectors, see Managing vector bucket policies in the Amazon S3 User Guide.
    policy: string
end

local record PutVectorBucketPolicyOutput
end

local record PutVectorsInput
    -- The name of the vector bucket that contains the vector index.
    vectorBucketName: string
    -- The name of the vector index where you want to write vectors.
    indexName: string
    -- The ARN of the vector index where you want to write vectors.
    indexArn: string
    -- The vectors to add to a vector index. The number of vectors in a single request must not exceed the resource capacity, otherwise the request will be rejected with the error ServiceUnavailableException with the error message "Currently unable to handle the request".
    vectors: {PutInputVector}
end

local record PutVectorsOutput
end

local record QueryVectorsInput
    -- The name of the vector bucket that contains the vector index.
    vectorBucketName: string
    -- The name of the vector index that you want to query.
    indexName: string
    -- The ARN of the vector index that you want to query.
    indexArn: string
    -- The number of results to return for each query.
    topK: number
    -- The query vector. Ensure that the query vector has the same dimension as the dimension of the vector index that's being queried. For example, if your vector index contains vectors with 384 dimensions, your query vector must also have 384 dimensions.
    queryVector: VectorData
    -- Metadata filter to apply during the query. For more information about metadata keys, see Metadata filtering in the Amazon S3 User Guide.
    filter: any
    -- Indicates whether to include metadata in the response. The default value is false.
    returnMetadata: boolean
    -- Indicates whether to include the computed distance in the response. The default value is false.
    returnDistance: boolean
end

local record QueryVectorsOutput
    -- The vectors in the approximate nearest neighbor search.
    vectors: {QueryOutputVector}
    -- The distance metric that was used for the similarity search calculation. This is the same distance metric that was configured for the vector index when it was created.
    distanceMetric: string
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the Amazon S3 Vectors resource that you're applying tags to. The tagged resource can be a vector bucket or a vector index.
    resourceArn: string
    -- The user-defined tag that you want to add to the specified S3 Vectors resource. For more information, see Tagging for cost allocation or attribute-based access control (ABAC).
    tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the Amazon S3 Vectors resource that you're removing tags from. The tagged resource can be a vector bucket or a vector index.
    resourceArn: string
    -- The array of tag keys that you're removing from the S3 Vectors resource. For more information, see Tagging for cost allocation or attribute-based access control (ABAC).
    tagKeys: {string}
end

local record UntagResourceOutput
end

-- The request failed because a vector bucket name or a vector index name already exists. Vector bucket names must be unique within your Amazon Web Services account for each Amazon Web Services Region. Vector index names must be unique within your vector bucket. Choose a different vector bucket name or vector index name, and try again.
local record ConflictException
    message: string
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- Your request exceeds a service quota.
local record ServiceQuotaExceededException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request failed because a vector bucket name or a vector index name already exists. Vector bucket names must be unique within your Amazon Web Services account for each Amazon Web Services Region. Vector index names must be unique within your vector bucket. Choose a different vector bucket name or vector index name, and try again.
local record ConflictException
    message: string
end

-- Your request exceeds a service quota.
local record ServiceQuotaExceededException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request failed because a vector bucket name or a vector index name already exists. Vector bucket names must be unique within your Amazon Web Services account for each Amazon Web Services Region. Vector index names must be unique within your vector bucket. Choose a different vector bucket name or vector index name, and try again.
local record ConflictException
    message: string
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The specified Amazon Web Services KMS key isn't enabled.
local record KmsDisabledException
    message: string
end

-- The request was rejected for one of the following reasons: The KeyUsage value of the KMS key is incompatible with the API operation. The encryption algorithm or signing algorithm specified for the operation is incompatible with the type of key material in the KMS key (KeySpec). For more information, see InvalidKeyUsageException in the Amazon Web Services Key Management Service API Reference.
local record KmsInvalidKeyUsageException
    message: string
end

-- The key state of the KMS key isn't compatible with the operation. For more information, see KMSInvalidStateException in the Amazon Web Services Key Management Service API Reference.
local record KmsInvalidStateException
    message: string
end

-- The KMS key can't be found.
local record KmsNotFoundException
    message: string
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The specified Amazon Web Services KMS key isn't enabled.
local record KmsDisabledException
    message: string
end

-- The request was rejected for one of the following reasons: The KeyUsage value of the KMS key is incompatible with the API operation. The encryption algorithm or signing algorithm specified for the operation is incompatible with the type of key material in the KMS key (KeySpec). For more information, see InvalidKeyUsageException in the Amazon Web Services Key Management Service API Reference.
local record KmsInvalidKeyUsageException
    message: string
end

-- The key state of the KMS key isn't compatible with the operation. For more information, see KMSInvalidStateException in the Amazon Web Services Key Management Service API Reference.
local record KmsInvalidStateException
    message: string
end

-- The KMS key can't be found.
local record KmsNotFoundException
    message: string
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The specified Amazon Web Services KMS key isn't enabled.
local record KmsDisabledException
    message: string
end

-- The request was rejected for one of the following reasons: The KeyUsage value of the KMS key is incompatible with the API operation. The encryption algorithm or signing algorithm specified for the operation is incompatible with the type of key material in the KMS key (KeySpec). For more information, see InvalidKeyUsageException in the Amazon Web Services Key Management Service API Reference.
local record KmsInvalidKeyUsageException
    message: string
end

-- The key state of the KMS key isn't compatible with the operation. For more information, see KMSInvalidStateException in the Amazon Web Services Key Management Service API Reference.
local record KmsInvalidStateException
    message: string
end

-- The KMS key can't be found.
local record KmsNotFoundException
    message: string
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- Your request exceeds a service quota.
local record ServiceQuotaExceededException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The specified Amazon Web Services KMS key isn't enabled.
local record KmsDisabledException
    message: string
end

-- The request was rejected for one of the following reasons: The KeyUsage value of the KMS key is incompatible with the API operation. The encryption algorithm or signing algorithm specified for the operation is incompatible with the type of key material in the KMS key (KeySpec). For more information, see InvalidKeyUsageException in the Amazon Web Services Key Management Service API Reference.
local record KmsInvalidKeyUsageException
    message: string
end

-- The key state of the KMS key isn't compatible with the operation. For more information, see KMSInvalidStateException in the Amazon Web Services Key Management Service API Reference.
local record KmsInvalidStateException
    message: string
end

-- The KMS key can't be found.
local record KmsNotFoundException
    message: string
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request failed because a vector bucket name or a vector index name already exists. Vector bucket names must be unique within your Amazon Web Services account for each Amazon Web Services Region. Vector index names must be unique within your vector bucket. Choose a different vector bucket name or vector index name, and try again.
local record ConflictException
    message: string
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

-- The request failed because a vector bucket name or a vector index name already exists. Vector bucket names must be unique within your Amazon Web Services account for each Amazon Web Services Region. Vector index names must be unique within your vector bucket. Choose a different vector bucket name or vector index name, and try again.
local record ConflictException
    message: string
end

-- The request was rejected because the specified resource can't be found.
local record NotFoundException
    message: string
end

-- The service is unavailable. Wait briefly and retry your request. If it continues to fail, increase your waiting time between retries.
local record ServiceUnavailableException
    message: string
end

-- Access denied.
local record AccessDeniedException
    message: string
end

-- The request failed due to an internal server error.
local record InternalServerException
    message: string
end

-- The request timed out. Retry your request.
local record RequestTimeoutException
    message: string
end

-- The request was denied due to request throttling.
local record TooManyRequestsException
    message: string
end

-- The requested action isn't valid.
local record ValidationException
    message: string
    -- A list of specific validation failures that are encountered during input processing. Each entry in the list contains a path to the field that failed validation and a detailed message that explains why the validation failed. To satisfy multiple constraints, a field can appear multiple times in this list if it failed. You can use the information to identify and fix the specific validation issues in your request.
    fieldList: {ValidationExceptionField}
end

local type DataType = "float32"

local type DistanceMetric = "euclidean" | "cosine"

local record M
    CreateIndexInput: CreateIndexInput
    CreateIndexOutput: CreateIndexOutput
    CreateVectorBucketInput: CreateVectorBucketInput
    CreateVectorBucketOutput: CreateVectorBucketOutput
    DeleteIndexInput: DeleteIndexInput
    DeleteIndexOutput: DeleteIndexOutput
    DeleteVectorBucketInput: DeleteVectorBucketInput
    DeleteVectorBucketOutput: DeleteVectorBucketOutput
    DeleteVectorBucketPolicyInput: DeleteVectorBucketPolicyInput
    DeleteVectorBucketPolicyOutput: DeleteVectorBucketPolicyOutput
    DeleteVectorsInput: DeleteVectorsInput
    DeleteVectorsOutput: DeleteVectorsOutput
    GetIndexInput: GetIndexInput
    GetIndexOutput: GetIndexOutput
    GetVectorBucketInput: GetVectorBucketInput
    GetVectorBucketOutput: GetVectorBucketOutput
    GetVectorBucketPolicyInput: GetVectorBucketPolicyInput
    GetVectorBucketPolicyOutput: GetVectorBucketPolicyOutput
    GetVectorsInput: GetVectorsInput
    GetVectorsOutput: GetVectorsOutput
    ListIndexesInput: ListIndexesInput
    ListIndexesOutput: ListIndexesOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    ListVectorBucketsInput: ListVectorBucketsInput
    ListVectorBucketsOutput: ListVectorBucketsOutput
    ListVectorsInput: ListVectorsInput
    ListVectorsOutput: ListVectorsOutput
    PutVectorBucketPolicyInput: PutVectorBucketPolicyInput
    PutVectorBucketPolicyOutput: PutVectorBucketPolicyOutput
    PutVectorsInput: PutVectorsInput
    PutVectorsOutput: PutVectorsOutput
    QueryVectorsInput: QueryVectorsInput
    QueryVectorsOutput: QueryVectorsOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
end

return M
