-- Code generated by lua-client-codegen. DO NOT EDIT.

local record AcceptNetworkFirewallTransitGatewayAttachmentInput
    -- Required. The unique identifier of the transit gateway attachment to accept. This ID is returned in the response when creating a transit gateway-attached firewall.
    TransitGatewayAttachmentId: string
end

local record AcceptNetworkFirewallTransitGatewayAttachmentOutput
    -- The unique identifier of the transit gateway attachment that was accepted.
    TransitGatewayAttachmentId: string
    -- The current status of the transit gateway attachment. Valid values are:
    -- CREATING - The attachment is being created
    -- DELETING - The attachment is being deleted
    -- DELETED - The attachment has been deleted
    -- FAILED - The attachment creation has failed and cannot be recovered
    -- ERROR - The attachment is in an error state that might be recoverable
    -- READY - The attachment is active and processing traffic
    -- PENDING_ACCEPTANCE - The attachment is waiting to be accepted
    -- REJECTING - The attachment is in the process of being rejected
    -- REJECTED - The attachment has been rejected
    TransitGatewayAttachmentStatus: string
end

local record AssociateAvailabilityZonesInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- Required. The Availability Zones where you want to create firewall endpoints. You must specify at least one Availability Zone.
    AvailabilityZoneMappings: {AvailabilityZoneMapping}
end

local record AssociateAvailabilityZonesOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- The Availability Zones where Network Firewall created firewall endpoints. Each mapping specifies an Availability Zone where the firewall processes traffic.
    AvailabilityZoneMappings: {AvailabilityZoneMapping}
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record AssociateFirewallPolicyInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- The Amazon Resource Name (ARN) of the firewall policy.
    FirewallPolicyArn: string
end

local record AssociateFirewallPolicyOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- The Amazon Resource Name (ARN) of the firewall policy.
    FirewallPolicyArn: string
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record AssociateSubnetsInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- The IDs of the subnets that you want to associate with the firewall.
    SubnetMappings: {SubnetMapping}
end

local record AssociateSubnetsOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- The IDs of the subnets that are associated with the firewall.
    SubnetMappings: {SubnetMapping}
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record AttachRuleGroupsToProxyConfigurationInput
    -- The descriptive name of the proxy configuration. You can't change the name of a proxy configuration after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationName: string
    -- The Amazon Resource Name (ARN) of a proxy configuration.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationArn: string
    -- The proxy rule group(s) to attach to the proxy configuration
    RuleGroups: {ProxyRuleGroupAttachment}
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy configuration. The token marks the state of the proxy configuration resource at the time of the request.
    -- To make changes to the proxy configuration, you provide the token in your request. Network Firewall uses the token to ensure that the proxy configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record AttachRuleGroupsToProxyConfigurationOutput
    -- The updated proxy configuration resource that reflects the updates from the request.
    ProxyConfiguration: ProxyConfiguration
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy configuration. The token marks the state of the proxy configuration resource at the time of the request.
    -- To make changes to the proxy configuration, you provide the token in your request. Network Firewall uses the token to ensure that the proxy configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record CreateFirewallInput
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- The Amazon Resource Name (ARN) of the FirewallPolicy that you want to
    -- use for the firewall.
    FirewallPolicyArn: string
    -- The unique identifier of the VPC where Network Firewall should create the firewall.
    -- You can't change this setting after you create the firewall.
    VpcId: string
    -- The public subnets to use for your Network Firewall firewalls. Each subnet must belong to a
    -- different Availability Zone in the VPC. Network Firewall creates a firewall endpoint in each
    -- subnet.
    SubnetMappings: {SubnetMapping}
    -- A flag indicating whether it is possible to delete the firewall. A setting of TRUE indicates
    -- that the firewall is protected against deletion. Use this setting to protect against
    -- accidentally deleting a firewall that is in use. When you create a firewall, the operation initializes this flag to TRUE.
    DeleteProtection: boolean
    -- A setting indicating whether the firewall is protected against changes to the subnet associations.
    -- Use this setting to protect against
    -- accidentally modifying the subnet associations for a firewall that is in use. When you create a firewall, the operation initializes this setting to TRUE.
    SubnetChangeProtection: boolean
    -- A setting indicating whether the firewall is protected against a change to the firewall policy association.
    -- Use this setting to protect against
    -- accidentally modifying the firewall policy for a firewall that is in use. When you create a firewall, the operation initializes this setting to TRUE.
    FirewallPolicyChangeProtection: boolean
    -- A description of the firewall.
    Description: string
    -- The key:value pairs to associate with the resource.
    Tags: {Tag}
    -- A complex type that contains settings for encryption of your firewall resources.
    EncryptionConfiguration: EncryptionConfiguration
    -- An optional setting indicating the specific traffic analysis types to enable on the firewall.
    EnabledAnalysisTypes: {string}
    -- Required when creating a transit gateway-attached firewall. The unique identifier of the transit gateway to attach to this firewall. You can provide either a transit gateway from your account or one that has been shared with you through Resource Access Manager.
    -- After creating the firewall, you cannot change the transit gateway association. To use a different transit gateway, you must create a new firewall.
    -- For information about creating firewalls, see CreateFirewall. For specific guidance about transit gateway-attached firewalls, see Considerations for transit gateway-attached firewalls in the Network Firewall Developer Guide.
    TransitGatewayId: string
    -- Required. The Availability Zones where you want to create firewall endpoints for a transit gateway-attached firewall. You must specify at least one Availability Zone. Consider enabling the firewall in every Availability Zone where you have workloads to maintain Availability Zone isolation.
    -- You can modify Availability Zones later using AssociateAvailabilityZones or DisassociateAvailabilityZones, but this may briefly disrupt traffic. The AvailabilityZoneChangeProtection setting controls whether you can make these modifications.
    AvailabilityZoneMappings: {AvailabilityZoneMapping}
    -- Optional. A setting indicating whether the firewall is protected against changes to its Availability Zone configuration. When set to TRUE, you cannot add or remove Availability Zones without first disabling this protection using UpdateAvailabilityZoneChangeProtection.
    -- Default value: FALSE
    AvailabilityZoneChangeProtection: boolean
end

local record CreateFirewallOutput
    -- The configuration settings for the firewall. These settings include the firewall policy and the subnets in your VPC to use for the firewall endpoints.
    Firewall: Firewall
    -- Detailed information about the current status of a Firewall. You can retrieve this for a firewall by calling DescribeFirewall and providing the firewall name and ARN.
    -- The firewall status indicates a combined status. It indicates whether all subnets are up-to-date with the latest firewall configurations, which is based on the sync states config values, and also whether all subnets have their endpoints fully enabled, based on their sync states attachment values.
    FirewallStatus: FirewallStatus
end

local record CreateFirewallPolicyInput
    -- The descriptive name of the firewall policy. You can't change the name of a firewall policy after you create it.
    FirewallPolicyName: string
    -- The rule groups and policy actions to use in the firewall policy.
    FirewallPolicy: FirewallPolicy
    -- A description of the firewall policy.
    Description: string
    -- The key:value pairs to associate with the resource.
    Tags: {Tag}
    -- Indicates whether you want Network Firewall to just check the validity of the request, rather than run the request.
    -- If set to TRUE, Network Firewall checks whether the request can run successfully,
    -- but doesn't actually make the requested changes. The call returns the value that the request would return if you ran it with
    -- dry run set to FALSE, but doesn't make additions or changes to your resources. This option allows you to make sure that you have
    -- the required permissions to run the request and that your request parameters are valid.
    -- If set to FALSE, Network Firewall makes the requested changes to your resources.
    DryRun: boolean
    -- A complex type that contains settings for encryption of your firewall policy resources.
    EncryptionConfiguration: EncryptionConfiguration
end

local record CreateFirewallPolicyOutput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the firewall policy. The token marks the state of the policy resource at the time of the request.
    -- To make changes to the policy, you provide the token in your request. Network Firewall uses the token to ensure that the policy hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall policy again to get a current copy of it with current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The high-level properties of a firewall policy. This, along with the FirewallPolicy, define the policy. You can retrieve all objects for a firewall policy by calling DescribeFirewallPolicy.
    FirewallPolicyResponse: FirewallPolicyResponse
end

local record CreateProxyInput
    -- The descriptive name of the proxy. You can't change the name of a proxy after you create it.
    ProxyName: string
    -- A unique identifier for the NAT gateway to use with proxy resources.
    NatGatewayId: string
    -- The descriptive name of the proxy configuration. You can't change the name of a proxy configuration after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationName: string
    -- The Amazon Resource Name (ARN) of a proxy configuration.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationArn: string
    -- Listener properties for HTTP and HTTPS traffic.
    ListenerProperties: {ListenerPropertyRequest}
    -- TLS decryption on traffic to filter on attributes in the HTTP header.
    TlsInterceptProperties: TlsInterceptPropertiesRequest
    -- The key:value pairs to associate with the resource.
    Tags: {Tag}
end

local record CreateProxyOutput
    -- Proxy attached to a NAT gateway.
    Proxy: Proxy
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy. The token marks the state of the proxy resource at the time of the request.
    -- To make changes to the proxy, you provide the token in your request. Network Firewall uses the token to ensure that the proxy hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record CreateProxyConfigurationInput
    -- The descriptive name of the proxy configuration. You can't change the name of a proxy configuration after you create it.
    ProxyConfigurationName: string
    -- A description of the proxy configuration.
    Description: string
    -- The proxy rule group name(s) to attach to the proxy configuration.
    -- You must specify the ARNs or the names, and you can specify both.
    RuleGroupNames: {string}
    -- The proxy rule group arn(s) to attach to the proxy configuration.
    -- You must specify the ARNs or the names, and you can specify both.
    RuleGroupArns: {string}
    -- Evaluation points in the traffic flow where rules are applied. There are three phases in a traffic where the rule match is applied.
    DefaultRulePhaseActions: ProxyConfigDefaultRulePhaseActionsRequest
    -- The key:value pairs to associate with the resource.
    Tags: {Tag}
end

local record CreateProxyConfigurationOutput
    -- The properties that define the proxy configuration.
    ProxyConfiguration: ProxyConfiguration
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy configuration. The token marks the state of the proxy configuration resource at the time of the request.
    -- To make changes to the proxy configuration, you provide the token in your request. Network Firewall uses the token to ensure that the proxy configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record CreateProxyRuleGroupInput
    -- The descriptive name of the proxy rule group. You can't change the name of a proxy rule group after you create it.
    ProxyRuleGroupName: string
    -- A description of the proxy rule group.
    Description: string
    -- Individual rules that define match conditions and actions for application-layer traffic. Rules specify what to inspect (domains, headers, methods) and what action to take (allow, deny, alert).
    Rules: ProxyRulesByRequestPhase
    -- The key:value pairs to associate with the resource.
    Tags: {Tag}
end

local record CreateProxyRuleGroupOutput
    -- The properties that define the proxy rule group.
    ProxyRuleGroup: ProxyRuleGroup
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy rule group. The token marks the state of the proxy rule group resource at the time of the request.
    -- To make changes to the proxy rule group, you provide the token in your request. Network Firewall uses the token to ensure that the proxy rule group hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy rule group again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record CreateProxyRulesInput
    -- The Amazon Resource Name (ARN) of a proxy rule group.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupArn: string
    -- The descriptive name of the proxy rule group. You can't change the name of a proxy rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupName: string
    -- Individual rules that define match conditions and actions for application-layer traffic. Rules specify what to inspect (domains, headers, methods) and what action to take (allow, deny, alert).
    Rules: CreateProxyRulesByRequestPhase
end

local record CreateProxyRulesOutput
    -- The properties that define the proxy rule group with the newly created proxy rule(s).
    ProxyRuleGroup: ProxyRuleGroup
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy rule. The token marks the state of the proxy rule resource at the time of the request.
    -- To make changes to the proxy rule, you provide the token in your request. Network Firewall uses the token to ensure that the proxy rule hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy rule again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record CreateRuleGroupInput
    -- The descriptive name of the rule group. You can't change the name of a rule group after you create it.
    RuleGroupName: string
    -- An object that defines the rule group rules.
    -- You must provide either this rule group setting or a Rules setting, but not both.
    RuleGroup: RuleGroup
    -- A string containing stateful rule group rules specifications in Suricata flat format, with one rule
    -- per line. Use this to import your existing Suricata compatible rule groups.
    -- You must provide either this rules setting or a populated RuleGroup setting, but not both.
    -- You can provide your rule group specification in Suricata flat format through this setting when you create or update your rule group. The call
    -- response returns a RuleGroup object that Network Firewall has populated from your string.
    Rules: string
    -- Indicates whether the rule group is stateless or stateful. If the rule group is stateless, it contains
    -- stateless rules. If it is stateful, it contains stateful rules.
    Type: string
    -- A description of the rule group.
    Description: string
    -- The maximum operating resources that this rule group can use. Rule group capacity is fixed at creation.
    -- When you update a rule group, you are limited to this capacity. When you reference a rule group
    -- from a firewall policy, Network Firewall reserves this capacity for the rule group.
    -- You can retrieve the capacity that would be required for a rule group before you create the rule group by calling
    -- CreateRuleGroup with DryRun set to TRUE.
    -- You can't change or exceed this capacity when you update the rule group, so leave
    -- room for your rule group to grow.
    -- Capacity for a stateless rule group
    -- For a stateless rule group, the capacity required is the sum of the capacity
    -- requirements of the individual rules that you expect to have in the rule group.
    -- To calculate the capacity requirement of a single rule, multiply the capacity
    -- requirement values of each of the rule's match settings:
    -- A match setting with no criteria specified has a value of 1.
    -- A match setting with Any specified has a value of 1.
    -- All other match settings have a value equal to the number of elements provided in
    -- the setting. For example, a protocol setting ["UDP"] and a source setting
    -- ["10.0.0.0/24"] each have a value of 1. A protocol setting ["UDP","TCP"] has a value
    -- of 2. A source setting ["10.0.0.0/24","10.0.0.1/24","10.0.0.2/24"] has a value of 3.
    -- A rule with no criteria specified in any of its match settings has a capacity
    -- requirement of 1. A rule with protocol setting ["UDP","TCP"], source setting
    -- ["10.0.0.0/24","10.0.0.1/24","10.0.0.2/24"], and a single specification or no specification
    -- for each of the other match settings has a capacity requirement of 6.
    -- Capacity for a stateful rule group
    -- For a stateful rule group, the minimum capacity required is the number of individual rules that
    -- you expect to have in the rule group.
    Capacity: number
    -- The key:value pairs to associate with the resource.
    Tags: {Tag}
    -- Indicates whether you want Network Firewall to just check the validity of the request, rather than run the request.
    -- If set to TRUE, Network Firewall checks whether the request can run successfully,
    -- but doesn't actually make the requested changes. The call returns the value that the request would return if you ran it with
    -- dry run set to FALSE, but doesn't make additions or changes to your resources. This option allows you to make sure that you have
    -- the required permissions to run the request and that your request parameters are valid.
    -- If set to FALSE, Network Firewall makes the requested changes to your resources.
    DryRun: boolean
    -- A complex type that contains settings for encryption of your rule group resources.
    EncryptionConfiguration: EncryptionConfiguration
    -- A complex type that contains metadata about the rule group that your own rule group is copied from. You can use the metadata to keep track of updates made to the originating rule group.
    SourceMetadata: SourceMetadata
    -- Indicates whether you want Network Firewall to analyze the stateless rules in the rule group for rule behavior such as asymmetric routing. If set to TRUE, Network Firewall runs the analysis and then creates the rule group for you. To run the stateless rule group analyzer without creating the rule group, set DryRun to TRUE.
    AnalyzeRuleGroup: boolean
    -- An object that contains a RuleOptions array of strings.
    -- You use RuleOptions to determine which of the following RuleSummary values are returned in response to DescribeRuleGroupSummary.
    -- Metadata - returns
    -- Msg
    -- SID
    SummaryConfiguration: SummaryConfiguration
end

local record CreateRuleGroupOutput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the rule group. The token marks the state of the rule group resource at the time of the request.
    -- To make changes to the rule group, you provide the token in your request. Network Firewall uses the token to ensure that the rule group hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the rule group again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The high-level properties of a rule group. This, along with the RuleGroup, define the rule group. You can retrieve all objects for a rule group by calling DescribeRuleGroup.
    RuleGroupResponse: RuleGroupResponse
end

local record CreateTLSInspectionConfigurationInput
    -- The descriptive name of the TLS inspection configuration. You can't change the name of a TLS inspection configuration after you create it.
    TLSInspectionConfigurationName: string
    -- The object that defines a TLS inspection configuration. This, along with TLSInspectionConfigurationResponse, define the TLS inspection configuration. You can retrieve all objects for a TLS inspection configuration by calling DescribeTLSInspectionConfiguration.
    -- Network Firewall uses a TLS inspection configuration to decrypt traffic. Network Firewall re-encrypts the traffic before sending it to its destination.
    -- To use a TLS inspection configuration, you add it to a new Network Firewall firewall policy, then you apply the firewall policy to a firewall. Network Firewall acts as a proxy service to decrypt and inspect the traffic traveling through your firewalls. You can reference a TLS inspection configuration from more than one firewall policy, and you can use a firewall policy in more than one firewall. For more information about using TLS inspection configurations, see
    -- Inspecting SSL/TLS traffic with TLS
    -- inspection configurations in the Network Firewall Developer Guide.
    TLSInspectionConfiguration: TLSInspectionConfiguration
    -- A description of the TLS inspection configuration.
    Description: string
    -- The key:value pairs to associate with the resource.
    Tags: {Tag}
    EncryptionConfiguration: EncryptionConfiguration
end

local record CreateTLSInspectionConfigurationOutput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the TLS inspection configuration. The token marks the state of the TLS inspection configuration resource at the time of the request.
    -- To make changes to the TLS inspection configuration, you provide the token in your request. Network Firewall uses the token to ensure that the TLS inspection configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the TLS inspection configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The high-level properties of a TLS inspection configuration. This, along with the TLSInspectionConfiguration, define the TLS inspection configuration. You can retrieve all objects for a TLS inspection configuration by calling DescribeTLSInspectionConfiguration.
    TLSInspectionConfigurationResponse: TLSInspectionConfigurationResponse
end

local record CreateVpcEndpointAssociationInput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The unique identifier of the VPC where you want to create a firewall endpoint.
    VpcId: string
    SubnetMapping: SubnetMapping
    -- A description of the VPC endpoint association.
    Description: string
    -- The key:value pairs to associate with the resource.
    Tags: {Tag}
end

local record CreateVpcEndpointAssociationOutput
    -- The configuration settings for the VPC endpoint association. These settings include the firewall and the VPC and subnet to use for the firewall endpoint.
    VpcEndpointAssociation: VpcEndpointAssociation
    -- Detailed information about the current status of a VpcEndpointAssociation. You can retrieve this
    -- by calling DescribeVpcEndpointAssociation and providing the VPC endpoint association ARN.
    VpcEndpointAssociationStatus: VpcEndpointAssociationStatus
end

local record DeleteFirewallInput
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
end

local record DeleteFirewallOutput
    Firewall: Firewall
    FirewallStatus: FirewallStatus
end

local record DeleteFirewallPolicyInput
    -- The descriptive name of the firewall policy. You can't change the name of a firewall policy after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallPolicyName: string
    -- The Amazon Resource Name (ARN) of the firewall policy.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallPolicyArn: string
end

local record DeleteFirewallPolicyOutput
    -- The object containing the definition of the FirewallPolicyResponse
    -- that you asked to delete.
    FirewallPolicyResponse: FirewallPolicyResponse
end

local record DeleteNetworkFirewallTransitGatewayAttachmentInput
    -- Required. The unique identifier of the transit gateway attachment to delete.
    TransitGatewayAttachmentId: string
end

local record DeleteNetworkFirewallTransitGatewayAttachmentOutput
    -- The ID of the transit gateway attachment that was deleted.
    TransitGatewayAttachmentId: string
    -- The current status of the transit gateway attachment deletion process.
    -- Valid values are:
    -- CREATING - The attachment is being created
    -- DELETING - The attachment is being deleted
    -- DELETED - The attachment has been deleted
    -- FAILED - The attachment creation has failed and cannot be recovered
    -- ERROR - The attachment is in an error state that might be recoverable
    -- READY - The attachment is active and processing traffic
    -- PENDING_ACCEPTANCE - The attachment is waiting to be accepted
    -- REJECTING - The attachment is in the process of being rejected
    -- REJECTED - The attachment has been rejected
    TransitGatewayAttachmentStatus: string
end

local record DeleteProxyInput
    -- The NAT Gateway the proxy is attached to.
    NatGatewayId: string
    -- The descriptive name of the proxy. You can't change the name of a proxy after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyName: string
    -- The Amazon Resource Name (ARN) of a proxy.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyArn: string
end

local record DeleteProxyOutput
    -- The NAT Gateway the Proxy was attached to.
    NatGatewayId: string
    -- The descriptive name of the proxy. You can't change the name of a proxy after you create it.
    ProxyName: string
    -- The Amazon Resource Name (ARN) of a proxy.
    ProxyArn: string
end

local record DeleteProxyConfigurationInput
    -- The descriptive name of the proxy configuration. You can't change the name of a proxy configuration after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationName: string
    -- The Amazon Resource Name (ARN) of a proxy configuration.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationArn: string
end

local record DeleteProxyConfigurationOutput
    -- The descriptive name of the proxy configuration. You can't change the name of a proxy configuration after you create it.
    ProxyConfigurationName: string
    -- The Amazon Resource Name (ARN) of a proxy configuration.
    ProxyConfigurationArn: string
end

local record DeleteProxyRuleGroupInput
    -- The descriptive name of the proxy rule group. You can't change the name of a proxy rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupName: string
    -- The Amazon Resource Name (ARN) of a proxy rule group.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupArn: string
end

local record DeleteProxyRuleGroupOutput
    -- The descriptive name of the proxy rule group. You can't change the name of a proxy rule group after you create it.
    ProxyRuleGroupName: string
    -- The Amazon Resource Name (ARN) of a proxy rule group.
    ProxyRuleGroupArn: string
end

local record DeleteProxyRulesInput
    -- The Amazon Resource Name (ARN) of a proxy rule group.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupArn: string
    -- The descriptive name of the proxy rule group. You can't change the name of a proxy rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupName: string
    -- The proxy rule(s) to remove from the existing proxy rule group.
    Rules: {string}
end

local record DeleteProxyRulesOutput
    -- The properties that define the proxy rule group with the newly created proxy rule(s).
    ProxyRuleGroup: ProxyRuleGroup
end

local record DeleteResourcePolicyInput
    -- The Amazon Resource Name (ARN) of the rule group or firewall policy whose resource policy you want to delete.
    ResourceArn: string
end

local record DeleteResourcePolicyOutput
end

local record DeleteRuleGroupInput
    -- The descriptive name of the rule group. You can't change the name of a rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupName: string
    -- The Amazon Resource Name (ARN) of the rule group.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupArn: string
    -- Indicates whether the rule group is stateless or stateful. If the rule group is stateless, it contains
    -- stateless rules. If it is stateful, it contains stateful rules.
    -- This setting is required for requests that do not include the RuleGroupARN.
    Type: string
end

local record DeleteRuleGroupOutput
    -- The high-level properties of a rule group. This, along with the RuleGroup, define the rule group. You can retrieve all objects for a rule group by calling DescribeRuleGroup.
    RuleGroupResponse: RuleGroupResponse
end

local record DeleteTLSInspectionConfigurationInput
    -- The Amazon Resource Name (ARN) of the TLS inspection configuration.
    -- You must specify the ARN or the name, and you can specify both.
    TLSInspectionConfigurationArn: string
    -- The descriptive name of the TLS inspection configuration. You can't change the name of a TLS inspection configuration after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    TLSInspectionConfigurationName: string
end

local record DeleteTLSInspectionConfigurationOutput
    -- The high-level properties of a TLS inspection configuration. This, along with the TLSInspectionConfiguration, define the TLS inspection configuration. You can retrieve all objects for a TLS inspection configuration by calling DescribeTLSInspectionConfiguration.
    TLSInspectionConfigurationResponse: TLSInspectionConfigurationResponse
end

local record DeleteVpcEndpointAssociationInput
    -- The Amazon Resource Name (ARN) of a VPC endpoint association.
    VpcEndpointAssociationArn: string
end

local record DeleteVpcEndpointAssociationOutput
    -- The configuration settings for the VPC endpoint association. These settings include the firewall and the VPC and subnet to use for the firewall endpoint.
    VpcEndpointAssociation: VpcEndpointAssociation
    -- Detailed information about the current status of a VpcEndpointAssociation. You can retrieve this
    -- by calling DescribeVpcEndpointAssociation and providing the VPC endpoint association ARN.
    VpcEndpointAssociationStatus: VpcEndpointAssociationStatus
end

local record DescribeFirewallInput
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
end

local record DescribeFirewallOutput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The configuration settings for the firewall. These settings include the firewall policy and the subnets in your VPC to use for the firewall endpoints.
    Firewall: Firewall
    -- Detailed information about the current status of a Firewall. You can retrieve this for a firewall by calling DescribeFirewall and providing the firewall name and ARN.
    -- The firewall status indicates a combined status. It indicates whether all subnets are up-to-date with the latest firewall configurations, which is based on the sync states config values, and also whether all subnets have their endpoints fully enabled, based on their sync states attachment values.
    FirewallStatus: FirewallStatus
end

local record DescribeFirewallMetadataInput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
end

local record DescribeFirewallMetadataOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The Amazon Resource Name (ARN) of the firewall policy.
    FirewallPolicyArn: string
    -- A description of the firewall.
    Description: string
    -- The readiness of the configured firewall to handle network traffic across all of the
    -- Availability Zones where you have it configured. This setting is READY only when
    -- the ConfigurationSyncStateSummary value is IN_SYNC and the
    -- Attachment
    -- Status values for all of the configured subnets are READY.
    Status: string
    -- The Availability Zones that the firewall currently supports. This includes all Availability Zones for which
    -- the firewall has a subnet defined.
    SupportedAvailabilityZones: {string : AvailabilityZoneMetadata}
    -- The unique identifier of the transit gateway attachment associated with this firewall. This field is only present for transit gateway-attached firewalls.
    TransitGatewayAttachmentId: string
end

local record DescribeFirewallPolicyInput
    -- The descriptive name of the firewall policy. You can't change the name of a firewall policy after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallPolicyName: string
    -- The Amazon Resource Name (ARN) of the firewall policy.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallPolicyArn: string
end

local record DescribeFirewallPolicyOutput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the firewall policy. The token marks the state of the policy resource at the time of the request.
    -- To make changes to the policy, you provide the token in your request. Network Firewall uses the token to ensure that the policy hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall policy again to get a current copy of it with current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The high-level properties of a firewall policy. This, along with the FirewallPolicy, define the policy. You can retrieve all objects for a firewall policy by calling DescribeFirewallPolicy.
    FirewallPolicyResponse: FirewallPolicyResponse
    -- The policy for the specified firewall policy.
    FirewallPolicy: FirewallPolicy
end

local record DescribeFlowOperationInput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The ID of the Availability Zone where the firewall is located. For example, us-east-2a.
    -- Defines the scope a flow operation. You can use up to 20 filters to configure a single flow operation.
    AvailabilityZone: string
    -- The Amazon Resource Name (ARN) of a VPC endpoint association.
    VpcEndpointAssociationArn: string
    -- A unique identifier for the primary endpoint associated with a firewall.
    VpcEndpointId: string
    -- A unique identifier for the flow operation. This ID is returned in the responses to start and list commands. You provide to describe commands.
    FlowOperationId: string
end

local record DescribeFlowOperationOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The ID of the Availability Zone where the firewall is located. For example, us-east-2a.
    -- Defines the scope a flow operation. You can use up to 20 filters to configure a single flow operation.
    AvailabilityZone: string
    -- The Amazon Resource Name (ARN) of a VPC endpoint association.
    VpcEndpointAssociationArn: string
    -- A unique identifier for the primary endpoint associated with a firewall.
    VpcEndpointId: string
    -- A unique identifier for the flow operation. This ID is returned in the responses to start and list commands. You provide to describe commands.
    FlowOperationId: string
    -- Defines the type of FlowOperation.
    FlowOperationType: string
    -- Returns the status of the flow operation. This string is returned in the responses to start, list, and describe commands.
    -- If the status is COMPLETED_WITH_ERRORS, results may be returned with any number of Flows missing from the response.
    -- If the status is FAILED, Flows returned will be empty.
    FlowOperationStatus: string
    -- If the asynchronous operation fails, Network Firewall populates this with the reason for the error or failure. Options include Flow operation error and Flow timeout.
    StatusMessage: string
    -- A timestamp indicating when the Suricata engine identified flows impacted by an operation.
    FlowRequestTimestamp: number
    -- Returns key information about a flow operation, such as related statuses, unique identifiers, and all filters defined in the operation.
    FlowOperation: FlowOperation
end

local record DescribeLoggingConfigurationInput
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
end

local record DescribeLoggingConfigurationOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    LoggingConfiguration: LoggingConfiguration
    -- A boolean that reflects whether or not the firewall monitoring dashboard is enabled on a firewall.
    -- Returns TRUE when the firewall monitoring dashboard is enabled on the firewall.
    -- Returns FALSE when the firewall monitoring dashboard is not enabled on the firewall.
    EnableMonitoringDashboard: boolean
end

local record DescribeProxyInput
    -- The descriptive name of the proxy. You can't change the name of a proxy after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyName: string
    -- The Amazon Resource Name (ARN) of a proxy.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyArn: string
end

local record DescribeProxyOutput
    -- Proxy attached to a NAT gateway.
    Proxy: DescribeProxyResource
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy. The token marks the state of the proxy resource at the time of the request.
    -- To make changes to the proxy, you provide the token in your request. Network Firewall uses the token to ensure that the proxy hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record DescribeProxyConfigurationInput
    -- The descriptive name of the proxy configuration. You can't change the name of a proxy configuration after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationName: string
    -- The Amazon Resource Name (ARN) of a proxy configuration.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationArn: string
end

local record DescribeProxyConfigurationOutput
    -- The configuration for the specified proxy configuration.
    ProxyConfiguration: ProxyConfiguration
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy configuration. The token marks the state of the proxy configuration resource at the time of the request.
    -- To make changes to the proxy configuration, you provide the token in your request. Network Firewall uses the token to ensure that the proxy configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record DescribeProxyRuleInput
    -- The descriptive name of the proxy rule. You can't change the name of a proxy rule after you create it.
    ProxyRuleName: string
    -- The descriptive name of the proxy rule group. You can't change the name of a proxy rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupName: string
    -- The Amazon Resource Name (ARN) of a proxy rule group.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupArn: string
end

local record DescribeProxyRuleOutput
    -- The configuration for the specified proxy rule.
    ProxyRule: ProxyRule
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy rule. The token marks the state of the proxy rule resource at the time of the request.
    -- To make changes to the proxy rule, you provide the token in your request. Network Firewall uses the token to ensure that the proxy rule hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy rule again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record DescribeProxyRuleGroupInput
    -- The descriptive name of the proxy rule group. You can't change the name of a proxy rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupName: string
    -- The Amazon Resource Name (ARN) of a proxy rule group.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupArn: string
end

local record DescribeProxyRuleGroupOutput
    -- The configuration for the specified proxy rule group.
    ProxyRuleGroup: ProxyRuleGroup
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy rule group. The token marks the state of the proxy rule group resource at the time of the request.
    -- To make changes to the proxy rule group, you provide the token in your request. Network Firewall uses the token to ensure that the proxy rule group hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy rule group again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record DescribeResourcePolicyInput
    -- The Amazon Resource Name (ARN) of the rule group or firewall policy whose resource policy you want to retrieve.
    ResourceArn: string
end

local record DescribeResourcePolicyOutput
    -- The IAM policy for the resource.
    Policy: string
end

local record DescribeRuleGroupInput
    -- The descriptive name of the rule group. You can't change the name of a rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupName: string
    -- The Amazon Resource Name (ARN) of the rule group.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupArn: string
    -- Indicates whether the rule group is stateless or stateful. If the rule group is stateless, it contains
    -- stateless rules. If it is stateful, it contains stateful rules.
    -- This setting is required for requests that do not include the RuleGroupARN.
    Type: string
    -- Indicates whether you want Network Firewall to analyze the stateless rules in the rule group for rule behavior such as asymmetric routing. If set to TRUE, Network Firewall runs the analysis.
    AnalyzeRuleGroup: boolean
end

local record DescribeRuleGroupOutput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the rule group. The token marks the state of the rule group resource at the time of the request.
    -- To make changes to the rule group, you provide the token in your request. Network Firewall uses the token to ensure that the rule group hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the rule group again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The object that defines the rules in a rule group. This, along with RuleGroupResponse, define the rule group. You can retrieve all objects for a rule group by calling DescribeRuleGroup.
    -- Network Firewall uses a rule group to inspect and control network traffic.
    -- You define stateless rule groups to inspect individual packets and you define stateful rule groups to inspect packets in the context of their
    -- traffic flow.
    -- To use a rule group, you include it by reference in an Network Firewall firewall policy, then you use the policy in a firewall. You can reference a rule group from
    -- more than one firewall policy, and you can use a firewall policy in more than one firewall.
    RuleGroup: RuleGroup
    -- The high-level properties of a rule group. This, along with the RuleGroup, define the rule group. You can retrieve all objects for a rule group by calling DescribeRuleGroup.
    RuleGroupResponse: RuleGroupResponse
end

local record DescribeRuleGroupMetadataInput
    -- The descriptive name of the rule group. You can't change the name of a rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupName: string
    -- The descriptive name of the rule group. You can't change the name of a rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupArn: string
    -- Indicates whether the rule group is stateless or stateful. If the rule group is stateless, it contains
    -- stateless rules. If it is stateful, it contains stateful rules.
    -- This setting is required for requests that do not include the RuleGroupARN.
    Type: string
end

local record DescribeRuleGroupMetadataOutput
    -- The descriptive name of the rule group. You can't change the name of a rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupArn: string
    -- The descriptive name of the rule group. You can't change the name of a rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupName: string
    -- Returns the metadata objects for the specified rule group.
    Description: string
    -- Indicates whether the rule group is stateless or stateful. If the rule group is stateless, it contains
    -- stateless rules. If it is stateful, it contains stateful rules.
    -- This setting is required for requests that do not include the RuleGroupARN.
    Type: string
    -- The maximum operating resources that this rule group can use. Rule group capacity is fixed at creation.
    -- When you update a rule group, you are limited to this capacity. When you reference a rule group
    -- from a firewall policy, Network Firewall reserves this capacity for the rule group.
    -- You can retrieve the capacity that would be required for a rule group before you create the rule group by calling
    -- CreateRuleGroup with DryRun set to TRUE.
    Capacity: number
    StatefulRuleOptions: StatefulRuleOptions
    -- A timestamp indicating when the rule group was last modified.
    LastModifiedTime: number
    -- The name of the Amazon Web Services Marketplace vendor that provides this rule group.
    VendorName: string
    -- The unique identifier for the product listing associated with this rule group.
    ProductId: string
    -- The display name of the product listing for this rule group.
    ListingName: string
end

local record DescribeRuleGroupSummaryInput
    -- The descriptive name of the rule group. You can't change the name of a rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupName: string
    -- Required. The Amazon Resource Name (ARN) of the rule group.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupArn: string
    -- The type of rule group you want a summary for. This is a required field.
    -- Valid value: STATEFUL
    -- Note that STATELESS exists but is not currently supported. If you provide STATELESS, an exception is returned.
    Type: string
end

local record DescribeRuleGroupSummaryOutput
    -- The descriptive name of the rule group. You can't change the name of a rule group after you create it.
    RuleGroupName: string
    -- A description of the rule group.
    Description: string
    -- A complex type that contains rule information based on the rule group's configured summary settings. The content varies depending on the fields that you specified to extract in your SummaryConfiguration. When you haven't configured any summary settings, this returns an empty array. The response might include:
    -- Rule identifiers
    -- Rule descriptions
    -- Any metadata fields that you specified in your SummaryConfiguration
    Summary: Summary
end

local record DescribeTLSInspectionConfigurationInput
    -- The Amazon Resource Name (ARN) of the TLS inspection configuration.
    -- You must specify the ARN or the name, and you can specify both.
    TLSInspectionConfigurationArn: string
    -- The descriptive name of the TLS inspection configuration. You can't change the name of a TLS inspection configuration after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    TLSInspectionConfigurationName: string
end

local record DescribeTLSInspectionConfigurationOutput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the TLS inspection configuration. The token marks the state of the TLS inspection configuration resource at the time of the request.
    -- To make changes to the TLS inspection configuration, you provide the token in your request. Network Firewall uses the token to ensure that the TLS inspection configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the TLS inspection configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The object that defines a TLS inspection configuration. This, along with TLSInspectionConfigurationResponse, define the TLS inspection configuration. You can retrieve all objects for a TLS inspection configuration by calling DescribeTLSInspectionConfiguration.
    -- Network Firewall uses a TLS inspection configuration to decrypt traffic. Network Firewall re-encrypts the traffic before sending it to its destination.
    -- To use a TLS inspection configuration, you add it to a new Network Firewall firewall policy, then you apply the firewall policy to a firewall. Network Firewall acts as a proxy service to decrypt and inspect the traffic traveling through your firewalls. You can reference a TLS inspection configuration from more than one firewall policy, and you can use a firewall policy in more than one firewall. For more information about using TLS inspection configurations, see
    -- Inspecting SSL/TLS traffic with TLS
    -- inspection configurations in the Network Firewall Developer Guide.
    TLSInspectionConfiguration: TLSInspectionConfiguration
    -- The high-level properties of a TLS inspection configuration. This, along with the TLSInspectionConfiguration, define the TLS inspection configuration. You can retrieve all objects for a TLS inspection configuration by calling DescribeTLSInspectionConfiguration.
    TLSInspectionConfigurationResponse: TLSInspectionConfigurationResponse
end

local record DescribeVpcEndpointAssociationInput
    -- The Amazon Resource Name (ARN) of a VPC endpoint association.
    VpcEndpointAssociationArn: string
end

local record DescribeVpcEndpointAssociationOutput
    -- The configuration settings for the VPC endpoint association. These settings include the firewall and the VPC and subnet to use for the firewall endpoint.
    VpcEndpointAssociation: VpcEndpointAssociation
    -- Detailed information about the current status of a VpcEndpointAssociation. You can retrieve this
    -- by calling DescribeVpcEndpointAssociation and providing the VPC endpoint association ARN.
    VpcEndpointAssociationStatus: VpcEndpointAssociationStatus
end

local record DetachRuleGroupsFromProxyConfigurationInput
    -- The descriptive name of the proxy configuration. You can't change the name of a proxy configuration after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationName: string
    -- The Amazon Resource Name (ARN) of a proxy configuration.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationArn: string
    -- The proxy rule group names to detach from the proxy configuration
    RuleGroupNames: {string}
    -- The proxy rule group arns to detach from the proxy configuration
    RuleGroupArns: {string}
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy configuration. The token marks the state of the proxy configuration resource at the time of the request.
    -- To make changes to the proxy configuration, you provide the token in your request. Network Firewall uses the token to ensure that the proxy configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record DetachRuleGroupsFromProxyConfigurationOutput
    -- The updated proxy configuration resource that reflects the updates from the request.
    ProxyConfiguration: ProxyConfiguration
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy configuration. The token marks the state of the proxy configuration resource at the time of the request.
    -- To make changes to the proxy configuration, you provide the token in your request. Network Firewall uses the token to ensure that the proxy configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record DisassociateAvailabilityZonesInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- Required. The Availability Zones to remove from the firewall's configuration.
    AvailabilityZoneMappings: {AvailabilityZoneMapping}
end

local record DisassociateAvailabilityZonesOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- The remaining Availability Zones where the firewall has endpoints after the disassociation.
    AvailabilityZoneMappings: {AvailabilityZoneMapping}
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record DisassociateSubnetsInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- The unique identifiers for the subnets that you want to disassociate.
    SubnetIds: {string}
end

local record DisassociateSubnetsOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- The IDs of the subnets that are associated with the firewall.
    SubnetMappings: {SubnetMapping}
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record GetAnalysisReportResultsInput
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- The unique ID of the query that ran when you requested an analysis report.
    AnalysisReportId: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
end

local record GetAnalysisReportResultsOutput
    -- The status of the analysis report you specify. Statuses include RUNNING, COMPLETED, or FAILED.
    Status: string
    -- The date and time within the last 30 days from which to start retrieving analysis data,
    -- in UTC format (for example, YYYY-MM-DDTHH:MM:SSZ.
    StartTime: number
    -- The date and time, up to the current date, from which to stop retrieving analysis data,
    -- in UTC format (for example, YYYY-MM-DDTHH:MM:SSZ).
    EndTime: number
    -- The date and time the analysis report was ran.
    ReportTime: number
    -- The type of traffic that will be used to generate a report.
    AnalysisType: string
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- Retrieves the results of a traffic analysis report.
    AnalysisReportResults: {AnalysisTypeReportResult}
end

local record ListAnalysisReportsInput
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
end

local record ListAnalysisReportsOutput
    -- The id and ReportTime associated with a requested analysis report. Does not provide the status of the analysis report.
    AnalysisReports: {AnalysisReport}
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
end

local record ListFirewallPoliciesInput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
end

local record ListFirewallPoliciesOutput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The metadata for the firewall policies. Depending on your setting for max results and
    -- the number of firewall policies that you have, this might not be the full list.
    FirewallPolicies: {FirewallPolicyMetadata}
end

local record ListFirewallsInput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The unique identifiers of the VPCs that you want Network Firewall to retrieve the firewalls
    -- for. Leave this blank to retrieve all firewalls that you have defined.
    VpcIds: {string}
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
end

local record ListFirewallsOutput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The firewall metadata objects for the VPCs that you specified. Depending on your setting
    -- for max results and the number of firewalls you have, a single call might not be the full
    -- list.
    Firewalls: {FirewallMetadata}
end

local record ListFlowOperationResultsInput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- A unique identifier for the flow operation. This ID is returned in the responses to start and list commands. You provide to describe commands.
    FlowOperationId: string
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
    -- The ID of the Availability Zone where the firewall is located. For example, us-east-2a.
    -- Defines the scope a flow operation. You can use up to 20 filters to configure a single flow operation.
    AvailabilityZone: string
    -- A unique identifier for the primary endpoint associated with a firewall.
    VpcEndpointId: string
    -- The Amazon Resource Name (ARN) of a VPC endpoint association.
    VpcEndpointAssociationArn: string
end

local record ListFlowOperationResultsOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The ID of the Availability Zone where the firewall is located. For example, us-east-2a.
    -- Defines the scope a flow operation. You can use up to 20 filters to configure a single flow operation.
    AvailabilityZone: string
    VpcEndpointAssociationArn: string
    VpcEndpointId: string
    -- A unique identifier for the flow operation. This ID is returned in the responses to start and list commands. You provide to describe commands.
    FlowOperationId: string
    -- Returns the status of the flow operation. This string is returned in the responses to start, list, and describe commands.
    -- If the status is COMPLETED_WITH_ERRORS, results may be returned with any number of Flows missing from the response.
    -- If the status is FAILED, Flows returned will be empty.
    FlowOperationStatus: string
    -- If the asynchronous operation fails, Network Firewall populates this with the reason for the error or failure.
    -- Options include Flow operation error and Flow timeout.
    StatusMessage: string
    -- A timestamp indicating when the Suricata engine identified flows impacted by an operation.
    FlowRequestTimestamp: number
    -- Any number of arrays, where each array is a single flow identified in the scope of the operation.
    -- If multiple flows were in the scope of the operation, multiple Flows arrays are returned.
    Flows: {Flow}
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
end

local record ListFlowOperationsInput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The ID of the Availability Zone where the firewall is located. For example, us-east-2a.
    -- Defines the scope a flow operation. You can use up to 20 filters to configure a single flow operation.
    AvailabilityZone: string
    -- The Amazon Resource Name (ARN) of a VPC endpoint association.
    VpcEndpointAssociationArn: string
    -- A unique identifier for the primary endpoint associated with a firewall.
    VpcEndpointId: string
    -- An optional string that defines whether any or all operation types are returned.
    FlowOperationType: string
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
end

local record ListFlowOperationsOutput
    -- Flow operations let you manage the flows tracked in the flow table, also known as the firewall table.
    -- A flow is network traffic that is monitored by a firewall, either by stateful or stateless rules.
    -- For traffic to be considered part of a flow, it must share Destination, DestinationPort, Direction, Protocol, Source, and SourcePort.
    FlowOperations: {FlowOperationMetadata}
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
end

local record ListProxiesInput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
end

local record ListProxiesOutput
    -- The metadata for the proxies. Depending on your setting for max results and
    -- the number of proxies that you have, this might not be the full list.
    Proxies: {ProxyMetadata}
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
end

local record ListProxyConfigurationsInput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
end

local record ListProxyConfigurationsOutput
    -- The metadata for the proxy configurations. Depending on your setting for max results and
    -- the number of proxy configurations that you have, this might not be the full list.
    ProxyConfigurations: {ProxyConfigurationMetadata}
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
end

local record ListProxyRuleGroupsInput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
end

local record ListProxyRuleGroupsOutput
    -- The metadata for the proxy rule groups. Depending on your setting for max results and
    -- the number of proxy rule groups that you have, this might not be the full list.
    ProxyRuleGroups: {ProxyRuleGroupMetadata}
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
end

local record ListRuleGroupsInput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
    -- The scope of the request. The default setting of ACCOUNT or a setting of
    -- NULL returns all of the rule groups in your account. A setting of
    -- MANAGED returns all available managed rule groups.
    Scope: string
    -- Indicates the general category of the Amazon Web Services managed rule group.
    ManagedType: string
    -- Filters the results to show only rule groups with the specified subscription status. Use this to find subscribed or unsubscribed rule groups.
    SubscriptionStatus: string
    -- Indicates whether the rule group is stateless or stateful. If the rule group is stateless, it contains stateless rules. If it is stateful, it contains stateful rules.
    Type: string
end

local record ListRuleGroupsOutput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The rule group metadata objects that you've defined. Depending on your setting for max
    -- results and the number of rule groups, this might not be the full list.
    RuleGroups: {RuleGroupMetadata}
end

local record ListTagsForResourceInput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
    -- The Amazon Resource Name (ARN) of the resource.
    ResourceArn: string
end

local record ListTagsForResourceOutput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The tags that are associated with the resource.
    Tags: {Tag}
end

local record ListTLSInspectionConfigurationsInput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
end

local record ListTLSInspectionConfigurationsOutput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The TLS inspection configuration metadata objects that you've defined. Depending on your setting for max results and the number of TLS inspection configurations, this might not be the full list.
    TLSInspectionConfigurations: {TLSInspectionConfigurationMetadata}
end

local record ListVpcEndpointAssociationsInput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The maximum number of objects that you want Network Firewall to return for this request. If more
    -- objects are available, in the response, Network Firewall provides a
    -- NextToken value that you can use in a subsequent call to get the next batch of objects.
    MaxResults: number
    -- The Amazon Resource Name (ARN) of the firewall.
    -- If you don't specify this, Network Firewall retrieves all VPC endpoint associations that you have defined.
    FirewallArn: string
end

local record ListVpcEndpointAssociationsOutput
    -- When you request a list of objects with a MaxResults setting, if the number of objects that are still available
    -- for retrieval exceeds the maximum you requested, Network Firewall returns a NextToken
    -- value in the response. To retrieve the next batch of objects, use the token returned from the prior request in your next request.
    NextToken: string
    -- The VPC endpoint assocation metadata objects for the firewall that you specified. If you didn't
    -- specify a firewall, this is all VPC endpoint associations that you have defined.
    -- Depending on your setting
    -- for max results and the number of firewalls you have, a single call might not be the full
    -- list.
    VpcEndpointAssociations: {VpcEndpointAssociationMetadata}
end

local record PutResourcePolicyInput
    -- The Amazon Resource Name (ARN) of the account that you want to share your Network Firewall resources with.
    ResourceArn: string
    -- The IAM policy statement that lists the accounts that you want to share your Network Firewall resources with
    -- and the operations that you want the accounts to be able to perform.
    -- For a rule group resource, you can specify the following operations in the Actions section of the statement:
    -- network-firewall:CreateFirewallPolicy
    -- network-firewall:UpdateFirewallPolicy
    -- network-firewall:ListRuleGroups
    -- For a firewall policy resource, you can specify the following operations in the Actions section of the statement:
    -- network-firewall:AssociateFirewallPolicy
    -- network-firewall:ListFirewallPolicies
    -- For a firewall resource, you can specify the following operations in the Actions section of the statement:
    -- network-firewall:CreateVpcEndpointAssociation
    -- network-firewall:DescribeFirewallMetadata
    -- network-firewall:ListFirewalls
    -- In the Resource section of the statement, you specify the ARNs for the Network Firewall resources that you want to share with the account that you specified in Arn.
    Policy: string
end

local record PutResourcePolicyOutput
end

local record RejectNetworkFirewallTransitGatewayAttachmentInput
    -- Required. The unique identifier of the transit gateway attachment to reject. This ID is returned in the response when creating a transit gateway-attached firewall.
    TransitGatewayAttachmentId: string
end

local record RejectNetworkFirewallTransitGatewayAttachmentOutput
    -- The unique identifier of the transit gateway attachment that was rejected.
    TransitGatewayAttachmentId: string
    -- The current status of the transit gateway attachment. Valid values are:
    -- CREATING - The attachment is being created
    -- DELETING - The attachment is being deleted
    -- DELETED - The attachment has been deleted
    -- FAILED - The attachment creation has failed and cannot be recovered
    -- ERROR - The attachment is in an error state that might be recoverable
    -- READY - The attachment is active and processing traffic
    -- PENDING_ACCEPTANCE - The attachment is waiting to be accepted
    -- REJECTING - The attachment is in the process of being rejected
    -- REJECTED - The attachment has been rejected
    -- For information about troubleshooting endpoint failures, see Troubleshooting firewall endpoint failures in the Network Firewall Developer Guide.
    TransitGatewayAttachmentStatus: string
end

local record StartAnalysisReportInput
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The type of traffic that will be used to generate a report.
    AnalysisType: string
end

local record StartAnalysisReportOutput
    -- The unique ID of the query that ran when you requested an analysis report.
    AnalysisReportId: string
end

local record StartFlowCaptureInput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The ID of the Availability Zone where the firewall is located. For example, us-east-2a.
    -- Defines the scope a flow operation. You can use up to 20 filters to configure a single flow operation.
    AvailabilityZone: string
    -- The Amazon Resource Name (ARN) of a VPC endpoint association.
    VpcEndpointAssociationArn: string
    -- A unique identifier for the primary endpoint associated with a firewall.
    VpcEndpointId: string
    -- The reqested FlowOperation ignores flows with an age (in seconds) lower than MinimumFlowAgeInSeconds.
    -- You provide this for start commands.
    -- We recommend setting this value to at least 1 minute (60 seconds) to reduce chance of capturing flows that are not yet established.
    MinimumFlowAgeInSeconds: number
    -- Defines the scope a flow operation. You can use up to 20 filters to configure a single flow operation.
    FlowFilters: {FlowFilter}
end

local record StartFlowCaptureOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- A unique identifier for the flow operation. This ID is returned in the responses to start and list commands. You provide to describe commands.
    FlowOperationId: string
    -- Returns the status of the flow operation. This string is returned in the responses to start, list, and describe commands.
    -- If the status is COMPLETED_WITH_ERRORS, results may be returned with any number of Flows missing from the response.
    -- If the status is FAILED, Flows returned will be empty.
    FlowOperationStatus: string
end

local record StartFlowFlushInput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The ID of the Availability Zone where the firewall is located. For example, us-east-2a.
    -- Defines the scope a flow operation. You can use up to 20 filters to configure a single flow operation.
    AvailabilityZone: string
    -- The Amazon Resource Name (ARN) of a VPC endpoint association.
    VpcEndpointAssociationArn: string
    -- A unique identifier for the primary endpoint associated with a firewall.
    VpcEndpointId: string
    -- The reqested FlowOperation ignores flows with an age (in seconds) lower than MinimumFlowAgeInSeconds.
    -- You provide this for start commands.
    MinimumFlowAgeInSeconds: number
    -- Defines the scope a flow operation. You can use up to 20 filters to configure a single flow operation.
    FlowFilters: {FlowFilter}
end

local record StartFlowFlushOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- A unique identifier for the flow operation. This ID is returned in the responses to start and list commands. You provide to describe commands.
    FlowOperationId: string
    -- Returns the status of the flow operation. This string is returned in the responses to start, list, and describe commands.
    -- If the status is COMPLETED_WITH_ERRORS, results may be returned with any number of Flows missing from the response.
    -- If the status is FAILED, Flows returned will be empty.
    FlowOperationStatus: string
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    ResourceArn: string
    Tags: {Tag}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    ResourceArn: string
    TagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateAvailabilityZoneChangeProtectionInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- A setting indicating whether the firewall is protected against changes to the subnet associations.
    -- Use this setting to protect against
    -- accidentally modifying the subnet associations for a firewall that is in use. When you create a firewall, the operation initializes this setting to TRUE.
    AvailabilityZoneChangeProtection: boolean
end

local record UpdateAvailabilityZoneChangeProtectionOutput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- A setting indicating whether the firewall is protected against changes to the subnet associations.
    -- Use this setting to protect against
    -- accidentally modifying the subnet associations for a firewall that is in use. When you create a firewall, the operation initializes this setting to TRUE.
    AvailabilityZoneChangeProtection: boolean
end

local record UpdateFirewallAnalysisSettingsInput
    -- An optional setting indicating the specific traffic analysis types to enable on the firewall.
    EnabledAnalysisTypes: {string}
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateFirewallAnalysisSettingsOutput
    -- An optional setting indicating the specific traffic analysis types to enable on the firewall.
    EnabledAnalysisTypes: {string}
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateFirewallDeleteProtectionInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- A flag indicating whether it is possible to delete the firewall. A setting of TRUE indicates
    -- that the firewall is protected against deletion. Use this setting to protect against
    -- accidentally deleting a firewall that is in use. When you create a firewall, the operation initializes this flag to TRUE.
    DeleteProtection: boolean
end

local record UpdateFirewallDeleteProtectionOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- A flag indicating whether it is possible to delete the firewall. A setting of TRUE indicates
    -- that the firewall is protected against deletion. Use this setting to protect against
    -- accidentally deleting a firewall that is in use. When you create a firewall, the operation initializes this flag to TRUE.
    DeleteProtection: boolean
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateFirewallDescriptionInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- The new description for the firewall. If you omit this setting, Network Firewall removes
    -- the description for the firewall.
    Description: string
end

local record UpdateFirewallDescriptionOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- A description of the firewall.
    Description: string
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateFirewallEncryptionConfigurationInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    EncryptionConfiguration: EncryptionConfiguration
end

local record UpdateFirewallEncryptionConfigurationOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    EncryptionConfiguration: EncryptionConfiguration
end

local record UpdateFirewallPolicyInput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the firewall policy. The token marks the state of the policy resource at the time of the request.
    -- To make changes to the policy, you provide the token in your request. Network Firewall uses the token to ensure that the policy hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall policy again to get a current copy of it with current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall policy.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallPolicyArn: string
    -- The descriptive name of the firewall policy. You can't change the name of a firewall policy after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallPolicyName: string
    -- The updated firewall policy to use for the firewall. You can't add or remove a TLSInspectionConfiguration after you create a firewall policy. However, you can replace an existing TLS inspection configuration with another TLSInspectionConfiguration.
    FirewallPolicy: FirewallPolicy
    -- A description of the firewall policy.
    Description: string
    -- Indicates whether you want Network Firewall to just check the validity of the request, rather than run the request.
    -- If set to TRUE, Network Firewall checks whether the request can run successfully,
    -- but doesn't actually make the requested changes. The call returns the value that the request would return if you ran it with
    -- dry run set to FALSE, but doesn't make additions or changes to your resources. This option allows you to make sure that you have
    -- the required permissions to run the request and that your request parameters are valid.
    -- If set to FALSE, Network Firewall makes the requested changes to your resources.
    DryRun: boolean
    -- A complex type that contains settings for encryption of your firewall policy resources.
    EncryptionConfiguration: EncryptionConfiguration
end

local record UpdateFirewallPolicyOutput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the firewall policy. The token marks the state of the policy resource at the time of the request.
    -- To make changes to the policy, you provide the token in your request. Network Firewall uses the token to ensure that the policy hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall policy again to get a current copy of it with current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The high-level properties of a firewall policy. This, along with the FirewallPolicy, define the policy. You can retrieve all objects for a firewall policy by calling DescribeFirewallPolicy.
    FirewallPolicyResponse: FirewallPolicyResponse
end

local record UpdateFirewallPolicyChangeProtectionInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- A setting indicating whether the firewall is protected against a change to the firewall policy association.
    -- Use this setting to protect against
    -- accidentally modifying the firewall policy for a firewall that is in use. When you create a firewall, the operation initializes this setting to TRUE.
    FirewallPolicyChangeProtection: boolean
end

local record UpdateFirewallPolicyChangeProtectionOutput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- A setting indicating whether the firewall is protected against a change to the firewall policy association.
    -- Use this setting to protect against
    -- accidentally modifying the firewall policy for a firewall that is in use. When you create a firewall, the operation initializes this setting to TRUE.
    FirewallPolicyChangeProtection: boolean
end

local record UpdateLoggingConfigurationInput
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- Defines how Network Firewall performs logging for a firewall. If you omit this setting,
    -- Network Firewall disables logging for the firewall.
    LoggingConfiguration: LoggingConfiguration
    -- A boolean that lets you enable or disable the detailed firewall monitoring dashboard on the firewall.
    -- The monitoring dashboard provides comprehensive visibility into your firewall's flow logs and alert logs.
    -- After you enable detailed monitoring, you can access these dashboards directly from the Monitoring page of the Network Firewall console.
    -- Specify TRUE to enable the the detailed monitoring dashboard on the firewall.
    -- Specify FALSE to disable the the detailed monitoring dashboard on the firewall.
    EnableMonitoringDashboard: boolean
end

local record UpdateLoggingConfigurationOutput
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    LoggingConfiguration: LoggingConfiguration
    -- A boolean that reflects whether or not the firewall monitoring dashboard is enabled on a firewall.
    -- Returns TRUE when the firewall monitoring dashboard is enabled on the firewall.
    -- Returns FALSE when the firewall monitoring dashboard is not enabled on the firewall.
    EnableMonitoringDashboard: boolean
end

local record UpdateProxyInput
    -- The NAT Gateway the proxy is attached to.
    NatGatewayId: string
    -- The descriptive name of the proxy. You can't change the name of a proxy after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyName: string
    -- The Amazon Resource Name (ARN) of a proxy.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyArn: string
    -- Listener properties for HTTP and HTTPS traffic to add.
    ListenerPropertiesToAdd: {ListenerPropertyRequest}
    -- Listener properties for HTTP and HTTPS traffic to remove.
    ListenerPropertiesToRemove: {ListenerPropertyRequest}
    -- TLS decryption on traffic to filter on attributes in the HTTP header.
    TlsInterceptProperties: TlsInterceptPropertiesRequest
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy. The token marks the state of the proxy resource at the time of the request.
    -- To make changes to the proxy, you provide the token in your request. Network Firewall uses the token to ensure that the proxy hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateProxyOutput
    -- The updated proxy resource that reflects the updates from the request.
    Proxy: Proxy
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy. The token marks the state of the proxy resource at the time of the request.
    -- To make changes to the proxy, you provide the token in your request. Network Firewall uses the token to ensure that the proxy hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateProxyConfigurationInput
    -- The descriptive name of the proxy configuration. You can't change the name of a proxy configuration after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationName: string
    -- The Amazon Resource Name (ARN) of a proxy configuration.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationArn: string
    -- Evaluation points in the traffic flow where rules are applied. There are three phases in a traffic where the rule match is applied.
    DefaultRulePhaseActions: ProxyConfigDefaultRulePhaseActionsRequest
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy configuration. The token marks the state of the proxy configuration resource at the time of the request.
    -- To make changes to the proxy configuration, you provide the token in your request. Network Firewall uses the token to ensure that the proxy configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateProxyConfigurationOutput
    -- The updated proxy configuration resource that reflects the updates from the request.
    ProxyConfiguration: ProxyConfiguration
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy configuration. The token marks the state of the proxy configuration resource at the time of the request.
    -- To make changes to the proxy configuration, you provide the token in your request. Network Firewall uses the token to ensure that the proxy configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateProxyRuleInput
    -- The descriptive name of the proxy rule group. You can't change the name of a proxy rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupName: string
    -- The Amazon Resource Name (ARN) of a proxy rule group.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupArn: string
    -- The descriptive name of the proxy rule. You can't change the name of a proxy rule after you create it.
    ProxyRuleName: string
    -- A description of the proxy rule.
    Description: string
    -- Depending on the match action, the proxy either stops the evaluation (if the action is terminal - allow or deny), or continues it (if the action is alert) until it matches a rule with a terminal action.
    Action: string
    -- Proxy rule conditions to add. Match criteria that specify what traffic attributes to examine. Conditions include operators (StringEquals, StringLike) and values to match against.
    AddConditions: {ProxyRuleCondition}
    -- Proxy rule conditions to remove. Match criteria that specify what traffic attributes to examine. Conditions include operators (StringEquals, StringLike) and values to match against.
    RemoveConditions: {ProxyRuleCondition}
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy rule. The token marks the state of the proxy rule resource at the time of the request.
    -- To make changes to the proxy rule, you provide the token in your request. Network Firewall uses the token to ensure that the proxy rule hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy rule again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateProxyRuleOutput
    -- The updated proxy rule resource that reflects the updates from the request.
    ProxyRule: ProxyRule
    -- Proxy rule conditions removed from the rule.
    RemovedConditions: {ProxyRuleCondition}
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy rule. The token marks the state of the proxy rule resource at the time of the request.
    -- To make changes to the proxy rule, you provide the token in your request. Network Firewall uses the token to ensure that the proxy rule hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy rule again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateProxyRuleGroupPrioritiesInput
    -- The descriptive name of the proxy configuration. You can't change the name of a proxy configuration after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationName: string
    -- The Amazon Resource Name (ARN) of a proxy configuration.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyConfigurationArn: string
    -- proxy rule group resources to update to new positions.
    RuleGroups: {ProxyRuleGroupPriority}
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy configuration. The token marks the state of the proxy configuration resource at the time of the request.
    -- To make changes to the proxy configuration, you provide the token in your request. Network Firewall uses the token to ensure that the proxy configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateProxyRuleGroupPrioritiesOutput
    -- The updated proxy rule group hierarchy that reflects the updates from the request.
    ProxyRuleGroups: {ProxyRuleGroupPriorityResult}
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy configuration. The token marks the state of the proxy configuration resource at the time of the request.
    -- To make changes to the proxy configuration, you provide the token in your request. Network Firewall uses the token to ensure that the proxy configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateProxyRulePrioritiesInput
    -- The descriptive name of the proxy rule group. You can't change the name of a proxy rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupName: string
    -- The Amazon Resource Name (ARN) of a proxy rule group.
    -- You must specify the ARN or the name, and you can specify both.
    ProxyRuleGroupArn: string
    -- Evaluation points in the traffic flow where rules are applied. There are three phases in a traffic where the rule match is applied.
    RuleGroupRequestPhase: string
    -- proxy rule resources to update to new positions.
    Rules: {ProxyRulePriority}
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy rule group. The token marks the state of the proxy rule group resource at the time of the request.
    -- To make changes to the proxy rule group, you provide the token in your request. Network Firewall uses the token to ensure that the proxy rule group hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy rule group again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateProxyRulePrioritiesOutput
    -- The descriptive name of the proxy rule group. You can't change the name of a proxy rule group after you create it.
    ProxyRuleGroupName: string
    -- The Amazon Resource Name (ARN) of a proxy rule group.
    ProxyRuleGroupArn: string
    -- Evaluation points in the traffic flow where rules are applied. There are three phases in a traffic where the rule match is applied.
    RuleGroupRequestPhase: string
    -- The updated proxy rule hierarchy that reflects the updates from the request.
    Rules: {ProxyRulePriority}
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the proxy rule group. The token marks the state of the proxy rule group resource at the time of the request.
    -- To make changes to the proxy rule group, you provide the token in your request. Network Firewall uses the token to ensure that the proxy rule group hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the proxy rule group again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateRuleGroupInput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the rule group. The token marks the state of the rule group resource at the time of the request.
    -- To make changes to the rule group, you provide the token in your request. Network Firewall uses the token to ensure that the rule group hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the rule group again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the rule group.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupArn: string
    -- The descriptive name of the rule group. You can't change the name of a rule group after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    RuleGroupName: string
    -- An object that defines the rule group rules.
    -- You must provide either this rule group setting or a Rules setting, but not both.
    RuleGroup: RuleGroup
    -- A string containing stateful rule group rules specifications in Suricata flat format, with one rule
    -- per line. Use this to import your existing Suricata compatible rule groups.
    -- You must provide either this rules setting or a populated RuleGroup setting, but not both.
    -- You can provide your rule group specification in Suricata flat format through this setting when you create or update your rule group. The call
    -- response returns a RuleGroup object that Network Firewall has populated from your string.
    Rules: string
    -- Indicates whether the rule group is stateless or stateful. If the rule group is stateless, it contains
    -- stateless rules. If it is stateful, it contains stateful rules.
    -- This setting is required for requests that do not include the RuleGroupARN.
    Type: string
    -- A description of the rule group.
    Description: string
    -- Indicates whether you want Network Firewall to just check the validity of the request, rather than run the request.
    -- If set to TRUE, Network Firewall checks whether the request can run successfully,
    -- but doesn't actually make the requested changes. The call returns the value that the request would return if you ran it with
    -- dry run set to FALSE, but doesn't make additions or changes to your resources. This option allows you to make sure that you have
    -- the required permissions to run the request and that your request parameters are valid.
    -- If set to FALSE, Network Firewall makes the requested changes to your resources.
    DryRun: boolean
    -- A complex type that contains settings for encryption of your rule group resources.
    EncryptionConfiguration: EncryptionConfiguration
    -- A complex type that contains metadata about the rule group that your own rule group is copied from. You can use the metadata to keep track of updates made to the originating rule group.
    SourceMetadata: SourceMetadata
    -- Indicates whether you want Network Firewall to analyze the stateless rules in the rule group for rule behavior such as asymmetric routing. If set to TRUE, Network Firewall runs the analysis and then updates the rule group for you. To run the stateless rule group analyzer without updating the rule group, set DryRun to TRUE.
    AnalyzeRuleGroup: boolean
    -- Updates the selected summary configuration for a rule group.
    -- Changes affect subsequent responses from DescribeRuleGroupSummary.
    SummaryConfiguration: SummaryConfiguration
end

local record UpdateRuleGroupOutput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the rule group. The token marks the state of the rule group resource at the time of the request.
    -- To make changes to the rule group, you provide the token in your request. Network Firewall uses the token to ensure that the rule group hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the rule group again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The high-level properties of a rule group. This, along with the RuleGroup, define the rule group. You can retrieve all objects for a rule group by calling DescribeRuleGroup.
    RuleGroupResponse: RuleGroupResponse
end

local record UpdateSubnetChangeProtectionInput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    -- You must specify the ARN or the name, and you can specify both.
    FirewallName: string
    -- A setting indicating whether the firewall is protected against changes to the subnet associations.
    -- Use this setting to protect against
    -- accidentally modifying the subnet associations for a firewall that is in use. When you create a firewall, the operation initializes this setting to TRUE.
    SubnetChangeProtection: boolean
end

local record UpdateSubnetChangeProtectionOutput
    -- An optional token that you can use for optimistic locking. Network Firewall returns a token to your requests that access the firewall. The token marks the state of the firewall resource at the time of the request.
    -- To make an unconditional change to the firewall, omit the token in your update request. Without the token, Network Firewall performs your updates regardless of whether the firewall has changed since you last retrieved it.
    -- To make a conditional change to the firewall, provide the token in your update request. Network Firewall uses the token to ensure that the firewall hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the firewall again to get a current copy of it with a new token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The Amazon Resource Name (ARN) of the firewall.
    FirewallArn: string
    -- The descriptive name of the firewall. You can't change the name of a firewall after you create it.
    FirewallName: string
    -- A setting indicating whether the firewall is protected against changes to the subnet associations.
    -- Use this setting to protect against
    -- accidentally modifying the subnet associations for a firewall that is in use. When you create a firewall, the operation initializes this setting to TRUE.
    SubnetChangeProtection: boolean
end

local record UpdateTLSInspectionConfigurationInput
    -- The Amazon Resource Name (ARN) of the TLS inspection configuration.
    TLSInspectionConfigurationArn: string
    -- The descriptive name of the TLS inspection configuration. You can't change the name of a TLS inspection configuration after you create it.
    TLSInspectionConfigurationName: string
    -- The object that defines a TLS inspection configuration. This, along with TLSInspectionConfigurationResponse, define the TLS inspection configuration. You can retrieve all objects for a TLS inspection configuration by calling DescribeTLSInspectionConfiguration.
    -- Network Firewall uses a TLS inspection configuration to decrypt traffic. Network Firewall re-encrypts the traffic before sending it to its destination.
    -- To use a TLS inspection configuration, you add it to a new Network Firewall firewall policy, then you apply the firewall policy to a firewall. Network Firewall acts as a proxy service to decrypt and inspect the traffic traveling through your firewalls. You can reference a TLS inspection configuration from more than one firewall policy, and you can use a firewall policy in more than one firewall. For more information about using TLS inspection configurations, see
    -- Inspecting SSL/TLS traffic with TLS
    -- inspection configurations in the Network Firewall Developer Guide.
    TLSInspectionConfiguration: TLSInspectionConfiguration
    -- A description of the TLS inspection configuration.
    Description: string
    -- A complex type that contains the Amazon Web Services KMS encryption configuration settings for your TLS inspection configuration.
    EncryptionConfiguration: EncryptionConfiguration
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the TLS inspection configuration. The token marks the state of the TLS inspection configuration resource at the time of the request.
    -- To make changes to the TLS inspection configuration, you provide the token in your request. Network Firewall uses the token to ensure that the TLS inspection configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the TLS inspection configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
end

local record UpdateTLSInspectionConfigurationOutput
    -- A token used for optimistic locking. Network Firewall returns a token to your requests that access the TLS inspection configuration. The token marks the state of the TLS inspection configuration resource at the time of the request.
    -- To make changes to the TLS inspection configuration, you provide the token in your request. Network Firewall uses the token to ensure that the TLS inspection configuration hasn't changed since you last retrieved it. If it has changed, the operation fails with an InvalidTokenException. If this happens, retrieve the TLS inspection configuration again to get a current copy of it with a current token. Reapply your changes as needed, then try the operation again using the new token.
    UpdateToken: string
    -- The high-level properties of a TLS inspection configuration. This, along with the TLSInspectionConfiguration, define the TLS inspection configuration. You can retrieve all objects for a TLS inspection configuration by calling DescribeTLSInspectionConfiguration.
    TLSInspectionConfigurationResponse: TLSInspectionConfigurationResponse
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Amazon Web Services doesn't currently have enough available capacity to fulfill your request. Try your
-- request later.
local record InsufficientCapacityException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Amazon Web Services doesn't currently have enough available capacity to fulfill your request. Try your
-- request later.
local record InsufficientCapacityException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Amazon Web Services doesn't currently have enough available capacity to fulfill your request. Try your
-- request later.
local record InsufficientCapacityException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to perform the operation because doing so would violate a limit setting.
local record LimitExceededException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Amazon Web Services doesn't currently have enough available capacity to fulfill your request. Try your
-- request later.
local record InsufficientCapacityException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to perform the operation because doing so would violate a limit setting.
local record LimitExceededException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to perform the operation because doing so would violate a limit setting.
local record LimitExceededException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- The operation you requested isn't supported by Network Firewall.
local record UnsupportedOperationException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to perform the operation because doing so would violate a limit setting.
local record LimitExceededException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to perform the operation because doing so would violate a limit setting.
local record LimitExceededException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Amazon Web Services doesn't currently have enough available capacity to fulfill your request. Try your
-- request later.
local record InsufficientCapacityException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to perform the operation because doing so would violate a limit setting.
local record LimitExceededException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Amazon Web Services doesn't currently have enough available capacity to fulfill your request. Try your
-- request later.
local record InsufficientCapacityException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to perform the operation because doing so would violate a limit setting.
local record LimitExceededException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Amazon Web Services doesn't currently have enough available capacity to fulfill your request. Try your
-- request later.
local record InsufficientCapacityException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to perform the operation because doing so would violate a limit setting.
local record LimitExceededException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- The operation you requested isn't supported by Network Firewall.
local record UnsupportedOperationException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- The operation you requested isn't supported by Network Firewall.
local record UnsupportedOperationException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- The operation you requested isn't supported by Network Firewall.
local record UnsupportedOperationException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The policy statement failed validation.
local record InvalidResourcePolicyException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- The operation you requested isn't supported by Network Firewall.
local record UnsupportedOperationException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because it's not valid. For example, you might have tried to delete
-- a rule group or firewall policy that's in use.
local record InvalidOperationException
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The policy statement failed validation.
local record InvalidResourcePolicyException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to change the resource because your account doesn't own it.
local record ResourceOwnerCheckException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to change the resource because your account doesn't own it.
local record ResourceOwnerCheckException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to change the resource because your account doesn't own it.
local record ResourceOwnerCheckException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to change the resource because your account doesn't own it.
local record ResourceOwnerCheckException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to send logs to a configured logging destination.
local record LogDestinationPermissionException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- The operation you requested isn't supported by Network Firewall.
local record UnsupportedOperationException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to change the resource because your account doesn't own it.
local record ResourceOwnerCheckException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

-- Your request is valid, but Network Firewall couldn't perform the operation because of a
-- system problem. Retry your request.
local record InternalServerError
    Message: string
end

-- The operation failed because of a problem with your request. Examples include:
-- You specified an unsupported parameter name or value.
-- You tried to update a property with a value that isn't among the available
-- types.
-- Your request references an ARN that is malformed, or corresponds to a resource
-- that isn't valid in the context of the request.
local record InvalidRequestException
    Message: string
end

-- The token you provided is stale or isn't valid for the operation.
local record InvalidTokenException
    Message: string
end

-- Unable to locate a resource using the parameters that you provided.
local record ResourceNotFoundException
    Message: string
end

-- Unable to process the request due to throttling limitations.
local record ThrottlingException
    Message: string
end

local type EnabledAnalysisType = "TLS_SNI" | "HTTP_HOST"

local type FirewallStatusValue = "PROVISIONING" | "DELETING" | "READY"

local type FlowOperationStatus = "COMPLETED" | "IN_PROGRESS" | "FAILED" | "COMPLETED_WITH_ERRORS"

local type FlowOperationType = "FLOW_FLUSH" | "FLOW_CAPTURE"

local type ProxyRulePhaseAction = "ALLOW" | "DENY" | "ALERT"

local type ResourceManagedStatus = "MANAGED" | "ACCOUNT"

local type ResourceManagedType = "AWS_MANAGED_THREAT_SIGNATURES" | "AWS_MANAGED_DOMAIN_LISTS" | "ACTIVE_THREAT_DEFENSE" | "PARTNER_MANAGED"

local type RuleGroupRequestPhase = "PRE_DNS" | "PRE_REQ" | "POST_RES"

local type RuleGroupType = "STATELESS" | "STATEFUL" | "STATEFUL_DOMAIN"

local type SubscriptionStatus = "NOT_SUBSCRIBED" | "SUBSCRIBED"

local type TransitGatewayAttachmentStatus = "CREATING" | "DELETING" | "DELETED" | "FAILED" | "ERROR" | "READY" | "PENDING_ACCEPTANCE" | "REJECTING" | "REJECTED"

local record M
    AcceptNetworkFirewallTransitGatewayAttachmentInput: AcceptNetworkFirewallTransitGatewayAttachmentInput
    AcceptNetworkFirewallTransitGatewayAttachmentOutput: AcceptNetworkFirewallTransitGatewayAttachmentOutput
    AssociateAvailabilityZonesInput: AssociateAvailabilityZonesInput
    AssociateAvailabilityZonesOutput: AssociateAvailabilityZonesOutput
    AssociateFirewallPolicyInput: AssociateFirewallPolicyInput
    AssociateFirewallPolicyOutput: AssociateFirewallPolicyOutput
    AssociateSubnetsInput: AssociateSubnetsInput
    AssociateSubnetsOutput: AssociateSubnetsOutput
    AttachRuleGroupsToProxyConfigurationInput: AttachRuleGroupsToProxyConfigurationInput
    AttachRuleGroupsToProxyConfigurationOutput: AttachRuleGroupsToProxyConfigurationOutput
    CreateFirewallInput: CreateFirewallInput
    CreateFirewallOutput: CreateFirewallOutput
    CreateFirewallPolicyInput: CreateFirewallPolicyInput
    CreateFirewallPolicyOutput: CreateFirewallPolicyOutput
    CreateProxyInput: CreateProxyInput
    CreateProxyOutput: CreateProxyOutput
    CreateProxyConfigurationInput: CreateProxyConfigurationInput
    CreateProxyConfigurationOutput: CreateProxyConfigurationOutput
    CreateProxyRuleGroupInput: CreateProxyRuleGroupInput
    CreateProxyRuleGroupOutput: CreateProxyRuleGroupOutput
    CreateProxyRulesInput: CreateProxyRulesInput
    CreateProxyRulesOutput: CreateProxyRulesOutput
    CreateRuleGroupInput: CreateRuleGroupInput
    CreateRuleGroupOutput: CreateRuleGroupOutput
    CreateTLSInspectionConfigurationInput: CreateTLSInspectionConfigurationInput
    CreateTLSInspectionConfigurationOutput: CreateTLSInspectionConfigurationOutput
    CreateVpcEndpointAssociationInput: CreateVpcEndpointAssociationInput
    CreateVpcEndpointAssociationOutput: CreateVpcEndpointAssociationOutput
    DeleteFirewallInput: DeleteFirewallInput
    DeleteFirewallOutput: DeleteFirewallOutput
    DeleteFirewallPolicyInput: DeleteFirewallPolicyInput
    DeleteFirewallPolicyOutput: DeleteFirewallPolicyOutput
    DeleteNetworkFirewallTransitGatewayAttachmentInput: DeleteNetworkFirewallTransitGatewayAttachmentInput
    DeleteNetworkFirewallTransitGatewayAttachmentOutput: DeleteNetworkFirewallTransitGatewayAttachmentOutput
    DeleteProxyInput: DeleteProxyInput
    DeleteProxyOutput: DeleteProxyOutput
    DeleteProxyConfigurationInput: DeleteProxyConfigurationInput
    DeleteProxyConfigurationOutput: DeleteProxyConfigurationOutput
    DeleteProxyRuleGroupInput: DeleteProxyRuleGroupInput
    DeleteProxyRuleGroupOutput: DeleteProxyRuleGroupOutput
    DeleteProxyRulesInput: DeleteProxyRulesInput
    DeleteProxyRulesOutput: DeleteProxyRulesOutput
    DeleteResourcePolicyInput: DeleteResourcePolicyInput
    DeleteResourcePolicyOutput: DeleteResourcePolicyOutput
    DeleteRuleGroupInput: DeleteRuleGroupInput
    DeleteRuleGroupOutput: DeleteRuleGroupOutput
    DeleteTLSInspectionConfigurationInput: DeleteTLSInspectionConfigurationInput
    DeleteTLSInspectionConfigurationOutput: DeleteTLSInspectionConfigurationOutput
    DeleteVpcEndpointAssociationInput: DeleteVpcEndpointAssociationInput
    DeleteVpcEndpointAssociationOutput: DeleteVpcEndpointAssociationOutput
    DescribeFirewallInput: DescribeFirewallInput
    DescribeFirewallOutput: DescribeFirewallOutput
    DescribeFirewallMetadataInput: DescribeFirewallMetadataInput
    DescribeFirewallMetadataOutput: DescribeFirewallMetadataOutput
    DescribeFirewallPolicyInput: DescribeFirewallPolicyInput
    DescribeFirewallPolicyOutput: DescribeFirewallPolicyOutput
    DescribeFlowOperationInput: DescribeFlowOperationInput
    DescribeFlowOperationOutput: DescribeFlowOperationOutput
    DescribeLoggingConfigurationInput: DescribeLoggingConfigurationInput
    DescribeLoggingConfigurationOutput: DescribeLoggingConfigurationOutput
    DescribeProxyInput: DescribeProxyInput
    DescribeProxyOutput: DescribeProxyOutput
    DescribeProxyConfigurationInput: DescribeProxyConfigurationInput
    DescribeProxyConfigurationOutput: DescribeProxyConfigurationOutput
    DescribeProxyRuleInput: DescribeProxyRuleInput
    DescribeProxyRuleOutput: DescribeProxyRuleOutput
    DescribeProxyRuleGroupInput: DescribeProxyRuleGroupInput
    DescribeProxyRuleGroupOutput: DescribeProxyRuleGroupOutput
    DescribeResourcePolicyInput: DescribeResourcePolicyInput
    DescribeResourcePolicyOutput: DescribeResourcePolicyOutput
    DescribeRuleGroupInput: DescribeRuleGroupInput
    DescribeRuleGroupOutput: DescribeRuleGroupOutput
    DescribeRuleGroupMetadataInput: DescribeRuleGroupMetadataInput
    DescribeRuleGroupMetadataOutput: DescribeRuleGroupMetadataOutput
    DescribeRuleGroupSummaryInput: DescribeRuleGroupSummaryInput
    DescribeRuleGroupSummaryOutput: DescribeRuleGroupSummaryOutput
    DescribeTLSInspectionConfigurationInput: DescribeTLSInspectionConfigurationInput
    DescribeTLSInspectionConfigurationOutput: DescribeTLSInspectionConfigurationOutput
    DescribeVpcEndpointAssociationInput: DescribeVpcEndpointAssociationInput
    DescribeVpcEndpointAssociationOutput: DescribeVpcEndpointAssociationOutput
    DetachRuleGroupsFromProxyConfigurationInput: DetachRuleGroupsFromProxyConfigurationInput
    DetachRuleGroupsFromProxyConfigurationOutput: DetachRuleGroupsFromProxyConfigurationOutput
    DisassociateAvailabilityZonesInput: DisassociateAvailabilityZonesInput
    DisassociateAvailabilityZonesOutput: DisassociateAvailabilityZonesOutput
    DisassociateSubnetsInput: DisassociateSubnetsInput
    DisassociateSubnetsOutput: DisassociateSubnetsOutput
    GetAnalysisReportResultsInput: GetAnalysisReportResultsInput
    GetAnalysisReportResultsOutput: GetAnalysisReportResultsOutput
    ListAnalysisReportsInput: ListAnalysisReportsInput
    ListAnalysisReportsOutput: ListAnalysisReportsOutput
    ListFirewallPoliciesInput: ListFirewallPoliciesInput
    ListFirewallPoliciesOutput: ListFirewallPoliciesOutput
    ListFirewallsInput: ListFirewallsInput
    ListFirewallsOutput: ListFirewallsOutput
    ListFlowOperationResultsInput: ListFlowOperationResultsInput
    ListFlowOperationResultsOutput: ListFlowOperationResultsOutput
    ListFlowOperationsInput: ListFlowOperationsInput
    ListFlowOperationsOutput: ListFlowOperationsOutput
    ListProxiesInput: ListProxiesInput
    ListProxiesOutput: ListProxiesOutput
    ListProxyConfigurationsInput: ListProxyConfigurationsInput
    ListProxyConfigurationsOutput: ListProxyConfigurationsOutput
    ListProxyRuleGroupsInput: ListProxyRuleGroupsInput
    ListProxyRuleGroupsOutput: ListProxyRuleGroupsOutput
    ListRuleGroupsInput: ListRuleGroupsInput
    ListRuleGroupsOutput: ListRuleGroupsOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    ListTLSInspectionConfigurationsInput: ListTLSInspectionConfigurationsInput
    ListTLSInspectionConfigurationsOutput: ListTLSInspectionConfigurationsOutput
    ListVpcEndpointAssociationsInput: ListVpcEndpointAssociationsInput
    ListVpcEndpointAssociationsOutput: ListVpcEndpointAssociationsOutput
    PutResourcePolicyInput: PutResourcePolicyInput
    PutResourcePolicyOutput: PutResourcePolicyOutput
    RejectNetworkFirewallTransitGatewayAttachmentInput: RejectNetworkFirewallTransitGatewayAttachmentInput
    RejectNetworkFirewallTransitGatewayAttachmentOutput: RejectNetworkFirewallTransitGatewayAttachmentOutput
    StartAnalysisReportInput: StartAnalysisReportInput
    StartAnalysisReportOutput: StartAnalysisReportOutput
    StartFlowCaptureInput: StartFlowCaptureInput
    StartFlowCaptureOutput: StartFlowCaptureOutput
    StartFlowFlushInput: StartFlowFlushInput
    StartFlowFlushOutput: StartFlowFlushOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateAvailabilityZoneChangeProtectionInput: UpdateAvailabilityZoneChangeProtectionInput
    UpdateAvailabilityZoneChangeProtectionOutput: UpdateAvailabilityZoneChangeProtectionOutput
    UpdateFirewallAnalysisSettingsInput: UpdateFirewallAnalysisSettingsInput
    UpdateFirewallAnalysisSettingsOutput: UpdateFirewallAnalysisSettingsOutput
    UpdateFirewallDeleteProtectionInput: UpdateFirewallDeleteProtectionInput
    UpdateFirewallDeleteProtectionOutput: UpdateFirewallDeleteProtectionOutput
    UpdateFirewallDescriptionInput: UpdateFirewallDescriptionInput
    UpdateFirewallDescriptionOutput: UpdateFirewallDescriptionOutput
    UpdateFirewallEncryptionConfigurationInput: UpdateFirewallEncryptionConfigurationInput
    UpdateFirewallEncryptionConfigurationOutput: UpdateFirewallEncryptionConfigurationOutput
    UpdateFirewallPolicyInput: UpdateFirewallPolicyInput
    UpdateFirewallPolicyOutput: UpdateFirewallPolicyOutput
    UpdateFirewallPolicyChangeProtectionInput: UpdateFirewallPolicyChangeProtectionInput
    UpdateFirewallPolicyChangeProtectionOutput: UpdateFirewallPolicyChangeProtectionOutput
    UpdateLoggingConfigurationInput: UpdateLoggingConfigurationInput
    UpdateLoggingConfigurationOutput: UpdateLoggingConfigurationOutput
    UpdateProxyInput: UpdateProxyInput
    UpdateProxyOutput: UpdateProxyOutput
    UpdateProxyConfigurationInput: UpdateProxyConfigurationInput
    UpdateProxyConfigurationOutput: UpdateProxyConfigurationOutput
    UpdateProxyRuleInput: UpdateProxyRuleInput
    UpdateProxyRuleOutput: UpdateProxyRuleOutput
    UpdateProxyRuleGroupPrioritiesInput: UpdateProxyRuleGroupPrioritiesInput
    UpdateProxyRuleGroupPrioritiesOutput: UpdateProxyRuleGroupPrioritiesOutput
    UpdateProxyRulePrioritiesInput: UpdateProxyRulePrioritiesInput
    UpdateProxyRulePrioritiesOutput: UpdateProxyRulePrioritiesOutput
    UpdateRuleGroupInput: UpdateRuleGroupInput
    UpdateRuleGroupOutput: UpdateRuleGroupOutput
    UpdateSubnetChangeProtectionInput: UpdateSubnetChangeProtectionInput
    UpdateSubnetChangeProtectionOutput: UpdateSubnetChangeProtectionOutput
    UpdateTLSInspectionConfigurationInput: UpdateTLSInspectionConfigurationInput
    UpdateTLSInspectionConfigurationOutput: UpdateTLSInspectionConfigurationOutput
end

return M
