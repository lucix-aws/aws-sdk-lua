-- Code generated by lua-client-codegen. DO NOT EDIT.

local record ActivateMessageTemplateInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the message template. Can be either the ID or the ARN. It cannot contain any qualifier.
    messageTemplateId: string
    -- The version number of the message template version to activate.
    versionNumber: number
end

local record ActivateMessageTemplateOutput
    -- The Amazon Resource Name (ARN) of the message template.
    messageTemplateArn: string
    -- The identifier of the message template.
    messageTemplateId: string
    -- The version number of the message template version that is activated.
    versionNumber: number
end

local record CreateAIAgentInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs..
    clientToken: string
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The name of the AI Agent.
    name: string
    -- The type of the AI Agent.
    type: string
    -- The configuration of the AI Agent.
    configuration: AIAgentConfiguration
    -- The visibility status of the AI Agent.
    visibilityStatus: string
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
    -- The description of the AI Agent.
    description: string
end

local record CreateAIAgentOutput
    -- The data of the created AI Agent.
    aiAgent: AIAgentData
end

local record CreateAIAgentVersionInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Agent.
    aiAgentId: string
    -- The modification time of the AI Agent should be tracked for version creation. This field should be specified to avoid version creation when simultaneous update to the underlying AI Agent are possible. The value should be the modifiedTime returned from the request to create or update an AI Agent so that version creation can fail if an update to the AI Agent post the specified modification time has been made.
    modifiedTime: number
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs..
    clientToken: string
end

local record CreateAIAgentVersionOutput
    -- The data of the AI Agent version.
    aiAgent: AIAgentData
    -- The version number of the AI Agent version.
    versionNumber: number
end

local record CreateAIGuardrailInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs..
    clientToken: string
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The name of the AI Guardrail.
    name: string
    -- The message to return when the AI Guardrail blocks a prompt.
    blockedInputMessaging: string
    -- The message to return when the AI Guardrail blocks a model response.
    blockedOutputsMessaging: string
    -- The visibility status of the AI Guardrail.
    visibilityStatus: string
    -- A description of the AI Guardrail.
    description: string
    -- The topic policies to configure for the AI Guardrail.
    topicPolicyConfig: AIGuardrailTopicPolicyConfig
    -- The content filter policies to configure for the AI Guardrail.
    contentPolicyConfig: AIGuardrailContentPolicyConfig
    -- The word policy you configure for the AI Guardrail.
    wordPolicyConfig: AIGuardrailWordPolicyConfig
    -- The sensitive information policy to configure for the AI Guardrail.
    sensitiveInformationPolicyConfig: AIGuardrailSensitiveInformationPolicyConfig
    -- The contextual grounding policy configuration used to create an AI Guardrail.
    contextualGroundingPolicyConfig: AIGuardrailContextualGroundingPolicyConfig
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
end

local record CreateAIGuardrailOutput
    -- The data of the AI Guardrail.
    aiGuardrail: AIGuardrailData
end

local record CreateAIGuardrailVersionInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Guardrail.
    aiGuardrailId: string
    -- The time the AI Guardrail was last modified.
    modifiedTime: number
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs..
    clientToken: string
end

local record CreateAIGuardrailVersionOutput
    -- The data of the AI Guardrail version.
    aiGuardrail: AIGuardrailData
    -- The version number of the AI Guardrail version.
    versionNumber: number
end

local record CreateAIPromptInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs..
    clientToken: string
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The name of the AI Prompt.
    name: string
    -- The type of this AI Prompt.
    type: string
    -- The configuration of the prompt template for this AI Prompt.
    templateConfiguration: AIPromptTemplateConfiguration
    -- The visibility status of the AI Prompt.
    visibilityStatus: string
    -- The type of the prompt template for this AI Prompt.
    templateType: string
    -- The identifier of the model used for this AI Prompt. For information about which models are supported in each Amazon Web Services Region, see Supported models for system/custom prompts.
    modelId: string
    -- The API Format of the AI Prompt. Recommended values: MESSAGES | TEXT_COMPLETIONS The values ANTHROPIC_CLAUDE_MESSAGES | ANTHROPIC_CLAUDE_TEXT_COMPLETIONS will be deprecated.
    apiFormat: string
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
    -- The description of the AI Prompt.
    description: string
    -- The inference configuration for the AI Prompt being created.
    inferenceConfiguration: AIPromptInferenceConfiguration
end

local record CreateAIPromptOutput
    -- The data of the AI Prompt.
    aiPrompt: AIPromptData
end

local record CreateAIPromptVersionInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI prompt.
    aiPromptId: string
    -- The time the AI Prompt was last modified.
    modifiedTime: number
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs..
    clientToken: string
end

local record CreateAIPromptVersionOutput
    -- The data of the AI Prompt version.
    aiPrompt: AIPromptData
    -- The version number of the AI Prompt version.
    versionNumber: number
end

local record CreateAssistantInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
    clientToken: string
    -- The name of the assistant.
    name: string
    -- The type of assistant.
    type: string
    -- The description of the assistant.
    description: string
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
    -- The configuration information for the customer managed key used for encryption. The customer managed key must have a policy that allows kms:CreateGrant, kms:DescribeKey, kms:Decrypt, and kms:GenerateDataKey* permissions to the IAM identity using the key to invoke Amazon Q in Connect. To use Amazon Q in Connect with chat, the key policy must also allow kms:Decrypt, kms:GenerateDataKey*, and kms:DescribeKey permissions to the connect.amazonaws.com service principal. For more information about setting up a customer managed key for Amazon Q in Connect, see Enable Amazon Q in Connect for your instance.
    serverSideEncryptionConfiguration: ServerSideEncryptionConfiguration
end

local record CreateAssistantOutput
    -- Information about the assistant.
    assistant: AssistantData
end

local record CreateAssistantAssociationInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The type of association.
    associationType: string
    -- The identifier of the associated resource.
    association: AssistantAssociationInputData
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
    clientToken: string
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
end

local record CreateAssistantAssociationOutput
    -- The assistant association.
    assistantAssociation: AssistantAssociationData
end

local record CreateContentInput
    -- The identifier of the knowledge base. This should not be a QUICK_RESPONSES type knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The name of the content. Each piece of content in a knowledge base must have a unique name. You can retrieve a piece of content using only its knowledge base and its name with the SearchContent API.
    name: string
    -- The title of the content. If not set, the title is equal to the name.
    title: string
    -- The URI you want to use for the article. If the knowledge base has a templateUri, setting this argument overrides it for this piece of content.
    overrideLinkOutUri: string
    -- A key/value map to store attributes without affecting tagging or recommendations. For example, when synchronizing data between an external system and Amazon Q in Connect, you can store an external version identifier as metadata to utilize for determining drift.
    metadata: {string : string}
    -- A pointer to the uploaded asset. This value is returned by StartContentUpload.
    uploadId: string
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
    clientToken: string
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
end

local record CreateContentOutput
    -- The content.
    content: ContentData
end

local record CreateContentAssociationInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
    clientToken: string
    -- The identifier of the knowledge base.
    knowledgeBaseId: string
    -- The identifier of the content.
    contentId: string
    -- The type of association.
    associationType: string
    -- The identifier of the associated resource.
    association: ContentAssociationContents
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
end

local record CreateContentAssociationOutput
    -- The association between Amazon Q in Connect content and another resource.
    contentAssociation: ContentAssociationData
end

local record CreateKnowledgeBaseInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
    clientToken: string
    -- The name of the knowledge base.
    name: string
    -- The type of knowledge base. Only CUSTOM knowledge bases allow you to upload your own content. EXTERNAL knowledge bases support integrations with third-party systems whose content is synchronized automatically.
    knowledgeBaseType: string
    -- The source of the knowledge base content. Only set this argument for EXTERNAL or Managed knowledge bases.
    sourceConfiguration: SourceConfiguration
    -- Information about how to render the content.
    renderingConfiguration: RenderingConfiguration
    -- Contains details about how to ingest the documents in a data source.
    vectorIngestionConfiguration: VectorIngestionConfiguration
    -- The configuration information for the customer managed key used for encryption. This KMS key must have a policy that allows kms:CreateGrant, kms:DescribeKey, kms:Decrypt, and kms:GenerateDataKey* permissions to the IAM identity using the key to invoke Amazon Q in Connect. For more information about setting up a customer managed key for Amazon Q in Connect, see Enable Amazon Q in Connect for your instance.
    serverSideEncryptionConfiguration: ServerSideEncryptionConfiguration
    -- The description.
    description: string
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
end

local record CreateKnowledgeBaseOutput
    -- The knowledge base.
    knowledgeBase: KnowledgeBaseData
end

local record CreateMessageTemplateInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The name of the message template.
    name: string
    -- The content of the message template.
    content: MessageTemplateContentProvider
    -- The description of the message template.
    description: string
    -- The channel subtype this message template applies to.
    channelSubtype: string
    -- The language code value for the language in which the quick response is written. The supported language codes include de_DE, en_US, es_ES, fr_FR, id_ID, it_IT, ja_JP, ko_KR, pt_BR, zh_CN, zh_TW
    language: string
    -- The source configuration of the message template. Only set this argument for WHATSAPP channel subtype.
    sourceConfiguration: MessageTemplateSourceConfiguration
    -- An object that specifies the default values to use for variables in the message template. This object contains different categories of key-value pairs. Each key defines a variable or placeholder in the message template. The corresponding value defines the default value for that variable.
    defaultAttributes: MessageTemplateAttributes
    groupingConfiguration: GroupingConfiguration
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
    clientToken: string
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
end

local record CreateMessageTemplateOutput
    -- The message template.
    messageTemplate: MessageTemplateData
end

local record CreateMessageTemplateAttachmentInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the message template. Can be either the ID or the ARN. It cannot contain any qualifier.
    messageTemplateId: string
    -- The presentation information for the attachment file.
    contentDisposition: string
    -- The name of the attachment file being uploaded. The name should include the file extension.
    name: string
    -- The body of the attachment file being uploaded. It should be encoded using base64 encoding.
    body: string
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
    clientToken: string
end

local record CreateMessageTemplateAttachmentOutput
    -- The message template attachment.
    attachment: MessageTemplateAttachment
end

local record CreateMessageTemplateVersionInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the message template. Can be either the ID or the ARN. It cannot contain any qualifier.
    messageTemplateId: string
    -- The checksum value of the message template content that is referenced by the $LATEST qualifier. It can be returned in MessageTemplateData or ExtendedMessageTemplateData. It’s calculated by content, language, defaultAttributes and Attachments of the message template. If not supplied, the message template version will be created based on the message template content that is referenced by the $LATEST qualifier by default.
    messageTemplateContentSha256: string
end

local record CreateMessageTemplateVersionOutput
    -- The message template.
    messageTemplate: ExtendedMessageTemplateData
end

local record CreateQuickResponseInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The name of the quick response.
    name: string
    -- The content of the quick response.
    content: QuickResponseDataProvider
    -- The media type of the quick response content. Use application/x.quickresponse;format=plain for a quick response written in plain text. Use application/x.quickresponse;format=markdown for a quick response written in richtext.
    contentType: string
    -- The configuration information of the user groups that the quick response is accessible to.
    groupingConfiguration: GroupingConfiguration
    -- The description of the quick response.
    description: string
    -- The shortcut key of the quick response. The value should be unique across the knowledge base.
    shortcutKey: string
    -- Whether the quick response is active.
    isActive: boolean
    -- The Amazon Connect channels this quick response applies to.
    channels: {string}
    -- The language code value for the language in which the quick response is written. The supported language codes include de_DE, en_US, es_ES, fr_FR, id_ID, it_IT, ja_JP, ko_KR, pt_BR, zh_CN, zh_TW
    language: string
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
    clientToken: string
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
end

local record CreateQuickResponseOutput
    -- The quick response.
    quickResponse: QuickResponseData
end

local record CreateSessionInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs.
    clientToken: string
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The name of the session.
    name: string
    -- The description.
    description: string
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
    -- An object that can be used to specify Tag conditions.
    tagFilter: TagFilter
    -- The configuration of the AI Agents (mapped by AI Agent Type to AI Agent version) that should be used by Amazon Q in Connect for this Session.
    aiAgentConfiguration: {string : AIAgentConfigurationData}
    -- The Amazon Resource Name (ARN) of the email contact in Amazon Connect. Used to retrieve email content and establish session context for AI-powered email assistance.
    contactArn: string
    -- The list of orchestrator configurations for the session being created.
    orchestratorConfigurationList: {OrchestratorConfigurationEntry}
    -- The list of orchestrator configurations to remove from the session.
    removeOrchestratorConfigurationList: boolean
end

local record CreateSessionOutput
    -- The session.
    session: SessionData
end

local record DeactivateMessageTemplateInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the message template. Can be either the ID or the ARN. It cannot contain any qualifier.
    messageTemplateId: string
    -- The version number of the message template version to deactivate.
    versionNumber: number
end

local record DeactivateMessageTemplateOutput
    -- The Amazon Resource Name (ARN) of the message template.
    messageTemplateArn: string
    -- The identifier of the message template.
    messageTemplateId: string
    -- The version number of the message template version that has been deactivated.
    versionNumber: number
end

local record DeleteAIAgentInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Agent. Can be either the ID or the ARN. URLs cannot contain the ARN.
    aiAgentId: string
end

local record DeleteAIAgentOutput
end

local record DeleteAIAgentVersionInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Agent. Can be either the ID or the ARN. URLs cannot contain the ARN.
    aiAgentId: string
    -- The version number of the AI Agent version.
    versionNumber: number
end

local record DeleteAIAgentVersionOutput
end

local record DeleteAIGuardrailInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Guardrail. Can be either the ID or the ARN. URLs cannot contain the ARN.
    aiGuardrailId: string
end

local record DeleteAIGuardrailOutput
end

local record DeleteAIGuardrailVersionInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Guardrail.
    aiGuardrailId: string
    -- The version number of the AI Guardrail version to be deleted.
    versionNumber: number
end

local record DeleteAIGuardrailVersionOutput
end

local record DeleteAIPromptInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI prompt. Can be either the ID or the ARN. URLs cannot contain the ARN.
    aiPromptId: string
end

local record DeleteAIPromptOutput
end

local record DeleteAIPromptVersionInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI prompt.
    aiPromptId: string
    -- The version number of the AI Prompt version to be deleted.
    versionNumber: number
end

local record DeleteAIPromptVersionOutput
end

local record DeleteAssistantInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
end

local record DeleteAssistantOutput
end

local record DeleteAssistantAssociationInput
    -- The identifier of the assistant association. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantAssociationId: string
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
end

local record DeleteAssistantAssociationOutput
end

local record DeleteContentInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the content. Can be either the ID or the ARN. URLs cannot contain the ARN.
    contentId: string
end

local record DeleteContentOutput
end

local record DeleteContentAssociationInput
    -- The identifier of the knowledge base.
    knowledgeBaseId: string
    -- The identifier of the content.
    contentId: string
    -- The identifier of the content association. Can be either the ID or the ARN. URLs cannot contain the ARN.
    contentAssociationId: string
end

local record DeleteContentAssociationOutput
end

local record DeleteImportJobInput
    -- The identifier of the knowledge base.
    knowledgeBaseId: string
    -- The identifier of the import job to be deleted.
    importJobId: string
end

local record DeleteImportJobOutput
end

local record DeleteKnowledgeBaseInput
    -- The knowledge base to delete content from. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
end

local record DeleteKnowledgeBaseOutput
end

local record DeleteMessageTemplateInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the message template. Can be either the ID or the ARN.
    messageTemplateId: string
end

local record DeleteMessageTemplateOutput
end

local record DeleteMessageTemplateAttachmentInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the message template. Can be either the ID or the ARN. It cannot contain any qualifier.
    messageTemplateId: string
    -- The identifier of the attachment file.
    attachmentId: string
end

local record DeleteMessageTemplateAttachmentOutput
end

local record DeleteQuickResponseInput
    -- The knowledge base from which the quick response is deleted. The identifier of the knowledge base.
    knowledgeBaseId: string
    -- The identifier of the quick response to delete.
    quickResponseId: string
end

local record DeleteQuickResponseOutput
end

local record GetAIAgentInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Agent (with or without a version qualifier). Can be either the ID or the ARN. URLs cannot contain the ARN.
    aiAgentId: string
end

local record GetAIAgentOutput
    -- The data of the AI Agent.
    aiAgent: AIAgentData
    -- The version number of the AI Agent version (returned if an AI Agent version was specified via use of a qualifier for the aiAgentId on the request).
    versionNumber: number
end

local record GetAIGuardrailInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Guardrail.
    aiGuardrailId: string
end

local record GetAIGuardrailOutput
    -- The data of the AI Guardrail.
    aiGuardrail: AIGuardrailData
    -- The version number of the AI Guardrail version (returned if an AI Guardrail version was specified via use of a qualifier for the aiGuardrailId on the request).
    versionNumber: number
end

local record GetAIPromptInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI prompt.
    aiPromptId: string
end

local record GetAIPromptOutput
    -- The data of the AI Prompt.
    aiPrompt: AIPromptData
    -- The version number of the AI Prompt version (returned if an AI Prompt version was specified via use of a qualifier for the aiPromptId on the request).
    versionNumber: number
end

local record GetAssistantInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
end

local record GetAssistantOutput
    -- Information about the assistant.
    assistant: AssistantData
end

local record GetAssistantAssociationInput
    -- The identifier of the assistant association. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantAssociationId: string
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
end

local record GetAssistantAssociationOutput
    -- The assistant association.
    assistantAssociation: AssistantAssociationData
end

local record GetContentInput
    -- The identifier of the content. Can be either the ID or the ARN. URLs cannot contain the ARN.
    contentId: string
    -- The identifier of the knowledge base. This should not be a QUICK_RESPONSES type knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
end

local record GetContentOutput
    -- The content.
    content: ContentData
end

local record GetContentAssociationInput
    -- The identifier of the knowledge base.
    knowledgeBaseId: string
    -- The identifier of the content.
    contentId: string
    -- The identifier of the content association. Can be either the ID or the ARN. URLs cannot contain the ARN.
    contentAssociationId: string
end

local record GetContentAssociationOutput
    -- The association between Amazon Q in Connect content and another resource.
    contentAssociation: ContentAssociationData
end

local record GetContentSummaryInput
    -- The identifier of the content. Can be either the ID or the ARN. URLs cannot contain the ARN.
    contentId: string
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
end

local record GetContentSummaryOutput
    -- The content summary.
    contentSummary: ContentSummary
end

local record GetImportJobInput
    -- The identifier of the import job to retrieve.
    importJobId: string
    -- The identifier of the knowledge base that the import job belongs to.
    knowledgeBaseId: string
end

local record GetImportJobOutput
    -- The import job.
    importJob: ImportJobData
end

local record GetKnowledgeBaseInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
end

local record GetKnowledgeBaseOutput
    -- The knowledge base.
    knowledgeBase: KnowledgeBaseData
end

local record GetMessageTemplateInput
    -- The identifier of the message template. Can be either the ID or the ARN.
    messageTemplateId: string
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
end

local record GetMessageTemplateOutput
    -- The message template.
    messageTemplate: ExtendedMessageTemplateData
end

local record GetNextMessageInput
    -- The identifier of the Amazon Q in Connect assistant.
    assistantId: string
    -- The identifier of the Amazon Q in Connect session.
    sessionId: string
    -- The token for the next message. Use the value returned in the SendMessage or previous response in the next request to retrieve the next message.
    nextMessageToken: string
end

local record GetNextMessageOutput
    -- The type of message response.
    type: string
    -- The message response to the requested message.
    response: MessageOutput
    -- The identifier of the submitted message.
    requestMessageId: string
    -- The state of current conversation.
    conversationState: ConversationState
    -- The token for the next message.
    nextMessageToken: string
    -- The conversation data stored on an Amazon Q in Connect Session.
    conversationSessionData: {RuntimeSessionData}
    -- Indicates whether the chunked response has been terminated.
    chunkedResponseTerminated: boolean
end

local record GetQuickResponseInput
    -- The identifier of the quick response.
    quickResponseId: string
    -- The identifier of the knowledge base. This should be a QUICK_RESPONSES type knowledge base.
    knowledgeBaseId: string
end

local record GetQuickResponseOutput
    -- The quick response.
    quickResponse: QuickResponseData
end

local record GetRecommendationsInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the session. Can be either the ID or the ARN. URLs cannot contain the ARN.
    sessionId: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The duration (in seconds) for which the call waits for a recommendation to be made available before returning. If a recommendation is available, the call returns sooner than WaitTimeSeconds. If no messages are available and the wait time expires, the call returns successfully with an empty list.
    waitTimeSeconds: number
    -- The token for the next set of chunks. Use the value returned in the previous response in the next request to retrieve the next set of chunks.
    nextChunkToken: string
    -- The type of recommendation being requested.
    recommendationType: string
end

local record GetRecommendationsOutput
    -- The recommendations.
    recommendations: {RecommendationData}
    -- The triggers corresponding to recommendations.
    triggers: {RecommendationTrigger}
end

local record GetSessionInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the session. Can be either the ID or the ARN. URLs cannot contain the ARN.
    sessionId: string
end

local record GetSessionOutput
    -- The session.
    session: SessionData
end

local record ListAIAgentsInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The origin of the AI Agents to be listed. SYSTEM for a default AI Agent created by Q in Connect or CUSTOMER for an AI Agent created by calling AI Agent creation APIs.
    origin: string
end

local record ListAIAgentsOutput
    -- The summaries of AI Agents.
    aiAgentSummaries: {AIAgentSummary}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListAIAgentVersionsInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Agent for which versions are to be listed.
    aiAgentId: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The origin of the AI Agent versions to be listed. SYSTEM for a default AI Agent created by Q in Connect or CUSTOMER for an AI Agent created by calling AI Agent creation APIs.
    origin: string
end

local record ListAIAgentVersionsOutput
    -- The summaries of AI Agent versions.
    aiAgentVersionSummaries: {AIAgentVersionSummary}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListAIGuardrailsInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
end

local record ListAIGuardrailsOutput
    -- The summaries of the AI Guardrails.
    aiGuardrailSummaries: {AIGuardrailSummary}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListAIGuardrailVersionsInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Guardrail for which versions are to be listed.
    aiGuardrailId: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
end

local record ListAIGuardrailVersionsOutput
    -- The summaries of the AI Guardrail versions.
    aiGuardrailVersionSummaries: {AIGuardrailVersionSummary}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListAIPromptsInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The origin of the AI Prompts to be listed. SYSTEM for a default AI Agent created by Q in Connect or CUSTOMER for an AI Agent created by calling AI Agent creation APIs.
    origin: string
end

local record ListAIPromptsOutput
    -- The summaries of the AI Prompts.
    aiPromptSummaries: {AIPromptSummary}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListAIPromptVersionsInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI prompt for which versions are to be listed.
    aiPromptId: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The origin of the AI Prompt versions to be listed. SYSTEM for a default AI Agent created by Q in Connect or CUSTOMER for an AI Agent created by calling AI Agent creation APIs.
    origin: string
end

local record ListAIPromptVersionsOutput
    -- The summaries of the AI Prompt versions.
    aiPromptVersionSummaries: {AIPromptVersionSummary}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListAssistantAssociationsInput
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
end

local record ListAssistantAssociationsOutput
    -- Summary information about assistant associations.
    assistantAssociationSummaries: {AssistantAssociationSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListAssistantsInput
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
end

local record ListAssistantsOutput
    -- Information about the assistants.
    assistantSummaries: {AssistantSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListContentAssociationsInput
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The identifier of the knowledge base.
    knowledgeBaseId: string
    -- The identifier of the content.
    contentId: string
end

local record ListContentAssociationsOutput
    -- Summary information about content associations.
    contentAssociationSummaries: {ContentAssociationSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListContentsInput
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The identifier of the knowledge base. This should not be a QUICK_RESPONSES type knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
end

local record ListContentsOutput
    -- Information about the content.
    contentSummaries: {ContentSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListImportJobsInput
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
end

local record ListImportJobsOutput
    -- Summary information about the import jobs.
    importJobSummaries: {ImportJobSummary}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListKnowledgeBasesInput
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
end

local record ListKnowledgeBasesOutput
    -- Information about the knowledge bases.
    knowledgeBaseSummaries: {KnowledgeBaseSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListMessagesInput
    -- The identifier of the Amazon Q in Connect assistant.
    assistantId: string
    -- The identifier of the Amazon Q in Connect session.
    sessionId: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The filter criteria for listing messages.
    filter: string
end

local record ListMessagesOutput
    -- The message information.
    messages: {MessageOutput}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListMessageTemplatesInput
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
end

local record ListMessageTemplatesOutput
    -- Summary information about the message template.
    messageTemplateSummaries: {MessageTemplateSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListMessageTemplateVersionsInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the message template. Can be either the ID or the ARN. It cannot contain any qualifier.
    messageTemplateId: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
end

local record ListMessageTemplateVersionsOutput
    -- Summary information about the versions of a message template.
    messageTemplateVersionSummaries: {MessageTemplateVersionSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListQuickResponsesInput
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
end

local record ListQuickResponsesOutput
    -- Summary information about the quick responses.
    quickResponseSummaries: {QuickResponseSummary}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListSpansInput
    -- UUID or ARN of the Connect AI Assistant resource
    assistantId: string
    -- UUID or ARN of the Connect AI Session resource
    sessionId: string
    -- Pagination token for retrieving the next page of results
    nextToken: string
    -- Maximum number of spans to return per page
    maxResults: number
end

local record ListSpansOutput
    -- Array of span objects for the session
    spans: {Span}
    -- Pagination token for retrieving additional results
    nextToken: string
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
end

local record NotifyRecommendationsReceivedInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the session. Can be either the ID or the ARN. URLs cannot contain the ARN.
    sessionId: string
    -- The identifiers of the recommendations.
    recommendationIds: {string}
end

local record NotifyRecommendationsReceivedOutput
    -- The identifiers of the recommendations.
    recommendationIds: {string}
    -- The identifiers of recommendations that are causing errors.
    errors: {NotifyRecommendationsReceivedError}
end

local record PutFeedbackInput
    -- The identifier of the Amazon Q in Connect assistant.
    assistantId: string
    -- The identifier of the feedback target.
    targetId: string
    -- The type of the feedback target.
    targetType: string
    -- Information about the feedback provided.
    contentFeedback: ContentFeedbackData
end

local record PutFeedbackOutput
    -- The identifier of the Amazon Q in Connect assistant.
    assistantId: string
    -- The Amazon Resource Name (ARN) of the Amazon Q in Connect assistant.
    assistantArn: string
    -- The identifier of the feedback target.
    targetId: string
    -- The type of the feedback target.
    targetType: string
    -- Information about the feedback provided.
    contentFeedback: ContentFeedbackData
end

local record QueryAssistantInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The text to search for.
    queryText: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The identifier of the Amazon Q in Connect session. Can be either the ID or the ARN. URLs cannot contain the ARN.
    sessionId: string
    -- Information about how to query content.
    queryCondition: {QueryCondition}
    -- Information about the query.
    queryInputData: QueryInputData
    -- The search type to be used against the Knowledge Base for this request. The values can be SEMANTIC which uses vector embeddings or HYBRID which use vector embeddings and raw text.
    overrideKnowledgeBaseSearchType: string
end

local record QueryAssistantOutput
    -- The results of the query.
    results: {ResultData}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record RemoveAssistantAIAgentInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The type of the AI Agent being removed for use by default from the Amazon Q in Connect Assistant.
    aiAgentType: string
    -- The orchestrator use case for the AI Agent being removed.
    orchestratorUseCase: string
end

local record RemoveAssistantAIAgentOutput
end

local record RemoveKnowledgeBaseTemplateUriInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
end

local record RemoveKnowledgeBaseTemplateUriOutput
end

local record RenderMessageTemplateInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the message template. Can be either the ID or the ARN.
    messageTemplateId: string
    -- An object that specifies the values to use for variables in the message template. This object contains different categories of key-value pairs. Each key defines a variable or placeholder in the message template. The corresponding value defines the value for that variable.
    attributes: MessageTemplateAttributes
end

local record RenderMessageTemplateOutput
    -- The content of the message template.
    content: MessageTemplateContentProvider
    -- The source configuration of the message template.
    sourceConfigurationSummary: MessageTemplateSourceConfigurationSummary
    -- The attribute keys that are not resolved.
    attributesNotInterpolated: {string}
    -- The message template attachments.
    attachments: {MessageTemplateAttachment}
end

local record RetrieveInput
    -- The identifier of the Amazon Q in Connect assistant for content retrieval.
    assistantId: string
    -- The configuration for the content retrieval operation.
    retrievalConfiguration: RetrievalConfiguration
    -- The query for content retrieval.
    retrievalQuery: string
end

local record RetrieveOutput
    -- The results of the content retrieval operation.
    results: {RetrieveResult}
end

local record SearchContentInput
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The identifier of the knowledge base. This should not be a QUICK_RESPONSES type knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The search expression to filter results.
    searchExpression: SearchExpression
end

local record SearchContentOutput
    -- Summary information about the content.
    contentSummaries: {ContentSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record SearchMessageTemplatesInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The search expression for querying the message template.
    searchExpression: MessageTemplateSearchExpression
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
end

local record SearchMessageTemplatesOutput
    -- The results of the message template search.
    results: {MessageTemplateSearchResultData}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record SearchQuickResponsesInput
    -- The identifier of the knowledge base. This should be a QUICK_RESPONSES type knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The search expression for querying the quick response.
    searchExpression: QuickResponseSearchExpression
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The user-defined Amazon Connect contact attributes to be resolved when search results are returned.
    attributes: {string : string}
end

local record SearchQuickResponsesOutput
    -- The results of the quick response search.
    results: {QuickResponseSearchResultData}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record SearchSessionsInput
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The search expression to filter results.
    searchExpression: SearchExpression
end

local record SearchSessionsOutput
    -- Summary information about the sessions.
    sessionSummaries: {SessionSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record SendMessageInput
    -- The identifier of the Amazon Q in Connect assistant.
    assistantId: string
    -- The identifier of the Amazon Q in Connect session.
    sessionId: string
    -- The message type.
    type: string
    -- The message data to submit to the Amazon Q in Connect session.
    message: MessageInput
    -- The identifier of the AI Agent to use for processing the message.
    aiAgentId: string
    -- The conversation context before the Amazon Q in Connect session.
    conversationContext: ConversationContext
    -- The configuration of the SendMessage request.
    configuration: MessageConfiguration
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the AWS SDK populates this field.For more information about idempotency, see Making retries safe with idempotent APIs.
    clientToken: string
    -- The orchestrator use case for message processing.
    orchestratorUseCase: string
    -- Additional metadata for the message.
    metadata: {string : string}
    -- Request identifier from the origin system, used for end-to-end tracing across spans.
    originRequestId: string
end

local record SendMessageOutput
    -- The identifier of the submitted message.
    requestMessageId: string
    -- The configuration of the SendMessage request.
    configuration: MessageConfiguration
    -- The token for the next message, used by GetNextMessage.
    nextMessageToken: string
end

local record StartContentUploadInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The type of content to upload.
    contentType: string
    -- The expected expiration time of the generated presigned URL, specified in minutes.
    presignedUrlTimeToLive: number
end

local record StartContentUploadOutput
    -- The identifier of the upload.
    uploadId: string
    -- The URL of the upload.
    url: string
    -- The expiration time of the URL as an epoch timestamp.
    urlExpiry: number
    -- The headers to include in the upload.
    headersToInclude: {string : string}
end

local record StartImportJobInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN. For importing Amazon Q in Connect quick responses, this should be a QUICK_RESPONSES type knowledge base.
    knowledgeBaseId: string
    -- The type of the import job. For importing quick response resource, set the value to QUICK_RESPONSES.
    importJobType: string
    -- A pointer to the uploaded asset. This value is returned by StartContentUpload.
    uploadId: string
    -- The tags used to organize, track, or control access for this resource.
    clientToken: string
    -- The metadata fields of the imported Amazon Q in Connect resources.
    metadata: {string : string}
    -- The configuration information of the external source that the resource data are imported from.
    externalSourceConfiguration: ExternalSourceConfiguration
end

local record StartImportJobOutput
    -- The import job.
    importJob: ImportJobData
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
    -- The tags used to organize, track, or control access for this resource.
    tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
    -- The tag keys.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateAIAgentInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs..
    clientToken: string
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Agent.
    aiAgentId: string
    -- The visbility status of the Amazon Q in Connect AI Agent.
    visibilityStatus: string
    -- The configuration of the Amazon Q in Connect AI Agent.
    configuration: AIAgentConfiguration
    -- The description of the Amazon Q in Connect AI Agent.
    description: string
end

local record UpdateAIAgentOutput
    -- The data of the updated Amazon Q in Connect AI Agent.
    aiAgent: AIAgentData
end

local record UpdateAIGuardrailInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs..
    clientToken: string
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Guardrail.
    aiGuardrailId: string
    -- The visibility status of the Amazon Q in Connect AI Guardrail.
    visibilityStatus: string
    -- The message to return when the AI Guardrail blocks a prompt.
    blockedInputMessaging: string
    -- The message to return when the AI Guardrail blocks a model response.
    blockedOutputsMessaging: string
    -- A description of the AI Guardrail.
    description: string
    -- The topic policies to configure for the AI Guardrail.
    topicPolicyConfig: AIGuardrailTopicPolicyConfig
    -- The content filter policies to configure for the AI Guardrail.
    contentPolicyConfig: AIGuardrailContentPolicyConfig
    -- The word policy you configure for the AI Guardrail.
    wordPolicyConfig: AIGuardrailWordPolicyConfig
    -- The sensitive information policy to configure for the AI Guardrail.
    sensitiveInformationPolicyConfig: AIGuardrailSensitiveInformationPolicyConfig
    -- The contextual grounding policy configuration used to create an AI Guardrail.
    contextualGroundingPolicyConfig: AIGuardrailContextualGroundingPolicyConfig
end

local record UpdateAIGuardrailOutput
    -- The data of the updated Amazon Q in Connect AI Guardrail.
    aiGuardrail: AIGuardrailData
end

local record UpdateAIPromptInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If not provided, the Amazon Web Services SDK populates this field. For more information about idempotency, see Making retries safe with idempotent APIs..
    clientToken: string
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the Amazon Q in Connect AI Prompt.
    aiPromptId: string
    -- The visibility status of the Amazon Q in Connect AI prompt.
    visibilityStatus: string
    -- The configuration of the prompt template for this AI Prompt.
    templateConfiguration: AIPromptTemplateConfiguration
    -- The description of the Amazon Q in Connect AI Prompt.
    description: string
    -- The identifier of the model used for this AI Prompt. For information about which models are supported in each Amazon Web Services Region, see Supported models for system/custom prompts.
    modelId: string
    -- The updated inference configuration for the AI Prompt.
    inferenceConfiguration: AIPromptInferenceConfiguration
end

local record UpdateAIPromptOutput
    -- The data of the updated Amazon Q in Connect AI Prompt.
    aiPrompt: AIPromptData
end

local record UpdateAssistantAIAgentInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The type of the AI Agent being updated for use by default on the Amazon Q in Connect Assistant.
    aiAgentType: string
    -- The configuration of the AI Agent being updated for use by default on the Amazon Q in Connect Assistant.
    configuration: AIAgentConfigurationData
    -- The orchestrator use case for the AI Agent being added.
    orchestratorUseCase: string
end

local record UpdateAssistantAIAgentOutput
    assistant: AssistantData
end

local record UpdateContentInput
    -- The identifier of the knowledge base. This should not be a QUICK_RESPONSES type knowledge base. Can be either the ID or the ARN
    knowledgeBaseId: string
    -- The identifier of the content. Can be either the ID or the ARN. URLs cannot contain the ARN.
    contentId: string
    -- The revisionId of the content resource to update, taken from an earlier call to GetContent, GetContentSummary, SearchContent, or ListContents. If included, this argument acts as an optimistic lock to ensure content was not modified since it was last read. If it has been modified, this API throws a PreconditionFailedException.
    revisionId: string
    -- The title of the content.
    title: string
    -- The URI for the article. If the knowledge base has a templateUri, setting this argument overrides it for this piece of content. To remove an existing overrideLinkOurUri, exclude this argument and set removeOverrideLinkOutUri to true.
    overrideLinkOutUri: string
    -- Unset the existing overrideLinkOutUri if it exists.
    removeOverrideLinkOutUri: boolean
    -- A key/value map to store attributes without affecting tagging or recommendations. For example, when synchronizing data between an external system and Amazon Q in Connect, you can store an external version identifier as metadata to utilize for determining drift.
    metadata: {string : string}
    -- A pointer to the uploaded asset. This value is returned by StartContentUpload.
    uploadId: string
end

local record UpdateContentOutput
    -- The content.
    content: ContentData
end

local record UpdateKnowledgeBaseTemplateUriInput
    -- The identifier of the knowledge base. This should not be a QUICK_RESPONSES type knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The template URI to update.
    templateUri: string
end

local record UpdateKnowledgeBaseTemplateUriOutput
    -- The knowledge base to update.
    knowledgeBase: KnowledgeBaseData
end

local record UpdateMessageTemplateInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the message template. Can be either the ID or the ARN. It cannot contain any qualifier.
    messageTemplateId: string
    -- The content of the message template.
    content: MessageTemplateContentProvider
    -- The language code value for the language in which the quick response is written. The supported language codes include de_DE, en_US, es_ES, fr_FR, id_ID, it_IT, ja_JP, ko_KR, pt_BR, zh_CN, zh_TW
    language: string
    -- The source configuration of the message template. Only set this argument for WHATSAPP channel subtype.
    sourceConfiguration: MessageTemplateSourceConfiguration
    -- An object that specifies the default values to use for variables in the message template. This object contains different categories of key-value pairs. Each key defines a variable or placeholder in the message template. The corresponding value defines the default value for that variable.
    defaultAttributes: MessageTemplateAttributes
end

local record UpdateMessageTemplateOutput
    -- The message template.
    messageTemplate: MessageTemplateData
end

local record UpdateMessageTemplateMetadataInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the message template. Can be either the ID or the ARN. It cannot contain any qualifier.
    messageTemplateId: string
    -- The name of the message template.
    name: string
    -- The description of the message template.
    description: string
    groupingConfiguration: GroupingConfiguration
end

local record UpdateMessageTemplateMetadataOutput
    -- The message template.
    messageTemplate: MessageTemplateData
end

local record UpdateQuickResponseInput
    -- The identifier of the knowledge base. Can be either the ID or the ARN. URLs cannot contain the ARN.
    knowledgeBaseId: string
    -- The identifier of the quick response.
    quickResponseId: string
    -- The name of the quick response.
    name: string
    -- The updated content of the quick response.
    content: QuickResponseDataProvider
    -- The media type of the quick response content. Use application/x.quickresponse;format=plain for quick response written in plain text. Use application/x.quickresponse;format=markdown for quick response written in richtext.
    contentType: string
    -- The updated grouping configuration of the quick response.
    groupingConfiguration: GroupingConfiguration
    -- Whether to remove the grouping configuration of the quick response.
    removeGroupingConfiguration: boolean
    -- The updated description of the quick response.
    description: string
    -- Whether to remove the description from the quick response.
    removeDescription: boolean
    -- The shortcut key of the quick response. The value should be unique across the knowledge base.
    shortcutKey: string
    -- Whether to remove the shortcut key of the quick response.
    removeShortcutKey: boolean
    -- Whether the quick response is active.
    isActive: boolean
    -- The Amazon Connect contact channels this quick response applies to. The supported contact channel types include Chat.
    channels: {string}
    -- The language code value for the language in which the quick response is written. The supported language codes include de_DE, en_US, es_ES, fr_FR, id_ID, it_IT, ja_JP, ko_KR, pt_BR, zh_CN, zh_TW
    language: string
end

local record UpdateQuickResponseOutput
    -- The quick response.
    quickResponse: QuickResponseData
end

local record UpdateSessionInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the session. Can be either the ID or the ARN. URLs cannot contain the ARN.
    sessionId: string
    -- The description.
    description: string
    -- An object that can be used to specify Tag conditions.
    tagFilter: TagFilter
    -- The configuration of the AI Agents (mapped by AI Agent Type to AI Agent version) that should be used by Amazon Q in Connect for this Session.
    aiAgentConfiguration: {string : AIAgentConfigurationData}
    -- The updated list of orchestrator configurations for the session.
    orchestratorConfigurationList: {OrchestratorConfigurationEntry}
    -- The list of orchestrator configurations to remove from the session.
    removeOrchestratorConfigurationList: boolean
end

local record UpdateSessionOutput
    session: SessionData
end

local record UpdateSessionDataInput
    -- The identifier of the Amazon Q in Connect assistant. Can be either the ID or the ARN. URLs cannot contain the ARN.
    assistantId: string
    -- The identifier of the session. Can be either the ID or the ARN. URLs cannot contain the ARN.
    sessionId: string
    -- The namespace into which the session data is stored. Supported namespaces are: Custom
    namespace: string
    -- The data stored on the Amazon Q in Connect Session.
    data: {RuntimeSessionData}
end

local record UpdateSessionDataOutput
    -- The Amazon Resource Name (ARN) of the session.
    sessionArn: string
    -- The identifier of the session.
    sessionId: string
    -- The namespace into which the session data is stored. Supported namespaces are: Custom
    namespace: string
    -- Data stored in the session.
    data: {RuntimeSessionData}
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The request failed because it depends on another request that failed.
local record DependencyFailedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The server has a failure of processing the message
local record UnprocessableContentException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request reached the service more than 15 minutes after the date stamp on the request or more than 15 minutes after the request expiration date (such as for pre-signed URLs), or the date stamp on the request is more than 15 minutes in the future.
local record RequestTimeoutException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The request failed because it depends on another request that failed.
local record DependencyFailedException
    message: string
end

-- The request reached the service more than 15 minutes after the date stamp on the request or more than 15 minutes after the request expiration date (such as for pre-signed URLs), or the date stamp on the request is more than 15 minutes in the future.
local record RequestTimeoutException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request reached the service more than 15 minutes after the date stamp on the request or more than 15 minutes after the request expiration date (such as for pre-signed URLs), or the date stamp on the request is more than 15 minutes in the future.
local record RequestTimeoutException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The request failed because it depends on another request that failed.
local record DependencyFailedException
    message: string
end

-- The request reached the service more than 15 minutes after the date stamp on the request or more than 15 minutes after the request expiration date (such as for pre-signed URLs), or the date stamp on the request is more than 15 minutes in the future.
local record RequestTimeoutException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You've exceeded your service quota. To perform the requested action, remove some of the relevant resources, or use service quotas to request a service quota increase.
local record ServiceQuotaExceededException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- Amazon Q in Connect throws this exception if you have too many tags in your tag set.
local record TooManyTagsException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The provided revisionId does not match, indicating the content has been modified since it was last read.
local record PreconditionFailedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- The throttling limit has been exceeded.
local record ThrottlingException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request could not be processed because of conflict in the current state of the resource. For example, if you're using a Create API (such as CreateAssistant) that accepts name, a conflicting resource (usually with the same name) is being created or mutated.
local record ConflictException
    message: string
end

-- The provided revisionId does not match, indicating the content has been modified since it was last read.
local record PreconditionFailedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    message: string
    -- The specified resource name.
    resourceName: string
end

-- You do not have permission to perform this action.
local record UnauthorizedException
    message: string
end

-- The input fails to satisfy the constraints specified by a service.
local record ValidationException
    message: string
end

local record M
    ActivateMessageTemplateInput: ActivateMessageTemplateInput
    ActivateMessageTemplateOutput: ActivateMessageTemplateOutput
    CreateAIAgentInput: CreateAIAgentInput
    CreateAIAgentOutput: CreateAIAgentOutput
    CreateAIAgentVersionInput: CreateAIAgentVersionInput
    CreateAIAgentVersionOutput: CreateAIAgentVersionOutput
    CreateAIGuardrailInput: CreateAIGuardrailInput
    CreateAIGuardrailOutput: CreateAIGuardrailOutput
    CreateAIGuardrailVersionInput: CreateAIGuardrailVersionInput
    CreateAIGuardrailVersionOutput: CreateAIGuardrailVersionOutput
    CreateAIPromptInput: CreateAIPromptInput
    CreateAIPromptOutput: CreateAIPromptOutput
    CreateAIPromptVersionInput: CreateAIPromptVersionInput
    CreateAIPromptVersionOutput: CreateAIPromptVersionOutput
    CreateAssistantInput: CreateAssistantInput
    CreateAssistantOutput: CreateAssistantOutput
    CreateAssistantAssociationInput: CreateAssistantAssociationInput
    CreateAssistantAssociationOutput: CreateAssistantAssociationOutput
    CreateContentInput: CreateContentInput
    CreateContentOutput: CreateContentOutput
    CreateContentAssociationInput: CreateContentAssociationInput
    CreateContentAssociationOutput: CreateContentAssociationOutput
    CreateKnowledgeBaseInput: CreateKnowledgeBaseInput
    CreateKnowledgeBaseOutput: CreateKnowledgeBaseOutput
    CreateMessageTemplateInput: CreateMessageTemplateInput
    CreateMessageTemplateOutput: CreateMessageTemplateOutput
    CreateMessageTemplateAttachmentInput: CreateMessageTemplateAttachmentInput
    CreateMessageTemplateAttachmentOutput: CreateMessageTemplateAttachmentOutput
    CreateMessageTemplateVersionInput: CreateMessageTemplateVersionInput
    CreateMessageTemplateVersionOutput: CreateMessageTemplateVersionOutput
    CreateQuickResponseInput: CreateQuickResponseInput
    CreateQuickResponseOutput: CreateQuickResponseOutput
    CreateSessionInput: CreateSessionInput
    CreateSessionOutput: CreateSessionOutput
    DeactivateMessageTemplateInput: DeactivateMessageTemplateInput
    DeactivateMessageTemplateOutput: DeactivateMessageTemplateOutput
    DeleteAIAgentInput: DeleteAIAgentInput
    DeleteAIAgentOutput: DeleteAIAgentOutput
    DeleteAIAgentVersionInput: DeleteAIAgentVersionInput
    DeleteAIAgentVersionOutput: DeleteAIAgentVersionOutput
    DeleteAIGuardrailInput: DeleteAIGuardrailInput
    DeleteAIGuardrailOutput: DeleteAIGuardrailOutput
    DeleteAIGuardrailVersionInput: DeleteAIGuardrailVersionInput
    DeleteAIGuardrailVersionOutput: DeleteAIGuardrailVersionOutput
    DeleteAIPromptInput: DeleteAIPromptInput
    DeleteAIPromptOutput: DeleteAIPromptOutput
    DeleteAIPromptVersionInput: DeleteAIPromptVersionInput
    DeleteAIPromptVersionOutput: DeleteAIPromptVersionOutput
    DeleteAssistantInput: DeleteAssistantInput
    DeleteAssistantOutput: DeleteAssistantOutput
    DeleteAssistantAssociationInput: DeleteAssistantAssociationInput
    DeleteAssistantAssociationOutput: DeleteAssistantAssociationOutput
    DeleteContentInput: DeleteContentInput
    DeleteContentOutput: DeleteContentOutput
    DeleteContentAssociationInput: DeleteContentAssociationInput
    DeleteContentAssociationOutput: DeleteContentAssociationOutput
    DeleteImportJobInput: DeleteImportJobInput
    DeleteImportJobOutput: DeleteImportJobOutput
    DeleteKnowledgeBaseInput: DeleteKnowledgeBaseInput
    DeleteKnowledgeBaseOutput: DeleteKnowledgeBaseOutput
    DeleteMessageTemplateInput: DeleteMessageTemplateInput
    DeleteMessageTemplateOutput: DeleteMessageTemplateOutput
    DeleteMessageTemplateAttachmentInput: DeleteMessageTemplateAttachmentInput
    DeleteMessageTemplateAttachmentOutput: DeleteMessageTemplateAttachmentOutput
    DeleteQuickResponseInput: DeleteQuickResponseInput
    DeleteQuickResponseOutput: DeleteQuickResponseOutput
    GetAIAgentInput: GetAIAgentInput
    GetAIAgentOutput: GetAIAgentOutput
    GetAIGuardrailInput: GetAIGuardrailInput
    GetAIGuardrailOutput: GetAIGuardrailOutput
    GetAIPromptInput: GetAIPromptInput
    GetAIPromptOutput: GetAIPromptOutput
    GetAssistantInput: GetAssistantInput
    GetAssistantOutput: GetAssistantOutput
    GetAssistantAssociationInput: GetAssistantAssociationInput
    GetAssistantAssociationOutput: GetAssistantAssociationOutput
    GetContentInput: GetContentInput
    GetContentOutput: GetContentOutput
    GetContentAssociationInput: GetContentAssociationInput
    GetContentAssociationOutput: GetContentAssociationOutput
    GetContentSummaryInput: GetContentSummaryInput
    GetContentSummaryOutput: GetContentSummaryOutput
    GetImportJobInput: GetImportJobInput
    GetImportJobOutput: GetImportJobOutput
    GetKnowledgeBaseInput: GetKnowledgeBaseInput
    GetKnowledgeBaseOutput: GetKnowledgeBaseOutput
    GetMessageTemplateInput: GetMessageTemplateInput
    GetMessageTemplateOutput: GetMessageTemplateOutput
    GetNextMessageInput: GetNextMessageInput
    GetNextMessageOutput: GetNextMessageOutput
    GetQuickResponseInput: GetQuickResponseInput
    GetQuickResponseOutput: GetQuickResponseOutput
    GetRecommendationsInput: GetRecommendationsInput
    GetRecommendationsOutput: GetRecommendationsOutput
    GetSessionInput: GetSessionInput
    GetSessionOutput: GetSessionOutput
    ListAIAgentsInput: ListAIAgentsInput
    ListAIAgentsOutput: ListAIAgentsOutput
    ListAIAgentVersionsInput: ListAIAgentVersionsInput
    ListAIAgentVersionsOutput: ListAIAgentVersionsOutput
    ListAIGuardrailsInput: ListAIGuardrailsInput
    ListAIGuardrailsOutput: ListAIGuardrailsOutput
    ListAIGuardrailVersionsInput: ListAIGuardrailVersionsInput
    ListAIGuardrailVersionsOutput: ListAIGuardrailVersionsOutput
    ListAIPromptsInput: ListAIPromptsInput
    ListAIPromptsOutput: ListAIPromptsOutput
    ListAIPromptVersionsInput: ListAIPromptVersionsInput
    ListAIPromptVersionsOutput: ListAIPromptVersionsOutput
    ListAssistantAssociationsInput: ListAssistantAssociationsInput
    ListAssistantAssociationsOutput: ListAssistantAssociationsOutput
    ListAssistantsInput: ListAssistantsInput
    ListAssistantsOutput: ListAssistantsOutput
    ListContentAssociationsInput: ListContentAssociationsInput
    ListContentAssociationsOutput: ListContentAssociationsOutput
    ListContentsInput: ListContentsInput
    ListContentsOutput: ListContentsOutput
    ListImportJobsInput: ListImportJobsInput
    ListImportJobsOutput: ListImportJobsOutput
    ListKnowledgeBasesInput: ListKnowledgeBasesInput
    ListKnowledgeBasesOutput: ListKnowledgeBasesOutput
    ListMessagesInput: ListMessagesInput
    ListMessagesOutput: ListMessagesOutput
    ListMessageTemplatesInput: ListMessageTemplatesInput
    ListMessageTemplatesOutput: ListMessageTemplatesOutput
    ListMessageTemplateVersionsInput: ListMessageTemplateVersionsInput
    ListMessageTemplateVersionsOutput: ListMessageTemplateVersionsOutput
    ListQuickResponsesInput: ListQuickResponsesInput
    ListQuickResponsesOutput: ListQuickResponsesOutput
    ListSpansInput: ListSpansInput
    ListSpansOutput: ListSpansOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    NotifyRecommendationsReceivedInput: NotifyRecommendationsReceivedInput
    NotifyRecommendationsReceivedOutput: NotifyRecommendationsReceivedOutput
    PutFeedbackInput: PutFeedbackInput
    PutFeedbackOutput: PutFeedbackOutput
    QueryAssistantInput: QueryAssistantInput
    QueryAssistantOutput: QueryAssistantOutput
    RemoveAssistantAIAgentInput: RemoveAssistantAIAgentInput
    RemoveAssistantAIAgentOutput: RemoveAssistantAIAgentOutput
    RemoveKnowledgeBaseTemplateUriInput: RemoveKnowledgeBaseTemplateUriInput
    RemoveKnowledgeBaseTemplateUriOutput: RemoveKnowledgeBaseTemplateUriOutput
    RenderMessageTemplateInput: RenderMessageTemplateInput
    RenderMessageTemplateOutput: RenderMessageTemplateOutput
    RetrieveInput: RetrieveInput
    RetrieveOutput: RetrieveOutput
    SearchContentInput: SearchContentInput
    SearchContentOutput: SearchContentOutput
    SearchMessageTemplatesInput: SearchMessageTemplatesInput
    SearchMessageTemplatesOutput: SearchMessageTemplatesOutput
    SearchQuickResponsesInput: SearchQuickResponsesInput
    SearchQuickResponsesOutput: SearchQuickResponsesOutput
    SearchSessionsInput: SearchSessionsInput
    SearchSessionsOutput: SearchSessionsOutput
    SendMessageInput: SendMessageInput
    SendMessageOutput: SendMessageOutput
    StartContentUploadInput: StartContentUploadInput
    StartContentUploadOutput: StartContentUploadOutput
    StartImportJobInput: StartImportJobInput
    StartImportJobOutput: StartImportJobOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateAIAgentInput: UpdateAIAgentInput
    UpdateAIAgentOutput: UpdateAIAgentOutput
    UpdateAIGuardrailInput: UpdateAIGuardrailInput
    UpdateAIGuardrailOutput: UpdateAIGuardrailOutput
    UpdateAIPromptInput: UpdateAIPromptInput
    UpdateAIPromptOutput: UpdateAIPromptOutput
    UpdateAssistantAIAgentInput: UpdateAssistantAIAgentInput
    UpdateAssistantAIAgentOutput: UpdateAssistantAIAgentOutput
    UpdateContentInput: UpdateContentInput
    UpdateContentOutput: UpdateContentOutput
    UpdateKnowledgeBaseTemplateUriInput: UpdateKnowledgeBaseTemplateUriInput
    UpdateKnowledgeBaseTemplateUriOutput: UpdateKnowledgeBaseTemplateUriOutput
    UpdateMessageTemplateInput: UpdateMessageTemplateInput
    UpdateMessageTemplateOutput: UpdateMessageTemplateOutput
    UpdateMessageTemplateMetadataInput: UpdateMessageTemplateMetadataInput
    UpdateMessageTemplateMetadataOutput: UpdateMessageTemplateMetadataOutput
    UpdateQuickResponseInput: UpdateQuickResponseInput
    UpdateQuickResponseOutput: UpdateQuickResponseOutput
    UpdateSessionInput: UpdateSessionInput
    UpdateSessionOutput: UpdateSessionOutput
    UpdateSessionDataInput: UpdateSessionDataInput
    UpdateSessionDataOutput: UpdateSessionDataOutput
end

return M
