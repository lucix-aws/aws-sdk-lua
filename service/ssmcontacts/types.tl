-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record AcceptPageInput
        -- The Amazon Resource Name (ARN) of the engagement to a contact channel.
        PageId: string
        -- The ARN of the contact channel.
        ContactChannelId: string
        -- The type indicates if the page was DELIVERED or READ.
        AcceptType: string
        -- Information provided by the user when the user acknowledges the page.
        Note: string
        -- A 6-digit code used to acknowledge the page.
        AcceptCode: string
        -- An optional field that Incident Manager uses to ENFORCE
        -- AcceptCode validation when acknowledging an page. Acknowledgement can occur by
        -- replying to a page, or when entering the AcceptCode in the console. Enforcing AcceptCode
        -- validation causes Incident Manager to verify that the code entered by the user matches the
        -- code sent by Incident Manager with the page.
        -- Incident Manager can also IGNORE
        -- AcceptCode validation. Ignoring AcceptCode validation causes
        -- Incident Manager to accept any value entered for the AcceptCode.
        AcceptCodeValidation: string
    end

    record AcceptPageOutput
    end

    record ActivateContactChannelInput
        -- The Amazon Resource Name (ARN) of the contact channel.
        ContactChannelId: string
        -- The code sent to the contact channel when it was created in the contact.
        ActivationCode: string
    end

    record ActivateContactChannelOutput
    end

    record CreateContactInput
        -- The short name to quickly identify a contact or escalation plan. The contact alias must
        -- be unique and identifiable.
        Alias: string
        -- The full name of the contact or escalation plan.
        DisplayName: string
        -- The type of contact to create.
        -- PERSONAL: A single, individual contact.
        -- ESCALATION: An escalation plan.
        -- ONCALL_SCHEDULE: An on-call schedule.
        Type: string
        -- A list of stages. A contact has an engagement plan with stages that contact specified
        -- contact channels. An escalation plan uses stages that contact specified contacts.
        Plan: Plan
        -- Adds a tag to the target. You can only tag resources created in the first Region of your
        -- replication set.
        Tags: {Tag}
        -- A token ensuring that the operation is called only once with the specified
        -- details.
        IdempotencyToken: string
    end

    record CreateContactOutput
        -- The Amazon Resource Name (ARN) of the created contact or escalation plan.
        ContactArn: string
    end

    record CreateContactChannelInput
        -- The Amazon Resource Name (ARN) of the contact you are adding the contact channel
        -- to.
        ContactId: string
        -- The name of the contact channel.
        Name: string
        -- Incident Manager supports three types of contact channels:
        -- SMS
        -- VOICE
        -- EMAIL
        Type: string
        -- The details that Incident Manager uses when trying to engage the contact channel. The format
        -- is dependent on the type of the contact channel. The following are the expected
        -- formats:
        -- SMS - '+' followed by the country code and phone number
        -- VOICE - '+' followed by the country code and phone number
        -- EMAIL - any standard email format
        DeliveryAddress: ContactChannelAddress
        -- If you want to activate the channel at a later time, you can choose to defer activation.
        -- Incident Manager can't engage your contact channel until it has been activated.
        DeferActivation: boolean
        -- A token ensuring that the operation is called only once with the specified
        -- details.
        IdempotencyToken: string
    end

    record CreateContactChannelOutput
        -- The Amazon Resource Name (ARN) of the contact channel.
        ContactChannelArn: string
    end

    record CreateRotationInput
        -- The name of the rotation.
        Name: string
        -- The Amazon Resource Names (ARNs) of the contacts to add to the rotation.
        -- Only the PERSONAL contact type is supported. The contact types
        -- ESCALATION and ONCALL_SCHEDULE are not supported for this
        -- operation.
        -- The order that you list the contacts in is their shift order in the rotation schedule.
        -- To change the order of the contact's shifts, use the UpdateRotation
        -- operation.
        ContactIds: {string}
        -- The date and time that the rotation goes into effect.
        StartTime: number
        -- The time zone to base the rotation’s activity on in Internet Assigned Numbers Authority
        -- (IANA) format. For example: "America/Los_Angeles", "UTC", or "Asia/Seoul". For more
        -- information, see the Time Zone
        -- Database on the IANA website.
        -- Designators for time zones that don’t support Daylight Savings Time rules, such as
        -- Pacific Standard Time (PST), are not supported.
        TimeZoneId: string
        -- Information about the rule that specifies when a shift's team members rotate.
        Recurrence: RecurrenceSettings
        -- Optional metadata to assign to the rotation. Tags enable you to categorize a resource in
        -- different ways, such as by purpose, owner, or environment. For more information, see Tagging
        -- Incident Manager resources in the Incident Manager User
        -- Guide.
        Tags: {Tag}
        -- A token that ensures that the operation is called only once with the specified
        -- details.
        IdempotencyToken: string
    end

    record CreateRotationOutput
        -- The Amazon Resource Name (ARN) of the created rotation.
        RotationArn: string
    end

    record CreateRotationOverrideInput
        -- The Amazon Resource Name (ARN) of the rotation to create an override for.
        RotationId: string
        -- The Amazon Resource Names (ARNs) of the contacts to replace those in the current on-call
        -- rotation with.
        -- If you want to include any current team members in the override shift, you must include
        -- their ARNs in the new contact ID list.
        NewContactIds: {string}
        -- The date and time when the override goes into effect.
        StartTime: number
        -- The date and time when the override ends.
        EndTime: number
        -- A token that ensures that the operation is called only once with the specified
        -- details.
        IdempotencyToken: string
    end

    record CreateRotationOverrideOutput
        -- The Amazon Resource Name (ARN) of the created rotation override.
        RotationOverrideId: string
    end

    record DeactivateContactChannelInput
        -- The Amazon Resource Name (ARN) of the contact channel you're deactivating.
        ContactChannelId: string
    end

    record DeactivateContactChannelOutput
    end

    record DeleteContactInput
        -- The Amazon Resource Name (ARN) of the contact that you're deleting.
        ContactId: string
    end

    record DeleteContactOutput
    end

    record DeleteContactChannelInput
        -- The Amazon Resource Name (ARN) of the contact channel.
        ContactChannelId: string
    end

    record DeleteContactChannelOutput
    end

    record DeleteRotationInput
        -- The Amazon Resource Name (ARN) of the on-call rotation to delete.
        RotationId: string
    end

    record DeleteRotationOutput
    end

    record DeleteRotationOverrideInput
        -- The Amazon Resource Name (ARN) of the rotation that was overridden.
        RotationId: string
        -- The Amazon Resource Name (ARN) of the on-call rotation override to delete.
        RotationOverrideId: string
    end

    record DeleteRotationOverrideOutput
    end

    record DescribeEngagementInput
        -- The Amazon Resource Name (ARN) of the engagement you want the details of.
        EngagementId: string
    end

    record DescribeEngagementOutput
        -- The ARN of the escalation plan or contacts involved in the engagement.
        ContactArn: string
        -- The ARN of the engagement.
        EngagementArn: string
        -- The user that started the engagement.
        Sender: string
        -- The secure subject of the message that was sent to the contact. Use this field for
        -- engagements to VOICE and EMAIL.
        Subject: string
        -- The secure content of the message that was sent to the contact. Use this field for
        -- engagements to VOICE and EMAIL.
        Content: string
        -- The insecure subject of the message that was sent to the contact. Use this field for
        -- engagements to SMS.
        PublicSubject: string
        -- The insecure content of the message that was sent to the contact. Use this field for
        -- engagements to SMS.
        PublicContent: string
        -- The ARN of the incident in which the engagement occurred.
        IncidentId: string
        -- The time that the engagement started.
        StartTime: number
        -- The time that the engagement ended.
        StopTime: number
    end

    record DescribePageInput
        -- The ID of the engagement to a contact channel.
        PageId: string
    end

    record DescribePageOutput
        -- The Amazon Resource Name (ARN) of the engagement to a contact channel.
        PageArn: string
        -- The ARN of the engagement that engaged the contact channel.
        EngagementArn: string
        -- The ARN of the contact that was engaged.
        ContactArn: string
        -- The user that started the engagement.
        Sender: string
        -- The secure subject of the message that was sent to the contact. Use this field for
        -- engagements to VOICE and EMAIL.
        Subject: string
        -- The secure content of the message that was sent to the contact. Use this field for
        -- engagements to VOICE and EMAIL.
        Content: string
        -- The insecure subject of the message that was sent to the contact. Use this field for
        -- engagements to SMS.
        PublicSubject: string
        -- The insecure content of the message that was sent to the contact. Use this field for
        -- engagements to SMS.
        PublicContent: string
        -- The ARN of the incident that engaged the contact channel.
        IncidentId: string
        -- The time the engagement was sent to the contact channel.
        SentTime: number
        -- The time that the contact channel acknowledged the engagement.
        ReadTime: number
        -- The time that the contact channel received the engagement.
        DeliveryTime: number
    end

    record GetContactInput
        -- The Amazon Resource Name (ARN) of the contact or escalation plan.
        ContactId: string
    end

    record GetContactOutput
        -- The ARN of the contact or escalation plan.
        ContactArn: string
        -- The alias of the contact or escalation plan. The alias is unique and
        -- identifiable.
        Alias: string
        -- The full name of the contact or escalation plan.
        DisplayName: string
        -- The type of contact.
        Type: string
        -- Details about the specific timing or stages and targets of the escalation plan or
        -- engagement plan.
        Plan: Plan
    end

    record GetContactChannelInput
        -- The Amazon Resource Name (ARN) of the contact channel you want information about.
        ContactChannelId: string
    end

    record GetContactChannelOutput
        -- The ARN of the contact that the channel belongs to.
        ContactArn: string
        -- The ARN of the contact channel.
        ContactChannelArn: string
        -- The name of the contact channel
        Name: string
        -- The type of contact channel. The type is SMS, VOICE, or
        -- EMAIL.
        Type: string
        -- The details that Incident Manager uses when trying to engage the contact channel.
        DeliveryAddress: ContactChannelAddress
        -- A Boolean value indicating if the contact channel has been activated or not.
        ActivationStatus: string
    end

    record GetContactPolicyInput
        -- The Amazon Resource Name (ARN) of the contact or escalation plan.
        ContactArn: string
    end

    record GetContactPolicyOutput
        -- The ARN of the contact or escalation plan.
        ContactArn: string
        -- Details about the resource policy attached to the contact or escalation plan.
        Policy: string
    end

    record GetRotationInput
        -- The Amazon Resource Name (ARN) of the on-call rotation to retrieve information
        -- about.
        RotationId: string
    end

    record GetRotationOutput
        -- The Amazon Resource Name (ARN) of the on-call rotation.
        RotationArn: string
        -- The name of the on-call rotation.
        Name: string
        -- The Amazon Resource Names (ARNs) of the contacts assigned to the on-call rotation
        -- team.
        ContactIds: {string}
        -- The specified start time for the on-call rotation.
        StartTime: number
        -- The time zone that the rotation’s activity is based on, in Internet Assigned Numbers
        -- Authority (IANA) format.
        TimeZoneId: string
        -- Specifies how long a rotation lasts before restarting at the beginning of the shift
        -- order.
        Recurrence: RecurrenceSettings
    end

    record GetRotationOverrideInput
        -- The Amazon Resource Name (ARN) of the overridden rotation to retrieve information
        -- about.
        RotationId: string
        -- The Amazon Resource Name (ARN) of the on-call rotation override to retrieve information
        -- about.
        RotationOverrideId: string
    end

    record GetRotationOverrideOutput
        -- The Amazon Resource Name (ARN) of the override to an on-call rotation.
        RotationOverrideId: string
        -- The Amazon Resource Name (ARN) of the on-call rotation that was overridden.
        RotationArn: string
        -- The Amazon Resource Names (ARNs) of the contacts assigned to the override of the on-call
        -- rotation.
        NewContactIds: {string}
        -- The date and time when the override goes into effect.
        StartTime: number
        -- The date and time when the override ends.
        EndTime: number
        -- The date and time when the override was created.
        CreateTime: number
    end

    record ListContactChannelsInput
        -- The Amazon Resource Name (ARN) of the contact.
        ContactId: string
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- The maximum number of contact channels per page.
        MaxResults: number
    end

    record ListContactChannelsOutput
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- A list of contact channels related to the specified contact.
        ContactChannels: {ContactChannel}
    end

    record ListContactsInput
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- The maximum number of contacts and escalation plans per page of results.
        MaxResults: number
        -- Used to list only contacts who's aliases start with the specified prefix.
        AliasPrefix: string
        -- The type of contact.
        Type: string
    end

    record ListContactsOutput
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- A list of the contacts and escalation plans in your Incident Manager account.
        Contacts: {Contact}
    end

    record ListEngagementsInput
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- The maximum number of engagements per page of results.
        MaxResults: number
        -- The Amazon Resource Name (ARN) of the incident you're listing engagements for.
        IncidentId: string
        -- The time range to lists engagements for an incident.
        TimeRangeValue: TimeRange
    end

    record ListEngagementsOutput
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- A list of each engagement that occurred during the specified time range of an
        -- incident.
        Engagements: {Engagement}
    end

    record ListPageReceiptsInput
        -- The Amazon Resource Name (ARN) of the engagement to a specific contact channel.
        PageId: string
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- The maximum number of acknowledgements per page of results.
        MaxResults: number
    end

    record ListPageReceiptsOutput
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- A list of each acknowledgement.
        Receipts: {Receipt}
    end

    record ListPageResolutionsInput
        -- A token to start the list. Use this token to get the next set of results.
        NextToken: string
        -- The Amazon Resource Name (ARN) of the contact engaged for the incident.
        PageId: string
    end

    record ListPageResolutionsOutput
        -- The token for the next set of items to return. Use this token to get the next set of
        -- results.
        NextToken: string
        -- Information about the resolution for an engagement.
        PageResolutions: {ResolutionContact}
    end

    record ListPagesByContactInput
        -- The Amazon Resource Name (ARN) of the contact you are retrieving engagements for.
        ContactId: string
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- The maximum number of engagements to contact channels to list per page of results.
        MaxResults: number
    end

    record ListPagesByContactOutput
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- The list of engagements to a contact's contact channel.
        Pages: {Page}
    end

    record ListPagesByEngagementInput
        -- The Amazon Resource Name (ARN) of the engagement.
        EngagementId: string
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- The maximum number of engagements to contact channels to list per page of
        -- results.
        MaxResults: number
    end

    record ListPagesByEngagementOutput
        -- The pagination token to continue to the next page of results.
        NextToken: string
        -- The list of engagements to contact channels.
        Pages: {Page}
    end

    record ListPreviewRotationShiftsInput
        -- The date and time a rotation would begin. The first shift is calculated from this date
        -- and time.
        RotationStartTime: number
        -- Used to filter the range of calculated shifts before sending the response back to the
        -- user.
        StartTime: number
        -- The date and time a rotation shift would end.
        EndTime: number
        -- The contacts that would be assigned to a rotation.
        Members: {string}
        -- The time zone the rotation’s activity would be based on, in Internet Assigned Numbers
        -- Authority (IANA) format. For example: "America/Los_Angeles", "UTC", or "Asia/Seoul".
        TimeZoneId: string
        -- Information about how long a rotation would last before restarting at the beginning of
        -- the shift order.
        Recurrence: RecurrenceSettings
        -- Information about changes that would be made in a rotation override.
        Overrides: {PreviewOverride}
        -- A token to start the list. This token is used to get the next set of results.
        NextToken: string
        -- The maximum number of items to return for this call. The call also returns a token that
        -- can be specified in a subsequent call to get the next set of results.
        MaxResults: number
    end

    record ListPreviewRotationShiftsOutput
        -- Details about a rotation shift, including times, types, and contacts.
        RotationShifts: {RotationShift}
        -- The token for the next set of items to return. This token is used to get the next set of
        -- results.
        NextToken: string
    end

    record ListRotationOverridesInput
        -- The Amazon Resource Name (ARN) of the rotation to retrieve information about.
        RotationId: string
        -- The date and time for the beginning of a time range for listing overrides.
        StartTime: number
        -- The date and time for the end of a time range for listing overrides.
        EndTime: number
        -- A token to start the list. Use this token to get the next set of results.
        NextToken: string
        -- The maximum number of items to return for this call. The call also returns a token that
        -- you can specify in a subsequent call to get the next set of results.
        MaxResults: number
    end

    record ListRotationOverridesOutput
        -- A list of rotation overrides in the specified time range.
        RotationOverrides: {RotationOverride}
        -- The token for the next set of items to return. Use this token to get the next set of
        -- results.
        NextToken: string
    end

    record ListRotationsInput
        -- A filter to include rotations in list results based on their common prefix. For example,
        -- entering prod returns a list of all rotation names that begin with prod, such
        -- as production and prod-1.
        RotationNamePrefix: string
        -- A token to start the list. Use this token to get the next set of results.
        NextToken: string
        -- The maximum number of items to return for this call. The call also returns a token that
        -- you can specify in a subsequent call to get the next set of results.
        MaxResults: number
    end

    record ListRotationsOutput
        -- The token for the next set of items to return. Use this token to get the next set of
        -- results.
        NextToken: string
        -- Information about rotations that meet the filter criteria.
        Rotations: {Rotation}
    end

    record ListRotationShiftsInput
        -- The Amazon Resource Name (ARN) of the rotation to retrieve shift information about.
        RotationId: string
        -- The date and time for the beginning of the time range to list shifts for.
        StartTime: number
        -- The date and time for the end of the time range to list shifts for.
        EndTime: number
        -- A token to start the list. Use this token to get the next set of results.
        NextToken: string
        -- The maximum number of items to return for this call. The call also returns a token that
        -- you can specify in a subsequent call to get the next set of results.
        MaxResults: number
    end

    record ListRotationShiftsOutput
        -- Information about shifts that meet the filter criteria.
        RotationShifts: {RotationShift}
        -- The token for the next set of items to return. Use this token to get the next set of
        -- results.
        NextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the contact, escalation plan, rotation, or on-call
        -- schedule.
        ResourceARN: string
    end

    record ListTagsForResourceOutput
        -- The tags related to the contact or escalation plan.
        Tags: {Tag}
    end

    record PutContactPolicyInput
        -- The Amazon Resource Name (ARN) of the contact or escalation plan.
        ContactArn: string
        -- Details of the resource policy.
        Policy: string
    end

    record PutContactPolicyOutput
    end

    record SendActivationCodeInput
        -- The Amazon Resource Name (ARN) of the contact channel.
        ContactChannelId: string
    end

    record SendActivationCodeOutput
    end

    record StartEngagementInput
        -- The Amazon Resource Name (ARN) of the contact being engaged.
        ContactId: string
        -- The user that started the engagement.
        Sender: string
        -- The secure subject of the message that was sent to the contact. Use this field for
        -- engagements to VOICE or EMAIL.
        Subject: string
        -- The secure content of the message that was sent to the contact. Use this field for
        -- engagements to VOICE or EMAIL.
        Content: string
        -- The insecure subject of the message that was sent to the contact. Use this field for
        -- engagements to SMS.
        PublicSubject: string
        -- The insecure content of the message that was sent to the contact. Use this field for
        -- engagements to SMS.
        PublicContent: string
        -- The ARN of the incident that the engagement is part of.
        IncidentId: string
        -- A token ensuring that the operation is called only once with the specified
        -- details.
        IdempotencyToken: string
    end

    record StartEngagementOutput
        -- The ARN of the engagement.
        EngagementArn: string
    end

    record StopEngagementInput
        -- The Amazon Resource Name (ARN) of the engagement.
        EngagementId: string
        -- The reason that you're stopping the engagement.
        Reason: string
    end

    record StopEngagementOutput
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the contact or escalation plan.
        ResourceARN: string
        -- A list of tags that you are adding to the contact or escalation plan.
        Tags: {Tag}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the contact or escalation plan.
        ResourceARN: string
        -- The key of the tag that you want to remove.
        TagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateContactInput
        -- The Amazon Resource Name (ARN) of the contact or escalation plan you're updating.
        ContactId: string
        -- The full name of the contact or escalation plan.
        DisplayName: string
        -- A list of stages. A contact has an engagement plan with stages for specified contact
        -- channels. An escalation plan uses these stages to contact specified contacts.
        Plan: Plan
    end

    record UpdateContactOutput
    end

    record UpdateContactChannelInput
        -- The Amazon Resource Name (ARN) of the contact channel you want to update.
        ContactChannelId: string
        -- The name of the contact channel.
        Name: string
        -- The details that Incident Manager uses when trying to engage the contact channel.
        DeliveryAddress: ContactChannelAddress
    end

    record UpdateContactChannelOutput
    end

    record UpdateRotationInput
        -- The Amazon Resource Name (ARN) of the rotation to update.
        RotationId: string
        -- The Amazon Resource Names (ARNs) of the contacts to include in the updated rotation.
        -- Only the PERSONAL contact type is supported. The contact types
        -- ESCALATION and ONCALL_SCHEDULE are not supported for this
        -- operation.
        -- The order in which you list the contacts is their shift order in the rotation
        -- schedule.
        ContactIds: {string}
        -- The date and time the rotation goes into effect.
        StartTime: number
        -- The time zone to base the updated rotation’s activity on, in Internet Assigned Numbers
        -- Authority (IANA) format. For example: "America/Los_Angeles", "UTC", or "Asia/Seoul". For
        -- more information, see the Time Zone
        -- Database on the IANA website.
        -- Designators for time zones that don’t support Daylight Savings Time Rules, such as
        -- Pacific Standard Time (PST), aren't supported.
        TimeZoneId: string
        -- Information about how long the updated rotation lasts before restarting at the beginning
        -- of the shift order.
        Recurrence: RecurrenceSettings
    end

    record UpdateRotationOutput
    end

    -- You don't have sufficient access to perform this operation.
    record AccessDeniedException
        Message: string
    end

    -- Unexpected error occurred while processing the request.
    record InternalServerException
        Message: string
        -- Advice to clients on when the call can be safely retried
        RetryAfterSeconds: number
    end

    -- Request references a resource that doesn't exist.
    record ResourceNotFoundException
        Message: string
        -- Hypothetical resource identifier that was not found
        ResourceId: string
        -- Hypothetical resource type that was not found
        ResourceType: string
    end

    -- The request was denied due to request throttling.
    record ThrottlingException
        Message: string
        -- Service Quotas requirement to identify originating service
        QuotaCode: string
        -- Service Quotas requirement to identify originating quota
        ServiceCode: string
        -- Advice to clients on when the call can be safely retried
        RetryAfterSeconds: number
    end

    -- The input fails to satisfy the constraints specified by an Amazon Web Services
    -- service.
    record ValidationException
        Message: string
        -- Reason the request failed validation
        Reason: string
        -- The fields that caused the error
        Fields: {ValidationExceptionField}
    end

    -- Updating or deleting a resource causes an inconsistent state.
    record ConflictException
        Message: string
        -- Identifier of the resource in use
        ResourceId: string
        -- Type of the resource in use
        ResourceType: string
        -- List of dependent entities containing information on relation type and resourceArns linked to the resource in use
        DependentEntities: {DependentEntity}
    end

    -- The operation failed to due an encryption key error.
    record DataEncryptionException
        Message: string
    end

    -- Request would cause a service quota to be exceeded.
    record ServiceQuotaExceededException
        Message: string
        -- Identifier of the resource affected
        ResourceId: string
        -- Type of the resource affected
        ResourceType: string
        -- Service Quotas requirement to identify originating service
        QuotaCode: string
        -- Service Quotas requirement to identify originating quota
        ServiceCode: string
    end

    enum AcceptCodeValidation
        "IGNORE"
        "ENFORCE"
    end

    enum AcceptType
        "DELIVERED"
        "READ"
    end

    enum ActivationStatus
        "ACTIVATED"
        "NOT_ACTIVATED"
    end

    enum ChannelType
        "SMS"
        "VOICE"
        "EMAIL"
    end

    enum ContactType
        "PERSONAL"
        "ESCALATION"
        "ONCALL_SCHEDULE"
    end

end

return M
