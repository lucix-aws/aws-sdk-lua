-- Code generated by lua-client-codegen. DO NOT EDIT.

local record CreateAwsLogSourceInput
    -- Specify the natively-supported Amazon Web Services service to add as a source in Security Lake.
    sources: {AwsLogSourceConfiguration}
end

local record CreateAwsLogSourceOutput
    -- Lists all accounts in which enabling a natively supported Amazon Web Services service as
    -- a Security Lake source failed. The failure occurred as these accounts are not part of an
    -- organization.
    failed: {string}
end

local record CreateCustomLogSourceInput
    -- Specify the name for a third-party custom source. This must be a Regionally unique
    -- value. The sourceName you enter here, is used in the
    -- LogProviderRole name which follows the convention
    -- AmazonSecurityLake-Provider-{name of the custom source}-{region}. You must
    -- use a CustomLogSource name that is shorter than or equal to 20 characters.
    -- This ensures that the LogProviderRole name is below the 64 character
    -- limit.
    sourceName: string
    -- Specify the source version for the third-party custom source, to limit log collection to
    -- a specific version of custom data source.
    sourceVersion: string
    -- The Open Cybersecurity Schema Framework (OCSF) event classes which describes the type of
    -- data that the custom source will send to Security Lake. For the list of supported event classes, see the Amazon Security Lake User Guide.
    eventClasses: {string}
    -- The configuration used for the third-party custom source.
    configuration: CustomLogSourceConfiguration
end

local record CreateCustomLogSourceOutput
    -- The third-party custom source that was created.
    source: CustomLogSourceResource
end

local record CreateDataLakeInput
    -- Specify the Region or Regions that will contribute data to the rollup region.
    configurations: {DataLakeConfiguration}
    -- The Amazon Resource Name (ARN) used to create and update the Glue table.
    -- This table contains partitions generated by the ingestion and normalization of
    -- Amazon Web Services log sources and custom sources.
    metaStoreManagerRoleArn: string
    -- An array of objects, one for each tag to associate with the data lake configuration. For each tag, you must specify both a tag key and a tag value. A tag
    -- value cannot be null, but it can be an empty string.
    tags: {Tag}
end

local record CreateDataLakeOutput
    -- The created Security Lake configuration object.
    dataLakes: {DataLakeResource}
end

local record CreateDataLakeExceptionSubscriptionInput
    -- The subscription protocol to which exception notifications are posted.
    subscriptionProtocol: string
    -- The Amazon Web Services account where you want to receive exception notifications.
    notificationEndpoint: string
    -- The expiration period and time-to-live (TTL). It is the duration of time until which the exception message remains.
    exceptionTimeToLive: number
end

local record CreateDataLakeExceptionSubscriptionOutput
end

local record CreateDataLakeOrganizationConfigurationInput
    -- Enable Security Lake with the specified configuration settings, to begin collecting security
    -- data for new accounts in your organization.
    autoEnableNewAccount: {DataLakeAutoEnableNewAccountConfiguration}
end

local record CreateDataLakeOrganizationConfigurationOutput
end

local record CreateSubscriberInput
    -- The Amazon Web Services identity used to access your data.
    subscriberIdentity: AwsIdentity
    -- The name of your Security Lake subscriber account.
    subscriberName: string
    -- The description for your subscriber account in Security Lake.
    subscriberDescription: string
    -- The supported Amazon Web Services services from which logs and events are collected.
    -- Security Lake supports log and event collection for natively supported Amazon Web Services services.
    sources: {LogSourceResource}
    -- The Amazon S3 or Lake Formation access type.
    accessTypes: {string}
    -- An array of objects, one for each tag to associate with the subscriber. For each tag, you must specify both a tag key and a tag value. A tag
    -- value cannot be null, but it can be an empty string.
    tags: {Tag}
end

local record CreateSubscriberOutput
    -- Retrieve information about the subscriber created using the
    -- CreateSubscriber API.
    subscriber: SubscriberResource
end

local record CreateSubscriberNotificationInput
    -- The subscriber ID for the notification subscription.
    subscriberId: string
    -- Specify the configuration using which you want to create the subscriber
    -- notification.
    configuration: NotificationConfiguration
end

local record CreateSubscriberNotificationOutput
    -- The subscriber endpoint to which exception messages are posted.
    subscriberEndpoint: string
end

local record DeleteAwsLogSourceInput
    -- Specify the natively-supported Amazon Web Services service to remove as a source in
    -- Security Lake.
    sources: {AwsLogSourceConfiguration}
end

local record DeleteAwsLogSourceOutput
    -- Deletion of the Amazon Web Services sources failed as the account is not a part of the organization.
    failed: {string}
end

local record DeleteCustomLogSourceInput
    -- The source name of custom log source that you want to delete.
    sourceName: string
    -- The source version for the third-party custom source. You can limit the custom source
    -- removal to the specified source version.
    sourceVersion: string
end

local record DeleteCustomLogSourceOutput
end

local record DeleteDataLakeInput
    -- The list of Regions where Security Lake is enabled.
    regions: {string}
end

local record DeleteDataLakeOutput
end

local record DeleteDataLakeExceptionSubscriptionInput
end

local record DeleteDataLakeExceptionSubscriptionOutput
end

local record DeleteDataLakeOrganizationConfigurationInput
    -- Turns off automatic enablement of Security Lake for member accounts that are added to an organization.
    autoEnableNewAccount: {DataLakeAutoEnableNewAccountConfiguration}
end

local record DeleteDataLakeOrganizationConfigurationOutput
end

local record DeleteSubscriberInput
    -- A value created by Security Lake that uniquely identifies your DeleteSubscriber API request.
    subscriberId: string
end

local record DeleteSubscriberOutput
end

local record DeleteSubscriberNotificationInput
    -- The ID of the Security Lake subscriber account.
    subscriberId: string
end

local record DeleteSubscriberNotificationOutput
end

local record DeregisterDataLakeDelegatedAdministratorInput
end

local record DeregisterDataLakeDelegatedAdministratorOutput
end

local record GetDataLakeExceptionSubscriptionInput
end

local record GetDataLakeExceptionSubscriptionOutput
    -- The subscription protocol to which exception notifications are posted.
    subscriptionProtocol: string
    -- The Amazon Web Services account where you receive exception notifications.
    notificationEndpoint: string
    -- The expiration period and time-to-live (TTL). It is the duration of time until which the exception message remains.
    exceptionTimeToLive: number
end

local record GetDataLakeOrganizationConfigurationInput
end

local record GetDataLakeOrganizationConfigurationOutput
    -- The configuration used for new accounts in Security Lake.
    autoEnableNewAccount: {DataLakeAutoEnableNewAccountConfiguration}
end

local record GetDataLakeSourcesInput
    -- The Amazon Web Services account ID for which a static snapshot of the current Amazon Web Services Region, including enabled accounts and log sources, is retrieved.
    accounts: {string}
    -- The maximum limit of accounts for which the static snapshot of the current Region,
    -- including enabled accounts and log sources, is retrieved.
    maxResults: number
    -- Lists if there are more results available. The value of nextToken is a unique pagination
    -- token for each page. Repeat the call using the returned token to retrieve the next page.
    -- Keep all other arguments unchanged.
    -- Each pagination token expires after 24 hours. Using an expired pagination token will
    -- return an HTTP 400 InvalidToken error.
    nextToken: string
end

local record GetDataLakeSourcesOutput
    -- The Amazon Resource Name (ARN) created by you to provide to the subscriber. For more information about ARNs and how to use them in policies, see the Amazon Security Lake User Guide.
    dataLakeArn: string
    -- The list of enabled accounts and enabled sources.
    dataLakeSources: {DataLakeSource}
    -- Lists if there are more results available. The value of nextToken is a unique pagination
    -- token for each page. Repeat the call using the returned token to retrieve the next page.
    -- Keep all other arguments unchanged.
    -- Each pagination token expires after 24 hours. Using an expired pagination token will
    -- return an HTTP 400 InvalidToken error.
    nextToken: string
end

local record GetSubscriberInput
    -- A value created by Amazon Security Lake that uniquely identifies your
    -- GetSubscriber API request.
    subscriberId: string
end

local record GetSubscriberOutput
    -- The subscriber information for the specified subscriber ID.
    subscriber: SubscriberResource
end

local record ListDataLakeExceptionsInput
    -- The Amazon Web Services Regions from which exceptions are retrieved.
    regions: {string}
    -- Lists the maximum number of failures in Security Lake.
    maxResults: number
    -- Lists if there are more results available. The value of nextToken is a unique pagination
    -- token for each page. Repeat the call using the returned token to retrieve the next page.
    -- Keep all other arguments unchanged.
    -- Each pagination token expires after 24 hours. Using an expired pagination token will
    -- return an HTTP 400 InvalidToken error.
    nextToken: string
end

local record ListDataLakeExceptionsOutput
    -- Lists the failures that cannot be retried.
    exceptions: {DataLakeException}
    -- Lists if there are more results available. The value of nextToken is a unique pagination
    -- token for each page. Repeat the call using the returned token to retrieve the next page.
    -- Keep all other arguments unchanged.
    -- Each pagination token expires after 24 hours. Using an expired pagination token will
    -- return an HTTP 400 InvalidToken error.
    nextToken: string
end

local record ListDataLakesInput
    -- The list of Regions where Security Lake is enabled.
    regions: {string}
end

local record ListDataLakesOutput
    -- Retrieves the Security Lake configuration object.
    dataLakes: {DataLakeResource}
end

local record ListLogSourcesInput
    -- The list of Amazon Web Services accounts for which log sources are displayed.
    accounts: {string}
    -- The list of Regions for which log sources are displayed.
    regions: {string}
    -- The list of sources for which log sources are displayed.
    sources: {LogSourceResource}
    -- The maximum number of accounts for which the log sources are displayed.
    maxResults: number
    -- If nextToken is returned, there are more results available. You can repeat the call
    -- using the returned token to retrieve the next page.
    nextToken: string
end

local record ListLogSourcesOutput
    -- The list of log sources in your organization that send data to the data lake.
    sources: {LogSource}
    -- If nextToken is returned, there are more results available. You can repeat the call
    -- using the returned token to retrieve the next page.
    nextToken: string
end

local record ListSubscribersInput
    -- If nextToken is returned, there are more results available. You can repeat the call
    -- using the returned token to retrieve the next page.
    nextToken: string
    -- The maximum number of accounts for which the configuration is displayed.
    maxResults: number
end

local record ListSubscribersOutput
    -- The subscribers available for the specified Security Lake account ID.
    subscribers: {SubscriberResource}
    -- If nextToken is returned, there are more results available. You can repeat the call
    -- using the returned token to retrieve the next page.
    nextToken: string
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the Amazon Security Lake resource for which you want to retrieve the tags.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- An array of objects, one for each tag (key and value) that’s associated with the Amazon Security Lake resource.
    tags: {Tag}
end

local record RegisterDataLakeDelegatedAdministratorInput
    -- The Amazon Web Services account ID of the Security Lake delegated administrator.
    accountId: string
end

local record RegisterDataLakeDelegatedAdministratorOutput
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the Amazon Security Lake resource to add or update the tags for.
    resourceArn: string
    -- An array of objects, one for each tag (key and value) to associate with the Amazon Security Lake resource. For each tag, you must
    -- specify both a tag key and a tag value. A tag value cannot be null, but it can be an empty string.
    tags: {Tag}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the Amazon Security Lake resource to remove one or more tags from.
    resourceArn: string
    -- A list of one or more tag keys. For each value in the list, specify the tag key for a tag to remove from the Amazon Security Lake resource.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateDataLakeInput
    -- Specifies the Region or Regions that will contribute data to the rollup region.
    configurations: {DataLakeConfiguration}
    -- The Amazon Resource Name (ARN) used to create and update the Glue table.
    -- This table contains partitions generated by the ingestion and normalization of
    -- Amazon Web Services log sources and custom sources.
    metaStoreManagerRoleArn: string
end

local record UpdateDataLakeOutput
    -- The created Security Lake configuration object.
    dataLakes: {DataLakeResource}
end

local record UpdateDataLakeExceptionSubscriptionInput
    -- The subscription protocol to which exception messages are posted.
    subscriptionProtocol: string
    -- The account that is subscribed to receive exception notifications.
    notificationEndpoint: string
    -- The time-to-live (TTL) for the exception message to remain. It is the duration of time until which the exception message remains.
    exceptionTimeToLive: number
end

local record UpdateDataLakeExceptionSubscriptionOutput
end

local record UpdateSubscriberInput
    -- A value created by Security Lake that uniquely identifies your subscription.
    subscriberId: string
    -- The Amazon Web Services identity used to access your data.
    subscriberIdentity: AwsIdentity
    -- The name of the Security Lake account subscriber.
    subscriberName: string
    -- The description of the Security Lake account subscriber.
    subscriberDescription: string
    -- The supported Amazon Web Services services from which logs and events are collected. For
    -- the list of supported Amazon Web Services services, see the Amazon Security Lake User Guide.
    sources: {LogSourceResource}
end

local record UpdateSubscriberOutput
    -- The updated subscriber information.
    subscriber: SubscriberResource
end

local record UpdateSubscriberNotificationInput
    -- The subscription ID for which the subscription notification is specified.
    subscriberId: string
    -- The configuration for subscriber notification.
    configuration: NotificationConfiguration
end

local record UpdateSubscriberNotificationOutput
    -- The subscriber endpoint to which exception messages are posted.
    subscriberEndpoint: string
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

-- You do not have sufficient access to perform this action. Access denied errors appear when Amazon Security Lake explicitly or implicitly denies an authorization
-- request. An explicit denial occurs when a policy contains a Deny statement for the specific
-- Amazon Web Services action. An implicit denial occurs when there is no applicable Deny statement and also
-- no applicable Allow statement.
local record AccessDeniedException
    message: string
    -- A coded string to provide more information about the access denied exception. You can use the error code to check the exception type.
    errorCode: string
end

-- The request is malformed or contains an error such as an invalid parameter value or a missing required parameter.
local record BadRequestException
    message: string
end

-- Occurs when a conflict with a previous successful write is detected. This generally
-- occurs when the previous write did not have time to propagate to the host serving the
-- current request. A retry (with appropriate backoff logic) is the recommended response to
-- this exception.
local record ConflictException
    message: string
    -- The resource name.
    resourceName: string
    -- The resource type.
    resourceType: string
end

-- Internal service exceptions are sometimes caused by transient issues. Before you start
-- troubleshooting, perform the operation again.
local record InternalServerException
    message: string
end

-- The resource could not be found.
local record ResourceNotFoundException
    message: string
    -- The name of the resource that could not be found.
    resourceName: string
    -- The type of the resource that could not be found.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The code for the service in Service Quotas.
    serviceCode: string
    -- That the rate of requests to Security Lake is exceeding the request quotas for your Amazon Web Services account.
    quotaCode: string
    -- Retry the request after the specified time.
    retryAfterSeconds: number
end

local record M
    CreateAwsLogSourceInput: CreateAwsLogSourceInput
    CreateAwsLogSourceOutput: CreateAwsLogSourceOutput
    CreateCustomLogSourceInput: CreateCustomLogSourceInput
    CreateCustomLogSourceOutput: CreateCustomLogSourceOutput
    CreateDataLakeInput: CreateDataLakeInput
    CreateDataLakeOutput: CreateDataLakeOutput
    CreateDataLakeExceptionSubscriptionInput: CreateDataLakeExceptionSubscriptionInput
    CreateDataLakeExceptionSubscriptionOutput: CreateDataLakeExceptionSubscriptionOutput
    CreateDataLakeOrganizationConfigurationInput: CreateDataLakeOrganizationConfigurationInput
    CreateDataLakeOrganizationConfigurationOutput: CreateDataLakeOrganizationConfigurationOutput
    CreateSubscriberInput: CreateSubscriberInput
    CreateSubscriberOutput: CreateSubscriberOutput
    CreateSubscriberNotificationInput: CreateSubscriberNotificationInput
    CreateSubscriberNotificationOutput: CreateSubscriberNotificationOutput
    DeleteAwsLogSourceInput: DeleteAwsLogSourceInput
    DeleteAwsLogSourceOutput: DeleteAwsLogSourceOutput
    DeleteCustomLogSourceInput: DeleteCustomLogSourceInput
    DeleteCustomLogSourceOutput: DeleteCustomLogSourceOutput
    DeleteDataLakeInput: DeleteDataLakeInput
    DeleteDataLakeOutput: DeleteDataLakeOutput
    DeleteDataLakeExceptionSubscriptionInput: DeleteDataLakeExceptionSubscriptionInput
    DeleteDataLakeExceptionSubscriptionOutput: DeleteDataLakeExceptionSubscriptionOutput
    DeleteDataLakeOrganizationConfigurationInput: DeleteDataLakeOrganizationConfigurationInput
    DeleteDataLakeOrganizationConfigurationOutput: DeleteDataLakeOrganizationConfigurationOutput
    DeleteSubscriberInput: DeleteSubscriberInput
    DeleteSubscriberOutput: DeleteSubscriberOutput
    DeleteSubscriberNotificationInput: DeleteSubscriberNotificationInput
    DeleteSubscriberNotificationOutput: DeleteSubscriberNotificationOutput
    DeregisterDataLakeDelegatedAdministratorInput: DeregisterDataLakeDelegatedAdministratorInput
    DeregisterDataLakeDelegatedAdministratorOutput: DeregisterDataLakeDelegatedAdministratorOutput
    GetDataLakeExceptionSubscriptionInput: GetDataLakeExceptionSubscriptionInput
    GetDataLakeExceptionSubscriptionOutput: GetDataLakeExceptionSubscriptionOutput
    GetDataLakeOrganizationConfigurationInput: GetDataLakeOrganizationConfigurationInput
    GetDataLakeOrganizationConfigurationOutput: GetDataLakeOrganizationConfigurationOutput
    GetDataLakeSourcesInput: GetDataLakeSourcesInput
    GetDataLakeSourcesOutput: GetDataLakeSourcesOutput
    GetSubscriberInput: GetSubscriberInput
    GetSubscriberOutput: GetSubscriberOutput
    ListDataLakeExceptionsInput: ListDataLakeExceptionsInput
    ListDataLakeExceptionsOutput: ListDataLakeExceptionsOutput
    ListDataLakesInput: ListDataLakesInput
    ListDataLakesOutput: ListDataLakesOutput
    ListLogSourcesInput: ListLogSourcesInput
    ListLogSourcesOutput: ListLogSourcesOutput
    ListSubscribersInput: ListSubscribersInput
    ListSubscribersOutput: ListSubscribersOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    RegisterDataLakeDelegatedAdministratorInput: RegisterDataLakeDelegatedAdministratorInput
    RegisterDataLakeDelegatedAdministratorOutput: RegisterDataLakeDelegatedAdministratorOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateDataLakeInput: UpdateDataLakeInput
    UpdateDataLakeOutput: UpdateDataLakeOutput
    UpdateDataLakeExceptionSubscriptionInput: UpdateDataLakeExceptionSubscriptionInput
    UpdateDataLakeExceptionSubscriptionOutput: UpdateDataLakeExceptionSubscriptionOutput
    UpdateSubscriberInput: UpdateSubscriberInput
    UpdateSubscriberOutput: UpdateSubscriberOutput
    UpdateSubscriberNotificationInput: UpdateSubscriberNotificationInput
    UpdateSubscriberNotificationOutput: UpdateSubscriberNotificationOutput
end

return M
