-- Code generated by lua-client-codegen. DO NOT EDIT.

local record CreateCapabilityInput
    -- Specifies the name of the capability, used to identify it.
    name: string
    -- Specifies the type of the capability. Currently, only edi is supported.
    type: string
    -- Specifies a structure that contains the details for a capability.
    configuration: CapabilityConfiguration
    -- Specifies one or more locations in Amazon S3, each specifying an EDI document that can be used with this capability. Each item contains the name of the bucket and the key, to identify the document's location.
    instructionsDocuments: {S3Location}
    -- Reserved for future use.
    clientToken: string
    -- Specifies the key-value pairs assigned to ARNs that you can use to group and search for resources by type. You can attach this metadata to resources (capabilities, partnerships, and so on) for any purpose.
    tags: {Tag}
end

local record CreateCapabilityOutput
    -- Returns a system-assigned unique identifier for the capability.
    capabilityId: string
    -- Returns an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    capabilityArn: string
    -- Returns the name of the capability used to identify it.
    name: string
    -- Returns the type of the capability. Currently, only edi is supported.
    type: string
    -- Returns a structure that contains the details for a capability.
    configuration: CapabilityConfiguration
    -- Returns one or more locations in Amazon S3, each specifying an EDI document that can be used with this capability. Each item contains the name of the bucket and the key, to identify the document's location.
    instructionsDocuments: {S3Location}
    -- Returns a timestamp for creation date and time of the capability.
    createdAt: number
end

local record CreatePartnershipInput
    -- Specifies the unique, system-generated identifier for the profile connected to this partnership.
    profileId: string
    -- Specifies a descriptive name for the partnership.
    name: string
    -- Specifies the email address associated with this trading partner.
    email: string
    -- Specifies the phone number associated with the partnership.
    phone: string
    -- Specifies a list of the capabilities associated with this partnership.
    capabilities: {string}
    -- Specify the structure that contains the details for the associated capabilities.
    capabilityOptions: CapabilityOptions
    -- Reserved for future use.
    clientToken: string
    -- Specifies the key-value pairs assigned to ARNs that you can use to group and search for resources by type. You can attach this metadata to resources (capabilities, partnerships, and so on) for any purpose.
    tags: {Tag}
end

local record CreatePartnershipOutput
    -- Returns the unique, system-generated identifier for the profile connected to this partnership.
    profileId: string
    -- Returns the unique, system-generated identifier for a partnership.
    partnershipId: string
    -- Returns an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    partnershipArn: string
    -- Returns a descriptive name for the partnership.
    name: string
    -- Returns the email address associated with this trading partner.
    email: string
    -- Returns the phone number associated with the partnership.
    phone: string
    -- Returns one or more capabilities associated with this partnership.
    capabilities: {string}
    -- Returns the structure that contains the details for the associated capabilities.
    capabilityOptions: CapabilityOptions
    -- Returns the unique, system-generated identifier for a trading partner.
    tradingPartnerId: string
    -- Returns a timestamp for creation date and time of the partnership.
    createdAt: number
end

local record CreateProfileInput
    -- Specifies the name of the profile.
    name: string
    -- Specifies the email address associated with this customer profile.
    email: string
    -- Specifies the phone number associated with the profile.
    phone: string
    -- Specifies the name for the business associated with this profile.
    businessName: string
    -- Specifies whether or not logging is enabled for this profile.
    logging: string
    -- Reserved for future use.
    clientToken: string
    -- Specifies the key-value pairs assigned to ARNs that you can use to group and search for resources by type. You can attach this metadata to resources (capabilities, partnerships, and so on) for any purpose.
    tags: {Tag}
end

local record CreateProfileOutput
    -- Returns the unique, system-generated identifier for the profile.
    profileId: string
    -- Returns an Amazon Resource Name (ARN) for the profile.
    profileArn: string
    -- Returns the name of the profile, used to identify it.
    name: string
    -- Returns the name for the business associated with this profile.
    businessName: string
    -- Returns the phone number associated with the profile.
    phone: string
    -- Returns the email address associated with this customer profile.
    email: string
    -- Returns whether or not logging is turned on for this profile.
    logging: string
    -- Returns the name of the logging group.
    logGroupName: string
    -- Returns a timestamp representing the time the profile was created.
    createdAt: number
end

local record CreateStarterMappingTemplateInput
    -- Specify the location of the sample EDI file that is used to generate the mapping template.
    outputSampleLocation: S3Location
    -- Specify the format for the mapping template: either JSONATA or XSLT.
    mappingType: string
    -- Describes the details needed for generating the template. Specify the X12 transaction set and version for which the template is used: currently, we only support X12.
    templateDetails: TemplateDetails
end

local record CreateStarterMappingTemplateOutput
    -- Returns a string that represents the mapping template.
    mappingTemplate: string
end

local record CreateTransformerInput
    -- Specifies the name of the transformer, used to identify it.
    name: string
    -- Reserved for future use.
    clientToken: string
    -- Specifies the key-value pairs assigned to ARNs that you can use to group and search for resources by type. You can attach this metadata to resources (capabilities, partnerships, and so on) for any purpose.
    tags: {Tag}
    -- Specifies that the currently supported file formats for EDI transformations are JSON and XML.
    fileFormat: string
    -- Specifies the mapping template for the transformer. This template is used to map the parsed EDI file using JSONata or XSLT. This parameter is available for backwards compatibility. Use the Mapping data type instead.
    mappingTemplate: string
    -- Specifies the details for the EDI standard that is being used for the transformer. Currently, only X12 is supported. X12 is a set of standards and corresponding messages that define specific business documents.
    ediType: EdiType
    -- Specifies a sample EDI document that is used by a transformer as a guide for processing the EDI data.
    sampleDocument: string
    -- Specify the InputConversion object, which contains the format options for the inbound transformation.
    inputConversion: InputConversion
    -- Specify the structure that contains the mapping template and its language (either XSLT or JSONATA).
    mapping: Mapping
    -- A structure that contains the OutputConversion object, which contains the format options for the outbound transformation.
    outputConversion: OutputConversion
    -- Specify a structure that contains the Amazon S3 bucket and an array of the corresponding keys used to identify the location for your sample documents.
    sampleDocuments: SampleDocuments
end

local record CreateTransformerOutput
    -- Returns the system-assigned unique identifier for the transformer.
    transformerId: string
    -- Returns an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    transformerArn: string
    -- Returns the name of the transformer, used to identify it.
    name: string
    -- Returns the state of the newly created transformer. The transformer can be either active or inactive. For the transformer to be used in a capability, its status must active.
    status: string
    -- Returns a timestamp for creation date and time of the transformer.
    createdAt: number
    -- Returns that the currently supported file formats for EDI transformations are JSON and XML.
    fileFormat: string
    -- Returns the mapping template for the transformer. This template is used to map the parsed EDI file using JSONata or XSLT.
    mappingTemplate: string
    -- Returns the details for the EDI standard that is being used for the transformer. Currently, only X12 is supported. X12 is a set of standards and corresponding messages that define specific business documents.
    ediType: EdiType
    -- Returns a sample EDI document that is used by a transformer as a guide for processing the EDI data.
    sampleDocument: string
    -- Returns the InputConversion object, which contains the format options for the inbound transformation.
    inputConversion: InputConversion
    -- Returns the structure that contains the mapping template and its language (either XSLT or JSONATA).
    mapping: Mapping
    -- Returns the OutputConversion object, which contains the format options for the outbound transformation.
    outputConversion: OutputConversion
    -- Returns a structure that contains the Amazon S3 bucket and an array of the corresponding keys used to identify the location for your sample documents.
    sampleDocuments: SampleDocuments
end

local record DeleteCapabilityInput
    -- Specifies a system-assigned unique identifier for the capability.
    capabilityId: string
end

local record DeleteCapabilityOutput
end

local record DeletePartnershipInput
    -- Specifies the unique, system-generated identifier for a partnership.
    partnershipId: string
end

local record DeletePartnershipOutput
end

local record DeleteProfileInput
    -- Specifies the unique, system-generated identifier for the profile.
    profileId: string
end

local record DeleteProfileOutput
end

local record DeleteTransformerInput
    -- Specifies the system-assigned unique identifier for the transformer.
    transformerId: string
end

local record DeleteTransformerOutput
end

local record GenerateMappingInput
    -- Provide the contents of a sample X12 EDI file, either in JSON or XML format, to use as a starting point for the mapping.
    inputFileContent: string
    -- Provide the contents of a sample X12 EDI file, either in JSON or XML format, to use as a target for the mapping.
    outputFileContent: string
    -- Specify the mapping type: either JSONATA or XSLT.
    mappingType: string
end

local record GenerateMappingOutput
    -- Returns a mapping template based on your inputs.
    mappingTemplate: string
    -- Returns a percentage that estimates the accuracy of the generated mapping.
    mappingAccuracy: number
end

local record GetCapabilityInput
    -- Specifies a system-assigned unique identifier for the capability.
    capabilityId: string
end

local record GetCapabilityOutput
    -- Returns a system-assigned unique identifier for the capability.
    capabilityId: string
    -- Returns an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    capabilityArn: string
    -- Returns the name of the capability, used to identify it.
    name: string
    -- Returns the type of the capability. Currently, only edi is supported.
    type: string
    -- Returns a structure that contains the details for a capability.
    configuration: CapabilityConfiguration
    -- Returns one or more locations in Amazon S3, each specifying an EDI document that can be used with this capability. Each item contains the name of the bucket and the key, to identify the document's location.
    instructionsDocuments: {S3Location}
    -- Returns a timestamp for creation date and time of the capability.
    createdAt: number
    -- Returns a timestamp for last time the capability was modified.
    modifiedAt: number
end

local record GetPartnershipInput
    -- Specifies the unique, system-generated identifier for a partnership.
    partnershipId: string
end

local record GetPartnershipOutput
    -- Returns the unique, system-generated identifier for the profile connected to this partnership.
    profileId: string
    -- Returns the unique, system-generated identifier for a partnership.
    partnershipId: string
    -- Returns an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    partnershipArn: string
    -- Returns the display name of the partnership
    name: string
    -- Returns the email address associated with this trading partner.
    email: string
    -- Returns the phone number associated with the partnership.
    phone: string
    -- Returns one or more capabilities associated with this partnership.
    capabilities: {string}
    capabilityOptions: CapabilityOptions
    -- Returns the unique identifier for the partner for this partnership.
    tradingPartnerId: string
    -- Returns a timestamp for creation date and time of the partnership.
    createdAt: number
    -- Returns a timestamp that identifies the most recent date and time that the partnership was modified.
    modifiedAt: number
end

local record GetProfileInput
    -- Specifies the unique, system-generated identifier for the profile.
    profileId: string
end

local record GetProfileOutput
    -- Returns the unique, system-generated identifier for the profile.
    profileId: string
    -- Returns an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    profileArn: string
    -- Returns the name of the profile, used to identify it.
    name: string
    -- Returns the email address associated with this customer profile.
    email: string
    -- Returns the phone number associated with the profile.
    phone: string
    -- Returns the name for the business associated with this profile.
    businessName: string
    -- Returns whether or not logging is enabled for this profile.
    logging: string
    -- Returns the name of the logging group.
    logGroupName: string
    -- Returns a timestamp for creation date and time of the transformer.
    createdAt: number
    -- Returns a timestamp for last time the profile was modified.
    modifiedAt: number
end

local record GetTransformerInput
    -- Specifies the system-assigned unique identifier for the transformer.
    transformerId: string
end

local record GetTransformerOutput
    -- Returns the system-assigned unique identifier for the transformer.
    transformerId: string
    -- Returns an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    transformerArn: string
    -- Returns the name of the transformer, used to identify it.
    name: string
    -- Returns the state of the newly created transformer. The transformer can be either active or inactive. For the transformer to be used in a capability, its status must active.
    status: string
    -- Returns a timestamp for creation date and time of the transformer.
    createdAt: number
    -- Returns a timestamp for last time the transformer was modified.
    modifiedAt: number
    -- Returns that the currently supported file formats for EDI transformations are JSON and XML.
    fileFormat: string
    -- Returns the mapping template for the transformer. This template is used to map the parsed EDI file using JSONata or XSLT.
    mappingTemplate: string
    -- Returns the details for the EDI standard that is being used for the transformer. Currently, only X12 is supported. X12 is a set of standards and corresponding messages that define specific business documents.
    ediType: EdiType
    -- Returns a sample EDI document that is used by a transformer as a guide for processing the EDI data.
    sampleDocument: string
    -- Returns the InputConversion object, which contains the format options for the inbound transformation.
    inputConversion: InputConversion
    -- Returns the structure that contains the mapping template and its language (either XSLT or JSONATA).
    mapping: Mapping
    -- Returns the OutputConversion object, which contains the format options for the outbound transformation.
    outputConversion: OutputConversion
    -- Returns a structure that contains the Amazon S3 bucket and an array of the corresponding keys used to identify the location for your sample documents.
    sampleDocuments: SampleDocuments
end

local record GetTransformerJobInput
    -- Specifies the unique, system-generated identifier for a transformer run.
    transformerJobId: string
    -- Specifies the system-assigned unique identifier for the transformer.
    transformerId: string
end

local record GetTransformerJobOutput
    -- Returns the current state of the transformer job, either running, succeeded, or failed.
    status: string
    -- Returns the location for the output files. If the caller specified a directory for the output, then this contains the full path to the output file, including the file name generated by the service.
    outputFiles: {S3Location}
    -- Returns an optional error message, which gets populated when the job is not run successfully.
    message: string
end

local record ListCapabilitiesInput
    -- When additional results are obtained from the command, a NextToken parameter is returned in the output. You can then pass the NextToken parameter in a subsequent command to continue listing additional resources.
    nextToken: string
    -- Specifies the maximum number of capabilities to return.
    maxResults: number
end

local record ListCapabilitiesOutput
    -- Returns one or more capabilities associated with this partnership.
    capabilities: {CapabilitySummary}
    -- When additional results are obtained from the command, a NextToken parameter is returned in the output. You can then pass the NextToken parameter in a subsequent command to continue listing additional resources.
    nextToken: string
end

local record ListPartnershipsInput
    -- Specifies the unique, system-generated identifier for the profile connected to this partnership.
    profileId: string
    -- When additional results are obtained from the command, a NextToken parameter is returned in the output. You can then pass the NextToken parameter in a subsequent command to continue listing additional resources.
    nextToken: string
    -- Specifies the maximum number of capabilities to return.
    maxResults: number
end

local record ListPartnershipsOutput
    -- Specifies a list of your partnerships.
    partnerships: {PartnershipSummary}
    -- When additional results are obtained from the command, a NextToken parameter is returned in the output. You can then pass the NextToken parameter in a subsequent command to continue listing additional resources.
    nextToken: string
end

local record ListProfilesInput
    -- When additional results are obtained from the command, a NextToken parameter is returned in the output. You can then pass the NextToken parameter in a subsequent command to continue listing additional resources.
    nextToken: string
    -- Specifies the maximum number of profiles to return.
    maxResults: number
end

local record ListProfilesOutput
    -- Returns an array of ProfileSummary objects.
    profiles: {ProfileSummary}
    -- When additional results are obtained from the command, a NextToken parameter is returned in the output. You can then pass the NextToken parameter in a subsequent command to continue listing additional resources.
    nextToken: string
end

local record ListTagsForResourceInput
    -- Requests the tags associated with a particular Amazon Resource Name (ARN). An ARN is an identifier for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    ResourceARN: string
end

local record ListTagsForResourceOutput
    -- Returns the key-value pairs assigned to ARNs that you can use to group and search for resources by type. You can attach this metadata to resources (capabilities, partnerships, and so on) for any purpose.
    Tags: {Tag}
end

local record ListTransformersInput
    -- When additional results are obtained from the command, a NextToken parameter is returned in the output. You can then pass the NextToken parameter in a subsequent command to continue listing additional resources.
    nextToken: string
    -- Specifies the number of items to return for the API response.
    maxResults: number
end

local record ListTransformersOutput
    -- Returns an array of one or more transformer objects. For each transformer, a TransformerSummary object is returned. The TransformerSummary contains all the details for a specific transformer.
    transformers: {TransformerSummary}
    -- When additional results are obtained from the command, a NextToken parameter is returned in the output. You can then pass the NextToken parameter in a subsequent command to continue listing additional resources.
    nextToken: string
end

local record StartTransformerJobInput
    -- Specifies the location of the input file for the transformation. The location consists of an Amazon S3 bucket and prefix.
    inputFile: S3Location
    -- Specifies the location of the output file for the transformation. The location consists of an Amazon S3 bucket and prefix.
    outputLocation: S3Location
    -- Specifies the system-assigned unique identifier for the transformer.
    transformerId: string
    -- Reserved for future use.
    clientToken: string
end

local record StartTransformerJobOutput
    -- Returns the unique, system-generated identifier for a transformer run.
    transformerJobId: string
end

local record TagResourceInput
    -- Specifies an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    ResourceARN: string
    -- Specifies the key-value pairs assigned to ARNs that you can use to group and search for resources by type. You can attach this metadata to resources (capabilities, partnerships, and so on) for any purpose.
    Tags: {Tag}
end

local record TagResourceOutput
end

local record TestConversionInput
    -- Specify the source file for an outbound EDI request.
    source: ConversionSource
    -- Specify the format (X12 is the only currently supported format), and other details for the conversion target.
    target: ConversionTarget
end

local record TestConversionOutput
    -- Returns the converted file content.
    convertedFileContent: string
    -- Returns an array of validation messages that Amazon Web Services B2B Data Interchange generates during the conversion process. These messages include both standard EDI validation results and custom validation messages when custom validation rules are configured. Custom validation messages provide detailed feedback on element length constraints, code list validations, and element requirement checks applied during the outbound EDI generation process.
    validationMessages: {string}
end

local record TestMappingInput
    -- Specify the contents of the EDI (electronic data interchange) XML or JSON file that is used as input for the transform.
    inputFileContent: string
    -- Specifies the mapping template for the transformer. This template is used to map the parsed EDI file using JSONata or XSLT. This parameter is available for backwards compatibility. Use the Mapping data type instead.
    mappingTemplate: string
    -- Specifies that the currently supported file formats for EDI transformations are JSON and XML.
    fileFormat: string
end

local record TestMappingOutput
    -- Returns a string for the mapping that can be used to identify the mapping. Similar to a fingerprint
    mappedFileContent: string
end

local record TestParsingInput
    -- Specifies an S3Location object, which contains the Amazon S3 bucket and prefix for the location of the input file.
    inputFile: S3Location
    -- Specifies that the currently supported file formats for EDI transformations are JSON and XML.
    fileFormat: string
    -- Specifies the details for the EDI standard that is being used for the transformer. Currently, only X12 is supported. X12 is a set of standards and corresponding messages that define specific business documents.
    ediType: EdiType
    -- Specifies advanced options for parsing the input EDI file. These options allow for more granular control over the parsing process, including split options for X12 files.
    advancedOptions: AdvancedOptions
end

local record TestParsingOutput
    -- Returns the contents of the input file being tested, parsed according to the specified EDI (electronic data interchange) type.
    parsedFileContent: string
    -- Returns an array of parsed file contents when the input file is split according to the specified split options. Each element in the array represents a separate split file's parsed content.
    parsedSplitFileContents: {string}
    -- Returns an array of validation messages generated during EDI validation. These messages provide detailed information about validation errors, warnings, or confirmations based on the configured X12 validation rules such as element length constraints, code list validations, and element requirement checks. This field is populated when the TestParsing API validates EDI documents.
    validationMessages: {string}
end

local record UntagResourceInput
    -- Specifies an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    ResourceARN: string
    -- Specifies the key-value pairs assigned to ARNs that you can use to group and search for resources by type. You can attach this metadata to resources (capabilities, partnerships, and so on) for any purpose.
    TagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateCapabilityInput
    -- Specifies a system-assigned unique identifier for the capability.
    capabilityId: string
    -- Specifies a new name for the capability, to replace the existing name.
    name: string
    -- Specifies a structure that contains the details for a capability.
    configuration: CapabilityConfiguration
    -- Specifies one or more locations in Amazon S3, each specifying an EDI document that can be used with this capability. Each item contains the name of the bucket and the key, to identify the document's location.
    instructionsDocuments: {S3Location}
end

local record UpdateCapabilityOutput
    -- Returns a system-assigned unique identifier for the capability.
    capabilityId: string
    -- Returns an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    capabilityArn: string
    -- Returns the name of the capability, used to identify it.
    name: string
    -- Returns the type of the capability. Currently, only edi is supported.
    type: string
    -- Returns a structure that contains the details for a capability.
    configuration: CapabilityConfiguration
    -- Returns one or more locations in Amazon S3, each specifying an EDI document that can be used with this capability. Each item contains the name of the bucket and the key, to identify the document's location.
    instructionsDocuments: {S3Location}
    -- Returns a timestamp for creation date and time of the capability.
    createdAt: number
    -- Returns a timestamp for last time the capability was modified.
    modifiedAt: number
end

local record UpdatePartnershipInput
    -- Specifies the unique, system-generated identifier for a partnership.
    partnershipId: string
    -- The name of the partnership, used to identify it.
    name: string
    -- List of the capabilities associated with this partnership.
    capabilities: {string}
    -- To update, specify the structure that contains the details for the associated capabilities.
    capabilityOptions: CapabilityOptions
end

local record UpdatePartnershipOutput
    -- Returns the unique, system-generated identifier for the profile connected to this partnership.
    profileId: string
    -- Returns the unique, system-generated identifier for a partnership.
    partnershipId: string
    -- Returns an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    partnershipArn: string
    -- The name of the partnership, used to identify it.
    name: string
    -- Returns the email address associated with this trading partner.
    email: string
    -- Returns the phone number associated with the partnership.
    phone: string
    -- Returns one or more capabilities associated with this partnership.
    capabilities: {string}
    -- Returns the structure that contains the details for the associated capabilities.
    capabilityOptions: CapabilityOptions
    -- Returns the unique, system-generated identifier for a trading partner.
    tradingPartnerId: string
    -- Returns a timestamp that identifies the most recent date and time that the partnership was modified.
    createdAt: number
    -- Returns a timestamp that identifies the most recent date and time that the partnership was modified.
    modifiedAt: number
end

local record UpdateProfileInput
    -- Specifies the unique, system-generated identifier for the profile.
    profileId: string
    -- The name of the profile, used to identify it.
    name: string
    -- Specifies the email address associated with this customer profile.
    email: string
    -- Specifies the phone number associated with the profile.
    phone: string
    -- Specifies the name for the business associated with this profile.
    businessName: string
end

local record UpdateProfileOutput
    -- Returns the unique, system-generated identifier for the profile.
    profileId: string
    -- Returns an Amazon Resource Name (ARN) for the profile.
    profileArn: string
    -- Returns the name of the profile.
    name: string
    -- Returns the email address associated with this customer profile.
    email: string
    -- Returns the phone number associated with the profile.
    phone: string
    -- Returns the name for the business associated with this profile.
    businessName: string
    -- Specifies whether or not logging is enabled for this profile.
    logging: string
    -- Returns the name of the logging group.
    logGroupName: string
    -- Returns a timestamp for creation date and time of the profile.
    createdAt: number
    -- Returns a timestamp for last time the profile was modified.
    modifiedAt: number
end

local record UpdateTransformerInput
    -- Specifies the system-assigned unique identifier for the transformer.
    transformerId: string
    -- Specify a new name for the transformer, if you want to update it.
    name: string
    -- Specifies the transformer's status. You can update the state of the transformer from inactive to active.
    status: string
    -- Specifies that the currently supported file formats for EDI transformations are JSON and XML.
    fileFormat: string
    -- Specifies the mapping template for the transformer. This template is used to map the parsed EDI file using JSONata or XSLT. This parameter is available for backwards compatibility. Use the Mapping data type instead.
    mappingTemplate: string
    -- Specifies the details for the EDI standard that is being used for the transformer. Currently, only X12 is supported. X12 is a set of standards and corresponding messages that define specific business documents.
    ediType: EdiType
    -- Specifies a sample EDI document that is used by a transformer as a guide for processing the EDI data.
    sampleDocument: string
    -- To update, specify the InputConversion object, which contains the format options for the inbound transformation.
    inputConversion: InputConversion
    -- Specify the structure that contains the mapping template and its language (either XSLT or JSONATA).
    mapping: Mapping
    -- To update, specify the OutputConversion object, which contains the format options for the outbound transformation.
    outputConversion: OutputConversion
    -- Specify a structure that contains the Amazon S3 bucket and an array of the corresponding keys used to identify the location for your sample documents.
    sampleDocuments: SampleDocuments
end

local record UpdateTransformerOutput
    -- Returns the system-assigned unique identifier for the transformer.
    transformerId: string
    -- Returns an Amazon Resource Name (ARN) for a specific Amazon Web Services resource, such as a capability, partnership, profile, or transformer.
    transformerArn: string
    -- Returns the name of the transformer.
    name: string
    -- Returns the state of the newly created transformer. The transformer can be either active or inactive. For the transformer to be used in a capability, its status must active.
    status: string
    -- Returns a timestamp for creation date and time of the transformer.
    createdAt: number
    -- Returns a timestamp for last time the transformer was modified.
    modifiedAt: number
    -- Returns that the currently supported file formats for EDI transformations are JSON and XML.
    fileFormat: string
    -- Returns the mapping template for the transformer. This template is used to map the parsed EDI file using JSONata or XSLT.
    mappingTemplate: string
    -- Returns the details for the EDI standard that is being used for the transformer. Currently, only X12 is supported. X12 is a set of standards and corresponding messages that define specific business documents.
    ediType: EdiType
    -- Returns a sample EDI document that is used by a transformer as a guide for processing the EDI data.
    sampleDocument: string
    -- Returns the InputConversion object, which contains the format options for the inbound transformation.
    inputConversion: InputConversion
    -- Returns the structure that contains the mapping template and its language (either XSLT or JSONATA).
    mapping: Mapping
    -- Returns the OutputConversion object, which contains the format options for the outbound transformation.
    outputConversion: OutputConversion
    -- Returns a structure that contains the Amazon S3 bucket and an array of the corresponding keys used to identify the location for your sample documents.
    sampleDocuments: SampleDocuments
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when the calling command attempts to exceed one of the service quotas, for example trying to create a capability when you already have the maximum number of capabilities allowed.
local record ServiceQuotaExceededException
    message: string
    -- The ID for the resource that exceeded the quota, which caused the exception.
    resourceId: string
    -- The resource type (profile, partnership, transformer, or capability) that exceeded the quota, which caused the exception.
    resourceType: string
    -- The code responsible for exceeding the quota, which caused the exception.
    serviceCode: string
    -- The quota that was exceeded, which caused the exception.
    quotaCode: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when the calling command attempts to exceed one of the service quotas, for example trying to create a capability when you already have the maximum number of capabilities allowed.
local record ServiceQuotaExceededException
    message: string
    -- The ID for the resource that exceeded the quota, which caused the exception.
    resourceId: string
    -- The resource type (profile, partnership, transformer, or capability) that exceeded the quota, which caused the exception.
    resourceType: string
    -- The code responsible for exceeding the quota, which caused the exception.
    serviceCode: string
    -- The quota that was exceeded, which caused the exception.
    quotaCode: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when the calling command attempts to exceed one of the service quotas, for example trying to create a capability when you already have the maximum number of capabilities allowed.
local record ServiceQuotaExceededException
    message: string
    -- The ID for the resource that exceeded the quota, which caused the exception.
    resourceId: string
    -- The resource type (profile, partnership, transformer, or capability) that exceeded the quota, which caused the exception.
    resourceType: string
    -- The code responsible for exceeding the quota, which caused the exception.
    serviceCode: string
    -- The quota that was exceeded, which caused the exception.
    quotaCode: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when the calling command attempts to exceed one of the service quotas, for example trying to create a capability when you already have the maximum number of capabilities allowed.
local record ServiceQuotaExceededException
    message: string
    -- The ID for the resource that exceeded the quota, which caused the exception.
    resourceId: string
    -- The resource type (profile, partnership, transformer, or capability) that exceeded the quota, which caused the exception.
    resourceType: string
    -- The code responsible for exceeding the quota, which caused the exception.
    serviceCode: string
    -- The quota that was exceeded, which caused the exception.
    quotaCode: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when the calling command attempts to exceed one of the service quotas, for example trying to create a capability when you already have the maximum number of capabilities allowed.
local record ServiceQuotaExceededException
    message: string
    -- The ID for the resource that exceeded the quota, which caused the exception.
    resourceId: string
    -- The resource type (profile, partnership, transformer, or capability) that exceeded the quota, which caused the exception.
    resourceType: string
    -- The code responsible for exceeding the quota, which caused the exception.
    serviceCode: string
    -- The quota that was exceeded, which caused the exception.
    quotaCode: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when the calling command attempts to exceed one of the service quotas, for example trying to create a capability when you already have the maximum number of capabilities allowed.
local record ServiceQuotaExceededException
    message: string
    -- The ID for the resource that exceeded the quota, which caused the exception.
    resourceId: string
    -- The resource type (profile, partnership, transformer, or capability) that exceeded the quota, which caused the exception.
    resourceType: string
    -- The code responsible for exceeding the quota, which caused the exception.
    serviceCode: string
    -- The quota that was exceeded, which caused the exception.
    quotaCode: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when the calling command attempts to exceed one of the service quotas, for example trying to create a capability when you already have the maximum number of capabilities allowed.
local record ServiceQuotaExceededException
    message: string
    -- The ID for the resource that exceeded the quota, which caused the exception.
    resourceId: string
    -- The resource type (profile, partnership, transformer, or capability) that exceeded the quota, which caused the exception.
    resourceType: string
    -- The code responsible for exceeding the quota, which caused the exception.
    serviceCode: string
    -- The quota that was exceeded, which caused the exception.
    quotaCode: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- A conflict exception is thrown when you attempt to delete a resource (such as a profile or a capability) that is being used by other resources.
local record ConflictException
    message: string
end

-- This exception is thrown when an error occurs in the Amazon Web Services B2B Data Interchange service.
local record InternalServerException
    message: string
    -- The server attempts to retry a failed command.
    retryAfterSeconds: number
end

-- Occurs when the requested resource does not exist, or cannot be found. In some cases, the resource exists in a region other than the region specified in the API call.
local record ResourceNotFoundException
    message: string
end

-- Occurs when the calling command attempts to exceed one of the service quotas, for example trying to create a capability when you already have the maximum number of capabilities allowed.
local record ServiceQuotaExceededException
    message: string
    -- The ID for the resource that exceeded the quota, which caused the exception.
    resourceId: string
    -- The resource type (profile, partnership, transformer, or capability) that exceeded the quota, which caused the exception.
    resourceType: string
    -- The code responsible for exceeding the quota, which caused the exception.
    serviceCode: string
    -- The quota that was exceeded, which caused the exception.
    quotaCode: string
end

-- The request was denied due to throttling: the data speed and rendering may be limited depending on various parameters and conditions.
local record ThrottlingException
    message: string
    -- The server attempts to retry a command that was throttled.
    retryAfterSeconds: number
end

-- Occurs when a B2BI object cannot be validated against a request from another object. This exception can be thrown during standard EDI validation or when custom validation rules fail, such as when element length constraints are violated, invalid codes are used in code list validations, or required elements are missing based on configured element requirement rules.
local record ValidationException
    Message: string
end

local type CapabilityType = "edi"

local type FileFormat = "XML" | "JSON" | "NOT_USED"

local type Logging = "ENABLED" | "DISABLED"

local type MappingType = "JSONATA" | "XSLT"

local type TransformerJobStatus = "running" | "succeeded" | "failed"

local type TransformerStatus = "active" | "inactive"

local record M
    CreateCapabilityInput: CreateCapabilityInput
    CreateCapabilityOutput: CreateCapabilityOutput
    CreatePartnershipInput: CreatePartnershipInput
    CreatePartnershipOutput: CreatePartnershipOutput
    CreateProfileInput: CreateProfileInput
    CreateProfileOutput: CreateProfileOutput
    CreateStarterMappingTemplateInput: CreateStarterMappingTemplateInput
    CreateStarterMappingTemplateOutput: CreateStarterMappingTemplateOutput
    CreateTransformerInput: CreateTransformerInput
    CreateTransformerOutput: CreateTransformerOutput
    DeleteCapabilityInput: DeleteCapabilityInput
    DeleteCapabilityOutput: DeleteCapabilityOutput
    DeletePartnershipInput: DeletePartnershipInput
    DeletePartnershipOutput: DeletePartnershipOutput
    DeleteProfileInput: DeleteProfileInput
    DeleteProfileOutput: DeleteProfileOutput
    DeleteTransformerInput: DeleteTransformerInput
    DeleteTransformerOutput: DeleteTransformerOutput
    GenerateMappingInput: GenerateMappingInput
    GenerateMappingOutput: GenerateMappingOutput
    GetCapabilityInput: GetCapabilityInput
    GetCapabilityOutput: GetCapabilityOutput
    GetPartnershipInput: GetPartnershipInput
    GetPartnershipOutput: GetPartnershipOutput
    GetProfileInput: GetProfileInput
    GetProfileOutput: GetProfileOutput
    GetTransformerInput: GetTransformerInput
    GetTransformerOutput: GetTransformerOutput
    GetTransformerJobInput: GetTransformerJobInput
    GetTransformerJobOutput: GetTransformerJobOutput
    ListCapabilitiesInput: ListCapabilitiesInput
    ListCapabilitiesOutput: ListCapabilitiesOutput
    ListPartnershipsInput: ListPartnershipsInput
    ListPartnershipsOutput: ListPartnershipsOutput
    ListProfilesInput: ListProfilesInput
    ListProfilesOutput: ListProfilesOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    ListTransformersInput: ListTransformersInput
    ListTransformersOutput: ListTransformersOutput
    StartTransformerJobInput: StartTransformerJobInput
    StartTransformerJobOutput: StartTransformerJobOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    TestConversionInput: TestConversionInput
    TestConversionOutput: TestConversionOutput
    TestMappingInput: TestMappingInput
    TestMappingOutput: TestMappingOutput
    TestParsingInput: TestParsingInput
    TestParsingOutput: TestParsingOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateCapabilityInput: UpdateCapabilityInput
    UpdateCapabilityOutput: UpdateCapabilityOutput
    UpdatePartnershipInput: UpdatePartnershipInput
    UpdatePartnershipOutput: UpdatePartnershipOutput
    UpdateProfileInput: UpdateProfileInput
    UpdateProfileOutput: UpdateProfileOutput
    UpdateTransformerInput: UpdateTransformerInput
    UpdateTransformerOutput: UpdateTransformerOutput
end

return M
