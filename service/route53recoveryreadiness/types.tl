-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record CreateCellInput
        -- The name of the cell to create.
        CellName: string
        -- A list of cell Amazon Resource Names (ARNs) contained within this cell, for use in nested cells. For example, Availability Zones within specific Amazon Web Services Regions.
        Cells: {string}
        Tags: {string : string}
    end

    record CreateCellOutput
        -- The Amazon Resource Name (ARN) for the cell.
        CellArn: string
        -- The name of the cell.
        CellName: string
        -- A list of cell ARNs.
        Cells: {string}
        -- The readiness scope for the cell, which can be a cell Amazon Resource Name (ARN) or a recovery group ARN. This is a list but currently can have only one element.
        ParentReadinessScopes: {string}
        -- Tags on the resources.
        Tags: {string : string}
    end

    record CreateCrossAccountAuthorizationInput
        -- The cross-account authorization.
        CrossAccountAuthorization: string
    end

    record CreateCrossAccountAuthorizationOutput
        -- The cross-account authorization.
        CrossAccountAuthorization: string
    end

    record CreateReadinessCheckInput
        -- The name of the readiness check to create.
        ReadinessCheckName: string
        -- The name of the resource set to check.
        ResourceSetName: string
        Tags: {string : string}
    end

    record CreateReadinessCheckOutput
        -- The Amazon Resource Name (ARN) associated with a readiness check.
        ReadinessCheckArn: string
        -- Name of a readiness check.
        ReadinessCheckName: string
        -- Name of the resource set to be checked.
        ResourceSet: string
        Tags: {string : string}
    end

    record CreateRecoveryGroupInput
        -- A list of the cell Amazon Resource Names (ARNs) in the recovery group.
        Cells: {string}
        -- The name of the recovery group to create.
        RecoveryGroupName: string
        Tags: {string : string}
    end

    record CreateRecoveryGroupOutput
        -- A list of a cell's Amazon Resource Names (ARNs).
        Cells: {string}
        -- The Amazon Resource Name (ARN) for the recovery group.
        RecoveryGroupArn: string
        -- The name of the recovery group.
        RecoveryGroupName: string
        -- The tags associated with the recovery group.
        Tags: {string : string}
    end

    record CreateResourceSetInput
        -- The name of the resource set to create.
        ResourceSetName: string
        -- The resource type of the resources in the resource set. Enter one of the following values for resource type: AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup, AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table, AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer, AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck, AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC, AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource
        ResourceSetType: string
        -- A list of resource objects in the resource set.
        Resources: {Resource}
        -- A tag to associate with the parameters for a resource set.
        Tags: {string : string}
    end

    record CreateResourceSetOutput
        -- The Amazon Resource Name (ARN) for the resource set.
        ResourceSetArn: string
        -- The name of the resource set.
        ResourceSetName: string
        -- The resource type of the resources in the resource set. Enter one of the following values for resource type: AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup, AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table, AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer, AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck, AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC, AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource
        ResourceSetType: string
        -- A list of resource objects.
        Resources: {Resource}
        Tags: {string : string}
    end

    record DeleteCellInput
        -- The name of the cell.
        CellName: string
    end

    record DeleteCellOutput
    end

    record DeleteCrossAccountAuthorizationInput
        -- The cross-account authorization.
        CrossAccountAuthorization: string
    end

    record DeleteCrossAccountAuthorizationOutput
    end

    record DeleteReadinessCheckInput
        -- Name of a readiness check.
        ReadinessCheckName: string
    end

    record DeleteReadinessCheckOutput
    end

    record DeleteRecoveryGroupInput
        -- The name of a recovery group.
        RecoveryGroupName: string
    end

    record DeleteRecoveryGroupOutput
    end

    record DeleteResourceSetInput
        -- Name of a resource set.
        ResourceSetName: string
    end

    record DeleteResourceSetOutput
    end

    record GetArchitectureRecommendationsInput
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- The name of a recovery group.
        RecoveryGroupName: string
    end

    record GetArchitectureRecommendationsOutput
        -- The time that a recovery group was last assessed for recommendations, in UTC ISO-8601 format.
        LastAuditTimestamp: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- A list of the recommendations for the customer's application.
        Recommendations: {Recommendation}
    end

    record GetCellInput
        -- The name of the cell.
        CellName: string
    end

    record GetCellOutput
        -- The Amazon Resource Name (ARN) for the cell.
        CellArn: string
        -- The name of the cell.
        CellName: string
        -- A list of cell ARNs.
        Cells: {string}
        -- The readiness scope for the cell, which can be a cell Amazon Resource Name (ARN) or a recovery group ARN. This is a list but currently can have only one element.
        ParentReadinessScopes: {string}
        -- Tags on the resources.
        Tags: {string : string}
    end

    record GetCellReadinessSummaryInput
        -- The name of the cell.
        CellName: string
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
    end

    record GetCellReadinessSummaryOutput
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- The readiness at a cell level.
        Readiness: string
        -- Summaries for the readiness checks that make up the cell.
        ReadinessChecks: {ReadinessCheckSummary}
    end

    record GetReadinessCheckInput
        -- Name of a readiness check.
        ReadinessCheckName: string
    end

    record GetReadinessCheckOutput
        -- The Amazon Resource Name (ARN) associated with a readiness check.
        ReadinessCheckArn: string
        -- Name of a readiness check.
        ReadinessCheckName: string
        -- Name of the resource set to be checked.
        ResourceSet: string
        Tags: {string : string}
    end

    record GetReadinessCheckResourceStatusInput
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- Name of a readiness check.
        ReadinessCheckName: string
        -- The resource identifier, which is the Amazon Resource Name (ARN) or the identifier generated for the resource by Application Recovery Controller (for example, for a DNS target resource).
        ResourceIdentifier: string
    end

    record GetReadinessCheckResourceStatusOutput
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- The readiness at a rule level.
        Readiness: string
        -- Details of the rule's results.
        Rules: {RuleResult}
    end

    record GetReadinessCheckStatusInput
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- Name of a readiness check.
        ReadinessCheckName: string
    end

    record GetReadinessCheckStatusOutput
        -- Top level messages for readiness check status
        Messages: {Message}
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- The readiness at rule level.
        Readiness: string
        -- Summary of the readiness of resources.
        Resources: {ResourceResult}
    end

    record GetRecoveryGroupInput
        -- The name of a recovery group.
        RecoveryGroupName: string
    end

    record GetRecoveryGroupOutput
        -- A list of a cell's Amazon Resource Names (ARNs).
        Cells: {string}
        -- The Amazon Resource Name (ARN) for the recovery group.
        RecoveryGroupArn: string
        -- The name of the recovery group.
        RecoveryGroupName: string
        -- The tags associated with the recovery group.
        Tags: {string : string}
    end

    record GetRecoveryGroupReadinessSummaryInput
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- The name of a recovery group.
        RecoveryGroupName: string
    end

    record GetRecoveryGroupReadinessSummaryOutput
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- The readiness status at a recovery group level.
        Readiness: string
        -- Summaries of the readiness checks for the recovery group.
        ReadinessChecks: {ReadinessCheckSummary}
    end

    record GetResourceSetInput
        -- Name of a resource set.
        ResourceSetName: string
    end

    record GetResourceSetOutput
        -- The Amazon Resource Name (ARN) for the resource set.
        ResourceSetArn: string
        -- The name of the resource set.
        ResourceSetName: string
        -- The resource type of the resources in the resource set. Enter one of the following values for resource type: AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup, AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table, AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer, AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck, AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC, AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource
        ResourceSetType: string
        -- A list of resource objects.
        Resources: {Resource}
        Tags: {string : string}
    end

    record ListCellsInput
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
    end

    record ListCellsOutput
        -- A list of cells.
        Cells: {CellOutput}
        -- The token that identifies which batch of results you want to see.
        NextToken: string
    end

    record ListCrossAccountAuthorizationsInput
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
    end

    record ListCrossAccountAuthorizationsOutput
        -- A list of cross-account authorizations.
        CrossAccountAuthorizations: {string}
        -- The token that identifies which batch of results you want to see.
        NextToken: string
    end

    record ListReadinessChecksInput
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
    end

    record ListReadinessChecksOutput
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- A list of readiness checks associated with the account.
        ReadinessChecks: {ReadinessCheckOutput}
    end

    record ListRecoveryGroupsInput
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
    end

    record ListRecoveryGroupsOutput
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- A list of recovery groups.
        RecoveryGroups: {RecoveryGroupOutput}
    end

    record ListResourceSetsInput
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
    end

    record ListResourceSetsOutput
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- A list of resource sets associated with the account.
        ResourceSets: {ResourceSetOutput}
    end

    record ListRulesInput
        -- The number of objects that you want to return with this call.
        MaxResults: number
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- The resource type that a readiness rule applies to.
        ResourceType: string
    end

    record ListRulesOperationOutput
        -- The token that identifies which batch of results you want to see.
        NextToken: string
        -- A list of readiness rules for a specific resource type.
        Rules: {ListRulesOutput}
    end

    record ListTagsForResourcesInput
        -- The Amazon Resource Name (ARN) for a resource.
        ResourceArn: string
    end

    record ListTagsForResourcesOutput
        Tags: {string : string}
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) for a resource.
        ResourceArn: string
        Tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) for a resource.
        ResourceArn: string
        -- The keys for tags you add to resources.
        TagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateCellInput
        -- The name of the cell.
        CellName: string
        -- A list of cell Amazon Resource Names (ARNs), which completely replaces the previous list.
        Cells: {string}
    end

    record UpdateCellOutput
        -- The Amazon Resource Name (ARN) for the cell.
        CellArn: string
        -- The name of the cell.
        CellName: string
        -- A list of cell ARNs.
        Cells: {string}
        -- The readiness scope for the cell, which can be a cell Amazon Resource Name (ARN) or a recovery group ARN. This is a list but currently can have only one element.
        ParentReadinessScopes: {string}
        -- Tags on the resources.
        Tags: {string : string}
    end

    -- Name of a readiness check to describe.
    record UpdateReadinessCheckInput
        -- Name of a readiness check.
        ReadinessCheckName: string
        -- The name of the resource set to be checked.
        ResourceSetName: string
    end

    record UpdateReadinessCheckOutput
        -- The Amazon Resource Name (ARN) associated with a readiness check.
        ReadinessCheckArn: string
        -- Name of a readiness check.
        ReadinessCheckName: string
        -- Name of the resource set to be checked.
        ResourceSet: string
        Tags: {string : string}
    end

    -- Name of a recovery group.
    record UpdateRecoveryGroupInput
        -- A list of cell Amazon Resource Names (ARNs). This list completely replaces the previous list.
        Cells: {string}
        -- The name of a recovery group.
        RecoveryGroupName: string
    end

    record UpdateRecoveryGroupOutput
        -- A list of a cell's Amazon Resource Names (ARNs).
        Cells: {string}
        -- The Amazon Resource Name (ARN) for the recovery group.
        RecoveryGroupArn: string
        -- The name of the recovery group.
        RecoveryGroupName: string
        -- The tags associated with the recovery group.
        Tags: {string : string}
    end

    -- Name of a resource set.
    record UpdateResourceSetInput
        -- Name of a resource set.
        ResourceSetName: string
        -- The resource type of the resources in the resource set. Enter one of the following values for resource type: AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup, AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table, AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer, AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck, AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC, AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource
        ResourceSetType: string
        -- A list of resource objects.
        Resources: {Resource}
    end

    record UpdateResourceSetOutput
        -- The Amazon Resource Name (ARN) for the resource set.
        ResourceSetArn: string
        -- The name of the resource set.
        ResourceSetName: string
        -- The resource type of the resources in the resource set. Enter one of the following values for resource type: AWS::ApiGateway::Stage, AWS::ApiGatewayV2::Stage, AWS::AutoScaling::AutoScalingGroup, AWS::CloudWatch::Alarm, AWS::EC2::CustomerGateway, AWS::DynamoDB::Table, AWS::EC2::Volume, AWS::ElasticLoadBalancing::LoadBalancer, AWS::ElasticLoadBalancingV2::LoadBalancer, AWS::Lambda::Function, AWS::MSK::Cluster, AWS::RDS::DBCluster, AWS::Route53::HealthCheck, AWS::SQS::Queue, AWS::SNS::Topic, AWS::SNS::Subscription, AWS::EC2::VPC, AWS::EC2::VPNConnection, AWS::EC2::VPNGateway, AWS::Route53RecoveryReadiness::DNSTargetResource
        ResourceSetType: string
        -- A list of resource objects.
        Resources: {Resource}
        Tags: {string : string}
    end

    -- User does not have sufficient access to perform this action.
    record AccessDeniedException
        Message: string
    end

    -- Updating or deleting a resource can cause an inconsistent state.
    record ConflictException
        Message: string
    end

    -- An unexpected error occurred.
    record InternalServerException
        Message: string
    end

    -- Request was denied due to request throttling.
    record ThrottlingException
        Message: string
    end

    -- The input fails to satisfy the constraints specified by an AWS service.
    record ValidationException
        Message: string
    end

    -- The requested resource does not exist.
    record ResourceNotFoundException
        Message: string
    end

    enum Readiness
        "READY"
        "NOT_READY"
        "UNKNOWN"
        "NOT_AUTHORIZED"
    end

end

return M
