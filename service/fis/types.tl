-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    enum AccountTargeting
        "single-account"
        "multi-account"
    end

    -- Describes a parameter for an action.
    record ActionParameter
        -- The parameter description.
        description: string
        -- Indicates whether the parameter is required.
        required: boolean
    end

    -- Describes a target for an action.
    record ActionTarget
        -- The resource type of the target.
        resourceType: string
    end

    -- Describes an action. For more information, see FIS actions
    -- in the Fault Injection Service User Guide.
    record Action
        -- The ID of the action.
        id: string
        -- The Amazon Resource Name (ARN) of the action.
        arn: string
        -- The description for the action.
        description: string
        -- The action parameters, if applicable.
        parameters: {string : ActionParameter}
        -- The supported targets for the action.
        targets: {string : ActionTarget}
        -- The tags for the action.
        tags: {string : string}
    end

    enum ActionsMode
        "skip-all"
        "run-all"
    end

    -- Provides a summary of an action.
    record ActionSummary
        -- The ID of the action.
        id: string
        -- The Amazon Resource Name (ARN) of the action.
        arn: string
        -- The description for the action.
        description: string
        -- The targets for the action.
        targets: {string : ActionTarget}
        -- The tags for the action.
        tags: {string : string}
    end

    -- The request could not be processed because of a conflict.
    record ConflictException
        message: string
    end

    -- Specifies an action for an experiment template.
    -- For more information, see Actions
    -- in the Fault Injection Service User Guide.
    record CreateExperimentTemplateActionInput
        -- The ID of the action. The format of the action ID is: aws:service-name:action-type.
        actionId: string
        -- A description for the action.
        description: string
        -- The parameters for the action, if applicable.
        parameters: {string : string}
        -- The targets for the action.
        targets: {string : string}
        -- The name of the action that must be completed before the current action starts. Omit this parameter to run the action at the start of the experiment.
        startAfter: {string}
    end

    enum EmptyTargetResolutionMode
        "fail"
        "skip"
    end

    -- Specifies experiment options for an experiment template.
    record CreateExperimentTemplateExperimentOptionsInput
        -- Specifies the account targeting setting for experiment options.
        accountTargeting: string
        -- Specifies the empty target resolution mode for experiment options.
        emptyTargetResolutionMode: string
    end

    -- Specifies the CloudWatch dashboard for the experiment report.
    record ReportConfigurationCloudWatchDashboardInput
        -- The Amazon Resource Name (ARN) of the CloudWatch dashboard to include in the experiment report.
        dashboardIdentifier: string
    end

    -- Specifies the data sources for the experiment report.
    record ExperimentTemplateReportConfigurationDataSourcesInput
        -- The CloudWatch dashboards to include as data sources in the experiment report.
        cloudWatchDashboards: {ReportConfigurationCloudWatchDashboardInput}
    end

    -- Specifies the S3 destination for the experiment report.
    record ReportConfigurationS3OutputInput
        -- The name of the S3 bucket where the experiment report will be stored.
        bucketName: string
        -- The prefix of the S3 bucket where the experiment report will be stored.
        prefix: string
    end

    -- Specifies the outputs for the experiment templates.
    record ExperimentTemplateReportConfigurationOutputsInput
        -- The S3 destination for the experiment report.
        s3Configuration: ReportConfigurationS3OutputInput
    end

    -- Specifies the configuration for experiment reports.
    record CreateExperimentTemplateReportConfigurationInput
        -- The output destinations of the experiment report.
        outputs: ExperimentTemplateReportConfigurationOutputsInput
        -- The data sources for the experiment report.
        dataSources: ExperimentTemplateReportConfigurationDataSourcesInput
        -- The duration before the experiment start time for the data sources to include in the report.
        preExperimentDuration: string
        -- The duration after the experiment end time for the data sources to include in the report.
        postExperimentDuration: string
    end

    -- Specifies the configuration for experiment logging to Amazon CloudWatch Logs.
    record ExperimentTemplateCloudWatchLogsLogConfigurationInput
        -- The Amazon Resource Name (ARN) of the destination Amazon CloudWatch Logs log group.
        logGroupArn: string
    end

    -- Specifies the configuration for experiment logging to Amazon S3.
    record ExperimentTemplateS3LogConfigurationInput
        -- The name of the destination bucket.
        bucketName: string
        -- The bucket prefix.
        prefix: string
    end

    -- Specifies the configuration for experiment logging.
    record CreateExperimentTemplateLogConfigurationInput
        -- The configuration for experiment logging to Amazon CloudWatch Logs.
        cloudWatchLogsConfiguration: ExperimentTemplateCloudWatchLogsLogConfigurationInput
        -- The configuration for experiment logging to Amazon S3.
        s3Configuration: ExperimentTemplateS3LogConfigurationInput
        -- The schema version.
        logSchemaVersion: number
    end

    -- Specifies a stop condition for an experiment template.
    record CreateExperimentTemplateStopConditionInput
        -- The source for the stop condition. Specify aws:cloudwatch:alarm if the stop
        -- condition is defined by a CloudWatch alarm. Specify none if there is no stop
        -- condition.
        source: string
        -- The Amazon Resource Name (ARN) of the CloudWatch alarm. This is required if the source is
        -- a CloudWatch alarm.
        value: string
    end

    -- Specifies a filter used for the target resource input in an experiment template.
    -- For more information, see Resource filters
    -- in the Fault Injection Service User Guide.
    record ExperimentTemplateTargetInputFilter
        -- The attribute path for the filter.
        path: string
        -- The attribute values for the filter.
        values: {string}
    end

    -- Specifies a target for an experiment. You must specify at least one Amazon Resource Name (ARN) or
    -- at least one resource tag. You cannot specify both ARNs and tags.
    -- For more information, see Targets
    -- in the Fault Injection Service User Guide.
    record CreateExperimentTemplateTargetInput
        -- The resource type. The resource type must be supported for the specified action.
        resourceType: string
        -- The Amazon Resource Names (ARNs) of the resources.
        resourceArns: {string}
        -- The tags for the target resources.
        resourceTags: {string : string}
        -- The filters to apply to identify target resources using specific attributes.
        filters: {ExperimentTemplateTargetInputFilter}
        -- Scopes the identified resources to a specific count of the resources at random, or a percentage of the resources. All identified resources are included in the target.
        -- ALL - Run the action on all identified targets. This is the default.
        -- COUNT(n) - Run the action on the specified number of targets, chosen from the identified targets at random.
        -- For example, COUNT(1) selects one of the targets.
        -- PERCENT(n) - Run the action on the specified percentage of targets, chosen from the identified targets
        -- at random. For example, PERCENT(25) selects 25% of the targets.
        selectionMode: string
        -- The resource type parameters.
        parameters: {string : string}
    end

    record CreateExperimentTemplateInput
        -- Unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
        clientToken: string
        -- A description for the experiment template.
        description: string
        -- The stop conditions.
        stopConditions: {CreateExperimentTemplateStopConditionInput}
        -- The targets for the experiment.
        targets: {string : CreateExperimentTemplateTargetInput}
        -- The actions for the experiment.
        actions: {string : CreateExperimentTemplateActionInput}
        -- The Amazon Resource Name (ARN) of an IAM role that grants the FIS service permission to perform service actions on your behalf.
        roleArn: string
        -- The tags to apply to the experiment template.
        tags: {string : string}
        -- The configuration for experiment logging.
        logConfiguration: CreateExperimentTemplateLogConfigurationInput
        -- The experiment options for the experiment template.
        experimentOptions: CreateExperimentTemplateExperimentOptionsInput
        -- The experiment report configuration for the experiment template.
        experimentReportConfiguration: CreateExperimentTemplateReportConfigurationInput
    end

    -- Describes an action for an experiment template.
    record ExperimentTemplateAction
        -- The ID of the action.
        actionId: string
        -- A description for the action.
        description: string
        -- The parameters for the action.
        parameters: {string : string}
        -- The targets for the action.
        targets: {string : string}
        -- The name of the action that must be completed before the current action starts.
        startAfter: {string}
    end

    -- Describes the experiment options for an experiment template.
    record ExperimentTemplateExperimentOptions
        -- The account targeting setting for an experiment template.
        accountTargeting: string
        -- The empty target resolution mode for an experiment template.
        emptyTargetResolutionMode: string
    end

    -- The CloudWatch dashboards to include as data sources in the experiment report.
    record ExperimentTemplateReportConfigurationCloudWatchDashboard
        -- The Amazon Resource Name (ARN) of the CloudWatch dashboard to include in the experiment report.
        dashboardIdentifier: string
    end

    -- Describes the data sources for the experiment report.
    record ExperimentTemplateReportConfigurationDataSources
        -- The CloudWatch dashboards to include as data sources in the experiment report.
        cloudWatchDashboards: {ExperimentTemplateReportConfigurationCloudWatchDashboard}
    end

    -- Describes the S3 destination for the experiment report.
    record ReportConfigurationS3Output
        -- The name of the S3 bucket where the experiment report will be stored.
        bucketName: string
        -- The prefix of the S3 bucket where the experiment report will be stored.
        prefix: string
    end

    -- The output destinations of the experiment report.
    record ExperimentTemplateReportConfigurationOutputs
        -- The S3 destination for the experiment report.
        s3Configuration: ReportConfigurationS3Output
    end

    -- Describes the experiment report configuration. For more information, see Experiment report configurations for AWS FIS.
    record ExperimentTemplateReportConfiguration
        -- Describes the output destinations of the experiment report.
        outputs: ExperimentTemplateReportConfigurationOutputs
        -- The data sources for the experiment report.
        dataSources: ExperimentTemplateReportConfigurationDataSources
        -- The duration before the experiment start time for the data sources to include in the report.
        preExperimentDuration: string
        -- The duration after the experiment end time for the data sources to include in the report.
        postExperimentDuration: string
    end

    -- Describes the configuration for experiment logging to Amazon CloudWatch Logs.
    record ExperimentTemplateCloudWatchLogsLogConfiguration
        -- The Amazon Resource Name (ARN) of the destination Amazon CloudWatch Logs log group.
        logGroupArn: string
    end

    -- Describes the configuration for experiment logging to Amazon S3.
    record ExperimentTemplateS3LogConfiguration
        -- The name of the destination bucket.
        bucketName: string
        -- The bucket prefix.
        prefix: string
    end

    -- Describes the configuration for experiment logging.
    record ExperimentTemplateLogConfiguration
        -- The configuration for experiment logging to Amazon CloudWatch Logs.
        cloudWatchLogsConfiguration: ExperimentTemplateCloudWatchLogsLogConfiguration
        -- The configuration for experiment logging to Amazon S3.
        s3Configuration: ExperimentTemplateS3LogConfiguration
        -- The schema version.
        logSchemaVersion: number
    end

    -- Describes a stop condition for an experiment template.
    record ExperimentTemplateStopCondition
        -- The source for the stop condition.
        source: string
        -- The Amazon Resource Name (ARN) of the CloudWatch alarm, if applicable.
        value: string
    end

    -- Describes a filter used for the target resources in an experiment template.
    record ExperimentTemplateTargetFilter
        -- The attribute path for the filter.
        path: string
        -- The attribute values for the filter.
        values: {string}
    end

    -- Describes a target for an experiment template.
    record ExperimentTemplateTarget
        -- The resource type.
        resourceType: string
        -- The Amazon Resource Names (ARNs) of the targets.
        resourceArns: {string}
        -- The tags for the target resources.
        resourceTags: {string : string}
        -- The filters to apply to identify target resources using specific attributes.
        filters: {ExperimentTemplateTargetFilter}
        -- Scopes the identified resources to a specific count or percentage.
        selectionMode: string
        -- The resource type parameters.
        parameters: {string : string}
    end

    -- Describes an experiment template.
    record ExperimentTemplate
        -- The ID of the experiment template.
        id: string
        -- The Amazon Resource Name (ARN) of the experiment template.
        arn: string
        -- The description for the experiment template.
        description: string
        -- The targets for the experiment.
        targets: {string : ExperimentTemplateTarget}
        -- The actions for the experiment.
        actions: {string : ExperimentTemplateAction}
        -- The stop conditions for the experiment.
        stopConditions: {ExperimentTemplateStopCondition}
        -- The time the experiment template was created.
        creationTime: number
        -- The time the experiment template was last updated.
        lastUpdateTime: number
        -- The Amazon Resource Name (ARN) of an IAM role.
        roleArn: string
        -- The tags for the experiment template.
        tags: {string : string}
        -- The configuration for experiment logging.
        logConfiguration: ExperimentTemplateLogConfiguration
        -- The experiment options for an experiment template.
        experimentOptions: ExperimentTemplateExperimentOptions
        -- The count of target account configurations for the experiment template.
        targetAccountConfigurationsCount: number
        -- Describes the report configuration for the experiment template.
        experimentReportConfiguration: ExperimentTemplateReportConfiguration
    end

    record CreateExperimentTemplateOutput
        -- Information about the experiment template.
        experimentTemplate: ExperimentTemplate
    end

    -- The specified resource cannot be found.
    record ResourceNotFoundException
        message: string
    end

    -- You have exceeded your service quota.
    record ServiceQuotaExceededException
        message: string
    end

    -- The specified input is not valid, or fails to satisfy the constraints for the request.
    record ValidationException
        message: string
    end

    record CreateTargetAccountConfigurationInput
        -- Unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
        clientToken: string
        -- The experiment template ID.
        experimentTemplateId: string
        -- The Amazon Web Services account ID of the target account.
        accountId: string
        -- The Amazon Resource Name (ARN) of an IAM role for the target account.
        roleArn: string
        -- The description of the target account.
        description: string
    end

    -- Describes a target account configuration.
    record TargetAccountConfiguration
        -- The Amazon Resource Name (ARN) of an IAM role for the target account.
        roleArn: string
        -- The Amazon Web Services account ID of the target account.
        accountId: string
        -- The description of the target account.
        description: string
    end

    record CreateTargetAccountConfigurationOutput
        -- Information about the target account configuration.
        targetAccountConfiguration: TargetAccountConfiguration
    end

    record DeleteExperimentTemplateInput
        -- The ID of the experiment template.
        id: string
    end

    record DeleteExperimentTemplateOutput
        -- Information about the experiment template.
        experimentTemplate: ExperimentTemplate
    end

    record DeleteTargetAccountConfigurationInput
        -- The ID of the experiment template.
        experimentTemplateId: string
        -- The Amazon Web Services account ID of the target account.
        accountId: string
    end

    record DeleteTargetAccountConfigurationOutput
        -- Information about the target account configuration.
        targetAccountConfiguration: TargetAccountConfiguration
    end

    enum ExperimentActionStatus
        "pending"
        "initiating"
        "running"
        "completed"
        "cancelled"
        "stopping"
        "stopped"
        "failed"
        "skipped"
    end

    -- Describes the state of an action.
    record ExperimentActionState
        -- The state of the action.
        status: string
        -- The reason for the state.
        reason: string
    end

    -- Describes the action for an experiment.
    record ExperimentAction
        -- The ID of the action.
        actionId: string
        -- The description for the action.
        description: string
        -- The parameters for the action.
        parameters: {string : string}
        -- The targets for the action.
        targets: {string : string}
        -- The name of the action that must be completed before this action starts.
        startAfter: {string}
        -- The state of the action.
        state: ExperimentActionState
        -- The time that the action started.
        startTime: number
        -- The time that the action ended.
        endTime: number
    end

    -- Describes the options for an experiment.
    record ExperimentOptions
        -- The account targeting setting for an experiment.
        accountTargeting: string
        -- The empty target resolution mode for an experiment.
        emptyTargetResolutionMode: string
        -- The actions mode of the experiment that is set from the StartExperiment API command.
        actionsMode: string
    end

    -- Describes the S3 destination for the report.
    record ExperimentReportS3Report
        -- The Amazon Resource Name (ARN) of the generated report.
        arn: string
        -- The report type for the experiment report.
        reportType: string
    end

    -- Describes the error when experiment report generation has failed.
    record ExperimentReportError
        -- The error code for the failed experiment report generation.
        code: string
    end

    enum ExperimentReportStatus
        "pending"
        "running"
        "completed"
        "cancelled"
        "failed"
    end

    -- Describes the state of the experiment report generation.
    record ExperimentReportState
        -- The state of the experiment report generation.
        status: string
        -- The reason for the state of the experiment report generation.
        reason: string
        -- The error information of the experiment when the experiment report generation has failed.
        error: ExperimentReportError
    end

    -- Describes the experiment report.
    record ExperimentReport
        -- The state of the experiment report.
        state: ExperimentReportState
        -- The S3 destination of the experiment report.
        s3Reports: {ExperimentReportS3Report}
    end

    -- Specifies the CloudWatch dashboard to include in the experiment report. The dashboard widgets will be captured as snapshot graphs within the report.
    record ExperimentReportConfigurationCloudWatchDashboard
        -- The Amazon Resource Name (ARN) of the CloudWatch dashboard to include in the experiment report.
        dashboardIdentifier: string
    end

    -- Describes the data sources for the experiment report.
    record ExperimentReportConfigurationDataSources
        -- The CloudWatch dashboards to include as data sources in the experiment report.
        cloudWatchDashboards: {ExperimentReportConfigurationCloudWatchDashboard}
    end

    -- Specifies the S3 destination for the experiment report.
    record ExperimentReportConfigurationOutputsS3Configuration
        -- The name of the S3 bucket where the experiment report will be stored.
        bucketName: string
        -- The prefix of the S3 bucket where the experiment report will be stored.
        prefix: string
    end

    -- Describes the output destinations of the experiment report.
    record ExperimentReportConfigurationOutputs
        -- The S3 destination for the experiment report.
        s3Configuration: ExperimentReportConfigurationOutputsS3Configuration
    end

    -- Describes the report configuration for the experiment. For more information, see Experiment report configurations for AWS FIS.
    record ExperimentReportConfiguration
        -- The output destinations of the experiment report.
        outputs: ExperimentReportConfigurationOutputs
        -- The data sources for the experiment report.
        dataSources: ExperimentReportConfigurationDataSources
        -- The duration before the experiment start time for the data sources to include in the report.
        preExperimentDuration: string
        -- The duration after the experiment end time for the data sources to include in the report.
        postExperimentDuration: string
    end

    -- Describes the configuration for experiment logging to Amazon CloudWatch Logs.
    record ExperimentCloudWatchLogsLogConfiguration
        -- The Amazon Resource Name (ARN) of the destination Amazon CloudWatch Logs log group.
        logGroupArn: string
    end

    -- Describes the configuration for experiment logging to Amazon S3.
    record ExperimentS3LogConfiguration
        -- The name of the destination bucket.
        bucketName: string
        -- The bucket prefix.
        prefix: string
    end

    -- Describes the configuration for experiment logging.
    record ExperimentLogConfiguration
        -- The configuration for experiment logging to Amazon CloudWatch Logs.
        cloudWatchLogsConfiguration: ExperimentCloudWatchLogsLogConfiguration
        -- The configuration for experiment logging to Amazon S3.
        s3Configuration: ExperimentS3LogConfiguration
        -- The schema version.
        logSchemaVersion: number
    end

    -- Describes the error when an experiment has failed.
    record ExperimentError
        -- The Amazon Web Services Account ID where the experiment failure occurred.
        accountId: string
        -- The error code for the failed experiment.
        code: string
        -- Context for the section of the experiment template that failed.
        location: string
    end

    enum ExperimentStatus
        "pending"
        "initiating"
        "running"
        "completed"
        "stopping"
        "stopped"
        "failed"
        "cancelled"
    end

    -- Describes the state of an experiment.
    record ExperimentState
        -- The state of the experiment.
        status: string
        -- The reason for the state.
        reason: string
        -- The error information of the experiment when the action has failed.
        error: ExperimentError
    end

    -- Describes the stop condition for an experiment.
    record ExperimentStopCondition
        -- The source for the stop condition.
        source: string
        -- The Amazon Resource Name (ARN) of the CloudWatch alarm, if applicable.
        value: string
    end

    -- Describes a filter used for the target resources in an experiment.
    record ExperimentTargetFilter
        -- The attribute path for the filter.
        path: string
        -- The attribute values for the filter.
        values: {string}
    end

    -- Describes a target for an experiment.
    record ExperimentTarget
        -- The resource type.
        resourceType: string
        -- The Amazon Resource Names (ARNs) of the resources.
        resourceArns: {string}
        -- The tags for the target resources.
        resourceTags: {string : string}
        -- The filters to apply to identify target resources using specific attributes.
        filters: {ExperimentTargetFilter}
        -- Scopes the identified resources to a specific count or percentage.
        selectionMode: string
        -- The resource type parameters.
        parameters: {string : string}
    end

    -- Describes an experiment.
    record Experiment
        -- The ID of the experiment.
        id: string
        -- The Amazon Resource Name (ARN) of the experiment.
        arn: string
        -- The ID of the experiment template.
        experimentTemplateId: string
        -- The Amazon Resource Name (ARN) of an IAM role that grants the FIS service permission to perform service actions on your behalf.
        roleArn: string
        -- The state of the experiment.
        state: ExperimentState
        -- The targets for the experiment.
        targets: {string : ExperimentTarget}
        -- The actions for the experiment.
        actions: {string : ExperimentAction}
        -- The stop conditions for the experiment.
        stopConditions: {ExperimentStopCondition}
        -- The time that the experiment was created.
        creationTime: number
        -- The time that the experiment started.
        startTime: number
        -- The time that the experiment ended.
        endTime: number
        -- The tags for the experiment.
        tags: {string : string}
        -- The configuration for experiment logging.
        logConfiguration: ExperimentLogConfiguration
        -- The experiment options for the experiment.
        experimentOptions: ExperimentOptions
        -- The count of target account configurations for the experiment.
        targetAccountConfigurationsCount: number
        -- The experiment report configuration for the experiment.
        experimentReportConfiguration: ExperimentReportConfiguration
        -- The experiment report for the experiment.
        experimentReport: ExperimentReport
    end

    -- Provides a summary of an experiment.
    record ExperimentSummary
        -- The ID of the experiment.
        id: string
        -- The Amazon Resource Name (ARN) of the experiment.
        arn: string
        -- The ID of the experiment template.
        experimentTemplateId: string
        -- The state of the experiment.
        state: ExperimentState
        -- The time that the experiment was created.
        creationTime: number
        -- The tags for the experiment.
        tags: {string : string}
        -- The experiment options for the experiment.
        experimentOptions: ExperimentOptions
    end

    -- Describes a target account configuration for an experiment.
    record ExperimentTargetAccountConfiguration
        -- The Amazon Resource Name (ARN) of an IAM role for the target account.
        roleArn: string
        -- The Amazon Web Services account ID of the target account.
        accountId: string
        -- The description of the target account.
        description: string
    end

    -- Provides a summary of a target account configuration.
    record ExperimentTargetAccountConfigurationSummary
        -- The Amazon Resource Name (ARN) of an IAM role for the target account.
        roleArn: string
        -- The Amazon Web Services account ID of the target account.
        accountId: string
        -- The description of the target account.
        description: string
    end

    -- Provides a summary of an experiment template.
    record ExperimentTemplateSummary
        -- The ID of the experiment template.
        id: string
        -- The Amazon Resource Name (ARN) of the experiment template.
        arn: string
        -- The description of the experiment template.
        description: string
        -- The time that the experiment template was created.
        creationTime: number
        -- The time that the experiment template was last updated.
        lastUpdateTime: number
        -- The tags for the experiment template.
        tags: {string : string}
    end

    record GetActionInput
        -- The ID of the action.
        id: string
    end

    record GetActionOutput
        -- Information about the action.
        action: Action
    end

    record GetExperimentInput
        -- The ID of the experiment.
        id: string
    end

    record GetExperimentOutput
        -- Information about the experiment.
        experiment: Experiment
    end

    record GetExperimentTargetAccountConfigurationInput
        -- The ID of the experiment.
        experimentId: string
        -- The Amazon Web Services account ID of the target account.
        accountId: string
    end

    record GetExperimentTargetAccountConfigurationOutput
        -- Information about the target account configuration.
        targetAccountConfiguration: ExperimentTargetAccountConfiguration
    end

    record GetExperimentTemplateInput
        -- The ID of the experiment template.
        id: string
    end

    record GetExperimentTemplateOutput
        -- Information about the experiment template.
        experimentTemplate: ExperimentTemplate
    end

    record GetSafetyLeverInput
        -- The ID of the safety lever.
        id: string
    end

    enum SafetyLeverStatus
        "disengaged"
        "engaged"
        "engaging"
    end

    -- Describes the state of the safety lever.
    record SafetyLeverState
        -- The state of the safety lever.
        status: string
        -- The reason for the state of the safety lever.
        reason: string
    end

    -- Describes a safety lever.
    record SafetyLever
        -- The ID of the safety lever.
        id: string
        -- The Amazon Resource Name (ARN) of the safety lever.
        arn: string
        -- The state of the safety lever.
        state: SafetyLeverState
    end

    record GetSafetyLeverOutput
        -- Information about the safety lever.
        safetyLever: SafetyLever
    end

    record GetTargetAccountConfigurationInput
        -- The ID of the experiment template.
        experimentTemplateId: string
        -- The Amazon Web Services account ID of the target account.
        accountId: string
    end

    record GetTargetAccountConfigurationOutput
        -- Information about the target account configuration.
        targetAccountConfiguration: TargetAccountConfiguration
    end

    record GetTargetResourceTypeInput
        -- The resource type.
        resourceType: string
    end

    -- Describes the parameters for a resource type. Use parameters to determine which tasks are
    -- identified during target resolution.
    record TargetResourceTypeParameter
        -- A description of the parameter.
        description: string
        -- Indicates whether the parameter is required.
        required: boolean
    end

    -- Describes a resource type.
    record TargetResourceType
        -- The resource type.
        resourceType: string
        -- A description of the resource type.
        description: string
        -- The parameters for the resource type.
        parameters: {string : TargetResourceTypeParameter}
    end

    record GetTargetResourceTypeOutput
        -- Information about the resource type.
        targetResourceType: TargetResourceType
    end

    record ListActionsInput
        -- The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    record ListActionsOutput
        -- The actions.
        actions: {ActionSummary}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    record ListExperimentResolvedTargetsInput
        -- The ID of the experiment.
        experimentId: string
        -- The maximum number of results to return with a single call. To retrieve the remaining results,
        -- make another call with the returned nextToken value.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
        -- The name of the target.
        targetName: string
    end

    -- Describes a resolved target.
    record ResolvedTarget
        -- The resource type of the target.
        resourceType: string
        -- The name of the target.
        targetName: string
        -- Information about the target.
        targetInformation: {string : string}
    end

    record ListExperimentResolvedTargetsOutput
        -- The resolved targets.
        resolvedTargets: {ResolvedTarget}
        -- The token to use to retrieve the next page of results.
        -- This value is null when there are no more results to return.
        nextToken: string
    end

    record ListExperimentsInput
        -- The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
        -- The ID of the experiment template.
        experimentTemplateId: string
    end

    record ListExperimentsOutput
        -- The experiments.
        experiments: {ExperimentSummary}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    record ListExperimentTargetAccountConfigurationsInput
        -- The ID of the experiment.
        experimentId: string
        -- The token for the next page of results.
        nextToken: string
    end

    record ListExperimentTargetAccountConfigurationsOutput
        -- The target account configurations.
        targetAccountConfigurations: {ExperimentTargetAccountConfigurationSummary}
        -- The token to use to retrieve the next page of results.
        -- This value is null when there are no more results to return.
        nextToken: string
    end

    record ListExperimentTemplatesInput
        -- The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    record ListExperimentTemplatesOutput
        -- The experiment templates.
        experimentTemplates: {ExperimentTemplateSummary}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- The tags for the resource.
        tags: {string : string}
    end

    record ListTargetAccountConfigurationsInput
        -- The ID of the experiment template.
        experimentTemplateId: string
        -- The maximum number of results to return with a single call. To retrieve the remaining results,
        -- make another call with the returned nextToken value.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    -- Provides a summary of a target account configuration.
    record TargetAccountConfigurationSummary
        -- The Amazon Resource Name (ARN) of an IAM role for the target account.
        roleArn: string
        -- The Amazon Web Services account ID of the target account.
        accountId: string
        -- The description of the target account.
        description: string
    end

    record ListTargetAccountConfigurationsOutput
        -- The target account configurations.
        targetAccountConfigurations: {TargetAccountConfigurationSummary}
        -- The token to use to retrieve the next page of results.
        -- This value is null when there are no more results to return.
        nextToken: string
    end

    record ListTargetResourceTypesInput
        -- The maximum number of results to return with a single call. To retrieve the remaining results, make another call with the returned nextToken value.
        maxResults: number
        -- The token for the next page of results.
        nextToken: string
    end

    -- Describes a resource type.
    record TargetResourceTypeSummary
        -- The resource type.
        resourceType: string
        -- A description of the resource type.
        description: string
    end

    record ListTargetResourceTypesOutput
        -- The target resource types.
        targetResourceTypes: {TargetResourceTypeSummary}
        -- The token to use to retrieve the next page of results. This value is null when there are no more results to return.
        nextToken: string
    end

    -- Specifies experiment options for running an experiment.
    record StartExperimentExperimentOptionsInput
        -- Specifies the actions mode for experiment options.
        actionsMode: string
    end

    record StartExperimentInput
        -- Unique, case-sensitive identifier that you provide to ensure the idempotency of the request.
        clientToken: string
        -- The ID of the experiment template.
        experimentTemplateId: string
        -- The experiment options for running the experiment.
        experimentOptions: StartExperimentExperimentOptionsInput
        -- The tags to apply to the experiment.
        tags: {string : string}
    end

    record StartExperimentOutput
        -- Information about the experiment.
        experiment: Experiment
    end

    record StopExperimentInput
        -- The ID of the experiment.
        id: string
    end

    record StopExperimentOutput
        -- Information about the experiment.
        experiment: Experiment
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
        -- The tags for the resource.
        tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
        -- The tag keys to remove.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    -- Specifies an action for an experiment template.
    record UpdateExperimentTemplateActionInputItem
        -- The ID of the action.
        actionId: string
        -- A description for the action.
        description: string
        -- The parameters for the action, if applicable.
        parameters: {string : string}
        -- The targets for the action.
        targets: {string : string}
        -- The name of the action that must be completed before the current action starts. Omit this parameter to run the action at the start of the experiment.
        startAfter: {string}
    end

    -- Specifies an experiment option for an experiment template.
    record UpdateExperimentTemplateExperimentOptionsInput
        -- The empty target resolution mode of the experiment template.
        emptyTargetResolutionMode: string
    end

    -- Specifies the input for the experiment report configuration.
    record UpdateExperimentTemplateReportConfigurationInput
        -- Describes the output destinations of the experiment report.
        outputs: ExperimentTemplateReportConfigurationOutputsInput
        -- The data sources for the experiment report.
        dataSources: ExperimentTemplateReportConfigurationDataSourcesInput
        -- The duration before the experiment start time for the data sources to include in the report.
        preExperimentDuration: string
        -- The duration after the experiment end time for the data sources to include in the report.
        postExperimentDuration: string
    end

    -- Specifies the configuration for experiment logging.
    record UpdateExperimentTemplateLogConfigurationInput
        -- The configuration for experiment logging to Amazon CloudWatch Logs.
        cloudWatchLogsConfiguration: ExperimentTemplateCloudWatchLogsLogConfigurationInput
        -- The configuration for experiment logging to Amazon S3.
        s3Configuration: ExperimentTemplateS3LogConfigurationInput
        -- The schema version.
        logSchemaVersion: number
    end

    -- Specifies a stop condition for an experiment. You can define a stop condition as a CloudWatch alarm.
    record UpdateExperimentTemplateStopConditionInput
        -- The source for the stop condition. Specify aws:cloudwatch:alarm if the stop
        -- condition is defined by a CloudWatch alarm. Specify none if there is no stop
        -- condition.
        source: string
        -- The Amazon Resource Name (ARN) of the CloudWatch alarm.
        value: string
    end

    -- Specifies a target for an experiment. You must specify at least one Amazon Resource Name (ARN) or at least one resource tag. You cannot specify both.
    record UpdateExperimentTemplateTargetInput
        -- The resource type. The resource type must be supported for the specified action.
        resourceType: string
        -- The Amazon Resource Names (ARNs) of the targets.
        resourceArns: {string}
        -- The tags for the target resources.
        resourceTags: {string : string}
        -- The filters to apply to identify target resources using specific attributes.
        filters: {ExperimentTemplateTargetInputFilter}
        -- Scopes the identified resources to a specific count or percentage.
        selectionMode: string
        -- The resource type parameters.
        parameters: {string : string}
    end

    record UpdateExperimentTemplateInput
        -- The ID of the experiment template.
        id: string
        -- A description for the template.
        description: string
        -- The stop conditions for the experiment.
        stopConditions: {UpdateExperimentTemplateStopConditionInput}
        -- The targets for the experiment.
        targets: {string : UpdateExperimentTemplateTargetInput}
        -- The actions for the experiment.
        actions: {string : UpdateExperimentTemplateActionInputItem}
        -- The Amazon Resource Name (ARN) of an IAM role that grants the FIS service permission to perform service actions on your behalf.
        roleArn: string
        -- The configuration for experiment logging.
        logConfiguration: UpdateExperimentTemplateLogConfigurationInput
        -- The experiment options for the experiment template.
        experimentOptions: UpdateExperimentTemplateExperimentOptionsInput
        -- The experiment report configuration for the experiment template.
        experimentReportConfiguration: UpdateExperimentTemplateReportConfigurationInput
    end

    record UpdateExperimentTemplateOutput
        -- Information about the experiment template.
        experimentTemplate: ExperimentTemplate
    end

    enum SafetyLeverStatusInput
        "disengaged"
        "engaged"
    end

    -- Specifies a state for a safety lever.
    record UpdateSafetyLeverStateInput
        -- The updated state of the safety lever.
        status: string
        -- The reason for updating the state of the safety lever.
        reason: string
    end

    record UpdateSafetyLeverStateOperationInput
        -- The ID of the safety lever.
        id: string
        -- The state of the safety lever.
        state: UpdateSafetyLeverStateInput
    end

    record UpdateSafetyLeverStateOutput
        -- Information about the safety lever.
        safetyLever: SafetyLever
    end

    record UpdateTargetAccountConfigurationInput
        -- The ID of the experiment template.
        experimentTemplateId: string
        -- The Amazon Web Services account ID of the target account.
        accountId: string
        -- The Amazon Resource Name (ARN) of an IAM role for the target account.
        roleArn: string
        -- The description of the target account.
        description: string
    end

    record UpdateTargetAccountConfigurationOutput
        -- Information about the target account configuration.
        targetAccountConfiguration: TargetAccountConfiguration
    end

end

return M
