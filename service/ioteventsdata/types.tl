-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record BatchAcknowledgeAlarmInput
        -- The list of acknowledge action requests. You can specify up to 10 requests per operation.
        acknowledgeActionRequests: {AcknowledgeAlarmActionRequest}
    end

    record BatchAcknowledgeAlarmOutput
        -- A list of errors associated with the request, or null if there are no errors.
        -- Each error entry contains an entry ID that helps you identify the entry that failed.
        errorEntries: {BatchAlarmActionErrorEntry}
    end

    record BatchDeleteDetectorInput
        -- The list of one or more detectors to be deleted.
        detectors: {DeleteDetectorRequest}
    end

    record BatchDeleteDetectorOutput
        -- A list of errors associated with the request, or an empty array ([]) if there are no errors. Each error entry contains a messageId that helps you identify the entry that failed.
        batchDeleteDetectorErrorEntries: {BatchDeleteDetectorErrorEntry}
    end

    record BatchDisableAlarmInput
        -- The list of disable action requests. You can specify up to 10 requests per operation.
        disableActionRequests: {DisableAlarmActionRequest}
    end

    record BatchDisableAlarmOutput
        -- A list of errors associated with the request, or null if there are no errors.
        -- Each error entry contains an entry ID that helps you identify the entry that failed.
        errorEntries: {BatchAlarmActionErrorEntry}
    end

    record BatchEnableAlarmInput
        -- The list of enable action requests. You can specify up to 10 requests per operation.
        enableActionRequests: {EnableAlarmActionRequest}
    end

    record BatchEnableAlarmOutput
        -- A list of errors associated with the request, or null if there are no errors.
        -- Each error entry contains an entry ID that helps you identify the entry that failed.
        errorEntries: {BatchAlarmActionErrorEntry}
    end

    record BatchPutMessageInput
        -- The list of messages to send. Each message has the following format: '{ "messageId":
        -- "string", "inputName": "string", "payload": "string"}'
        messages: {Message}
    end

    record BatchPutMessageOutput
        -- A list of any errors encountered when sending the messages.
        BatchPutMessageErrorEntries: {BatchPutMessageErrorEntry}
    end

    record BatchResetAlarmInput
        -- The list of reset action requests. You can specify up to 10 requests per operation.
        resetActionRequests: {ResetAlarmActionRequest}
    end

    record BatchResetAlarmOutput
        -- A list of errors associated with the request, or null if there are no errors.
        -- Each error entry contains an entry ID that helps you identify the entry that failed.
        errorEntries: {BatchAlarmActionErrorEntry}
    end

    record BatchSnoozeAlarmInput
        -- The list of snooze action requests. You can specify up to 10 requests per operation.
        snoozeActionRequests: {SnoozeAlarmActionRequest}
    end

    record BatchSnoozeAlarmOutput
        -- A list of errors associated with the request, or null if there are no errors.
        -- Each error entry contains an entry ID that helps you identify the entry that failed.
        errorEntries: {BatchAlarmActionErrorEntry}
    end

    record BatchUpdateDetectorInput
        -- The list of detectors (instances) to update, along with the values to update.
        detectors: {UpdateDetectorRequest}
    end

    record BatchUpdateDetectorOutput
        -- A list of those detector updates that resulted in errors. (If an error is listed here, the
        -- specific update did not occur.)
        batchUpdateDetectorErrorEntries: {BatchUpdateDetectorErrorEntry}
    end

    record DescribeAlarmInput
        -- The name of the alarm model.
        alarmModelName: string
        -- The value of the key used as a filter to select only the alarms associated with the
        -- key.
        keyValue: string
    end

    record DescribeAlarmOutput
        -- Contains information about an alarm.
        alarm: Alarm
    end

    record DescribeDetectorInput
        -- The name of the detector model whose detectors (instances) you want information
        -- about.
        detectorModelName: string
        -- A filter used to limit results to detectors (instances) created because of the given key
        -- ID.
        keyValue: string
    end

    record DescribeDetectorOutput
        -- Information about the detector (instance).
        detector: Detector
    end

    record ListAlarmsInput
        -- The name of the alarm model.
        alarmModelName: string
        -- The token that you can use to return the next set of results.
        nextToken: string
        -- The maximum number of results to be returned per request.
        maxResults: number
    end

    record ListAlarmsOutput
        -- A list that summarizes each alarm.
        alarmSummaries: {AlarmSummary}
        -- The token that you can use to return the next set of results,
        -- or null if there are no more results.
        nextToken: string
    end

    record ListDetectorsInput
        -- The name of the detector model whose detectors (instances) are listed.
        detectorModelName: string
        -- A filter that limits results to those detectors (instances) in the given state.
        stateName: string
        -- The token that you can use to return the next set of results.
        nextToken: string
        -- The maximum number of results to be returned per request.
        maxResults: number
    end

    record ListDetectorsOutput
        -- A list of summary information about the detectors (instances).
        detectorSummaries: {DetectorSummary}
        -- The token that you can use to return the next set of results,
        -- or null if there are no more results.
        nextToken: string
    end

    -- An internal failure occurred.
    record InternalFailureException
        -- The message for the exception.
        message: string
    end

    -- The request was invalid.
    record InvalidRequestException
        -- The message for the exception.
        message: string
    end

    -- The service is currently unavailable.
    record ServiceUnavailableException
        -- The message for the exception.
        message: string
    end

    -- The request could not be completed due to throttling.
    record ThrottlingException
        -- The message for the exception.
        message: string
    end

    -- The resource was not found.
    record ResourceNotFoundException
        -- The message for the exception.
        message: string
    end

end

return M
