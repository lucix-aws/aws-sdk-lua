-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    enum ErrorCode
        "BadRequest"
        "Conflict"
        "Forbidden"
        "NotFound"
        "PreconditionFailed"
        "ResourceLimitExceeded"
        "ServiceFailure"
        "AccessDenied"
        "ServiceUnavailable"
        "Throttled"
        "Throttling"
        "Unauthorized"
        "Unprocessable"
        "VoiceConnectorGroupAssociationsExist"
        "PhoneNumberAssociationsExist"
    end

    -- You don't have permissions to perform the requested operation.
    record AccessDeniedException
        Code: string
        Message: string
    end

    enum AccountStatus
        "Suspended"
        "Active"
    end

    enum AccountType
        "Team"
        "EnterpriseDirectory"
        "EnterpriseLWA"
        "EnterpriseOIDC"
    end

    enum License
        "Basic"
        "Plus"
        "Pro"
        "ProTrial"
    end

    -- An Active Directory (AD) group whose members are granted permission to act as delegates.
    record SigninDelegateGroup
        -- The group name.
        GroupName: string
    end

    -- The Amazon Chime account details. An AWS account can have multiple Amazon Chime accounts.
    record Account
        -- The AWS account ID.
        AwsAccountId: string
        -- The Amazon Chime account ID.
        AccountId: string
        -- The Amazon Chime account name.
        Name: string
        -- The Amazon Chime account type. For more information about different account types, see
        -- Managing Your Amazon Chime Accounts in the Amazon Chime Administration
        -- Guide.
        AccountType: string
        -- The Amazon Chime account creation timestamp, in ISO 8601 format.
        CreatedTimestamp: number
        -- The default license for the Amazon Chime account.
        DefaultLicense: string
        -- Supported licenses for the Amazon Chime account.
        SupportedLicenses: {string}
        -- The status of the account.
        AccountStatus: string
        -- The sign-in delegate groups associated with the account.
        SigninDelegateGroups: {SigninDelegateGroup}
    end

    -- Settings related to the Amazon Chime account. This includes settings that start or stop
    -- remote control of shared screens, or start or stop the dial-out option in the Amazon Chime web application. For more information about these settings, see
    -- Use the Policies Page in the Amazon Chime Administration Guide.
    record AccountSettings
        -- Setting that stops or starts remote control of shared screens during meetings.
        DisableRemoteControl: boolean
        -- Setting that allows meeting participants to choose the Call me at a phone number option. For more information, see
        -- Join a Meeting without the Amazon Chime App.
        EnableDialOut: boolean
    end

    -- The Alexa for Business metadata associated with an Amazon Chime user, used to integrate Alexa for Business with a device.
    record AlexaForBusinessMetadata
        -- Starts or stops Alexa for Business.
        IsAlexaForBusinessEnabled: boolean
        -- The ARN of the room resource.
        AlexaForBusinessRoomArn: string
    end

    record AssociatePhoneNumberWithUserInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The user ID.
        UserId: string
        -- The phone number, in E.164 format.
        E164PhoneNumber: string
    end

    record AssociatePhoneNumberWithUserOutput
    end

    -- The input parameters don't match the service's restrictions.
    record BadRequestException
        Code: string
        Message: string
    end

    -- The client is permanently forbidden from making the request.
    record ForbiddenException
        Code: string
        Message: string
    end

    -- One or more of the resources in the request does not exist in the system.
    record NotFoundException
        Code: string
        Message: string
    end

    -- The service encountered an unexpected error.
    record ServiceFailureException
        Code: string
        Message: string
    end

    -- The service is currently unavailable.
    record ServiceUnavailableException
        Code: string
        Message: string
    end

    -- The client exceeded its request rate limit.
    record ThrottledClientException
        Code: string
        Message: string
    end

    -- The client is not currently authorized to make the request.
    record UnauthorizedClientException
        Code: string
        Message: string
    end

    record AssociateSigninDelegateGroupsWithAccountInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The sign-in delegate groups.
        SigninDelegateGroups: {SigninDelegateGroup}
    end

    record AssociateSigninDelegateGroupsWithAccountOutput
    end

    enum RoomMembershipRole
        "Administrator"
        "Member"
    end

    -- Membership details, such as member ID and member role.
    record MembershipItem
        -- The member ID.
        MemberId: string
        -- The member role.
        Role: string
    end

    record BatchCreateRoomMembershipInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The room ID.
        RoomId: string
        -- The list of membership items.
        MembershipItemList: {MembershipItem}
    end

    -- The list of errors returned when a member action results in an error.
    record MemberError
        -- The member ID.
        MemberId: string
        -- The error code.
        ErrorCode: string
        -- The error message.
        ErrorMessage: string
    end

    record BatchCreateRoomMembershipOutput
        -- If the action fails for one or more of the member IDs in the request, a list of the member IDs is returned, along with error codes and error messages.
        Errors: {MemberError}
    end

    record BatchDeletePhoneNumberInput
        -- List of phone number IDs.
        PhoneNumberIds: {string}
    end

    -- If the phone number action fails for one or more of the phone numbers in the request, a list of
    -- the phone numbers is returned, along with error codes and error messages.
    record PhoneNumberError
        -- The phone number ID for which the action failed.
        PhoneNumberId: string
        -- The error code.
        ErrorCode: string
        -- The error message.
        ErrorMessage: string
    end

    record BatchDeletePhoneNumberOutput
        -- If the action fails for one or more of the phone numbers in the request, a list of the phone numbers is returned, along with error codes and error messages.
        PhoneNumberErrors: {PhoneNumberError}
    end

    record BatchSuspendUserInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The request containing the user IDs to suspend.
        UserIdList: {string}
    end

    -- The list of errors returned when errors are encountered during the BatchSuspendUser, BatchUnsuspendUser, or
    -- BatchUpdateUser actions. This includes user IDs, error codes, and error messages.
    record UserError
        -- The user ID for which the action failed.
        UserId: string
        -- The error code.
        ErrorCode: string
        -- The error message.
        ErrorMessage: string
    end

    record BatchSuspendUserOutput
        -- If the BatchSuspendUser action fails for one or more of the user IDs in the
        -- request, a list of the user IDs is returned, along with error codes and error messages.
        UserErrors: {UserError}
    end

    record BatchUnsuspendUserInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The request containing the user IDs to unsuspend.
        UserIdList: {string}
    end

    record BatchUnsuspendUserOutput
        -- If the BatchUnsuspendUser action fails for one or more of the user IDs in the
        -- request, a list of the user IDs is returned, along with error codes and error messages.
        UserErrors: {UserError}
    end

    enum PhoneNumberProductType
        "BusinessCalling"
        "VoiceConnector"
        "SipMediaApplicationDialIn"
    end

    -- The phone number ID, product type, or calling name fields to update, used with the
    -- BatchUpdatePhoneNumber and UpdatePhoneNumber actions.
    record UpdatePhoneNumberRequestItem
        -- The phone number ID to update.
        PhoneNumberId: string
        -- The product type to update.
        ProductType: string
        -- The outbound calling name to update.
        CallingName: string
    end

    record BatchUpdatePhoneNumberInput
        -- The request containing the phone number IDs and product types or calling names to update.
        UpdatePhoneNumberRequestItems: {UpdatePhoneNumberRequestItem}
    end

    record BatchUpdatePhoneNumberOutput
        -- If the action fails for one or more of the phone numbers in the request, a list of the phone numbers is returned, along with error codes and error messages.
        PhoneNumberErrors: {PhoneNumberError}
    end

    enum UserType
        "PrivateUser"
        "SharedDevice"
    end

    -- The user ID and user fields to update, used with the
    -- BatchUpdateUser action.
    record UpdateUserRequestItem
        -- The user ID.
        UserId: string
        -- The user license type.
        LicenseType: string
        -- The user type.
        UserType: string
        -- The Alexa for Business metadata.
        AlexaForBusinessMetadata: AlexaForBusinessMetadata
    end

    record BatchUpdateUserInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The request containing the user IDs and details to update.
        UpdateUserRequestItems: {UpdateUserRequestItem}
    end

    record BatchUpdateUserOutput
        -- If the BatchUpdateUser action fails for one or more of the user IDs in the
        -- request, a list of the user IDs is returned, along with error codes and error messages.
        UserErrors: {UserError}
    end

    enum BotType
        "ChatBot"
    end

    -- A resource that allows Enterprise account administrators to configure an interface to receive events from Amazon Chime.
    record Bot
        -- The bot ID.
        BotId: string
        -- The unique ID for the bot user.
        UserId: string
        -- The bot display name.
        DisplayName: string
        -- The bot type.
        BotType: string
        -- When true, the bot is stopped from running in your account.
        Disabled: boolean
        -- The bot creation timestamp, in ISO 8601 format.
        CreatedTimestamp: number
        -- The updated bot timestamp, in ISO 8601 format.
        UpdatedTimestamp: number
        -- The bot email address.
        BotEmail: string
        -- The security token used to authenticate Amazon Chime with the outgoing event endpoint.
        SecurityToken: string
    end

    -- The Amazon Chime Business Calling settings for the administrator's AWS account. Includes any
    -- Amazon S3 buckets designated for storing call detail records.
    record BusinessCallingSettings
        -- The Amazon S3 bucket designated for call detail record storage.
        CdrBucket: string
    end

    enum CallingNameStatus
        "Unassigned"
        "UpdateInProgress"
        "UpdateSucceeded"
        "UpdateFailed"
    end

    -- The request could not be processed because of conflict in the current state of the
    -- resource.
    record ConflictException
        Code: string
        Message: string
    end

    -- The retention settings that determine how long to retain conversation messages for an Amazon Chime Enterprise account.
    record ConversationRetentionSettings
        -- The number of days for which to retain conversation messages.
        RetentionDays: number
    end

    record CreateAccountInput
        -- The name of the Amazon Chime account.
        Name: string
    end

    record CreateAccountOutput
        -- The Amazon Chime account details.
        Account: Account
    end

    record CreateBotInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The bot display name.
        DisplayName: string
        -- The domain of the Amazon Chime Enterprise account.
        Domain: string
    end

    record CreateBotOutput
        -- The bot details.
        Bot: Bot
    end

    -- The request exceeds the resource limit.
    record ResourceLimitExceededException
        Code: string
        Message: string
    end

    record CreateMeetingDialOutInput
        -- The Amazon Chime SDK meeting ID.
        MeetingId: string
        -- Phone number used as the caller ID when the remote party receives a call.
        FromPhoneNumber: string
        -- Phone number called when inviting someone to a meeting.
        ToPhoneNumber: string
        -- Token used by the Amazon Chime SDK attendee. Call the CreateAttendee action to get a join token.
        JoinToken: string
    end

    record CreateMeetingDialOutOutput
        -- Unique ID that tracks API calls.
        TransactionId: string
    end

    record CreatePhoneNumberOrderInput
        -- The phone number product type.
        ProductType: string
        -- List of phone numbers, in E.164 format.
        E164PhoneNumbers: {string}
    end

    enum OrderedPhoneNumberStatus
        "Processing"
        "Acquired"
        "Failed"
    end

    -- A phone number for which an order has been placed.
    record OrderedPhoneNumber
        -- The phone number, in E.164 format.
        E164PhoneNumber: string
        -- The phone number status.
        Status: string
    end

    enum PhoneNumberOrderStatus
        "Processing"
        "Successful"
        "Failed"
        "Partial"
    end

    -- The details of a phone number order created for Amazon Chime.
    record PhoneNumberOrder
        -- The phone number order ID.
        PhoneNumberOrderId: string
        -- The phone number order product type.
        ProductType: string
        -- The status of the phone number order.
        Status: string
        -- The ordered phone number details, such as the phone number in E.164 format and the phone
        -- number status.
        OrderedPhoneNumbers: {OrderedPhoneNumber}
        -- The phone number order creation time stamp, in ISO 8601 format.
        CreatedTimestamp: number
        -- The updated phone number order time stamp, in ISO 8601 format.
        UpdatedTimestamp: number
    end

    record CreatePhoneNumberOrderOutput
        -- The phone number order details.
        PhoneNumberOrder: PhoneNumberOrder
    end

    record CreateRoomInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The room name.
        Name: string
        -- The idempotency token for the request.
        ClientRequestToken: string
    end

    -- The Amazon Chime chat room details.
    record Room
        -- The room ID.
        RoomId: string
        -- The room name.
        Name: string
        -- The Amazon Chime account ID.
        AccountId: string
        -- The identifier of the room creator.
        CreatedBy: string
        -- The room creation timestamp, in ISO 8601 format.
        CreatedTimestamp: number
        -- The room update timestamp, in ISO 8601 format.
        UpdatedTimestamp: number
    end

    record CreateRoomOutput
        -- The room details.
        Room: Room
    end

    record CreateRoomMembershipInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The room ID.
        RoomId: string
        -- The Amazon Chime member ID (user ID or bot ID).
        MemberId: string
        -- The role of the member.
        Role: string
    end

    enum MemberType
        "User"
        "Bot"
        "Webhook"
    end

    -- The member details, such as email address, name, member ID, and member type.
    record Member
        -- The member ID (user ID or bot ID).
        MemberId: string
        -- The member type.
        MemberType: string
        -- The member email address.
        Email: string
        -- The member name.
        FullName: string
        -- The Amazon Chime account ID.
        AccountId: string
    end

    -- The room membership details.
    record RoomMembership
        -- The room ID.
        RoomId: string
        -- The member details, such as email address, name, member ID, and member type.
        Member: Member
        -- The membership role.
        Role: string
        -- The identifier of the user that invited the room member.
        InvitedBy: string
        -- The room membership update timestamp, in ISO 8601 format.
        UpdatedTimestamp: number
    end

    record CreateRoomMembershipOutput
        -- The room membership details.
        RoomMembership: RoomMembership
    end

    record CreateUserInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The user name.
        Username: string
        -- The user's email address.
        Email: string
        -- The user type.
        UserType: string
    end

    enum InviteStatus
        "Pending"
        "Accepted"
        "Failed"
    end

    enum RegistrationStatus
        "Unregistered"
        "Registered"
        "Suspended"
    end

    -- The user on the Amazon Chime account.
    record User
        -- The user ID.
        UserId: string
        -- The Amazon Chime account ID.
        AccountId: string
        -- The primary email address of the user.
        PrimaryEmail: string
        -- The primary phone number associated with the user.
        PrimaryProvisionedNumber: string
        -- The display name of the user.
        DisplayName: string
        -- The license type for the user.
        LicenseType: string
        -- The user type.
        UserType: string
        -- The user registration status.
        UserRegistrationStatus: string
        -- The user invite status.
        UserInvitationStatus: string
        -- Date and time when the user is registered, in ISO 8601 format.
        RegisteredOn: number
        -- Date and time when the user is invited to the Amazon Chime account, in ISO 8601 format.
        InvitedOn: number
        -- The Alexa for Business metadata.
        AlexaForBusinessMetadata: AlexaForBusinessMetadata
        -- The user's personal meeting PIN.
        PersonalPIN: string
    end

    record CreateUserOutput
        -- The user on the Amazon Chime account.
        User: User
    end

    record DeleteAccountInput
        -- The Amazon Chime account ID.
        AccountId: string
    end

    record DeleteAccountOutput
    end

    -- The request was well-formed but was unable to be followed due to semantic errors.
    record UnprocessableEntityException
        Code: string
        Message: string
    end

    record DeleteEventsConfigurationInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The bot ID.
        BotId: string
    end

    record DeleteEventsConfigurationOutput
    end

    record DeletePhoneNumberInput
        -- The phone number ID.
        PhoneNumberId: string
    end

    record DeletePhoneNumberOutput
    end

    record DeleteRoomInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The chat room ID.
        RoomId: string
    end

    record DeleteRoomOutput
    end

    record DeleteRoomMembershipInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The room ID.
        RoomId: string
        -- The member ID (user ID or bot ID).
        MemberId: string
    end

    record DeleteRoomMembershipOutput
    end

    record DisassociatePhoneNumberFromUserInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The user ID.
        UserId: string
    end

    record DisassociatePhoneNumberFromUserOutput
    end

    record DisassociateSigninDelegateGroupsFromAccountInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The sign-in delegate group names.
        GroupNames: {string}
    end

    record DisassociateSigninDelegateGroupsFromAccountOutput
    end

    enum EmailStatus
        "NotSent"
        "Sent"
        "Failed"
    end

    -- The configuration that allows a bot to receive outgoing events. Can be either an HTTPS endpoint or a Lambda function ARN.
    record EventsConfiguration
        -- The bot ID.
        BotId: string
        -- HTTPS endpoint that allows a bot to receive outgoing events.
        OutboundEventsHTTPSEndpoint: string
        -- Lambda function ARN that allows a bot to receive outgoing events.
        LambdaFunctionArn: string
    end

    record GetAccountInput
        -- The Amazon Chime account ID.
        AccountId: string
    end

    record GetAccountOutput
        -- The Amazon Chime account details.
        Account: Account
    end

    record GetAccountSettingsInput
        -- The Amazon Chime account ID.
        AccountId: string
    end

    record GetAccountSettingsOutput
        -- The Amazon Chime account settings.
        AccountSettings: AccountSettings
    end

    record GetBotInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The bot ID.
        BotId: string
    end

    record GetBotOutput
        -- The chat bot details.
        Bot: Bot
    end

    record GetEventsConfigurationInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The bot ID.
        BotId: string
    end

    record GetEventsConfigurationOutput
        -- The events configuration details.
        EventsConfiguration: EventsConfiguration
    end

    record GetGlobalSettingsInput
    end

    -- The Amazon Chime Voice Connector settings. Includes any Amazon S3 buckets designated for
    -- storing call detail records.
    record VoiceConnectorSettings
        -- The Amazon S3 bucket designated for call detail record storage.
        CdrBucket: string
    end

    record GetGlobalSettingsOutput
        -- The Amazon Chime Business Calling settings.
        BusinessCalling: BusinessCallingSettings
        -- The Amazon Chime Voice Connector settings.
        VoiceConnector: VoiceConnectorSettings
    end

    record GetPhoneNumberInput
        -- The phone number ID.
        PhoneNumberId: string
    end

    enum PhoneNumberAssociationName
        "AccountId"
        "UserId"
        "VoiceConnectorId"
        "VoiceConnectorGroupId"
        "SipRuleId"
    end

    -- The phone number associations, such as Amazon Chime account ID, Amazon Chime user ID, Amazon
    -- Chime Voice Connector ID, or Amazon Chime Voice Connector group ID.
    record PhoneNumberAssociation
        -- Contains the ID for the entity specified in Name.
        Value: string
        -- Defines the association with an Amazon Chime account ID, user ID, Amazon Chime Voice Connector
        -- ID, or Amazon Chime Voice Connector group ID.
        Name: string
        -- The timestamp of the phone number association, in ISO 8601 format.
        AssociatedTimestamp: number
    end

    -- The phone number capabilities for Amazon Chime Business Calling phone numbers, such as enabled
    -- inbound and outbound calling and text messaging.
    record PhoneNumberCapabilities
        -- Allows or denies inbound calling for the specified phone number.
        InboundCall: boolean
        -- Allows or denies outbound calling for the specified phone number.
        OutboundCall: boolean
        -- Allows or denies inbound SMS messaging for the specified phone number.
        InboundSMS: boolean
        -- Allows or denies outbound SMS messaging for the specified phone number.
        OutboundSMS: boolean
        -- Allows or denies inbound MMS messaging for the specified phone number.
        InboundMMS: boolean
        -- Allows or denies outbound MMS messaging for the specified phone number.
        OutboundMMS: boolean
    end

    enum PhoneNumberStatus
        "AcquireInProgress"
        "AcquireFailed"
        "Unassigned"
        "Assigned"
        "ReleaseInProgress"
        "DeleteInProgress"
        "ReleaseFailed"
        "DeleteFailed"
    end

    enum PhoneNumberType
        "Local"
        "TollFree"
    end

    -- A phone number used for Amazon Chime Business Calling or an Amazon Chime Voice
    -- Connector.
    record PhoneNumber
        -- The phone number ID.
        PhoneNumberId: string
        -- The phone number, in E.164 format.
        E164PhoneNumber: string
        -- The phone number country. Format: ISO 3166-1 alpha-2.
        Country: string
        -- The phone number type.
        Type: string
        -- The phone number product type.
        ProductType: string
        -- The phone number status.
        Status: string
        -- The phone number capabilities.
        Capabilities: PhoneNumberCapabilities
        -- The phone number associations.
        Associations: {PhoneNumberAssociation}
        -- The outbound calling name associated with the phone number.
        CallingName: string
        -- The outbound calling name status.
        CallingNameStatus: string
        -- The phone number creation timestamp, in ISO 8601 format.
        CreatedTimestamp: number
        -- The updated phone number timestamp, in ISO 8601 format.
        UpdatedTimestamp: number
        -- The deleted phone number timestamp, in ISO 8601 format.
        DeletionTimestamp: number
    end

    record GetPhoneNumberOutput
        -- The phone number details.
        PhoneNumber: PhoneNumber
    end

    record GetPhoneNumberOrderInput
        -- The ID for the phone number order.
        PhoneNumberOrderId: string
    end

    record GetPhoneNumberOrderOutput
        -- The phone number order details.
        PhoneNumberOrder: PhoneNumberOrder
    end

    record GetPhoneNumberSettingsInput
    end

    record GetPhoneNumberSettingsOutput
        -- The default outbound calling name for the account.
        CallingName: string
        -- The updated outbound calling name timestamp, in ISO 8601 format.
        CallingNameUpdatedTimestamp: number
    end

    record GetRetentionSettingsInput
        -- The Amazon Chime account ID.
        AccountId: string
    end

    -- The retention settings that determine how long to retain chat-room messages for an Amazon Chime Enterprise account.
    record RoomRetentionSettings
        -- The number of days for which to retain chat-room messages.
        RetentionDays: number
    end

    -- The retention settings for an Amazon Chime Enterprise account that determine how long to retain items such as chat-room messages and chat-conversation messages.
    record RetentionSettings
        -- The chat room retention settings.
        RoomRetentionSettings: RoomRetentionSettings
        -- The chat conversation retention settings.
        ConversationRetentionSettings: ConversationRetentionSettings
    end

    record GetRetentionSettingsOutput
        -- The retention settings.
        RetentionSettings: RetentionSettings
        -- The timestamp representing the time at which the specified items are permanently deleted, in ISO 8601 format.
        InitiateDeletionTimestamp: number
    end

    record GetRoomInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The room ID.
        RoomId: string
    end

    record GetRoomOutput
        -- The room details.
        Room: Room
    end

    record GetUserInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The user ID.
        UserId: string
    end

    record GetUserOutput
        -- The user details.
        User: User
    end

    record GetUserSettingsInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The user ID.
        UserId: string
    end

    -- Settings that allow management of telephony permissions for an Amazon Chime user, such as
    -- inbound and outbound calling and text messaging.
    record TelephonySettings
        -- Allows or denies inbound calling.
        InboundCalling: boolean
        -- Allows or denies outbound calling.
        OutboundCalling: boolean
        -- Allows or denies SMS messaging.
        SMS: boolean
    end

    -- Settings associated with an Amazon Chime user, including inbound and outbound calling and text
    -- messaging.
    record UserSettings
        -- The telephony settings associated with the user.
        Telephony: TelephonySettings
    end

    record GetUserSettingsOutput
        -- The user settings.
        UserSettings: UserSettings
    end

    -- Invitation object returned after emailing users to invite them to join the Amazon Chime
    -- Team account.
    record Invite
        -- The invite ID.
        InviteId: string
        -- The status of the invite.
        Status: string
        -- The email address to which the invite is sent.
        EmailAddress: string
        -- The status of the invite email.
        EmailStatus: string
    end

    record InviteUsersInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The user email addresses to which to send the email invitation.
        UserEmailList: {string}
        -- The user type.
        UserType: string
    end

    record InviteUsersOutput
        -- The email invitation details.
        Invites: {Invite}
    end

    record ListAccountsInput
        -- Amazon Chime account name prefix with which to filter results.
        Name: string
        -- User email address with which to filter results.
        UserEmail: string
        -- The token to use to retrieve the next page of results.
        NextToken: string
        -- The maximum number of results to return in a single call. Defaults to 100.
        MaxResults: number
    end

    record ListAccountsOutput
        -- List of Amazon Chime accounts and account details.
        Accounts: {Account}
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListBotsInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The maximum number of results to return in a single call. The default is 10.
        MaxResults: number
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListBotsOutput
        -- List of bots and bot details.
        Bots: {Bot}
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListPhoneNumberOrdersInput
        -- The token to use to retrieve the next page of results.
        NextToken: string
        -- The maximum number of results to return in a single call.
        MaxResults: number
    end

    record ListPhoneNumberOrdersOutput
        -- The phone number order details.
        PhoneNumberOrders: {PhoneNumberOrder}
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListPhoneNumbersInput
        -- The phone number status.
        Status: string
        -- The phone number product type.
        ProductType: string
        -- The filter to use to limit the number of results.
        FilterName: string
        -- The value to use for the filter.
        FilterValue: string
        -- The maximum number of results to return in a single call.
        MaxResults: number
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListPhoneNumbersOutput
        -- The phone number details.
        PhoneNumbers: {PhoneNumber}
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListRoomMembershipsInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The room ID.
        RoomId: string
        -- The maximum number of results to return in a single call.
        MaxResults: number
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListRoomMembershipsOutput
        -- The room membership details.
        RoomMemberships: {RoomMembership}
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListRoomsInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The member ID (user ID or bot ID).
        MemberId: string
        -- The maximum number of results to return in a single call.
        MaxResults: number
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListRoomsOutput
        -- The room details.
        Rooms: {Room}
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListSupportedPhoneNumberCountriesInput
        -- The phone number product type.
        ProductType: string
    end

    -- The phone number country.
    record PhoneNumberCountry
        -- The phone number country code. Format: ISO 3166-1 alpha-2.
        CountryCode: string
        -- The supported phone number types.
        SupportedPhoneNumberTypes: {string}
    end

    record ListSupportedPhoneNumberCountriesOutput
        -- The supported phone number countries.
        PhoneNumberCountries: {PhoneNumberCountry}
    end

    record ListUsersInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- Optional. The user email address used to filter results. Maximum 1.
        UserEmail: string
        -- The user type.
        UserType: string
        -- The maximum number of results to return in a single call. Defaults to 100.
        MaxResults: number
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record ListUsersOutput
        -- List of users and user details.
        Users: {User}
        -- The token to use to retrieve the next page of results.
        NextToken: string
    end

    record LogoutUserInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The user ID.
        UserId: string
    end

    record LogoutUserOutput
    end

    record PutEventsConfigurationInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The bot ID.
        BotId: string
        -- HTTPS endpoint that allows the bot to receive outgoing events.
        OutboundEventsHTTPSEndpoint: string
        -- Lambda function ARN that allows the bot to receive outgoing events.
        LambdaFunctionArn: string
    end

    record PutEventsConfigurationOutput
        -- The configuration that allows a bot to receive outgoing events. Can be an HTTPS endpoint or an
        -- AWS Lambda function ARN.
        EventsConfiguration: EventsConfiguration
    end

    record PutRetentionSettingsInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The retention settings.
        RetentionSettings: RetentionSettings
    end

    record PutRetentionSettingsOutput
        -- The retention settings.
        RetentionSettings: RetentionSettings
        -- The timestamp representing the time at which the specified items are permanently deleted, in ISO 8601 format.
        InitiateDeletionTimestamp: number
    end

    record RedactConversationMessageInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The conversation ID.
        ConversationId: string
        -- The message ID.
        MessageId: string
    end

    record RedactConversationMessageOutput
    end

    record RedactRoomMessageInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The room ID.
        RoomId: string
        -- The message ID.
        MessageId: string
    end

    record RedactRoomMessageOutput
    end

    record RegenerateSecurityTokenInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The bot ID.
        BotId: string
    end

    record RegenerateSecurityTokenOutput
        -- A resource that allows Enterprise account administrators to configure an interface that receives events from Amazon Chime.
        Bot: Bot
    end

    record ResetPersonalPINInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The user ID.
        UserId: string
    end

    record ResetPersonalPINOutput
        -- The user details and new personal meeting PIN.
        User: User
    end

    record RestorePhoneNumberInput
        -- The phone number.
        PhoneNumberId: string
    end

    record RestorePhoneNumberOutput
        -- The phone number details.
        PhoneNumber: PhoneNumber
    end

    record SearchAvailablePhoneNumbersInput
        -- The area code used to filter results. Only applies to the US.
        AreaCode: string
        -- The city used to filter results. Only applies to the US.
        City: string
        -- The country used to filter results. Defaults to the US Format: ISO 3166-1 alpha-2.
        Country: string
        -- The state used to filter results. Required only if you provide City. Only applies to the US.
        State: string
        -- The toll-free prefix that you use to filter results. Only applies to the US.
        TollFreePrefix: string
        -- The phone number type used to filter results. Required for non-US numbers.
        PhoneNumberType: string
        -- The maximum number of results to return in a single call.
        MaxResults: number
        -- The token used to retrieve the next page of results.
        NextToken: string
    end

    record SearchAvailablePhoneNumbersOutput
        -- List of phone numbers, in E.164 format.
        E164PhoneNumbers: {string}
        -- The token used to retrieve the next page of search results.
        NextToken: string
    end

    record UpdateAccountInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The new name for the specified Amazon Chime account.
        Name: string
        -- The default license applied when you add users to an Amazon Chime account.
        DefaultLicense: string
    end

    record UpdateAccountOutput
        -- The updated Amazon Chime account details.
        Account: Account
    end

    record UpdateAccountSettingsInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The Amazon Chime account settings to update.
        AccountSettings: AccountSettings
    end

    record UpdateAccountSettingsOutput
    end

    record UpdateBotInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The bot ID.
        BotId: string
        -- When true, stops the specified bot from running in your account.
        Disabled: boolean
    end

    record UpdateBotOutput
        -- The updated bot details.
        Bot: Bot
    end

    record UpdateGlobalSettingsInput
        -- The Amazon Chime Business Calling settings.
        BusinessCalling: BusinessCallingSettings
        -- The Amazon Chime Voice Connector settings.
        VoiceConnector: VoiceConnectorSettings
    end

    record UpdateGlobalSettingsOutput
    end

    record UpdatePhoneNumberInput
        -- The phone number ID.
        PhoneNumberId: string
        -- The product type.
        ProductType: string
        -- The outbound calling name associated with the phone number.
        CallingName: string
    end

    record UpdatePhoneNumberOutput
        -- The updated phone number details.
        PhoneNumber: PhoneNumber
    end

    record UpdatePhoneNumberSettingsInput
        -- The default outbound calling name for the account.
        CallingName: string
    end

    record UpdatePhoneNumberSettingsOutput
    end

    record UpdateRoomInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The room ID.
        RoomId: string
        -- The room name.
        Name: string
    end

    record UpdateRoomOutput
        -- The room details.
        Room: Room
    end

    record UpdateRoomMembershipInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The room ID.
        RoomId: string
        -- The member ID.
        MemberId: string
        -- The role of the member.
        Role: string
    end

    record UpdateRoomMembershipOutput
        -- The room membership details.
        RoomMembership: RoomMembership
    end

    record UpdateUserInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The user ID.
        UserId: string
        -- The user license type to update. This must be a supported license type for the Amazon Chime
        -- account that the user belongs to.
        LicenseType: string
        -- The user type.
        UserType: string
        -- The Alexa for Business metadata.
        AlexaForBusinessMetadata: AlexaForBusinessMetadata
    end

    record UpdateUserOutput
        -- The updated user details.
        User: User
    end

    record UpdateUserSettingsInput
        -- The Amazon Chime account ID.
        AccountId: string
        -- The user ID.
        UserId: string
        -- The user settings to update.
        UserSettings: UserSettings
    end

    record UpdateUserSettingsOutput
    end

end

return M
