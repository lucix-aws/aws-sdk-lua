-- Code generated by lua-client-codegen. DO NOT EDIT.

local record BatchGetTokenBalanceInput
    -- An array of BatchGetTokenBalanceInputItem objects whose balance is being requested.
    getTokenBalanceInputs: {BatchGetTokenBalanceInputItem}
end

local record BatchGetTokenBalanceOutput
    -- An array of BatchGetTokenBalanceOutputItem objects returned by the response.
    tokenBalances: {BatchGetTokenBalanceOutputItem}
    -- An array of BatchGetTokenBalanceErrorItem objects returned from the request.
    errors: {BatchGetTokenBalanceErrorItem}
end

local record GetAssetContractInput
    -- Contains the blockchain address and network information about the contract.
    contractIdentifier: ContractIdentifier
end

local record GetAssetContractOutput
    -- Contains the blockchain address and network information about the contract.
    contractIdentifier: ContractIdentifier
    -- The token standard of the contract requested.
    tokenStandard: string
    -- The address of the deployer of contract.
    deployerAddress: string
    metadata: ContractMetadata
end

local record GetTokenBalanceInput
    -- The container for the identifier for the token, including the unique
    -- token ID and its blockchain network.
    tokenIdentifier: TokenIdentifier
    -- The container for the identifier for the owner.
    ownerIdentifier: OwnerIdentifier
    -- The time for when the TokenBalance is requested or
    -- the current time if a time is not provided in the request.
    -- This time will only be recorded up to the second.
    atBlockchainInstant: BlockchainInstant
end

local record GetTokenBalanceOutput
    ownerIdentifier: OwnerIdentifier
    tokenIdentifier: TokenIdentifier
    -- The container for the token balance.
    balance: string
    atBlockchainInstant: BlockchainInstant
    lastUpdatedTime: BlockchainInstant
end

local record GetTransactionInput
    -- The hash of a transaction. It is generated when a transaction is created.
    transactionHash: string
    -- The identifier of a Bitcoin transaction. It is generated when a transaction is created.
    -- transactionId is only supported on the Bitcoin networks.
    transactionId: string
    -- The blockchain network where the transaction occurred.
    network: string
end

local record GetTransactionOutput
    -- Contains the details of the transaction.
    transaction: Transaction
end

local record ListAssetContractsInput
    -- Contains the filter parameter for the request.
    contractFilter: ContractFilter
    -- The pagination token that indicates the next set of results to retrieve.
    nextToken: string
    -- The maximum number of contracts to list.
    -- Default: 100
    -- Even if additional results can be retrieved, the request can return less
    -- results than maxResults or an empty array of results.
    -- To retrieve the next set of results, make another request with the
    -- returned nextToken value. The value of nextToken is
    -- null when there are no more results to return
    maxResults: number
end

local record ListAssetContractsOutput
    -- An array of contract objects that contain the properties for each contract.
    contracts: {AssetContract}
    -- The pagination token that indicates the next set of results to retrieve.
    nextToken: string
end

local record ListFilteredTransactionEventsInput
    -- The blockchain network where the transaction occurred.
    -- Valid Values: BITCOIN_MAINNET | BITCOIN_TESTNET
    network: string
    -- This is the unique public address on the blockchain for which the transaction events are being requested.
    addressIdentifierFilter: AddressIdentifierFilter
    -- This container specifies the time frame for the transaction events returned in the response.
    timeFilter: TimeFilter
    -- This container specifies filtering attributes related to BITCOIN_VOUT event types
    voutFilter: VoutFilter
    confirmationStatusFilter: ConfirmationStatusFilter
    -- The order by which the results will be sorted.
    sort: ListFilteredTransactionEventsSort
    -- The pagination token that indicates the next set of results to retrieve.
    nextToken: string
    -- The maximum number of transaction events to list.
    -- Default: 100
    -- Even if additional results can be retrieved, the request can return less
    -- results than maxResults or an empty array of results.
    -- To retrieve the next set of results, make another request with the
    -- returned nextToken value. The value of nextToken is
    -- null when there are no more results to return
    maxResults: number
end

local record ListFilteredTransactionEventsOutput
    -- The transaction events returned by the request.
    events: {TransactionEvent}
    -- The pagination token that indicates the next set of results to retrieve.
    nextToken: string
end

local record ListTokenBalancesInput
    -- The contract or wallet address on the blockchain network by which to filter the
    -- request. You must specify the address property of the ownerFilter
    -- when listing balances of tokens owned by the address.
    ownerFilter: OwnerFilter
    -- The contract address or a token identifier on the
    -- blockchain network by which to filter the request. You must specify the contractAddress
    -- property of this container when listing tokens minted by a contract.
    -- You must always specify the network property of this
    -- container when using this operation.
    tokenFilter: TokenFilter
    -- The pagination token that indicates the next set of results to retrieve.
    nextToken: string
    -- The maximum number of token balances to return.
    -- Default: 100
    -- Even if additional results can be retrieved, the request can return less
    -- results than maxResults or an empty array of results.
    -- To retrieve the next set of results, make another request with the
    -- returned nextToken value. The value of nextToken is
    -- null when there are no more results to return
    maxResults: number
end

local record ListTokenBalancesOutput
    -- An array of TokenBalance objects. Each object contains details about
    -- the token balance.
    tokenBalances: {TokenBalance}
    -- The pagination token that indicates the next set of results to retrieve.
    nextToken: string
end

local record ListTransactionEventsInput
    -- The hash of a transaction. It is generated when a transaction is created.
    transactionHash: string
    -- The identifier of a Bitcoin transaction. It is generated when a transaction is created.
    -- transactionId is only supported on the Bitcoin networks.
    transactionId: string
    -- The blockchain network where the transaction events occurred.
    network: string
    -- The pagination token that indicates the next set of results to retrieve.
    nextToken: string
    -- The maximum number of transaction events to list.
    -- Default: 100
    -- Even if additional results can be retrieved, the request can return less
    -- results than maxResults or an empty array of results.
    -- To retrieve the next set of results, make another request with the
    -- returned nextToken value. The value of nextToken is
    -- null when there are no more results to return
    maxResults: number
end

local record ListTransactionEventsOutput
    -- An array of TransactionEvent objects. Each object contains details
    -- about the transaction events.
    events: {TransactionEvent}
    -- The pagination token that indicates the next set of results to retrieve.
    nextToken: string
end

local record ListTransactionsInput
    -- The address (either a contract or wallet), whose transactions are being requested.
    address: string
    -- The blockchain network where the transactions occurred.
    network: string
    fromBlockchainInstant: BlockchainInstant
    toBlockchainInstant: BlockchainInstant
    -- The order by which the results will be sorted.
    sort: ListTransactionsSort
    -- The pagination token that indicates the next set of results to retrieve.
    nextToken: string
    -- The maximum number of transactions to list.
    -- Default: 100
    -- Even if additional results can be retrieved, the request can return less
    -- results than maxResults or an empty array of results.
    -- To retrieve the next set of results, make another request with the
    -- returned nextToken value. The value of nextToken is
    -- null when there are no more results to return
    maxResults: number
    -- This filter is used to include transactions in the response that haven't reached
    -- finality
    -- . Transactions that have reached finality are always
    -- part of the response.
    confirmationStatusFilter: ConfirmationStatusFilter
end

local record ListTransactionsOutput
    -- The array of transactions returned by the request.
    transactions: {TransactionOutputItem}
    -- The pagination token that indicates the next set of results to retrieve.
    nextToken: string
end

-- The Amazon Web Services account doesn’t have access to this resource.
local record AccessDeniedException
    -- The container for the exception message.
    message: string
end

-- The request processing has failed because of an internal error in the service.
local record InternalServerException
    -- The container for the exception message.
    message: string
    -- Specifies the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource was not found.
local record ResourceNotFoundException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
end

-- The service quota has been exceeded for this resource.
local record ServiceQuotaExceededException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
end

-- The request or operation couldn't be performed because a service is throttling
-- requests. The most common source of throttling errors is when you create resources
-- that exceed your service limit for this resource type. Request a limit increase or
-- delete unused resources, if possible.
local record ThrottlingException
    -- The container for the exception message.
    message: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
    -- The container of the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource passed is invalid.
local record ValidationException
    -- The container for the exception message.
    message: string
    -- The container for the reason for the exception
    reason: string
    -- The container for the fieldList of the exception.
    fieldList: {ValidationExceptionField}
end

-- The Amazon Web Services account doesn’t have access to this resource.
local record AccessDeniedException
    -- The container for the exception message.
    message: string
end

-- The request processing has failed because of an internal error in the service.
local record InternalServerException
    -- The container for the exception message.
    message: string
    -- Specifies the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource was not found.
local record ResourceNotFoundException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
end

-- The service quota has been exceeded for this resource.
local record ServiceQuotaExceededException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
end

-- The request or operation couldn't be performed because a service is throttling
-- requests. The most common source of throttling errors is when you create resources
-- that exceed your service limit for this resource type. Request a limit increase or
-- delete unused resources, if possible.
local record ThrottlingException
    -- The container for the exception message.
    message: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
    -- The container of the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource passed is invalid.
local record ValidationException
    -- The container for the exception message.
    message: string
    -- The container for the reason for the exception
    reason: string
    -- The container for the fieldList of the exception.
    fieldList: {ValidationExceptionField}
end

-- The Amazon Web Services account doesn’t have access to this resource.
local record AccessDeniedException
    -- The container for the exception message.
    message: string
end

-- The request processing has failed because of an internal error in the service.
local record InternalServerException
    -- The container for the exception message.
    message: string
    -- Specifies the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource was not found.
local record ResourceNotFoundException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
end

-- The service quota has been exceeded for this resource.
local record ServiceQuotaExceededException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
end

-- The request or operation couldn't be performed because a service is throttling
-- requests. The most common source of throttling errors is when you create resources
-- that exceed your service limit for this resource type. Request a limit increase or
-- delete unused resources, if possible.
local record ThrottlingException
    -- The container for the exception message.
    message: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
    -- The container of the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource passed is invalid.
local record ValidationException
    -- The container for the exception message.
    message: string
    -- The container for the reason for the exception
    reason: string
    -- The container for the fieldList of the exception.
    fieldList: {ValidationExceptionField}
end

-- The Amazon Web Services account doesn’t have access to this resource.
local record AccessDeniedException
    -- The container for the exception message.
    message: string
end

-- The request processing has failed because of an internal error in the service.
local record InternalServerException
    -- The container for the exception message.
    message: string
    -- Specifies the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource was not found.
local record ResourceNotFoundException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
end

-- The service quota has been exceeded for this resource.
local record ServiceQuotaExceededException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
end

-- The request or operation couldn't be performed because a service is throttling
-- requests. The most common source of throttling errors is when you create resources
-- that exceed your service limit for this resource type. Request a limit increase or
-- delete unused resources, if possible.
local record ThrottlingException
    -- The container for the exception message.
    message: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
    -- The container of the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource passed is invalid.
local record ValidationException
    -- The container for the exception message.
    message: string
    -- The container for the reason for the exception
    reason: string
    -- The container for the fieldList of the exception.
    fieldList: {ValidationExceptionField}
end

-- The Amazon Web Services account doesn’t have access to this resource.
local record AccessDeniedException
    -- The container for the exception message.
    message: string
end

-- The request processing has failed because of an internal error in the service.
local record InternalServerException
    -- The container for the exception message.
    message: string
    -- Specifies the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The service quota has been exceeded for this resource.
local record ServiceQuotaExceededException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
end

-- The request or operation couldn't be performed because a service is throttling
-- requests. The most common source of throttling errors is when you create resources
-- that exceed your service limit for this resource type. Request a limit increase or
-- delete unused resources, if possible.
local record ThrottlingException
    -- The container for the exception message.
    message: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
    -- The container of the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource passed is invalid.
local record ValidationException
    -- The container for the exception message.
    message: string
    -- The container for the reason for the exception
    reason: string
    -- The container for the fieldList of the exception.
    fieldList: {ValidationExceptionField}
end

-- The Amazon Web Services account doesn’t have access to this resource.
local record AccessDeniedException
    -- The container for the exception message.
    message: string
end

-- The request processing has failed because of an internal error in the service.
local record InternalServerException
    -- The container for the exception message.
    message: string
    -- Specifies the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The service quota has been exceeded for this resource.
local record ServiceQuotaExceededException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
end

-- The request or operation couldn't be performed because a service is throttling
-- requests. The most common source of throttling errors is when you create resources
-- that exceed your service limit for this resource type. Request a limit increase or
-- delete unused resources, if possible.
local record ThrottlingException
    -- The container for the exception message.
    message: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
    -- The container of the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource passed is invalid.
local record ValidationException
    -- The container for the exception message.
    message: string
    -- The container for the reason for the exception
    reason: string
    -- The container for the fieldList of the exception.
    fieldList: {ValidationExceptionField}
end

-- The Amazon Web Services account doesn’t have access to this resource.
local record AccessDeniedException
    -- The container for the exception message.
    message: string
end

-- The request processing has failed because of an internal error in the service.
local record InternalServerException
    -- The container for the exception message.
    message: string
    -- Specifies the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The service quota has been exceeded for this resource.
local record ServiceQuotaExceededException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
end

-- The request or operation couldn't be performed because a service is throttling
-- requests. The most common source of throttling errors is when you create resources
-- that exceed your service limit for this resource type. Request a limit increase or
-- delete unused resources, if possible.
local record ThrottlingException
    -- The container for the exception message.
    message: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
    -- The container of the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource passed is invalid.
local record ValidationException
    -- The container for the exception message.
    message: string
    -- The container for the reason for the exception
    reason: string
    -- The container for the fieldList of the exception.
    fieldList: {ValidationExceptionField}
end

-- The Amazon Web Services account doesn’t have access to this resource.
local record AccessDeniedException
    -- The container for the exception message.
    message: string
end

-- The request processing has failed because of an internal error in the service.
local record InternalServerException
    -- The container for the exception message.
    message: string
    -- Specifies the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The service quota has been exceeded for this resource.
local record ServiceQuotaExceededException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
end

-- The request or operation couldn't be performed because a service is throttling
-- requests. The most common source of throttling errors is when you create resources
-- that exceed your service limit for this resource type. Request a limit increase or
-- delete unused resources, if possible.
local record ThrottlingException
    -- The container for the exception message.
    message: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
    -- The container of the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource passed is invalid.
local record ValidationException
    -- The container for the exception message.
    message: string
    -- The container for the reason for the exception
    reason: string
    -- The container for the fieldList of the exception.
    fieldList: {ValidationExceptionField}
end

-- The Amazon Web Services account doesn’t have access to this resource.
local record AccessDeniedException
    -- The container for the exception message.
    message: string
end

-- The request processing has failed because of an internal error in the service.
local record InternalServerException
    -- The container for the exception message.
    message: string
    -- Specifies the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The service quota has been exceeded for this resource.
local record ServiceQuotaExceededException
    -- The container for the exception message.
    message: string
    -- The resourceId of the resource that caused the exception.
    resourceId: string
    -- The resourceType of the resource that caused the exception.
    resourceType: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
end

-- The request or operation couldn't be performed because a service is throttling
-- requests. The most common source of throttling errors is when you create resources
-- that exceed your service limit for this resource type. Request a limit increase or
-- delete unused resources, if possible.
local record ThrottlingException
    -- The container for the exception message.
    message: string
    -- The container for the serviceCode.
    serviceCode: string
    -- The container for the quotaCode.
    quotaCode: string
    -- The container of the retryAfterSeconds value.
    retryAfterSeconds: number
end

-- The resource passed is invalid.
local record ValidationException
    -- The container for the exception message.
    message: string
    -- The container for the reason for the exception
    reason: string
    -- The container for the fieldList of the exception.
    fieldList: {ValidationExceptionField}
end

local record M
    BatchGetTokenBalanceInput: BatchGetTokenBalanceInput
    BatchGetTokenBalanceOutput: BatchGetTokenBalanceOutput
    GetAssetContractInput: GetAssetContractInput
    GetAssetContractOutput: GetAssetContractOutput
    GetTokenBalanceInput: GetTokenBalanceInput
    GetTokenBalanceOutput: GetTokenBalanceOutput
    GetTransactionInput: GetTransactionInput
    GetTransactionOutput: GetTransactionOutput
    ListAssetContractsInput: ListAssetContractsInput
    ListAssetContractsOutput: ListAssetContractsOutput
    ListFilteredTransactionEventsInput: ListFilteredTransactionEventsInput
    ListFilteredTransactionEventsOutput: ListFilteredTransactionEventsOutput
    ListTokenBalancesInput: ListTokenBalancesInput
    ListTokenBalancesOutput: ListTokenBalancesOutput
    ListTransactionEventsInput: ListTransactionEventsInput
    ListTransactionEventsOutput: ListTransactionEventsOutput
    ListTransactionsInput: ListTransactionsInput
    ListTransactionsOutput: ListTransactionsOutput
end

return M
