-- Code generated by lua-client-codegen. DO NOT EDIT.

-- The request structure used to create apps in Amplify.
local record CreateAppInput
    -- The name of the Amplify app.
    name: string
    -- The description of the Amplify app.
    description: string
    -- The Git repository for the Amplify app.
    repository: string
    -- The platform for the Amplify app. For a static app, set the platform type to
    -- WEB. For a dynamic server-side rendered (SSR) app, set the platform
    -- type to WEB_COMPUTE. For an app requiring Amplify Hosting's original SSR
    -- support only, set the platform type to WEB_DYNAMIC.
    -- If you are deploying an SSG only app with Next.js version 14 or later, you must set
    -- the platform type to WEB_COMPUTE and set the artifacts
    -- baseDirectory to .next in the application's build
    -- settings. For an example of the build specification settings, see Amplify build settings for a Next.js 14 SSG application in the
    -- Amplify Hosting User Guide.
    platform: string
    -- The Amazon Resource Name (ARN) of the IAM role to assign to an SSR app.
    -- The SSR Compute role allows the Amplify Hosting compute service to
    -- securely access specific Amazon Web Services resources based on the role's permissions.
    -- For more information about the SSR Compute role, see Adding an SSR Compute
    -- role in the Amplify User Guide.
    computeRoleArn: string
    -- The Amazon Resource Name (ARN) of the IAM service role for the Amplify app.
    iamServiceRoleArn: string
    -- The OAuth token for a third-party source control system for an Amplify app. The OAuth
    -- token is used to create a webhook and a read-only deploy key using SSH cloning. The
    -- OAuth token is not stored.
    -- Use oauthToken for repository providers other than GitHub, such as
    -- Bitbucket or CodeCommit. To authorize access to GitHub as your repository provider, use
    -- accessToken.
    -- You must specify either oauthToken or accessToken when you
    -- create a new app.
    -- Existing Amplify apps deployed from a GitHub repository using OAuth continue to work
    -- with CI/CD. However, we strongly recommend that you migrate these apps to use the GitHub
    -- App. For more information, see Migrating an existing OAuth app to the Amplify GitHub App in the
    -- Amplify User Guide .
    oauthToken: string
    -- The personal access token for a GitHub repository for an Amplify app. The personal
    -- access token is used to authorize access to a GitHub repository using the Amplify GitHub
    -- App. The token is not stored.
    -- Use accessToken for GitHub repositories only. To authorize access to a
    -- repository provider such as Bitbucket or CodeCommit, use oauthToken.
    -- You must specify either accessToken or oauthToken when you
    -- create a new app.
    -- Existing Amplify apps deployed from a GitHub repository using OAuth continue to work
    -- with CI/CD. However, we strongly recommend that you migrate these apps to use the GitHub
    -- App. For more information, see Migrating an existing OAuth app to the Amplify GitHub App in the
    -- Amplify User Guide .
    accessToken: string
    -- The environment variables map for an Amplify app.
    -- For a list of the environment variables that are accessible to Amplify by default, see
    -- Amplify
    -- Environment variables in the Amplify Hosting User
    -- Guide.
    environmentVariables: {string : string}
    -- Enables the auto building of branches for an Amplify app.
    enableBranchAutoBuild: boolean
    -- Automatically disconnects a branch in the Amplify console when you delete a branch
    -- from your Git repository.
    enableBranchAutoDeletion: boolean
    -- Enables basic authorization for an Amplify app. This will apply to all branches that
    -- are part of this app.
    enableBasicAuth: boolean
    -- The credentials for basic authorization for an Amplify app. You must base64-encode the
    -- authorization credentials and provide them in the format
    -- user:password.
    basicAuthCredentials: string
    -- The custom rewrite and redirect rules for an Amplify app.
    customRules: {CustomRule}
    -- The tag for an Amplify app.
    tags: {string : string}
    -- The build specification (build spec) for an Amplify app.
    buildSpec: string
    -- The custom HTTP headers for an Amplify app.
    customHeaders: string
    -- Enables automated branch creation for an Amplify app.
    enableAutoBranchCreation: boolean
    -- The automated branch creation glob patterns for an Amplify app.
    autoBranchCreationPatterns: {string}
    -- The automated branch creation configuration for an Amplify app.
    autoBranchCreationConfig: AutoBranchCreationConfig
    -- Describes the configuration details that apply to the jobs for an Amplify app.
    jobConfig: JobConfig
    -- The cache configuration for the Amplify app.
    cacheConfig: CacheConfig
end

local record CreateAppOutput
    app: App
end

-- The request structure for the backend environment create request.
local record CreateBackendEnvironmentInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name for the backend environment.
    environmentName: string
    -- The AWS CloudFormation stack name of a backend environment.
    stackName: string
    -- The name of deployment artifacts.
    deploymentArtifacts: string
end

-- The result structure for the create backend environment request.
local record CreateBackendEnvironmentOutput
    -- Describes the backend environment for an Amplify app.
    backendEnvironment: BackendEnvironment
end

-- The request structure for the create branch request.
local record CreateBranchInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name for the branch.
    branchName: string
    -- The description for the branch.
    description: string
    -- Describes the current stage for the branch.
    stage: string
    -- The framework for the branch.
    framework: string
    -- Enables notifications for the branch.
    enableNotification: boolean
    -- Enables auto building for the branch.
    enableAutoBuild: boolean
    -- Specifies whether the skew protection feature is enabled for the branch.
    -- Deployment skew protection is available to Amplify applications to
    -- eliminate version skew issues between client and servers in web applications. When you
    -- apply skew protection to a branch, you can ensure that your clients always interact with
    -- the correct version of server-side assets, regardless of when a deployment occurs. For
    -- more information about skew protection, see Skew protection for Amplify deployments in the Amplify User
    -- Guide.
    enableSkewProtection: boolean
    -- The environment variables for the branch.
    environmentVariables: {string : string}
    -- The basic authorization credentials for the branch. You must base64-encode the
    -- authorization credentials and provide them in the format
    -- user:password.
    basicAuthCredentials: string
    -- Enables basic authorization for the branch.
    enableBasicAuth: boolean
    -- Enables performance mode for the branch.
    -- Performance mode optimizes for faster hosting performance by keeping content cached at
    -- the edge for a longer interval. When performance mode is enabled, hosting configuration
    -- or code changes can take up to 10 minutes to roll out.
    enablePerformanceMode: boolean
    -- The tag for the branch.
    tags: {string : string}
    -- The build specification (build spec) for the branch.
    buildSpec: string
    -- The content Time To Live (TTL) for the website in seconds.
    ttl: string
    -- The display name for a branch. This is used as the default domain prefix.
    displayName: string
    -- Enables pull request previews for this branch.
    enablePullRequestPreview: boolean
    -- The Amplify environment name for the pull request.
    pullRequestEnvironmentName: string
    -- The Amazon Resource Name (ARN) for a backend environment that is part of a Gen 1
    -- Amplify app.
    -- This field is available to Amplify Gen 1 apps only where the backend is
    -- created using Amplify Studio or the Amplify command line
    -- interface (CLI).
    backendEnvironmentArn: string
    -- The backend for a Branch of an Amplify app. Use for a
    -- backend created from an CloudFormation stack.
    -- This field is available to Amplify Gen 2 apps only. When you deploy an
    -- application with Amplify Gen 2, you provision the app's backend infrastructure using
    -- Typescript code.
    backend: Backend
    -- The Amazon Resource Name (ARN) of the IAM role to assign to a branch of
    -- an SSR app. The SSR Compute role allows the Amplify Hosting compute
    -- service to securely access specific Amazon Web Services resources based on the role's
    -- permissions. For more information about the SSR Compute role, see Adding an SSR Compute role in the Amplify User
    -- Guide.
    computeRoleArn: string
end

-- The result structure for create branch request.
local record CreateBranchOutput
    -- Describes the branch for an Amplify app, which maps to a third-party repository
    -- branch.
    branch: Branch
end

-- The request structure for the create a new deployment request.
local record CreateDeploymentInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the branch to use for the job.
    branchName: string
    -- An optional file map that contains the file name as the key and the file content md5
    -- hash as the value. If this argument is provided, the service will generate a unique
    -- upload URL per file. Otherwise, the service will only generate a single upload URL for
    -- the zipped files.
    fileMap: {string : string}
end

-- The result structure for the create a new deployment request.
local record CreateDeploymentOutput
    -- The job ID for this deployment. will supply to start deployment api.
    jobId: string
    -- When the fileMap argument is provided in the request,
    -- fileUploadUrls will contain a map of file names to upload URLs.
    fileUploadUrls: {string : string}
    -- When the fileMap argument is not provided in the request, this
    -- zipUploadUrl is returned.
    zipUploadUrl: string
end

-- The request structure for the create domain association request.
local record CreateDomainAssociationInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The domain name for the domain association.
    domainName: string
    -- Enables the automated creation of subdomains for branches.
    enableAutoSubDomain: boolean
    -- The setting for the subdomain.
    subDomainSettings: {SubDomainSetting}
    -- Sets the branch patterns for automatic subdomain creation.
    autoSubDomainCreationPatterns: {string}
    -- The required AWS Identity and Access Management (IAM) service role for the Amazon
    -- Resource Name (ARN) for automatically creating subdomains.
    autoSubDomainIAMRole: string
    -- The type of SSL/TLS certificate to use for your custom domain. If you don't specify a
    -- certificate type, Amplify uses the default certificate that it provisions and manages
    -- for you.
    certificateSettings: CertificateSettings
end

-- The result structure for the create domain association request.
local record CreateDomainAssociationOutput
    -- Describes the structure of a domain association, which associates a custom domain
    -- with an Amplify app.
    domainAssociation: DomainAssociation
end

-- The request structure for the create webhook request.
local record CreateWebhookInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name for a branch that is part of an Amplify app.
    branchName: string
    -- The description for a webhook.
    description: string
end

-- The result structure for the create webhook request.
local record CreateWebhookOutput
    -- Describes a webhook that connects repository events to an Amplify app.
    webhook: Webhook
end

-- Describes the request structure for the delete app request.
local record DeleteAppInput
    -- The unique ID for an Amplify app.
    appId: string
end

-- The result structure for the delete app request.
local record DeleteAppOutput
    app: App
end

-- The request structure for the delete backend environment request.
local record DeleteBackendEnvironmentInput
    -- The unique ID of an Amplify app.
    appId: string
    -- The name of a backend environment of an Amplify app.
    environmentName: string
end

-- The result structure of the delete backend environment result.
local record DeleteBackendEnvironmentOutput
    -- Describes the backend environment for an Amplify app.
    backendEnvironment: BackendEnvironment
end

-- The request structure for the delete branch request.
local record DeleteBranchInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the branch.
    branchName: string
end

-- The result structure for the delete branch request.
local record DeleteBranchOutput
    -- The branch for an Amplify app, which maps to a third-party repository branch.
    branch: Branch
end

-- The request structure for the delete domain association request.
local record DeleteDomainAssociationInput
    -- The unique id for an Amplify app.
    appId: string
    -- The name of the domain.
    domainName: string
end

local record DeleteDomainAssociationOutput
    domainAssociation: DomainAssociation
end

-- The request structure for the delete job request.
local record DeleteJobInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the branch to use for the job.
    branchName: string
    -- The unique ID for the job.
    jobId: string
end

-- The result structure for the delete job request.
local record DeleteJobOutput
    jobSummary: JobSummary
end

-- The request structure for the delete webhook request.
local record DeleteWebhookInput
    -- The unique ID for a webhook.
    webhookId: string
end

-- The result structure for the delete webhook request.
local record DeleteWebhookOutput
    -- Describes a webhook that connects repository events to an Amplify app.
    webhook: Webhook
end

-- The request structure for the generate access logs request.
local record GenerateAccessLogsInput
    -- The time at which the logs should start. The time range specified is inclusive of the
    -- start time.
    startTime: number
    -- The time at which the logs should end. The time range specified is inclusive of the
    -- end time.
    endTime: number
    -- The name of the domain.
    domainName: string
    -- The unique ID for an Amplify app.
    appId: string
end

-- The result structure for the generate access logs request.
local record GenerateAccessLogsOutput
    -- The pre-signed URL for the requested access logs.
    logUrl: string
end

-- The request structure for the get app request.
local record GetAppInput
    -- The unique ID for an Amplify app.
    appId: string
end

local record GetAppOutput
    app: App
end

-- Returns the request structure for the get artifact request.
local record GetArtifactUrlInput
    -- The unique ID for an artifact.
    artifactId: string
end

-- Returns the result structure for the get artifact request.
local record GetArtifactUrlOutput
    -- The unique ID for an artifact.
    artifactId: string
    -- The presigned URL for the artifact.
    artifactUrl: string
end

-- The request structure for the get backend environment request.
local record GetBackendEnvironmentInput
    -- The unique id for an Amplify app.
    appId: string
    -- The name for the backend environment.
    environmentName: string
end

-- The result structure for the get backend environment result.
local record GetBackendEnvironmentOutput
    -- Describes the backend environment for an Amplify app.
    backendEnvironment: BackendEnvironment
end

-- The request structure for the get branch request.
local record GetBranchInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the branch.
    branchName: string
end

local record GetBranchOutput
    branch: Branch
end

-- The request structure for the get domain association request.
local record GetDomainAssociationInput
    -- The unique id for an Amplify app.
    appId: string
    -- The name of the domain.
    domainName: string
end

-- The result structure for the get domain association request.
local record GetDomainAssociationOutput
    -- Describes the structure of a domain association, which associates a custom domain
    -- with an Amplify app.
    domainAssociation: DomainAssociation
end

-- The request structure for the get job request.
local record GetJobInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the branch to use for the job.
    branchName: string
    -- The unique ID for the job.
    jobId: string
end

local record GetJobOutput
    job: Job
end

-- The request structure for the get webhook request.
local record GetWebhookInput
    -- The unique ID for a webhook.
    webhookId: string
end

-- The result structure for the get webhook request.
local record GetWebhookOutput
    -- Describes the structure of a webhook.
    webhook: Webhook
end

-- The request structure for the list apps request.
local record ListAppsInput
    -- A pagination token. If non-null, the pagination token is returned in a result. Pass
    -- its value in another request to retrieve more entries.
    nextToken: string
    -- The maximum number of records to list in a single response.
    maxResults: number
end

-- The result structure for an Amplify app list request.
local record ListAppsOutput
    -- A list of Amplify apps.
    apps: {App}
    -- A pagination token. Set to null to start listing apps from start. If non-null, the
    -- pagination token is returned in a result. Pass its value in here to list more projects.
    nextToken: string
end

-- Describes the request structure for the list artifacts request.
local record ListArtifactsInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of a branch that is part of an Amplify app.
    branchName: string
    -- The unique ID for a job.
    jobId: string
    -- A pagination token. Set to null to start listing artifacts from start. If a non-null
    -- pagination token is returned in a result, pass its value in here to list more artifacts.
    nextToken: string
    -- The maximum number of records to list in a single response.
    maxResults: number
end

-- The result structure for the list artifacts request.
local record ListArtifactsOutput
    -- A list of artifacts.
    artifacts: {Artifact}
    -- A pagination token. If a non-null pagination token is returned in a result, pass its
    -- value in another request to retrieve more entries.
    nextToken: string
end

-- The request structure for the list backend environments request.
local record ListBackendEnvironmentsInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the backend environment
    environmentName: string
    -- A pagination token. Set to null to start listing backend environments from the start.
    -- If a non-null pagination token is returned in a result, pass its value in here to list
    -- more backend environments.
    nextToken: string
    -- The maximum number of records to list in a single response.
    maxResults: number
end

-- The result structure for the list backend environments result.
local record ListBackendEnvironmentsOutput
    -- The list of backend environments for an Amplify app.
    backendEnvironments: {BackendEnvironment}
    -- A pagination token. If a non-null pagination token is returned in a result, pass its
    -- value in another request to retrieve more entries.
    nextToken: string
end

-- The request structure for the list branches request.
local record ListBranchesInput
    -- The unique ID for an Amplify app.
    appId: string
    -- A pagination token. Set to null to start listing branches from the start. If a
    -- non-null pagination token is returned in a result, pass its value in here to list more
    -- branches.
    nextToken: string
    -- The maximum number of records to list in a single response.
    maxResults: number
end

-- The result structure for the list branches request.
local record ListBranchesOutput
    -- A list of branches for an Amplify app.
    branches: {Branch}
    -- A pagination token. If a non-null pagination token is returned in a result, pass its
    -- value in another request to retrieve more entries.
    nextToken: string
end

-- The request structure for the list domain associations request.
local record ListDomainAssociationsInput
    -- The unique ID for an Amplify app.
    appId: string
    -- A pagination token. Set to null to start listing apps from the start. If non-null, a
    -- pagination token is returned in a result. Pass its value in here to list more projects.
    nextToken: string
    -- The maximum number of records to list in a single response.
    maxResults: number
end

-- The result structure for the list domain association request.
local record ListDomainAssociationsOutput
    -- A list of domain associations.
    domainAssociations: {DomainAssociation}
    -- A pagination token. If non-null, a pagination token is returned in a result. Pass its
    -- value in another request to retrieve more entries.
    nextToken: string
end

-- The request structure for the list jobs request.
local record ListJobsInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the branch to use for the request.
    branchName: string
    -- A pagination token. Set to null to start listing steps from the start. If a non-null
    -- pagination token is returned in a result, pass its value in here to list more steps.
    nextToken: string
    -- The maximum number of records to list in a single response.
    maxResults: number
end

-- The maximum number of records to list in a single response.
local record ListJobsOutput
    -- The result structure for the list job result request.
    jobSummaries: {JobSummary}
    -- A pagination token. If non-null the pagination token is returned in a result. Pass its
    -- value in another request to retrieve more entries.
    nextToken: string
end

-- The request structure to use to list tags for a resource.
local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) to use to list tags.
    resourceArn: string
end

-- The response for the list tags for resource request.
local record ListTagsForResourceOutput
    -- A list of tags for the specified The Amazon Resource Name (ARN).
    tags: {string : string}
end

-- The request structure for the list webhooks request.
local record ListWebhooksInput
    -- The unique ID for an Amplify app.
    appId: string
    -- A pagination token. Set to null to start listing webhooks from the start. If
    -- non-null,the pagination token is returned in a result. Pass its value in here to list
    -- more webhooks.
    nextToken: string
    -- The maximum number of records to list in a single response.
    maxResults: number
end

-- The result structure for the list webhooks request.
local record ListWebhooksOutput
    -- A list of webhooks.
    webhooks: {Webhook}
    -- A pagination token. If non-null, the pagination token is returned in a result. Pass
    -- its value in another request to retrieve more entries.
    nextToken: string
end

-- The request structure for the start a deployment request.
local record StartDeploymentInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the branch to use for the deployment job.
    branchName: string
    -- The job ID for this deployment that is generated by the CreateDeployment
    -- request.
    jobId: string
    -- The source URL for the deployment that is used when calling
    -- StartDeployment without CreateDeployment. The source URL
    -- can be either an HTTP GET URL that is publicly accessible and downloads a single .zip
    -- file, or an Amazon S3 bucket and prefix.
    sourceUrl: string
    -- The type of source specified by the sourceURL. If the value is
    -- ZIP, the source is a .zip file. If the value is
    -- BUCKET_PREFIX, the source is an Amazon S3 bucket and prefix. If
    -- no value is specified, the default is ZIP.
    sourceUrlType: string
end

-- The result structure for the start a deployment request.
local record StartDeploymentOutput
    -- The summary for the job.
    jobSummary: JobSummary
end

-- The request structure for the start job request.
local record StartJobInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the branch to use for the job.
    branchName: string
    -- The unique ID for an existing job. This is required if the value of
    -- jobType is RETRY.
    jobId: string
    -- Describes the type for the job. The job type RELEASE starts a new job
    -- with the latest change from the specified branch. This value is available only for apps
    -- that are connected to a repository.
    -- The job type RETRY retries an existing job. If the job type value is
    -- RETRY, the jobId is also required.
    jobType: string
    -- A descriptive reason for starting the job.
    jobReason: string
    -- The commit ID from a third-party repository provider for the job.
    commitId: string
    -- The commit message from a third-party repository provider for the job.
    commitMessage: string
    -- The commit date and time for the job.
    commitTime: number
end

-- The result structure for the run job request.
local record StartJobOutput
    -- The summary for the job.
    jobSummary: JobSummary
end

-- The request structure for the stop job request.
local record StopJobInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the branch to use for the stop job request.
    branchName: string
    -- The unique id for the job.
    jobId: string
end

-- The result structure for the stop job request.
local record StopJobOutput
    -- The summary for the job.
    jobSummary: JobSummary
end

-- The request structure to tag a resource with a tag key and value.
local record TagResourceInput
    -- The Amazon Resource Name (ARN) to use to tag a resource.
    resourceArn: string
    -- The tags used to tag the resource.
    tags: {string : string}
end

-- The response for the tag resource request.
local record TagResourceOutput
end

-- The request structure for the untag resource request.
local record UntagResourceInput
    -- The Amazon Resource Name (ARN) to use to untag a resource.
    resourceArn: string
    -- The tag keys to use to untag a resource.
    tagKeys: {string}
end

-- The response for the untag resource request.
local record UntagResourceOutput
end

-- The request structure for the update app request.
local record UpdateAppInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name for an Amplify app.
    name: string
    -- The description for an Amplify app.
    description: string
    -- The platform for the Amplify app. For a static app, set the platform type to
    -- WEB. For a dynamic server-side rendered (SSR) app, set the platform
    -- type to WEB_COMPUTE. For an app requiring Amplify Hosting's original SSR
    -- support only, set the platform type to WEB_DYNAMIC.
    -- If you are deploying an SSG only app with Next.js version 14 or later, you must set
    -- the platform type to WEB_COMPUTE.
    platform: string
    -- The Amazon Resource Name (ARN) of the IAM role to assign to an SSR app.
    -- The SSR Compute role allows the Amplify Hosting compute service to
    -- securely access specific Amazon Web Services resources based on the role's permissions.
    -- For more information about the SSR Compute role, see Adding an SSR Compute
    -- role in the Amplify User Guide.
    computeRoleArn: string
    -- The Amazon Resource Name (ARN) of the IAM service role for the Amplify app.
    iamServiceRoleArn: string
    -- The environment variables for an Amplify app.
    environmentVariables: {string : string}
    -- Enables branch auto-building for an Amplify app.
    enableBranchAutoBuild: boolean
    -- Automatically disconnects a branch in the Amplify console when you delete a branch
    -- from your Git repository.
    enableBranchAutoDeletion: boolean
    -- Enables basic authorization for an Amplify app.
    enableBasicAuth: boolean
    -- The basic authorization credentials for an Amplify app. You must base64-encode the
    -- authorization credentials and provide them in the format
    -- user:password.
    basicAuthCredentials: string
    -- The custom redirect and rewrite rules for an Amplify app.
    customRules: {CustomRule}
    -- The build specification (build spec) for an Amplify app.
    buildSpec: string
    -- The custom HTTP headers for an Amplify app.
    customHeaders: string
    -- Enables automated branch creation for an Amplify app.
    enableAutoBranchCreation: boolean
    -- Describes the automated branch creation glob patterns for an Amplify app.
    autoBranchCreationPatterns: {string}
    -- The automated branch creation configuration for an Amplify app.
    autoBranchCreationConfig: AutoBranchCreationConfig
    -- The name of the Git repository for an Amplify app.
    repository: string
    -- The OAuth token for a third-party source control system for an Amplify app. The OAuth
    -- token is used to create a webhook and a read-only deploy key using SSH cloning. The
    -- OAuth token is not stored.
    -- Use oauthToken for repository providers other than GitHub, such as
    -- Bitbucket or CodeCommit.
    -- To authorize access to GitHub as your repository provider, use
    -- accessToken.
    -- You must specify either oauthToken or accessToken when you
    -- update an app.
    -- Existing Amplify apps deployed from a GitHub repository using OAuth continue to work
    -- with CI/CD. However, we strongly recommend that you migrate these apps to use the GitHub
    -- App. For more information, see Migrating an existing OAuth app to the Amplify GitHub App in the
    -- Amplify User Guide .
    oauthToken: string
    -- The personal access token for a GitHub repository for an Amplify app. The personal
    -- access token is used to authorize access to a GitHub repository using the Amplify GitHub
    -- App. The token is not stored.
    -- Use accessToken for GitHub repositories only. To authorize access to a
    -- repository provider such as Bitbucket or CodeCommit, use oauthToken.
    -- You must specify either accessToken or oauthToken when you
    -- update an app.
    -- Existing Amplify apps deployed from a GitHub repository using OAuth continue to work
    -- with CI/CD. However, we strongly recommend that you migrate these apps to use the GitHub
    -- App. For more information, see Migrating an existing OAuth app to the Amplify GitHub App in the
    -- Amplify User Guide .
    accessToken: string
    -- Describes the configuration details that apply to the jobs for an Amplify app.
    jobConfig: JobConfig
    -- The cache configuration for the Amplify app.
    cacheConfig: CacheConfig
end

-- The result structure for an Amplify app update request.
local record UpdateAppOutput
    -- Represents the updated Amplify app.
    app: App
end

-- The request structure for the update branch request.
local record UpdateBranchInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the branch.
    branchName: string
    -- The description for the branch.
    description: string
    -- The framework for the branch.
    framework: string
    -- Describes the current stage for the branch.
    stage: string
    -- Enables notifications for the branch.
    enableNotification: boolean
    -- Enables auto building for the branch.
    enableAutoBuild: boolean
    -- Specifies whether the skew protection feature is enabled for the branch.
    -- Deployment skew protection is available to Amplify applications to
    -- eliminate version skew issues between client and servers in web applications. When you
    -- apply skew protection to a branch, you can ensure that your clients always interact with
    -- the correct version of server-side assets, regardless of when a deployment occurs. For
    -- more information about skew protection, see Skew protection for Amplify deployments in the Amplify User
    -- Guide.
    enableSkewProtection: boolean
    -- The environment variables for the branch.
    environmentVariables: {string : string}
    -- The basic authorization credentials for the branch. You must base64-encode the
    -- authorization credentials and provide them in the format
    -- user:password.
    basicAuthCredentials: string
    -- Enables basic authorization for the branch.
    enableBasicAuth: boolean
    -- Enables performance mode for the branch.
    -- Performance mode optimizes for faster hosting performance by keeping content cached at
    -- the edge for a longer interval. When performance mode is enabled, hosting configuration
    -- or code changes can take up to 10 minutes to roll out.
    enablePerformanceMode: boolean
    -- The build specification (build spec) for the branch.
    buildSpec: string
    -- The content Time to Live (TTL) for the website in seconds.
    ttl: string
    -- The display name for a branch. This is used as the default domain prefix.
    displayName: string
    -- Enables pull request previews for this branch.
    enablePullRequestPreview: boolean
    -- The Amplify environment name for the pull request.
    pullRequestEnvironmentName: string
    -- The Amazon Resource Name (ARN) for a backend environment that is part of a Gen 1
    -- Amplify app.
    -- This field is available to Amplify Gen 1 apps only where the backend is
    -- created using Amplify Studio or the Amplify command line
    -- interface (CLI).
    backendEnvironmentArn: string
    -- The backend for a Branch of an Amplify app. Use for a
    -- backend created from an CloudFormation stack.
    -- This field is available to Amplify Gen 2 apps only. When you deploy an
    -- application with Amplify Gen 2, you provision the app's backend infrastructure using
    -- Typescript code.
    backend: Backend
    -- The Amazon Resource Name (ARN) of the IAM role to assign to a branch of
    -- an SSR app. The SSR Compute role allows the Amplify Hosting compute
    -- service to securely access specific Amazon Web Services resources based on the role's
    -- permissions. For more information about the SSR Compute role, see Adding an SSR Compute role in the Amplify User
    -- Guide.
    computeRoleArn: string
end

-- The result structure for the update branch request.
local record UpdateBranchOutput
    -- The branch for an Amplify app, which maps to a third-party repository branch.
    branch: Branch
end

-- The request structure for the update domain association request.
local record UpdateDomainAssociationInput
    -- The unique ID for an Amplify app.
    appId: string
    -- The name of the domain.
    domainName: string
    -- Enables the automated creation of subdomains for branches.
    enableAutoSubDomain: boolean
    -- Describes the settings for the subdomain.
    subDomainSettings: {SubDomainSetting}
    -- Sets the branch patterns for automatic subdomain creation.
    autoSubDomainCreationPatterns: {string}
    -- The required AWS Identity and Access Management (IAM) service role for the Amazon
    -- Resource Name (ARN) for automatically creating subdomains.
    autoSubDomainIAMRole: string
    -- The type of SSL/TLS certificate to use for your custom domain.
    certificateSettings: CertificateSettings
end

-- The result structure for the update domain association request.
local record UpdateDomainAssociationOutput
    -- Describes a domain association, which associates a custom domain with an Amplify app.
    domainAssociation: DomainAssociation
end

-- The request structure for the update webhook request.
local record UpdateWebhookInput
    -- The unique ID for a webhook.
    webhookId: string
    -- The name for a branch that is part of an Amplify app.
    branchName: string
    -- The description for a webhook.
    description: string
end

-- The result structure for the update webhook request.
local record UpdateWebhookOutput
    -- Describes a webhook that connects repository events to an Amplify app.
    webhook: Webhook
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An operation failed due to a non-existent resource.
local record ResourceNotFoundException
    code: string
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- A resource could not be created because service quotas were exceeded.
local record LimitExceededException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An operation failed due to a non-existent resource.
local record ResourceNotFoundException
    code: string
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An operation failed due to a non-existent resource.
local record ResourceNotFoundException
    code: string
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

-- A request contains unexpected data.
local record BadRequestException
    message: string
end

-- An operation failed because a dependent service threw an exception.
local record DependentServiceFailureException
    message: string
end

-- The service failed to perform an operation due to an internal issue.
local record InternalFailureException
    message: string
end

-- An entity was not found during an operation.
local record NotFoundException
    message: string
end

-- An operation failed due to a lack of access.
local record UnauthorizedException
    message: string
end

local type JobType = "RELEASE" | "RETRY" | "MANUAL" | "WEB_HOOK"

local type Platform = "WEB" | "WEB_DYNAMIC" | "WEB_COMPUTE"

local type SourceUrlType = "ZIP" | "BUCKET_PREFIX"

local type Stage = "PRODUCTION" | "BETA" | "DEVELOPMENT" | "EXPERIMENTAL" | "PULL_REQUEST"

local record M
    CreateAppInput: CreateAppInput
    CreateAppOutput: CreateAppOutput
    CreateBackendEnvironmentInput: CreateBackendEnvironmentInput
    CreateBackendEnvironmentOutput: CreateBackendEnvironmentOutput
    CreateBranchInput: CreateBranchInput
    CreateBranchOutput: CreateBranchOutput
    CreateDeploymentInput: CreateDeploymentInput
    CreateDeploymentOutput: CreateDeploymentOutput
    CreateDomainAssociationInput: CreateDomainAssociationInput
    CreateDomainAssociationOutput: CreateDomainAssociationOutput
    CreateWebhookInput: CreateWebhookInput
    CreateWebhookOutput: CreateWebhookOutput
    DeleteAppInput: DeleteAppInput
    DeleteAppOutput: DeleteAppOutput
    DeleteBackendEnvironmentInput: DeleteBackendEnvironmentInput
    DeleteBackendEnvironmentOutput: DeleteBackendEnvironmentOutput
    DeleteBranchInput: DeleteBranchInput
    DeleteBranchOutput: DeleteBranchOutput
    DeleteDomainAssociationInput: DeleteDomainAssociationInput
    DeleteDomainAssociationOutput: DeleteDomainAssociationOutput
    DeleteJobInput: DeleteJobInput
    DeleteJobOutput: DeleteJobOutput
    DeleteWebhookInput: DeleteWebhookInput
    DeleteWebhookOutput: DeleteWebhookOutput
    GenerateAccessLogsInput: GenerateAccessLogsInput
    GenerateAccessLogsOutput: GenerateAccessLogsOutput
    GetAppInput: GetAppInput
    GetAppOutput: GetAppOutput
    GetArtifactUrlInput: GetArtifactUrlInput
    GetArtifactUrlOutput: GetArtifactUrlOutput
    GetBackendEnvironmentInput: GetBackendEnvironmentInput
    GetBackendEnvironmentOutput: GetBackendEnvironmentOutput
    GetBranchInput: GetBranchInput
    GetBranchOutput: GetBranchOutput
    GetDomainAssociationInput: GetDomainAssociationInput
    GetDomainAssociationOutput: GetDomainAssociationOutput
    GetJobInput: GetJobInput
    GetJobOutput: GetJobOutput
    GetWebhookInput: GetWebhookInput
    GetWebhookOutput: GetWebhookOutput
    ListAppsInput: ListAppsInput
    ListAppsOutput: ListAppsOutput
    ListArtifactsInput: ListArtifactsInput
    ListArtifactsOutput: ListArtifactsOutput
    ListBackendEnvironmentsInput: ListBackendEnvironmentsInput
    ListBackendEnvironmentsOutput: ListBackendEnvironmentsOutput
    ListBranchesInput: ListBranchesInput
    ListBranchesOutput: ListBranchesOutput
    ListDomainAssociationsInput: ListDomainAssociationsInput
    ListDomainAssociationsOutput: ListDomainAssociationsOutput
    ListJobsInput: ListJobsInput
    ListJobsOutput: ListJobsOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    ListWebhooksInput: ListWebhooksInput
    ListWebhooksOutput: ListWebhooksOutput
    StartDeploymentInput: StartDeploymentInput
    StartDeploymentOutput: StartDeploymentOutput
    StartJobInput: StartJobInput
    StartJobOutput: StartJobOutput
    StopJobInput: StopJobInput
    StopJobOutput: StopJobOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateAppInput: UpdateAppInput
    UpdateAppOutput: UpdateAppOutput
    UpdateBranchInput: UpdateBranchInput
    UpdateBranchOutput: UpdateBranchOutput
    UpdateDomainAssociationInput: UpdateDomainAssociationInput
    UpdateDomainAssociationOutput: UpdateDomainAssociationOutput
    UpdateWebhookInput: UpdateWebhookInput
    UpdateWebhookOutput: UpdateWebhookOutput
end

return M
