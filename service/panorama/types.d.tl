-- Code generated by lua-client-codegen. DO NOT EDIT.

local record CreateApplicationInstanceInput
    -- A name for the application instance.
    Name: string
    -- A description for the application instance.
    Description: string
    -- The application's manifest document.
    ManifestPayload: ManifestPayload
    -- Setting overrides for the application manifest.
    ManifestOverridesPayload: ManifestOverridesPayload
    -- The ID of an application instance to replace with the new instance.
    ApplicationInstanceIdToReplace: string
    -- The ARN of a runtime role for the application instance.
    RuntimeRoleArn: string
    -- A device's ID.
    DefaultRuntimeContextDevice: string
    -- Tags for the application instance.
    Tags: {string : string}
end

local record CreateApplicationInstanceOutput
    -- The application instance's ID.
    ApplicationInstanceId: string
end

local record CreateJobForDevicesInput
    -- ID of target device.
    DeviceIds: {string}
    -- Configuration settings for a software update job.
    DeviceJobConfig: DeviceJobConfig
    -- The type of job to run.
    JobType: string
end

local record CreateJobForDevicesOutput
    -- A list of jobs.
    Jobs: {Job}
end

local record CreateNodeFromTemplateJobInput
    -- The type of node.
    TemplateType: string
    -- An output package name for the node.
    OutputPackageName: string
    -- An output package version for the node.
    OutputPackageVersion: string
    -- A name for the node.
    NodeName: string
    -- A description for the node.
    NodeDescription: string
    -- Template parameters for the node.
    TemplateParameters: {string : string}
    -- Tags for the job.
    JobTags: {JobResourceTags}
end

local record CreateNodeFromTemplateJobOutput
    -- The job's ID.
    JobId: string
end

local record CreatePackageInput
    -- A name for the package.
    PackageName: string
    -- Tags for the package.
    Tags: {string : string}
end

local record CreatePackageOutput
    -- The package's ID.
    PackageId: string
    -- The package's ARN.
    Arn: string
    -- The package's storage location.
    StorageLocation: StorageLocation
end

local record CreatePackageImportJobInput
    -- A job type for the package import job.
    JobType: string
    -- An input config for the package import job.
    InputConfig: PackageImportJobInputConfig
    -- An output config for the package import job.
    OutputConfig: PackageImportJobOutputConfig
    -- A client token for the package import job.
    ClientToken: string
    -- Tags for the package import job.
    JobTags: {JobResourceTags}
end

local record CreatePackageImportJobOutput
    -- The job's ID.
    JobId: string
end

local record DeleteDeviceInput
    -- The device's ID.
    DeviceId: string
end

local record DeleteDeviceOutput
    -- The device's ID.
    DeviceId: string
end

local record DeletePackageInput
    -- The package's ID.
    PackageId: string
    -- Delete the package even if it has artifacts stored in its access point. Deletes the package's artifacts from
    -- Amazon S3.
    ForceDelete: boolean
end

local record DeletePackageOutput
end

local record DeregisterPackageVersionInput
    -- An owner account.
    OwnerAccount: string
    -- A package ID.
    PackageId: string
    -- A package version.
    PackageVersion: string
    -- A patch version.
    PatchVersion: string
    -- If the version was marked latest, the new version to maker as latest.
    UpdatedLatestPatchVersion: string
end

local record DeregisterPackageVersionOutput
end

local record DescribeApplicationInstanceInput
    -- The application instance's ID.
    ApplicationInstanceId: string
end

local record DescribeApplicationInstanceOutput
    -- The application instance's name.
    Name: string
    -- The application instance's description.
    Description: string
    -- The device's ID.
    DefaultRuntimeContextDevice: string
    -- The device's bane.
    DefaultRuntimeContextDeviceName: string
    -- The ID of the application instance that this instance replaced.
    ApplicationInstanceIdToReplace: string
    -- The application instance's runtime role ARN.
    RuntimeRoleArn: string
    -- The application instance's status.
    Status: string
    -- The application instance's health status.
    HealthStatus: string
    -- The application instance's status description.
    StatusDescription: string
    -- When the application instance was created.
    CreatedTime: number
    -- The application instance was updated.
    LastUpdatedTime: number
    -- The application instance's ID.
    ApplicationInstanceId: string
    -- The application instance's ARN.
    Arn: string
    -- The application instance's tags.
    Tags: {string : string}
    -- The application instance's state.
    RuntimeContextStates: {ReportedRuntimeContextState}
end

local record DescribeApplicationInstanceDetailsInput
    -- The application instance's ID.
    ApplicationInstanceId: string
end

local record DescribeApplicationInstanceDetailsOutput
    -- The application instance's name.
    Name: string
    -- The application instance's description.
    Description: string
    -- The application instance's default runtime context device.
    DefaultRuntimeContextDevice: string
    -- The application instance's configuration manifest.
    ManifestPayload: ManifestPayload
    -- Parameter overrides for the configuration manifest.
    ManifestOverridesPayload: ManifestOverridesPayload
    -- The ID of the application instance that this instance replaced.
    ApplicationInstanceIdToReplace: string
    -- When the application instance was created.
    CreatedTime: number
    -- The application instance's ID.
    ApplicationInstanceId: string
end

local record DescribeDeviceInput
    -- The device's ID.
    DeviceId: string
end

local record DescribeDeviceOutput
    -- The device's ID.
    DeviceId: string
    -- The device's name.
    Name: string
    -- The device's ARN.
    Arn: string
    -- The device's description.
    Description: string
    -- The device's type.
    Type: string
    -- The device's connection status.
    DeviceConnectionStatus: string
    -- When the device was created.
    CreatedTime: number
    -- The device's provisioning status.
    ProvisioningStatus: string
    -- The latest software version available for the device.
    LatestSoftware: string
    -- The device's current software version.
    CurrentSoftware: string
    -- The device's serial number.
    SerialNumber: string
    -- The device's tags.
    Tags: {string : string}
    -- The device's networking configuration.
    NetworkingConfiguration: NetworkPayload
    -- The device's networking status.
    CurrentNetworkingStatus: NetworkStatus
    -- The device's lease expiration time.
    LeaseExpirationTime: number
    -- Beta software releases available for the device.
    AlternateSoftwares: {AlternateSoftwareMetadata}
    -- The most recent beta software release.
    LatestAlternateSoftware: string
    -- The device's maker.
    Brand: string
    -- A device's latest job. Includes the target image version, and the job status.
    LatestDeviceJob: LatestDeviceJob
    -- A device's aggregated status. Including the device's connection status, provisioning status, and lease status.
    DeviceAggregatedStatus: string
end

local record DescribeDeviceJobInput
    -- The job's ID.
    JobId: string
end

local record DescribeDeviceJobOutput
    -- The job's ID.
    JobId: string
    -- The device's ID.
    DeviceId: string
    -- The device's ARN.
    DeviceArn: string
    -- The device's name.
    DeviceName: string
    -- The device's type.
    DeviceType: string
    -- For an OTA job, the target version of the device software.
    ImageVersion: string
    -- The job's status.
    Status: string
    -- When the job was created.
    CreatedTime: number
    -- The job's type.
    JobType: string
end

local record DescribeNodeInput
    -- The node's ID.
    NodeId: string
    -- The account ID of the node's owner.
    OwnerAccount: string
end

local record DescribeNodeOutput
    -- The node's ID.
    NodeId: string
    -- The node's name.
    Name: string
    -- The node's category.
    Category: string
    -- The account ID of the node's owner.
    OwnerAccount: string
    -- The node's package name.
    PackageName: string
    -- The node's package ID.
    PackageId: string
    -- The node's ARN.
    PackageArn: string
    -- The node's package version.
    PackageVersion: string
    -- The node's patch version.
    PatchVersion: string
    -- The node's interface.
    NodeInterface: NodeInterface
    -- The node's asset name.
    AssetName: string
    -- The node's description.
    Description: string
    -- When the node was created.
    CreatedTime: number
    -- When the node was updated.
    LastUpdatedTime: number
end

local record DescribeNodeFromTemplateJobInput
    -- The job's ID.
    JobId: string
end

local record DescribeNodeFromTemplateJobOutput
    -- The job's ID.
    JobId: string
    -- The job's status.
    Status: string
    -- The job's status message.
    StatusMessage: string
    -- When the job was created.
    CreatedTime: number
    -- When the job was updated.
    LastUpdatedTime: number
    -- The job's output package name.
    OutputPackageName: string
    -- The job's output package version.
    OutputPackageVersion: string
    -- The node's name.
    NodeName: string
    -- The node's description.
    NodeDescription: string
    -- The job's template type.
    TemplateType: string
    -- The job's template parameters.
    TemplateParameters: {string : string}
    -- The job's tags.
    JobTags: {JobResourceTags}
end

local record DescribePackageInput
    -- The package's ID.
    PackageId: string
end

local record DescribePackageOutput
    -- The package's ID.
    PackageId: string
    -- The package's name.
    PackageName: string
    -- The package's ARN.
    Arn: string
    -- The package's storage location.
    StorageLocation: StorageLocation
    -- ARNs of accounts that have read access to the package.
    ReadAccessPrincipalArns: {string}
    -- ARNs of accounts that have write access to the package.
    WriteAccessPrincipalArns: {string}
    -- When the package was created.
    CreatedTime: number
    -- The package's tags.
    Tags: {string : string}
end

local record DescribePackageImportJobInput
    -- The job's ID.
    JobId: string
end

local record DescribePackageImportJobOutput
    -- The job's ID.
    JobId: string
    -- The job's client token.
    ClientToken: string
    -- The job's type.
    JobType: string
    -- The job's input config.
    InputConfig: PackageImportJobInputConfig
    -- The job's output config.
    OutputConfig: PackageImportJobOutputConfig
    -- The job's output.
    Output: PackageImportJobOutput
    -- When the job was created.
    CreatedTime: number
    -- When the job was updated.
    LastUpdatedTime: number
    -- The job's status.
    Status: string
    -- The job's status message.
    StatusMessage: string
    -- The job's tags.
    JobTags: {JobResourceTags}
end

local record DescribePackageVersionInput
    -- The version's owner account.
    OwnerAccount: string
    -- The version's ID.
    PackageId: string
    -- The version's version.
    PackageVersion: string
    -- The version's patch version.
    PatchVersion: string
end

local record DescribePackageVersionOutput
    -- The account ID of the version's owner.
    OwnerAccount: string
    -- The version's ID.
    PackageId: string
    -- The ARN of the package.
    PackageArn: string
    -- The version's name.
    PackageName: string
    -- The version's version.
    PackageVersion: string
    -- The version's patch version.
    PatchVersion: string
    -- Whether the version is the latest available.
    IsLatestPatch: boolean
    -- The version's status.
    Status: string
    -- The version's status description.
    StatusDescription: string
    -- The version's registered time.
    RegisteredTime: number
end

local record ListApplicationInstanceDependenciesInput
    -- The application instance's ID.
    ApplicationInstanceId: string
    -- The maximum number of application instance dependencies to return in one page of results.
    MaxResults: number
    -- Specify the pagination token from a previous request to retrieve the next page of results.
    NextToken: string
end

local record ListApplicationInstanceDependenciesOutput
    -- A list of package objects.
    PackageObjects: {PackageObject}
    -- A pagination token that's included if more results are available.
    NextToken: string
end

local record ListApplicationInstanceNodeInstancesInput
    -- The node instances' application instance ID.
    ApplicationInstanceId: string
    -- The maximum number of node instances to return in one page of results.
    MaxResults: number
    -- Specify the pagination token from a previous request to retrieve the next page of results.
    NextToken: string
end

local record ListApplicationInstanceNodeInstancesOutput
    -- A list of node instances.
    NodeInstances: {NodeInstance}
    -- A pagination token that's included if more results are available.
    NextToken: string
end

local record ListApplicationInstancesInput
    -- The application instances' device ID.
    DeviceId: string
    -- Only include instances with a specific status.
    StatusFilter: string
    -- The maximum number of application instances to return in one page of results.
    MaxResults: number
    -- Specify the pagination token from a previous request to retrieve the next page of results.
    NextToken: string
end

local record ListApplicationInstancesOutput
    -- A list of application instances.
    ApplicationInstances: {ApplicationInstance}
    -- A pagination token that's included if more results are available.
    NextToken: string
end

local record ListDevicesInput
    -- Specify the pagination token from a previous request to retrieve the next page of results.
    NextToken: string
    -- The maximum number of devices to return in one page of results.
    MaxResults: number
    -- The target column to be sorted on. Default column sort is CREATED_TIME.
    SortBy: string
    -- The sorting order for the returned list. SortOrder is DESCENDING by default based on CREATED_TIME. Otherwise, SortOrder is ASCENDING.
    SortOrder: string
    -- Filter based on device's name. Prefixes supported.
    NameFilter: string
    -- Filter based on a device's status.
    DeviceAggregatedStatusFilter: string
end

local record ListDevicesOutput
    -- A list of devices.
    Devices: {Device}
    -- A pagination token that's included if more results are available.
    NextToken: string
end

local record ListDevicesJobsInput
    -- Filter results by the job's target device ID.
    DeviceId: string
    -- Specify the pagination token from a previous request to retrieve the next page of results.
    NextToken: string
    -- The maximum number of device jobs to return in one page of results.
    MaxResults: number
end

local record ListDevicesJobsOutput
    -- A list of jobs.
    DeviceJobs: {DeviceJob}
    -- A pagination token that's included if more results are available.
    NextToken: string
end

local record ListNodeFromTemplateJobsInput
    -- Specify the pagination token from a previous request to retrieve the next page of results.
    NextToken: string
    -- The maximum number of node from template jobs to return in one page of results.
    MaxResults: number
end

local record ListNodeFromTemplateJobsOutput
    -- A list of jobs.
    NodeFromTemplateJobs: {NodeFromTemplateJob}
    -- A pagination token that's included if more results are available.
    NextToken: string
end

local record ListNodesInput
    -- Search for nodes by category.
    Category: string
    -- Search for nodes by the account ID of the nodes' owner.
    OwnerAccount: string
    -- Search for nodes by name.
    PackageName: string
    -- Search for nodes by version.
    PackageVersion: string
    -- Search for nodes by patch version.
    PatchVersion: string
    -- Specify the pagination token from a previous request to retrieve the next page of results.
    NextToken: string
    -- The maximum number of nodes to return in one page of results.
    MaxResults: number
end

local record ListNodesOutput
    -- A list of nodes.
    Nodes: {Node}
    -- A pagination token that's included if more results are available.
    NextToken: string
end

local record ListPackageImportJobsInput
    -- Specify the pagination token from a previous request to retrieve the next page of results.
    NextToken: string
    -- The maximum number of package import jobs to return in one page of results.
    MaxResults: number
end

local record ListPackageImportJobsOutput
    -- A list of package import jobs.
    PackageImportJobs: {PackageImportJob}
    -- A pagination token that's included if more results are available.
    NextToken: string
end

local record ListPackagesInput
    -- The maximum number of packages to return in one page of results.
    MaxResults: number
    -- Specify the pagination token from a previous request to retrieve the next page of results.
    NextToken: string
end

local record ListPackagesOutput
    -- A list of packages.
    Packages: {PackageListItem}
    -- A pagination token that's included if more results are available.
    NextToken: string
end

local record ListTagsForResourceInput
    -- The resource's ARN.
    ResourceArn: string
end

local record ListTagsForResourceOutput
    -- A list of tags.
    Tags: {string : string}
end

local record ProvisionDeviceInput
    -- A name for the device.
    Name: string
    -- A description for the device.
    Description: string
    -- Tags for the device.
    Tags: {string : string}
    -- A networking configuration for the device.
    NetworkingConfiguration: NetworkPayload
end

local record ProvisionDeviceOutput
    -- The device's ID.
    DeviceId: string
    -- The device's ARN.
    Arn: string
    -- The device's status.
    Status: string
    -- The device's configuration bundle.
    Certificates: string
    -- The device's IoT thing name.
    IotThingName: string
end

local record RegisterPackageVersionInput
    -- An owner account.
    OwnerAccount: string
    -- A package ID.
    PackageId: string
    -- A package version.
    PackageVersion: string
    -- A patch version.
    PatchVersion: string
    -- Whether to mark the new version as the latest version.
    MarkLatest: boolean
end

local record RegisterPackageVersionOutput
end

local record RemoveApplicationInstanceInput
    -- An application instance ID.
    ApplicationInstanceId: string
end

local record RemoveApplicationInstanceOutput
end

local record SignalApplicationInstanceNodeInstancesInput
    -- An application instance ID.
    ApplicationInstanceId: string
    -- A list of signals.
    NodeSignals: {NodeSignal}
end

local record SignalApplicationInstanceNodeInstancesOutput
    -- An application instance ID.
    ApplicationInstanceId: string
end

local record TagResourceInput
    -- The resource's ARN.
    ResourceArn: string
    -- Tags for the resource.
    Tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The resource's ARN.
    ResourceArn: string
    -- Tag keys to remove.
    TagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateDeviceMetadataInput
    -- The device's ID.
    DeviceId: string
    -- A description for the device.
    Description: string
end

local record UpdateDeviceMetadataOutput
    -- The device's ID.
    DeviceId: string
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request would cause a limit to be exceeded.
local record ServiceQuotaExceededException
    Message: string
    -- The target resource's ID.
    ResourceId: string
    -- The target resource's type.
    ResourceType: string
    -- The name of the limit.
    QuotaCode: string
    -- The name of the service.
    ServiceCode: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request would cause a limit to be exceeded.
local record ServiceQuotaExceededException
    Message: string
    -- The target resource's ID.
    ResourceId: string
    -- The target resource's type.
    ResourceType: string
    -- The name of the limit.
    QuotaCode: string
    -- The name of the service.
    ServiceCode: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The request would cause a limit to be exceeded.
local record ServiceQuotaExceededException
    Message: string
    -- The target resource's ID.
    ResourceId: string
    -- The target resource's type.
    ResourceType: string
    -- The name of the limit.
    QuotaCode: string
    -- The name of the service.
    ServiceCode: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

-- The requestor does not have permission to access the target action or resource.
local record AccessDeniedException
    Message: string
end

-- The target resource is in use.
local record ConflictException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ConflictExceptionErrorArgument}
end

-- An internal error occurred.
local record InternalServerException
    Message: string
    -- The number of seconds a client should wait before retrying the call.
    RetryAfterSeconds: number
end

-- The target resource was not found.
local record ResourceNotFoundException
    Message: string
    -- The resource's ID.
    ResourceId: string
    -- The resource's type.
    ResourceType: string
end

-- The request contains an invalid parameter value.
local record ValidationException
    Message: string
    -- The reason that validation failed.
    Reason: string
    -- A unique ID for the error.
    ErrorId: string
    -- A list of attributes that led to the exception and their values.
    ErrorArguments: {ValidationExceptionErrorArgument}
    -- A list of request parameters that failed validation.
    Fields: {ValidationExceptionField}
end

local record M
    CreateApplicationInstanceInput: CreateApplicationInstanceInput
    CreateApplicationInstanceOutput: CreateApplicationInstanceOutput
    CreateJobForDevicesInput: CreateJobForDevicesInput
    CreateJobForDevicesOutput: CreateJobForDevicesOutput
    CreateNodeFromTemplateJobInput: CreateNodeFromTemplateJobInput
    CreateNodeFromTemplateJobOutput: CreateNodeFromTemplateJobOutput
    CreatePackageInput: CreatePackageInput
    CreatePackageOutput: CreatePackageOutput
    CreatePackageImportJobInput: CreatePackageImportJobInput
    CreatePackageImportJobOutput: CreatePackageImportJobOutput
    DeleteDeviceInput: DeleteDeviceInput
    DeleteDeviceOutput: DeleteDeviceOutput
    DeletePackageInput: DeletePackageInput
    DeletePackageOutput: DeletePackageOutput
    DeregisterPackageVersionInput: DeregisterPackageVersionInput
    DeregisterPackageVersionOutput: DeregisterPackageVersionOutput
    DescribeApplicationInstanceInput: DescribeApplicationInstanceInput
    DescribeApplicationInstanceOutput: DescribeApplicationInstanceOutput
    DescribeApplicationInstanceDetailsInput: DescribeApplicationInstanceDetailsInput
    DescribeApplicationInstanceDetailsOutput: DescribeApplicationInstanceDetailsOutput
    DescribeDeviceInput: DescribeDeviceInput
    DescribeDeviceOutput: DescribeDeviceOutput
    DescribeDeviceJobInput: DescribeDeviceJobInput
    DescribeDeviceJobOutput: DescribeDeviceJobOutput
    DescribeNodeInput: DescribeNodeInput
    DescribeNodeOutput: DescribeNodeOutput
    DescribeNodeFromTemplateJobInput: DescribeNodeFromTemplateJobInput
    DescribeNodeFromTemplateJobOutput: DescribeNodeFromTemplateJobOutput
    DescribePackageInput: DescribePackageInput
    DescribePackageOutput: DescribePackageOutput
    DescribePackageImportJobInput: DescribePackageImportJobInput
    DescribePackageImportJobOutput: DescribePackageImportJobOutput
    DescribePackageVersionInput: DescribePackageVersionInput
    DescribePackageVersionOutput: DescribePackageVersionOutput
    ListApplicationInstanceDependenciesInput: ListApplicationInstanceDependenciesInput
    ListApplicationInstanceDependenciesOutput: ListApplicationInstanceDependenciesOutput
    ListApplicationInstanceNodeInstancesInput: ListApplicationInstanceNodeInstancesInput
    ListApplicationInstanceNodeInstancesOutput: ListApplicationInstanceNodeInstancesOutput
    ListApplicationInstancesInput: ListApplicationInstancesInput
    ListApplicationInstancesOutput: ListApplicationInstancesOutput
    ListDevicesInput: ListDevicesInput
    ListDevicesOutput: ListDevicesOutput
    ListDevicesJobsInput: ListDevicesJobsInput
    ListDevicesJobsOutput: ListDevicesJobsOutput
    ListNodeFromTemplateJobsInput: ListNodeFromTemplateJobsInput
    ListNodeFromTemplateJobsOutput: ListNodeFromTemplateJobsOutput
    ListNodesInput: ListNodesInput
    ListNodesOutput: ListNodesOutput
    ListPackageImportJobsInput: ListPackageImportJobsInput
    ListPackageImportJobsOutput: ListPackageImportJobsOutput
    ListPackagesInput: ListPackagesInput
    ListPackagesOutput: ListPackagesOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    ProvisionDeviceInput: ProvisionDeviceInput
    ProvisionDeviceOutput: ProvisionDeviceOutput
    RegisterPackageVersionInput: RegisterPackageVersionInput
    RegisterPackageVersionOutput: RegisterPackageVersionOutput
    RemoveApplicationInstanceInput: RemoveApplicationInstanceInput
    RemoveApplicationInstanceOutput: RemoveApplicationInstanceOutput
    SignalApplicationInstanceNodeInstancesInput: SignalApplicationInstanceNodeInstancesInput
    SignalApplicationInstanceNodeInstancesOutput: SignalApplicationInstanceNodeInstancesOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateDeviceMetadataInput: UpdateDeviceMetadataInput
    UpdateDeviceMetadataOutput: UpdateDeviceMetadataOutput
end

return M
