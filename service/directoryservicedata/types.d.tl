-- Code generated by lua-client-codegen. DO NOT EDIT.

local record AddGroupMemberInput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The name of the group.
    GroupName: string
    -- The SAMAccountName of the user, group, or computer to add as a group member.
    MemberName: string
    -- The domain name that's associated with the group member. This parameter is required only
    -- when adding a member outside of your Managed Microsoft AD domain to a group inside of your
    -- Managed Microsoft AD domain. This parameter defaults to the Managed Microsoft AD domain.
    -- This parameter is case insensitive.
    MemberRealm: string
    -- A unique and case-sensitive identifier that you provide to make sure the idempotency of
    -- the request, so multiple identical calls have the same effect as one single call.
    -- A client token is valid for 8 hours after the first request that uses it completes. After
    -- 8 hours, any request with the same client token is treated as a new request. If the request
    -- succeeds, any future uses of that token will be idempotent for another 8 hours.
    -- If you submit a request with the same client token but change one of the other parameters
    -- within the 8-hour idempotency window, Directory Service Data returns an ConflictException.
    -- This parameter is optional when using the CLI or SDK.
    ClientToken: string
end

local record AddGroupMemberOutput
end

local record CreateGroupInput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The name of the group.
    SAMAccountName: string
    -- The AD group type. For details, see Active Directory security group type.
    GroupType: string
    -- The scope of the AD group. For details, see Active Directory security group scope.
    GroupScope: string
    -- An expression that defines one or more attributes with the data type and value of each
    -- attribute.
    OtherAttributes: {string : AttributeValue}
    -- A unique and case-sensitive identifier that you provide to make sure the idempotency of
    -- the request, so multiple identical calls have the same effect as one single call.
    -- A client token is valid for 8 hours after the first request that uses it completes. After
    -- 8 hours, any request with the same client token is treated as a new request. If the request
    -- succeeds, any future uses of that token will be idempotent for another 8 hours.
    -- If you submit a request with the same client token but change one of the other parameters
    -- within the 8-hour idempotency window, Directory Service Data returns an ConflictException.
    -- This parameter is optional when using the CLI or SDK.
    ClientToken: string
end

local record CreateGroupOutput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The name of the group.
    SAMAccountName: string
    -- The unique security identifier (SID) of the group.
    SID: string
end

local record CreateUserInput
    -- The identifier (ID) of the directory that’s associated with the user.
    DirectoryId: string
    -- The name of the user.
    SAMAccountName: string
    -- The email address of the user.
    EmailAddress: string
    -- The first name of the user.
    GivenName: string
    -- The last name of the user.
    Surname: string
    -- An expression that defines one or more attribute names with the data type and value of
    -- each attribute. A key is an attribute name, and the value is a list of maps. For a list of
    -- supported attributes, see Directory Service Data Attributes.
    -- Attribute names are case insensitive.
    OtherAttributes: {string : AttributeValue}
    -- A unique and case-sensitive identifier that you provide to make sure the idempotency of
    -- the request, so multiple identical calls have the same effect as one single call.
    -- A client token is valid for 8 hours after the first request that uses it completes. After
    -- 8 hours, any request with the same client token is treated as a new request. If the request
    -- succeeds, any future uses of that token will be idempotent for another 8 hours.
    -- If you submit a request with the same client token but change one of the other parameters
    -- within the 8-hour idempotency window, Directory Service Data returns an ConflictException.
    -- This parameter is optional when using the CLI or SDK.
    ClientToken: string
end

local record CreateUserOutput
    -- The identifier (ID) of the directory where the address block is added.
    DirectoryId: string
    -- The unique security identifier (SID) of the user.
    SID: string
    -- The name of the user.
    SAMAccountName: string
end

local record DeleteGroupInput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The name of the group.
    SAMAccountName: string
    -- A unique and case-sensitive identifier that you provide to make sure the idempotency of
    -- the request, so multiple identical calls have the same effect as one single call.
    -- A client token is valid for 8 hours after the first request that uses it completes. After
    -- 8 hours, any request with the same client token is treated as a new request. If the request
    -- succeeds, any future uses of that token will be idempotent for another 8 hours.
    -- If you submit a request with the same client token but change one of the other parameters
    -- within the 8-hour idempotency window, Directory Service Data returns an ConflictException.
    -- This parameter is optional when using the CLI or SDK.
    ClientToken: string
end

local record DeleteGroupOutput
end

local record DeleteUserInput
    -- The identifier (ID) of the directory that's associated with the user.
    DirectoryId: string
    -- The name of the user.
    SAMAccountName: string
    -- A unique and case-sensitive identifier that you provide to make sure the idempotency of
    -- the request, so multiple identical calls have the same effect as one single call.
    -- A client token is valid for 8 hours after the first request that uses it completes. After
    -- 8 hours, any request with the same client token is treated as a new request. If the request
    -- succeeds, any future uses of that token will be idempotent for another 8 hours.
    -- If you submit a request with the same client token but change one of the other parameters
    -- within the 8-hour idempotency window, Directory Service Data returns an ConflictException.
    -- This parameter is optional when using the CLI or SDK.
    ClientToken: string
end

local record DeleteUserOutput
end

local record DescribeGroupInput
    -- The Identifier (ID) of the directory associated with the group.
    DirectoryId: string
    -- The domain name that's associated with the group.
    -- This parameter is optional, so you can return groups outside of your Managed Microsoft AD
    -- domain. When no value is defined, only your Managed Microsoft AD groups are returned.
    -- This value is case insensitive.
    Realm: string
    -- The name of the group.
    SAMAccountName: string
    -- One or more attributes to be returned for the group. For a list of supported attributes,
    -- see Directory Service Data Attributes.
    OtherAttributes: {string}
end

local record DescribeGroupOutput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The domain name that's associated with the group.
    Realm: string
    -- The unique security identifier (SID) of the group.
    SID: string
    -- The name of the group.
    SAMAccountName: string
    -- The distinguished name of the object.
    DistinguishedName: string
    -- The AD group type. For details, see Active Directory security group type.
    GroupType: string
    -- The scope of the AD group. For details, see Active Directory security groups.
    GroupScope: string
    -- The attribute values that are returned for the attribute names that are included in the
    -- request.
    OtherAttributes: {string : AttributeValue}
end

local record DescribeUserInput
    -- The identifier (ID) of the directory that's associated with the user.
    DirectoryId: string
    -- The name of the user.
    SAMAccountName: string
    -- One or more attribute names to be returned for the user. A key is an attribute name, and
    -- the value is a list of maps. For a list of supported attributes, see Directory Service Data Attributes.
    OtherAttributes: {string}
    -- The domain name that's associated with the user.
    -- This parameter is optional, so you can return users outside your Managed Microsoft AD domain.
    -- When no value is defined, only your Managed Microsoft AD users are returned.
    -- This value is case insensitive.
    Realm: string
end

local record DescribeUserOutput
    -- The identifier (ID) of the directory that's associated with the user.
    DirectoryId: string
    -- The domain name that's associated with the user.
    Realm: string
    -- The unique security identifier (SID) of the user.
    SID: string
    -- The name of the user.
    SAMAccountName: string
    -- The distinguished name of the object.
    DistinguishedName: string
    -- The UPN that is an Internet-style login name for a user and is based on the Internet
    -- standard RFC 822. The UPN is shorter
    -- than the distinguished name and easier to remember.
    UserPrincipalName: string
    -- The email address of the user.
    EmailAddress: string
    -- The first name of the user.
    GivenName: string
    -- The last name of the user.
    Surname: string
    -- Indicates whether the user account is active.
    Enabled: boolean
    -- The attribute values that are returned for the attribute names that are included in the
    -- request.
    -- Attribute names are case insensitive.
    OtherAttributes: {string : AttributeValue}
end

local record DisableUserInput
    -- The identifier (ID) of the directory that's associated with the user.
    DirectoryId: string
    -- The name of the user.
    SAMAccountName: string
    -- A unique and case-sensitive identifier that you provide to make sure the idempotency of
    -- the request, so multiple identical calls have the same effect as one single call.
    -- A client token is valid for 8 hours after the first request that uses it completes. After
    -- 8 hours, any request with the same client token is treated as a new request. If the request
    -- succeeds, any future uses of that token will be idempotent for another 8 hours.
    -- If you submit a request with the same client token but change one of the other parameters
    -- within the 8-hour idempotency window, Directory Service Data returns an ConflictException.
    -- This parameter is optional when using the CLI or SDK.
    ClientToken: string
end

local record DisableUserOutput
end

local record ListGroupMembersInput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The domain name that's associated with the group.
    -- This parameter is optional, so you can return members from a group outside of your
    -- Managed Microsoft AD domain. When no value is defined, only members of your Managed Microsoft AD groups are
    -- returned.
    -- This value is case insensitive.
    Realm: string
    -- The domain name that's associated with the group member. This parameter defaults to the
    -- Managed Microsoft AD domain.
    -- This parameter is optional and case insensitive.
    MemberRealm: string
    -- The name of the group.
    SAMAccountName: string
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
    -- The maximum number of results to be returned per request.
    MaxResults: number
end

local record ListGroupMembersOutput
    -- Identifier (ID) of the directory associated with the group.
    DirectoryId: string
    -- The domain name that's associated with the group.
    Realm: string
    -- The domain name that's associated with the member.
    MemberRealm: string
    -- The member information that the request returns.
    Members: {Member}
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
end

local record ListGroupsInput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The domain name associated with the directory.
    -- This parameter is optional, so you can return groups outside of your Managed Microsoft AD
    -- domain. When no value is defined, only your Managed Microsoft AD groups are returned.
    -- This value is case insensitive.
    Realm: string
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
    -- The maximum number of results to be returned per request.
    MaxResults: number
end

local record ListGroupsOutput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The domain name associated with the group.
    Realm: string
    -- The group information that the request returns.
    Groups: {GroupSummary}
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
end

local record ListGroupsForMemberInput
    -- The identifier (ID) of the directory that's associated with the member.
    DirectoryId: string
    -- The domain name that's associated with the group.
    -- This parameter is optional, so you can return groups outside of your Managed Microsoft AD
    -- domain. When no value is defined, only your Managed Microsoft AD groups are returned.
    -- This value is case insensitive and defaults to your Managed Microsoft AD domain.
    Realm: string
    -- The domain name that's associated with the group member.
    -- This parameter is optional, so you can limit your results to the group members in a
    -- specific domain.
    -- This parameter is case insensitive and defaults to Realm
    MemberRealm: string
    -- The SAMAccountName of the user, group, or computer that's a member of the
    -- group.
    SAMAccountName: string
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
    -- The maximum number of results to be returned per request.
    MaxResults: number
end

local record ListGroupsForMemberOutput
    -- The identifier (ID) of the directory that's associated with the member.
    DirectoryId: string
    -- The domain that's associated with the group.
    Realm: string
    -- The domain that's associated with the member.
    MemberRealm: string
    -- The group information that the request returns.
    Groups: {GroupSummary}
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
end

local record ListUsersInput
    -- The identifier (ID) of the directory that's associated with the user.
    DirectoryId: string
    -- The domain name that's associated with the user.
    -- This parameter is optional, so you can return users outside of your Managed Microsoft AD
    -- domain. When no value is defined, only your Managed Microsoft AD users are returned.
    -- This value is case insensitive.
    Realm: string
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
    -- The maximum number of results to be returned per request.
    MaxResults: number
end

local record ListUsersOutput
    -- The identifier (ID) of the directory that's associated with the user.
    DirectoryId: string
    -- The domain that's associated with the user.
    Realm: string
    -- The user information that the request returns.
    Users: {UserSummary}
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
end

local record RemoveGroupMemberInput
    -- The identifier (ID) of the directory that's associated with the member.
    DirectoryId: string
    -- The name of the group.
    GroupName: string
    -- The SAMAccountName of the user, group, or computer to remove from the group.
    MemberName: string
    -- The domain name that's associated with the group member. This parameter defaults to the
    -- Managed Microsoft AD domain.
    -- This parameter is optional and case insensitive.
    MemberRealm: string
    -- A unique and case-sensitive identifier that you provide to make sure the idempotency of
    -- the request, so multiple identical calls have the same effect as one single call.
    -- A client token is valid for 8 hours after the first request that uses it completes. After
    -- 8 hours, any request with the same client token is treated as a new request. If the request
    -- succeeds, any future uses of that token will be idempotent for another 8 hours.
    -- If you submit a request with the same client token but change one of the other parameters
    -- within the 8-hour idempotency window, Directory Service Data returns an ConflictException.
    -- This parameter is optional when using the CLI or SDK.
    ClientToken: string
end

local record RemoveGroupMemberOutput
end

local record SearchGroupsInput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The attribute value that you want to search for.
    -- Wildcard (*) searches aren't supported. For a list of supported
    -- attributes, see Directory Service Data
    -- Attributes.
    SearchString: string
    -- One or more data attributes that are used to search for a group. For a list of supported
    -- attributes, see Directory Service Data Attributes.
    SearchAttributes: {string}
    -- The domain name that's associated with the group.
    -- This parameter is optional, so you can return groups outside of your Managed Microsoft AD
    -- domain. When no value is defined, only your Managed Microsoft AD groups are returned.
    -- This value is case insensitive.
    Realm: string
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
    -- The maximum number of results to be returned per request.
    MaxResults: number
end

local record SearchGroupsOutput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The domain that's associated with the group.
    Realm: string
    -- The group information that the request returns.
    Groups: {Group}
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
end

local record SearchUsersInput
    -- The identifier (ID) of the directory that's associated with the user.
    DirectoryId: string
    -- The domain name that's associated with the user.
    -- This parameter is optional, so you can return users outside of your Managed Microsoft AD
    -- domain. When no value is defined, only your Managed Microsoft AD users are returned.
    -- This value is case insensitive.
    Realm: string
    -- The attribute value that you want to search for.
    -- Wildcard (*) searches aren't supported. For a list of supported
    -- attributes, see Directory Service Data
    -- Attributes.
    SearchString: string
    -- One or more data attributes that are used to search for a user. For a list of supported
    -- attributes, see Directory Service Data Attributes.
    SearchAttributes: {string}
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
    -- The maximum number of results to be returned per request.
    MaxResults: number
end

local record SearchUsersOutput
    -- The identifier (ID) of the directory where the address block is added.
    DirectoryId: string
    -- The domain that's associated with the user.
    Realm: string
    -- The user information that the request returns.
    Users: {User}
    -- An encoded paging token for paginated calls that can be passed back to retrieve the next
    -- page.
    NextToken: string
end

local record UpdateGroupInput
    -- The identifier (ID) of the directory that's associated with the group.
    DirectoryId: string
    -- The name of the group.
    SAMAccountName: string
    -- The AD group type. For details, see Active Directory security group type.
    GroupType: string
    -- The scope of the AD group. For details, see Active Directory security groups.
    GroupScope: string
    -- An expression that defines one or more attributes with the data type and the value of
    -- each attribute.
    OtherAttributes: {string : AttributeValue}
    -- The type of update to be performed. If no value exists for the attribute, use
    -- ADD. Otherwise, use REPLACE to change an attribute value or
    -- REMOVE to clear the attribute value.
    UpdateType: string
    -- A unique and case-sensitive identifier that you provide to make sure the idempotency of
    -- the request, so multiple identical calls have the same effect as one single call.
    -- A client token is valid for 8 hours after the first request that uses it completes. After
    -- 8 hours, any request with the same client token is treated as a new request. If the request
    -- succeeds, any future uses of that token will be idempotent for another 8 hours.
    -- If you submit a request with the same client token but change one of the other parameters
    -- within the 8-hour idempotency window, Directory Service Data returns an ConflictException.
    -- This parameter is optional when using the CLI or SDK.
    ClientToken: string
end

local record UpdateGroupOutput
end

local record UpdateUserInput
    -- The identifier (ID) of the directory that's associated with the user.
    DirectoryId: string
    -- The name of the user.
    SAMAccountName: string
    -- The email address of the user.
    EmailAddress: string
    -- The first name of the user.
    GivenName: string
    -- The last name of the user.
    Surname: string
    -- An expression that defines one or more attribute names with the data type and value of
    -- each attribute. A key is an attribute name, and the value is a list of maps. For a list of
    -- supported attributes, see Directory Service Data Attributes.
    -- Attribute names are case insensitive.
    OtherAttributes: {string : AttributeValue}
    -- The type of update to be performed. If no value exists for the attribute, use
    -- ADD. Otherwise, use REPLACE to change an attribute value or
    -- REMOVE to clear the attribute value.
    UpdateType: string
    -- A unique and case-sensitive identifier that you provide to make sure the idempotency of
    -- the request, so multiple identical calls have the same effect as one single call.
    -- A client token is valid for 8 hours after the first request that uses it completes. After
    -- 8 hours, any request with the same client token is treated as a new request. If the request
    -- succeeds, any future uses of that token will be idempotent for another 8 hours.
    -- If you submit a request with the same client token but change one of the other parameters
    -- within the 8-hour idempotency window, Directory Service Data returns an ConflictException.
    -- This parameter is optional when using the CLI or SDK.
    ClientToken: string
end

local record UpdateUserOutput
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- This error will occur when you try to create a resource that conflicts with an existing
-- object. It can also occur when adding a member to a group that the member is already
-- in.
-- This error can be caused by a request sent within the 8-hour idempotency window with the
-- same client token but different input parameters. Client tokens should not be re-used across
-- different requests. After 8 hours, any request with the same client token is treated as a new
-- request.
local record ConflictException
    Message: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- This error will occur when you try to create a resource that conflicts with an existing
-- object. It can also occur when adding a member to a group that the member is already
-- in.
-- This error can be caused by a request sent within the 8-hour idempotency window with the
-- same client token but different input parameters. Client tokens should not be re-used across
-- different requests. After 8 hours, any request with the same client token is treated as a new
-- request.
local record ConflictException
    Message: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- This error will occur when you try to create a resource that conflicts with an existing
-- object. It can also occur when adding a member to a group that the member is already
-- in.
-- This error can be caused by a request sent within the 8-hour idempotency window with the
-- same client token but different input parameters. Client tokens should not be re-used across
-- different requests. After 8 hours, any request with the same client token is treated as a new
-- request.
local record ConflictException
    Message: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- This error will occur when you try to create a resource that conflicts with an existing
-- object. It can also occur when adding a member to a group that the member is already
-- in.
-- This error can be caused by a request sent within the 8-hour idempotency window with the
-- same client token but different input parameters. Client tokens should not be re-used across
-- different requests. After 8 hours, any request with the same client token is treated as a new
-- request.
local record ConflictException
    Message: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- This error will occur when you try to create a resource that conflicts with an existing
-- object. It can also occur when adding a member to a group that the member is already
-- in.
-- This error can be caused by a request sent within the 8-hour idempotency window with the
-- same client token but different input parameters. Client tokens should not be re-used across
-- different requests. After 8 hours, any request with the same client token is treated as a new
-- request.
local record ConflictException
    Message: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- This error will occur when you try to create a resource that conflicts with an existing
-- object. It can also occur when adding a member to a group that the member is already
-- in.
-- This error can be caused by a request sent within the 8-hour idempotency window with the
-- same client token but different input parameters. Client tokens should not be re-used across
-- different requests. After 8 hours, any request with the same client token is treated as a new
-- request.
local record ConflictException
    Message: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- This error will occur when you try to create a resource that conflicts with an existing
-- object. It can also occur when adding a member to a group that the member is already
-- in.
-- This error can be caused by a request sent within the 8-hour idempotency window with the
-- same client token but different input parameters. Client tokens should not be re-used across
-- different requests. After 8 hours, any request with the same client token is treated as a new
-- request.
local record ConflictException
    Message: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- This error will occur when you try to create a resource that conflicts with an existing
-- object. It can also occur when adding a member to a group that the member is already
-- in.
-- This error can be caused by a request sent within the 8-hour idempotency window with the
-- same client token but different input parameters. Client tokens should not be re-used across
-- different requests. After 8 hours, any request with the same client token is treated as a new
-- request.
local record ConflictException
    Message: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

-- You don't have permission to perform the request or access the directory. It can also
-- occur when the DirectoryId doesn't exist or the user, member, or group might be
-- outside of your organizational unit (OU).
-- Make sure that you have the authentication and authorization to perform the action.
-- Review the directory information in the request, and make sure that the object isn't outside
-- of your OU.
local record AccessDeniedException
    Message: string
    -- Reason the request was unauthorized.
    Reason: string
end

-- This error will occur when you try to create a resource that conflicts with an existing
-- object. It can also occur when adding a member to a group that the member is already
-- in.
-- This error can be caused by a request sent within the 8-hour idempotency window with the
-- same client token but different input parameters. Client tokens should not be re-used across
-- different requests. After 8 hours, any request with the same client token is treated as a new
-- request.
local record ConflictException
    Message: string
end

-- The request could not be completed due to a problem in the configuration or current state
-- of the specified directory.
local record DirectoryUnavailableException
    Message: string
    -- Reason the request failed for the specified directory.
    Reason: string
end

-- The operation didn't succeed because an internal error occurred. Try again later.
local record InternalServerException
    Message: string
end

-- The resource couldn't be found.
local record ResourceNotFoundException
    Message: string
end

-- The limit on the number of requests per second has been exceeded.
local record ThrottlingException
    Message: string
    -- The recommended amount of seconds to retry after a throttling exception.
    RetryAfterSeconds: number
end

-- The request isn't valid. Review the details in the error message to update the invalid
-- parameters or values in your request.
local record ValidationException
    Message: string
    -- Reason the request failed validation.
    Reason: string
end

local type GroupScope = "DomainLocal" | "Global" | "Universal" | "BuiltinLocal"

local type GroupType = "Distribution" | "Security"

local type UpdateType = "ADD" | "REPLACE" | "REMOVE"

local record M
    AddGroupMemberInput: AddGroupMemberInput
    AddGroupMemberOutput: AddGroupMemberOutput
    CreateGroupInput: CreateGroupInput
    CreateGroupOutput: CreateGroupOutput
    CreateUserInput: CreateUserInput
    CreateUserOutput: CreateUserOutput
    DeleteGroupInput: DeleteGroupInput
    DeleteGroupOutput: DeleteGroupOutput
    DeleteUserInput: DeleteUserInput
    DeleteUserOutput: DeleteUserOutput
    DescribeGroupInput: DescribeGroupInput
    DescribeGroupOutput: DescribeGroupOutput
    DescribeUserInput: DescribeUserInput
    DescribeUserOutput: DescribeUserOutput
    DisableUserInput: DisableUserInput
    DisableUserOutput: DisableUserOutput
    ListGroupMembersInput: ListGroupMembersInput
    ListGroupMembersOutput: ListGroupMembersOutput
    ListGroupsInput: ListGroupsInput
    ListGroupsOutput: ListGroupsOutput
    ListGroupsForMemberInput: ListGroupsForMemberInput
    ListGroupsForMemberOutput: ListGroupsForMemberOutput
    ListUsersInput: ListUsersInput
    ListUsersOutput: ListUsersOutput
    RemoveGroupMemberInput: RemoveGroupMemberInput
    RemoveGroupMemberOutput: RemoveGroupMemberOutput
    SearchGroupsInput: SearchGroupsInput
    SearchGroupsOutput: SearchGroupsOutput
    SearchUsersInput: SearchUsersInput
    SearchUsersOutput: SearchUsersOutput
    UpdateGroupInput: UpdateGroupInput
    UpdateGroupOutput: UpdateGroupOutput
    UpdateUserInput: UpdateUserInput
    UpdateUserOutput: UpdateUserOutput
end

return M
