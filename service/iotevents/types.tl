-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record CreateAlarmModelInput
        -- A unique name that helps you identify the alarm model. You can't change this name after
        -- you create the alarm model.
        alarmModelName: string
        -- A description that tells you what the alarm model detects.
        alarmModelDescription: string
        -- The ARN of the IAM role that allows the alarm to perform actions and access AWS resources. For more information, see Amazon Resource Names (ARNs) in the AWS General Reference.
        roleArn: string
        -- A list of key-value pairs that contain metadata for the alarm model. The tags help you
        -- manage the alarm model. For more information, see Tagging your AWS IoT Events
        -- resources in the AWS IoT Events Developer Guide.
        -- You can create up to 50 tags for one alarm model.
        tags: {Tag}
        -- An input attribute used as a key to create an alarm.
        -- AWS IoT Events routes inputs
        -- associated with this key to the alarm.
        key: string
        -- A non-negative integer that reflects the severity level of the alarm.
        severity: number
        -- Defines when your alarm is invoked.
        alarmRule: AlarmRule
        -- Contains information about one or more notification actions.
        alarmNotification: AlarmNotification
        -- Contains information about one or more alarm actions.
        alarmEventActions: AlarmEventActions
        -- Contains the configuration information of alarm state changes.
        alarmCapabilities: AlarmCapabilities
    end

    record CreateAlarmModelOutput
        -- The time the alarm model was created, in the Unix epoch format.
        creationTime: number
        -- The ARN of the alarm model. For more information, see Amazon Resource Names (ARNs) in the AWS General Reference.
        alarmModelArn: string
        -- The version of the alarm model.
        alarmModelVersion: string
        -- The time the alarm model was last updated, in the Unix epoch format.
        lastUpdateTime: number
        -- The status of the alarm model. The status can be one of the following values:
        -- ACTIVE - The alarm model is active and it's ready to evaluate data.
        -- ACTIVATING - AWS IoT Events is activating your alarm model.
        -- Activating an alarm model can take up to a few minutes.
        -- INACTIVE - The alarm model is inactive, so it isn't ready to evaluate data.
        -- Check your alarm model information and update the alarm model.
        -- FAILED - You couldn't create or update the alarm model. Check your alarm model information
        -- and try again.
        status: string
    end

    record CreateDetectorModelInput
        -- The name of the detector model.
        detectorModelName: string
        -- Information that defines how the detectors operate.
        detectorModelDefinition: DetectorModelDefinition
        -- A brief description of the detector model.
        detectorModelDescription: string
        -- The input attribute key used to identify a device or system to create a detector (an
        -- instance of the detector model) and then to route each input received to the appropriate
        -- detector (instance). This parameter uses a JSON-path expression in the message payload of each
        -- input to specify the attribute-value pair that is used to identify the device associated with
        -- the input.
        key: string
        -- The ARN of the role that grants permission to AWS IoT Events to perform its operations.
        roleArn: string
        -- Metadata that can be used to manage the detector model.
        tags: {Tag}
        -- Information about the order in which events are evaluated and how actions are executed.
        evaluationMethod: string
    end

    record CreateDetectorModelOutput
        -- Information about how the detector model is configured.
        detectorModelConfiguration: DetectorModelConfiguration
    end

    record CreateInputInput
        -- The name you want to give to the input.
        inputName: string
        -- A brief description of the input.
        inputDescription: string
        -- The definition of the input.
        inputDefinition: InputDefinition
        -- Metadata that can be used to manage the input.
        tags: {Tag}
    end

    record CreateInputOutput
        -- Information about the configuration of the input.
        inputConfiguration: InputConfiguration
    end

    record DeleteAlarmModelInput
        -- The name of the alarm model.
        alarmModelName: string
    end

    record DeleteAlarmModelOutput
    end

    record DeleteDetectorModelInput
        -- The name of the detector model to be deleted.
        detectorModelName: string
    end

    record DeleteDetectorModelOutput
    end

    record DeleteInputInput
        -- The name of the input to delete.
        inputName: string
    end

    record DeleteInputOutput
    end

    record DescribeAlarmModelInput
        -- The name of the alarm model.
        alarmModelName: string
        -- The version of the alarm model.
        alarmModelVersion: string
    end

    record DescribeAlarmModelOutput
        -- The time the alarm model was created, in the Unix epoch format.
        creationTime: number
        -- The ARN of the alarm model. For more information, see Amazon Resource Names (ARNs) in the AWS General Reference.
        alarmModelArn: string
        -- The version of the alarm model.
        alarmModelVersion: string
        -- The time the alarm model was last updated, in the Unix epoch format.
        lastUpdateTime: number
        -- The status of the alarm model. The status can be one of the following values:
        -- ACTIVE - The alarm model is active and it's ready to evaluate data.
        -- ACTIVATING - AWS IoT Events is activating your alarm model.
        -- Activating an alarm model can take up to a few minutes.
        -- INACTIVE - The alarm model is inactive, so it isn't ready to evaluate data.
        -- Check your alarm model information and update the alarm model.
        -- FAILED - You couldn't create or update the alarm model. Check your alarm model information
        -- and try again.
        status: string
        -- Contains information about the status of the alarm model.
        statusMessage: string
        -- The name of the alarm model.
        alarmModelName: string
        -- The description of the alarm model.
        alarmModelDescription: string
        -- The ARN of the IAM role that allows the alarm to perform actions and access AWS resources. For more information, see Amazon Resource Names (ARNs) in the AWS General Reference.
        roleArn: string
        -- An input attribute used as a key to create an alarm.
        -- AWS IoT Events routes inputs
        -- associated with this key to the alarm.
        key: string
        -- A non-negative integer that reflects the severity level of the alarm.
        severity: number
        -- Defines when your alarm is invoked.
        alarmRule: AlarmRule
        -- Contains information about one or more notification actions.
        alarmNotification: AlarmNotification
        -- Contains information about one or more alarm actions.
        alarmEventActions: AlarmEventActions
        -- Contains the configuration information of alarm state changes.
        alarmCapabilities: AlarmCapabilities
    end

    record DescribeDetectorModelInput
        -- The name of the detector model.
        detectorModelName: string
        -- The version of the detector model.
        detectorModelVersion: string
    end

    record DescribeDetectorModelOutput
        -- Information about the detector model.
        detectorModel: DetectorModel
    end

    record DescribeDetectorModelAnalysisInput
        -- The ID of the analysis result that you want to retrieve.
        analysisId: string
    end

    record DescribeDetectorModelAnalysisOutput
        -- The status of the analysis activity. The status can be one of the following values:
        -- RUNNING - AWS IoT Events is analyzing your detector model. This process can take
        -- several minutes to complete.
        -- COMPLETE - AWS IoT Events finished analyzing your detector model.
        -- FAILED - AWS IoT Events couldn't analyze your detector model. Try again
        -- later.
        status: string
    end

    record DescribeInputInput
        -- The name of the input.
        inputName: string
    end

    record DescribeInputOutput
        -- Information about the input.
        input: Input
    end

    record DescribeLoggingOptionsInput
    end

    record DescribeLoggingOptionsOutput
        -- The current settings of the AWS IoT Events logging options.
        loggingOptions: LoggingOptions
    end

    record GetDetectorModelAnalysisResultsInput
        -- The ID of the analysis result that you want to retrieve.
        analysisId: string
        -- The token that you can use to return the next set of results.
        nextToken: string
        -- The maximum number of results to be returned per request.
        maxResults: number
    end

    record GetDetectorModelAnalysisResultsOutput
        -- Contains information about one or more analysis results.
        analysisResults: {AnalysisResult}
        -- The token that you can use to return the next set of results,
        -- or null if there are no more results.
        nextToken: string
    end

    record ListAlarmModelsInput
        -- The token that you can use to return the next set of results.
        nextToken: string
        -- The maximum number of results to be returned per request.
        maxResults: number
    end

    record ListAlarmModelsOutput
        -- A list that summarizes each alarm model.
        alarmModelSummaries: {AlarmModelSummary}
        -- The token that you can use to return the next set of results,
        -- or null if there are no more results.
        nextToken: string
    end

    record ListAlarmModelVersionsInput
        -- The name of the alarm model.
        alarmModelName: string
        -- The token that you can use to return the next set of results.
        nextToken: string
        -- The maximum number of results to be returned per request.
        maxResults: number
    end

    record ListAlarmModelVersionsOutput
        -- A list that summarizes each alarm model version.
        alarmModelVersionSummaries: {AlarmModelVersionSummary}
        -- The token that you can use to return the next set of results,
        -- or null if there are no more results.
        nextToken: string
    end

    record ListDetectorModelsInput
        -- The token that you can use to return the next set of results.
        nextToken: string
        -- The maximum number of results to be returned per request.
        maxResults: number
    end

    record ListDetectorModelsOutput
        -- Summary information about the detector models.
        detectorModelSummaries: {DetectorModelSummary}
        -- The token that you can use to return the next set of results,
        -- or null if there are no more results.
        nextToken: string
    end

    record ListDetectorModelVersionsInput
        -- The name of the detector model whose versions are returned.
        detectorModelName: string
        -- The token that you can use to return the next set of results.
        nextToken: string
        -- The maximum number of results to be returned per request.
        maxResults: number
    end

    record ListDetectorModelVersionsOutput
        -- Summary information about the detector model versions.
        detectorModelVersionSummaries: {DetectorModelVersionSummary}
        -- The token that you can use to return the next set of results,
        -- or null if there are no more results.
        nextToken: string
    end

    record ListInputRoutingsInput
        -- The identifer of the routed input.
        inputIdentifier: InputIdentifier
        -- The maximum number of results to be returned per request.
        maxResults: number
        -- The token that you can use to return the next set of results.
        nextToken: string
    end

    record ListInputRoutingsOutput
        -- Summary information about the routed resources.
        routedResources: {RoutedResource}
        -- The token that you can use to return the next set of results,
        -- or null if there are no more results.
        nextToken: string
    end

    record ListInputsInput
        -- The token that you can use to return the next set of results.
        nextToken: string
        -- The maximum number of results to be returned per request.
        maxResults: number
    end

    record ListInputsOutput
        -- Summary information about the inputs.
        inputSummaries: {InputSummary}
        -- The token that you can use to return the next set of results,
        -- or null if there are no more results.
        nextToken: string
    end

    record ListTagsForResourceInput
        -- The ARN of the resource.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- The list of tags assigned to the resource.
        tags: {Tag}
    end

    record PutLoggingOptionsInput
        -- The new values of the AWS IoT Events logging options.
        loggingOptions: LoggingOptions
    end

    record PutLoggingOptionsOutput
    end

    record StartDetectorModelAnalysisInput
        detectorModelDefinition: DetectorModelDefinition
    end

    record StartDetectorModelAnalysisOutput
        -- The ID that you can use to retrieve the analysis result.
        analysisId: string
    end

    record TagResourceInput
        -- The ARN of the resource.
        resourceArn: string
        -- The new or modified tags for the resource.
        tags: {Tag}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The ARN of the resource.
        resourceArn: string
        -- A list of the keys of the tags to be removed from the resource.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateAlarmModelInput
        -- The name of the alarm model.
        alarmModelName: string
        -- The description of the alarm model.
        alarmModelDescription: string
        -- The ARN of the IAM role that allows the alarm to perform actions and access AWS resources. For more information, see Amazon Resource Names (ARNs) in the AWS General Reference.
        roleArn: string
        -- A non-negative integer that reflects the severity level of the alarm.
        severity: number
        -- Defines when your alarm is invoked.
        alarmRule: AlarmRule
        -- Contains information about one or more notification actions.
        alarmNotification: AlarmNotification
        -- Contains information about one or more alarm actions.
        alarmEventActions: AlarmEventActions
        -- Contains the configuration information of alarm state changes.
        alarmCapabilities: AlarmCapabilities
    end

    record UpdateAlarmModelOutput
        -- The time the alarm model was created, in the Unix epoch format.
        creationTime: number
        -- The ARN of the alarm model. For more information, see Amazon Resource Names (ARNs) in the AWS General Reference.
        alarmModelArn: string
        -- The version of the alarm model.
        alarmModelVersion: string
        -- The time the alarm model was last updated, in the Unix epoch format.
        lastUpdateTime: number
        -- The status of the alarm model. The status can be one of the following values:
        -- ACTIVE - The alarm model is active and it's ready to evaluate data.
        -- ACTIVATING - AWS IoT Events is activating your alarm model.
        -- Activating an alarm model can take up to a few minutes.
        -- INACTIVE - The alarm model is inactive, so it isn't ready to evaluate data.
        -- Check your alarm model information and update the alarm model.
        -- FAILED - You couldn't create or update the alarm model. Check your alarm model information
        -- and try again.
        status: string
    end

    record UpdateDetectorModelInput
        -- The name of the detector model that is updated.
        detectorModelName: string
        -- Information that defines how a detector operates.
        detectorModelDefinition: DetectorModelDefinition
        -- A brief description of the detector model.
        detectorModelDescription: string
        -- The ARN of the role that grants permission to AWS IoT Events to perform its operations.
        roleArn: string
        -- Information about the order in which events are evaluated and how actions are executed.
        evaluationMethod: string
    end

    record UpdateDetectorModelOutput
        -- Information about how the detector model is configured.
        detectorModelConfiguration: DetectorModelConfiguration
    end

    record UpdateInputInput
        -- The name of the input you want to update.
        inputName: string
        -- A brief description of the input.
        inputDescription: string
        -- The definition of the input.
        inputDefinition: InputDefinition
    end

    record UpdateInputOutput
        -- Information about the configuration of the input.
        inputConfiguration: InputConfiguration
    end

    -- An internal failure occurred.
    record InternalFailureException
        -- The message for the exception.
        message: string
    end

    -- The request was invalid.
    record InvalidRequestException
        -- The message for the exception.
        message: string
    end

    -- A limit was exceeded.
    record LimitExceededException
        -- The message for the exception.
        message: string
    end

    -- The resource already exists.
    record ResourceAlreadyExistsException
        -- The message for the exception.
        message: string
        -- The ID of the resource.
        resourceId: string
        -- The ARN of the resource.
        resourceArn: string
    end

    -- The resource is in use.
    record ResourceInUseException
        -- The message for the exception.
        message: string
    end

    -- The service is currently unavailable.
    record ServiceUnavailableException
        -- The message for the exception.
        message: string
    end

    -- The request could not be completed due to throttling.
    record ThrottlingException
        -- The message for the exception.
        message: string
    end

    -- The resource was not found.
    record ResourceNotFoundException
        -- The message for the exception.
        message: string
    end

    -- The requested operation is not supported.
    record UnsupportedOperationException
        -- The message for the exception.
        message: string
    end

    enum AlarmModelVersionStatus
        "ACTIVE"
        "ACTIVATING"
        "INACTIVE"
        "FAILED"
    end

    enum AnalysisStatus
        "RUNNING"
        "COMPLETE"
        "FAILED"
    end

    enum EvaluationMethod
        "BATCH"
        "SERIAL"
    end

end

return M
