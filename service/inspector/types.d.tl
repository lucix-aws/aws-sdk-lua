-- Code generated by lua-client-codegen. DO NOT EDIT.

local record AddAttributesToFindingsInput
    -- The ARNs that specify the findings that you want to assign attributes to.
    findingArns: {string}
    -- The array of attributes that you want to assign to specified findings.
    attributes: {Attribute}
end

local record AddAttributesToFindingsOutput
    -- Attribute details that cannot be described. An error code is provided for each failed
    -- item.
    failedItems: {string : FailedItemDetails}
end

local record CreateAssessmentTargetInput
    -- The user-defined name that identifies the assessment target that you want to create.
    -- The name must be unique within the AWS account.
    assessmentTargetName: string
    -- The ARN that specifies the resource group that is used to create the assessment
    -- target. If resourceGroupArn is not specified, all EC2 instances in the current AWS account
    -- and region are included in the assessment target.
    resourceGroupArn: string
end

local record CreateAssessmentTargetOutput
    -- The ARN that specifies the assessment target that is created.
    assessmentTargetArn: string
end

local record CreateAssessmentTemplateInput
    -- The ARN that specifies the assessment target for which you want to create the
    -- assessment template.
    assessmentTargetArn: string
    -- The user-defined name that identifies the assessment template that you want to
    -- create. You can create several assessment templates for an assessment target. The names of
    -- the assessment templates that correspond to a particular assessment target must be
    -- unique.
    assessmentTemplateName: string
    -- The duration of the assessment run in seconds.
    durationInSeconds: number
    -- The ARNs that specify the rules packages that you want to attach to the assessment
    -- template.
    rulesPackageArns: {string}
    -- The user-defined attributes that are assigned to every finding that is generated by
    -- the assessment run that uses this assessment template. An attribute is a key and value pair
    -- (an Attribute object). Within an assessment template, each key must be
    -- unique.
    userAttributesForFindings: {Attribute}
end

local record CreateAssessmentTemplateOutput
    -- The ARN that specifies the assessment template that is created.
    assessmentTemplateArn: string
end

local record CreateExclusionsPreviewInput
    -- The ARN that specifies the assessment template for which you want to create an
    -- exclusions preview.
    assessmentTemplateArn: string
end

local record CreateExclusionsPreviewOutput
    -- Specifies the unique identifier of the requested exclusions preview. You can use the
    -- unique identifier to retrieve the exclusions preview when running the GetExclusionsPreview
    -- API.
    previewToken: string
end

local record CreateResourceGroupInput
    -- A collection of keys and an array of possible values,
    -- '[{"key":"key1","values":["Value1","Value2"]},{"key":"Key2","values":["Value3"]}]'.
    -- For example,'[{"key":"Name","values":["TestEC2Instance"]}]'.
    resourceGroupTags: {ResourceGroupTag}
end

local record CreateResourceGroupOutput
    -- The ARN that specifies the resource group that is created.
    resourceGroupArn: string
end

local record DeleteAssessmentRunInput
    -- The ARN that specifies the assessment run that you want to delete.
    assessmentRunArn: string
end

local record DeleteAssessmentRunOutput
end

local record DeleteAssessmentTargetInput
    -- The ARN that specifies the assessment target that you want to delete.
    assessmentTargetArn: string
end

local record DeleteAssessmentTargetOutput
end

local record DeleteAssessmentTemplateInput
    -- The ARN that specifies the assessment template that you want to delete.
    assessmentTemplateArn: string
end

local record DeleteAssessmentTemplateOutput
end

local record DescribeAssessmentRunsInput
    -- The ARN that specifies the assessment run that you want to describe.
    assessmentRunArns: {string}
end

local record DescribeAssessmentRunsOutput
    -- Information about the assessment run.
    assessmentRuns: {AssessmentRun}
    -- Assessment run details that cannot be described. An error code is provided for each
    -- failed item.
    failedItems: {string : FailedItemDetails}
end

local record DescribeAssessmentTargetsInput
    -- The ARNs that specifies the assessment targets that you want to describe.
    assessmentTargetArns: {string}
end

local record DescribeAssessmentTargetsOutput
    -- Information about the assessment targets.
    assessmentTargets: {AssessmentTarget}
    -- Assessment target details that cannot be described. An error code is provided for
    -- each failed item.
    failedItems: {string : FailedItemDetails}
end

local record DescribeAssessmentTemplatesInput
    assessmentTemplateArns: {string}
end

local record DescribeAssessmentTemplatesOutput
    -- Information about the assessment templates.
    assessmentTemplates: {AssessmentTemplate}
    -- Assessment template details that cannot be described. An error code is provided for
    -- each failed item.
    failedItems: {string : FailedItemDetails}
end

local record DescribeCrossAccountAccessRoleInput
end

local record DescribeCrossAccountAccessRoleOutput
    -- The ARN that specifies the IAM role that Amazon Inspector uses to access your AWS
    -- account.
    roleArn: string
    -- A Boolean value that specifies whether the IAM role has the necessary policies
    -- attached to enable Amazon Inspector to access your AWS account.
    valid: boolean
    -- The date when the cross-account access role was registered.
    registeredAt: number
end

local record DescribeExclusionsInput
    -- The list of ARNs that specify the exclusions that you want to describe.
    exclusionArns: {string}
    -- The locale into which you want to translate the exclusion's title, description, and
    -- recommendation.
    locale: string
end

local record DescribeExclusionsOutput
    -- Information about the exclusions.
    exclusions: {string : Exclusion}
    -- Exclusion details that cannot be described. An error code is provided for each failed
    -- item.
    failedItems: {string : FailedItemDetails}
end

local record DescribeFindingsInput
    -- The ARN that specifies the finding that you want to describe.
    findingArns: {string}
    -- The locale into which you want to translate a finding description, recommendation,
    -- and the short description that identifies the finding.
    locale: string
end

local record DescribeFindingsOutput
    -- Information about the finding.
    findings: {Finding}
    -- Finding details that cannot be described. An error code is provided for each failed
    -- item.
    failedItems: {string : FailedItemDetails}
end

local record DescribeResourceGroupsInput
    -- The ARN that specifies the resource group that you want to describe.
    resourceGroupArns: {string}
end

local record DescribeResourceGroupsOutput
    -- Information about a resource group.
    resourceGroups: {ResourceGroup}
    -- Resource group details that cannot be described. An error code is provided for each
    -- failed item.
    failedItems: {string : FailedItemDetails}
end

local record DescribeRulesPackagesInput
    -- The ARN that specifies the rules package that you want to describe.
    rulesPackageArns: {string}
    -- The locale that you want to translate a rules package description into.
    locale: string
end

local record DescribeRulesPackagesOutput
    -- Information about the rules package.
    rulesPackages: {RulesPackage}
    -- Rules package details that cannot be described. An error code is provided for each
    -- failed item.
    failedItems: {string : FailedItemDetails}
end

local record GetAssessmentReportInput
    -- The ARN that specifies the assessment run for which you want to generate a
    -- report.
    assessmentRunArn: string
    -- Specifies the file format (html or pdf) of the assessment report that you want to
    -- generate.
    reportFileFormat: string
    -- Specifies the type of the assessment report that you want to generate. There are two
    -- types of assessment reports: a finding report and a full report. For more information, see
    -- Assessment Reports.
    reportType: string
end

local record GetAssessmentReportOutput
    -- Specifies the status of the request to generate an assessment report.
    status: string
    -- Specifies the URL where you can find the generated assessment report. This parameter
    -- is only returned if the report is successfully generated.
    url: string
end

local record GetExclusionsPreviewInput
    -- The ARN that specifies the assessment template for which the exclusions preview was
    -- requested.
    assessmentTemplateArn: string
    -- The unique identifier associated of the exclusions preview.
    previewToken: string
    -- You can use this parameter when paginating results. Set the value of this parameter
    -- to null on your first call to the GetExclusionsPreviewRequest action. Subsequent calls to
    -- the action fill nextToken in the request with the value of nextToken from the previous
    -- response to continue listing data.
    nextToken: string
    -- You can use this parameter to indicate the maximum number of items you want in the
    -- response. The default value is 100. The maximum value is 500.
    maxResults: number
    -- The locale into which you want to translate the exclusion's title, description, and
    -- recommendation.
    locale: string
end

local record GetExclusionsPreviewOutput
    -- Specifies the status of the request to generate an exclusions preview.
    previewStatus: string
    -- Information about the exclusions included in the preview.
    exclusionPreviews: {ExclusionPreview}
    -- When a response is generated, if there is more data to be listed, this parameters is
    -- present in the response and contains the value to use for the nextToken parameter in a
    -- subsequent pagination request. If there is no more data to be listed, this parameter is set
    -- to null.
    nextToken: string
end

local record GetTelemetryMetadataInput
    -- The ARN that specifies the assessment run that has the telemetry data that you want
    -- to obtain.
    assessmentRunArn: string
end

local record GetTelemetryMetadataOutput
    -- Telemetry details.
    telemetryMetadata: {TelemetryMetadata}
end

local record ListAssessmentRunAgentsInput
    -- The ARN that specifies the assessment run whose agents you want to list.
    assessmentRunArn: string
    -- You can use this parameter to specify a subset of data to be included in the action's
    -- response.
    -- For a record to match a filter, all specified filter attributes must match. When
    -- multiple values are specified for a filter attribute, any of the values can
    -- match.
    filter: AgentFilter
    -- You can use this parameter when paginating results. Set the value of this parameter
    -- to null on your first call to the ListAssessmentRunAgents
    -- action. Subsequent calls to the action fill nextToken in
    -- the request with the value of NextToken from the previous
    -- response to continue listing data.
    nextToken: string
    -- You can use this parameter to indicate the maximum number of items that you want in
    -- the response. The default value is 10. The maximum value is 500.
    maxResults: number
end

local record ListAssessmentRunAgentsOutput
    -- A list of ARNs that specifies the agents returned by the action.
    assessmentRunAgents: {AssessmentRunAgent}
    -- When a response is generated, if there is more data to be listed, this parameter is
    -- present in the response and contains the value to use for the nextToken parameter in a subsequent pagination request. If there is no more
    -- data to be listed, this parameter is set to null.
    nextToken: string
end

local record ListAssessmentRunsInput
    -- The ARNs that specify the assessment templates whose assessment runs you want to
    -- list.
    assessmentTemplateArns: {string}
    -- You can use this parameter to specify a subset of data to be included in the action's
    -- response.
    -- For a record to match a filter, all specified filter attributes must match. When
    -- multiple values are specified for a filter attribute, any of the values can
    -- match.
    filter: AssessmentRunFilter
    -- You can use this parameter when paginating results. Set the value of this parameter
    -- to null on your first call to the ListAssessmentRuns
    -- action. Subsequent calls to the action fill nextToken in
    -- the request with the value of NextToken from the previous
    -- response to continue listing data.
    nextToken: string
    -- You can use this parameter to indicate the maximum number of items that you want in
    -- the response. The default value is 10. The maximum value is 500.
    maxResults: number
end

local record ListAssessmentRunsOutput
    -- A list of ARNs that specifies the assessment runs that are returned by the
    -- action.
    assessmentRunArns: {string}
    -- When a response is generated, if there is more data to be listed, this parameter is
    -- present in the response and contains the value to use for the nextToken parameter in a subsequent pagination request. If there is no more
    -- data to be listed, this parameter is set to null.
    nextToken: string
end

local record ListAssessmentTargetsInput
    -- You can use this parameter to specify a subset of data to be included in the action's
    -- response.
    -- For a record to match a filter, all specified filter attributes must match. When
    -- multiple values are specified for a filter attribute, any of the values can
    -- match.
    filter: AssessmentTargetFilter
    -- You can use this parameter when paginating results. Set the value of this parameter
    -- to null on your first call to the ListAssessmentTargets
    -- action. Subsequent calls to the action fill nextToken in
    -- the request with the value of NextToken from the previous
    -- response to continue listing data.
    nextToken: string
    -- You can use this parameter to indicate the maximum number of items you want in the
    -- response. The default value is 10. The maximum value is 500.
    maxResults: number
end

local record ListAssessmentTargetsOutput
    -- A list of ARNs that specifies the assessment targets that are returned by the
    -- action.
    assessmentTargetArns: {string}
    -- When a response is generated, if there is more data to be listed, this parameter is
    -- present in the response and contains the value to use for the nextToken parameter in a subsequent pagination request. If there is no more
    -- data to be listed, this parameter is set to null.
    nextToken: string
end

local record ListAssessmentTemplatesInput
    -- A list of ARNs that specifies the assessment targets whose assessment templates you
    -- want to list.
    assessmentTargetArns: {string}
    -- You can use this parameter to specify a subset of data to be included in the action's
    -- response.
    -- For a record to match a filter, all specified filter attributes must match. When
    -- multiple values are specified for a filter attribute, any of the values can
    -- match.
    filter: AssessmentTemplateFilter
    -- You can use this parameter when paginating results. Set the value of this parameter
    -- to null on your first call to the ListAssessmentTemplates
    -- action. Subsequent calls to the action fill nextToken in
    -- the request with the value of NextToken from the previous
    -- response to continue listing data.
    nextToken: string
    -- You can use this parameter to indicate the maximum number of items you want in the
    -- response. The default value is 10. The maximum value is 500.
    maxResults: number
end

local record ListAssessmentTemplatesOutput
    -- A list of ARNs that specifies the assessment templates returned by the
    -- action.
    assessmentTemplateArns: {string}
    -- When a response is generated, if there is more data to be listed, this parameter is
    -- present in the response and contains the value to use for the nextToken parameter in a subsequent pagination request. If there is no more
    -- data to be listed, this parameter is set to null.
    nextToken: string
end

local record ListEventSubscriptionsInput
    -- The ARN of the assessment template for which you want to list the existing event
    -- subscriptions.
    resourceArn: string
    -- You can use this parameter when paginating results. Set the value of this parameter
    -- to null on your first call to the ListEventSubscriptions
    -- action. Subsequent calls to the action fill nextToken in
    -- the request with the value of NextToken from the previous
    -- response to continue listing data.
    nextToken: string
    -- You can use this parameter to indicate the maximum number of items you want in the
    -- response. The default value is 10. The maximum value is 500.
    maxResults: number
end

local record ListEventSubscriptionsOutput
    -- Details of the returned event subscriptions.
    subscriptions: {Subscription}
    -- When a response is generated, if there is more data to be listed, this parameter is
    -- present in the response and contains the value to use for the nextToken parameter in a subsequent pagination request. If there is no more
    -- data to be listed, this parameter is set to null.
    nextToken: string
end

local record ListExclusionsInput
    -- The ARN of the assessment run that generated the exclusions that you want to
    -- list.
    assessmentRunArn: string
    -- You can use this parameter when paginating results. Set the value of this parameter
    -- to null on your first call to the ListExclusionsRequest action. Subsequent calls to the
    -- action fill nextToken in the request with the value of nextToken from the previous response
    -- to continue listing data.
    nextToken: string
    -- You can use this parameter to indicate the maximum number of items you want in the
    -- response. The default value is 100. The maximum value is 500.
    maxResults: number
end

local record ListExclusionsOutput
    -- A list of exclusions' ARNs returned by the action.
    exclusionArns: {string}
    -- When a response is generated, if there is more data to be listed, this parameters is
    -- present in the response and contains the value to use for the nextToken parameter in a
    -- subsequent pagination request. If there is no more data to be listed, this parameter is set
    -- to null.
    nextToken: string
end

local record ListFindingsInput
    -- The ARNs of the assessment runs that generate the findings that you want to
    -- list.
    assessmentRunArns: {string}
    -- You can use this parameter to specify a subset of data to be included in the action's
    -- response.
    -- For a record to match a filter, all specified filter attributes must match. When
    -- multiple values are specified for a filter attribute, any of the values can
    -- match.
    filter: FindingFilter
    -- You can use this parameter when paginating results. Set the value of this parameter
    -- to null on your first call to the ListFindings action.
    -- Subsequent calls to the action fill nextToken in the
    -- request with the value of NextToken from the previous
    -- response to continue listing data.
    nextToken: string
    -- You can use this parameter to indicate the maximum number of items you want in the
    -- response. The default value is 10. The maximum value is 500.
    maxResults: number
end

local record ListFindingsOutput
    -- A list of ARNs that specifies the findings returned by the action.
    findingArns: {string}
    -- When a response is generated, if there is more data to be listed, this parameter is
    -- present in the response and contains the value to use for the nextToken parameter in a subsequent pagination request. If there is no more
    -- data to be listed, this parameter is set to null.
    nextToken: string
end

local record ListRulesPackagesInput
    -- You can use this parameter when paginating results. Set the value of this parameter
    -- to null on your first call to the ListRulesPackages
    -- action. Subsequent calls to the action fill nextToken in
    -- the request with the value of NextToken from the previous
    -- response to continue listing data.
    nextToken: string
    -- You can use this parameter to indicate the maximum number of items you want in the
    -- response. The default value is 10. The maximum value is 500.
    maxResults: number
end

local record ListRulesPackagesOutput
    -- The list of ARNs that specifies the rules packages returned by the action.
    rulesPackageArns: {string}
    -- When a response is generated, if there is more data to be listed, this parameter is
    -- present in the response and contains the value to use for the nextToken parameter in a subsequent pagination request. If there is no more
    -- data to be listed, this parameter is set to null.
    nextToken: string
end

local record ListTagsForResourceInput
    -- The ARN that specifies the assessment template whose tags you want to list.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- A collection of key and value pairs.
    tags: {Tag}
end

local record PreviewAgentsInput
    -- The ARN of the assessment target whose agents you want to preview.
    previewAgentsArn: string
    -- You can use this parameter when paginating results. Set the value of this parameter
    -- to null on your first call to the PreviewAgents action.
    -- Subsequent calls to the action fill nextToken in the
    -- request with the value of NextToken from the previous
    -- response to continue listing data.
    nextToken: string
    -- You can use this parameter to indicate the maximum number of items you want in the
    -- response. The default value is 10. The maximum value is 500.
    maxResults: number
end

local record PreviewAgentsOutput
    -- The resulting list of agents.
    agentPreviews: {AgentPreview}
    -- When a response is generated, if there is more data to be listed, this parameter is
    -- present in the response and contains the value to use for the nextToken parameter in a subsequent pagination request. If there is no more
    -- data to be listed, this parameter is set to null.
    nextToken: string
end

local record RegisterCrossAccountAccessRoleInput
    -- The ARN of the IAM role that grants Amazon Inspector access to AWS Services needed to
    -- perform security assessments.
    roleArn: string
end

local record RegisterCrossAccountAccessRoleOutput
end

local record RemoveAttributesFromFindingsInput
    -- The ARNs that specify the findings that you want to remove attributes from.
    findingArns: {string}
    -- The array of attribute keys that you want to remove from specified
    -- findings.
    attributeKeys: {string}
end

local record RemoveAttributesFromFindingsOutput
    -- Attributes details that cannot be described. An error code is provided for each
    -- failed item.
    failedItems: {string : FailedItemDetails}
end

local record SetTagsForResourceInput
    -- The ARN of the assessment template that you want to set tags to.
    resourceArn: string
    -- A collection of key and value pairs that you want to set to the assessment
    -- template.
    tags: {Tag}
end

local record SetTagsForResourceOutput
end

local record StartAssessmentRunInput
    -- The ARN of the assessment template of the assessment run that you want to
    -- start.
    assessmentTemplateArn: string
    -- You can specify the name for the assessment run. The name must be unique for the
    -- assessment template whose ARN is used to start the assessment run.
    assessmentRunName: string
end

local record StartAssessmentRunOutput
    -- The ARN of the assessment run that has been started.
    assessmentRunArn: string
end

local record StopAssessmentRunInput
    -- The ARN of the assessment run that you want to stop.
    assessmentRunArn: string
    -- An input option that can be set to either START_EVALUATION or SKIP_EVALUATION.
    -- START_EVALUATION (the default value), stops the AWS agent from collecting data and begins
    -- the results evaluation and the findings generation process. SKIP_EVALUATION cancels the
    -- assessment run immediately, after which no findings are generated.
    stopAction: string
end

local record StopAssessmentRunOutput
end

local record SubscribeToEventInput
    -- The ARN of the assessment template that is used during the event for which you want
    -- to receive SNS notifications.
    resourceArn: string
    -- The event for which you want to receive SNS notifications.
    event: string
    -- The ARN of the SNS topic to which the SNS notifications are sent.
    topicArn: string
end

local record SubscribeToEventOutput
end

local record UnsubscribeFromEventInput
    -- The ARN of the assessment template that is used during the event for which you want
    -- to stop receiving SNS notifications.
    resourceArn: string
    -- The event for which you want to stop receiving SNS notifications.
    event: string
    -- The ARN of the SNS topic to which SNS notifications are sent.
    topicArn: string
end

local record UnsubscribeFromEventOutput
end

local record UpdateAssessmentTargetInput
    -- The ARN of the assessment target that you want to update.
    assessmentTargetArn: string
    -- The name of the assessment target that you want to update.
    assessmentTargetName: string
    -- The ARN of the resource group that is used to specify the new resource group to
    -- associate with the assessment target.
    resourceGroupArn: string
end

local record UpdateAssessmentTargetOutput
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Amazon Inspector cannot assume the cross-account role that it needs to list your EC2
-- instances during the assessment run.
local record InvalidCrossAccountRoleException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it attempted to create resources beyond the current
-- AWS account limits. The error code describes the limit exceeded.
local record LimitExceededException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it attempted to create resources beyond the current
-- AWS account limits. The error code describes the limit exceeded.
local record LimitExceededException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request is rejected. The specified assessment template is currently generating an
-- exclusions preview.
local record PreviewGenerationInProgressException
    message: string
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it attempted to create resources beyond the current
-- AWS account limits. The error code describes the limit exceeded.
local record LimitExceededException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You cannot perform a specified action if an assessment run is currently in
-- progress.
local record AssessmentRunInProgressException
    -- Details of the exception error.
    message: string
    -- The ARNs of the assessment runs that are currently in progress.
    assessmentRunArns: {string}
    -- Boolean value that indicates whether the ARN list of the assessment runs is
    -- truncated.
    assessmentRunArnsTruncated: boolean
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You cannot perform a specified action if an assessment run is currently in
-- progress.
local record AssessmentRunInProgressException
    -- Details of the exception error.
    message: string
    -- The ARNs of the assessment runs that are currently in progress.
    assessmentRunArns: {string}
    -- Boolean value that indicates whether the ARN list of the assessment runs is
    -- truncated.
    assessmentRunArnsTruncated: boolean
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You cannot perform a specified action if an assessment run is currently in
-- progress.
local record AssessmentRunInProgressException
    -- Details of the exception error.
    message: string
    -- The ARNs of the assessment runs that are currently in progress.
    assessmentRunArns: {string}
    -- Boolean value that indicates whether the ARN list of the assessment runs is
    -- truncated.
    assessmentRunArnsTruncated: boolean
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You cannot perform a specified action if an assessment run is currently in
-- progress.
local record AssessmentRunInProgressException
    -- Details of the exception error.
    message: string
    -- The ARNs of the assessment runs that are currently in progress.
    assessmentRunArns: {string}
    -- Boolean value that indicates whether the ARN list of the assessment runs is
    -- truncated.
    assessmentRunArnsTruncated: boolean
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- Used by the GetAssessmentReport API. The request was rejected
-- because you tried to generate a report for an assessment run that existed before reporting
-- was supported in Amazon Inspector. You can only generate reports for assessment runs that
-- took place or will take place after generating reports in Amazon Inspector became
-- available.
local record UnsupportedFeatureException
    message: string
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Amazon Inspector cannot assume the cross-account role that it needs to list your EC2
-- instances during the assessment run.
local record InvalidCrossAccountRoleException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Amazon Inspector cannot assume the cross-account role that it needs to list your EC2
-- instances during the assessment run.
local record InvalidCrossAccountRoleException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- You started an assessment run, but one of the instances is already participating in
-- another assessment run.
local record AgentsAlreadyRunningAssessmentException
    -- Details of the exception error.
    message: string
    agents: {AgentAlreadyRunningAssessment}
    agentsTruncated: boolean
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Amazon Inspector cannot assume the cross-account role that it needs to list your EC2
-- instances during the assessment run.
local record InvalidCrossAccountRoleException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it attempted to create resources beyond the current
-- AWS account limits. The error code describes the limit exceeded.
local record LimitExceededException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it attempted to create resources beyond the current
-- AWS account limits. The error code describes the limit exceeded.
local record LimitExceededException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

-- You do not have required permissions to access the requested resource.
local record AccessDeniedException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- Internal server error.
local record InternalException
    -- Details of the exception error.
    message: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because an invalid or out-of-range value was supplied for an
-- input parameter.
local record InvalidInputException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The request was rejected because it referenced an entity that does not exist. The
-- error code describes the entity.
local record NoSuchEntityException
    -- Details of the exception error.
    message: string
    -- Code that indicates the type of error that is generated.
    errorCode: string
    -- You can immediately retry your request.
    canRetry: boolean
end

-- The serice is temporary unavailable.
local record ServiceTemporarilyUnavailableException
    -- Details of the exception error.
    message: string
    -- You can wait and then retry your request.
    canRetry: boolean
end

local type InspectorEvent = "ASSESSMENT_RUN_STARTED" | "ASSESSMENT_RUN_COMPLETED" | "ASSESSMENT_RUN_STATE_CHANGED" | "FINDING_REPORTED" | "OTHER"

local type Locale = "EN_US"

local type PreviewStatus = "WORK_IN_PROGRESS" | "COMPLETED"

local type ReportFileFormat = "HTML" | "PDF"

local type ReportStatus = "WORK_IN_PROGRESS" | "FAILED" | "COMPLETED"

local type ReportType = "FINDING" | "FULL"

local type StopAction = "START_EVALUATION" | "SKIP_EVALUATION"

local record M
    AddAttributesToFindingsInput: AddAttributesToFindingsInput
    AddAttributesToFindingsOutput: AddAttributesToFindingsOutput
    CreateAssessmentTargetInput: CreateAssessmentTargetInput
    CreateAssessmentTargetOutput: CreateAssessmentTargetOutput
    CreateAssessmentTemplateInput: CreateAssessmentTemplateInput
    CreateAssessmentTemplateOutput: CreateAssessmentTemplateOutput
    CreateExclusionsPreviewInput: CreateExclusionsPreviewInput
    CreateExclusionsPreviewOutput: CreateExclusionsPreviewOutput
    CreateResourceGroupInput: CreateResourceGroupInput
    CreateResourceGroupOutput: CreateResourceGroupOutput
    DeleteAssessmentRunInput: DeleteAssessmentRunInput
    DeleteAssessmentRunOutput: DeleteAssessmentRunOutput
    DeleteAssessmentTargetInput: DeleteAssessmentTargetInput
    DeleteAssessmentTargetOutput: DeleteAssessmentTargetOutput
    DeleteAssessmentTemplateInput: DeleteAssessmentTemplateInput
    DeleteAssessmentTemplateOutput: DeleteAssessmentTemplateOutput
    DescribeAssessmentRunsInput: DescribeAssessmentRunsInput
    DescribeAssessmentRunsOutput: DescribeAssessmentRunsOutput
    DescribeAssessmentTargetsInput: DescribeAssessmentTargetsInput
    DescribeAssessmentTargetsOutput: DescribeAssessmentTargetsOutput
    DescribeAssessmentTemplatesInput: DescribeAssessmentTemplatesInput
    DescribeAssessmentTemplatesOutput: DescribeAssessmentTemplatesOutput
    DescribeCrossAccountAccessRoleInput: DescribeCrossAccountAccessRoleInput
    DescribeCrossAccountAccessRoleOutput: DescribeCrossAccountAccessRoleOutput
    DescribeExclusionsInput: DescribeExclusionsInput
    DescribeExclusionsOutput: DescribeExclusionsOutput
    DescribeFindingsInput: DescribeFindingsInput
    DescribeFindingsOutput: DescribeFindingsOutput
    DescribeResourceGroupsInput: DescribeResourceGroupsInput
    DescribeResourceGroupsOutput: DescribeResourceGroupsOutput
    DescribeRulesPackagesInput: DescribeRulesPackagesInput
    DescribeRulesPackagesOutput: DescribeRulesPackagesOutput
    GetAssessmentReportInput: GetAssessmentReportInput
    GetAssessmentReportOutput: GetAssessmentReportOutput
    GetExclusionsPreviewInput: GetExclusionsPreviewInput
    GetExclusionsPreviewOutput: GetExclusionsPreviewOutput
    GetTelemetryMetadataInput: GetTelemetryMetadataInput
    GetTelemetryMetadataOutput: GetTelemetryMetadataOutput
    ListAssessmentRunAgentsInput: ListAssessmentRunAgentsInput
    ListAssessmentRunAgentsOutput: ListAssessmentRunAgentsOutput
    ListAssessmentRunsInput: ListAssessmentRunsInput
    ListAssessmentRunsOutput: ListAssessmentRunsOutput
    ListAssessmentTargetsInput: ListAssessmentTargetsInput
    ListAssessmentTargetsOutput: ListAssessmentTargetsOutput
    ListAssessmentTemplatesInput: ListAssessmentTemplatesInput
    ListAssessmentTemplatesOutput: ListAssessmentTemplatesOutput
    ListEventSubscriptionsInput: ListEventSubscriptionsInput
    ListEventSubscriptionsOutput: ListEventSubscriptionsOutput
    ListExclusionsInput: ListExclusionsInput
    ListExclusionsOutput: ListExclusionsOutput
    ListFindingsInput: ListFindingsInput
    ListFindingsOutput: ListFindingsOutput
    ListRulesPackagesInput: ListRulesPackagesInput
    ListRulesPackagesOutput: ListRulesPackagesOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    PreviewAgentsInput: PreviewAgentsInput
    PreviewAgentsOutput: PreviewAgentsOutput
    RegisterCrossAccountAccessRoleInput: RegisterCrossAccountAccessRoleInput
    RegisterCrossAccountAccessRoleOutput: RegisterCrossAccountAccessRoleOutput
    RemoveAttributesFromFindingsInput: RemoveAttributesFromFindingsInput
    RemoveAttributesFromFindingsOutput: RemoveAttributesFromFindingsOutput
    SetTagsForResourceInput: SetTagsForResourceInput
    SetTagsForResourceOutput: SetTagsForResourceOutput
    StartAssessmentRunInput: StartAssessmentRunInput
    StartAssessmentRunOutput: StartAssessmentRunOutput
    StopAssessmentRunInput: StopAssessmentRunInput
    StopAssessmentRunOutput: StopAssessmentRunOutput
    SubscribeToEventInput: SubscribeToEventInput
    SubscribeToEventOutput: SubscribeToEventOutput
    UnsubscribeFromEventInput: UnsubscribeFromEventInput
    UnsubscribeFromEventOutput: UnsubscribeFromEventOutput
    UpdateAssessmentTargetInput: UpdateAssessmentTargetInput
    UpdateAssessmentTargetOutput: UpdateAssessmentTargetOutput
end

return M
