-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record AssociateServiceQuotaTemplateInput
    end

    record AssociateServiceQuotaTemplateOutput
    end

    record CreateSupportCaseInput
        -- The ID of the pending quota increase request for which you want to open a Support case.
        RequestId: string
    end

    record CreateSupportCaseOutput
    end

    record DeleteServiceQuotaIncreaseRequestFromTemplateInput
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies the quota identifier. To find the quota code for a specific
        -- quota, use the ListServiceQuotas operation, and look for the
        -- QuotaCode response in the output for the quota you want.
        QuotaCode: string
        -- Specifies the Amazon Web Services Region for which the request was made.
        AwsRegion: string
    end

    record DeleteServiceQuotaIncreaseRequestFromTemplateOutput
    end

    record DisassociateServiceQuotaTemplateInput
    end

    record DisassociateServiceQuotaTemplateOutput
    end

    record GetAssociationForServiceQuotaTemplateInput
    end

    record GetAssociationForServiceQuotaTemplateOutput
        -- The association status. If the status is ASSOCIATED, the quota increase
        -- requests in the template are automatically applied to new Amazon Web Services accounts in your
        -- organization.
        ServiceQuotaTemplateAssociationStatus: string
    end

    record GetAutoManagementConfigurationInput
    end

    record GetAutoManagementConfigurationOutput
        -- Information on the opt-in level for Automatic Management. Only Amazon Web Services account level is supported.
        OptInLevel: string
        -- Information on the opt-in type for Automatic Management. There are two modes: Notify only and Notify and Auto-Adjust. Currently, only
        -- NotifyOnly is available.
        OptInType: string
        -- The User Notifications Amazon Resource Name (ARN) for Automatic Management notifications.
        NotificationArn: string
        -- Status on whether Automatic Management is started or stopped.
        OptInStatus: string
        -- List of Amazon Web Services services excluded from Automatic Management.
        -- You won't be notified of Service Quotas utilization for Amazon Web Services services added to the
        -- Automatic Management exclusion list.
        ExclusionList: {string : {QuotaInfo}}
    end

    record GetAWSDefaultServiceQuotaInput
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies the quota identifier. To find the quota code for a specific
        -- quota, use the ListServiceQuotas operation, and look for the
        -- QuotaCode response in the output for the quota you want.
        QuotaCode: string
    end

    record GetAWSDefaultServiceQuotaOutput
        -- Information about the quota.
        Quota: ServiceQuota
    end

    record GetQuotaUtilizationReportInput
        -- The unique identifier for the quota utilization report. This identifier is returned by
        -- the StartQuotaUtilizationReport operation.
        ReportId: string
        -- A token that indicates the next page of results to retrieve. This token is returned in
        -- the response when there are more results available. Omit this parameter for the first request.
        NextToken: string
        -- The maximum number of results to return per page. The default value is 1,000 and the
        -- maximum allowed value is 1,000.
        MaxResults: number
    end

    record GetQuotaUtilizationReportOutput
        -- The unique identifier for the quota utilization report.
        ReportId: string
        -- The current status of the report generation. Possible values are:
        -- PENDING - The report generation is in progress. Retry this operation
        -- after a few seconds.
        -- IN_PROGRESS - The report is being processed. Continue polling until
        -- the status changes to COMPLETED.
        -- COMPLETED - The report is ready and quota utilization data is available
        -- in the response.
        -- FAILED - The report generation failed. Check the ErrorCode
        -- and ErrorMessage fields for details.
        Status: string
        -- The timestamp when the report was generated, in ISO 8601 format.
        GeneratedAt: number
        -- The total number of quotas included in the report across all pages.
        TotalCount: number
        -- A list of quota utilization records, sorted by utilization percentage in descending order.
        -- Each record includes the quota code, service code, service name, quota name, namespace,
        -- utilization percentage, default value, applied value, and whether the quota is adjustable.
        -- Up to 1,000 records are returned per page.
        Quotas: {QuotaUtilizationInfo}
        -- A token that indicates more results are available. Include this token in the next request
        -- to retrieve the next page of results. If this field is not present, you have retrieved all
        -- available results.
        NextToken: string
        -- An error code indicating the reason for failure when the report status is FAILED.
        -- This field is only present when the status is FAILED.
        ErrorCode: string
        -- A detailed error message describing the failure when the report status is FAILED.
        -- This field is only present when the status is FAILED.
        ErrorMessage: string
    end

    record GetRequestedServiceQuotaChangeInput
        -- Specifies the ID of the quota increase request.
        RequestId: string
    end

    record GetRequestedServiceQuotaChangeOutput
        -- Information about the quota increase request.
        RequestedQuota: RequestedServiceQuotaChange
    end

    record GetServiceQuotaInput
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies the quota identifier. To find the quota code for a specific
        -- quota, use the ListServiceQuotas operation, and look for the
        -- QuotaCode response in the output for the quota you want.
        QuotaCode: string
        -- Specifies the resource with an Amazon Resource Name (ARN).
        ContextId: string
    end

    record GetServiceQuotaOutput
        -- Information about the quota.
        Quota: ServiceQuota
    end

    record GetServiceQuotaIncreaseRequestFromTemplateInput
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies the quota identifier. To find the quota code for a specific
        -- quota, use the ListServiceQuotas operation, and look for the
        -- QuotaCode response in the output for the quota you want.
        QuotaCode: string
        -- Specifies the Amazon Web Services Region for which you made the request.
        AwsRegion: string
    end

    record GetServiceQuotaIncreaseRequestFromTemplateOutput
        -- Information about the quota increase request.
        ServiceQuotaIncreaseRequestInTemplate: ServiceQuotaIncreaseRequestInTemplate
    end

    record ListAWSDefaultServiceQuotasInput
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies a value for receiving additional results after you
        -- receive a NextToken response in a previous request. A NextToken
        -- response indicates that more output is available. Set this parameter to the value of the previous
        -- call's NextToken response to indicate where the output should continue
        -- from.
        NextToken: string
        -- Specifies the maximum number of results that you want included on each
        -- page of the response. If you do not include this parameter, it defaults to a value appropriate
        -- to the operation. If additional items exist beyond those included in the current response, the
        -- NextToken response element is present and has a value (is not null). Include that
        -- value as the NextToken request parameter in the next call to the operation to get
        -- the next part of the results.
        -- An API operation can return fewer results than the maximum even when there are
        -- more results available. You should check NextToken after every operation to ensure
        -- that you receive all of the results.
        MaxResults: number
    end

    record ListAWSDefaultServiceQuotasOutput
        -- If present, indicates that more output is available than is
        -- included in the current response. Use this value in the NextToken request parameter
        -- in a subsequent call to the operation to get the next part of the output. You should repeat this
        -- until the NextToken response element comes back as null.
        NextToken: string
        -- Information about the quotas.
        Quotas: {ServiceQuota}
    end

    record ListRequestedServiceQuotaChangeHistoryInput
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies that you want to filter the results to only the requests with the matching
        -- status.
        Status: string
        -- Specifies a value for receiving additional results after you
        -- receive a NextToken response in a previous request. A NextToken
        -- response indicates that more output is available. Set this parameter to the value of the previous
        -- call's NextToken response to indicate where the output should continue
        -- from.
        NextToken: string
        -- Specifies the maximum number of results that you want included on each
        -- page of the response. If you do not include this parameter, it defaults to a value appropriate
        -- to the operation. If additional items exist beyond those included in the current response, the
        -- NextToken response element is present and has a value (is not null). Include that
        -- value as the NextToken request parameter in the next call to the operation to get
        -- the next part of the results.
        -- An API operation can return fewer results than the maximum even when there are
        -- more results available. You should check NextToken after every operation to ensure
        -- that you receive all of the results.
        MaxResults: number
        -- Filters the response to return quota requests for the ACCOUNT, RESOURCE, or ALL levels. ACCOUNT is the default.
        QuotaRequestedAtLevel: string
    end

    record ListRequestedServiceQuotaChangeHistoryOutput
        -- If present, indicates that more output is available than is
        -- included in the current response. Use this value in the NextToken request parameter
        -- in a subsequent call to the operation to get the next part of the output. You should repeat this
        -- until the NextToken response element comes back as null.
        NextToken: string
        -- Information about the quota increase requests.
        RequestedQuotas: {RequestedServiceQuotaChange}
    end

    record ListRequestedServiceQuotaChangeHistoryByQuotaInput
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies the quota identifier. To find the quota code for a specific
        -- quota, use the ListServiceQuotas operation, and look for the
        -- QuotaCode response in the output for the quota you want.
        QuotaCode: string
        -- Specifies that you want to filter the results to only the requests with the matching
        -- status.
        Status: string
        -- Specifies a value for receiving additional results after you
        -- receive a NextToken response in a previous request. A NextToken
        -- response indicates that more output is available. Set this parameter to the value of the previous
        -- call's NextToken response to indicate where the output should continue
        -- from.
        NextToken: string
        -- Specifies the maximum number of results that you want included on each
        -- page of the response. If you do not include this parameter, it defaults to a value appropriate
        -- to the operation. If additional items exist beyond those included in the current response, the
        -- NextToken response element is present and has a value (is not null). Include that
        -- value as the NextToken request parameter in the next call to the operation to get
        -- the next part of the results.
        -- An API operation can return fewer results than the maximum even when there are
        -- more results available. You should check NextToken after every operation to ensure
        -- that you receive all of the results.
        MaxResults: number
        -- Filters the response to return quota requests for the ACCOUNT, RESOURCE, or ALL levels. ACCOUNT is the default.
        QuotaRequestedAtLevel: string
    end

    record ListRequestedServiceQuotaChangeHistoryByQuotaOutput
        -- If present, indicates that more output is available than is
        -- included in the current response. Use this value in the NextToken request parameter
        -- in a subsequent call to the operation to get the next part of the output. You should repeat this
        -- until the NextToken response element comes back as null.
        NextToken: string
        -- Information about the quota increase requests.
        RequestedQuotas: {RequestedServiceQuotaChange}
    end

    record ListServiceQuotaIncreaseRequestsInTemplateInput
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies the Amazon Web Services Region for which you made the request.
        AwsRegion: string
        -- Specifies a value for receiving additional results after you
        -- receive a NextToken response in a previous request. A NextToken
        -- response indicates that more output is available. Set this parameter to the value of the previous
        -- call's NextToken response to indicate where the output should continue
        -- from.
        NextToken: string
        -- Specifies the maximum number of results that you want included on each
        -- page of the response. If you do not include this parameter, it defaults to a value appropriate
        -- to the operation. If additional items exist beyond those included in the current response, the
        -- NextToken response element is present and has a value (is not null). Include that
        -- value as the NextToken request parameter in the next call to the operation to get
        -- the next part of the results.
        -- An API operation can return fewer results than the maximum even when there are
        -- more results available. You should check NextToken after every operation to ensure
        -- that you receive all of the results.
        MaxResults: number
    end

    record ListServiceQuotaIncreaseRequestsInTemplateOutput
        -- Information about the quota increase requests.
        ServiceQuotaIncreaseRequestInTemplateList: {ServiceQuotaIncreaseRequestInTemplate}
        -- If present, indicates that more output is available than is
        -- included in the current response. Use this value in the NextToken request parameter
        -- in a subsequent call to the operation to get the next part of the output. You should repeat this
        -- until the NextToken response element comes back as null.
        NextToken: string
    end

    record ListServiceQuotasInput
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies a value for receiving additional results after you
        -- receive a NextToken response in a previous request. A NextToken
        -- response indicates that more output is available. Set this parameter to the value of the previous
        -- call's NextToken response to indicate where the output should continue
        -- from.
        NextToken: string
        -- Specifies the maximum number of results that you want included on each
        -- page of the response. If you do not include this parameter, it defaults to a value appropriate
        -- to the operation. If additional items exist beyond those included in the current response, the
        -- NextToken response element is present and has a value (is not null). Include that
        -- value as the NextToken request parameter in the next call to the operation to get
        -- the next part of the results.
        -- An API operation can return fewer results than the maximum even when there are
        -- more results available. You should check NextToken after every operation to ensure
        -- that you receive all of the results.
        MaxResults: number
        -- Specifies the quota identifier. To find the quota code for a specific
        -- quota, use the ListServiceQuotas operation, and look for the
        -- QuotaCode response in the output for the quota you want.
        QuotaCode: string
        -- Filters the response to return applied quota values for the ACCOUNT, RESOURCE, or ALL levels. ACCOUNT is the default.
        QuotaAppliedAtLevel: string
    end

    record ListServiceQuotasOutput
        -- If present, indicates that more output is available than is
        -- included in the current response. Use this value in the NextToken request parameter
        -- in a subsequent call to the operation to get the next part of the output. You should repeat this
        -- until the NextToken response element comes back as null.
        NextToken: string
        -- Information about the quotas.
        Quotas: {ServiceQuota}
    end

    record ListServicesInput
        -- Specifies a value for receiving additional results after you
        -- receive a NextToken response in a previous request. A NextToken
        -- response indicates that more output is available. Set this parameter to the value of the previous
        -- call's NextToken response to indicate where the output should continue
        -- from.
        NextToken: string
        -- Specifies the maximum number of results that you want included on each
        -- page of the response. If you do not include this parameter, it defaults to a value appropriate
        -- to the operation. If additional items exist beyond those included in the current response, the
        -- NextToken response element is present and has a value (is not null). Include that
        -- value as the NextToken request parameter in the next call to the operation to get
        -- the next part of the results.
        -- An API operation can return fewer results than the maximum even when there are
        -- more results available. You should check NextToken after every operation to ensure
        -- that you receive all of the results.
        MaxResults: number
    end

    record ListServicesOutput
        -- If present, indicates that more output is available than is
        -- included in the current response. Use this value in the NextToken request parameter
        -- in a subsequent call to the operation to get the next part of the output. You should repeat this
        -- until the NextToken response element comes back as null.
        NextToken: string
        -- The list of the Amazon Web Services service names and service codes.
        Services: {ServiceInfo}
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) for the applied quota for which you want to list tags.
        -- You can get this information by using the Service Quotas console, or by listing the quotas
        -- using the list-service-quotas CLI command or the ListServiceQuotas Amazon Web Services API operation.
        ResourceARN: string
    end

    record ListTagsForResourceOutput
        -- A complex data type that contains zero or more tag elements.
        Tags: {Tag}
    end

    record PutServiceQuotaIncreaseRequestIntoTemplateInput
        -- Specifies the quota identifier. To find the quota code for a specific
        -- quota, use the ListServiceQuotas operation, and look for the
        -- QuotaCode response in the output for the quota you want.
        QuotaCode: string
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies the Amazon Web Services Region to which the template applies.
        AwsRegion: string
        -- Specifies the new, increased value for the quota.
        DesiredValue: number
    end

    record PutServiceQuotaIncreaseRequestIntoTemplateOutput
        -- Information about the quota increase request.
        ServiceQuotaIncreaseRequestInTemplate: ServiceQuotaIncreaseRequestInTemplate
    end

    record RequestServiceQuotaIncreaseInput
        -- Specifies the service identifier. To find the service code value
        -- for an Amazon Web Services service, use the ListServices operation.
        ServiceCode: string
        -- Specifies the quota identifier. To find the quota code for a specific
        -- quota, use the ListServiceQuotas operation, and look for the
        -- QuotaCode response in the output for the quota you want.
        QuotaCode: string
        -- Specifies the new, increased value for the quota.
        DesiredValue: number
        -- Specifies the resource with an Amazon Resource Name (ARN).
        ContextId: string
        -- Specifies if an Amazon Web Services Support case can be opened for the quota increase request. This parameter is optional.
        -- By default, this flag is set to True and Amazon Web Services may create a support case for some quota increase requests.
        -- You can set this flag to False
        -- if you do not want a support case created when you request a quota increase. If you set the flag to False,
        -- Amazon Web Services does not open a support case and updates the request status to Not approved.
        SupportCaseAllowed: boolean
    end

    record RequestServiceQuotaIncreaseOutput
        -- Information about the quota increase request.
        RequestedQuota: RequestedServiceQuotaChange
    end

    record StartAutoManagementInput
        -- Sets the opt-in level for Automatic Management. Only Amazon Web Services account level is supported.
        OptInLevel: string
        -- Sets the opt-in type for Automatic Management. There are two modes: Notify only and Notify and Auto-Adjust. Currently, only
        -- NotifyOnly is available.
        OptInType: string
        -- The User Notifications Amazon Resource Name (ARN) for Automatic Management notifications.
        NotificationArn: string
        -- List of Amazon Web Services services excluded from Automatic Management.
        -- You won't be notified of Service Quotas utilization for Amazon Web Services services added to the
        -- Automatic Management exclusion list.
        ExclusionList: {string : {string}}
    end

    record StartAutoManagementOutput
    end

    record StartQuotaUtilizationReportInput
    end

    record StartQuotaUtilizationReportOutput
        -- A unique identifier for the quota utilization report. Use this identifier with the
        -- GetQuotaUtilizationReport operation to retrieve the report results.
        ReportId: string
        -- The current status of the report generation. The status will be PENDING
        -- when the report is first initiated.
        Status: string
        -- An optional message providing additional information about the report generation status.
        -- This field may contain details about the report initiation or indicate if an existing recent
        -- report is being reused.
        Message: string
    end

    record StopAutoManagementInput
    end

    record StopAutoManagementOutput
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) for the applied quota. You can get this information by
        -- using the Service Quotas console, or by listing the quotas using the list-service-quotas CLI command or the ListServiceQuotas Amazon Web Services API operation.
        ResourceARN: string
        -- The tags that you want to add to the resource.
        Tags: {Tag}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) for the applied quota that you want to untag. You can
        -- get this information by using the Service Quotas console, or by listing the quotas using the
        -- list-service-quotas CLI command or the ListServiceQuotas Amazon Web Services API operation.
        ResourceARN: string
        -- The keys of the tags that you want to remove from the resource.
        TagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateAutoManagementInput
        -- Information on the opt-in type for your Automatic Management configuration.
        -- There are two modes: Notify only and Notify and Auto-Adjust. Currently, only
        -- NotifyOnly is available.
        OptInType: string
        -- The User Notifications Amazon Resource Name (ARN) for Automatic Management notifications you want
        -- to update.
        NotificationArn: string
        -- List of Amazon Web Services services you want to exclude from Automatic Management.
        -- You won't be notified of Service Quotas utilization for Amazon Web Services services added to the
        -- Automatic Management exclusion list.
        ExclusionList: {string : {string}}
    end

    record UpdateAutoManagementOutput
    end

    -- You do not have sufficient permission to perform this action.
    record AccessDeniedException
        Message: string
    end

    -- The action you attempted is not allowed unless Service Access with Service Quotas is enabled in
    -- your organization.
    record AWSServiceAccessNotEnabledException
        Message: string
    end

    -- You can't perform this action because a dependency does not have access.
    record DependencyAccessDeniedException
        Message: string
    end

    -- The Amazon Web Services account making this call is not a member of an organization.
    record NoAvailableOrganizationException
        Message: string
    end

    -- The organization that your Amazon Web Services account belongs to is not in All Features
    -- mode.
    record OrganizationNotInAllFeaturesModeException
        Message: string
    end

    -- Something went wrong.
    record ServiceException
        Message: string
    end

    -- The Service Quotas template is not available in this Amazon Web Services Region.
    record TemplatesNotAvailableInRegionException
        Message: string
    end

    -- Due to throttling, the request was denied. Slow down the rate of request calls, or
    -- request an increase for this quota.
    record TooManyRequestsException
        Message: string
    end

    -- Invalid input was provided.
    record IllegalArgumentException
        Message: string
    end

    -- The resource is in an invalid state.
    record InvalidResourceStateException
        Message: string
    end

    -- The specified resource does not exist.
    record NoSuchResourceException
        Message: string
    end

    -- The specified resource already exists.
    record ResourceAlreadyExistsException
        Message: string
    end

    -- The quota request template is not associated with your organization.
    record ServiceQuotaTemplateNotInUseException
        Message: string
    end

    -- Invalid input was provided.
    record InvalidPaginationTokenException
        Message: string
    end

    -- You have exceeded your service quota. To perform the requested action, remove some of
    -- the relevant resources, or use Service Quotas to request a service quota increase.
    record QuotaExceededException
        Message: string
    end

    -- The specified tag is a reserved word and cannot be used.
    record TagPolicyViolationException
        Message: string
    end

    -- You've exceeded the number of tags allowed for a resource. For more information, see
    -- Tag
    -- restrictions in the Service Quotas User Guide.
    record TooManyTagsException
        Message: string
    end

    enum AppliedLevelEnum
        "ACCOUNT"
        "RESOURCE"
        "ALL"
    end

    enum OptInLevel
        "ACCOUNT"
    end

    enum OptInStatus
        "ENABLED"
        "DISABLED"
    end

    enum OptInType
        "NotifyOnly"
        "NotifyAndAdjust"
    end

    enum ReportStatus
        "PENDING"
        "IN_PROGRESS"
        "COMPLETED"
        "FAILED"
    end

    enum RequestStatus
        "PENDING"
        "CASE_OPENED"
        "APPROVED"
        "DENIED"
        "CASE_CLOSED"
        "NOT_APPROVED"
        "INVALID_REQUEST"
    end

    enum ServiceQuotaTemplateAssociationStatus
        "ASSOCIATED"
        "DISASSOCIATED"
    end

end

return M
