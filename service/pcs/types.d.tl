-- Code generated by lua-client-codegen. DO NOT EDIT.

local record CreateClusterInput
    -- A name to identify the cluster. Example: MyCluster
    clusterName: string
    -- The cluster management and job scheduling software associated with the cluster.
    scheduler: SchedulerRequest
    -- A value that determines the maximum number of compute nodes in the cluster and the maximum number of jobs (active and queued). SMALL: 32 compute nodes and 256 jobs MEDIUM: 512 compute nodes and 8192 jobs LARGE: 2048 compute nodes and 16,384 jobs
    size: string
    -- The networking configuration used to set up the cluster's control plane.
    networking: NetworkingRequest
    -- Additional options related to the Slurm scheduler.
    slurmConfiguration: ClusterSlurmConfigurationRequest
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the CLI and SDK automatically generate 1 for you.
    clientToken: string
    -- 1 or more tags added to the resource. Each tag consists of a tag key and tag value. The tag value is optional and can be an empty string.
    tags: {string : string}
end

local record CreateClusterOutput
    -- The cluster resource.
    cluster: Cluster
end

local record CreateComputeNodeGroupInput
    -- The name or ID of the cluster to create a compute node group in.
    clusterIdentifier: string
    -- A name to identify the cluster. Example: MyCluster
    computeNodeGroupName: string
    -- The ID of the Amazon Machine Image (AMI) that PCS uses to launch compute nodes (Amazon EC2 instances). If you don't provide this value, PCS uses the AMI ID specified in the custom launch template.
    amiId: string
    -- The list of subnet IDs where the compute node group launches instances. Subnets must be in the same VPC as the cluster.
    subnetIds: {string}
    -- Specifies how EC2 instances are purchased on your behalf. PCS supports On-Demand Instances, Spot Instances, and Amazon EC2 Capacity Blocks for ML. For more information, see Amazon EC2 billing and purchasing options in the Amazon Elastic Compute Cloud User Guide. For more information about PCS support for Capacity Blocks, see Using Amazon EC2 Capacity Blocks for ML with PCS in the PCS User Guide. If you don't provide this option, it defaults to On-Demand.
    purchaseOption: string
    customLaunchTemplate: CustomLaunchTemplate
    -- The Amazon Resource Name (ARN) of the IAM instance profile used to pass an IAM role when launching EC2 instances. The role contained in your instance profile must have the pcs:RegisterComputeNodeGroupInstance permission and the role name must start with AWSPCS or must have the path /aws-pcs/. For more information, see IAM instance profiles for PCS in the PCS User Guide.
    iamInstanceProfileArn: string
    -- Specifies the boundaries of the compute node group auto scaling.
    scalingConfiguration: ScalingConfigurationRequest
    -- A list of EC2 instance configurations that PCS can provision in the compute node group.
    instanceConfigs: {InstanceConfig}
    spotOptions: SpotOptions
    -- Additional options related to the Slurm scheduler.
    slurmConfiguration: ComputeNodeGroupSlurmConfigurationRequest
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the CLI and SDK automatically generate 1 for you.
    clientToken: string
    -- 1 or more tags added to the resource. Each tag consists of a tag key and tag value. The tag value is optional and can be an empty string.
    tags: {string : string}
end

local record CreateComputeNodeGroupOutput
    computeNodeGroup: ComputeNodeGroup
end

local record CreateQueueInput
    -- The name or ID of the cluster for which to create a queue.
    clusterIdentifier: string
    -- A name to identify the queue.
    queueName: string
    -- The list of compute node group configurations to associate with the queue. Queues assign jobs to associated compute node groups.
    computeNodeGroupConfigurations: {ComputeNodeGroupConfiguration}
    -- Additional options related to the Slurm scheduler.
    slurmConfiguration: QueueSlurmConfigurationRequest
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the CLI and SDK automatically generate 1 for you.
    clientToken: string
    -- 1 or more tags added to the resource. Each tag consists of a tag key and tag value. The tag value is optional and can be an empty string.
    tags: {string : string}
end

local record CreateQueueOutput
    queue: Queue
end

local record DeleteClusterInput
    -- The name or ID of the cluster to delete.
    clusterIdentifier: string
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the CLI and SDK automatically generate 1 for you.
    clientToken: string
end

local record DeleteClusterOutput
end

local record DeleteComputeNodeGroupInput
    -- The name or ID of the cluster of the compute node group.
    clusterIdentifier: string
    -- The name or ID of the compute node group to delete.
    computeNodeGroupIdentifier: string
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the CLI and SDK automatically generate 1 for you.
    clientToken: string
end

local record DeleteComputeNodeGroupOutput
end

local record DeleteQueueInput
    -- The name or ID of the cluster of the queue.
    clusterIdentifier: string
    -- The name or ID of the queue to delete.
    queueIdentifier: string
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the CLI and SDK automatically generate 1 for you.
    clientToken: string
end

local record DeleteQueueOutput
end

local record GetClusterInput
    -- The name or ID of the cluster.
    clusterIdentifier: string
end

local record GetClusterOutput
    -- The cluster resource.
    cluster: Cluster
end

local record GetComputeNodeGroupInput
    -- The name or ID of the cluster.
    clusterIdentifier: string
    -- The name or ID of the compute node group.
    computeNodeGroupIdentifier: string
end

local record GetComputeNodeGroupOutput
    computeNodeGroup: ComputeNodeGroup
end

local record GetQueueInput
    -- The name or ID of the cluster of the queue.
    clusterIdentifier: string
    -- The name or ID of the queue.
    queueIdentifier: string
end

local record GetQueueOutput
    queue: Queue
end

local record ListClustersInput
    -- The value of nextToken is a unique pagination token for each page of results returned. If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours. Using an expired pagination token returns an HTTP 400 InvalidToken error.
    nextToken: string
    -- The maximum number of results that are returned per call. You can use nextToken to obtain further pages of results. The default is 10 results, and the maximum allowed page size is 100 results. A value of 0 uses the default.
    maxResults: number
end

local record ListClustersOutput
    -- The list of clusters.
    clusters: {ClusterSummary}
    -- The value of nextToken is a unique pagination token for each page of results returned. If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours. Using an expired pagination token returns an HTTP 400 InvalidToken error.
    nextToken: string
end

local record ListComputeNodeGroupsInput
    -- The name or ID of the cluster to list compute node groups for.
    clusterIdentifier: string
    -- The value of nextToken is a unique pagination token for each page of results returned. If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours. Using an expired pagination token returns an HTTP 400 InvalidToken error.
    nextToken: string
    -- The maximum number of results that are returned per call. You can use nextToken to obtain further pages of results. The default is 10 results, and the maximum allowed page size is 100 results. A value of 0 uses the default.
    maxResults: number
end

local record ListComputeNodeGroupsOutput
    -- The list of compute node groups for the cluster.
    computeNodeGroups: {ComputeNodeGroupSummary}
    -- The value of nextToken is a unique pagination token for each page of results returned. If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours. Using an expired pagination token returns an HTTP 400 InvalidToken error.
    nextToken: string
end

local record ListQueuesInput
    -- The name or ID of the cluster to list queues for.
    clusterIdentifier: string
    -- The value of nextToken is a unique pagination token for each page of results returned. If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours. Using an expired pagination token returns an HTTP 400 InvalidToken error.
    nextToken: string
    -- The maximum number of results that are returned per call. You can use nextToken to obtain further pages of results. The default is 10 results, and the maximum allowed page size is 100 results. A value of 0 uses the default.
    maxResults: number
end

local record ListQueuesOutput
    -- The list of queues associated with the cluster.
    queues: {QueueSummary}
    -- The value of nextToken is a unique pagination token for each page of results returned. If nextToken is returned, there are more results available. Make the call again using the returned token to retrieve the next page. Keep all other arguments unchanged. Each pagination token expires after 24 hours. Using an expired pagination token returns an HTTP 400 InvalidToken error.
    nextToken: string
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the resource for which to list tags.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- 1 or more tags added to the resource. Each tag consists of a tag key and tag value. The tag value is optional and can be an empty string.
    tags: {string : string}
end

local record RegisterComputeNodeGroupInstanceInput
    -- The name or ID of the cluster to register the compute node group instance in.
    clusterIdentifier: string
    -- The client-generated token to allow for retries.
    bootstrapId: string
end

local record RegisterComputeNodeGroupInstanceOutput
    -- The scheduler node ID for this instance.
    nodeID: string
    -- For the Slurm scheduler, this is the shared Munge key the scheduler uses to authenticate compute node group instances.
    sharedSecret: string
    -- The list of endpoints available for interaction with the scheduler.
    endpoints: {Endpoint}
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
    -- 1 or more tags added to the resource. Each tag consists of a tag key and tag value. The tag value is optional and can be an empty string.
    tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
    -- 1 or more tag keys to remove from the resource. Specify only tag keys and not tag values.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateClusterInput
    -- The name or ID of the cluster to update.
    clusterIdentifier: string
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the CLI and SDK automatically generate 1 for you.
    clientToken: string
    -- Additional options related to the Slurm scheduler.
    slurmConfiguration: UpdateClusterSlurmConfigurationRequest
end

local record UpdateClusterOutput
    cluster: Cluster
end

local record UpdateComputeNodeGroupInput
    -- The name or ID of the cluster of the compute node group.
    clusterIdentifier: string
    -- The name or ID of the compute node group.
    computeNodeGroupIdentifier: string
    -- The ID of the Amazon Machine Image (AMI) that PCS uses to launch instances. If not provided, PCS uses the AMI ID specified in the custom launch template.
    amiId: string
    -- The list of subnet IDs where the compute node group provisions instances. The subnets must be in the same VPC as the cluster.
    subnetIds: {string}
    customLaunchTemplate: CustomLaunchTemplate
    -- Specifies how EC2 instances are purchased on your behalf. PCS supports On-Demand Instances, Spot Instances, and Amazon EC2 Capacity Blocks for ML. For more information, see Amazon EC2 billing and purchasing options in the Amazon Elastic Compute Cloud User Guide. For more information about PCS support for Capacity Blocks, see Using Amazon EC2 Capacity Blocks for ML with PCS in the PCS User Guide. If you don't provide this option, it defaults to On-Demand.
    purchaseOption: string
    spotOptions: SpotOptions
    -- Specifies the boundaries of the compute node group auto scaling.
    scalingConfiguration: ScalingConfigurationRequest
    -- The Amazon Resource Name (ARN) of the IAM instance profile used to pass an IAM role when launching EC2 instances. The role contained in your instance profile must have the pcs:RegisterComputeNodeGroupInstance permission and the role name must start with AWSPCS or must have the path /aws-pcs/. For more information, see IAM instance profiles for PCS in the PCS User Guide.
    iamInstanceProfileArn: string
    -- Additional options related to the Slurm scheduler.
    slurmConfiguration: UpdateComputeNodeGroupSlurmConfigurationRequest
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the CLI and SDK automatically generate 1 for you.
    clientToken: string
end

local record UpdateComputeNodeGroupOutput
    computeNodeGroup: ComputeNodeGroup
end

local record UpdateQueueInput
    -- The name or ID of the cluster of the queue.
    clusterIdentifier: string
    -- The name or ID of the queue.
    queueIdentifier: string
    -- The list of compute node group configurations to associate with the queue. Queues assign jobs to associated compute node groups.
    computeNodeGroupConfigurations: {ComputeNodeGroupConfiguration}
    -- Additional options related to the Slurm scheduler.
    slurmConfiguration: UpdateQueueSlurmConfigurationRequest
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. Idempotency ensures that an API request completes only once. With an idempotent request, if the original request completes successfully, the subsequent retries with the same client token return the result from the original successful request and they have no additional effect. If you don't specify a client token, the CLI and SDK automatically generate 1 for you.
    clientToken: string
end

local record UpdateQueueOutput
    queue: Queue
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account. To learn how to increase your service quota, see Requesting a quota increase in the Service Quotas User Guide Examples The max number of clusters or queues has been reached for the account. The max number of compute node groups has been reached for the associated cluster. The total of maxInstances across all compute node groups has been reached for associated cluster.
local record ServiceQuotaExceededException
    message: string
    -- The service code associated with the quota that was exceeded.
    serviceCode: string
    -- The unique identifier of the resource that caused the quota to be exceeded.
    resourceId: string
    -- The type or category of the resource that caused the quota to be exceeded.
    resourceType: string
    -- The quota code of the service quota that was exceeded.
    quotaCode: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account. To learn how to increase your service quota, see Requesting a quota increase in the Service Quotas User Guide Examples The max number of clusters or queues has been reached for the account. The max number of compute node groups has been reached for the associated cluster. The total of maxInstances across all compute node groups has been reached for associated cluster.
local record ServiceQuotaExceededException
    message: string
    -- The service code associated with the quota that was exceeded.
    serviceCode: string
    -- The unique identifier of the resource that caused the quota to be exceeded.
    resourceId: string
    -- The type or category of the resource that caused the quota to be exceeded.
    resourceType: string
    -- The quota code of the service quota that was exceeded.
    quotaCode: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account. To learn how to increase your service quota, see Requesting a quota increase in the Service Quotas User Guide Examples The max number of clusters or queues has been reached for the account. The max number of compute node groups has been reached for the associated cluster. The total of maxInstances across all compute node groups has been reached for associated cluster.
local record ServiceQuotaExceededException
    message: string
    -- The service code associated with the quota that was exceeded.
    serviceCode: string
    -- The unique identifier of the resource that caused the quota to be exceeded.
    resourceId: string
    -- The type or category of the resource that caused the quota to be exceeded.
    resourceType: string
    -- The quota code of the service quota that was exceeded.
    quotaCode: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account. To learn how to increase your service quota, see Requesting a quota increase in the Service Quotas User Guide Examples The max number of clusters or queues has been reached for the account. The max number of compute node groups has been reached for the associated cluster. The total of maxInstances across all compute node groups has been reached for associated cluster.
local record ServiceQuotaExceededException
    message: string
    -- The service code associated with the quota that was exceeded.
    serviceCode: string
    -- The unique identifier of the resource that caused the quota to be exceeded.
    resourceId: string
    -- The type or category of the resource that caused the quota to be exceeded.
    resourceType: string
    -- The quota code of the service quota that was exceeded.
    quotaCode: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account. To learn how to increase your service quota, see Requesting a quota increase in the Service Quotas User Guide Examples The max number of clusters or queues has been reached for the account. The max number of compute node groups has been reached for the associated cluster. The total of maxInstances across all compute node groups has been reached for associated cluster.
local record ServiceQuotaExceededException
    message: string
    -- The service code associated with the quota that was exceeded.
    serviceCode: string
    -- The unique identifier of the resource that caused the quota to be exceeded.
    resourceId: string
    -- The type or category of the resource that caused the quota to be exceeded.
    resourceType: string
    -- The quota code of the service quota that was exceeded.
    quotaCode: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have permission to perform the action. Examples The launch template instance profile doesn't pass iam:PassRole verification. There is a mismatch between the account ID and cluster ID. The cluster ID doesn't exist. The EC2 instance isn't present.
local record AccessDeniedException
    message: string
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than 1 operation on the same resource at the same time. Examples A cluster with the same name already exists. A cluster isn't in ACTIVE status. A cluster to delete is in an unstable state. For example, because it still has ACTIVE node groups or queues. A queue already exists in a cluster.
local record ConflictException
    message: string
    -- The unique identifier of the resource that caused the conflict exception.
    resourceId: string
    -- The type or category of the resource that caused the conflict exception."
    resourceType: string
end

-- PCS can't process your request right now. Try again later.
local record InternalServerException
    message: string
end

-- The requested resource can't be found. The cluster, node group, or queue you're attempting to get, update, list, or delete doesn't exist. Examples
local record ResourceNotFoundException
    message: string
    -- The unique identifier of the resource that was not found.
    resourceId: string
    -- The type or category of the resource that was not found.
    resourceType: string
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account. To learn how to increase your service quota, see Requesting a quota increase in the Service Quotas User Guide Examples The max number of clusters or queues has been reached for the account. The max number of compute node groups has been reached for the associated cluster. The total of maxInstances across all compute node groups has been reached for associated cluster.
local record ServiceQuotaExceededException
    message: string
    -- The service code associated with the quota that was exceeded.
    serviceCode: string
    -- The unique identifier of the resource that caused the quota to be exceeded.
    resourceId: string
    -- The type or category of the resource that caused the quota to be exceeded.
    resourceType: string
    -- The quota code of the service quota that was exceeded.
    quotaCode: string
end

-- Your request exceeded a request rate quota. Check the resource's request rate quota and try again.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request.
    retryAfterSeconds: number
end

-- The request isn't valid. Examples Your request contains malformed JSON or unsupported characters. The scheduler version isn't supported. There are networking related errors, such as network validation failure. AMI type is CUSTOM and the launch template doesn't define the AMI ID, or the AMI type is AL2 and the launch template defines the AMI.
local record ValidationException
    message: string
    -- The specific reason or cause of the validation error.
    reason: string
    -- A list of fields or properties that failed validation.
    fieldList: {ValidationExceptionField}
end

local type PurchaseOption = "ONDEMAND" | "SPOT" | "CAPACITY_BLOCK"

local type Size = "SMALL" | "MEDIUM" | "LARGE"

local record M
    CreateClusterInput: CreateClusterInput
    CreateClusterOutput: CreateClusterOutput
    CreateComputeNodeGroupInput: CreateComputeNodeGroupInput
    CreateComputeNodeGroupOutput: CreateComputeNodeGroupOutput
    CreateQueueInput: CreateQueueInput
    CreateQueueOutput: CreateQueueOutput
    DeleteClusterInput: DeleteClusterInput
    DeleteClusterOutput: DeleteClusterOutput
    DeleteComputeNodeGroupInput: DeleteComputeNodeGroupInput
    DeleteComputeNodeGroupOutput: DeleteComputeNodeGroupOutput
    DeleteQueueInput: DeleteQueueInput
    DeleteQueueOutput: DeleteQueueOutput
    GetClusterInput: GetClusterInput
    GetClusterOutput: GetClusterOutput
    GetComputeNodeGroupInput: GetComputeNodeGroupInput
    GetComputeNodeGroupOutput: GetComputeNodeGroupOutput
    GetQueueInput: GetQueueInput
    GetQueueOutput: GetQueueOutput
    ListClustersInput: ListClustersInput
    ListClustersOutput: ListClustersOutput
    ListComputeNodeGroupsInput: ListComputeNodeGroupsInput
    ListComputeNodeGroupsOutput: ListComputeNodeGroupsOutput
    ListQueuesInput: ListQueuesInput
    ListQueuesOutput: ListQueuesOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    RegisterComputeNodeGroupInstanceInput: RegisterComputeNodeGroupInstanceInput
    RegisterComputeNodeGroupInstanceOutput: RegisterComputeNodeGroupInstanceOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateClusterInput: UpdateClusterInput
    UpdateClusterOutput: UpdateClusterOutput
    UpdateComputeNodeGroupInput: UpdateComputeNodeGroupInput
    UpdateComputeNodeGroupOutput: UpdateComputeNodeGroupOutput
    UpdateQueueInput: UpdateQueueInput
    UpdateQueueOutput: UpdateQueueOutput
end

return M
