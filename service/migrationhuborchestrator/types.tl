-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record CreateTemplateInput
        -- The name of the migration workflow template.
        templateName: string
        -- A description of the migration workflow template.
        templateDescription: string
        -- The source of the migration workflow template.
        templateSource: TemplateSource
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the
        -- request. For more information, see Idempotency in the Smithy documentation.
        clientToken: string
        -- The tags to add to the migration workflow template.
        tags: {string : string}
    end

    record CreateTemplateOutput
        -- The ID of the migration workflow template.
        templateId: string
        -- The Amazon Resource Name (ARN) of the migration workflow template. The format for an
        -- Migration Hub Orchestrator template ARN is
        -- arn:aws:migrationhub-orchestrator:region:account:template/template-abcd1234.
        -- For more information about ARNs, see Amazon Resource Names (ARNs)
        -- in the AWS General Reference.
        templateArn: string
        -- The tags added to the migration workflow template.
        tags: {string : string}
    end

    record CreateWorkflowInput
        -- The name of the migration workflow.
        name: string
        -- The description of the migration workflow.
        description: string
        -- The ID of the template.
        templateId: string
        -- The configuration ID of the application configured in Application Discovery Service.
        applicationConfigurationId: string
        -- The input parameters required to create a migration workflow.
        inputParameters: {string : StepInput}
        -- The servers on which a step will be run.
        stepTargets: {string}
        -- The tags to add on a migration workflow.
        tags: {string : string}
    end

    record CreateWorkflowOutput
        -- The ID of the migration workflow.
        id: string
        -- The Amazon Resource Name (ARN) of the migration workflow.
        arn: string
        -- The name of the migration workflow.
        name: string
        -- The description of the migration workflow.
        description: string
        -- The ID of the template.
        templateId: string
        -- The configuration ID of the application configured in Application Discovery Service.
        adsApplicationConfigurationId: string
        -- The inputs for creating a migration workflow.
        workflowInputs: {string : StepInput}
        -- The servers on which a step will be run.
        stepTargets: {string}
        -- The status of the migration workflow.
        status: string
        -- The time at which the migration workflow was created.
        creationTime: number
        -- The tags to add on a migration workflow.
        tags: {string : string}
    end

    record CreateWorkflowStepInput
        -- The name of the step.
        name: string
        -- The ID of the step group.
        stepGroupId: string
        -- The ID of the migration workflow.
        workflowId: string
        -- The action type of the step. You must run and update the status of a manual step for
        -- the workflow to continue after the completion of the step.
        stepActionType: string
        -- The description of the step.
        description: string
        -- The custom script to run tests on source or target environments.
        workflowStepAutomationConfiguration: WorkflowStepAutomationConfiguration
        -- The servers on which a step will be run.
        stepTarget: {string}
        -- The key value pairs added for the expected output.
        outputs: {WorkflowStepOutput}
        -- The previous step.
        previous: {string}
        -- The next step.
        next: {string}
    end

    record CreateWorkflowStepOutput
        -- The ID of the step.
        id: string
        -- The ID of the step group.
        stepGroupId: string
        -- The ID of the migration workflow.
        workflowId: string
        -- The name of the step.
        name: string
    end

    record CreateWorkflowStepGroupInput
        -- The ID of the migration workflow that will contain the step group.
        workflowId: string
        -- The name of the step group.
        name: string
        -- The description of the step group.
        description: string
        -- The next step group.
        next: {string}
        -- The previous step group.
        previous: {string}
    end

    record CreateWorkflowStepGroupOutput
        -- The ID of the migration workflow that contains the step group.
        workflowId: string
        -- The name of the step group.
        name: string
        -- The ID of the step group.
        id: string
        -- The description of the step group.
        description: string
        -- List of AWS services utilized in a migration workflow.
        tools: {Tool}
        -- The next step group.
        next: {string}
        -- The previous step group.
        previous: {string}
        -- The time at which the step group is created.
        creationTime: number
    end

    record DeleteTemplateInput
        -- The ID of the request to delete a migration workflow template.
        id: string
    end

    record DeleteTemplateOutput
    end

    record DeleteWorkflowInput
        -- The ID of the migration workflow you want to delete.
        id: string
    end

    record DeleteWorkflowOutput
        -- The ID of the migration workflow.
        id: string
        -- The Amazon Resource Name (ARN) of the migration workflow.
        arn: string
        -- The status of the migration workflow.
        status: string
    end

    record DeleteWorkflowStepInput
        -- The ID of the step you want to delete.
        id: string
        -- The ID of the step group that contains the step you want to delete.
        stepGroupId: string
        -- The ID of the migration workflow.
        workflowId: string
    end

    record DeleteWorkflowStepOutput
    end

    record DeleteWorkflowStepGroupInput
        -- The ID of the migration workflow.
        workflowId: string
        -- The ID of the step group you want to delete.
        id: string
    end

    record DeleteWorkflowStepGroupOutput
    end

    record GetTemplateInput
        -- The ID of the template.
        id: string
    end

    record GetTemplateOutput
        -- The ID of the template.
        id: string
        -- >The Amazon Resource Name (ARN) of the migration workflow template. The format for an
        -- Migration Hub Orchestrator template ARN is
        -- arn:aws:migrationhub-orchestrator:region:account:template/template-abcd1234.
        -- For more information about ARNs, see Amazon Resource Names (ARNs)
        -- in the AWS General Reference.
        templateArn: string
        -- The name of the template.
        name: string
        -- The time at which the template was last created.
        description: string
        -- The inputs provided for the creation of the migration workflow.
        inputs: {TemplateInput}
        -- List of AWS services utilized in a migration workflow.
        tools: {Tool}
        -- The time at which the template was last created.
        creationTime: number
        -- The owner of the migration workflow template.
        owner: string
        -- The status of the template.
        status: string
        -- The status message of retrieving migration workflow templates.
        statusMessage: string
        -- The class of the migration workflow template. The available template classes
        -- are:
        -- A2C
        -- MGN
        -- SAP_MULTI
        -- SQL_EC2
        -- SQL_RDS
        -- VMIE
        templateClass: string
        -- The tags added to the migration workflow template.
        tags: {string : string}
    end

    record GetTemplateStepInput
        -- The ID of the step.
        id: string
        -- The ID of the template.
        templateId: string
        -- The ID of the step group.
        stepGroupId: string
    end

    record GetTemplateStepOutput
        -- The ID of the step.
        id: string
        -- The ID of the step group.
        stepGroupId: string
        -- The ID of the template.
        templateId: string
        -- The name of the step.
        name: string
        -- The description of the step.
        description: string
        -- The action type of the step. You must run and update the status of a manual step for
        -- the workflow to continue after the completion of the step.
        stepActionType: string
        -- The time at which the step was created.
        creationTime: string
        -- The previous step.
        previous: {string}
        -- The next step.
        next: {string}
        -- The outputs of the step.
        outputs: {StepOutput}
        -- The custom script to run tests on source or target environments.
        stepAutomationConfiguration: StepAutomationConfiguration
    end

    record GetTemplateStepGroupInput
        -- The ID of the template.
        templateId: string
        -- The ID of the step group.
        id: string
    end

    record GetTemplateStepGroupOutput
        -- The ID of the template.
        templateId: string
        -- The ID of the step group.
        id: string
        -- The name of the step group.
        name: string
        -- The description of the step group.
        description: string
        -- The status of the step group.
        status: string
        -- The time at which the step group was created.
        creationTime: number
        -- The time at which the step group was last modified.
        lastModifiedTime: number
        -- List of AWS services utilized in a migration workflow.
        tools: {Tool}
        -- The previous step group.
        previous: {string}
        -- The next step group.
        next: {string}
    end

    record GetWorkflowInput
        -- The ID of the migration workflow.
        id: string
    end

    record GetWorkflowOutput
        -- The ID of the migration workflow.
        id: string
        -- The Amazon Resource Name (ARN) of the migration workflow.
        arn: string
        -- The name of the migration workflow.
        name: string
        -- The description of the migration workflow.
        description: string
        -- The ID of the template.
        templateId: string
        -- The configuration ID of the application configured in Application Discovery Service.
        adsApplicationConfigurationId: string
        -- The name of the application configured in Application Discovery Service.
        adsApplicationName: string
        -- The status of the migration workflow.
        status: string
        -- The status message of the migration workflow.
        statusMessage: string
        -- The time at which the migration workflow was created.
        creationTime: number
        -- The time at which the migration workflow was last started.
        lastStartTime: number
        -- The time at which the migration workflow was last stopped.
        lastStopTime: number
        -- The time at which the migration workflow was last modified.
        lastModifiedTime: number
        -- The time at which the migration workflow ended.
        endTime: number
        -- List of AWS services utilized in a migration workflow.
        tools: {Tool}
        -- The total number of steps in the migration workflow.
        totalSteps: number
        -- Get a list of completed steps in the migration workflow.
        completedSteps: number
        -- The inputs required for creating the migration workflow.
        workflowInputs: {string : StepInput}
        -- The tags added to the migration workflow.
        tags: {string : string}
        -- The Amazon S3 bucket where the migration logs are stored.
        workflowBucket: string
    end

    record GetWorkflowStepInput
        -- The ID of the migration workflow.
        workflowId: string
        -- The ID of the step group.
        stepGroupId: string
        -- The ID of the step.
        id: string
    end

    record GetWorkflowStepOutput
        -- The name of the step.
        name: string
        -- The ID of the step group.
        stepGroupId: string
        -- The ID of the migration workflow.
        workflowId: string
        -- The ID of the step.
        stepId: string
        -- The description of the step.
        description: string
        -- The action type of the step. You must run and update the status of a manual step for
        -- the workflow to continue after the completion of the step.
        stepActionType: string
        -- The owner of the step.
        owner: string
        -- The custom script to run tests on source or target environments.
        workflowStepAutomationConfiguration: WorkflowStepAutomationConfiguration
        -- The servers on which a step will be run.
        stepTarget: {string}
        -- The outputs of the step.
        outputs: {WorkflowStepOutput}
        -- The previous step.
        previous: {string}
        -- The next step.
        next: {string}
        -- The status of the step.
        status: string
        -- The status message of the migration workflow.
        statusMessage: string
        -- The output location of the script.
        scriptOutputLocation: string
        -- The time at which the step was created.
        creationTime: number
        -- The time at which the workflow was last started.
        lastStartTime: number
        -- The time at which the step ended.
        endTime: number
        -- The number of servers that have been migrated.
        noOfSrvCompleted: number
        -- The number of servers that have failed to migrate.
        noOfSrvFailed: number
        -- The total number of servers that have been migrated.
        totalNoOfSrv: number
    end

    record GetWorkflowStepGroupInput
        -- The ID of the step group.
        id: string
        -- The ID of the migration workflow.
        workflowId: string
    end

    record GetWorkflowStepGroupOutput
        -- The ID of the step group.
        id: string
        -- The ID of the migration workflow.
        workflowId: string
        -- The name of the step group.
        name: string
        -- The description of the step group.
        description: string
        -- The status of the step group.
        status: string
        -- The owner of the step group.
        owner: string
        -- The time at which the step group was created.
        creationTime: number
        -- The time at which the step group was last modified.
        lastModifiedTime: number
        -- The time at which the step group ended.
        endTime: number
        -- List of AWS services utilized in a migration workflow.
        tools: {Tool}
        -- The previous step group.
        previous: {string}
        -- The next step group.
        next: {string}
    end

    record ListPluginsInput
        -- The maximum number of plugins that can be returned.
        maxResults: number
        -- The pagination token.
        nextToken: string
    end

    record ListPluginsOutput
        -- The pagination token.
        nextToken: string
        -- Migration Hub Orchestrator plugins.
        plugins: {PluginSummary}
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceArn: string
    end

    record ListTagsForResourceOutput
        -- The tags added to a resource.
        tags: {string : string}
    end

    record ListTemplatesInput
        -- The maximum number of results that can be returned.
        maxResults: number
        -- The pagination token.
        nextToken: string
        -- The name of the template.
        name: string
    end

    record ListTemplatesOutput
        -- The pagination token.
        nextToken: string
        -- The summary of the template.
        templateSummary: {TemplateSummary}
    end

    record ListTemplateStepGroupsInput
        -- The maximum number of results that can be returned.
        maxResults: number
        -- The pagination token.
        nextToken: string
        -- The ID of the template.
        templateId: string
    end

    record ListTemplateStepGroupsOutput
        -- The pagination token.
        nextToken: string
        -- The summary of the step group in the template.
        templateStepGroupSummary: {TemplateStepGroupSummary}
    end

    record ListTemplateStepsInput
        -- The maximum number of results that can be returned.
        maxResults: number
        -- The pagination token.
        nextToken: string
        -- The ID of the template.
        templateId: string
        -- The ID of the step group.
        stepGroupId: string
    end

    record ListTemplateStepsOutput
        -- The pagination token.
        nextToken: string
        -- The list of summaries of steps in a template.
        templateStepSummaryList: {TemplateStepSummary}
    end

    record ListWorkflowsInput
        -- The maximum number of results that can be returned.
        maxResults: number
        -- The pagination token.
        nextToken: string
        -- The ID of the template.
        templateId: string
        -- The name of the application configured in Application Discovery Service.
        adsApplicationConfigurationName: string
        -- The status of the migration workflow.
        status: string
        -- The name of the migration workflow.
        name: string
    end

    record ListWorkflowsOutput
        -- The pagination token.
        nextToken: string
        -- The summary of the migration workflow.
        migrationWorkflowSummary: {MigrationWorkflowSummary}
    end

    record ListWorkflowStepGroupsInput
        -- The pagination token.
        nextToken: string
        -- The maximum number of results that can be returned.
        maxResults: number
        -- The ID of the migration workflow.
        workflowId: string
    end

    record ListWorkflowStepGroupsOutput
        -- The pagination token.
        nextToken: string
        -- The summary of step groups in a migration workflow.
        workflowStepGroupsSummary: {WorkflowStepGroupSummary}
    end

    record ListWorkflowStepsInput
        -- The pagination token.
        nextToken: string
        -- The maximum number of results that can be returned.
        maxResults: number
        -- The ID of the migration workflow.
        workflowId: string
        -- The ID of the step group.
        stepGroupId: string
    end

    record ListWorkflowStepsOutput
        -- The pagination token.
        nextToken: string
        -- The summary of steps in a migration workflow.
        workflowStepsSummary: {WorkflowStepSummary}
    end

    record RetryWorkflowStepInput
        -- The ID of the migration workflow.
        workflowId: string
        -- The ID of the step group.
        stepGroupId: string
        -- The ID of the step.
        id: string
    end

    record RetryWorkflowStepOutput
        -- The ID of the step group.
        stepGroupId: string
        -- The ID of the migration workflow.
        workflowId: string
        -- The ID of the step.
        id: string
        -- The status of the step.
        status: string
    end

    record StartWorkflowInput
        -- The ID of the migration workflow.
        id: string
    end

    record StartWorkflowOutput
        -- The ID of the migration workflow.
        id: string
        -- The Amazon Resource Name (ARN) of the migration workflow.
        arn: string
        -- The status of the migration workflow.
        status: string
        -- The status message of the migration workflow.
        statusMessage: string
        -- The time at which the migration workflow was last started.
        lastStartTime: number
    end

    record StopWorkflowInput
        -- The ID of the migration workflow.
        id: string
    end

    record StopWorkflowOutput
        -- The ID of the migration workflow.
        id: string
        -- The Amazon Resource Name (ARN) of the migration workflow.
        arn: string
        -- The status of the migration workflow.
        status: string
        -- The status message of the migration workflow.
        statusMessage: string
        -- The time at which the migration workflow was stopped.
        lastStopTime: number
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource to which you want to add tags.
        resourceArn: string
        -- A collection of labels, in the form of key:value pairs, that apply to this
        -- resource.
        tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource from which you want to remove
        -- tags.
        resourceArn: string
        -- One or more tag keys. Specify only the tag keys, not the tag values.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateTemplateInput
        -- The ID of the request to update a migration workflow template.
        id: string
        -- The name of the migration workflow template to update.
        templateName: string
        -- The description of the migration workflow template to update.
        templateDescription: string
        -- A unique, case-sensitive identifier that you provide to ensure the
        -- idempotency of the request.
        clientToken: string
    end

    record UpdateTemplateOutput
        -- The ID of the migration workflow template being updated.
        templateId: string
        -- The ARN of the migration workflow template being updated. The format for an Migration Hub Orchestrator
        -- template ARN is
        -- arn:aws:migrationhub-orchestrator:region:account:template/template-abcd1234.
        -- For more information about ARNs, see Amazon Resource Names (ARNs)
        -- in the AWS General Reference.
        templateArn: string
        -- The tags added to the migration workflow template.
        tags: {string : string}
    end

    record UpdateWorkflowInput
        -- The ID of the migration workflow.
        id: string
        -- The name of the migration workflow.
        name: string
        -- The description of the migration workflow.
        description: string
        -- The input parameters required to update a migration workflow.
        inputParameters: {string : StepInput}
        -- The servers on which a step will be run.
        stepTargets: {string}
    end

    record UpdateWorkflowOutput
        -- The ID of the migration workflow.
        id: string
        -- The Amazon Resource Name (ARN) of the migration workflow.
        arn: string
        -- The name of the migration workflow.
        name: string
        -- The description of the migration workflow.
        description: string
        -- The ID of the template.
        templateId: string
        -- The ID of the application configured in Application Discovery Service.
        adsApplicationConfigurationId: string
        -- The inputs required to update a migration workflow.
        workflowInputs: {string : StepInput}
        -- The servers on which a step will be run.
        stepTargets: {string}
        -- The status of the migration workflow.
        status: string
        -- The time at which the migration workflow was created.
        creationTime: number
        -- The time at which the migration workflow was last modified.
        lastModifiedTime: number
        -- The tags added to the migration workflow.
        tags: {string : string}
    end

    record UpdateWorkflowStepInput
        -- The ID of the step.
        id: string
        -- The ID of the step group.
        stepGroupId: string
        -- The ID of the migration workflow.
        workflowId: string
        -- The name of the step.
        name: string
        -- The description of the step.
        description: string
        -- The action type of the step. You must run and update the status of a manual step for
        -- the workflow to continue after the completion of the step.
        stepActionType: string
        -- The custom script to run tests on the source and target environments.
        workflowStepAutomationConfiguration: WorkflowStepAutomationConfiguration
        -- The servers on which a step will be run.
        stepTarget: {string}
        -- The outputs of a step.
        outputs: {WorkflowStepOutput}
        -- The previous step.
        previous: {string}
        -- The next step.
        next: {string}
        -- The status of the step.
        status: string
    end

    record UpdateWorkflowStepOutput
        -- The ID of the step.
        id: string
        -- The ID of the step group.
        stepGroupId: string
        -- The ID of the migration workflow.
        workflowId: string
        -- The name of the step.
        name: string
    end

    record UpdateWorkflowStepGroupInput
        -- The ID of the migration workflow.
        workflowId: string
        -- The ID of the step group.
        id: string
        -- The name of the step group.
        name: string
        -- The description of the step group.
        description: string
        -- The next step group.
        next: {string}
        -- The previous step group.
        previous: {string}
    end

    record UpdateWorkflowStepGroupOutput
        -- The ID of the migration workflow.
        workflowId: string
        -- The name of the step group.
        name: string
        -- The ID of the step group.
        id: string
        -- The description of the step group.
        description: string
        -- List of AWS services utilized in a migration workflow.
        tools: {Tool}
        -- The next step group.
        next: {string}
        -- The previous step group.
        previous: {string}
        -- The time at which the step group was last modified.
        lastModifiedTime: number
    end

    -- You do not have sufficient access to perform this action.
    record AccessDeniedException
        message: string
    end

    -- This exception is thrown when an attempt to update or delete
    -- a resource would cause an inconsistent state.
    record ConflictException
        message: string
    end

    -- An internal error has occurred.
    record InternalServerException
        message: string
    end

    -- The request was denied due to request throttling.
    record ThrottlingException
        message: string
    end

    -- The input fails to satisfy the constraints specified by an AWS service.
    record ValidationException
        message: string
    end

    -- The resource is not available.
    record ResourceNotFoundException
        message: string
    end

end

return M
