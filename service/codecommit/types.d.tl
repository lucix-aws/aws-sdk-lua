-- Code generated by lua-client-codegen. DO NOT EDIT.

local record AssociateApprovalRuleTemplateWithRepositoryInput
    -- The name for the approval rule template.
    approvalRuleTemplateName: string
    -- The name of the repository that you want to associate with the template.
    repositoryName: string
end

local record AssociateApprovalRuleTemplateWithRepositoryOutput
end

local record BatchAssociateApprovalRuleTemplateWithRepositoriesInput
    -- The name of the template you want to associate with one or more repositories.
    approvalRuleTemplateName: string
    -- The names of the repositories you want to associate with the template.
    -- The length constraint limit is for each string in the array. The array itself can be empty.
    repositoryNames: {string}
end

local record BatchAssociateApprovalRuleTemplateWithRepositoriesOutput
    -- A list of names of the repositories that have been associated with the template.
    associatedRepositoryNames: {string}
    -- A list of any errors that might have occurred while attempting to create the association between the template and the repositories.
    errors: {BatchAssociateApprovalRuleTemplateWithRepositoriesError}
end

local record BatchDescribeMergeConflictsInput
    -- The name of the repository that contains the merge conflicts you want to review.
    repositoryName: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    destinationCommitSpecifier: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    sourceCommitSpecifier: string
    -- The merge option or strategy you want to use to merge the code.
    mergeOption: string
    -- The maximum number of merge hunks to include in the output.
    maxMergeHunks: number
    -- The maximum number of files to include in the output.
    maxConflictFiles: number
    -- The path of the target files used to describe the conflicts. If not specified, the default is all conflict files.
    filePaths: {string}
    -- The level of conflict detail to use. If unspecified, the default FILE_LEVEL is used,
    -- which returns a not-mergeable result if the same file has differences in both branches.
    -- If LINE_LEVEL is specified, a conflict is considered not mergeable if the same file in
    -- both branches has differences on the same line.
    conflictDetailLevel: string
    -- Specifies which branch to use when resolving conflicts, or whether to attempt
    -- automatically merging two versions of a file. The default is NONE, which requires any
    -- conflicts to be resolved manually before the merge operation is successful.
    conflictResolutionStrategy: string
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    nextToken: string
end

local record BatchDescribeMergeConflictsOutput
    -- A list of conflicts for each file, including the conflict metadata and the hunks of the differences between the files.
    conflicts: {Conflict}
    -- An enumeration token that can be used in a request to return the next batch of the results.
    nextToken: string
    -- A list of any errors returned while describing the merge conflicts for each file.
    errors: {BatchDescribeMergeConflictsError}
    -- The commit ID of the destination commit specifier that was used in the merge evaluation.
    destinationCommitId: string
    -- The commit ID of the source commit specifier that was used in the merge evaluation.
    sourceCommitId: string
    -- The commit ID of the merge base.
    baseCommitId: string
end

local record BatchDisassociateApprovalRuleTemplateFromRepositoriesInput
    -- The name of the template that you want to disassociate from one or more repositories.
    approvalRuleTemplateName: string
    -- The repository names that you want to disassociate from the approval rule
    -- template.
    -- The length constraint limit is for each string in the array. The array itself can be empty.
    repositoryNames: {string}
end

local record BatchDisassociateApprovalRuleTemplateFromRepositoriesOutput
    -- A list of repository names that have had their association with the template
    -- removed.
    disassociatedRepositoryNames: {string}
    -- A list of any errors that might have occurred while attempting to remove the association between the template and the repositories.
    errors: {BatchDisassociateApprovalRuleTemplateFromRepositoriesError}
end

local record BatchGetCommitsInput
    -- The full commit IDs of the commits to get information about.
    -- You must supply the full SHA IDs of each commit. You cannot use shortened SHA
    -- IDs.
    commitIds: {string}
    -- The name of the repository that contains the commits.
    repositoryName: string
end

local record BatchGetCommitsOutput
    -- An array of commit data type objects, each of which contains information about a specified commit.
    commits: {Commit}
    -- Returns any commit IDs for which information could not be found. For example, if one
    -- of the commit IDs was a shortened SHA ID or that commit was not found in the specified
    -- repository, the ID returns an error object with more information.
    errors: {BatchGetCommitsError}
end

-- Represents the input of a batch get repositories operation.
local record BatchGetRepositoriesInput
    -- The names of the repositories to get information about.
    -- The length constraint limit is for each string in the array. The array itself can be empty.
    repositoryNames: {string}
end

-- Represents the output of a batch get repositories operation.
local record BatchGetRepositoriesOutput
    -- A list of repositories returned by the batch get repositories operation.
    repositories: {RepositoryMetadata}
    -- Returns a list of repository names for which information could not be found.
    repositoriesNotFound: {string}
    -- Returns information about any errors returned when attempting to retrieve information about the repositories.
    errors: {BatchGetRepositoriesError}
end

local record CreateApprovalRuleTemplateInput
    -- The name of the approval rule template. Provide descriptive names, because this name
    -- is applied to the approval rules created automatically in associated
    -- repositories.
    approvalRuleTemplateName: string
    -- The content of the approval rule that is created on pull requests in associated
    -- repositories. If you specify one or more destination references (branches), approval
    -- rules are created in an associated repository only if their destination references
    -- (branches) match those specified in the template.
    -- When you create the content of the approval rule template, you can specify
    -- approvers in an approval pool in one of two ways:
    -- CodeCommitApprovers: This option only
    -- requires an Amazon Web Services account and a resource. It can be used for both IAM users
    -- and federated access users whose name matches the provided resource name.
    -- This is a very powerful option that offers a great deal of flexibility. For
    -- example, if you specify the Amazon Web Services account 123456789012
    -- and Mary_Major, all of the following are counted as
    -- approvals coming from that user:
    -- An IAM user in the account
    -- (arn:aws:iam::123456789012:user/Mary_Major)
    -- A federated user identified in IAM as Mary_Major
    -- (arn:aws:sts::123456789012:federated-user/Mary_Major)
    -- This option does not recognize an active session of someone assuming the
    -- role of CodeCommitReview with a role session name of
    -- Mary_Major
    -- (arn:aws:sts::123456789012:assumed-role/CodeCommitReview/Mary_Major)
    -- unless you include a wildcard (*Mary_Major).
    -- Fully qualified ARN: This option allows
    -- you to specify the fully qualified Amazon Resource Name (ARN) of the IAM
    -- user or role.
    -- For more information about IAM ARNs, wildcards, and formats, see IAM
    -- Identifiers in the IAM User Guide.
    approvalRuleTemplateContent: string
    -- The description of the approval rule template. Consider providing a description that
    -- explains what this template does and when it might be appropriate to associate it with
    -- repositories.
    approvalRuleTemplateDescription: string
end

local record CreateApprovalRuleTemplateOutput
    -- The content and structure of the created approval rule template.
    approvalRuleTemplate: ApprovalRuleTemplate
end

-- Represents the input of a create branch operation.
local record CreateBranchInput
    -- The name of the repository in which you want to create the new branch.
    repositoryName: string
    -- The name of the new branch to create.
    branchName: string
    -- The ID of the commit to point the new branch to.
    commitId: string
end

local record CreateBranchOutput
end

local record CreateCommitInput
    -- The name of the repository where you create the commit.
    repositoryName: string
    -- The name of the branch where you create the commit.
    branchName: string
    -- The ID of the commit that is the parent of the commit you create. Not required if this
    -- is an empty repository.
    parentCommitId: string
    -- The name of the author who created the commit. This information is used as both the
    -- author and committer for the commit.
    authorName: string
    -- The email address of the person who created the commit.
    email: string
    -- The commit message you want to include in the commit. Commit messages are limited to
    -- 256 KB. If no message is specified, a default message is used.
    commitMessage: string
    -- If the commit contains deletions, whether to keep a folder or folder structure if the
    -- changes leave the folders empty. If true, a ..gitkeep file is created for empty folders.
    -- The default is false.
    keepEmptyFolders: boolean
    -- The files to add or update in this commit.
    putFiles: {PutFileEntry}
    -- The files to delete in this commit. These files still exist in earlier commits.
    deleteFiles: {DeleteFileEntry}
    -- The file modes to update for files in this commit.
    setFileModes: {SetFileModeEntry}
end

local record CreateCommitOutput
    -- The full commit ID of the commit that contains your committed file changes.
    commitId: string
    -- The full SHA-1 pointer of the tree information for the commit that contains the commited file changes.
    treeId: string
    -- The files added as part of the committed file changes.
    filesAdded: {FileMetadata}
    -- The files updated as part of the commited file changes.
    filesUpdated: {FileMetadata}
    -- The files deleted as part of the committed file changes.
    filesDeleted: {FileMetadata}
end

local record CreatePullRequestInput
    -- The title of the pull request. This title is used to identify the pull request to
    -- other users in the repository.
    title: string
    -- A description of the pull request.
    description: string
    -- The targets for the pull request, including the source of the code to be reviewed (the
    -- source branch) and the destination where the creator of the pull request intends the
    -- code to be merged after the pull request is closed (the destination branch).
    targets: {Target}
    -- A unique, client-generated idempotency token that, when provided in a request, ensures
    -- the request cannot be repeated with a changed parameter. If a request is received with
    -- the same parameters and a token is included, the request returns information about the
    -- initial request that used that token.
    -- The Amazon Web ServicesSDKs prepopulate client request tokens. If you are using an Amazon Web ServicesSDK, an
    -- idempotency token is created for you.
    clientRequestToken: string
end

local record CreatePullRequestOutput
    -- Information about the newly created pull request.
    pullRequest: PullRequest
end

local record CreatePullRequestApprovalRuleInput
    -- The system-generated ID of the pull request for which you want to create the approval rule.
    pullRequestId: string
    -- The name for the approval rule.
    approvalRuleName: string
    -- The content of the approval rule, including the number of approvals needed and the structure of an approval pool defined for approvals, if any. For more information
    -- about approval pools, see the CodeCommit User Guide.
    -- When you create the content of the approval rule, you can specify approvers in an
    -- approval pool in one of two ways:
    -- CodeCommitApprovers: This option only
    -- requires an Amazon Web Services account and a resource. It can be used for both IAM users
    -- and federated access users whose name matches the provided resource name.
    -- This is a very powerful option that offers a great deal of flexibility. For
    -- example, if you specify the Amazon Web Services account 123456789012
    -- and Mary_Major, all of the following would be counted
    -- as approvals coming from that user:
    -- An IAM user in the account
    -- (arn:aws:iam::123456789012:user/Mary_Major)
    -- A federated user identified in IAM as Mary_Major
    -- (arn:aws:sts::123456789012:federated-user/Mary_Major)
    -- This option does not recognize an active session of someone assuming the
    -- role of CodeCommitReview with a role session name of
    -- Mary_Major
    -- (arn:aws:sts::123456789012:assumed-role/CodeCommitReview/Mary_Major)
    -- unless you include a wildcard (*Mary_Major).
    -- Fully qualified ARN: This option allows
    -- you to specify the fully qualified Amazon Resource Name (ARN) of the IAM
    -- user or role.
    -- For more information about IAM ARNs, wildcards, and formats, see
    -- IAM
    -- Identifiers in the IAM User Guide.
    approvalRuleContent: string
end

local record CreatePullRequestApprovalRuleOutput
    -- Information about the created approval rule.
    approvalRule: ApprovalRule
end

-- Represents the input of a create repository operation.
local record CreateRepositoryInput
    -- The name of the new repository to be created.
    -- The repository name must be unique across the calling Amazon Web Services account. Repository names
    -- are limited to 100 alphanumeric, dash, and underscore characters, and cannot include
    -- certain characters. For more information about the limits on repository names, see
    -- Quotas in the CodeCommit User Guide. The
    -- suffix .git is prohibited.
    repositoryName: string
    -- A comment or description about the new repository.
    -- The description field for a repository accepts all HTML characters and all valid
    -- Unicode characters. Applications that do not HTML-encode the description and display
    -- it in a webpage can expose users to potentially malicious code. Make sure that you
    -- HTML-encode the description field in any application that uses this API to display
    -- the repository description on a webpage.
    repositoryDescription: string
    -- One or more tag key-value pairs to use when tagging this repository.
    tags: {string : string}
    -- The ID of the encryption key. You can view the ID of an encryption key in the KMS console, or use the KMS APIs to
    -- programmatically retrieve a key ID. For more information about acceptable values for kmsKeyID, see
    -- KeyId in the Decrypt API description in
    -- the Key Management Service API Reference.
    -- If no key is specified, the default aws/codecommit Amazon Web Services managed key is used.
    kmsKeyId: string
end

-- Represents the output of a create repository operation.
local record CreateRepositoryOutput
    -- Information about the newly created repository.
    repositoryMetadata: RepositoryMetadata
end

local record CreateUnreferencedMergeCommitInput
    -- The name of the repository where you want to create the unreferenced merge commit.
    repositoryName: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    sourceCommitSpecifier: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    destinationCommitSpecifier: string
    -- The merge option or strategy you want to use to merge the code.
    mergeOption: string
    -- The level of conflict detail to use. If unspecified, the default FILE_LEVEL is used,
    -- which returns a not-mergeable result if the same file has differences in both branches.
    -- If LINE_LEVEL is specified, a conflict is considered not mergeable if the same file in
    -- both branches has differences on the same line.
    conflictDetailLevel: string
    -- Specifies which branch to use when resolving conflicts, or whether to attempt
    -- automatically merging two versions of a file. The default is NONE, which requires any
    -- conflicts to be resolved manually before the merge operation is successful.
    conflictResolutionStrategy: string
    -- The name of the author who created the unreferenced commit. This information is used
    -- as both the author and committer for the commit.
    authorName: string
    -- The email address for the person who created the unreferenced commit.
    email: string
    -- The commit message for the unreferenced commit.
    commitMessage: string
    -- If the commit contains deletions, whether to keep a folder or folder structure if the
    -- changes leave the folders empty. If this is specified as true, a .gitkeep file is
    -- created for empty folders. The default is false.
    keepEmptyFolders: boolean
    -- If AUTOMERGE is the conflict resolution strategy, a list of inputs to use when
    -- resolving conflicts during a merge.
    conflictResolution: ConflictResolution
end

local record CreateUnreferencedMergeCommitOutput
    -- The full commit ID of the commit that contains your merge results.
    commitId: string
    -- The full SHA-1 pointer of the tree information for the commit that contains the merge results.
    treeId: string
end

local record DeleteApprovalRuleTemplateInput
    -- The name of the approval rule template to delete.
    approvalRuleTemplateName: string
end

local record DeleteApprovalRuleTemplateOutput
    -- The system-generated ID of the deleted approval rule template. If the template has
    -- been previously deleted, the only response is a 200 OK.
    approvalRuleTemplateId: string
end

-- Represents the input of a delete branch operation.
local record DeleteBranchInput
    -- The name of the repository that contains the branch to be deleted.
    repositoryName: string
    -- The name of the branch to delete.
    branchName: string
end

-- Represents the output of a delete branch operation.
local record DeleteBranchOutput
    -- Information about the branch deleted by the operation, including the branch name and the commit ID that was the tip of the branch.
    deletedBranch: BranchInfo
end

local record DeleteCommentContentInput
    -- The unique, system-generated ID of the comment. To get this ID, use GetCommentsForComparedCommit
    -- or GetCommentsForPullRequest.
    commentId: string
end

local record DeleteCommentContentOutput
    -- Information about the comment you just deleted.
    comment: Comment
end

local record DeleteFileInput
    -- The name of the repository that contains the file to delete.
    repositoryName: string
    -- The name of the branch where the commit that deletes the file is made.
    branchName: string
    -- The fully qualified path to the file that to be deleted, including the full name and
    -- extension of that file. For example, /examples/file.md is a fully qualified path to a
    -- file named file.md in a folder named examples.
    filePath: string
    -- The ID of the commit that is the tip of the branch where you want to create the commit
    -- that deletes the file. This must be the HEAD commit for the branch. The commit that
    -- deletes the file is created from this commit ID.
    parentCommitId: string
    -- If a file is the only object in the folder or directory, specifies whether to delete
    -- the folder or directory that contains the file. By default, empty folders are deleted.
    -- This includes empty folders that are part of the directory structure. For example, if
    -- the path to a file is dir1/dir2/dir3/dir4, and dir2 and dir3 are empty, deleting the
    -- last file in dir4 also deletes the empty folders dir4, dir3, and dir2.
    keepEmptyFolders: boolean
    -- The commit message you want to include as part of deleting the file. Commit messages
    -- are limited to 256 KB. If no message is specified, a default message is used.
    commitMessage: string
    -- The name of the author of the commit that deletes the file. If no name is specified,
    -- the user's ARN is used as the author name and committer name.
    name: string
    -- The email address for the commit that deletes the file. If no email address is
    -- specified, the email address is left blank.
    email: string
end

local record DeleteFileOutput
    -- The full commit ID of the commit that contains the change that deletes the file.
    commitId: string
    -- The blob ID removed from the tree as part of deleting the file.
    blobId: string
    -- The full SHA-1 pointer of the tree information for the commit that contains the delete file change.
    treeId: string
    -- The fully qualified path to the file to be deleted, including the full name and
    -- extension of that file.
    filePath: string
end

local record DeletePullRequestApprovalRuleInput
    -- The system-generated ID of the pull request that contains the approval rule you want to delete.
    pullRequestId: string
    -- The name of the approval rule you want to delete.
    approvalRuleName: string
end

local record DeletePullRequestApprovalRuleOutput
    -- The ID of the deleted approval rule.
    -- If the approval rule was deleted in an earlier API call, the response is 200 OK without
    -- content.
    approvalRuleId: string
end

-- Represents the input of a delete repository operation.
local record DeleteRepositoryInput
    -- The name of the repository to delete.
    repositoryName: string
end

-- Represents the output of a delete repository operation.
local record DeleteRepositoryOutput
    -- The ID of the repository that was deleted.
    repositoryId: string
end

local record DescribeMergeConflictsInput
    -- The name of the repository where you want to get information about a merge conflict.
    repositoryName: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    destinationCommitSpecifier: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    sourceCommitSpecifier: string
    -- The merge option or strategy you want to use to merge the code.
    mergeOption: string
    -- The maximum number of merge hunks to include in the output.
    maxMergeHunks: number
    -- The path of the target files used to describe the conflicts.
    filePath: string
    -- The level of conflict detail to use. If unspecified, the default FILE_LEVEL is used,
    -- which returns a not-mergeable result if the same file has differences in both branches.
    -- If LINE_LEVEL is specified, a conflict is considered not mergeable if the same file in
    -- both branches has differences on the same line.
    conflictDetailLevel: string
    -- Specifies which branch to use when resolving conflicts, or whether to attempt
    -- automatically merging two versions of a file. The default is NONE, which requires any
    -- conflicts to be resolved manually before the merge operation is successful.
    conflictResolutionStrategy: string
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    nextToken: string
end

local record DescribeMergeConflictsOutput
    -- Contains metadata about the conflicts found in the merge.
    conflictMetadata: ConflictMetadata
    -- A list of merge hunks of the differences between the files or lines.
    mergeHunks: {MergeHunk}
    -- An enumeration token that can be used in a request to return the next batch of the results.
    nextToken: string
    -- The commit ID of the destination commit specifier that was used in the merge evaluation.
    destinationCommitId: string
    -- The commit ID of the source commit specifier that was used in the merge evaluation.
    sourceCommitId: string
    -- The commit ID of the merge base.
    baseCommitId: string
end

local record DescribePullRequestEventsInput
    -- The system-generated ID of the pull request. To get this ID, use ListPullRequests.
    pullRequestId: string
    -- Optional. The pull request event type about which you want to return information.
    pullRequestEventType: string
    -- The Amazon Resource Name (ARN) of the user whose actions resulted in the event.
    -- Examples include updating the pull request with more commits or changing the status of a
    -- pull request.
    actorArn: string
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    nextToken: string
    -- A non-zero, non-negative integer used to limit the number of returned results.
    -- The default is 100 events, which is also the maximum number of events that can be returned in a result.
    maxResults: number
end

local record DescribePullRequestEventsOutput
    -- Information about the pull request events.
    pullRequestEvents: {PullRequestEvent}
    -- An enumeration token that can be used in a request to return the next batch of the results.
    nextToken: string
end

local record DisassociateApprovalRuleTemplateFromRepositoryInput
    -- The name of the approval rule template to disassociate from a specified repository.
    approvalRuleTemplateName: string
    -- The name of the repository you want to disassociate from the template.
    repositoryName: string
end

local record DisassociateApprovalRuleTemplateFromRepositoryOutput
end

local record EvaluatePullRequestApprovalRulesInput
    -- The system-generated ID of the pull request you want to evaluate.
    pullRequestId: string
    -- The system-generated ID for the pull request revision. To retrieve the most recent
    -- revision ID for a pull request, use
    -- GetPullRequest.
    revisionId: string
end

local record EvaluatePullRequestApprovalRulesOutput
    -- The result of the evaluation, including the names of the rules whose conditions have been met (if any), the names of the rules whose conditions have
    -- not been met (if any), whether the pull request is in the approved state, and whether the pull request approval rule has been set aside by an override.
    evaluation: Evaluation
end

local record GetApprovalRuleTemplateInput
    -- The name of the approval rule template for which you want to get information.
    approvalRuleTemplateName: string
end

local record GetApprovalRuleTemplateOutput
    -- The content and structure of the approval rule template.
    approvalRuleTemplate: ApprovalRuleTemplate
end

-- Represents the input of a get blob operation.
local record GetBlobInput
    -- The name of the repository that contains the blob.
    repositoryName: string
    -- The ID of the blob, which is its SHA-1 pointer.
    blobId: string
end

-- Represents the output of a get blob operation.
local record GetBlobOutput
    -- The content of the blob, usually a file.
    content: string
end

-- Represents the input of a get branch operation.
local record GetBranchInput
    -- The name of the repository that contains the branch for which you want to retrieve information.
    repositoryName: string
    -- The name of the branch for which you want to retrieve information.
    branchName: string
end

-- Represents the output of a get branch operation.
local record GetBranchOutput
    -- The name of the branch.
    branch: BranchInfo
end

local record GetCommentInput
    -- The unique, system-generated ID of the comment. To get this ID, use GetCommentsForComparedCommit
    -- or GetCommentsForPullRequest.
    commentId: string
end

local record GetCommentOutput
    -- The contents of the comment.
    comment: Comment
end

local record GetCommentReactionsInput
    -- The ID of the comment for which you want to get reactions information.
    commentId: string
    -- Optional. The Amazon Resource Name (ARN) of the user or identity for which you want to get reaction information.
    reactionUserArn: string
    -- An enumeration token that, when provided in a request, returns the next batch of the results.
    nextToken: string
    -- A non-zero, non-negative integer used to limit the number of returned results. The default is the same as the allowed maximum, 1,000.
    maxResults: number
end

local record GetCommentReactionsOutput
    -- An array of reactions to the specified comment.
    reactionsForComment: {ReactionForComment}
    -- An enumeration token that can be used in a request to return the next batch of the results.
    nextToken: string
end

local record GetCommentsForComparedCommitInput
    -- The name of the repository where you want to compare commits.
    repositoryName: string
    -- To establish the directionality of the comparison, the full commit ID of the before
    -- commit.
    beforeCommitId: string
    -- To establish the directionality of the comparison, the full commit ID of the after
    -- commit.
    afterCommitId: string
    -- An enumeration token that when provided in a request, returns the next batch of the results.
    nextToken: string
    -- A non-zero, non-negative integer used to limit the number of returned results. The
    -- default is 100 comments, but you can configure up to 500.
    maxResults: number
end

local record GetCommentsForComparedCommitOutput
    -- A list of comment objects on the compared commit.
    commentsForComparedCommitData: {CommentsForComparedCommit}
    -- An enumeration token that can be used in a request to return the next batch of the results.
    nextToken: string
end

local record GetCommentsForPullRequestInput
    -- The system-generated ID of the pull request. To get this ID, use ListPullRequests.
    pullRequestId: string
    -- The name of the repository that contains the pull request. Requirement is conditional: repositoryName must be specified when
    -- beforeCommitId and afterCommitId are included.
    repositoryName: string
    -- The full commit ID of the commit in the destination branch that was the tip of the branch at the time the pull request was created. Requirement is conditional:
    -- beforeCommitId must be specified when repositoryName is included.
    beforeCommitId: string
    -- The full commit ID of the commit in the source branch that was the tip of the branch at the time the comment was made. Requirement is conditional:
    -- afterCommitId must be specified when repositoryName is included.
    afterCommitId: string
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    nextToken: string
    -- A non-zero, non-negative integer used to limit the number of returned results. The default is 100 comments.
    -- You can return up to 500 comments with a single request.
    maxResults: number
end

local record GetCommentsForPullRequestOutput
    -- An array of comment objects on the pull request.
    commentsForPullRequestData: {CommentsForPullRequest}
    -- An enumeration token that can be used in a request to return the next batch of the results.
    nextToken: string
end

-- Represents the input of a get commit operation.
local record GetCommitInput
    -- The name of the repository to which the commit was made.
    repositoryName: string
    -- The commit ID. Commit IDs are the full SHA ID of the commit.
    commitId: string
end

-- Represents the output of a get commit operation.
local record GetCommitOutput
    -- A commit data type object that contains information about the specified commit.
    commit: Commit
end

local record GetDifferencesInput
    -- The name of the repository where you want to get differences.
    repositoryName: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, the full commit ID). Optional. If not specified, all changes before the
    -- afterCommitSpecifier value are shown. If you do not use
    -- beforeCommitSpecifier in your request, consider limiting the results
    -- with maxResults.
    beforeCommitSpecifier: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit.
    afterCommitSpecifier: string
    -- The file path in which to check for differences. Limits the results to this path. Can
    -- also be used to specify the previous name of a directory or folder. If
    -- beforePath and afterPath are not specified, differences
    -- are shown for all paths.
    beforePath: string
    -- The file path in which to check differences. Limits the results to this path. Can also
    -- be used to specify the changed name of a directory or folder, if it has changed. If not
    -- specified, differences are shown for all paths.
    afterPath: string
    -- A non-zero, non-negative integer used to limit the number of returned results.
    MaxResults: number
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    NextToken: string
end

local record GetDifferencesOutput
    -- A data type object that contains information about the differences, including whether
    -- the difference is added, modified, or deleted (A, D, M).
    differences: {Difference}
    -- An enumeration token that can be used in a request to return the next batch of the results.
    NextToken: string
end

local record GetFileInput
    -- The name of the repository that contains the file.
    repositoryName: string
    -- The fully quaified reference that identifies the commit that contains the file. For
    -- example, you can specify a full commit ID, a tag, a branch name, or a reference such as
    -- refs/heads/main. If none is provided, the head commit is used.
    commitSpecifier: string
    -- The fully qualified path to the file, including the full name and extension of the
    -- file. For example, /examples/file.md is the fully qualified path to a file named file.md
    -- in a folder named examples.
    filePath: string
end

local record GetFileOutput
    -- The full commit ID of the commit that contains the content returned by GetFile.
    commitId: string
    -- The blob ID of the object that represents the file content.
    blobId: string
    -- The fully qualified path to the specified file. Returns the name and extension of the
    -- file.
    filePath: string
    -- The extrapolated file mode permissions of the blob. Valid values include strings such as EXECUTABLE and not numeric values.
    -- The file mode permissions returned by this API are not the standard file mode
    -- permission values, such as 100644, but rather extrapolated values. See the supported
    -- return values.
    fileMode: string
    -- The size of the contents of the file, in bytes.
    fileSize: number
    -- The base-64 encoded binary data object that represents the content of the file.
    fileContent: string
end

local record GetFolderInput
    -- The name of the repository.
    repositoryName: string
    -- A fully qualified reference used to identify a commit that contains the version of the
    -- folder's content to return. A fully qualified reference can be a commit ID, branch name,
    -- tag, or reference such as HEAD. If no specifier is provided, the folder content is
    -- returned as it exists in the HEAD commit.
    commitSpecifier: string
    -- The fully qualified path to the folder whose contents are returned, including the
    -- folder name. For example, /examples is a fully-qualified path to a folder named examples
    -- that was created off of the root directory (/) of a repository.
    folderPath: string
end

local record GetFolderOutput
    -- The full commit ID used as a reference for the returned version of the folder
    -- content.
    commitId: string
    -- The fully qualified path of the folder whose contents are returned.
    folderPath: string
    -- The full SHA-1 pointer of the tree information for the commit that contains the folder.
    treeId: string
    -- The list of folders that exist under the specified folder, if any.
    subFolders: {Folder}
    -- The list of files in the specified folder, if any.
    files: {File}
    -- The list of symbolic links to other files and folders in the specified folder, if
    -- any.
    symbolicLinks: {SymbolicLink}
    -- The list of submodules in the specified folder, if any.
    subModules: {SubModule}
end

local record GetMergeCommitInput
    -- The name of the repository that contains the merge commit about which you want to get information.
    repositoryName: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    sourceCommitSpecifier: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    destinationCommitSpecifier: string
    -- The level of conflict detail to use. If unspecified, the default FILE_LEVEL is used,
    -- which returns a not-mergeable result if the same file has differences in both branches.
    -- If LINE_LEVEL is specified, a conflict is considered not mergeable if the same file in
    -- both branches has differences on the same line.
    conflictDetailLevel: string
    -- Specifies which branch to use when resolving conflicts, or whether to attempt
    -- automatically merging two versions of a file. The default is NONE, which requires any
    -- conflicts to be resolved manually before the merge operation is successful.
    conflictResolutionStrategy: string
end

local record GetMergeCommitOutput
    -- The commit ID of the source commit specifier that was used in the merge evaluation.
    sourceCommitId: string
    -- The commit ID of the destination commit specifier that was used in the merge evaluation.
    destinationCommitId: string
    -- The commit ID of the merge base.
    baseCommitId: string
    -- The commit ID for the merge commit created when the source branch was merged into the
    -- destination branch. If the fast-forward merge strategy was used, there is no merge
    -- commit.
    mergedCommitId: string
end

local record GetMergeConflictsInput
    -- The name of the repository where the pull request was created.
    repositoryName: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    destinationCommitSpecifier: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    sourceCommitSpecifier: string
    -- The merge option or strategy you want to use to merge the code.
    mergeOption: string
    -- The level of conflict detail to use. If unspecified, the default FILE_LEVEL is used,
    -- which returns a not-mergeable result if the same file has differences in both branches.
    -- If LINE_LEVEL is specified, a conflict is considered not mergeable if the same file in
    -- both branches has differences on the same line.
    conflictDetailLevel: string
    -- The maximum number of files to include in the output.
    maxConflictFiles: number
    -- Specifies which branch to use when resolving conflicts, or whether to attempt
    -- automatically merging two versions of a file. The default is NONE, which requires any
    -- conflicts to be resolved manually before the merge operation is successful.
    conflictResolutionStrategy: string
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    nextToken: string
end

local record GetMergeConflictsOutput
    -- A Boolean value that indicates whether the code is mergeable by the specified merge option.
    mergeable: boolean
    -- The commit ID of the destination commit specifier that was used in the merge evaluation.
    destinationCommitId: string
    -- The commit ID of the source commit specifier that was used in the merge evaluation.
    sourceCommitId: string
    -- The commit ID of the merge base.
    baseCommitId: string
    -- A list of metadata for any conflicting files. If the specified merge strategy is
    -- FAST_FORWARD_MERGE, this list is always empty.
    conflictMetadataList: {ConflictMetadata}
    -- An enumeration token that can be used in a request to return the next batch of the results.
    nextToken: string
end

local record GetMergeOptionsInput
    -- The name of the repository that contains the commits about which you want to get merge options.
    repositoryName: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    sourceCommitSpecifier: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    destinationCommitSpecifier: string
    -- The level of conflict detail to use. If unspecified, the default FILE_LEVEL is used,
    -- which returns a not-mergeable result if the same file has differences in both branches.
    -- If LINE_LEVEL is specified, a conflict is considered not mergeable if the same file in
    -- both branches has differences on the same line.
    conflictDetailLevel: string
    -- Specifies which branch to use when resolving conflicts, or whether to attempt
    -- automatically merging two versions of a file. The default is NONE, which requires any
    -- conflicts to be resolved manually before the merge operation is successful.
    conflictResolutionStrategy: string
end

local record GetMergeOptionsOutput
    -- The merge option or strategy used to merge the code.
    mergeOptions: {string}
    -- The commit ID of the source commit specifier that was used in the merge evaluation.
    sourceCommitId: string
    -- The commit ID of the destination commit specifier that was used in the merge evaluation.
    destinationCommitId: string
    -- The commit ID of the merge base.
    baseCommitId: string
end

local record GetPullRequestInput
    -- The system-generated ID of the pull request. To get this ID, use ListPullRequests.
    pullRequestId: string
end

local record GetPullRequestOutput
    -- Information about the specified pull request.
    pullRequest: PullRequest
end

local record GetPullRequestApprovalStatesInput
    -- The system-generated ID for the pull request.
    pullRequestId: string
    -- The system-generated ID for the pull request revision.
    revisionId: string
end

local record GetPullRequestApprovalStatesOutput
    -- Information about users who have approved the pull request.
    approvals: {Approval}
end

local record GetPullRequestOverrideStateInput
    -- The ID of the pull request for which you want to get information about whether approval rules have been set aside (overridden).
    pullRequestId: string
    -- The system-generated ID of the revision for the pull request. To retrieve the most
    -- recent revision ID, use
    -- GetPullRequest.
    revisionId: string
end

local record GetPullRequestOverrideStateOutput
    -- A Boolean value that indicates whether a pull request has had its rules set aside (TRUE) or whether all approval rules still apply (FALSE).
    overridden: boolean
    -- The Amazon Resource Name (ARN) of the user or identity that overrode the rules and their requirements for the pull request.
    overrider: string
end

-- Represents the input of a get repository operation.
local record GetRepositoryInput
    -- The name of the repository to get information about.
    repositoryName: string
end

-- Represents the output of a get repository operation.
local record GetRepositoryOutput
    -- Information about the repository.
    repositoryMetadata: RepositoryMetadata
end

-- Represents the input of a get repository triggers operation.
local record GetRepositoryTriggersInput
    -- The name of the repository for which the trigger is configured.
    repositoryName: string
end

-- Represents the output of a get repository triggers operation.
local record GetRepositoryTriggersOutput
    -- The system-generated unique ID for the trigger.
    configurationId: string
    -- The JSON block of configuration information for each trigger.
    triggers: {RepositoryTrigger}
end

local record ListApprovalRuleTemplatesInput
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    nextToken: string
    -- A non-zero, non-negative integer used to limit the number of returned results.
    maxResults: number
end

local record ListApprovalRuleTemplatesOutput
    -- The names of all the approval rule templates found in the Amazon Web Services Region for your Amazon Web Services account.
    approvalRuleTemplateNames: {string}
    -- An enumeration token that allows the operation to batch the next results of the operation.
    nextToken: string
end

local record ListAssociatedApprovalRuleTemplatesForRepositoryInput
    -- The name of the repository for which you want to list all associated approval rule templates.
    repositoryName: string
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    nextToken: string
    -- A non-zero, non-negative integer used to limit the number of returned results.
    maxResults: number
end

local record ListAssociatedApprovalRuleTemplatesForRepositoryOutput
    -- The names of all approval rule templates associated with the repository.
    approvalRuleTemplateNames: {string}
    -- An enumeration token that allows the operation to batch the next results of the operation.
    nextToken: string
end

-- Represents the input of a list branches operation.
local record ListBranchesInput
    -- The name of the repository that contains the branches.
    repositoryName: string
    -- An enumeration token that allows the operation to batch the results.
    nextToken: string
end

-- Represents the output of a list branches operation.
local record ListBranchesOutput
    -- The list of branch names.
    branches: {string}
    -- An enumeration token that returns the batch of the results.
    nextToken: string
end

local record ListFileCommitHistoryInput
    -- The name of the repository that contains the file.
    repositoryName: string
    -- The fully quaified reference that identifies the commit that contains the file. For
    -- example, you can specify a full commit ID, a tag, a branch name, or a reference such as
    -- refs/heads/main. If none is provided, the head commit is used.
    commitSpecifier: string
    -- The full path of the file whose history you want to retrieve, including the name of the file.
    filePath: string
    -- A non-zero, non-negative integer used to limit the number of returned results.
    maxResults: number
    -- An enumeration token that allows the operation to batch the results.
    nextToken: string
end

local record ListFileCommitHistoryOutput
    -- An array of FileVersion objects that form a directed acyclic graph (DAG) of the changes to the file made by the commits that changed the file.
    revisionDag: {FileVersion}
    -- An enumeration token that can be used to return the next batch of results.
    nextToken: string
end

local record ListPullRequestsInput
    -- The name of the repository for which you want to list pull requests.
    repositoryName: string
    -- Optional. The Amazon Resource Name (ARN) of the user who created the pull request. If used, this filters the results
    -- to pull requests created by that user.
    authorArn: string
    -- Optional. The status of the pull request. If used, this refines the results to the pull requests that match the specified status.
    pullRequestStatus: string
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    nextToken: string
    -- A non-zero, non-negative integer used to limit the number of returned results.
    maxResults: number
end

local record ListPullRequestsOutput
    -- The system-generated IDs of the pull requests.
    pullRequestIds: {string}
    -- An enumeration token that allows the operation to batch the next results of the operation.
    nextToken: string
end

-- Represents the input of a list repositories operation.
local record ListRepositoriesInput
    -- An enumeration token that allows the operation to batch the results of the operation.
    -- Batch sizes are 1,000 for list repository operations. When the client sends the token back to CodeCommit,
    -- another page of 1,000 records is retrieved.
    nextToken: string
    -- The criteria used to sort the results of a list repositories operation.
    sortBy: string
    -- The order in which to sort the results of a list repositories operation.
    order: string
end

-- Represents the output of a list repositories operation.
local record ListRepositoriesOutput
    -- Lists the repositories called by the list repositories operation.
    repositories: {RepositoryNameIdPair}
    -- An enumeration token that allows the operation to batch the results of the operation.
    -- Batch sizes are 1,000 for list repository operations. When the client sends the token back to CodeCommit,
    -- another page of 1,000 records is retrieved.
    nextToken: string
end

local record ListRepositoriesForApprovalRuleTemplateInput
    -- The name of the approval rule template for which you want to list repositories that are associated with that template.
    approvalRuleTemplateName: string
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    nextToken: string
    -- A non-zero, non-negative integer used to limit the number of returned results.
    maxResults: number
end

local record ListRepositoriesForApprovalRuleTemplateOutput
    -- A list of repository names that are associated with the specified approval rule template.
    repositoryNames: {string}
    -- An enumeration token that allows the operation to batch the next results of the operation.
    nextToken: string
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the resource for which you want to get information
    -- about tags, if any.
    resourceArn: string
    -- An enumeration token that, when provided in a request, returns the next batch of the
    -- results.
    nextToken: string
end

local record ListTagsForResourceOutput
    -- A list of tag key and value pairs associated with the specified resource.
    tags: {string : string}
    -- An enumeration token that allows the operation to batch the next results of the operation.
    nextToken: string
end

local record MergeBranchesByFastForwardInput
    -- The name of the repository where you want to merge two branches.
    repositoryName: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    sourceCommitSpecifier: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    destinationCommitSpecifier: string
    -- The branch where the merge is applied.
    targetBranch: string
end

local record MergeBranchesByFastForwardOutput
    -- The commit ID of the merge in the destination or target branch.
    commitId: string
    -- The tree ID of the merge in the destination or target branch.
    treeId: string
end

local record MergeBranchesBySquashInput
    -- The name of the repository where you want to merge two branches.
    repositoryName: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    sourceCommitSpecifier: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    destinationCommitSpecifier: string
    -- The branch where the merge is applied.
    targetBranch: string
    -- The level of conflict detail to use. If unspecified, the default FILE_LEVEL is used,
    -- which returns a not-mergeable result if the same file has differences in both branches.
    -- If LINE_LEVEL is specified, a conflict is considered not mergeable if the same file in
    -- both branches has differences on the same line.
    conflictDetailLevel: string
    -- Specifies which branch to use when resolving conflicts, or whether to attempt
    -- automatically merging two versions of a file. The default is NONE, which requires any
    -- conflicts to be resolved manually before the merge operation is successful.
    conflictResolutionStrategy: string
    -- The name of the author who created the commit. This information is used as both the
    -- author and committer for the commit.
    authorName: string
    -- The email address of the person merging the branches. This information is used in the
    -- commit information for the merge.
    email: string
    -- The commit message for the merge.
    commitMessage: string
    -- If the commit contains deletions, whether to keep a folder or folder structure if the
    -- changes leave the folders empty. If this is specified as true, a .gitkeep file is
    -- created for empty folders. The default is false.
    keepEmptyFolders: boolean
    -- If AUTOMERGE is the conflict resolution strategy, a list of inputs to use when
    -- resolving conflicts during a merge.
    conflictResolution: ConflictResolution
end

local record MergeBranchesBySquashOutput
    -- The commit ID of the merge in the destination or target branch.
    commitId: string
    -- The tree ID of the merge in the destination or target branch.
    treeId: string
end

local record MergeBranchesByThreeWayInput
    -- The name of the repository where you want to merge two branches.
    repositoryName: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    sourceCommitSpecifier: string
    -- The branch, tag, HEAD, or other fully qualified reference used to identify a commit
    -- (for example, a branch name or a full commit ID).
    destinationCommitSpecifier: string
    -- The branch where the merge is applied.
    targetBranch: string
    -- The level of conflict detail to use. If unspecified, the default FILE_LEVEL is used,
    -- which returns a not-mergeable result if the same file has differences in both branches.
    -- If LINE_LEVEL is specified, a conflict is considered not mergeable if the same file in
    -- both branches has differences on the same line.
    conflictDetailLevel: string
    -- Specifies which branch to use when resolving conflicts, or whether to attempt
    -- automatically merging two versions of a file. The default is NONE, which requires any
    -- conflicts to be resolved manually before the merge operation is successful.
    conflictResolutionStrategy: string
    -- The name of the author who created the commit. This information is used as both the
    -- author and committer for the commit.
    authorName: string
    -- The email address of the person merging the branches. This information is used in the
    -- commit information for the merge.
    email: string
    -- The commit message to include in the commit information for the merge.
    commitMessage: string
    -- If the commit contains deletions, whether to keep a folder or folder structure if the
    -- changes leave the folders empty. If true, a .gitkeep file is created for empty folders.
    -- The default is false.
    keepEmptyFolders: boolean
    -- If AUTOMERGE is the conflict resolution strategy, a list of inputs to use when
    -- resolving conflicts during a merge.
    conflictResolution: ConflictResolution
end

local record MergeBranchesByThreeWayOutput
    -- The commit ID of the merge in the destination or target branch.
    commitId: string
    -- The tree ID of the merge in the destination or target branch.
    treeId: string
end

local record MergePullRequestByFastForwardInput
    -- The system-generated ID of the pull request. To get this ID, use ListPullRequests.
    pullRequestId: string
    -- The name of the repository where the pull request was created.
    repositoryName: string
    -- The full commit ID of the original or updated commit in the pull request source branch. Pass this value if you want an
    -- exception thrown if the current commit ID of the tip of the source branch does not match this commit ID.
    sourceCommitId: string
end

local record MergePullRequestByFastForwardOutput
    -- Information about the specified pull request, including the merge.
    pullRequest: PullRequest
end

local record MergePullRequestBySquashInput
    -- The system-generated ID of the pull request. To get this ID, use ListPullRequests.
    pullRequestId: string
    -- The name of the repository where the pull request was created.
    repositoryName: string
    -- The full commit ID of the original or updated commit in the pull request source branch. Pass this value if you want an
    -- exception thrown if the current commit ID of the tip of the source branch does not match this commit ID.
    sourceCommitId: string
    -- The level of conflict detail to use. If unspecified, the default FILE_LEVEL is used,
    -- which returns a not-mergeable result if the same file has differences in both branches.
    -- If LINE_LEVEL is specified, a conflict is considered not mergeable if the same file in
    -- both branches has differences on the same line.
    conflictDetailLevel: string
    -- Specifies which branch to use when resolving conflicts, or whether to attempt
    -- automatically merging two versions of a file. The default is NONE, which requires any
    -- conflicts to be resolved manually before the merge operation is successful.
    conflictResolutionStrategy: string
    -- The commit message to include in the commit information for the merge.
    commitMessage: string
    -- The name of the author who created the commit. This information is used as both the
    -- author and committer for the commit.
    authorName: string
    -- The email address of the person merging the branches. This information is used in the
    -- commit information for the merge.
    email: string
    -- If the commit contains deletions, whether to keep a folder or folder structure if the
    -- changes leave the folders empty. If true, a .gitkeep file is created for empty folders.
    -- The default is false.
    keepEmptyFolders: boolean
    -- If AUTOMERGE is the conflict resolution strategy, a list of inputs to use when
    -- resolving conflicts during a merge.
    conflictResolution: ConflictResolution
end

local record MergePullRequestBySquashOutput
    pullRequest: PullRequest
end

local record MergePullRequestByThreeWayInput
    -- The system-generated ID of the pull request. To get this ID, use ListPullRequests.
    pullRequestId: string
    -- The name of the repository where the pull request was created.
    repositoryName: string
    -- The full commit ID of the original or updated commit in the pull request source branch. Pass this value if you want an
    -- exception thrown if the current commit ID of the tip of the source branch does not match this commit ID.
    sourceCommitId: string
    -- The level of conflict detail to use. If unspecified, the default FILE_LEVEL is used,
    -- which returns a not-mergeable result if the same file has differences in both branches.
    -- If LINE_LEVEL is specified, a conflict is considered not mergeable if the same file in
    -- both branches has differences on the same line.
    conflictDetailLevel: string
    -- Specifies which branch to use when resolving conflicts, or whether to attempt
    -- automatically merging two versions of a file. The default is NONE, which requires any
    -- conflicts to be resolved manually before the merge operation is successful.
    conflictResolutionStrategy: string
    -- The commit message to include in the commit information for the merge.
    commitMessage: string
    -- The name of the author who created the commit. This information is used as both the
    -- author and committer for the commit.
    authorName: string
    -- The email address of the person merging the branches. This information is used in the
    -- commit information for the merge.
    email: string
    -- If the commit contains deletions, whether to keep a folder or folder structure if the
    -- changes leave the folders empty. If true, a .gitkeep file is created for empty folders.
    -- The default is false.
    keepEmptyFolders: boolean
    -- If AUTOMERGE is the conflict resolution strategy, a list of inputs to use when
    -- resolving conflicts during a merge.
    conflictResolution: ConflictResolution
end

local record MergePullRequestByThreeWayOutput
    pullRequest: PullRequest
end

local record OverridePullRequestApprovalRulesInput
    -- The system-generated ID of the pull request for which you want to override all
    -- approval rule requirements. To get this information, use
    -- GetPullRequest.
    pullRequestId: string
    -- The system-generated ID of the most recent revision of the pull request. You cannot override approval rules for anything but the most recent revision of a pull request.
    -- To get the revision ID, use GetPullRequest.
    revisionId: string
    -- Whether you want to set aside approval rule requirements for the pull request (OVERRIDE) or revoke a previous override and apply
    -- approval rule requirements (REVOKE). REVOKE status is not stored.
    overrideStatus: string
end

local record OverridePullRequestApprovalRulesOutput
end

local record PostCommentForComparedCommitInput
    -- The name of the repository where you want to post a comment on the comparison between commits.
    repositoryName: string
    -- To establish the directionality of the comparison, the full commit ID of the before
    -- commit. Required for commenting on any commit unless that commit is the initial
    -- commit.
    beforeCommitId: string
    -- To establish the directionality of the comparison, the full commit ID of the after
    -- commit.
    afterCommitId: string
    -- The location of the comparison where you want to comment.
    location: Location
    -- The content of the comment you want to make.
    content: string
    -- A unique, client-generated idempotency token that, when provided in a request, ensures
    -- the request cannot be repeated with a changed parameter. If a request is received with
    -- the same parameters and a token is included, the request returns information about the
    -- initial request that used that token.
    clientRequestToken: string
end

local record PostCommentForComparedCommitOutput
    -- The name of the repository where you posted a comment on the comparison between commits.
    repositoryName: string
    -- In the directionality you established, the full commit ID of the before commit.
    beforeCommitId: string
    -- In the directionality you established, the full commit ID of the after commit.
    afterCommitId: string
    -- In the directionality you established, the blob ID of the before blob.
    beforeBlobId: string
    -- In the directionality you established, the blob ID of the after blob.
    afterBlobId: string
    -- The location of the comment in the comparison between the two commits.
    location: Location
    -- The content of the comment you posted.
    comment: Comment
end

local record PostCommentForPullRequestInput
    -- The system-generated ID of the pull request. To get this ID, use ListPullRequests.
    pullRequestId: string
    -- The name of the repository where you want to post a comment on a pull request.
    repositoryName: string
    -- The full commit ID of the commit in the destination branch that was the tip of the branch at the time the pull request was created.
    beforeCommitId: string
    -- The full commit ID of the commit in the source branch that is the current tip of the branch for the pull request when you post the comment.
    afterCommitId: string
    -- The location of the change where you want to post your comment. If no location is
    -- provided, the comment is posted as a general comment on the pull request difference
    -- between the before commit ID and the after commit ID.
    location: Location
    -- The content of your comment on the change.
    content: string
    -- A unique, client-generated idempotency token that, when provided in a request, ensures
    -- the request cannot be repeated with a changed parameter. If a request is received with
    -- the same parameters and a token is included, the request returns information about the
    -- initial request that used that token.
    clientRequestToken: string
end

local record PostCommentForPullRequestOutput
    -- The name of the repository where you posted a comment on a pull request.
    repositoryName: string
    -- The system-generated ID of the pull request.
    pullRequestId: string
    -- The full commit ID of the commit in the source branch used to create the pull request,
    -- or in the case of an updated pull request, the full commit ID of the commit used to update the pull request.
    beforeCommitId: string
    -- The full commit ID of the commit in the destination branch where the pull request is
    -- merged.
    afterCommitId: string
    -- In the directionality of the pull request, the blob ID of the before blob.
    beforeBlobId: string
    -- In the directionality of the pull request, the blob ID of the after blob.
    afterBlobId: string
    -- The location of the change where you posted your comment.
    location: Location
    -- The content of the comment you posted.
    comment: Comment
end

local record PostCommentReplyInput
    -- The system-generated ID of the comment to which you want to reply. To get this ID, use GetCommentsForComparedCommit
    -- or GetCommentsForPullRequest.
    inReplyTo: string
    -- A unique, client-generated idempotency token that, when provided in a request, ensures
    -- the request cannot be repeated with a changed parameter. If a request is received with
    -- the same parameters and a token is included, the request returns information about the
    -- initial request that used that token.
    clientRequestToken: string
    -- The contents of your reply to a comment.
    content: string
end

local record PostCommentReplyOutput
    -- Information about the reply to a comment.
    comment: Comment
end

local record PutCommentReactionInput
    -- The ID of the comment to which you want to add or update a reaction.
    commentId: string
    -- The emoji reaction you want to add or update. To remove a reaction, provide a value of blank or null. You can also provide the value of none.
    -- For information about emoji reaction values supported in CodeCommit, see the CodeCommit User Guide.
    reactionValue: string
end

local record PutCommentReactionOutput
end

local record PutFileInput
    -- The name of the repository where you want to add or update the file.
    repositoryName: string
    -- The name of the branch where you want to add or update the file. If this is an empty
    -- repository, this branch is created.
    branchName: string
    -- The content of the file, in binary object format.
    fileContent: string
    -- The name of the file you want to add or update, including the relative path to the file in the repository.
    -- If the path does not currently exist in the repository, the path is created as part of adding
    -- the file.
    filePath: string
    -- The file mode permissions of the blob. Valid file mode permissions are listed
    -- here.
    fileMode: string
    -- The full commit ID of the head commit in the branch where you want to add or update the file. If this is an empty repository,
    -- no commit ID is required. If this is not an empty repository, a commit ID is required.
    -- The commit ID must match the ID of the head commit at the time of the operation.
    -- Otherwise, an error occurs, and the file is not added or updated.
    parentCommitId: string
    -- A message about why this file was added or updated. Although it is optional, a message
    -- makes the commit history for your repository more useful.
    commitMessage: string
    -- The name of the person adding or updating the file. Although it is optional, a name
    -- makes the commit history for your repository more useful.
    name: string
    -- An email address for the person adding or updating the file.
    email: string
end

local record PutFileOutput
    -- The full SHA ID of the commit that contains this file change.
    commitId: string
    -- The ID of the blob, which is its SHA-1 pointer.
    blobId: string
    -- The full SHA-1 pointer of the tree information for the commit that contains this file change.
    treeId: string
end

-- Represents the input of a put repository triggers operation.
local record PutRepositoryTriggersInput
    -- The name of the repository where you want to create or update the trigger.
    repositoryName: string
    -- The JSON block of configuration information for each trigger.
    triggers: {RepositoryTrigger}
end

-- Represents the output of a put repository triggers operation.
local record PutRepositoryTriggersOutput
    -- The system-generated unique ID for the create or update operation.
    configurationId: string
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the resource to which you want to add or update tags.
    resourceArn: string
    -- The key-value pair to use when tagging this repository.
    tags: {string : string}
end

local record TagResourceOutput
end

-- Represents the input of a test repository triggers operation.
local record TestRepositoryTriggersInput
    -- The name of the repository in which to test the triggers.
    repositoryName: string
    -- The list of triggers to test.
    triggers: {RepositoryTrigger}
end

-- Represents the output of a test repository triggers operation.
local record TestRepositoryTriggersOutput
    -- The list of triggers that were successfully tested. This list provides the names of the triggers that were successfully tested, separated by commas.
    successfulExecutions: {string}
    -- The list of triggers that were not tested. This list provides the names of the
    -- triggers that could not be tested, separated by commas.
    failedExecutions: {RepositoryTriggerExecutionFailure}
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the resource to which you want to remove tags.
    resourceArn: string
    -- The tag key for each tag that you want to remove from the resource.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateApprovalRuleTemplateContentInput
    -- The name of the approval rule template where you want to update the content of the rule.
    approvalRuleTemplateName: string
    -- The content that replaces the existing content of the rule. Content statements must be
    -- complete. You cannot provide only the changes.
    newRuleContent: string
    -- The SHA-256 hash signature for the content of the approval rule. You can retrieve this
    -- information by using
    -- GetPullRequest.
    existingRuleContentSha256: string
end

local record UpdateApprovalRuleTemplateContentOutput
    approvalRuleTemplate: ApprovalRuleTemplate
end

local record UpdateApprovalRuleTemplateDescriptionInput
    -- The name of the template for which you want to update the description.
    approvalRuleTemplateName: string
    -- The updated description of the approval rule template.
    approvalRuleTemplateDescription: string
end

local record UpdateApprovalRuleTemplateDescriptionOutput
    -- The structure and content of the updated approval rule template.
    approvalRuleTemplate: ApprovalRuleTemplate
end

local record UpdateApprovalRuleTemplateNameInput
    -- The current name of the approval rule template.
    oldApprovalRuleTemplateName: string
    -- The new name you want to apply to the approval rule template.
    newApprovalRuleTemplateName: string
end

local record UpdateApprovalRuleTemplateNameOutput
    -- The structure and content of the updated approval rule template.
    approvalRuleTemplate: ApprovalRuleTemplate
end

local record UpdateCommentInput
    -- The system-generated ID of the comment you want to update. To get this ID, use GetCommentsForComparedCommit
    -- or GetCommentsForPullRequest.
    commentId: string
    -- The updated content to replace the existing content of the comment.
    content: string
end

local record UpdateCommentOutput
    -- Information about the updated comment.
    comment: Comment
end

-- Represents the input of an update default branch operation.
local record UpdateDefaultBranchInput
    -- The name of the repository for which you want to set or change the default branch.
    repositoryName: string
    -- The name of the branch to set as the default branch.
    defaultBranchName: string
end

local record UpdateDefaultBranchOutput
end

local record UpdatePullRequestApprovalRuleContentInput
    -- The system-generated ID of the pull request.
    pullRequestId: string
    -- The name of the approval rule you want to update.
    approvalRuleName: string
    -- The SHA-256 hash signature for the content of the approval rule. You can retrieve this
    -- information by using
    -- GetPullRequest.
    existingRuleContentSha256: string
    -- The updated content for the approval rule.
    -- When you update the content of the approval rule, you can specify approvers in an
    -- approval pool in one of two ways:
    -- CodeCommitApprovers: This option only
    -- requires an Amazon Web Services account and a resource. It can be used for both IAM users
    -- and federated access users whose name matches the provided resource name.
    -- This is a very powerful option that offers a great deal of flexibility. For
    -- example, if you specify the Amazon Web Services account 123456789012
    -- and Mary_Major, all of the following are counted as
    -- approvals coming from that user:
    -- An IAM user in the account
    -- (arn:aws:iam::123456789012:user/Mary_Major)
    -- A federated user identified in IAM as Mary_Major
    -- (arn:aws:sts::123456789012:federated-user/Mary_Major)
    -- This option does not recognize an active session of someone assuming the
    -- role of CodeCommitReview with a role session name of
    -- Mary_Major
    -- (arn:aws:sts::123456789012:assumed-role/CodeCommitReview/Mary_Major)
    -- unless you include a wildcard (*Mary_Major).
    -- Fully qualified ARN: This option allows
    -- you to specify the fully qualified Amazon Resource Name (ARN) of the IAM
    -- user or role.
    -- For more information about IAM ARNs, wildcards, and formats, see IAM
    -- Identifiers in the IAM User Guide.
    newRuleContent: string
end

local record UpdatePullRequestApprovalRuleContentOutput
    -- Information about the updated approval rule.
    approvalRule: ApprovalRule
end

local record UpdatePullRequestApprovalStateInput
    -- The system-generated ID of the pull request.
    pullRequestId: string
    -- The system-generated ID of the revision.
    revisionId: string
    -- The approval state to associate with the user on the pull request.
    approvalState: string
end

local record UpdatePullRequestApprovalStateOutput
end

local record UpdatePullRequestDescriptionInput
    -- The system-generated ID of the pull request. To get this ID, use ListPullRequests.
    pullRequestId: string
    -- The updated content of the description for the pull request. This content replaces the
    -- existing description.
    description: string
end

local record UpdatePullRequestDescriptionOutput
    -- Information about the updated pull request.
    pullRequest: PullRequest
end

local record UpdatePullRequestStatusInput
    -- The system-generated ID of the pull request. To get this ID, use ListPullRequests.
    pullRequestId: string
    -- The status of the pull request. The only valid operations are to update the status
    -- from OPEN to OPEN, OPEN to CLOSED or
    -- from CLOSED to CLOSED.
    pullRequestStatus: string
end

local record UpdatePullRequestStatusOutput
    -- Information about the pull request.
    pullRequest: PullRequest
end

local record UpdatePullRequestTitleInput
    -- The system-generated ID of the pull request. To get this ID, use ListPullRequests.
    pullRequestId: string
    -- The updated title of the pull request. This replaces the existing title.
    title: string
end

local record UpdatePullRequestTitleOutput
    -- Information about the updated pull request.
    pullRequest: PullRequest
end

-- Represents the input of an update repository description operation.
local record UpdateRepositoryDescriptionInput
    -- The name of the repository to set or change the comment or description for.
    repositoryName: string
    -- The new comment or description for the specified repository. Repository descriptions are limited to 1,000 characters.
    repositoryDescription: string
end

local record UpdateRepositoryDescriptionOutput
end

local record UpdateRepositoryEncryptionKeyInput
    -- The name of the repository for which you want to update the KMS encryption key used to encrypt and decrypt the repository.
    repositoryName: string
    -- The ID of the encryption key. You can view the ID of an encryption key in the KMS console, or use the KMS APIs to
    -- programmatically retrieve a key ID. For more information about acceptable values for keyID, see
    -- KeyId in the Decrypt API description in
    -- the Key Management Service API Reference.
    kmsKeyId: string
end

local record UpdateRepositoryEncryptionKeyOutput
    -- The ID of the repository.
    repositoryId: string
    -- The ID of the encryption key.
    kmsKeyId: string
    -- The ID of the encryption key formerly used to encrypt and decrypt the repository.
    originalKmsKeyId: string
end

-- Represents the input of an update repository description operation.
local record UpdateRepositoryNameInput
    -- The current name of the repository.
    oldName: string
    -- The new name for the repository.
    newName: string
end

local record UpdateRepositoryNameOutput
end

-- The specified approval rule template does not exist. Verify that the name is correct and that you are signed in to the Amazon Web Services Region where the template
-- was created, and then try again.
local record ApprovalRuleTemplateDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The maximum number of approval rule templates for a repository has been exceeded. You cannot associate more than 25
-- approval rule templates with a repository.
local record MaximumRuleTemplatesAssociatedWithRepositoryException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified approval rule template does not exist. Verify that the name is correct and that you are signed in to the Amazon Web Services Region where the template
-- was created, and then try again.
local record ApprovalRuleTemplateDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- The maximum number of allowed repository names was exceeded. Currently, this number is 100.
local record MaximumRepositoryNamesExceededException
    -- Any message associated with the exception.
    message: string
end

-- At least one repository name object is required, but was not specified.
local record RepositoryNamesRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict detail level is not valid.
local record InvalidConflictDetailLevelException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution strategy is not valid.
local record InvalidConflictResolutionStrategyException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified value for the number of conflict files to return is not valid.
local record InvalidMaxConflictFilesException
    -- Any message associated with the exception.
    message: string
end

-- The specified value for the number of merge hunks to return is not valid.
local record InvalidMaxMergeHunksException
    -- Any message associated with the exception.
    message: string
end

-- The specified merge option is not valid for this operation. Not all merge strategies are supported for all operations.
local record InvalidMergeOptionException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The number of files to load exceeds the allowed limit.
local record MaximumFileContentToLoadExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of items to compare between the source or destination branches and the merge base has exceeded the maximum allowed.
local record MaximumItemsToCompareExceededException
    -- Any message associated with the exception.
    message: string
end

-- A merge option or stategy is required, and none was provided.
local record MergeOptionRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified approval rule template does not exist. Verify that the name is correct and that you are signed in to the Amazon Web Services Region where the template
-- was created, and then try again.
local record ApprovalRuleTemplateDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- The maximum number of allowed repository names was exceeded. Currently, this number is 100.
local record MaximumRepositoryNamesExceededException
    -- Any message associated with the exception.
    message: string
end

-- At least one repository name object is required, but was not specified.
local record RepositoryNamesRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The maximum number of allowed commit IDs in a batch request is 100. Verify that your batch requests contains no more than 100 commit IDs, and then try again.
local record CommitIdsLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- A list of commit IDs is required, but was either not specified or the list was empty.
local record CommitIdsListRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The maximum number of allowed repository names was exceeded. Currently, this number is 100.
local record MaximumRepositoryNamesExceededException
    -- Any message associated with the exception.
    message: string
end

-- At least one repository name object is required, but was not specified.
local record RepositoryNamesRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The content for the approval rule template is empty. You must provide some content for an approval rule template. The content cannot be null.
local record ApprovalRuleTemplateContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- You cannot create an approval rule template with that name because a template with
-- that name already exists in this Amazon Web Services Region for your Amazon Web Services account. Approval rule template
-- names must be unique.
local record ApprovalRuleTemplateNameAlreadyExistsException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The content of the approval rule template is not valid.
local record InvalidApprovalRuleTemplateContentException
    -- Any message associated with the exception.
    message: string
end

-- The description for the approval rule template is not valid because it exceeds the
-- maximum characters allowed for a description. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateDescriptionException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- The maximum number of approval rule templates has been exceeded for this Amazon Web Services Region.
local record NumberOfRuleTemplatesExceededException
    -- Any message associated with the exception.
    message: string
end

-- Cannot create the branch with the specified name because the commit conflicts with an existing branch with the same name.
-- Branch names must be unique.
local record BranchNameExistsException
    -- Any message associated with the exception.
    message: string
end

-- A branch name is required, but was not specified.
local record BranchNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit ID was not specified.
local record CommitIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name is not valid.
local record InvalidBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID is not valid.
local record InvalidCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch does not exist.
local record BranchDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch name is not valid because it is a tag name. Enter the name of a
-- branch in the repository. For a list of valid branch names, use ListBranches.
local record BranchNameIsTagNameException
    -- Any message associated with the exception.
    message: string
end

-- A branch name is required, but was not specified.
local record BranchNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The commit message is too long. Provide a shorter string.
local record CommitMessageLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- A file cannot be added to the repository because the specified path name has the same name as a file that already exists in this repository.
-- Either provide a different name for the file, or specify a different path for the file.
local record DirectoryNameConflictsWithFileNameException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because both a source file and file content have been
-- specified for the same file. You cannot provide both. Either specify a source file or
-- provide the file content directly.
local record FileContentAndSourceFileSpecifiedException
    -- Any message associated with the exception.
    message: string
end

-- The file cannot be added because it is too large. The maximum file size is 6 MB, and
-- the combined file content change size is 7 MB. Consider making these changes using a Git
-- client.
local record FileContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified file does not exist. Verify that you have used the correct file name,
-- full path, and extension.
local record FileDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because no files have been specified as added, updated, or changed (PutFile or DeleteFile) for the commit.
local record FileEntryRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because no file mode has been specified. A file mode is
-- required to update mode permissions for a file.
local record FileModeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A file cannot be added to the repository because the specified file name has the same name as a directory in this repository. Either provide
-- another name for the file, or add the file in a directory that does not match the file name.
local record FileNameConflictsWithDirectoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because a specified file path points to a submodule. Verify that the destination files
-- have valid file paths that do not point to a submodule.
local record FilePathConflictsWithSubmodulePathException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because at least one of the overall changes in the commit results in a
-- folder whose contents exceed the limit of 6 MB. Either reduce the number and size of your changes,
-- or split the changes across multiple folders.
local record FolderContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name is not valid.
local record InvalidBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified deletion parameter is not valid.
local record InvalidDeletionParameterException
    -- Any message associated with the exception.
    message: string
end

-- The specified email address either contains one or more characters that are not allowed, or it exceeds the maximum number of characters
-- allowed for an email address.
local record InvalidEmailException
    -- Any message associated with the exception.
    message: string
end

-- The specified file mode permission is not valid. For a list of valid file mode permissions, see PutFile.
local record InvalidFileModeException
    -- Any message associated with the exception.
    message: string
end

-- The parent commit ID is not valid. The commit ID cannot be empty, and must match the head commit ID for the branch of the repository where you
-- want to add or update a file.
local record InvalidParentCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The number of specified files to change as part of this commit exceeds the maximum number of files
-- that can be changed in a single commit. Consider using a Git client for these changes.
local record MaximumFileEntriesExceededException
    -- Any message associated with the exception.
    message: string
end

-- The user name is not valid because it has exceeded the character limit for author names.
local record NameLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because no changes will be made to the repository as a result of this commit. A commit must contain at least one change.
local record NoChangeException
    -- Any message associated with the exception.
    message: string
end

-- The parent commit ID is not valid because it does not exist. The specified parent commit ID does not exist in the specified branch of the repository.
local record ParentCommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The file could not be added because the provided parent commit ID is not the current tip of the specified branch. To view the full commit ID of the current head
-- of the branch, use GetBranch.
local record ParentCommitIdOutdatedException
    -- Any message associated with the exception.
    message: string
end

-- A parent commit ID is required. To view the full commit ID of a branch in a repository, use GetBranch or a Git command
-- (for example, git pull or git log).
local record ParentCommitIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because one or more files specified in the commit reference both a file and a folder.
local record PutFileEntryConflictException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because one of the changes specifies copying or moving a .gitkeep file.
local record RestrictedSourceFileException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because one or more changes in this commit duplicate actions in the same file path. For example,
-- you cannot make the same delete request to the same file in the same file path twice, or make a delete request and a move request to the same
-- file as part of the same commit.
local record SamePathRequestException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because no source files or file content have been specified for the commit.
local record SourceFileOrContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A client request token is required. A client request token is an unique,
-- client-generated idempotency token that, when provided in a request, ensures the request
-- cannot be repeated with a changed parameter. If a request is received with the same
-- parameters and a token is included, the request returns information about the initial
-- request that used that token.
local record ClientRequestTokenRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The client request token is not valid. Either the token is not in a valid format, or
-- the token has been used in a previous request and cannot be reused.
local record IdempotencyParameterMismatchException
    -- Any message associated with the exception.
    message: string
end

-- The client request token is not valid.
local record InvalidClientRequestTokenException
    -- Any message associated with the exception.
    message: string
end

-- The pull request description is not valid. Descriptions cannot be more than 1,000
-- characters.
local record InvalidDescriptionException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name format is not valid. Reference names must conform to the
-- Git references format (for example, refs/heads/main). For more information, see Git Internals -
-- Git References or consult your Git documentation.
local record InvalidReferenceNameException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The target for the pull request is not valid. A target must contain the full values for the repository name, source branch, and destination branch for the pull request.
local record InvalidTargetException
    -- Any message associated with the exception.
    message: string
end

-- The targets for the pull request is not valid or not in a valid format. Targets are a list of target objects. Each target object must contain the full values for
-- the repository name, source branch, and destination branch for a pull request.
local record InvalidTargetsException
    -- Any message associated with the exception.
    message: string
end

-- The title of the pull request is not valid. Pull request titles cannot exceed 100 characters in length.
local record InvalidTitleException
    -- Any message associated with the exception.
    message: string
end

-- You cannot create the pull request because the repository has too many open pull requests.
-- The maximum number of open pull requests for a repository is 1,000. Close one or more open pull requests, and then try again.
local record MaximumOpenPullRequestsExceededException
    -- Any message associated with the exception.
    message: string
end

-- You cannot include more than one repository in a pull request. Make sure you have specified only one repository name in your request, and then try again.
local record MultipleRepositoriesInPullRequestException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference does not exist. You must provide a full commit ID.
local record ReferenceDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A reference name is required, but none was provided.
local record ReferenceNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference is not a supported type.
local record ReferenceTypeNotSupportedException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The source branch and destination branch for the pull request are the same. You must
-- specify different branches for the source and destination.
local record SourceAndDestinationAreSameException
    -- Any message associated with the exception.
    message: string
end

-- A pull request target is required. It cannot be empty or null. A pull request target must contain the full values for the repository name, source branch, and destination branch for the pull request.
local record TargetRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An array of target objects is required. It cannot be empty or null.
local record TargetsRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A pull request title is required. It cannot be empty or null.
local record TitleRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The content for the approval rule is empty. You must provide some content for an approval rule. The content cannot be null.
local record ApprovalRuleContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule with that name already exists. Approval rule names must be unique
-- within the scope of a pull request.
local record ApprovalRuleNameAlreadyExistsException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule name is required, but was not specified.
local record ApprovalRuleNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The content for the approval rule is not valid.
local record InvalidApprovalRuleContentException
    -- Any message associated with the exception.
    message: string
end

-- The name for the approval rule is not valid.
local record InvalidApprovalRuleNameException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The approval rule cannot be added. The pull request has the maximum number of approval rules associated with it.
local record NumberOfRulesExceededException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status cannot be updated because it is already closed.
local record PullRequestAlreadyClosedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- The Key Management Service encryption key is not valid.
local record EncryptionKeyInvalidIdException
    -- Any message associated with the exception.
    message: string
end

-- A KMS encryption key was used to try and encrypt or decrypt a repository, but either the repository or the key was not
-- in a valid state to support the operation.
local record EncryptionKeyInvalidUsageException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository description is not valid.
local record InvalidRepositoryDescriptionException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified tag is not valid. Key names cannot be prefixed with aws:.
local record InvalidSystemTagUsageException
    -- Any message associated with the exception.
    message: string
end

-- The map of tags is not valid.
local record InvalidTagsMapException
    -- Any message associated with the exception.
    message: string
end

-- The requested action is not allowed.
local record OperationNotAllowedException
    -- Any message associated with the exception.
    message: string
end

-- A repository resource limit was exceeded.
local record RepositoryLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository name already exists.
local record RepositoryNameExistsException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The tag policy is not valid.
local record TagPolicyException
    -- Any message associated with the exception.
    message: string
end

-- The maximum number of tags for an CodeCommit resource has been exceeded.
local record TooManyTagsException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The commit message is too long. Provide a shorter string.
local record CommitMessageLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The merge cannot be completed because the target branch has been modified. Another user might have modified the target branch while the merge was in progress. Wait a few minutes, and then try again.
local record ConcurrentReferenceUpdateException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The file cannot be added because it is too large. The maximum file size is 6 MB, and
-- the combined file content change size is 7 MB. Consider making these changes using a Git
-- client.
local record FileContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because no file mode has been specified. A file mode is
-- required to update mode permissions for a file.
local record FileModeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because at least one of the overall changes in the commit results in a
-- folder whose contents exceed the limit of 6 MB. Either reduce the number and size of your changes,
-- or split the changes across multiple folders.
local record FolderContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict detail level is not valid.
local record InvalidConflictDetailLevelException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution list is not valid.
local record InvalidConflictResolutionException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution strategy is not valid.
local record InvalidConflictResolutionStrategyException
    -- Any message associated with the exception.
    message: string
end

-- The specified email address either contains one or more characters that are not allowed, or it exceeds the maximum number of characters
-- allowed for an email address.
local record InvalidEmailException
    -- Any message associated with the exception.
    message: string
end

-- The specified file mode permission is not valid. For a list of valid file mode permissions, see PutFile.
local record InvalidFileModeException
    -- Any message associated with the exception.
    message: string
end

-- The specified merge option is not valid for this operation. Not all merge strategies are supported for all operations.
local record InvalidMergeOptionException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- Automerge was specified for resolving the conflict, but the replacement type is not valid or content is missing.
local record InvalidReplacementContentException
    -- Any message associated with the exception.
    message: string
end

-- Automerge was specified for resolving the conflict, but the specified replacement type is not valid.
local record InvalidReplacementTypeException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The pull request cannot be merged automatically into the destination branch. You must manually merge the branches and resolve any conflicts.
local record ManualMergeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The number of allowed conflict resolution entries was exceeded.
local record MaximumConflictResolutionEntriesExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of files to load exceeds the allowed limit.
local record MaximumFileContentToLoadExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of items to compare between the source or destination branches and the merge base has exceeded the maximum allowed.
local record MaximumItemsToCompareExceededException
    -- Any message associated with the exception.
    message: string
end

-- A merge option or stategy is required, and none was provided.
local record MergeOptionRequiredException
    -- Any message associated with the exception.
    message: string
end

-- More than one conflict resolution entries exists for the conflict. A conflict can have only one conflict resolution entry.
local record MultipleConflictResolutionEntriesException
    -- Any message associated with the exception.
    message: string
end

-- The user name is not valid because it has exceeded the character limit for author names.
local record NameLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- USE_NEW_CONTENT was specified, but no replacement content has been provided.
local record ReplacementContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A replacement type is required.
local record ReplacementTypeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- The approval rule template is associated with one or more repositories. You cannot delete a template that is associated with a repository. Remove
-- all associations, and then try again.
local record ApprovalRuleTemplateInUseException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- A branch name is required, but was not specified.
local record BranchNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch is the default branch for the repository, and cannot be deleted. To delete this branch, you must first set another branch as the default branch.
local record DefaultBranchCannotBeDeletedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name is not valid.
local record InvalidBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- This comment has already been deleted. You cannot edit or delete a deleted comment.
local record CommentDeletedException
    -- Any message associated with the exception.
    message: string
end

-- No comment exists with the provided ID. Verify that you have used the correct ID, and
-- then try again.
local record CommentDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is missing or null. A comment ID is required.
local record CommentIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is not in a valid format. Make sure that you have provided the full comment ID.
local record InvalidCommentIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch does not exist.
local record BranchDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch name is not valid because it is a tag name. Enter the name of a
-- branch in the repository. For a list of valid branch names, use ListBranches.
local record BranchNameIsTagNameException
    -- Any message associated with the exception.
    message: string
end

-- A branch name is required, but was not specified.
local record BranchNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The commit message is too long. Provide a shorter string.
local record CommitMessageLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified file does not exist. Verify that you have used the correct file name,
-- full path, and extension.
local record FileDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name is not valid.
local record InvalidBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified email address either contains one or more characters that are not allowed, or it exceeds the maximum number of characters
-- allowed for an email address.
local record InvalidEmailException
    -- Any message associated with the exception.
    message: string
end

-- The parent commit ID is not valid. The commit ID cannot be empty, and must match the head commit ID for the branch of the repository where you
-- want to add or update a file.
local record InvalidParentCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The user name is not valid because it has exceeded the character limit for author names.
local record NameLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- The parent commit ID is not valid because it does not exist. The specified parent commit ID does not exist in the specified branch of the repository.
local record ParentCommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The file could not be added because the provided parent commit ID is not the current tip of the specified branch. To view the full commit ID of the current head
-- of the branch, use GetBranch.
local record ParentCommitIdOutdatedException
    -- Any message associated with the exception.
    message: string
end

-- A parent commit ID is required. To view the full commit ID of a branch in a repository, use GetBranch or a Git command
-- (for example, git pull or git log).
local record ParentCommitIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule name is required, but was not specified.
local record ApprovalRuleNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The approval rule cannot be deleted from the pull request because it was created by an
-- approval rule template and applied to the pull request automatically.
local record CannotDeleteApprovalRuleFromTemplateException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The name for the approval rule is not valid.
local record InvalidApprovalRuleNameException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status cannot be updated because it is already closed.
local record PullRequestAlreadyClosedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified file does not exist. Verify that you have used the correct file name,
-- full path, and extension.
local record FileDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict detail level is not valid.
local record InvalidConflictDetailLevelException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution strategy is not valid.
local record InvalidConflictResolutionStrategyException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified value for the number of merge hunks to return is not valid.
local record InvalidMaxMergeHunksException
    -- Any message associated with the exception.
    message: string
end

-- The specified merge option is not valid for this operation. Not all merge strategies are supported for all operations.
local record InvalidMergeOptionException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The number of files to load exceeds the allowed limit.
local record MaximumFileContentToLoadExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of items to compare between the source or destination branches and the merge base has exceeded the maximum allowed.
local record MaximumItemsToCompareExceededException
    -- Any message associated with the exception.
    message: string
end

-- A merge option or stategy is required, and none was provided.
local record MergeOptionRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified Amazon Resource Name (ARN) does not exist in the Amazon Web Services account.
local record ActorDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The Amazon Resource Name (ARN) is not valid. Make sure that you have provided the full ARN for the user who initiated the change for the pull request,
-- and then try again.
local record InvalidActorArnException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified number of maximum results is not valid.
local record InvalidMaxResultsException
    -- Any message associated with the exception.
    message: string
end

-- The pull request event type is not valid.
local record InvalidPullRequestEventTypeException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified approval rule template does not exist. Verify that the name is correct and that you are signed in to the Amazon Web Services Region where the template
-- was created, and then try again.
local record ApprovalRuleTemplateDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The revision ID is not valid. Use GetPullRequest to determine the value.
local record InvalidRevisionIdException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A revision ID is required, but was not provided.
local record RevisionIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The revision ID provided in the request does not match the current revision ID. Use GetPullRequest to retrieve the current revision ID.
local record RevisionNotCurrentException
    -- Any message associated with the exception.
    message: string
end

-- The specified approval rule template does not exist. Verify that the name is correct and that you are signed in to the Amazon Web Services Region where the template
-- was created, and then try again.
local record ApprovalRuleTemplateDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified blob does not exist.
local record BlobIdDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A blob ID is required, but was not specified.
local record BlobIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified file exceeds the file size limit for CodeCommit. For more information about limits in CodeCommit, see
-- Quotas in the CodeCommit User Guide.
local record FileTooLargeException
    -- Any message associated with the exception.
    message: string
end

-- The specified blob is not valid.
local record InvalidBlobIdException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch does not exist.
local record BranchDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A branch name is required, but was not specified.
local record BranchNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name is not valid.
local record InvalidBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- This comment has already been deleted. You cannot edit or delete a deleted comment.
local record CommentDeletedException
    -- Any message associated with the exception.
    message: string
end

-- No comment exists with the provided ID. Verify that you have used the correct ID, and
-- then try again.
local record CommentDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is missing or null. A comment ID is required.
local record CommentIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is not in a valid format. Make sure that you have provided the full comment ID.
local record InvalidCommentIdException
    -- Any message associated with the exception.
    message: string
end

-- This comment has already been deleted. You cannot edit or delete a deleted comment.
local record CommentDeletedException
    -- Any message associated with the exception.
    message: string
end

-- No comment exists with the provided ID. Verify that you have used the correct ID, and
-- then try again.
local record CommentDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is missing or null. A comment ID is required.
local record CommentIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is not in a valid format. Make sure that you have provided the full comment ID.
local record InvalidCommentIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified number of maximum results is not valid.
local record InvalidMaxResultsException
    -- Any message associated with the exception.
    message: string
end

-- The Amazon Resource Name (ARN) of the user or identity is not valid.
local record InvalidReactionUserArnException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit ID was not specified.
local record CommitIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID is not valid.
local record InvalidCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified number of maximum results is not valid.
local record InvalidMaxResultsException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit ID was not specified.
local record CommitIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID is not valid.
local record InvalidCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified number of maximum results is not valid.
local record InvalidMaxResultsException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The repository does not contain any pull requests with that pull request ID. Use GetPullRequest to verify the correct repository name for the pull request ID.
local record RepositoryNotAssociatedWithPullRequestException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID does not exist.
local record CommitIdDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit ID was not specified.
local record CommitIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID is not valid.
local record InvalidCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID is not valid.
local record InvalidCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified number of maximum results is not valid.
local record InvalidMaxResultsException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified path does not exist.
local record PathDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified file does not exist. Verify that you have used the correct file name,
-- full path, and extension.
local record FileDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified file exceeds the file size limit for CodeCommit. For more information about limits in CodeCommit, see
-- Quotas in the CodeCommit User Guide.
local record FileTooLargeException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified folder does not exist. Either the folder name is not correct, or you did
-- not enter the full path to the folder.
local record FolderDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict detail level is not valid.
local record InvalidConflictDetailLevelException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution strategy is not valid.
local record InvalidConflictResolutionStrategyException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict detail level is not valid.
local record InvalidConflictDetailLevelException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution strategy is not valid.
local record InvalidConflictResolutionStrategyException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The destination commit specifier is not valid. You must provide a valid branch name, tag, or full commit ID.
local record InvalidDestinationCommitSpecifierException
    -- Any message associated with the exception.
    message: string
end

-- The specified value for the number of conflict files to return is not valid.
local record InvalidMaxConflictFilesException
    -- Any message associated with the exception.
    message: string
end

-- The specified merge option is not valid for this operation. Not all merge strategies are supported for all operations.
local record InvalidMergeOptionException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The source commit specifier is not valid. You must provide a valid branch name, tag, or full commit ID.
local record InvalidSourceCommitSpecifierException
    -- Any message associated with the exception.
    message: string
end

-- The number of files to load exceeds the allowed limit.
local record MaximumFileContentToLoadExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of items to compare between the source or destination branches and the merge base has exceeded the maximum allowed.
local record MaximumItemsToCompareExceededException
    -- Any message associated with the exception.
    message: string
end

-- A merge option or stategy is required, and none was provided.
local record MergeOptionRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict detail level is not valid.
local record InvalidConflictDetailLevelException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution strategy is not valid.
local record InvalidConflictResolutionStrategyException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The number of files to load exceeds the allowed limit.
local record MaximumFileContentToLoadExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of items to compare between the source or destination branches and the merge base has exceeded the maximum allowed.
local record MaximumItemsToCompareExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The revision ID is not valid. Use GetPullRequest to determine the value.
local record InvalidRevisionIdException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A revision ID is required, but was not provided.
local record RevisionIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The revision ID is not valid. Use GetPullRequest to determine the value.
local record InvalidRevisionIdException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A revision ID is required, but was not provided.
local record RevisionIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified number of maximum results is not valid.
local record InvalidMaxResultsException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified number of maximum results is not valid.
local record InvalidMaxResultsException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified number of maximum results is not valid.
local record InvalidMaxResultsException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified Amazon Resource Name (ARN) does not exist in the Amazon Web Services account.
local record AuthorDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The Amazon Resource Name (ARN) is not valid. Make sure that you have provided the full ARN for the author of the pull request, and then try again.
local record InvalidAuthorArnException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified number of maximum results is not valid.
local record InvalidMaxResultsException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status is not valid. The only valid values are OPEN and CLOSED.
local record InvalidPullRequestStatusException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified sort order is not valid.
local record InvalidOrderException
    -- Any message associated with the exception.
    message: string
end

-- The specified sort by value is not valid.
local record InvalidSortByException
    -- Any message associated with the exception.
    message: string
end

-- The specified approval rule template does not exist. Verify that the name is correct and that you are signed in to the Amazon Web Services Region where the template
-- was created, and then try again.
local record ApprovalRuleTemplateDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified continuation token is not valid.
local record InvalidContinuationTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified number of maximum results is not valid.
local record InvalidMaxResultsException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The value for the resource ARN is not valid. For more information about resources in CodeCommit, see
-- CodeCommit Resources and Operations
-- in the CodeCommit User Guide.
local record InvalidResourceArnException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A valid Amazon Resource Name (ARN) for an CodeCommit resource is required. For a list of valid resources in CodeCommit, see
-- CodeCommit Resources and Operations
-- in the CodeCommit User Guide.
local record ResourceArnRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch does not exist.
local record BranchDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch name is not valid because it is a tag name. Enter the name of a
-- branch in the repository. For a list of valid branch names, use ListBranches.
local record BranchNameIsTagNameException
    -- Any message associated with the exception.
    message: string
end

-- A branch name is required, but was not specified.
local record BranchNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The merge cannot be completed because the target branch has been modified. Another user might have modified the target branch while the merge was in progress. Wait a few minutes, and then try again.
local record ConcurrentReferenceUpdateException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name is not valid.
local record InvalidBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified target branch is not valid.
local record InvalidTargetBranchException
    -- Any message associated with the exception.
    message: string
end

-- The pull request cannot be merged automatically into the destination branch. You must manually merge the branches and resolve any conflicts.
local record ManualMergeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch does not exist.
local record BranchDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch name is not valid because it is a tag name. Enter the name of a
-- branch in the repository. For a list of valid branch names, use ListBranches.
local record BranchNameIsTagNameException
    -- Any message associated with the exception.
    message: string
end

-- A branch name is required, but was not specified.
local record BranchNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The commit message is too long. Provide a shorter string.
local record CommitMessageLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The merge cannot be completed because the target branch has been modified. Another user might have modified the target branch while the merge was in progress. Wait a few minutes, and then try again.
local record ConcurrentReferenceUpdateException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The file cannot be added because it is too large. The maximum file size is 6 MB, and
-- the combined file content change size is 7 MB. Consider making these changes using a Git
-- client.
local record FileContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because no file mode has been specified. A file mode is
-- required to update mode permissions for a file.
local record FileModeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because at least one of the overall changes in the commit results in a
-- folder whose contents exceed the limit of 6 MB. Either reduce the number and size of your changes,
-- or split the changes across multiple folders.
local record FolderContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name is not valid.
local record InvalidBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict detail level is not valid.
local record InvalidConflictDetailLevelException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution list is not valid.
local record InvalidConflictResolutionException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution strategy is not valid.
local record InvalidConflictResolutionStrategyException
    -- Any message associated with the exception.
    message: string
end

-- The specified email address either contains one or more characters that are not allowed, or it exceeds the maximum number of characters
-- allowed for an email address.
local record InvalidEmailException
    -- Any message associated with the exception.
    message: string
end

-- The specified file mode permission is not valid. For a list of valid file mode permissions, see PutFile.
local record InvalidFileModeException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- Automerge was specified for resolving the conflict, but the replacement type is not valid or content is missing.
local record InvalidReplacementContentException
    -- Any message associated with the exception.
    message: string
end

-- Automerge was specified for resolving the conflict, but the specified replacement type is not valid.
local record InvalidReplacementTypeException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified target branch is not valid.
local record InvalidTargetBranchException
    -- Any message associated with the exception.
    message: string
end

-- The pull request cannot be merged automatically into the destination branch. You must manually merge the branches and resolve any conflicts.
local record ManualMergeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The number of allowed conflict resolution entries was exceeded.
local record MaximumConflictResolutionEntriesExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of files to load exceeds the allowed limit.
local record MaximumFileContentToLoadExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of items to compare between the source or destination branches and the merge base has exceeded the maximum allowed.
local record MaximumItemsToCompareExceededException
    -- Any message associated with the exception.
    message: string
end

-- More than one conflict resolution entries exists for the conflict. A conflict can have only one conflict resolution entry.
local record MultipleConflictResolutionEntriesException
    -- Any message associated with the exception.
    message: string
end

-- The user name is not valid because it has exceeded the character limit for author names.
local record NameLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- USE_NEW_CONTENT was specified, but no replacement content has been provided.
local record ReplacementContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A replacement type is required.
local record ReplacementTypeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch does not exist.
local record BranchDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch name is not valid because it is a tag name. Enter the name of a
-- branch in the repository. For a list of valid branch names, use ListBranches.
local record BranchNameIsTagNameException
    -- Any message associated with the exception.
    message: string
end

-- A branch name is required, but was not specified.
local record BranchNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The commit message is too long. Provide a shorter string.
local record CommitMessageLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- A commit was not specified.
local record CommitRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The merge cannot be completed because the target branch has been modified. Another user might have modified the target branch while the merge was in progress. Wait a few minutes, and then try again.
local record ConcurrentReferenceUpdateException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The file cannot be added because it is too large. The maximum file size is 6 MB, and
-- the combined file content change size is 7 MB. Consider making these changes using a Git
-- client.
local record FileContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because no file mode has been specified. A file mode is
-- required to update mode permissions for a file.
local record FileModeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because at least one of the overall changes in the commit results in a
-- folder whose contents exceed the limit of 6 MB. Either reduce the number and size of your changes,
-- or split the changes across multiple folders.
local record FolderContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name is not valid.
local record InvalidBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit is not valid.
local record InvalidCommitException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict detail level is not valid.
local record InvalidConflictDetailLevelException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution list is not valid.
local record InvalidConflictResolutionException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution strategy is not valid.
local record InvalidConflictResolutionStrategyException
    -- Any message associated with the exception.
    message: string
end

-- The specified email address either contains one or more characters that are not allowed, or it exceeds the maximum number of characters
-- allowed for an email address.
local record InvalidEmailException
    -- Any message associated with the exception.
    message: string
end

-- The specified file mode permission is not valid. For a list of valid file mode permissions, see PutFile.
local record InvalidFileModeException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- Automerge was specified for resolving the conflict, but the replacement type is not valid or content is missing.
local record InvalidReplacementContentException
    -- Any message associated with the exception.
    message: string
end

-- Automerge was specified for resolving the conflict, but the specified replacement type is not valid.
local record InvalidReplacementTypeException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified target branch is not valid.
local record InvalidTargetBranchException
    -- Any message associated with the exception.
    message: string
end

-- The pull request cannot be merged automatically into the destination branch. You must manually merge the branches and resolve any conflicts.
local record ManualMergeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The number of allowed conflict resolution entries was exceeded.
local record MaximumConflictResolutionEntriesExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of files to load exceeds the allowed limit.
local record MaximumFileContentToLoadExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of items to compare between the source or destination branches and the merge base has exceeded the maximum allowed.
local record MaximumItemsToCompareExceededException
    -- Any message associated with the exception.
    message: string
end

-- More than one conflict resolution entries exists for the conflict. A conflict can have only one conflict resolution entry.
local record MultipleConflictResolutionEntriesException
    -- Any message associated with the exception.
    message: string
end

-- The user name is not valid because it has exceeded the character limit for author names.
local record NameLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- USE_NEW_CONTENT was specified, but no replacement content has been provided.
local record ReplacementContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A replacement type is required.
local record ReplacementTypeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- The merge cannot be completed because the target branch has been modified. Another user might have modified the target branch while the merge was in progress. Wait a few minutes, and then try again.
local record ConcurrentReferenceUpdateException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID is not valid.
local record InvalidCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The pull request cannot be merged automatically into the destination branch. You must manually merge the branches and resolve any conflicts.
local record ManualMergeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status cannot be updated because it is already closed.
local record PullRequestAlreadyClosedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request cannot be merged because one or more approval rules applied to the pull request have conditions that have not been met.
local record PullRequestApprovalRulesNotSatisfiedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference does not exist. You must provide a full commit ID.
local record ReferenceDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The repository does not contain any pull requests with that pull request ID. Use GetPullRequest to verify the correct repository name for the pull request ID.
local record RepositoryNotAssociatedWithPullRequestException
    -- Any message associated with the exception.
    message: string
end

-- The tip of the source branch in the destination repository does not match the tip of the source branch specified in your request.
-- The pull request might have been updated. Make sure that you have the latest changes.
local record TipOfSourceReferenceIsDifferentException
    -- Any message associated with the exception.
    message: string
end

-- The commit message is too long. Provide a shorter string.
local record CommitMessageLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- The merge cannot be completed because the target branch has been modified. Another user might have modified the target branch while the merge was in progress. Wait a few minutes, and then try again.
local record ConcurrentReferenceUpdateException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The file cannot be added because it is too large. The maximum file size is 6 MB, and
-- the combined file content change size is 7 MB. Consider making these changes using a Git
-- client.
local record FileContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because at least one of the overall changes in the commit results in a
-- folder whose contents exceed the limit of 6 MB. Either reduce the number and size of your changes,
-- or split the changes across multiple folders.
local record FolderContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID is not valid.
local record InvalidCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict detail level is not valid.
local record InvalidConflictDetailLevelException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution list is not valid.
local record InvalidConflictResolutionException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution strategy is not valid.
local record InvalidConflictResolutionStrategyException
    -- Any message associated with the exception.
    message: string
end

-- The specified email address either contains one or more characters that are not allowed, or it exceeds the maximum number of characters
-- allowed for an email address.
local record InvalidEmailException
    -- Any message associated with the exception.
    message: string
end

-- The specified file mode permission is not valid. For a list of valid file mode permissions, see PutFile.
local record InvalidFileModeException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- Automerge was specified for resolving the conflict, but the replacement type is not valid or content is missing.
local record InvalidReplacementContentException
    -- Any message associated with the exception.
    message: string
end

-- Automerge was specified for resolving the conflict, but the specified replacement type is not valid.
local record InvalidReplacementTypeException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The pull request cannot be merged automatically into the destination branch. You must manually merge the branches and resolve any conflicts.
local record ManualMergeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The number of allowed conflict resolution entries was exceeded.
local record MaximumConflictResolutionEntriesExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of files to load exceeds the allowed limit.
local record MaximumFileContentToLoadExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of items to compare between the source or destination branches and the merge base has exceeded the maximum allowed.
local record MaximumItemsToCompareExceededException
    -- Any message associated with the exception.
    message: string
end

-- More than one conflict resolution entries exists for the conflict. A conflict can have only one conflict resolution entry.
local record MultipleConflictResolutionEntriesException
    -- Any message associated with the exception.
    message: string
end

-- The user name is not valid because it has exceeded the character limit for author names.
local record NameLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status cannot be updated because it is already closed.
local record PullRequestAlreadyClosedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request cannot be merged because one or more approval rules applied to the pull request have conditions that have not been met.
local record PullRequestApprovalRulesNotSatisfiedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- USE_NEW_CONTENT was specified, but no replacement content has been provided.
local record ReplacementContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A replacement type is required.
local record ReplacementTypeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The repository does not contain any pull requests with that pull request ID. Use GetPullRequest to verify the correct repository name for the pull request ID.
local record RepositoryNotAssociatedWithPullRequestException
    -- Any message associated with the exception.
    message: string
end

-- The tip of the source branch in the destination repository does not match the tip of the source branch specified in your request.
-- The pull request might have been updated. Make sure that you have the latest changes.
local record TipOfSourceReferenceIsDifferentException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- The commit message is too long. Provide a shorter string.
local record CommitMessageLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- The merge cannot be completed because the target branch has been modified. Another user might have modified the target branch while the merge was in progress. Wait a few minutes, and then try again.
local record ConcurrentReferenceUpdateException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The file cannot be added because it is too large. The maximum file size is 6 MB, and
-- the combined file content change size is 7 MB. Consider making these changes using a Git
-- client.
local record FileContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because at least one of the overall changes in the commit results in a
-- folder whose contents exceed the limit of 6 MB. Either reduce the number and size of your changes,
-- or split the changes across multiple folders.
local record FolderContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID is not valid.
local record InvalidCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict detail level is not valid.
local record InvalidConflictDetailLevelException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution list is not valid.
local record InvalidConflictResolutionException
    -- Any message associated with the exception.
    message: string
end

-- The specified conflict resolution strategy is not valid.
local record InvalidConflictResolutionStrategyException
    -- Any message associated with the exception.
    message: string
end

-- The specified email address either contains one or more characters that are not allowed, or it exceeds the maximum number of characters
-- allowed for an email address.
local record InvalidEmailException
    -- Any message associated with the exception.
    message: string
end

-- The specified file mode permission is not valid. For a list of valid file mode permissions, see PutFile.
local record InvalidFileModeException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- Automerge was specified for resolving the conflict, but the replacement type is not valid or content is missing.
local record InvalidReplacementContentException
    -- Any message associated with the exception.
    message: string
end

-- Automerge was specified for resolving the conflict, but the specified replacement type is not valid.
local record InvalidReplacementTypeException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The pull request cannot be merged automatically into the destination branch. You must manually merge the branches and resolve any conflicts.
local record ManualMergeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The number of allowed conflict resolution entries was exceeded.
local record MaximumConflictResolutionEntriesExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of files to load exceeds the allowed limit.
local record MaximumFileContentToLoadExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of items to compare between the source or destination branches and the merge base has exceeded the maximum allowed.
local record MaximumItemsToCompareExceededException
    -- Any message associated with the exception.
    message: string
end

-- More than one conflict resolution entries exists for the conflict. A conflict can have only one conflict resolution entry.
local record MultipleConflictResolutionEntriesException
    -- Any message associated with the exception.
    message: string
end

-- The user name is not valid because it has exceeded the character limit for author names.
local record NameLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status cannot be updated because it is already closed.
local record PullRequestAlreadyClosedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request cannot be merged because one or more approval rules applied to the pull request have conditions that have not been met.
local record PullRequestApprovalRulesNotSatisfiedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- USE_NEW_CONTENT was specified, but no replacement content has been provided.
local record ReplacementContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A replacement type is required.
local record ReplacementTypeRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The repository does not contain any pull requests with that pull request ID. Use GetPullRequest to verify the correct repository name for the pull request ID.
local record RepositoryNotAssociatedWithPullRequestException
    -- Any message associated with the exception.
    message: string
end

-- The tip of the source branch in the destination repository does not match the tip of the source branch specified in your request.
-- The pull request might have been updated. Make sure that you have the latest changes.
local record TipOfSourceReferenceIsDifferentException
    -- Any message associated with the exception.
    message: string
end

-- The divergence between the tips of the provided commit specifiers is too great to determine whether there might be
-- any merge conflicts. Locally compare the specifiers using git diff or a diff tool.
local record TipsDivergenceExceededException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The override status is not valid. Valid statuses are OVERRIDE and REVOKE.
local record InvalidOverrideStatusException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The revision ID is not valid. Use GetPullRequest to determine the value.
local record InvalidRevisionIdException
    -- Any message associated with the exception.
    message: string
end

-- The pull request has already had its approval rules set to override.
local record OverrideAlreadySetException
    -- Any message associated with the exception.
    message: string
end

-- An override status is required, but no value was provided. Valid values include OVERRIDE and REVOKE.
local record OverrideStatusRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status cannot be updated because it is already closed.
local record PullRequestAlreadyClosedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A revision ID is required, but was not provided.
local record RevisionIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The revision ID provided in the request does not match the current revision ID. Use GetPullRequest to retrieve the current revision ID.
local record RevisionNotCurrentException
    -- Any message associated with the exception.
    message: string
end

-- The before commit ID and the after commit ID are the same, which is not valid. The before commit ID and the after commit ID must be different commit IDs.
local record BeforeCommitIdAndAfterCommitIdAreSameException
    -- Any message associated with the exception.
    message: string
end

-- A client request token is required. A client request token is an unique,
-- client-generated idempotency token that, when provided in a request, ensures the request
-- cannot be repeated with a changed parameter. If a request is received with the same
-- parameters and a token is included, the request returns information about the initial
-- request that used that token.
local record ClientRequestTokenRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The comment is empty. You must provide some content for a comment. The content cannot be null.
local record CommentContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The comment is too large. Comments are limited to 10,240 characters.
local record CommentContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit ID was not specified.
local record CommitIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The client request token is not valid. Either the token is not in a valid format, or
-- the token has been used in a previous request and cannot be reused.
local record IdempotencyParameterMismatchException
    -- Any message associated with the exception.
    message: string
end

-- The client request token is not valid.
local record InvalidClientRequestTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID is not valid.
local record InvalidCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The location of the file is not valid. Make sure that you include the file name and
-- extension.
local record InvalidFileLocationException
    -- Any message associated with the exception.
    message: string
end

-- The position is not valid. Make sure that the line number exists in the version of the file you want to comment on.
local record InvalidFilePositionException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- Either the enum is not in a valid format, or the specified file version enum is not valid in respect to the current file version.
local record InvalidRelativeFileVersionEnumException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified path does not exist.
local record PathDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The before commit ID and the after commit ID are the same, which is not valid. The before commit ID and the after commit ID must be different commit IDs.
local record BeforeCommitIdAndAfterCommitIdAreSameException
    -- Any message associated with the exception.
    message: string
end

-- A client request token is required. A client request token is an unique,
-- client-generated idempotency token that, when provided in a request, ensures the request
-- cannot be repeated with a changed parameter. If a request is received with the same
-- parameters and a token is included, the request returns information about the initial
-- request that used that token.
local record ClientRequestTokenRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The comment is empty. You must provide some content for a comment. The content cannot be null.
local record CommentContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The comment is too large. Comments are limited to 10,240 characters.
local record CommentContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit does not exist or no commit was specified, and the specified repository has no default branch.
local record CommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A commit ID was not specified.
local record CommitIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The client request token is not valid. Either the token is not in a valid format, or
-- the token has been used in a previous request and cannot be reused.
local record IdempotencyParameterMismatchException
    -- Any message associated with the exception.
    message: string
end

-- The client request token is not valid.
local record InvalidClientRequestTokenException
    -- Any message associated with the exception.
    message: string
end

-- The specified commit ID is not valid.
local record InvalidCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The location of the file is not valid. Make sure that you include the file name and
-- extension.
local record InvalidFileLocationException
    -- Any message associated with the exception.
    message: string
end

-- The position is not valid. Make sure that the line number exists in the version of the file you want to comment on.
local record InvalidFilePositionException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- Either the enum is not in a valid format, or the specified file version enum is not valid in respect to the current file version.
local record InvalidRelativeFileVersionEnumException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified path does not exist.
local record PathDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The repository does not contain any pull requests with that pull request ID. Use GetPullRequest to verify the correct repository name for the pull request ID.
local record RepositoryNotAssociatedWithPullRequestException
    -- Any message associated with the exception.
    message: string
end

-- A client request token is required. A client request token is an unique,
-- client-generated idempotency token that, when provided in a request, ensures the request
-- cannot be repeated with a changed parameter. If a request is received with the same
-- parameters and a token is included, the request returns information about the initial
-- request that used that token.
local record ClientRequestTokenRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The comment is empty. You must provide some content for a comment. The content cannot be null.
local record CommentContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The comment is too large. Comments are limited to 10,240 characters.
local record CommentContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- No comment exists with the provided ID. Verify that you have used the correct ID, and
-- then try again.
local record CommentDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is missing or null. A comment ID is required.
local record CommentIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The client request token is not valid. Either the token is not in a valid format, or
-- the token has been used in a previous request and cannot be reused.
local record IdempotencyParameterMismatchException
    -- Any message associated with the exception.
    message: string
end

-- The client request token is not valid.
local record InvalidClientRequestTokenException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is not in a valid format. Make sure that you have provided the full comment ID.
local record InvalidCommentIdException
    -- Any message associated with the exception.
    message: string
end

-- This comment has already been deleted. You cannot edit or delete a deleted comment.
local record CommentDeletedException
    -- Any message associated with the exception.
    message: string
end

-- No comment exists with the provided ID. Verify that you have used the correct ID, and
-- then try again.
local record CommentDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is missing or null. A comment ID is required.
local record CommentIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is not in a valid format. Make sure that you have provided the full comment ID.
local record InvalidCommentIdException
    -- Any message associated with the exception.
    message: string
end

-- The value of the reaction is not valid. For more information, see the CodeCommit User Guide.
local record InvalidReactionValueException
    -- Any message associated with the exception.
    message: string
end

-- The number of reactions has been exceeded. Reactions are limited to one reaction per user for each individual comment ID.
local record ReactionLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- A reaction value is required.
local record ReactionValueRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch does not exist.
local record BranchDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch name is not valid because it is a tag name. Enter the name of a
-- branch in the repository. For a list of valid branch names, use ListBranches.
local record BranchNameIsTagNameException
    -- Any message associated with the exception.
    message: string
end

-- A branch name is required, but was not specified.
local record BranchNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The commit message is too long. Provide a shorter string.
local record CommitMessageLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- A file cannot be added to the repository because the specified path name has the same name as a file that already exists in this repository.
-- Either provide a different name for the file, or specify a different path for the file.
local record DirectoryNameConflictsWithFileNameException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The file cannot be added because it is empty. Empty files cannot be added to the repository with this API.
local record FileContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The file cannot be added because it is too large. The maximum file size is 6 MB, and
-- the combined file content change size is 7 MB. Consider making these changes using a Git
-- client.
local record FileContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- A file cannot be added to the repository because the specified file name has the same name as a directory in this repository. Either provide
-- another name for the file, or add the file in a directory that does not match the file name.
local record FileNameConflictsWithDirectoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because a specified file path points to a submodule. Verify that the destination files
-- have valid file paths that do not point to a submodule.
local record FilePathConflictsWithSubmodulePathException
    -- Any message associated with the exception.
    message: string
end

-- The commit cannot be created because at least one of the overall changes in the commit results in a
-- folder whose contents exceed the limit of 6 MB. Either reduce the number and size of your changes,
-- or split the changes across multiple folders.
local record FolderContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name is not valid.
local record InvalidBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified deletion parameter is not valid.
local record InvalidDeletionParameterException
    -- Any message associated with the exception.
    message: string
end

-- The specified email address either contains one or more characters that are not allowed, or it exceeds the maximum number of characters
-- allowed for an email address.
local record InvalidEmailException
    -- Any message associated with the exception.
    message: string
end

-- The specified file mode permission is not valid. For a list of valid file mode permissions, see PutFile.
local record InvalidFileModeException
    -- Any message associated with the exception.
    message: string
end

-- The parent commit ID is not valid. The commit ID cannot be empty, and must match the head commit ID for the branch of the repository where you
-- want to add or update a file.
local record InvalidParentCommitIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified path is not valid.
local record InvalidPathException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The user name is not valid because it has exceeded the character limit for author names.
local record NameLengthExceededException
    -- Any message associated with the exception.
    message: string
end

-- The parent commit ID is not valid because it does not exist. The specified parent commit ID does not exist in the specified branch of the repository.
local record ParentCommitDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The file could not be added because the provided parent commit ID is not the current tip of the specified branch. To view the full commit ID of the current head
-- of the branch, use GetBranch.
local record ParentCommitIdOutdatedException
    -- Any message associated with the exception.
    message: string
end

-- A parent commit ID is required. To view the full commit ID of a branch in a repository, use GetBranch or a Git command
-- (for example, git pull or git log).
local record ParentCommitIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The folderPath for a location cannot be null.
local record PathRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The file was not added or updated because the content of the file is exactly the same as the content of that file in the repository and branch
-- that you specified.
local record SameFileContentException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- One or more branch names specified for the trigger is not valid.
local record InvalidRepositoryTriggerBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- The custom data provided for the trigger is not valid.
local record InvalidRepositoryTriggerCustomDataException
    -- Any message associated with the exception.
    message: string
end

-- The Amazon Resource Name (ARN) for the trigger is not valid for the specified destination. The most common reason for this error is that the ARN does not meet the requirements for the service type.
local record InvalidRepositoryTriggerDestinationArnException
    -- Any message associated with the exception.
    message: string
end

-- One or more events specified for the trigger is not valid. Check to make sure that all events specified match the requirements for allowed events.
local record InvalidRepositoryTriggerEventsException
    -- Any message associated with the exception.
    message: string
end

-- The name of the trigger is not valid.
local record InvalidRepositoryTriggerNameException
    -- Any message associated with the exception.
    message: string
end

-- The Amazon Web Services Region for the trigger target does not match the Amazon Web Services Region for the
-- repository. Triggers must be created in the same Amazon Web Services Region as the target for the
-- trigger.
local record InvalidRepositoryTriggerRegionException
    -- Any message associated with the exception.
    message: string
end

-- The number of branches for the trigger was exceeded.
local record MaximumBranchesExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of triggers allowed for the repository was exceeded.
local record MaximumRepositoryTriggersExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- At least one branch name is required, but was not specified in the trigger
-- configuration.
local record RepositoryTriggerBranchNameListRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A destination ARN for the target service for the trigger is required, but was not
-- specified.
local record RepositoryTriggerDestinationArnRequiredException
    -- Any message associated with the exception.
    message: string
end

-- At least one event for the trigger is required, but was not specified.
local record RepositoryTriggerEventsListRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A name for the trigger is required, but was not specified.
local record RepositoryTriggerNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The list of triggers for the repository is required, but was not specified.
local record RepositoryTriggersListRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The value for the resource ARN is not valid. For more information about resources in CodeCommit, see
-- CodeCommit Resources and Operations
-- in the CodeCommit User Guide.
local record InvalidResourceArnException
    -- Any message associated with the exception.
    message: string
end

-- The specified tag is not valid. Key names cannot be prefixed with aws:.
local record InvalidSystemTagUsageException
    -- Any message associated with the exception.
    message: string
end

-- The map of tags is not valid.
local record InvalidTagsMapException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A valid Amazon Resource Name (ARN) for an CodeCommit resource is required. For a list of valid resources in CodeCommit, see
-- CodeCommit Resources and Operations
-- in the CodeCommit User Guide.
local record ResourceArnRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The tag policy is not valid.
local record TagPolicyException
    -- Any message associated with the exception.
    message: string
end

-- A map of tags is required.
local record TagsMapRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The maximum number of tags for an CodeCommit resource has been exceeded.
local record TooManyTagsException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- One or more branch names specified for the trigger is not valid.
local record InvalidRepositoryTriggerBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- The custom data provided for the trigger is not valid.
local record InvalidRepositoryTriggerCustomDataException
    -- Any message associated with the exception.
    message: string
end

-- The Amazon Resource Name (ARN) for the trigger is not valid for the specified destination. The most common reason for this error is that the ARN does not meet the requirements for the service type.
local record InvalidRepositoryTriggerDestinationArnException
    -- Any message associated with the exception.
    message: string
end

-- One or more events specified for the trigger is not valid. Check to make sure that all events specified match the requirements for allowed events.
local record InvalidRepositoryTriggerEventsException
    -- Any message associated with the exception.
    message: string
end

-- The name of the trigger is not valid.
local record InvalidRepositoryTriggerNameException
    -- Any message associated with the exception.
    message: string
end

-- The Amazon Web Services Region for the trigger target does not match the Amazon Web Services Region for the
-- repository. Triggers must be created in the same Amazon Web Services Region as the target for the
-- trigger.
local record InvalidRepositoryTriggerRegionException
    -- Any message associated with the exception.
    message: string
end

-- The number of branches for the trigger was exceeded.
local record MaximumBranchesExceededException
    -- Any message associated with the exception.
    message: string
end

-- The number of triggers allowed for the repository was exceeded.
local record MaximumRepositoryTriggersExceededException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- At least one branch name is required, but was not specified in the trigger
-- configuration.
local record RepositoryTriggerBranchNameListRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A destination ARN for the target service for the trigger is required, but was not
-- specified.
local record RepositoryTriggerDestinationArnRequiredException
    -- Any message associated with the exception.
    message: string
end

-- At least one event for the trigger is required, but was not specified.
local record RepositoryTriggerEventsListRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A name for the trigger is required, but was not specified.
local record RepositoryTriggerNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The list of triggers for the repository is required, but was not specified.
local record RepositoryTriggersListRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The value for the resource ARN is not valid. For more information about resources in CodeCommit, see
-- CodeCommit Resources and Operations
-- in the CodeCommit User Guide.
local record InvalidResourceArnException
    -- Any message associated with the exception.
    message: string
end

-- The specified tag is not valid. Key names cannot be prefixed with aws:.
local record InvalidSystemTagUsageException
    -- Any message associated with the exception.
    message: string
end

-- The list of tags is not valid.
local record InvalidTagKeysListException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A valid Amazon Resource Name (ARN) for an CodeCommit resource is required. For a list of valid resources in CodeCommit, see
-- CodeCommit Resources and Operations
-- in the CodeCommit User Guide.
local record ResourceArnRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A list of tag keys is required. The list cannot be empty or null.
local record TagKeysListRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The tag policy is not valid.
local record TagPolicyException
    -- Any message associated with the exception.
    message: string
end

-- The maximum number of tags for an CodeCommit resource has been exceeded.
local record TooManyTagsException
    -- Any message associated with the exception.
    message: string
end

-- The content for the approval rule template is empty. You must provide some content for an approval rule template. The content cannot be null.
local record ApprovalRuleTemplateContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified approval rule template does not exist. Verify that the name is correct and that you are signed in to the Amazon Web Services Region where the template
-- was created, and then try again.
local record ApprovalRuleTemplateDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The content of the approval rule template is not valid.
local record InvalidApprovalRuleTemplateContentException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- The SHA-256 hash signature for the rule content is not valid.
local record InvalidRuleContentSha256Exception
    -- Any message associated with the exception.
    message: string
end

-- The specified approval rule template does not exist. Verify that the name is correct and that you are signed in to the Amazon Web Services Region where the template
-- was created, and then try again.
local record ApprovalRuleTemplateDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The description for the approval rule template is not valid because it exceeds the
-- maximum characters allowed for a description. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateDescriptionException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified approval rule template does not exist. Verify that the name is correct and that you are signed in to the Amazon Web Services Region where the template
-- was created, and then try again.
local record ApprovalRuleTemplateDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- You cannot create an approval rule template with that name because a template with
-- that name already exists in this Amazon Web Services Region for your Amazon Web Services account. Approval rule template
-- names must be unique.
local record ApprovalRuleTemplateNameAlreadyExistsException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule template name is required, but was not specified.
local record ApprovalRuleTemplateNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The name of the approval rule template is not valid. Template names must be between 1
-- and 100 valid characters in length. For more information about limits in CodeCommit,
-- see Quotas in the CodeCommit User Guide.
local record InvalidApprovalRuleTemplateNameException
    -- Any message associated with the exception.
    message: string
end

-- The comment is empty. You must provide some content for a comment. The content cannot be null.
local record CommentContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The comment is too large. Comments are limited to 10,240 characters.
local record CommentContentSizeLimitExceededException
    -- Any message associated with the exception.
    message: string
end

-- This comment has already been deleted. You cannot edit or delete a deleted comment.
local record CommentDeletedException
    -- Any message associated with the exception.
    message: string
end

-- No comment exists with the provided ID. Verify that you have used the correct ID, and
-- then try again.
local record CommentDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is missing or null. A comment ID is required.
local record CommentIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- You cannot modify or delete this comment. Only comment authors can modify or delete their comments.
local record CommentNotCreatedByCallerException
    -- Any message associated with the exception.
    message: string
end

-- The comment ID is not in a valid format. Make sure that you have provided the full comment ID.
local record InvalidCommentIdException
    -- Any message associated with the exception.
    message: string
end

-- The specified branch does not exist.
local record BranchDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A branch name is required, but was not specified.
local record BranchNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified reference name is not valid.
local record InvalidBranchNameException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The content for the approval rule is empty. You must provide some content for an approval rule. The content cannot be null.
local record ApprovalRuleContentRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The specified approval rule does not exist.
local record ApprovalRuleDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- An approval rule name is required, but was not specified.
local record ApprovalRuleNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The approval rule cannot be modified for the pull request because it was created by an
-- approval rule template and applied to the pull request automatically.
local record CannotModifyApprovalRuleFromTemplateException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The content for the approval rule is not valid.
local record InvalidApprovalRuleContentException
    -- Any message associated with the exception.
    message: string
end

-- The name for the approval rule is not valid.
local record InvalidApprovalRuleNameException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The SHA-256 hash signature for the rule content is not valid.
local record InvalidRuleContentSha256Exception
    -- Any message associated with the exception.
    message: string
end

-- The pull request status cannot be updated because it is already closed.
local record PullRequestAlreadyClosedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An approval state is required, but was not specified.
local record ApprovalStateRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The state for the approval is not valid. Valid values include APPROVE and REVOKE.
local record InvalidApprovalStateException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The revision ID is not valid. Use GetPullRequest to determine the value.
local record InvalidRevisionIdException
    -- Any message associated with the exception.
    message: string
end

-- The number of approvals required for the approval rule exceeds the maximum number allowed.
local record MaximumNumberOfApprovalsExceededException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status cannot be updated because it is already closed.
local record PullRequestAlreadyClosedException
    -- Any message associated with the exception.
    message: string
end

-- The approval cannot be applied because the user approving the pull request matches the user who created the pull request. You cannot approve a pull
-- request that you created.
local record PullRequestCannotBeApprovedByAuthorException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A revision ID is required, but was not provided.
local record RevisionIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The revision ID provided in the request does not match the current revision ID. Use GetPullRequest to retrieve the current revision ID.
local record RevisionNotCurrentException
    -- Any message associated with the exception.
    message: string
end

-- The pull request description is not valid. Descriptions cannot be more than 1,000
-- characters.
local record InvalidDescriptionException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status cannot be updated because it is already closed.
local record PullRequestAlreadyClosedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status is not valid. The only valid values are OPEN and CLOSED.
local record InvalidPullRequestStatusException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status update is not valid. The only valid update is from OPEN to CLOSED.
local record InvalidPullRequestStatusUpdateException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A pull request status is required, but none was provided.
local record PullRequestStatusRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID is not valid. Make sure that you have provided the full ID and that the pull request is in the specified repository, and then try again.
local record InvalidPullRequestIdException
    -- Any message associated with the exception.
    message: string
end

-- The title of the pull request is not valid. Pull request titles cannot exceed 100 characters in length.
local record InvalidTitleException
    -- Any message associated with the exception.
    message: string
end

-- The pull request status cannot be updated because it is already closed.
local record PullRequestAlreadyClosedException
    -- Any message associated with the exception.
    message: string
end

-- The pull request ID could not be found. Make sure that you have specified the correct repository name and pull request ID, and then try again.
local record PullRequestDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A pull request ID is required, but none was provided.
local record PullRequestIdRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A pull request title is required. It cannot be empty or null.
local record TitleRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository description is not valid.
local record InvalidRepositoryDescriptionException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- An encryption integrity check failed.
local record EncryptionIntegrityChecksFailedException
    -- Any message associated with the exception.
    message: string
end

-- An encryption key could not be accessed.
local record EncryptionKeyAccessDeniedException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is disabled.
local record EncryptionKeyDisabledException
    -- Any message associated with the exception.
    message: string
end

-- The Key Management Service encryption key is not valid.
local record EncryptionKeyInvalidIdException
    -- Any message associated with the exception.
    message: string
end

-- A KMS encryption key was used to try and encrypt or decrypt a repository, but either the repository or the key was not
-- in a valid state to support the operation.
local record EncryptionKeyInvalidUsageException
    -- Any message associated with the exception.
    message: string
end

-- No encryption key was found.
local record EncryptionKeyNotFoundException
    -- Any message associated with the exception.
    message: string
end

-- A KMS encryption key ID is required but was not specified.
local record EncryptionKeyRequiredException
    -- Any message associated with the exception.
    message: string
end

-- The encryption key is not available.
local record EncryptionKeyUnavailableException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

-- A specified repository name is not valid.
-- This exception occurs only when a specified repository name is not valid. Other
-- exceptions occur when a required repository parameter is missing, or when a
-- specified repository does not exist.
local record InvalidRepositoryNameException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository does not exist.
local record RepositoryDoesNotExistException
    -- Any message associated with the exception.
    message: string
end

-- The specified repository name already exists.
local record RepositoryNameExistsException
    -- Any message associated with the exception.
    message: string
end

-- A repository name is required, but was not specified.
local record RepositoryNameRequiredException
    -- Any message associated with the exception.
    message: string
end

local type ApprovalState = "APPROVE" | "REVOKE"

local type ConflictDetailLevelTypeEnum = "FILE_LEVEL" | "LINE_LEVEL"

local type ConflictResolutionStrategyTypeEnum = "NONE" | "ACCEPT_SOURCE" | "ACCEPT_DESTINATION" | "AUTOMERGE"

local type FileModeTypeEnum = "EXECUTABLE" | "NORMAL" | "SYMLINK"

local type MergeOptionTypeEnum = "FAST_FORWARD_MERGE" | "SQUASH_MERGE" | "THREE_WAY_MERGE"

local type OrderEnum = "ascending" | "descending"

local type OverrideStatus = "OVERRIDE" | "REVOKE"

local type PullRequestEventType = "PULL_REQUEST_CREATED" | "PULL_REQUEST_STATUS_CHANGED" | "PULL_REQUEST_SOURCE_REFERENCE_UPDATED" | "PULL_REQUEST_MERGE_STATE_CHANGED" | "PULL_REQUEST_APPROVAL_RULE_CREATED" | "PULL_REQUEST_APPROVAL_RULE_UPDATED" | "PULL_REQUEST_APPROVAL_RULE_DELETED" | "PULL_REQUEST_APPROVAL_RULE_OVERRIDDEN" | "PULL_REQUEST_APPROVAL_STATE_CHANGED"

local type PullRequestStatusEnum = "OPEN" | "CLOSED"

local type SortByEnum = "repositoryName" | "lastModifiedDate"

local record M
    AssociateApprovalRuleTemplateWithRepositoryInput: AssociateApprovalRuleTemplateWithRepositoryInput
    AssociateApprovalRuleTemplateWithRepositoryOutput: AssociateApprovalRuleTemplateWithRepositoryOutput
    BatchAssociateApprovalRuleTemplateWithRepositoriesInput: BatchAssociateApprovalRuleTemplateWithRepositoriesInput
    BatchAssociateApprovalRuleTemplateWithRepositoriesOutput: BatchAssociateApprovalRuleTemplateWithRepositoriesOutput
    BatchDescribeMergeConflictsInput: BatchDescribeMergeConflictsInput
    BatchDescribeMergeConflictsOutput: BatchDescribeMergeConflictsOutput
    BatchDisassociateApprovalRuleTemplateFromRepositoriesInput: BatchDisassociateApprovalRuleTemplateFromRepositoriesInput
    BatchDisassociateApprovalRuleTemplateFromRepositoriesOutput: BatchDisassociateApprovalRuleTemplateFromRepositoriesOutput
    BatchGetCommitsInput: BatchGetCommitsInput
    BatchGetCommitsOutput: BatchGetCommitsOutput
    BatchGetRepositoriesInput: BatchGetRepositoriesInput
    BatchGetRepositoriesOutput: BatchGetRepositoriesOutput
    CreateApprovalRuleTemplateInput: CreateApprovalRuleTemplateInput
    CreateApprovalRuleTemplateOutput: CreateApprovalRuleTemplateOutput
    CreateBranchInput: CreateBranchInput
    CreateBranchOutput: CreateBranchOutput
    CreateCommitInput: CreateCommitInput
    CreateCommitOutput: CreateCommitOutput
    CreatePullRequestInput: CreatePullRequestInput
    CreatePullRequestOutput: CreatePullRequestOutput
    CreatePullRequestApprovalRuleInput: CreatePullRequestApprovalRuleInput
    CreatePullRequestApprovalRuleOutput: CreatePullRequestApprovalRuleOutput
    CreateRepositoryInput: CreateRepositoryInput
    CreateRepositoryOutput: CreateRepositoryOutput
    CreateUnreferencedMergeCommitInput: CreateUnreferencedMergeCommitInput
    CreateUnreferencedMergeCommitOutput: CreateUnreferencedMergeCommitOutput
    DeleteApprovalRuleTemplateInput: DeleteApprovalRuleTemplateInput
    DeleteApprovalRuleTemplateOutput: DeleteApprovalRuleTemplateOutput
    DeleteBranchInput: DeleteBranchInput
    DeleteBranchOutput: DeleteBranchOutput
    DeleteCommentContentInput: DeleteCommentContentInput
    DeleteCommentContentOutput: DeleteCommentContentOutput
    DeleteFileInput: DeleteFileInput
    DeleteFileOutput: DeleteFileOutput
    DeletePullRequestApprovalRuleInput: DeletePullRequestApprovalRuleInput
    DeletePullRequestApprovalRuleOutput: DeletePullRequestApprovalRuleOutput
    DeleteRepositoryInput: DeleteRepositoryInput
    DeleteRepositoryOutput: DeleteRepositoryOutput
    DescribeMergeConflictsInput: DescribeMergeConflictsInput
    DescribeMergeConflictsOutput: DescribeMergeConflictsOutput
    DescribePullRequestEventsInput: DescribePullRequestEventsInput
    DescribePullRequestEventsOutput: DescribePullRequestEventsOutput
    DisassociateApprovalRuleTemplateFromRepositoryInput: DisassociateApprovalRuleTemplateFromRepositoryInput
    DisassociateApprovalRuleTemplateFromRepositoryOutput: DisassociateApprovalRuleTemplateFromRepositoryOutput
    EvaluatePullRequestApprovalRulesInput: EvaluatePullRequestApprovalRulesInput
    EvaluatePullRequestApprovalRulesOutput: EvaluatePullRequestApprovalRulesOutput
    GetApprovalRuleTemplateInput: GetApprovalRuleTemplateInput
    GetApprovalRuleTemplateOutput: GetApprovalRuleTemplateOutput
    GetBlobInput: GetBlobInput
    GetBlobOutput: GetBlobOutput
    GetBranchInput: GetBranchInput
    GetBranchOutput: GetBranchOutput
    GetCommentInput: GetCommentInput
    GetCommentOutput: GetCommentOutput
    GetCommentReactionsInput: GetCommentReactionsInput
    GetCommentReactionsOutput: GetCommentReactionsOutput
    GetCommentsForComparedCommitInput: GetCommentsForComparedCommitInput
    GetCommentsForComparedCommitOutput: GetCommentsForComparedCommitOutput
    GetCommentsForPullRequestInput: GetCommentsForPullRequestInput
    GetCommentsForPullRequestOutput: GetCommentsForPullRequestOutput
    GetCommitInput: GetCommitInput
    GetCommitOutput: GetCommitOutput
    GetDifferencesInput: GetDifferencesInput
    GetDifferencesOutput: GetDifferencesOutput
    GetFileInput: GetFileInput
    GetFileOutput: GetFileOutput
    GetFolderInput: GetFolderInput
    GetFolderOutput: GetFolderOutput
    GetMergeCommitInput: GetMergeCommitInput
    GetMergeCommitOutput: GetMergeCommitOutput
    GetMergeConflictsInput: GetMergeConflictsInput
    GetMergeConflictsOutput: GetMergeConflictsOutput
    GetMergeOptionsInput: GetMergeOptionsInput
    GetMergeOptionsOutput: GetMergeOptionsOutput
    GetPullRequestInput: GetPullRequestInput
    GetPullRequestOutput: GetPullRequestOutput
    GetPullRequestApprovalStatesInput: GetPullRequestApprovalStatesInput
    GetPullRequestApprovalStatesOutput: GetPullRequestApprovalStatesOutput
    GetPullRequestOverrideStateInput: GetPullRequestOverrideStateInput
    GetPullRequestOverrideStateOutput: GetPullRequestOverrideStateOutput
    GetRepositoryInput: GetRepositoryInput
    GetRepositoryOutput: GetRepositoryOutput
    GetRepositoryTriggersInput: GetRepositoryTriggersInput
    GetRepositoryTriggersOutput: GetRepositoryTriggersOutput
    ListApprovalRuleTemplatesInput: ListApprovalRuleTemplatesInput
    ListApprovalRuleTemplatesOutput: ListApprovalRuleTemplatesOutput
    ListAssociatedApprovalRuleTemplatesForRepositoryInput: ListAssociatedApprovalRuleTemplatesForRepositoryInput
    ListAssociatedApprovalRuleTemplatesForRepositoryOutput: ListAssociatedApprovalRuleTemplatesForRepositoryOutput
    ListBranchesInput: ListBranchesInput
    ListBranchesOutput: ListBranchesOutput
    ListFileCommitHistoryInput: ListFileCommitHistoryInput
    ListFileCommitHistoryOutput: ListFileCommitHistoryOutput
    ListPullRequestsInput: ListPullRequestsInput
    ListPullRequestsOutput: ListPullRequestsOutput
    ListRepositoriesInput: ListRepositoriesInput
    ListRepositoriesOutput: ListRepositoriesOutput
    ListRepositoriesForApprovalRuleTemplateInput: ListRepositoriesForApprovalRuleTemplateInput
    ListRepositoriesForApprovalRuleTemplateOutput: ListRepositoriesForApprovalRuleTemplateOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    MergeBranchesByFastForwardInput: MergeBranchesByFastForwardInput
    MergeBranchesByFastForwardOutput: MergeBranchesByFastForwardOutput
    MergeBranchesBySquashInput: MergeBranchesBySquashInput
    MergeBranchesBySquashOutput: MergeBranchesBySquashOutput
    MergeBranchesByThreeWayInput: MergeBranchesByThreeWayInput
    MergeBranchesByThreeWayOutput: MergeBranchesByThreeWayOutput
    MergePullRequestByFastForwardInput: MergePullRequestByFastForwardInput
    MergePullRequestByFastForwardOutput: MergePullRequestByFastForwardOutput
    MergePullRequestBySquashInput: MergePullRequestBySquashInput
    MergePullRequestBySquashOutput: MergePullRequestBySquashOutput
    MergePullRequestByThreeWayInput: MergePullRequestByThreeWayInput
    MergePullRequestByThreeWayOutput: MergePullRequestByThreeWayOutput
    OverridePullRequestApprovalRulesInput: OverridePullRequestApprovalRulesInput
    OverridePullRequestApprovalRulesOutput: OverridePullRequestApprovalRulesOutput
    PostCommentForComparedCommitInput: PostCommentForComparedCommitInput
    PostCommentForComparedCommitOutput: PostCommentForComparedCommitOutput
    PostCommentForPullRequestInput: PostCommentForPullRequestInput
    PostCommentForPullRequestOutput: PostCommentForPullRequestOutput
    PostCommentReplyInput: PostCommentReplyInput
    PostCommentReplyOutput: PostCommentReplyOutput
    PutCommentReactionInput: PutCommentReactionInput
    PutCommentReactionOutput: PutCommentReactionOutput
    PutFileInput: PutFileInput
    PutFileOutput: PutFileOutput
    PutRepositoryTriggersInput: PutRepositoryTriggersInput
    PutRepositoryTriggersOutput: PutRepositoryTriggersOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    TestRepositoryTriggersInput: TestRepositoryTriggersInput
    TestRepositoryTriggersOutput: TestRepositoryTriggersOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateApprovalRuleTemplateContentInput: UpdateApprovalRuleTemplateContentInput
    UpdateApprovalRuleTemplateContentOutput: UpdateApprovalRuleTemplateContentOutput
    UpdateApprovalRuleTemplateDescriptionInput: UpdateApprovalRuleTemplateDescriptionInput
    UpdateApprovalRuleTemplateDescriptionOutput: UpdateApprovalRuleTemplateDescriptionOutput
    UpdateApprovalRuleTemplateNameInput: UpdateApprovalRuleTemplateNameInput
    UpdateApprovalRuleTemplateNameOutput: UpdateApprovalRuleTemplateNameOutput
    UpdateCommentInput: UpdateCommentInput
    UpdateCommentOutput: UpdateCommentOutput
    UpdateDefaultBranchInput: UpdateDefaultBranchInput
    UpdateDefaultBranchOutput: UpdateDefaultBranchOutput
    UpdatePullRequestApprovalRuleContentInput: UpdatePullRequestApprovalRuleContentInput
    UpdatePullRequestApprovalRuleContentOutput: UpdatePullRequestApprovalRuleContentOutput
    UpdatePullRequestApprovalStateInput: UpdatePullRequestApprovalStateInput
    UpdatePullRequestApprovalStateOutput: UpdatePullRequestApprovalStateOutput
    UpdatePullRequestDescriptionInput: UpdatePullRequestDescriptionInput
    UpdatePullRequestDescriptionOutput: UpdatePullRequestDescriptionOutput
    UpdatePullRequestStatusInput: UpdatePullRequestStatusInput
    UpdatePullRequestStatusOutput: UpdatePullRequestStatusOutput
    UpdatePullRequestTitleInput: UpdatePullRequestTitleInput
    UpdatePullRequestTitleOutput: UpdatePullRequestTitleOutput
    UpdateRepositoryDescriptionInput: UpdateRepositoryDescriptionInput
    UpdateRepositoryDescriptionOutput: UpdateRepositoryDescriptionOutput
    UpdateRepositoryEncryptionKeyInput: UpdateRepositoryEncryptionKeyInput
    UpdateRepositoryEncryptionKeyOutput: UpdateRepositoryEncryptionKeyOutput
    UpdateRepositoryNameInput: UpdateRepositoryNameInput
    UpdateRepositoryNameOutput: UpdateRepositoryNameOutput
end

return M
