-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- You do not have sufficient access to perform this action.
    record AccessDeniedException
        Message: string
    end

    enum AlgorithmNameCloudRemoval
        "INTERPOLATION"
    end

    enum AlgorithmNameGeoMosaic
        "NEAR"
        "BILINEAR"
        "CUBIC"
        "CUBICSPLINE"
        "LANCZOS"
        "AVERAGE"
        "RMS"
        "MODE"
        "MAX"
        "MIN"
        "MED"
        "Q1"
        "Q3"
        "SUM"
    end

    enum AlgorithmNameResampling
        "NEAR"
        "BILINEAR"
        "CUBIC"
        "CUBICSPLINE"
        "LANCZOS"
        "AVERAGE"
        "RMS"
        "MODE"
        "MAX"
        "MIN"
        "MED"
        "Q1"
        "Q3"
        "SUM"
    end

    -- The structure representing Polygon Geometry based on the GeoJson spec.
    record MultiPolygonGeometryInput
        -- The coordinates of the multipolygon geometry.
        Coordinates: {{{{number}}}}
    end

    -- The structure representing Polygon Geometry based on the GeoJson spec.
    record PolygonGeometryInput
        -- Coordinates representing a Polygon based on the GeoJson spec.
        Coordinates: {{{number}}}
    end

    -- A GeoJSON object representing the geographic extent in the coordinate space.
    record AreaOfInterestGeometry
        -- The structure representing Polygon Geometry.
        PolygonGeometry: PolygonGeometryInput
        -- The structure representing the MultiPolygon Geometry.
        MultiPolygonGeometry: MultiPolygonGeometryInput
    end

    -- The geographic extent of the Earth Observation job.
    record AreaOfInterest
        -- A GeoJSON object representing the geographic extent in the coordinate space.
        AreaOfInterestGeometry: AreaOfInterestGeometry
    end

    -- The structure containing the asset properties.
    record AssetValue
        -- Link to the asset object.
        Href: string
    end

    enum OutputType
        "INT32"
        "FLOAT32"
        "INT16"
        "FLOAT64"
        "UINT16"
    end

    -- Represents an arithmetic operation to compute spectral index.
    record Operation
        -- The name of the operation.
        Name: string
        -- Textual representation of the math operation; Equation used to compute the spectral index.
        Equation: string
        -- The type of the operation.
        OutputType: string
    end

    -- Input object defining the custom BandMath indices to compute.
    record CustomIndicesInput
        -- A list of BandMath indices to compute.
        Operations: {Operation}
    end

    -- Input structure for the BandMath operation type.
    -- Defines Predefined and CustomIndices to be computed using BandMath.
    record BandMathConfigInput
        -- One or many of the supported predefined indices to compute.
        -- Allowed values: NDVI, EVI2, MSAVI,
        -- NDWI, NDMI, NDSI, and WDRVI.
        PredefinedIndices: {string}
        -- CustomIndices that are computed.
        CustomIndices: CustomIndicesInput
    end

    -- Input structure for CloudMasking operation type.
    record CloudMaskingConfigInput
    end

    -- Input structure for Cloud Removal Operation type
    record CloudRemovalConfigInput
        -- The name of the algorithm used for cloud removal.
        AlgorithmName: string
        -- The interpolation value you provide for cloud removal.
        InterpolationValue: string
        -- TargetBands to be returned in the output of CloudRemoval operation.
        TargetBands: {string}
    end

    enum ComparisonOperator
        "EQUALS"
        "NOT_EQUALS"
        "STARTS_WITH"
    end

    -- Updating or deleting a resource can cause an inconsistent state.
    record ConflictException
        Message: string
        -- Identifier of the resource affected.
        ResourceId: string
    end

    -- The structure representing the filters supported by a RasterDataCollection.
    record Filter
        -- The name of the filter.
        Name: string
        -- The type of the filter being used.
        Type: string
        -- The minimum value of the filter.
        Minimum: number
        -- The maximum value of the filter.
        Maximum: number
    end

    enum DataCollectionType
        "PUBLIC"
        "PREMIUM"
        "USER"
    end

    -- Response object containing details for a specific RasterDataCollection.
    record RasterDataCollectionMetadata
        -- The name of the raster data collection.
        Name: string
        -- The Amazon Resource Name (ARN) of the raster data collection.
        Arn: string
        -- The type of raster data collection.
        Type: string
        -- A description of the raster data collection.
        Description: string
        -- The description URL of the raster data collection.
        DescriptionPageUrl: string
        -- The list of filters supported by the raster data collection.
        SupportedFilters: {Filter}
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    record DeleteEarthObservationJobInput
        -- The Amazon Resource Name (ARN) of the Earth Observation job being deleted.
        Arn: string
    end

    record DeleteEarthObservationJobOutput
    end

    -- The request processing has failed because of an unknown error, exception, or failure.
    record InternalServerException
        Message: string
        ResourceId: string
    end

    -- The request references a resource which does not exist.
    record ResourceNotFoundException
        Message: string
        -- Identifier of the resource that was not found.
        ResourceId: string
    end

    -- The request was denied due to request throttling.
    record ThrottlingException
        Message: string
        ResourceId: string
    end

    -- The input fails to satisfy the constraints specified by an Amazon Web Services service.
    record ValidationException
        Message: string
        ResourceId: string
    end

    record DeleteVectorEnrichmentJobInput
        -- The Amazon Resource Name (ARN) of the Vector Enrichment job being deleted.
        Arn: string
    end

    record DeleteVectorEnrichmentJobOutput
    end

    -- The structure containing the Amazon S3 path to export the Earth Observation job output.
    record ExportS3DataInput
        -- The URL to the Amazon S3 data input.
        S3Uri: string
        -- The Key Management Service key ID for server-side encryption.
        KmsKeyId: string
    end

    -- The response structure for an OutputConfig returned by an ExportEarthObservationJob.
    record OutputConfigInput
        -- Path to Amazon S3 storage location for the output configuration file.
        S3Data: ExportS3DataInput
    end

    record ExportEarthObservationJobInput
        -- The input Amazon Resource Name (ARN) of the Earth Observation job being exported.
        Arn: string
        -- A unique token that guarantees that the call to this API is idempotent.
        ClientToken: string
        -- The Amazon Resource Name (ARN) of the IAM role that you specified for the job.
        ExecutionRoleArn: string
        -- An object containing information about the output file.
        OutputConfig: OutputConfigInput
        -- The source images provided to the Earth Observation job being exported.
        ExportSourceImages: boolean
    end

    enum EarthObservationJobExportStatus
        "IN_PROGRESS"
        "SUCCEEDED"
        "FAILED"
    end

    record ExportEarthObservationJobOutput
        -- The output Amazon Resource Name (ARN) of the Earth Observation job being exported.
        Arn: string
        -- The creation time.
        CreationTime: number
        -- The status of the results of the Earth Observation job being exported.
        ExportStatus: string
        -- The Amazon Resource Name (ARN) of the IAM role that you specified for the job.
        ExecutionRoleArn: string
        -- An object containing information about the output file.
        OutputConfig: OutputConfigInput
        -- The source images provided to the Earth Observation job being exported.
        ExportSourceImages: boolean
    end

    -- You have exceeded the service quota.
    record ServiceQuotaExceededException
        Message: string
        -- Identifier of the resource affected.
        ResourceId: string
    end

    record GetEarthObservationJobInput
        -- The Amazon Resource Name (ARN) of the Earth Observation job.
        Arn: string
    end

    enum EarthObservationJobErrorType
        "CLIENT_ERROR"
        "SERVER_ERROR"
    end

    -- The structure representing the errors in an EarthObservationJob.
    record EarthObservationJobErrorDetails
        -- The type of error in an Earth Observation job.
        Type: string
        -- A detailed message describing the error in an Earth Observation job.
        Message: string
    end

    enum ExportErrorType
        "CLIENT_ERROR"
        "SERVER_ERROR"
    end

    -- The structure representing the errors in an export EarthObservationJob operation.
    record ExportErrorDetailsOutput
        -- The type of error in an export EarthObservationJob operation.
        Type: string
        -- A detailed message describing the error in an export EarthObservationJob operation.
        Message: string
    end

    -- The structure for returning the export error details in a GetEarthObservationJob.
    record ExportErrorDetails
        -- The structure for returning the export error details while exporting results of an Earth Observation job.
        ExportResults: ExportErrorDetailsOutput
        -- The structure for returning the export error details
        -- while exporting the source images of an Earth Observation job.
        ExportSourceImages: ExportErrorDetailsOutput
    end

    enum LogicalOperator
        "AND"
    end

    -- The structure representing the EoCloudCover filter.
    record EoCloudCoverInput
        -- Lower bound for EoCloudCover.
        LowerBound: number
        -- Upper bound for EoCloudCover.
        UpperBound: number
    end

    -- The structure representing Land Cloud Cover property for Landsat data collection.
    record LandsatCloudCoverLandInput
        -- The minimum value for Land Cloud Cover property filter. This will filter items
        -- having Land Cloud Cover greater than or equal to this value.
        LowerBound: number
        -- The maximum value for Land Cloud Cover property filter.
        -- This will filter items having Land Cloud Cover less than or equal to this value.
        UpperBound: number
    end

    -- The input structure for specifying Platform.
    -- Platform refers to the unique name of the specific platform the
    -- instrument is attached to. For satellites it is the name of the satellite, eg. landsat-8 (Landsat-8), sentinel-2a.
    record PlatformInput
        -- The value of the platform.
        Value: string
        -- The ComparisonOperator to use with PlatformInput.
        ComparisonOperator: string
    end

    -- The input structure for specifying ViewOffNadir property filter.
    -- ViewOffNadir refers to the angle from the sensor between
    -- nadir (straight down) and the scene center. Measured in degrees (0-90).
    record ViewOffNadirInput
        -- The minimum value for ViewOffNadir property filter.
        -- This filters items having ViewOffNadir greater than or equal to this value.
        LowerBound: number
        -- The maximum value for ViewOffNadir property filter.
        -- This filters items having ViewOffNadir lesser than or equal to this value.
        UpperBound: number
    end

    -- The input structure for specifying ViewSunAzimuth property filter.
    -- ViewSunAzimuth refers to the Sun azimuth angle.
    -- From the scene center point on the ground,
    -- this is the angle between truth north and the sun.
    -- Measured clockwise in degrees (0-360).
    record ViewSunAzimuthInput
        -- The minimum value for ViewSunAzimuth property filter.
        -- This filters items having ViewSunAzimuth greater than or equal to this value.
        LowerBound: number
        -- The maximum value for ViewSunAzimuth property filter.
        -- This filters items having ViewSunAzimuth lesser than or equal to this value.
        UpperBound: number
    end

    -- The input structure for specifying ViewSunElevation angle property filter.
    record ViewSunElevationInput
        -- The lower bound to view the sun elevation.
        LowerBound: number
        -- The upper bound to view the sun elevation.
        UpperBound: number
    end

    -- Represents a single searchable property to search on.
    record Property
        -- The structure representing EoCloudCover property filter containing a lower bound and upper bound.
        EoCloudCover: EoCloudCoverInput
        -- The structure representing ViewOffNadir property filter containing a lower bound and upper bound.
        ViewOffNadir: ViewOffNadirInput
        -- The structure representing ViewSunAzimuth property filter containing a lower bound and upper bound.
        ViewSunAzimuth: ViewSunAzimuthInput
        -- The structure representing ViewSunElevation property filter containing a lower bound and upper bound.
        ViewSunElevation: ViewSunElevationInput
        -- The structure representing Platform property filter consisting of value and comparison operator.
        Platform: PlatformInput
        -- The structure representing Land Cloud Cover property filter
        -- for Landsat collection containing a lower bound and upper bound.
        LandsatCloudCoverLand: LandsatCloudCoverLandInput
    end

    -- The structure representing a single PropertyFilter.
    record PropertyFilter
        -- Represents a single property to match with when searching a raster data collection.
        Property: Property
    end

    -- A list of PropertyFilter objects.
    record PropertyFilters
        -- A list of Property Filters.
        Properties: {PropertyFilter}
        -- The Logical Operator used to combine the Property Filters.
        LogicalOperator: string
    end

    -- The output structure of the time range filter.
    record TimeRangeFilterOutput
        -- The starting time for the time range filter.
        StartTime: number
        -- The ending time for the time range filter.
        EndTime: number
    end

    -- The output structure contains the Raster Data Collection Query
    -- input along with some additional metadata.
    record RasterDataCollectionQueryOutput
        -- The ARN of the Raster Data Collection against which the search is done.
        RasterDataCollectionArn: string
        -- The name of the raster data collection.
        RasterDataCollectionName: string
        -- The TimeRange filter used in the search.
        TimeRangeFilter: TimeRangeFilterOutput
        -- The Area of Interest used in the search.
        AreaOfInterest: AreaOfInterest
        -- Property filters used in the search.
        PropertyFilters: PropertyFilters
    end

    -- The InputConfig for an EarthObservationJob response.
    record InputConfigOutput
        -- The Amazon Resource Name (ARN) of the previous Earth Observation job.
        PreviousEarthObservationJobArn: string
        -- The structure representing the RasterDataCollection Query consisting of the Area of Interest,
        -- RasterDataCollectionArn, RasterDataCollectionName, TimeRange, and Property Filters.
        RasterDataCollectionQuery: RasterDataCollectionQueryOutput
    end

    -- Input configuration information for the geomosaic.
    record GeoMosaicConfigInput
        -- The name of the algorithm being used for geomosaic.
        AlgorithmName: string
        -- The target bands for geomosaic.
        TargetBands: {string}
    end

    -- The input structure for Land Cover Operation type.
    record LandCoverSegmentationConfigInput
    end

    enum Unit
        "METERS"
    end

    -- The output resolution (in target georeferenced units)
    -- of the result of the operation
    record UserDefined
        -- The value for output resolution of the result.
        Value: number
        -- The units for output resolution of the result.
        Unit: string
    end

    -- OutputResolution Configuration indicating
    -- the target resolution for the output of Resampling operation.
    record OutputResolutionResamplingInput
        -- User Defined Resolution for the output
        -- of Resampling operation defined by value and unit.
        UserDefined: UserDefined
    end

    -- The structure representing input for resampling operation.
    record ResamplingConfigInput
        -- The structure representing output
        -- resolution (in target georeferenced units) of the result of resampling operation.
        OutputResolution: OutputResolutionResamplingInput
        -- The name of the algorithm used for resampling.
        AlgorithmName: string
        -- Bands used in the operation. If no target bands are specified, it uses all bands available in the input.
        TargetBands: {string}
    end

    enum PredefinedResolution
        "HIGHEST"
        "LOWEST"
        "AVERAGE"
    end

    -- The input structure representing Output Resolution for Stacking Operation.
    record OutputResolutionStackInput
        -- A string value representing Predefined Output Resolution for a stacking operation. Allowed values are HIGHEST, LOWEST, and AVERAGE.
        Predefined: string
        -- The structure representing User Output Resolution for a Stacking operation defined as a value and unit.
        UserDefined: UserDefined
    end

    -- The input structure for Stacking Operation.
    record StackConfigInput
        -- The structure representing output
        -- resolution (in target georeferenced units) of the
        -- result of stacking operation.
        OutputResolution: OutputResolutionStackInput
        -- A list of bands to be stacked in the specified order. When the parameter is not provided, all the available bands in the data collection are stacked in the alphabetical order of their asset names.
        TargetBands: {string}
    end

    enum GroupBy
        "ALL"
        "YEARLY"
    end

    enum TemporalStatistics
        "MEAN"
        "MEDIAN"
        "STANDARD_DEVIATION"
    end

    -- The structure representing the configuration for Temporal Statistics operation.
    record TemporalStatisticsConfigInput
        -- The input for the temporal statistics grouping by time frequency option.
        GroupBy: string
        -- The list of the statistics method options.
        Statistics: {string}
        -- The list of target band names for the temporal statistic to calculate.
        TargetBands: {string}
    end

    enum ZonalStatistics
        "MEAN"
        "MEDIAN"
        "STANDARD_DEVIATION"
        "MAX"
        "MIN"
        "SUM"
    end

    -- The structure representing input configuration of ZonalStatistics operation.
    record ZonalStatisticsConfigInput
        -- The Amazon S3 path pointing to the GeoJSON containing the polygonal zones.
        ZoneS3Path: string
        -- List of zonal statistics to compute.
        Statistics: {string}
        -- Bands used in the operation.
        -- If no target bands are specified, it uses all bands available input.
        TargetBands: {string}
        -- The Amazon Resource Name (ARN) or an ID of a Amazon Web Services Key Management Service (Amazon Web Services KMS) key that Amazon SageMaker uses to decrypt your output artifacts with Amazon S3 server-side encryption.
        -- The SageMaker execution role must have kms:GenerateDataKey permission.
        -- The KmsKeyId can be any of the following formats:
        -- // KMS Key ID
        -- "1234abcd-12ab-34cd-56ef-1234567890ab"
        -- // Amazon Resource Name (ARN) of a KMS Key
        -- "arn:aws:kms:<region>:<account>:key/<key-id-12ab-34cd-56ef-1234567890ab>"
        -- For more information about key identifiers, see
        -- Key identifiers (KeyID) in the
        -- Amazon Web Services Key Management Service (Amazon Web Services KMS) documentation.
        ZoneS3PathKmsKeyId: string
    end

    -- The input structure for the JobConfig in an EarthObservationJob.
    record JobConfigInput
        -- An object containing information about the job configuration for BandMath.
        BandMathConfig: BandMathConfigInput
        -- An object containing information about the job configuration for resampling.
        ResamplingConfig: ResamplingConfigInput
        -- An object containing information about the job configuration for temporal statistics.
        TemporalStatisticsConfig: TemporalStatisticsConfigInput
        -- An object containing information about the job configuration for cloud removal.
        CloudRemovalConfig: CloudRemovalConfigInput
        -- An object containing information about the job configuration for zonal statistics.
        ZonalStatisticsConfig: ZonalStatisticsConfigInput
        -- An object containing information about the job configuration for geomosaic.
        GeoMosaicConfig: GeoMosaicConfigInput
        -- An object containing information about the job configuration for a Stacking Earth Observation job.
        StackConfig: StackConfigInput
        -- An object containing information about the job configuration for cloud masking.
        CloudMaskingConfig: CloudMaskingConfigInput
        -- An object containing information about the job configuration for land cover segmentation.
        LandCoverSegmentationConfig: LandCoverSegmentationConfigInput
    end

    -- A single EarthObservationJob output band.
    record OutputBand
        -- The name of the band.
        BandName: string
        -- The datatype of the output band.
        OutputDataType: string
    end

    enum EarthObservationJobStatus
        "INITIALIZING"
        "IN_PROGRESS"
        "STOPPING"
        "COMPLETED"
        "STOPPED"
        "FAILED"
        "DELETING"
        "DELETED"
    end

    record GetEarthObservationJobOutput
        -- The Amazon Resource Name (ARN) of the Earth Observation job.
        Arn: string
        -- The name of the Earth Observation job.
        Name: string
        -- The creation time of the initiated Earth Observation job.
        CreationTime: number
        -- The duration of Earth Observation job, in seconds.
        DurationInSeconds: number
        -- The status of a previously initiated Earth Observation job.
        Status: string
        -- The Key Management Service key ID for server-side encryption.
        KmsKeyId: string
        -- Input data for the Earth Observation job.
        InputConfig: InputConfigOutput
        -- An object containing information about the job configuration.
        JobConfig: JobConfigInput
        -- Bands available in the output of an operation.
        OutputBands: {OutputBand}
        -- The Amazon Resource Name (ARN) of the IAM role that you specified for the job.
        ExecutionRoleArn: string
        -- Details about the errors generated during the Earth Observation job.
        ErrorDetails: EarthObservationJobErrorDetails
        -- The status of the Earth Observation job.
        ExportStatus: string
        -- Details about the errors generated during ExportEarthObservationJob.
        ExportErrorDetails: ExportErrorDetails
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    enum TargetOptions
        "INPUT"
        "OUTPUT"
    end

    record GetTileInput
        -- The x coordinate of the tile input.
        x: number
        -- The y coordinate of the tile input.
        y: number
        -- The z coordinate of the tile input.
        z: number
        -- The particular assets or bands to tile.
        ImageAssets: {string}
        -- Determines what part of the Earth Observation job to tile. 'INPUT' or 'OUTPUT' are the valid options.
        Target: string
        -- The Amazon Resource Name (ARN) of the tile operation.
        Arn: string
        -- Determines whether or not to return a valid data mask.
        ImageMask: boolean
        -- The data format of the output tile. The formats include .npy, .png and .jpg.
        OutputFormat: string
        -- Time range filter applied to imagery to find the images to tile.
        TimeRangeFilter: string
        -- Property filters for the imagery to tile.
        PropertyFilters: string
        -- The output data type of the tile operation.
        OutputDataType: string
        -- The Amazon Resource Name (ARN) of the IAM role that you specify.
        ExecutionRoleArn: string
    end

    record GetTileOutput
        -- The output binary file.
        BinaryFile: function(): string, string
    end

    enum SortOrder
        "ASCENDING"
        "DESCENDING"
    end

    record ListEarthObservationJobsInput
        -- A filter that retrieves only jobs with a specific status.
        StatusEquals: string
        -- An optional value that specifies whether you want the results sorted in Ascending or Descending order.
        SortOrder: string
        -- The parameter by which to sort the results.
        SortBy: string
        -- If the previous response was truncated, you receive this token.
        -- Use it in your next request to receive the next set of results.
        NextToken: string
        -- The total number of items to return.
        MaxResults: number
    end

    -- An object containing information about the output file.
    record ListEarthObservationJobOutputConfig
        -- The Amazon Resource Name (ARN) of the list of the Earth Observation jobs.
        Arn: string
        -- The names of the Earth Observation jobs in the list.
        Name: string
        -- The creation time.
        CreationTime: number
        -- The duration of the session, in seconds.
        DurationInSeconds: number
        -- The status of the list of the Earth Observation jobs.
        Status: string
        -- The operation type for an Earth Observation job.
        OperationType: string
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    record ListEarthObservationJobsOutput
        -- Contains summary information about the Earth Observation jobs.
        EarthObservationJobSummaries: {ListEarthObservationJobOutputConfig}
        -- If the previous response was truncated, you receive this token.
        -- Use it in your next request to receive the next set of results.
        NextToken: string
    end

    -- The input for the time-range filter.
    record TimeRangeFilterInput
        -- The start time for the time-range filter.
        StartTime: number
        -- The end time for the time-range filter.
        EndTime: number
    end

    -- The input structure for Raster Data Collection Query containing the Area of Interest, TimeRange Filters, and Property Filters.
    record RasterDataCollectionQueryInput
        -- The Amazon Resource Name (ARN) of the raster data collection.
        RasterDataCollectionArn: string
        -- The TimeRange Filter used in the RasterDataCollection Query.
        TimeRangeFilter: TimeRangeFilterInput
        -- The area of interest being queried for the raster data collection.
        AreaOfInterest: AreaOfInterest
        -- The list of Property filters used in the Raster Data Collection Query.
        PropertyFilters: PropertyFilters
    end

    -- Input configuration information.
    record InputConfigInput
        -- The Amazon Resource Name (ARN) of the previous Earth Observation job.
        PreviousEarthObservationJobArn: string
        -- The structure representing the RasterDataCollection Query consisting of
        -- the Area of Interest, RasterDataCollectionArn,TimeRange and Property Filters.
        RasterDataCollectionQuery: RasterDataCollectionQueryInput
    end

    record StartEarthObservationJobInput
        -- The name of the Earth Observation job.
        Name: string
        -- A unique token that guarantees that the call to this API is idempotent.
        ClientToken: string
        -- The Key Management Service key ID for server-side encryption.
        KmsKeyId: string
        -- Input configuration information for the Earth Observation job.
        InputConfig: InputConfigInput
        -- An object containing information about the job configuration.
        JobConfig: JobConfigInput
        -- The Amazon Resource Name (ARN) of the IAM role that you specified for the job.
        ExecutionRoleArn: string
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    record StartEarthObservationJobOutput
        -- The name of the Earth Observation job.
        Name: string
        -- The Amazon Resource Name (ARN) of the Earth Observation job.
        Arn: string
        -- The creation time.
        CreationTime: number
        -- The duration of the session, in seconds.
        DurationInSeconds: number
        -- The status of the Earth Observation job.
        Status: string
        -- The Key Management Service key ID for server-side encryption.
        KmsKeyId: string
        -- Input configuration information for the Earth Observation job.
        InputConfig: InputConfigOutput
        -- An object containing information about the job configuration.
        JobConfig: JobConfigInput
        -- The Amazon Resource Name (ARN) of the IAM role that you specified for the job.
        ExecutionRoleArn: string
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    record StopEarthObservationJobInput
        -- The Amazon Resource Name (ARN) of the Earth Observation job being stopped.
        Arn: string
    end

    record StopEarthObservationJobOutput
    end

    -- The Amazon S3 data for the Vector Enrichment job.
    record VectorEnrichmentJobS3Data
        -- The URL to the Amazon S3 data for the Vector Enrichment job.
        S3Uri: string
        -- The Key Management Service key ID for server-side encryption.
        KmsKeyId: string
    end

    -- An object containing information about the output file.
    record ExportVectorEnrichmentJobOutputConfig
        -- The input structure for Amazon S3 data; representing the Amazon S3 location of the input data objects.
        S3Data: VectorEnrichmentJobS3Data
    end

    record ExportVectorEnrichmentJobInput
        -- The Amazon Resource Name (ARN) of the Vector Enrichment job.
        Arn: string
        -- A unique token that guarantees that the call to this API is idempotent.
        ClientToken: string
        -- The Amazon Resource Name (ARN) of the IAM rolewith permission to upload to the location in OutputConfig.
        ExecutionRoleArn: string
        -- Output location information for exporting Vector Enrichment Job results.
        OutputConfig: ExportVectorEnrichmentJobOutputConfig
    end

    enum VectorEnrichmentJobExportStatus
        "IN_PROGRESS"
        "SUCCEEDED"
        "FAILED"
    end

    record ExportVectorEnrichmentJobOutput
        -- The Amazon Resource Name (ARN) of the Vector Enrichment job being exported.
        Arn: string
        -- The creation time.
        CreationTime: number
        -- The Amazon Resource Name (ARN) of the IAM role with permission to upload to the location in OutputConfig.
        ExecutionRoleArn: string
        -- The status of the results the Vector Enrichment job being exported.
        ExportStatus: string
        -- Output location information for exporting Vector Enrichment Job results.
        OutputConfig: ExportVectorEnrichmentJobOutputConfig
    end

    -- The structure representing a Geometry in
    -- terms of Type and Coordinates as per GeoJson spec.
    record Geometry
        -- GeoJson Geometry types like Polygon and MultiPolygon.
        Type: string
        -- The coordinates of the GeoJson Geometry.
        Coordinates: {{{number}}}
    end

    record GetRasterDataCollectionInput
        -- The Amazon Resource Name (ARN) of the raster data collection.
        Arn: string
    end

    record GetRasterDataCollectionOutput
        -- The name of the raster data collection.
        Name: string
        -- The Amazon Resource Name (ARN) of the raster data collection.
        Arn: string
        -- The raster data collection type.
        Type: string
        -- A description of the raster data collection.
        Description: string
        -- The URL of the description page.
        DescriptionPageUrl: string
        -- The filters supported by the raster data collection.
        SupportedFilters: {Filter}
        -- The list of image source bands in the raster data collection.
        ImageSourceBands: {string}
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    record GetVectorEnrichmentJobInput
        -- The Amazon Resource Name (ARN) of the Vector Enrichment job.
        Arn: string
    end

    enum VectorEnrichmentJobErrorType
        "CLIENT_ERROR"
        "SERVER_ERROR"
    end

    -- VectorEnrichmentJob error details in response from GetVectorEnrichmentJob.
    record VectorEnrichmentJobErrorDetails
        -- The type of error generated during the Vector Enrichment job.
        ErrorType: string
        -- A message that you define and then is processed and rendered by
        -- the Vector Enrichment job when the error occurs.
        ErrorMessage: string
    end

    enum VectorEnrichmentJobExportErrorType
        "CLIENT_ERROR"
        "SERVER_ERROR"
    end

    -- VectorEnrichmentJob export error details in response from GetVectorEnrichmentJob.
    record VectorEnrichmentJobExportErrorDetails
        -- The output error details for an Export operation on a Vector Enrichment job.
        Type: string
        -- The message providing details about the errors generated during the Vector Enrichment job.
        Message: string
    end

    -- The input structure for the data source that represents the storage type of the input data objects.
    record VectorEnrichmentJobDataSourceConfigInput
        -- The input structure for the Amazon S3 data that represents the Amazon S3 location of the input data objects.
        S3Data: VectorEnrichmentJobS3Data
    end

    enum VectorEnrichmentJobDocumentType
        "CSV"
    end

    -- The input structure for the InputConfig in a VectorEnrichmentJob.
    record VectorEnrichmentJobInputConfig
        -- The input structure that defines the data source file type.
        DocumentType: string
        -- The input structure for the data source that represents the storage type of the input data objects.
        DataSourceConfig: VectorEnrichmentJobDataSourceConfigInput
    end

    -- The input structure for Map Matching operation type.
    record MapMatchingConfig
        -- The field name for the data that describes the identifier representing a collection of GPS points belonging to an individual trace.
        IdAttributeName: string
        -- The name of the Y-attribute
        YAttributeName: string
        -- The name of the X-attribute
        XAttributeName: string
        -- The name of the timestamp attribute.
        TimestampAttributeName: string
    end

    -- The input structure for Reverse Geocoding operation type.
    record ReverseGeocodingConfig
        -- The field name for the data that describes y-axis coordinate, eg. latitude of a point.
        YAttributeName: string
        -- The field name for the data that describes x-axis coordinate, eg. longitude of a point.
        XAttributeName: string
    end

    -- It contains configs such as ReverseGeocodingConfig and MapMatchingConfig.
    record VectorEnrichmentJobConfig
        -- The input structure for Reverse Geocoding operation type.
        ReverseGeocodingConfig: ReverseGeocodingConfig
        -- The input structure for Map Matching operation type.
        MapMatchingConfig: MapMatchingConfig
    end

    enum VectorEnrichmentJobStatus
        "INITIALIZING"
        "IN_PROGRESS"
        "STOPPING"
        "STOPPED"
        "COMPLETED"
        "FAILED"
        "DELETING"
        "DELETED"
    end

    enum VectorEnrichmentJobType
        "REVERSE_GEOCODING"
        "MAP_MATCHING"
    end

    record GetVectorEnrichmentJobOutput
        -- The Amazon Resource Name (ARN) of the Vector Enrichment job.
        Arn: string
        -- The type of the Vector Enrichment job being initiated.
        Type: string
        -- The name of the Vector Enrichment job.
        Name: string
        -- The creation time.
        CreationTime: number
        -- The duration of the Vector Enrichment job, in seconds.
        DurationInSeconds: number
        -- The status of the initiated Vector Enrichment job.
        Status: string
        -- The Key Management Service key ID for server-side encryption.
        KmsKeyId: string
        -- Input configuration information for the Vector Enrichment job.
        InputConfig: VectorEnrichmentJobInputConfig
        -- An object containing information about the job configuration.
        JobConfig: VectorEnrichmentJobConfig
        -- The Amazon Resource Name (ARN) of the IAM role that you specified for the job.
        ExecutionRoleArn: string
        -- Details about the errors generated during the Vector Enrichment job.
        ErrorDetails: VectorEnrichmentJobErrorDetails
        -- The export status of the Vector Enrichment job being initiated.
        ExportStatus: string
        -- Details about the errors generated during the ExportVectorEnrichmentJob.
        ExportErrorDetails: VectorEnrichmentJobExportErrorDetails
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    -- Properties associated with the Item.
    record Properties
        -- Estimate of cloud cover.
        EoCloudCover: number
        -- The angle from the sensor between nadir (straight down) and the scene center. Measured in degrees (0-90).
        ViewOffNadir: number
        -- The sun azimuth angle. From the scene center point on the ground, this is the angle between truth north and the sun.
        -- Measured clockwise in degrees (0-360).
        ViewSunAzimuth: number
        -- The sun elevation angle. The angle from the tangent of the scene center point to the sun. Measured from the horizon in degrees (-90-90).
        -- Negative values indicate the sun is below the horizon, e.g. sun elevation of -10° means the data was captured during nautical twilight.
        ViewSunElevation: number
        -- Platform property. Platform refers to the unique name
        -- of the specific platform the instrument is attached to.
        -- For satellites it is the name of
        -- the satellite, eg. landsat-8 (Landsat-8), sentinel-2a.
        Platform: string
        -- Land cloud cover for Landsat Data Collection.
        LandsatCloudCoverLand: number
    end

    -- The structure representing the items in the response for SearchRasterDataCollection.
    record ItemSource
        -- A unique Id for the source item.
        Id: string
        -- The item Geometry in GeoJson format.
        Geometry: Geometry
        -- This is a dictionary of Asset Objects data associated with the Item that
        -- can be downloaded or streamed, each with a unique key.
        Assets: {string : AssetValue}
        -- The searchable date and time of the item, in UTC.
        DateTime: number
        -- This field contains additional properties of the item.
        Properties: Properties
    end

    record ListRasterDataCollectionsInput
        -- If the previous response was truncated, you receive this token.
        -- Use it in your next request to receive the next set of results.
        NextToken: string
        -- The total number of items to return.
        MaxResults: number
    end

    record ListRasterDataCollectionsOutput
        -- Contains summary information about the raster data collection.
        RasterDataCollectionSummaries: {RasterDataCollectionMetadata}
        -- If the previous response was truncated, you receive this token.
        -- Use it in your next request to receive the next set of results.
        NextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource you want to tag.
        ResourceArn: string
    end

    record ListTagsForResourceOutput
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    -- An object containing information about the output file.
    record ListVectorEnrichmentJobOutputConfig
        -- The Amazon Resource Name (ARN) of the list of the Vector Enrichment jobs.
        Arn: string
        -- The names of the Vector Enrichment jobs in the list.
        Name: string
        -- The type of the list of Vector Enrichment jobs.
        Type: string
        -- The creation time.
        CreationTime: number
        -- The duration of the session, in seconds.
        DurationInSeconds: number
        -- The status of the Vector Enrichment jobs list.
        Status: string
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    record ListVectorEnrichmentJobsInput
        -- A filter that retrieves only jobs with a specific status.
        StatusEquals: string
        -- An optional value that specifies whether you want the results sorted in Ascending or Descending order.
        SortOrder: string
        -- The parameter by which to sort the results.
        SortBy: string
        -- If the previous response was truncated, you receive this token.
        -- Use it in your next request to receive the next set of results.
        NextToken: string
        -- The maximum number of items to return.
        MaxResults: number
    end

    record ListVectorEnrichmentJobsOutput
        -- Contains summary information about the Vector Enrichment jobs.
        VectorEnrichmentJobSummaries: {ListVectorEnrichmentJobOutputConfig}
        -- If the previous response was truncated, you receive this token.
        -- Use it in your next request to receive the next set of results.
        NextToken: string
    end

    -- This is a RasterDataCollectionQueryInput containing AreaOfInterest, Time Range filter and Property filters.
    record RasterDataCollectionQueryWithBandFilterInput
        -- The TimeRange Filter used in the search query.
        TimeRangeFilter: TimeRangeFilterInput
        -- The Area of interest to be used in the search query.
        AreaOfInterest: AreaOfInterest
        -- The Property Filters used in the search query.
        PropertyFilters: PropertyFilters
        -- The list of Bands to be displayed in the result for each item.
        BandFilter: {string}
    end

    record SearchRasterDataCollectionInput
        -- The Amazon Resource Name (ARN) of the raster data collection.
        Arn: string
        -- RasterDataCollectionQuery consisting of AreaOfInterest(AOI), PropertyFilters and
        -- TimeRangeFilterInput used in SearchRasterDataCollection.
        RasterDataCollectionQuery: RasterDataCollectionQueryWithBandFilterInput
        -- If the previous response was truncated, you receive this token.
        -- Use it in your next request to receive the next set of results.
        NextToken: string
    end

    record SearchRasterDataCollectionOutput
        -- Approximate number of results in the response.
        ApproximateResultCount: number
        -- If the previous response was truncated, you receive this token.
        -- Use it in your next request to receive the next set of results.
        NextToken: string
        -- List of items matching the Raster DataCollectionQuery.
        Items: {ItemSource}
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource you want to tag.
        ResourceArn: string
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource you want to untag.
        ResourceArn: string
        -- Keys of the tags you want to remove.
        TagKeys: {string}
    end

    record UntagResourceOutput
    end

    record StartVectorEnrichmentJobInput
        -- The name of the Vector Enrichment job.
        Name: string
        -- A unique token that guarantees that the call to this API is idempotent.
        ClientToken: string
        -- The Key Management Service key ID for server-side encryption.
        KmsKeyId: string
        -- Input configuration information for the Vector Enrichment job.
        InputConfig: VectorEnrichmentJobInputConfig
        -- An object containing information about the job configuration.
        JobConfig: VectorEnrichmentJobConfig
        -- The Amazon Resource Name (ARN) of the IAM role that you specified for the job.
        ExecutionRoleArn: string
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    record StartVectorEnrichmentJobOutput
        -- The name of the Vector Enrichment job.
        Name: string
        -- The Amazon Resource Name (ARN) of the Vector Enrichment job.
        Arn: string
        -- The type of the Vector Enrichment job.
        Type: string
        -- The creation time.
        CreationTime: number
        -- The duration of the Vector Enrichment job, in seconds.
        DurationInSeconds: number
        -- The status of the Vector Enrichment job being started.
        Status: string
        -- The Key Management Service key ID for server-side encryption.
        KmsKeyId: string
        -- Input configuration information for starting the Vector Enrichment job.
        InputConfig: VectorEnrichmentJobInputConfig
        -- An object containing information about the job configuration.
        JobConfig: VectorEnrichmentJobConfig
        -- The Amazon Resource Name (ARN) of the IAM role that you specified for the job.
        ExecutionRoleArn: string
        -- Each tag consists of a key and a value.
        Tags: {string : string}
    end

    record StopVectorEnrichmentJobInput
        -- The Amazon Resource Name (ARN) of the Vector Enrichment job.
        Arn: string
    end

    record StopVectorEnrichmentJobOutput
    end

end

return M
