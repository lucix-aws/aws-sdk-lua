-- Code generated by lua-client-codegen. DO NOT EDIT.

local record CreateAccessorInput
    -- This is a unique, case-sensitive identifier that you provide to ensure the idempotency of
    -- the operation. An idempotent operation completes no more than once. This
    -- identifier is required only if you make a service request directly using
    -- an HTTP client. It is generated automatically if you use an Amazon Web Services SDK or the
    -- Amazon Web Services CLI.
    ClientRequestToken: string
    -- The type of accessor.
    -- Currently, accessor type is restricted to BILLING_TOKEN.
    AccessorType: string
    -- Tags to assign to the Accessor.
    -- Each tag consists of a key and an optional value. You can specify
    -- multiple key-value pairs in a single request with an overall maximum of 50 tags
    -- allowed per resource.
    -- For more information about tags, see Tagging Resources in the Amazon Managed Blockchain Ethereum Developer Guide, or Tagging Resources in the Amazon Managed Blockchain Hyperledger Fabric Developer Guide.
    Tags: {string : string}
    -- The blockchain network that the Accessor token is created for.
    -- Use the actual networkType value for the blockchain network that you are creating
    -- the Accessor token for.
    -- With the shut down of the Ethereum Goerli and Polygon Mumbai
    -- Testnet networks the following networkType values are no longer available
    -- for selection and use.
    -- ETHEREUM_MAINNET_AND_GOERLI
    -- ETHEREUM_GOERLI
    -- POLYGON_MUMBAI
    -- However, your existing Accessor tokens with these networkType
    -- values will remain unchanged.
    NetworkType: string
end

local record CreateAccessorOutput
    -- The unique identifier of the accessor.
    AccessorId: string
    -- The billing token is a property of the Accessor. Use this token to
    -- when making calls to the blockchain network. The billing token is used
    -- to track your accessor token for billing requests.
    BillingToken: string
    -- The blockchain network that the accessor token is created for.
    NetworkType: string
end

local record CreateMemberInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the operation. An idempotent operation completes no more than one time. This identifier is required only if you make a service request directly using an HTTP client. It is generated automatically if you use an Amazon Web Services SDK or the CLI.
    ClientRequestToken: string
    -- The unique identifier of the invitation that is sent to the member to join the network.
    InvitationId: string
    -- The unique identifier of the network in which the member is created.
    NetworkId: string
    -- Member configuration parameters.
    MemberConfiguration: MemberConfiguration
end

local record CreateMemberOutput
    -- The unique identifier of the member.
    MemberId: string
end

local record CreateNetworkInput
    -- This is a unique, case-sensitive identifier that you provide to ensure the idempotency of the
    -- operation. An idempotent operation completes no more than once. This identifier is required only
    -- if you make a service request directly using an HTTP client. It is generated automatically if you
    -- use an Amazon Web Services SDK or the Amazon Web Services CLI.
    ClientRequestToken: string
    -- The name of the network.
    Name: string
    -- An optional description for the network.
    Description: string
    -- The blockchain framework that the network uses.
    Framework: string
    -- The version of the blockchain framework that the network uses.
    FrameworkVersion: string
    -- Configuration properties of the blockchain framework relevant to the network configuration.
    FrameworkConfiguration: NetworkFrameworkConfiguration
    -- The voting rules used by the network to determine if a proposal is approved.
    VotingPolicy: VotingPolicy
    -- Configuration properties for the first member within the network.
    MemberConfiguration: MemberConfiguration
    -- Tags to assign to the network.
    -- Each tag consists of a key and an optional value. You can specify
    -- multiple key-value pairs in a single request with an overall maximum of 50 tags
    -- allowed per resource.
    -- For more information about tags, see Tagging Resources in the Amazon Managed Blockchain Ethereum Developer Guide, or Tagging Resources in the Amazon Managed Blockchain Hyperledger Fabric Developer Guide.
    Tags: {string : string}
end

local record CreateNetworkOutput
    -- The unique identifier for the network.
    NetworkId: string
    -- The unique identifier for the first member within the network.
    MemberId: string
end

local record CreateNodeInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the operation. An idempotent operation completes no more than one time. This identifier is required only if you make a service request directly using an HTTP client. It is generated automatically if you use an Amazon Web Services SDK or the CLI.
    ClientRequestToken: string
    -- The unique identifier of the network for the node.
    -- Ethereum public networks have the following NetworkIds:
    -- n-ethereum-mainnet
    NetworkId: string
    -- The unique identifier of the member that owns this node.
    -- Applies only to Hyperledger Fabric.
    MemberId: string
    -- The properties of a node configuration.
    NodeConfiguration: NodeConfiguration
    -- Tags to assign to the node.
    -- Each tag consists of a key and an optional value. You can specify
    -- multiple key-value pairs in a single request with an overall maximum of 50 tags
    -- allowed per resource.
    -- For more information about tags, see Tagging Resources in the Amazon Managed Blockchain Ethereum Developer Guide, or Tagging Resources in the Amazon Managed Blockchain Hyperledger Fabric Developer Guide.
    Tags: {string : string}
end

local record CreateNodeOutput
    -- The unique identifier of the node.
    NodeId: string
end

local record CreateProposalInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the operation. An idempotent operation completes no more than one time. This identifier is required only if you make a service request directly using an HTTP client. It is generated automatically if you use an Amazon Web Services SDK or the CLI.
    ClientRequestToken: string
    -- The unique identifier of the network for which the proposal is made.
    NetworkId: string
    -- The unique identifier of the member that is creating the proposal. This
    -- identifier is especially useful for identifying the member making the proposal
    -- when multiple members exist in a single Amazon Web Services account.
    MemberId: string
    -- The type of actions proposed, such as inviting a member or removing a member. The types of Actions in a proposal are mutually exclusive. For example, a proposal with Invitations actions cannot also contain Removals actions.
    Actions: ProposalActions
    -- A description for the proposal that is visible to voting members, for example, "Proposal to add Example Corp. as member."
    Description: string
    -- Tags to assign to the proposal.
    -- Each tag consists of a key and an optional value. You can specify
    -- multiple key-value pairs in a single request with an overall maximum of 50 tags
    -- allowed per resource.
    -- For more information about tags, see Tagging Resources in the Amazon Managed Blockchain Ethereum Developer Guide, or Tagging Resources in the Amazon Managed Blockchain Hyperledger Fabric Developer Guide.
    Tags: {string : string}
end

local record CreateProposalOutput
    -- The unique identifier of the proposal.
    ProposalId: string
end

local record DeleteAccessorInput
    -- The unique identifier of the accessor.
    AccessorId: string
end

local record DeleteAccessorOutput
end

local record DeleteMemberInput
    -- The unique identifier of the network from which the member is removed.
    NetworkId: string
    -- The unique identifier of the member to remove.
    MemberId: string
end

local record DeleteMemberOutput
end

local record DeleteNodeInput
    -- The unique identifier of the network that the node is on.
    -- Ethereum public networks have the following NetworkIds:
    -- n-ethereum-mainnet
    NetworkId: string
    -- The unique identifier of the member that owns this node.
    -- Applies only to Hyperledger Fabric and is required for Hyperledger Fabric.
    MemberId: string
    -- The unique identifier of the node.
    NodeId: string
end

local record DeleteNodeOutput
end

local record GetAccessorInput
    -- The unique identifier of the accessor.
    AccessorId: string
end

local record GetAccessorOutput
    -- The properties of the accessor.
    Accessor: Accessor
end

local record GetMemberInput
    -- The unique identifier of the network to which the member belongs.
    NetworkId: string
    -- The unique identifier of the member.
    MemberId: string
end

local record GetMemberOutput
    -- The properties of a member.
    Member: Member
end

local record GetNetworkInput
    -- The unique identifier of the network to get information about.
    NetworkId: string
end

local record GetNetworkOutput
    -- An object containing network configuration parameters.
    Network: Network
end

local record GetNodeInput
    -- The unique identifier of the network that the node is on.
    NetworkId: string
    -- The unique identifier of the member that owns the node.
    -- Applies only to Hyperledger Fabric and is required for Hyperledger Fabric.
    MemberId: string
    -- The unique identifier of the node.
    NodeId: string
end

local record GetNodeOutput
    -- Properties of the node configuration.
    Node: Node
end

local record GetProposalInput
    -- The unique identifier of the network for which the proposal is made.
    NetworkId: string
    -- The unique identifier of the proposal.
    ProposalId: string
end

local record GetProposalOutput
    -- Information about a proposal.
    Proposal: Proposal
end

local record ListAccessorsInput
    -- The maximum number of accessors to list.
    MaxResults: number
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
    -- The blockchain network that the Accessor token is created for.
    -- Use the value ETHEREUM_MAINNET_AND_GOERLI for all
    -- existing Accessors tokens that were created before the networkType
    -- property was introduced.
    NetworkType: string
end

local record ListAccessorsOutput
    -- An array of AccessorSummary objects that contain configuration properties for
    -- each accessor.
    Accessors: {AccessorSummary}
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListInvitationsInput
    -- The maximum number of invitations to return.
    MaxResults: number
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListInvitationsOutput
    -- The invitations for the network.
    Invitations: {Invitation}
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListMembersInput
    -- The unique identifier of the network for which to list members.
    NetworkId: string
    -- The optional name of the member to list.
    Name: string
    -- An optional status specifier. If provided, only members currently in this status are listed.
    Status: string
    -- An optional Boolean value. If provided, the request is limited either to
    -- members that the current Amazon Web Services account owns (true) or that other Amazon Web Services accountsn
    -- own (false). If omitted, all members are listed.
    IsOwned: boolean
    -- The maximum number of members to return in the request.
    MaxResults: number
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListMembersOutput
    -- An array of MemberSummary objects. Each object contains details about a network member.
    Members: {MemberSummary}
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListNetworksInput
    -- The name of the network.
    Name: string
    -- An optional framework specifier. If provided, only networks of this framework type are listed.
    Framework: string
    -- An optional status specifier. If provided, only networks currently in this status are listed.
    -- Applies only to Hyperledger Fabric.
    Status: string
    -- The maximum number of networks to list.
    MaxResults: number
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListNetworksOutput
    -- An array of NetworkSummary objects that contain configuration properties for each network.
    Networks: {NetworkSummary}
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListNodesInput
    -- The unique identifier of the network for which to list nodes.
    NetworkId: string
    -- The unique identifier of the member who owns the nodes to list.
    -- Applies only to Hyperledger Fabric and is required for Hyperledger Fabric.
    MemberId: string
    -- An optional status specifier. If provided, only nodes currently in this status are listed.
    Status: string
    -- The maximum number of nodes to list.
    MaxResults: number
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListNodesOutput
    -- An array of NodeSummary objects that contain configuration properties for each node.
    Nodes: {NodeSummary}
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListProposalsInput
    -- The unique identifier of the network.
    NetworkId: string
    -- The maximum number of proposals to return.
    MaxResults: number
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListProposalsOutput
    -- The summary of each proposal made on the network.
    Proposals: {ProposalSummary}
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListProposalVotesInput
    -- The unique identifier of the network.
    NetworkId: string
    -- The unique identifier of the proposal.
    ProposalId: string
    -- The maximum number of votes to return.
    MaxResults: number
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListProposalVotesOutput
    -- The list of votes.
    ProposalVotes: {VoteSummary}
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the resource. For more information about ARNs and their format, see Amazon Resource Names (ARNs) in the Amazon Web Services General Reference.
    ResourceArn: string
end

local record ListTagsForResourceOutput
    -- The tags assigned to the resource.
    Tags: {string : string}
end

local record RejectInvitationInput
    -- The unique identifier of the invitation to reject.
    InvitationId: string
end

local record RejectInvitationOutput
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the resource. For more information about ARNs and their format, see Amazon Resource Names (ARNs) in the Amazon Web Services General Reference.
    ResourceArn: string
    -- The tags to assign to the specified resource. Tag values can be empty, for example, "MyTagKey" : "". You can specify multiple key-value pairs in a single request, with an overall maximum of 50 tags added to each resource.
    Tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the resource. For more information about ARNs and their format, see Amazon Resource Names (ARNs) in the Amazon Web Services General Reference.
    ResourceArn: string
    -- The tag keys.
    TagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateMemberInput
    -- The unique identifier of the Managed Blockchain network to which the member belongs.
    NetworkId: string
    -- The unique identifier of the member.
    MemberId: string
    -- Configuration properties for publishing to Amazon CloudWatch Logs.
    LogPublishingConfiguration: MemberLogPublishingConfiguration
end

local record UpdateMemberOutput
end

local record UpdateNodeInput
    -- The unique identifier of the network that the node is on.
    NetworkId: string
    -- The unique identifier of the member that owns the node.
    -- Applies only to Hyperledger Fabric.
    MemberId: string
    -- The unique identifier of the node.
    NodeId: string
    -- Configuration properties for publishing to Amazon CloudWatch Logs.
    LogPublishingConfiguration: NodeLogPublishingConfiguration
end

local record UpdateNodeOutput
end

local record VoteOnProposalInput
    -- The unique identifier of the network.
    NetworkId: string
    -- The unique identifier of the proposal.
    ProposalId: string
    -- The unique identifier of the member casting the vote.
    VoterMemberId: string
    -- The value of the vote.
    Vote: string
end

local record VoteOnProposalOutput
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A resource request is issued for a resource that already exists.
local record ResourceAlreadyExistsException
    Message: string
end

-- The maximum number of resources of that type already exist. Ensure the resources requested
-- are within the boundaries of the service edition and your account limits.
local record ResourceLimitExceededException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

local record TooManyTagsException
    Message: string
    ResourceName: string
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A resource request is issued for a resource that already exists.
local record ResourceAlreadyExistsException
    Message: string
end

-- The maximum number of resources of that type already exist. Ensure the resources requested
-- are within the boundaries of the service edition and your account limits.
local record ResourceLimitExceededException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The requested resource exists but isn't in a status that can complete the operation.
local record ResourceNotReadyException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

local record TooManyTagsException
    Message: string
    ResourceName: string
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A resource request is issued for a resource that already exists.
local record ResourceAlreadyExistsException
    Message: string
end

-- The maximum number of resources of that type already exist. Ensure the resources requested
-- are within the boundaries of the service edition and your account limits.
local record ResourceLimitExceededException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

local record TooManyTagsException
    Message: string
    ResourceName: string
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A resource request is issued for a resource that already exists.
local record ResourceAlreadyExistsException
    Message: string
end

-- The maximum number of resources of that type already exist. Ensure the resources requested
-- are within the boundaries of the service edition and your account limits.
local record ResourceLimitExceededException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The requested resource exists but isn't in a status that can complete the operation.
local record ResourceNotReadyException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

local record TooManyTagsException
    Message: string
    ResourceName: string
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The requested resource exists but isn't in a status that can complete the operation.
local record ResourceNotReadyException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

local record TooManyTagsException
    Message: string
    ResourceName: string
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The requested resource exists but isn't in a status that can complete the operation.
local record ResourceNotReadyException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The requested resource exists but isn't in a status that can complete the operation.
local record ResourceNotReadyException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- The maximum number of resources of that type already exist. Ensure the resources requested
-- are within the boundaries of the service edition and your account limits.
local record ResourceLimitExceededException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The requested resource exists but isn't in a status that can complete the operation.
local record ResourceNotReadyException
    Message: string
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

local record IllegalActionException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The requested resource exists but isn't in a status that can complete the operation.
local record ResourceNotReadyException
    Message: string
end

local record TooManyTagsException
    Message: string
    ResourceName: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The requested resource exists but isn't in a status that can complete the operation.
local record ResourceNotReadyException
    Message: string
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

-- You don't have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

local record IllegalActionException
    Message: string
end

-- The request processing has failed because of an unknown error, exception or failure.
local record InternalServiceErrorException
end

-- The action or operation requested is invalid. Verify that the action is typed correctly.
local record InvalidRequestException
    Message: string
end

-- A requested resource doesn't exist. It may have been deleted or referenced incorrectly.
local record ResourceNotFoundException
    Message: string
    -- A requested resource doesn't exist. It may have been deleted or referenced inaccurately.
    ResourceName: string
end

-- The request or operation couldn't be performed because a service is
-- throttling requests. The most common source of throttling errors is
-- creating resources that exceed your service limit for this resource type.
-- Request a limit increase or delete unused resources if possible.
local record ThrottlingException
end

local type AccessorNetworkType = "ETHEREUM_GOERLI" | "ETHEREUM_MAINNET" | "ETHEREUM_MAINNET_AND_GOERLI" | "POLYGON_MAINNET" | "POLYGON_MUMBAI"

local type AccessorType = "BILLING_TOKEN"

local type Framework = "HYPERLEDGER_FABRIC" | "ETHEREUM"

local type MemberStatus = "CREATING" | "AVAILABLE" | "CREATE_FAILED" | "UPDATING" | "DELETING" | "DELETED" | "INACCESSIBLE_ENCRYPTION_KEY"

local type NetworkStatus = "CREATING" | "AVAILABLE" | "CREATE_FAILED" | "DELETING" | "DELETED"

local type NodeStatus = "CREATING" | "AVAILABLE" | "UNHEALTHY" | "CREATE_FAILED" | "UPDATING" | "DELETING" | "DELETED" | "FAILED" | "INACCESSIBLE_ENCRYPTION_KEY"

local type VoteValue = "YES" | "NO"

local record M
    CreateAccessorInput: CreateAccessorInput
    CreateAccessorOutput: CreateAccessorOutput
    CreateMemberInput: CreateMemberInput
    CreateMemberOutput: CreateMemberOutput
    CreateNetworkInput: CreateNetworkInput
    CreateNetworkOutput: CreateNetworkOutput
    CreateNodeInput: CreateNodeInput
    CreateNodeOutput: CreateNodeOutput
    CreateProposalInput: CreateProposalInput
    CreateProposalOutput: CreateProposalOutput
    DeleteAccessorInput: DeleteAccessorInput
    DeleteAccessorOutput: DeleteAccessorOutput
    DeleteMemberInput: DeleteMemberInput
    DeleteMemberOutput: DeleteMemberOutput
    DeleteNodeInput: DeleteNodeInput
    DeleteNodeOutput: DeleteNodeOutput
    GetAccessorInput: GetAccessorInput
    GetAccessorOutput: GetAccessorOutput
    GetMemberInput: GetMemberInput
    GetMemberOutput: GetMemberOutput
    GetNetworkInput: GetNetworkInput
    GetNetworkOutput: GetNetworkOutput
    GetNodeInput: GetNodeInput
    GetNodeOutput: GetNodeOutput
    GetProposalInput: GetProposalInput
    GetProposalOutput: GetProposalOutput
    ListAccessorsInput: ListAccessorsInput
    ListAccessorsOutput: ListAccessorsOutput
    ListInvitationsInput: ListInvitationsInput
    ListInvitationsOutput: ListInvitationsOutput
    ListMembersInput: ListMembersInput
    ListMembersOutput: ListMembersOutput
    ListNetworksInput: ListNetworksInput
    ListNetworksOutput: ListNetworksOutput
    ListNodesInput: ListNodesInput
    ListNodesOutput: ListNodesOutput
    ListProposalsInput: ListProposalsInput
    ListProposalsOutput: ListProposalsOutput
    ListProposalVotesInput: ListProposalVotesInput
    ListProposalVotesOutput: ListProposalVotesOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    RejectInvitationInput: RejectInvitationInput
    RejectInvitationOutput: RejectInvitationOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateMemberInput: UpdateMemberInput
    UpdateMemberOutput: UpdateMemberOutput
    UpdateNodeInput: UpdateNodeInput
    UpdateNodeOutput: UpdateNodeOutput
    VoteOnProposalInput: VoteOnProposalInput
    VoteOnProposalOutput: VoteOnProposalOutput
end

return M
