-- Code generated by lua-client-codegen. DO NOT EDIT.

local record AcceptMarketplaceRegistrationInput
    -- The registration token that's generated by Amazon Web Services Marketplace and sent to Oracle Database@Amazon Web Services.
    marketplaceRegistrationToken: string
end

local record AcceptMarketplaceRegistrationOutput
end

local record AssociateIamRoleToResourceInput
    -- The Amazon Resource Name (ARN) of the Amazon Web Services Identity and Access Management (IAM) service role to associate with the resource.
    iamRoleArn: string
    -- The Amazon Web Services integration configuration settings for the Amazon Web Services Identity and Access Management (IAM) service role association.
    awsIntegration: string
    -- The Amazon Resource Name (ARN) of the target resource to associate with the Amazon Web Services Identity and Access Management (IAM) service role.
    resourceArn: string
end

local record AssociateIamRoleToResourceOutput
end

local record CreateCloudAutonomousVmClusterInput
    -- The unique identifier of the Exadata infrastructure where the VM cluster will be created.
    cloudExadataInfrastructureId: string
    -- The unique identifier of the ODB network to be used for the VM cluster.
    odbNetworkId: string
    -- The display name for the Autonomous VM cluster. The name does not need to be unique.
    displayName: string
    -- A client-provided token to ensure idempotency of the request.
    clientToken: string
    -- The data disk group size to be allocated for Autonomous Databases, in terabytes (TB).
    autonomousDataStorageSizeInTBs: number
    -- The number of CPU cores to be enabled per VM cluster node.
    cpuCoreCountPerNode: number
    -- The list of database servers to be used for the Autonomous VM cluster.
    dbServers: {string}
    -- A user-provided description of the Autonomous VM cluster.
    description: string
    -- Specifies whether to enable mutual TLS (mTLS) authentication for the Autonomous VM cluster.
    isMtlsEnabledVmCluster: boolean
    -- The Oracle license model to apply to the Autonomous VM cluster.
    licenseModel: string
    -- The scheduling details for the maintenance window. Patching and system updates take place during the maintenance window.
    maintenanceWindow: MaintenanceWindow
    -- The amount of memory to be allocated per OCPU, in GB.
    memoryPerOracleComputeUnitInGBs: number
    -- The SCAN listener port for non-TLS (TCP) protocol.
    scanListenerPortNonTls: number
    -- The SCAN listener port for TLS (TCP) protocol.
    scanListenerPortTls: number
    -- Free-form tags for this resource. Each tag is a key-value pair with no predefined name, type, or namespace.
    tags: {string : string}
    -- The time zone to use for the Autonomous VM cluster.
    timeZone: string
    -- The total number of Autonomous CDBs that you can create in the Autonomous VM cluster.
    totalContainerDatabases: number
end

local record CreateCloudAutonomousVmClusterOutput
    -- The display name of the created Autonomous VM cluster.
    displayName: string
    -- The current status of the Autonomous VM cluster creation process.
    status: string
    -- Additional information about the current status of the Autonomous VM cluster creation process, if applicable.
    statusReason: string
    -- The unique identifier of the created Autonomous VM cluster.
    cloudAutonomousVmClusterId: string
end

local record CreateCloudExadataInfrastructureInput
    -- A user-friendly name for the Exadata infrastructure.
    displayName: string
    -- The model name of the Exadata infrastructure. For the list of valid model names, use the ListDbSystemShapes operation.
    shape: string
    -- The name of the Availability Zone (AZ) where the Exadata infrastructure is located. This operation requires that you specify a value for either availabilityZone or availabilityZoneId. Example: us-east-1a
    availabilityZone: string
    -- The AZ ID of the AZ where the Exadata infrastructure is located. This operation requires that you specify a value for either availabilityZone or availabilityZoneId. Example: use1-az1
    availabilityZoneId: string
    -- The list of resource tags to apply to the Exadata infrastructure.
    tags: {string : string}
    -- The number of database servers for the Exadata infrastructure. Valid values for this parameter depend on the shape. To get information about the minimum and maximum values, use the ListDbSystemShapes operation.
    computeCount: number
    -- The email addresses of contacts to receive notification from Oracle about maintenance updates for the Exadata infrastructure.
    customerContactsToSendToOCI: {CustomerContact}
    -- The maintenance window configuration for the Exadata Cloud infrastructure. This allows you to define when maintenance operations such as patching and updates can be performed on the infrastructure.
    maintenanceWindow: MaintenanceWindow
    -- The number of storage servers to activate for this Exadata infrastructure. Valid values for this parameter depend on the shape. To get information about the minimum and maximum values, use the ListDbSystemShapes operation.
    storageCount: number
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you don't specify a client token, the Amazon Web Services SDK automatically generates a client token and uses it for the request to ensure idempotency. The client token is valid for up to 24 hours after it's first used.
    clientToken: string
    -- The database server model type of the Exadata infrastructure. For the list of valid model names, use the ListDbSystemShapes operation.
    databaseServerType: string
    -- The storage server model type of the Exadata infrastructure. For the list of valid model names, use the ListDbSystemShapes operation.
    storageServerType: string
end

local record CreateCloudExadataInfrastructureOutput
    -- The user-friendly name for the Exadata infrastructure.
    displayName: string
    -- The current status of the Exadata infrastructure.
    status: string
    -- Additional information about the status of the Exadata infrastructure.
    statusReason: string
    -- The unique identifier of the Exadata infrastructure.
    cloudExadataInfrastructureId: string
end

local record CreateCloudVmClusterInput
    -- The unique identifier of the Exadata infrastructure for this VM cluster.
    cloudExadataInfrastructureId: string
    -- The number of CPU cores to enable on the VM cluster.
    cpuCoreCount: number
    -- A user-friendly name for the VM cluster.
    displayName: string
    -- A valid software version of Oracle Grid Infrastructure (GI). To get the list of valid values, use the ListGiVersions operation and specify the shape of the Exadata infrastructure. Example: 19.0.0.0
    giVersion: string
    -- The host name for the VM cluster. Constraints: Can't be "localhost" or "hostname". Can't contain "-version". The maximum length of the combined hostname and domain is 63 characters. The hostname must be unique within the subnet.
    hostname: string
    -- The public key portion of one or more key pairs used for SSH access to the VM cluster.
    sshPublicKeys: {string}
    -- The unique identifier of the ODB network for the VM cluster.
    odbNetworkId: string
    -- A name for the Grid Infrastructure cluster. The name isn't case sensitive.
    clusterName: string
    -- The set of preferences for the various diagnostic collection options for the VM cluster.
    dataCollectionOptions: DataCollectionOptions
    -- The size of the data disk group, in terabytes (TBs), to allocate for the VM cluster.
    dataStorageSizeInTBs: number
    -- The amount of local node storage, in gigabytes (GBs), to allocate for the VM cluster.
    dbNodeStorageSizeInGBs: number
    -- The list of database servers for the VM cluster.
    dbServers: {string}
    -- The list of resource tags to apply to the VM cluster.
    tags: {string : string}
    -- Specifies whether to enable database backups to local Exadata storage for the VM cluster.
    isLocalBackupEnabled: boolean
    -- Specifies whether to create a sparse disk group for the VM cluster.
    isSparseDiskgroupEnabled: boolean
    -- The Oracle license model to apply to the VM cluster. Default: LICENSE_INCLUDED
    licenseModel: string
    -- The amount of memory, in gigabytes (GBs), to allocate for the VM cluster.
    memorySizeInGBs: number
    -- The version of the operating system of the image for the VM cluster.
    systemVersion: string
    -- The time zone for the VM cluster. For a list of valid values for time zone, you can check the options in the console. Default: UTC
    timeZone: string
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you don't specify a client token, the Amazon Web Services SDK automatically generates a client token and uses it for the request to ensure idempotency. The client token is valid for up to 24 hours after it's first used.
    clientToken: string
    -- The port number for TCP connections to the single client access name (SCAN) listener. Valid values: 1024–8999 with the following exceptions: 2484, 6100, 6200, 7060, 7070, 7085, and 7879 Default: 1521
    scanListenerPortTcp: number
end

local record CreateCloudVmClusterOutput
    -- The user-friendly name for the VM cluster.
    displayName: string
    -- The current status of the VM cluster.
    status: string
    -- Additional information about the status of the VM cluster.
    statusReason: string
    -- The unique identifier for the VM cluster.
    cloudVmClusterId: string
end

local record CreateOdbNetworkInput
    -- A user-friendly name for the ODB network.
    displayName: string
    -- The Amazon Web Services Availability Zone (AZ) where the ODB network is located. This operation requires that you specify a value for either availabilityZone or availabilityZoneId.
    availabilityZone: string
    -- The AZ ID of the AZ where the ODB network is located. This operation requires that you specify a value for either availabilityZone or availabilityZoneId.
    availabilityZoneId: string
    -- The CIDR range of the client subnet for the ODB network. Constraints: Must not overlap with the CIDR range of the backup subnet. Must not overlap with the CIDR ranges of the VPCs that are connected to the ODB network. Must not use the following CIDR ranges that are reserved by OCI: 100.106.0.0/16 and 100.107.0.0/16 169.254.0.0/16 224.0.0.0 - 239.255.255.255 240.0.0.0 - 255.255.255.255
    clientSubnetCidr: string
    -- The CIDR range of the backup subnet for the ODB network. Constraints: Must not overlap with the CIDR range of the client subnet. Must not overlap with the CIDR ranges of the VPCs that are connected to the ODB network. Must not use the following CIDR ranges that are reserved by OCI: 100.106.0.0/16 and 100.107.0.0/16 169.254.0.0/16 224.0.0.0 - 239.255.255.255 240.0.0.0 - 255.255.255.255
    backupSubnetCidr: string
    -- The domain name to use for the resources in the ODB network.
    customDomainName: string
    -- The DNS prefix to the default DNS domain name. The default DNS domain name is oraclevcn.com.
    defaultDnsPrefix: string
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you don't specify a client token, the Amazon Web Services SDK automatically generates a client token and uses it for the request to ensure idempotency. The client token is valid for up to 24 hours after it's first used.
    clientToken: string
    -- Specifies the configuration for Amazon S3 access from the ODB network.
    s3Access: string
    -- Specifies the configuration for Zero-ETL access from the ODB network.
    zeroEtlAccess: string
    -- The Amazon Web Services Security Token Service (STS) access configuration for the ODB network.
    stsAccess: string
    -- The Amazon Web Services Key Management Service (KMS) access configuration for the ODB network.
    kmsAccess: string
    -- Specifies the endpoint policy for Amazon S3 access from the ODB network.
    s3PolicyDocument: string
    -- The Amazon Web Services Security Token Service (STS) policy document that defines permissions for token service usage within the ODB network.
    stsPolicyDocument: string
    -- The Amazon Web Services Key Management Service (KMS) policy document that defines permissions for key usage within the ODB network.
    kmsPolicyDocument: string
    -- The cross-Region Amazon S3 restore sources to enable for the ODB network.
    crossRegionS3RestoreSourcesToEnable: {string}
    -- The list of resource tags to apply to the ODB network.
    tags: {string : string}
end

local record CreateOdbNetworkOutput
    -- The user-friendly name of the ODB network.
    displayName: string
    -- The current status of the ODB network.
    status: string
    -- Additional information about the status of the ODB network.
    statusReason: string
    -- The unique identifier of the ODB network.
    odbNetworkId: string
end

local record CreateOdbPeeringConnectionInput
    -- The unique identifier of the ODB network that initiates the peering connection.
    odbNetworkId: string
    -- The unique identifier of the peer network. This can be either a VPC ID or another ODB network ID.
    peerNetworkId: string
    -- The display name for the ODB peering connection.
    displayName: string
    -- A list of CIDR blocks to add to the peering connection. These CIDR blocks define the IP address ranges that can communicate through the peering connection.
    peerNetworkCidrsToBeAdded: {string}
    -- The unique identifier of the VPC route table for which a route to the ODB network is automatically created during peering connection establishment.
    peerNetworkRouteTableIds: {string}
    -- The client token for the ODB peering connection request. Constraints: Must be unique for each request.
    clientToken: string
    -- The tags to assign to the ODB peering connection.
    tags: {string : string}
end

local record CreateOdbPeeringConnectionOutput
    -- The display name of the ODB peering connection.
    displayName: string
    -- The status of the ODB peering connection.
    status: string
    -- The reason for the current status of the ODB peering connection.
    statusReason: string
    -- The unique identifier of the ODB peering connection.
    odbPeeringConnectionId: string
end

local record DeleteCloudAutonomousVmClusterInput
    -- The unique identifier of the Autonomous VM cluster to delete.
    cloudAutonomousVmClusterId: string
end

local record DeleteCloudAutonomousVmClusterOutput
end

local record DeleteCloudExadataInfrastructureInput
    -- The unique identifier of the Exadata infrastructure to delete.
    cloudExadataInfrastructureId: string
end

local record DeleteCloudExadataInfrastructureOutput
end

local record DeleteCloudVmClusterInput
    -- The unique identifier of the VM cluster to delete.
    cloudVmClusterId: string
end

local record DeleteCloudVmClusterOutput
end

local record DeleteOdbNetworkInput
    -- The unique identifier of the ODB network to delete.
    odbNetworkId: string
    -- Specifies whether to delete associated OCI networking resources along with the ODB network.
    deleteAssociatedResources: boolean
end

local record DeleteOdbNetworkOutput
end

local record DeleteOdbPeeringConnectionInput
    -- The unique identifier of the ODB peering connection to delete.
    odbPeeringConnectionId: string
end

local record DeleteOdbPeeringConnectionOutput
end

local record DisassociateIamRoleFromResourceInput
    -- The Amazon Resource Name (ARN) of the Amazon Web Services Identity and Access Management (IAM) service role to disassociate from the resource.
    iamRoleArn: string
    -- The Amazon Web Services integration configuration settings for the Amazon Web Services Identity and Access Management (IAM) service role disassociation.
    awsIntegration: string
    -- The Amazon Resource Name (ARN) of the target resource to disassociate from the Amazon Web Services Identity and Access Management (IAM) service role.
    resourceArn: string
end

local record DisassociateIamRoleFromResourceOutput
end

local record GetCloudAutonomousVmClusterInput
    -- The unique identifier of the Autonomous VM cluster to retrieve information about.
    cloudAutonomousVmClusterId: string
end

local record GetCloudAutonomousVmClusterOutput
    -- The details of the requested Autonomous VM cluster.
    cloudAutonomousVmCluster: CloudAutonomousVmCluster
end

local record GetCloudExadataInfrastructureInput
    -- The unique identifier of the Exadata infrastructure.
    cloudExadataInfrastructureId: string
end

local record GetCloudExadataInfrastructureOutput
    -- The Exadata infrastructure.
    cloudExadataInfrastructure: CloudExadataInfrastructure
end

local record GetCloudExadataInfrastructureUnallocatedResourcesInput
    -- The unique identifier of the Cloud Exadata infrastructure for which to retrieve unallocated resources.
    cloudExadataInfrastructureId: string
    -- The database servers to include in the unallocated resources query.
    dbServers: {string}
end

local record GetCloudExadataInfrastructureUnallocatedResourcesOutput
    -- Details about the unallocated resources in the specified Cloud Exadata infrastructure.
    cloudExadataInfrastructureUnallocatedResources: CloudExadataInfrastructureUnallocatedResources
end

local record GetCloudVmClusterInput
    -- The unique identifier of the VM cluster.
    cloudVmClusterId: string
end

local record GetCloudVmClusterOutput
    -- The VM cluster.
    cloudVmCluster: CloudVmCluster
end

local record GetDbNodeInput
    -- The unique identifier of the VM cluster that contains the DB node.
    cloudVmClusterId: string
    -- The unique identifier of the DB node to retrieve information about.
    dbNodeId: string
end

local record GetDbNodeOutput
    dbNode: DbNode
end

local record GetDbServerInput
    -- The unique identifier of the Oracle Exadata infrastructure that contains the database server.
    cloudExadataInfrastructureId: string
    -- The unique identifier of the database server to retrieve information about.
    dbServerId: string
end

local record GetDbServerOutput
    -- The details of the requested database server.
    dbServer: DbServer
end

local record GetOciOnboardingStatusInput
end

local record GetOciOnboardingStatusOutput
    status: string
    -- The existing OCI tenancy activation link for your Amazon Web Services account.
    existingTenancyActivationLink: string
    -- A new OCI tenancy activation link for your Amazon Web Services account.
    newTenancyActivationLink: string
    -- The Oracle Cloud Infrastructure (OCI) identity domain information in the onboarding status response.
    ociIdentityDomain: OciIdentityDomain
end

local record GetOdbNetworkInput
    -- The unique identifier of the ODB network.
    odbNetworkId: string
end

local record GetOdbNetworkOutput
    -- The ODB network.
    odbNetwork: OdbNetwork
end

local record GetOdbPeeringConnectionInput
    -- The unique identifier of the ODB peering connection to retrieve information about.
    odbPeeringConnectionId: string
end

local record GetOdbPeeringConnectionOutput
    odbPeeringConnection: OdbPeeringConnection
end

local record InitializeServiceInput
    -- The Oracle Cloud Infrastructure (OCI) identity domain configuration for service initialization.
    ociIdentityDomain: boolean
end

local record InitializeServiceOutput
end

local record ListAutonomousVirtualMachinesInput
    -- The maximum number of items to return per page.
    maxResults: number
    -- The pagination token to continue listing from.
    nextToken: string
    -- The unique identifier of the Autonomous VM cluster whose virtual machines you're listing.
    cloudAutonomousVmClusterId: string
end

local record ListAutonomousVirtualMachinesOutput
    -- The pagination token from which to continue listing.
    nextToken: string
    -- The list of Autonomous VMs in the specified Autonomous VM cluster.
    autonomousVirtualMachines: {AutonomousVirtualMachineSummary}
end

local record ListCloudAutonomousVmClustersInput
    -- The maximum number of items to return per page.
    maxResults: number
    -- The pagination token to continue listing from.
    nextToken: string
    -- The unique identifier of the Cloud Exadata Infrastructure that hosts the Autonomous VM clusters to be listed.
    cloudExadataInfrastructureId: string
end

local record ListCloudAutonomousVmClustersOutput
    -- The pagination token to continue listing from.
    nextToken: string
    -- The list of Autonomous VM clusters in the specified Cloud Exadata Infrastructure.
    cloudAutonomousVmClusters: {CloudAutonomousVmClusterSummary}
end

local record ListCloudExadataInfrastructuresInput
    -- The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. Default: 10
    maxResults: number
    -- The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request.
    nextToken: string
end

local record ListCloudExadataInfrastructuresOutput
    -- The token to include in another request to get the next page of items. This value is null when there are no more items to return.
    nextToken: string
    -- The list of Exadata infrastructures along with their properties.
    cloudExadataInfrastructures: {CloudExadataInfrastructureSummary}
end

local record ListCloudVmClustersInput
    -- The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. Default: 10
    maxResults: number
    -- The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request.
    nextToken: string
    -- The unique identifier of the Oracle Exadata infrastructure.
    cloudExadataInfrastructureId: string
end

local record ListCloudVmClustersOutput
    -- The token to include in another request to get the next page of items. This value is null when there are no more items to return.
    nextToken: string
    -- The list of VM clusters along with their properties.
    cloudVmClusters: {CloudVmClusterSummary}
end

local record ListDbNodesInput
    -- The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. Default: 10
    maxResults: number
    -- The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request.
    nextToken: string
    -- The unique identifier of the VM cluster.
    cloudVmClusterId: string
end

local record ListDbNodesOutput
    -- The token to include in another request to get the next page of items. This value is null when there are no more items to return.
    nextToken: string
    -- The list of DB nodes along with their properties.
    dbNodes: {DbNodeSummary}
end

local record ListDbServersInput
    -- The unique identifier of the Oracle Exadata infrastructure.
    cloudExadataInfrastructureId: string
    -- The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. Default: 10
    maxResults: number
    -- The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request.
    nextToken: string
end

local record ListDbServersOutput
    -- The token to include in another request to get the next page of items. This value is null when there are no more items to return.
    nextToken: string
    -- The list of database servers along with their properties.
    dbServers: {DbServerSummary}
end

local record ListDbSystemShapesInput
    -- The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. Default: 10
    maxResults: number
    -- The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request.
    nextToken: string
    -- The logical name of the AZ, for example, us-east-1a. This name varies depending on the account.
    availabilityZone: string
    -- The physical ID of the AZ, for example, use1-az4. This ID persists across accounts.
    availabilityZoneId: string
end

local record ListDbSystemShapesOutput
    -- The token to include in another request to get the next page of items. This value is null when there are no more items to return.
    nextToken: string
    -- The list of shapes and their properties.
    dbSystemShapes: {DbSystemShapeSummary}
end

local record ListGiVersionsInput
    -- The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. Default: 10
    maxResults: number
    -- The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request.
    nextToken: string
    -- The shape to return GI versions for. For a list of valid shapes, use the ListDbSystemShapes operation..
    shape: string
end

local record ListGiVersionsOutput
    -- The token to include in another request to get the next page of items. This value is null when there are no more items to return.
    nextToken: string
    -- The list of GI versions and their properties.
    giVersions: {GiVersionSummary}
end

local record ListOdbNetworksInput
    -- The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. Default: 10
    maxResults: number
    -- The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request.
    nextToken: string
end

local record ListOdbNetworksOutput
    -- The token to include in another request to get the next page of items. This value is null when there are no more items to return.
    nextToken: string
    -- The list of ODB networks.
    odbNetworks: {OdbNetworkSummary}
end

local record ListOdbPeeringConnectionsInput
    -- The maximum number of ODB peering connections to return in the response. Default: 20 Constraints: Must be between 1 and 100.
    maxResults: number
    -- The pagination token for the next page of ODB peering connections.
    nextToken: string
    -- The identifier of the ODB network to list peering connections for. If not specified, lists all ODB peering connections in the account.
    odbNetworkId: string
end

local record ListOdbPeeringConnectionsOutput
    -- The pagination token for the next page of ODB peering connections.
    nextToken: string
    -- The list of ODB peering connections.
    odbPeeringConnections: {OdbPeeringConnectionSummary}
end

local record ListSystemVersionsInput
    -- The maximum number of items to return for this request. To get the next page of items, make another request with the token returned in the output. Default: 10
    maxResults: number
    -- The token returned from a previous paginated request. Pagination continues from the end of the items returned by the previous request.
    nextToken: string
    -- The software version of the Exadata Grid Infrastructure (GI).
    giVersion: string
    -- The Exadata hardware system model.
    shape: string
end

local record ListSystemVersionsOutput
    -- The token to include in another request to get the next page of items. This value is null when there are no more items to return.
    nextToken: string
    -- The list of system versions.
    systemVersions: {SystemVersionSummary}
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the resource to list tags for.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- The list of tags applied to the resource.
    tags: {string : string}
end

local record RebootDbNodeInput
    -- The unique identifier of the VM cluster that contains the DB node to reboot.
    cloudVmClusterId: string
    -- The unique identifier of the DB node to reboot.
    dbNodeId: string
end

local record RebootDbNodeOutput
    -- The unique identifier of the DB node that was rebooted.
    dbNodeId: string
    -- The current status of the DB node after the reboot operation.
    status: string
    -- Additional information about the status of the DB node after the reboot operation.
    statusReason: string
end

local record StartDbNodeInput
    -- The unique identifier of the VM cluster that contains the DB node to start.
    cloudVmClusterId: string
    -- The unique identifier of the DB node to start.
    dbNodeId: string
end

local record StartDbNodeOutput
    -- The unique identifier of the DB node that was started.
    dbNodeId: string
    -- The current status of the DB node after the start operation.
    status: string
    -- Additional information about the status of the DB node after the start operation.
    statusReason: string
end

local record StopDbNodeInput
    -- The unique identifier of the VM cluster that contains the DB node to stop.
    cloudVmClusterId: string
    -- The unique identifier of the DB node to stop.
    dbNodeId: string
end

local record StopDbNodeOutput
    -- The unique identifier of the DB node that was stopped.
    dbNodeId: string
    -- The current status of the DB node after the stop operation.
    status: string
    -- Additional information about the status of the DB node after the stop operation.
    statusReason: string
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the resource to apply tags to.
    resourceArn: string
    -- The list of tags to apply to the resource.
    tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the resource to remove tags from.
    resourceArn: string
    -- The names (keys) of the tags to remove from the resource.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateCloudExadataInfrastructureInput
    -- The unique identifier of the Exadata infrastructure to update.
    cloudExadataInfrastructureId: string
    maintenanceWindow: MaintenanceWindow
end

local record UpdateCloudExadataInfrastructureOutput
    -- The user-friendly name of the updated Exadata infrastructure.
    displayName: string
    -- The current status of the Exadata infrastructure after the update operation.
    status: string
    -- Additional information about the status of the Exadata infrastructure after the update operation.
    statusReason: string
    -- The unique identifier of the updated Exadata infrastructure.
    cloudExadataInfrastructureId: string
end

local record UpdateOdbNetworkInput
    -- The unique identifier of the ODB network to update.
    odbNetworkId: string
    -- The new user-friendly name of the ODB network.
    displayName: string
    -- The list of CIDR ranges from the peered VPC that allow access to the ODB network.
    peeredCidrsToBeAdded: {string}
    -- The list of CIDR ranges from the peered VPC to remove from the ODB network.
    peeredCidrsToBeRemoved: {string}
    -- Specifies the updated configuration for Amazon S3 access from the ODB network.
    s3Access: string
    -- Specifies the updated configuration for Zero-ETL access from the ODB network.
    zeroEtlAccess: string
    -- The Amazon Web Services Security Token Service (STS) access configuration for the ODB network.
    stsAccess: string
    -- The Amazon Web Services Key Management Service (KMS) access configuration for the ODB network.
    kmsAccess: string
    -- Specifies the updated endpoint policy for Amazon S3 access from the ODB network.
    s3PolicyDocument: string
    -- The Amazon Web Services Security Token Service (STS) policy document that defines permissions for token service usage within the ODB network.
    stsPolicyDocument: string
    -- The Amazon Web Services Key Management Service (KMS) policy document that defines permissions for key usage within the ODB network.
    kmsPolicyDocument: string
    -- The cross-Region Amazon S3 restore sources to enable for the ODB network.
    crossRegionS3RestoreSourcesToEnable: {string}
    -- The cross-Region Amazon S3 restore sources to disable for the ODB network.
    crossRegionS3RestoreSourcesToDisable: {string}
end

local record UpdateOdbNetworkOutput
    -- The user-friendly name of the ODB network.
    displayName: string
    -- The current status of the ODB network.
    status: string
    -- Additional information about the status of the ODB network.
    statusReason: string
    -- The unique identifier of the ODB network.
    odbNetworkId: string
end

local record UpdateOdbPeeringConnectionInput
    -- The identifier of the Oracle Database@Amazon Web Services peering connection to update.
    odbPeeringConnectionId: string
    -- A new display name for the peering connection.
    displayName: string
    -- A list of CIDR blocks to add to the peering connection. These CIDR blocks define the IP address ranges that can communicate through the peering connection. The CIDR blocks must not overlap with existing CIDR blocks in the Oracle Database@Amazon Web Services network.
    peerNetworkCidrsToBeAdded: {string}
    -- A list of CIDR blocks to remove from the peering connection. The CIDR blocks must currently exist in the peering connection.
    peerNetworkCidrsToBeRemoved: {string}
end

local record UpdateOdbPeeringConnectionOutput
    -- The display name of the peering connection.
    displayName: string
    -- The status of the peering connection update operation.
    status: string
    -- Additional information about the status of the peering connection update operation.
    statusReason: string
    -- The identifier of the Oracle Database@Amazon Web Services peering connection that was updated.
    odbPeeringConnectionId: string
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- You have exceeded the service quota.
local record ServiceQuotaExceededException
    message: string
    -- The identifier of the resource that exceeded the service quota.
    resourceId: string
    -- The type of resource that exceeded the service quota.
    resourceType: string
    -- The unqiue identifier of the service quota that was exceeded.
    quotaCode: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- You have exceeded the service quota.
local record ServiceQuotaExceededException
    message: string
    -- The identifier of the resource that exceeded the service quota.
    resourceId: string
    -- The type of resource that exceeded the service quota.
    resourceType: string
    -- The unqiue identifier of the service quota that was exceeded.
    quotaCode: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- You have exceeded the service quota.
local record ServiceQuotaExceededException
    message: string
    -- The identifier of the resource that exceeded the service quota.
    resourceId: string
    -- The type of resource that exceeded the service quota.
    resourceType: string
    -- The unqiue identifier of the service quota that was exceeded.
    quotaCode: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- You have exceeded the service quota.
local record ServiceQuotaExceededException
    message: string
    -- The identifier of the resource that exceeded the service quota.
    resourceId: string
    -- The type of resource that exceeded the service quota.
    resourceType: string
    -- The unqiue identifier of the service quota that was exceeded.
    quotaCode: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- You have exceeded the service quota.
local record ServiceQuotaExceededException
    message: string
    -- The identifier of the resource that exceeded the service quota.
    resourceId: string
    -- The type of resource that exceeded the service quota.
    resourceType: string
    -- The unqiue identifier of the service quota that was exceeded.
    quotaCode: string
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- You don't have sufficient access to perform this action. Make sure you have the required permissions and try again.
local record AccessDeniedException
    message: string
end

-- Occurs when a conflict with the current status of your resource. Fix any inconsistencies with your resource and try again.
local record ConflictException
    message: string
    -- The identifier of the resource that caused the conflict.
    resourceId: string
    -- The type of resource that caused the conflict.
    resourceType: string
end

-- Occurs when there is an internal failure in the Oracle Database@Amazon Web Services service. Wait and try again.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying the request after an internal server error.
    retryAfterSeconds: number
end

-- The operation tried to access a resource that doesn't exist. Make sure you provided the correct resource and try again.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that was not found.
    resourceId: string
    -- The type of resource that was not found.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The number of seconds to wait before retrying the request after being throttled.
    retryAfterSeconds: number
end

-- The request has failed validation because it is missing required fields or has invalid inputs.
local record ValidationException
    message: string
    -- The reason why the validation failed.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
end

local type Access = "ENABLED" | "DISABLED"

local type DbNodeResourceStatus = "AVAILABLE" | "FAILED" | "PROVISIONING" | "TERMINATED" | "TERMINATING" | "UPDATING" | "STOPPING" | "STOPPED" | "STARTING"

local type LicenseModel = "BRING_YOUR_OWN_LICENSE" | "LICENSE_INCLUDED"

local type OciOnboardingStatus = "NOT_STARTED" | "PENDING_LINK_GENERATION" | "PENDING_CUSTOMER_ACTION" | "PENDING_INITIALIZATION" | "ACTIVATING" | "ACTIVE_IN_HOME_REGION" | "ACTIVE" | "ACTIVE_LIMITED" | "FAILED" | "PUBLIC_OFFER_UNSUPPORTED" | "SUSPENDED" | "CANCELED"

local type ResourceStatus = "AVAILABLE" | "FAILED" | "PROVISIONING" | "TERMINATED" | "TERMINATING" | "UPDATING" | "MAINTENANCE_IN_PROGRESS"

local type SupportedAwsIntegration = "KmsTde"

local record M
    AcceptMarketplaceRegistrationInput: AcceptMarketplaceRegistrationInput
    AcceptMarketplaceRegistrationOutput: AcceptMarketplaceRegistrationOutput
    AssociateIamRoleToResourceInput: AssociateIamRoleToResourceInput
    AssociateIamRoleToResourceOutput: AssociateIamRoleToResourceOutput
    CreateCloudAutonomousVmClusterInput: CreateCloudAutonomousVmClusterInput
    CreateCloudAutonomousVmClusterOutput: CreateCloudAutonomousVmClusterOutput
    CreateCloudExadataInfrastructureInput: CreateCloudExadataInfrastructureInput
    CreateCloudExadataInfrastructureOutput: CreateCloudExadataInfrastructureOutput
    CreateCloudVmClusterInput: CreateCloudVmClusterInput
    CreateCloudVmClusterOutput: CreateCloudVmClusterOutput
    CreateOdbNetworkInput: CreateOdbNetworkInput
    CreateOdbNetworkOutput: CreateOdbNetworkOutput
    CreateOdbPeeringConnectionInput: CreateOdbPeeringConnectionInput
    CreateOdbPeeringConnectionOutput: CreateOdbPeeringConnectionOutput
    DeleteCloudAutonomousVmClusterInput: DeleteCloudAutonomousVmClusterInput
    DeleteCloudAutonomousVmClusterOutput: DeleteCloudAutonomousVmClusterOutput
    DeleteCloudExadataInfrastructureInput: DeleteCloudExadataInfrastructureInput
    DeleteCloudExadataInfrastructureOutput: DeleteCloudExadataInfrastructureOutput
    DeleteCloudVmClusterInput: DeleteCloudVmClusterInput
    DeleteCloudVmClusterOutput: DeleteCloudVmClusterOutput
    DeleteOdbNetworkInput: DeleteOdbNetworkInput
    DeleteOdbNetworkOutput: DeleteOdbNetworkOutput
    DeleteOdbPeeringConnectionInput: DeleteOdbPeeringConnectionInput
    DeleteOdbPeeringConnectionOutput: DeleteOdbPeeringConnectionOutput
    DisassociateIamRoleFromResourceInput: DisassociateIamRoleFromResourceInput
    DisassociateIamRoleFromResourceOutput: DisassociateIamRoleFromResourceOutput
    GetCloudAutonomousVmClusterInput: GetCloudAutonomousVmClusterInput
    GetCloudAutonomousVmClusterOutput: GetCloudAutonomousVmClusterOutput
    GetCloudExadataInfrastructureInput: GetCloudExadataInfrastructureInput
    GetCloudExadataInfrastructureOutput: GetCloudExadataInfrastructureOutput
    GetCloudExadataInfrastructureUnallocatedResourcesInput: GetCloudExadataInfrastructureUnallocatedResourcesInput
    GetCloudExadataInfrastructureUnallocatedResourcesOutput: GetCloudExadataInfrastructureUnallocatedResourcesOutput
    GetCloudVmClusterInput: GetCloudVmClusterInput
    GetCloudVmClusterOutput: GetCloudVmClusterOutput
    GetDbNodeInput: GetDbNodeInput
    GetDbNodeOutput: GetDbNodeOutput
    GetDbServerInput: GetDbServerInput
    GetDbServerOutput: GetDbServerOutput
    GetOciOnboardingStatusInput: GetOciOnboardingStatusInput
    GetOciOnboardingStatusOutput: GetOciOnboardingStatusOutput
    GetOdbNetworkInput: GetOdbNetworkInput
    GetOdbNetworkOutput: GetOdbNetworkOutput
    GetOdbPeeringConnectionInput: GetOdbPeeringConnectionInput
    GetOdbPeeringConnectionOutput: GetOdbPeeringConnectionOutput
    InitializeServiceInput: InitializeServiceInput
    InitializeServiceOutput: InitializeServiceOutput
    ListAutonomousVirtualMachinesInput: ListAutonomousVirtualMachinesInput
    ListAutonomousVirtualMachinesOutput: ListAutonomousVirtualMachinesOutput
    ListCloudAutonomousVmClustersInput: ListCloudAutonomousVmClustersInput
    ListCloudAutonomousVmClustersOutput: ListCloudAutonomousVmClustersOutput
    ListCloudExadataInfrastructuresInput: ListCloudExadataInfrastructuresInput
    ListCloudExadataInfrastructuresOutput: ListCloudExadataInfrastructuresOutput
    ListCloudVmClustersInput: ListCloudVmClustersInput
    ListCloudVmClustersOutput: ListCloudVmClustersOutput
    ListDbNodesInput: ListDbNodesInput
    ListDbNodesOutput: ListDbNodesOutput
    ListDbServersInput: ListDbServersInput
    ListDbServersOutput: ListDbServersOutput
    ListDbSystemShapesInput: ListDbSystemShapesInput
    ListDbSystemShapesOutput: ListDbSystemShapesOutput
    ListGiVersionsInput: ListGiVersionsInput
    ListGiVersionsOutput: ListGiVersionsOutput
    ListOdbNetworksInput: ListOdbNetworksInput
    ListOdbNetworksOutput: ListOdbNetworksOutput
    ListOdbPeeringConnectionsInput: ListOdbPeeringConnectionsInput
    ListOdbPeeringConnectionsOutput: ListOdbPeeringConnectionsOutput
    ListSystemVersionsInput: ListSystemVersionsInput
    ListSystemVersionsOutput: ListSystemVersionsOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    RebootDbNodeInput: RebootDbNodeInput
    RebootDbNodeOutput: RebootDbNodeOutput
    StartDbNodeInput: StartDbNodeInput
    StartDbNodeOutput: StartDbNodeOutput
    StopDbNodeInput: StopDbNodeInput
    StopDbNodeOutput: StopDbNodeOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateCloudExadataInfrastructureInput: UpdateCloudExadataInfrastructureInput
    UpdateCloudExadataInfrastructureOutput: UpdateCloudExadataInfrastructureOutput
    UpdateOdbNetworkInput: UpdateOdbNetworkInput
    UpdateOdbNetworkOutput: UpdateOdbNetworkOutput
    UpdateOdbPeeringConnectionInput: UpdateOdbPeeringConnectionInput
    UpdateOdbPeeringConnectionOutput: UpdateOdbPeeringConnectionOutput
end

return M
