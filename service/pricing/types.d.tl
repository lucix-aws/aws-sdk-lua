-- Code generated by lua-client-codegen. DO NOT EDIT.

local record DescribeServicesInput
    -- The code for the service whose information you want to retrieve, such as AmazonEC2. You can use the ServiceCode to filter the results in a GetProducts call. To retrieve a list of all services, leave this blank.
    ServiceCode: string
    -- The format version that you want the response to be in. Valid values are: aws_v1
    FormatVersion: string
    -- The pagination token that indicates the next set of results that you want to retrieve.
    NextToken: string
    -- The maximum number of results that you want returned in the response.
    MaxResults: number
end

local record DescribeServicesOutput
    -- The service metadata for the service or services in the response.
    Services: {Service}
    -- The format version of the response. For example, aws_v1.
    FormatVersion: string
    -- The pagination token for the next set of retrievable results.
    NextToken: string
end

local record GetAttributeValuesInput
    -- The service code for the service whose attributes you want to retrieve. For example, if you want the retrieve an EC2 attribute, use AmazonEC2.
    ServiceCode: string
    -- The name of the attribute that you want to retrieve the values for, such as volumeType.
    AttributeName: string
    -- The pagination token that indicates the next set of results that you want to retrieve.
    NextToken: string
    -- The maximum number of results to return in response.
    MaxResults: number
end

local record GetAttributeValuesOutput
    -- The list of values for an attribute. For example, Throughput Optimized HDD and Provisioned IOPS are two available values for the AmazonEC2 volumeType.
    AttributeValues: {AttributeValue}
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record GetPriceListFileUrlInput
    -- The unique identifier that maps to where your Price List files are located. PriceListArn can be obtained from the ListPriceLists response.
    PriceListArn: string
    -- The format that you want to retrieve your Price List files in. The FileFormat can be obtained from the ListPriceLists response.
    FileFormat: string
end

local record GetPriceListFileUrlOutput
    -- The URL to download your Price List file from.
    Url: string
end

local record GetProductsInput
    -- The code for the service whose products you want to retrieve.
    ServiceCode: string
    -- The list of filters that limit the returned products. only products that match all filters are returned.
    Filters: {Filter}
    -- The format version that you want the response to be in. Valid values are: aws_v1
    FormatVersion: string
    -- The pagination token that indicates the next set of results that you want to retrieve.
    NextToken: string
    -- The maximum number of results to return in the response.
    MaxResults: number
end

local record GetProductsOutput
    -- The format version of the response. For example, aws_v1.
    FormatVersion: string
    -- The list of products that match your filters. The list contains both the product metadata and the price information.
    PriceList: {string}
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

local record ListPriceListsInput
    -- The service code or the Savings Plans service code for the attributes that you want to retrieve. For example, to get the list of applicable Amazon EC2 price lists, use AmazonEC2. For a full list of service codes containing On-Demand and Reserved Instance (RI) pricing, use the DescribeServices API. To retrieve the Reserved Instance and Compute Savings Plans price lists, use ComputeSavingsPlans. To retrieve Machine Learning Savings Plans price lists, use MachineLearningSavingsPlans.
    ServiceCode: string
    -- The date that the Price List file prices are effective from.
    EffectiveDate: number
    -- This is used to filter the Price List by Amazon Web Services Region. For example, to get the price list only for the US East (N. Virginia) Region, use us-east-1. If nothing is specified, you retrieve price lists for all applicable Regions. The available RegionCode list can be retrieved from GetAttributeValues API.
    RegionCode: string
    -- The three alphabetical character ISO-4217 currency code that the Price List files are denominated in.
    CurrencyCode: string
    -- The pagination token that indicates the next set of results that you want to retrieve.
    NextToken: string
    -- The maximum number of results to return in the response.
    MaxResults: number
end

local record ListPriceListsOutput
    -- The type of price list references that match your request.
    PriceLists: {PriceList}
    -- The pagination token that indicates the next set of results to retrieve.
    NextToken: string
end

-- General authentication failure. The request wasn't signed correctly.
local record AccessDeniedException
    Message: string
end

-- The pagination token expired. Try again without a pagination token.
local record ExpiredNextTokenException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid. Try again without a pagination token.
local record InvalidNextTokenException
    Message: string
end

-- One or more parameters had an invalid value.
local record InvalidParameterException
    Message: string
end

-- The requested resource can't be found.
local record NotFoundException
    Message: string
end

-- You've made too many requests exceeding service quotas.
local record ThrottlingException
    Message: string
end

-- General authentication failure. The request wasn't signed correctly.
local record AccessDeniedException
    Message: string
end

-- The pagination token expired. Try again without a pagination token.
local record ExpiredNextTokenException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid. Try again without a pagination token.
local record InvalidNextTokenException
    Message: string
end

-- One or more parameters had an invalid value.
local record InvalidParameterException
    Message: string
end

-- The requested resource can't be found.
local record NotFoundException
    Message: string
end

-- You've made too many requests exceeding service quotas.
local record ThrottlingException
    Message: string
end

-- General authentication failure. The request wasn't signed correctly.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- One or more parameters had an invalid value.
local record InvalidParameterException
    Message: string
end

-- The requested resource can't be found.
local record NotFoundException
    Message: string
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    Message: string
end

-- You've made too many requests exceeding service quotas.
local record ThrottlingException
    Message: string
end

-- General authentication failure. The request wasn't signed correctly.
local record AccessDeniedException
    Message: string
end

-- The pagination token expired. Try again without a pagination token.
local record ExpiredNextTokenException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid. Try again without a pagination token.
local record InvalidNextTokenException
    Message: string
end

-- One or more parameters had an invalid value.
local record InvalidParameterException
    Message: string
end

-- The requested resource can't be found.
local record NotFoundException
    Message: string
end

-- You've made too many requests exceeding service quotas.
local record ThrottlingException
    Message: string
end

-- General authentication failure. The request wasn't signed correctly.
local record AccessDeniedException
    Message: string
end

-- The pagination token expired. Try again without a pagination token.
local record ExpiredNextTokenException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid. Try again without a pagination token.
local record InvalidNextTokenException
    Message: string
end

-- One or more parameters had an invalid value.
local record InvalidParameterException
    Message: string
end

-- The requested resource can't be found.
local record NotFoundException
    Message: string
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    Message: string
end

-- You've made too many requests exceeding service quotas.
local record ThrottlingException
    Message: string
end

local record M
    DescribeServicesInput: DescribeServicesInput
    DescribeServicesOutput: DescribeServicesOutput
    GetAttributeValuesInput: GetAttributeValuesInput
    GetAttributeValuesOutput: GetAttributeValuesOutput
    GetPriceListFileUrlInput: GetPriceListFileUrlInput
    GetPriceListFileUrlOutput: GetPriceListFileUrlOutput
    GetProductsInput: GetProductsInput
    GetProductsOutput: GetProductsOutput
    ListPriceListsInput: ListPriceListsInput
    ListPriceListsOutput: ListPriceListsOutput
end

return M
