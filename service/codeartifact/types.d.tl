-- Code generated by lua-client-codegen. DO NOT EDIT.

local record AssociateExternalConnectionInput
    -- The name of the domain that contains the repository.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository to which the external connection is added.
    repository: string
    -- The name of the external connection to add to the repository. The following values are supported:
    -- public:npmjs - for the npm public repository.
    -- public:nuget-org - for the NuGet Gallery.
    -- public:pypi - for the Python Package Index.
    -- public:maven-central - for Maven Central.
    -- public:maven-googleandroid - for the Google Android repository.
    -- public:maven-gradleplugins - for the Gradle plugins repository.
    -- public:maven-commonsware - for the CommonsWare Android repository.
    -- public:maven-clojars - for the Clojars repository.
    -- public:ruby-gems-org - for RubyGems.org.
    -- public:crates-io - for Crates.io.
    externalConnection: string
end

local record AssociateExternalConnectionOutput
    -- Information about the connected repository after processing the request.
    repository: RepositoryDescription
end

local record CopyPackageVersionsInput
    -- The name of the domain that contains the source and destination repositories.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the package versions to be copied.
    sourceRepository: string
    -- The name of the repository into which package versions are copied.
    destinationRepository: string
    -- The format of the package versions to be copied.
    format: string
    -- The namespace of the package versions to be copied. The package component that specifies its namespace depends on its type. For example:
    -- The namespace is required when copying package versions of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package that contains the versions to be copied.
    package: string
    -- The versions of the package to be copied.
    -- You must specify versions or versionRevisions. You cannot specify both.
    versions: {string}
    -- A list of key-value pairs. The keys are package versions and the values are package version revisions. A CopyPackageVersion operation
    -- succeeds if the specified versions in the source repository match the specified package version revision.
    -- You must specify versions or versionRevisions. You cannot specify both.
    versionRevisions: {string : string}
    -- Set to true to overwrite a package version that already exists in the destination repository.
    -- If set to false and the package version already exists in the destination repository,
    -- the package version is returned in the failedVersions field of the response with
    -- an ALREADY_EXISTS error code.
    allowOverwrite: boolean
    -- Set to true to copy packages from repositories that are upstream from the source
    -- repository to the destination repository. The default setting is false. For more information,
    -- see Working with
    -- upstream repositories.
    includeFromUpstream: boolean
end

local record CopyPackageVersionsOutput
    -- A list of the package versions that were successfully copied to your repository.
    successfulVersions: {string : SuccessfulPackageVersionInfo}
    -- A map of package versions that failed to copy and their error codes. The possible error codes are in
    -- the PackageVersionError data type. They are:
    -- ALREADY_EXISTS
    -- MISMATCHED_REVISION
    -- MISMATCHED_STATUS
    -- NOT_ALLOWED
    -- NOT_FOUND
    -- SKIPPED
    failedVersions: {string : PackageVersionError}
end

local record CreateDomainInput
    -- The name of the domain to create. All domain names in an Amazon Web Services Region that are in the
    -- same Amazon Web Services account must be unique. The domain name is used as the prefix in DNS hostnames. Do
    -- not use sensitive information in a domain name because it is publicly discoverable.
    domain: string
    -- The encryption key for the domain. This is used to encrypt content stored in a domain.
    -- An encryption key can be a key ID, a key Amazon Resource Name (ARN), a key alias, or a key
    -- alias ARN. To specify an encryptionKey, your IAM role must have
    -- kms:DescribeKey and kms:CreateGrant permissions on the encryption
    -- key that is used. For more information, see DescribeKey in the Key Management Service API Reference
    -- and Key Management Service API Permissions
    -- Reference in the Key Management Service Developer Guide.
    -- CodeArtifact supports only symmetric CMKs. Do not associate an asymmetric CMK with your
    -- domain. For more information, see Using symmetric and asymmetric
    -- keys in the Key Management Service Developer Guide.
    encryptionKey: string
    -- One or more tag key-value pairs for the domain.
    tags: {Tag}
end

local record CreateDomainOutput
    -- Contains information about the created domain after processing the request.
    domain: DomainDescription
end

local record CreatePackageGroupInput
    -- The name of the domain in which you want to create a package group.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The pattern of the package group to create. The pattern is also the identifier of the package group.
    packageGroup: string
    -- The contact information for the created package group.
    contactInfo: string
    -- A description of the package group.
    description: string
    -- One or more tag key-value pairs for the package group.
    tags: {Tag}
end

local record CreatePackageGroupOutput
    -- Information about the created package group after processing the request.
    packageGroup: PackageGroupDescription
end

local record CreateRepositoryInput
    -- The name of the domain that contains the created repository.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository to create.
    repository: string
    -- A description of the created repository.
    description: string
    -- A list of upstream repositories to associate with the repository. The order of the upstream repositories
    -- in the list determines their priority order when CodeArtifact looks for a requested package version. For more
    -- information, see Working with upstream repositories.
    upstreams: {UpstreamRepository}
    -- One or more tag key-value pairs for the repository.
    tags: {Tag}
end

local record CreateRepositoryOutput
    -- Information about the created repository after processing the request.
    repository: RepositoryDescription
end

local record DeleteDomainInput
    -- The name of the domain to delete.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
end

local record DeleteDomainOutput
    -- Contains information about the deleted domain after processing the request.
    domain: DomainDescription
end

local record DeleteDomainPermissionsPolicyInput
    -- The name of the domain associated with the resource policy to be deleted.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The current revision of the resource policy to be deleted. This revision is used for optimistic locking, which
    -- prevents others from overwriting your changes to the domain's resource policy.
    policyRevision: string
end

local record DeleteDomainPermissionsPolicyOutput
    -- Information about the deleted resource policy after processing the request.
    policy: ResourcePolicy
end

local record DeletePackageInput
    -- The name of the domain that contains the package to delete.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the package to delete.
    repository: string
    -- The format of the requested package to delete.
    format: string
    -- The namespace of the package to delete. The package component that specifies its namespace depends on its type. For example:
    -- The namespace is required when deleting packages of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package to delete.
    package: string
end

local record DeletePackageOutput
    deletedPackage: PackageSummary
end

local record DeletePackageGroupInput
    -- The domain that contains the package group to be deleted.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The pattern of the package group to be deleted.
    packageGroup: string
end

local record DeletePackageGroupOutput
    -- Information about the deleted package group after processing the request.
    packageGroup: PackageGroupDescription
end

local record DeletePackageVersionsInput
    -- The name of the domain that contains the package to delete.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the package versions to delete.
    repository: string
    -- The format of the package versions to delete.
    format: string
    -- The namespace of the package versions to be deleted. The package component that specifies its namespace depends on its type. For example:
    -- The namespace is required when deleting package versions of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package with the versions to delete.
    package: string
    -- An array of strings that specify the versions of the package to delete.
    versions: {string}
    -- The expected status of the package version to delete.
    expectedStatus: string
end

local record DeletePackageVersionsOutput
    -- A list of the package versions that were successfully deleted. The
    -- status of every successful version will be Deleted.
    successfulVersions: {string : SuccessfulPackageVersionInfo}
    -- A PackageVersionError object that contains a map of errors codes for the
    -- deleted package that failed. The possible error codes are:
    -- ALREADY_EXISTS
    -- MISMATCHED_REVISION
    -- MISMATCHED_STATUS
    -- NOT_ALLOWED
    -- NOT_FOUND
    -- SKIPPED
    failedVersions: {string : PackageVersionError}
end

local record DeleteRepositoryInput
    -- The name of the domain that contains the repository to delete.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository to delete.
    repository: string
end

local record DeleteRepositoryOutput
    -- Information about the deleted repository after processing the request.
    repository: RepositoryDescription
end

local record DeleteRepositoryPermissionsPolicyInput
    -- The name of the domain that contains the repository associated with the resource policy to be deleted.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that is associated with the resource policy to be deleted
    repository: string
    -- The revision of the repository's resource policy to be deleted. This revision is used for optimistic locking, which
    -- prevents others from accidentally overwriting your changes to the repository's resource policy.
    policyRevision: string
end

local record DeleteRepositoryPermissionsPolicyOutput
    -- Information about the deleted policy after processing the request.
    policy: ResourcePolicy
end

local record DescribeDomainInput
    -- A string that specifies the name of the requested domain.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
end

local record DescribeDomainOutput
    domain: DomainDescription
end

local record DescribePackageInput
    -- The name of the domain that contains the repository that contains the package.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the requested package.
    repository: string
    -- A format that specifies the type of the requested package.
    format: string
    -- The namespace of the requested package. The package component that specifies its namespace depends on its type. For example:
    -- The namespace is required when requesting packages of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the requested package.
    package: string
end

local record DescribePackageOutput
    -- A PackageDescription
    -- object that contains information about the requested package.
    package: PackageDescription
end

local record DescribePackageGroupInput
    -- The name of the domain that contains the package group.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The pattern of the requested package group.
    packageGroup: string
end

local record DescribePackageGroupOutput
    -- A PackageGroupDescription object
    -- that contains information about the requested package group.
    packageGroup: PackageGroupDescription
end

local record DescribePackageVersionInput
    -- The name of the domain that contains the repository that contains the package version.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the package version.
    repository: string
    -- A format that specifies the type of the requested package version.
    format: string
    -- The namespace of the requested package version. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace is required when requesting package versions of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the requested package version.
    package: string
    -- A string that contains the package version (for example, 3.5.2).
    packageVersion: string
end

local record DescribePackageVersionOutput
    -- A PackageVersionDescription
    -- object that contains information about the requested package version.
    packageVersion: PackageVersionDescription
end

local record DescribeRepositoryInput
    -- The name of the domain that contains the repository to describe.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- A string that specifies the name of the requested repository.
    repository: string
end

local record DescribeRepositoryOutput
    -- A RepositoryDescription object that contains the requested repository information.
    repository: RepositoryDescription
end

local record DisassociateExternalConnectionInput
    -- The name of the domain that contains the repository from which to remove the external
    -- repository.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository from which the external connection will be removed.
    repository: string
    -- The name of the external connection to be removed from the repository.
    externalConnection: string
end

local record DisassociateExternalConnectionOutput
    -- The repository associated with the removed external connection.
    repository: RepositoryDescription
end

local record DisposePackageVersionsInput
    -- The name of the domain that contains the repository you want to dispose.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the package versions you want to dispose.
    repository: string
    -- A format that specifies the type of package versions you want to dispose.
    format: string
    -- The namespace of the package versions to be disposed. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace is required when disposing package versions of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package with the versions you want to dispose.
    package: string
    -- The versions of the package you want to dispose.
    versions: {string}
    -- The revisions of the package versions you want to dispose.
    versionRevisions: {string : string}
    -- The expected status of the package version to dispose.
    expectedStatus: string
end

local record DisposePackageVersionsOutput
    -- A list of the package versions that were successfully disposed.
    successfulVersions: {string : SuccessfulPackageVersionInfo}
    -- A PackageVersionError object that contains a map of errors codes for the
    -- disposed package versions that failed. The possible error codes are:
    -- ALREADY_EXISTS
    -- MISMATCHED_REVISION
    -- MISMATCHED_STATUS
    -- NOT_ALLOWED
    -- NOT_FOUND
    -- SKIPPED
    failedVersions: {string : PackageVersionError}
end

local record GetAssociatedPackageGroupInput
    -- The name of the domain that contains the package from which to get the associated package group.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The format of the package from which to get the associated package group.
    format: string
    -- The namespace of the package from which to get the associated package group. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace is required when getting associated package groups from packages of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The package from which to get the associated package group.
    package: string
end

local record GetAssociatedPackageGroupOutput
    -- The package group that is associated with the requested package.
    packageGroup: PackageGroupDescription
    -- Describes the strength of the association between the package and package group. A strong match is also known as an
    -- exact match, and a weak match is known as a relative match.
    associationType: string
end

local record GetAuthorizationTokenInput
    -- The name of the domain that is in scope for the generated authorization token.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The time, in seconds, that the generated authorization token is valid. Valid values are
    -- 0 and any number between 900 (15 minutes) and 43200 (12 hours).
    -- A value of 0 will set the expiration of the authorization token to the same expiration of
    -- the user's role's temporary credentials.
    durationSeconds: number
end

local record GetAuthorizationTokenOutput
    -- The returned authentication token.
    authorizationToken: string
    -- A timestamp that specifies the date and time the authorization token expires.
    expiration: number
end

local record GetDomainPermissionsPolicyInput
    -- The name of the domain to which the resource policy is attached.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
end

local record GetDomainPermissionsPolicyOutput
    -- The returned resource policy.
    policy: ResourcePolicy
end

local record GetPackageVersionAssetInput
    -- The name of the domain that contains the repository that contains the package version with the requested asset.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The repository that contains the package version with the requested asset.
    repository: string
    -- A format that specifies the type of the package version with the requested asset file.
    format: string
    -- The namespace of the package version with the requested asset file. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace is required when requesting assets from package versions of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package that contains the requested asset.
    package: string
    -- A string that contains the package version (for example, 3.5.2).
    packageVersion: string
    -- The name of the requested asset.
    asset: string
    -- The name of the package version revision that contains the requested asset.
    packageVersionRevision: string
end

local record GetPackageVersionAssetOutput
    -- The binary file, or asset, that is downloaded.
    asset: function(): string, string
    -- The name of the asset that is downloaded.
    assetName: string
    -- A string that contains the package version (for example, 3.5.2).
    packageVersion: string
    -- The name of the package version revision that contains the downloaded asset.
    packageVersionRevision: string
end

local record GetPackageVersionReadmeInput
    -- The name of the domain that contains the repository that contains the package version with the requested readme file.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The repository that contains the package with the requested readme file.
    repository: string
    -- A format that specifies the type of the package version with the requested readme file.
    format: string
    -- The namespace of the package version with the requested readme file. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace is required when requesting the readme from package versions of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package version that contains the requested readme file.
    package: string
    -- A string that contains the package version (for example, 3.5.2).
    packageVersion: string
end

local record GetPackageVersionReadmeOutput
    -- The format of the package with the requested readme file.
    format: string
    -- The namespace of the package version with the requested readme file. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package that contains the returned readme file.
    package: string
    -- The version of the package with the requested readme file.
    version: string
    -- The current revision associated with the package version.
    versionRevision: string
    -- The text of the returned readme file.
    readme: string
end

local record GetRepositoryEndpointInput
    -- The name of the domain that contains the repository.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain that contains the repository. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository.
    repository: string
    -- Returns which endpoint of a repository to return. A repository has one endpoint for each
    -- package format.
    format: string
    -- A string that specifies the type of endpoint.
    endpointType: string
end

local record GetRepositoryEndpointOutput
    -- A string that specifies the URL of the returned endpoint.
    repositoryEndpoint: string
end

local record GetRepositoryPermissionsPolicyInput
    -- The name of the domain containing the repository whose associated resource policy is to be retrieved.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository whose associated resource policy is to be retrieved.
    repository: string
end

local record GetRepositoryPermissionsPolicyOutput
    -- The returned resource policy.
    policy: ResourcePolicy
end

local record ListAllowedRepositoriesForGroupInput
    -- The name of the domain that contains the package group from which to list allowed repositories.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The pattern of the package group from which to list allowed repositories.
    packageGroup: string
    -- The origin configuration restriction type of which to list allowed repositories.
    originRestrictionType: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListAllowedRepositoriesForGroupOutput
    -- The list of allowed repositories for the package group and origin configuration restriction type.
    allowedRepositories: {string}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListAssociatedPackagesInput
    -- The name of the domain that contains the package group from which to list associated packages.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The pattern of the package group from which to list associated packages.
    packageGroup: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- When this flag is included, ListAssociatedPackages will return a list of packages that would be associated with a package
    -- group, even if it does not exist.
    preview: boolean
end

local record ListAssociatedPackagesOutput
    -- The list of packages associated with the requested package group.
    packages: {AssociatedPackage}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListDomainsInput
    -- The maximum number of results to return per page.
    maxResults: number
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListDomainsOutput
    -- The returned list of DomainSummary objects.
    domains: {DomainSummary}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListPackageGroupsInput
    -- The domain for which you want to list package groups.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- A prefix for which to search package groups. When included, ListPackageGroups will return only
    -- package groups with patterns that match the prefix.
    prefix: string
end

local record ListPackageGroupsOutput
    -- The list of package groups in the requested domain.
    packageGroups: {PackageGroupSummary}
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListPackagesInput
    -- The name of the domain that contains the repository that contains the requested packages.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the requested packages.
    repository: string
    -- The format used to filter requested packages. Only packages from the provided format will be returned.
    format: string
    -- The namespace prefix used to filter requested packages.
    -- Only packages with a namespace that starts with the provided string value are returned.
    -- Note that although this option is called --namespace and not --namespace-prefix, it has prefix-matching behavior.
    -- Each package format uses namespace as follows:
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- A prefix used to filter requested packages. Only packages with names that start with
    -- packagePrefix are returned.
    packagePrefix: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The value of the Publish package origin control restriction used to filter requested packages.
    -- Only packages with the provided restriction are returned.
    -- For more information, see PackageOriginRestrictions.
    publish: string
    -- The value of the Upstream package origin control restriction used to filter requested packages.
    -- Only packages with the provided restriction are returned. For more information, see PackageOriginRestrictions.
    upstream: string
end

local record ListPackagesOutput
    -- The list of returned PackageSummary
    -- objects.
    packages: {PackageSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListPackageVersionAssetsInput
    -- The name of the domain that contains the repository associated with the package version assets.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the package that contains the requested package version assets.
    repository: string
    -- The format of the package that contains the requested package version assets.
    format: string
    -- The namespace of the package version that contains the requested package version assets. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace is required requesting assets from package versions of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package that contains the requested package version assets.
    package: string
    -- A string that contains the package version (for example, 3.5.2).
    packageVersion: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListPackageVersionAssetsOutput
    -- The format of the package that contains the requested package version assets.
    format: string
    -- The namespace of the package version that contains the requested package version assets. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package that contains the requested package version assets.
    package: string
    -- The version of the package associated with the requested assets.
    version: string
    -- The current revision associated with the package version.
    versionRevision: string
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
    -- The returned list of AssetSummary objects.
    assets: {AssetSummary}
end

local record ListPackageVersionDependenciesInput
    -- The name of the domain that contains the repository that contains the requested package version dependencies.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the requested package version.
    repository: string
    -- The format of the package with the requested dependencies.
    format: string
    -- The namespace of the package version with the requested dependencies. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace is required when listing dependencies from package versions of the following formats:
    -- Maven
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm package version is its scope.
    -- Python and NuGet package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package versions' package.
    package: string
    -- A string that contains the package version (for example, 3.5.2).
    packageVersion: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListPackageVersionDependenciesOutput
    -- A format that specifies the type of the package that contains the returned dependencies.
    format: string
    -- The namespace of the package version that contains the returned dependencies. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace is required when listing dependencies from package versions of the following formats:
    -- Maven
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm package version is its scope.
    -- Python and NuGet package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package that contains the returned package versions dependencies.
    package: string
    -- The version of the package that is specified in the request.
    version: string
    -- The current revision associated with the package version.
    versionRevision: string
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The returned list of PackageDependency objects.
    dependencies: {PackageDependency}
end

local record ListPackageVersionsInput
    -- The name of the domain that contains the repository that contains the requested package versions.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the requested package versions.
    repository: string
    -- The format of the package versions you want to list.
    format: string
    -- The namespace of the package that contains the requested package versions. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace is required when deleting package versions of the following formats:
    -- Maven
    -- Swift
    -- generic
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package for which you want to request package versions.
    package: string
    -- A string that filters the requested package versions by status.
    status: string
    -- How to sort the requested list of package versions.
    sortBy: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
    -- The originType used to filter package versions.
    -- Only package versions with the provided originType will be returned.
    originType: string
end

local record ListPackageVersionsOutput
    -- The default package version to display. This depends on the package format:
    -- For Maven and PyPI packages, it's the most recently published package version.
    -- For npm packages, it's the version referenced by the
    -- latest tag. If the latest tag is not set, it's the most recently published package version.
    defaultDisplayVersion: string
    -- A format of the package.
    format: string
    -- The namespace of the package that contains the requested package versions. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package.
    package: string
    -- The returned list of
    -- PackageVersionSummary
    -- objects.
    versions: {PackageVersionSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListRepositoriesInput
    -- A prefix used to filter returned repositories. Only repositories with names that start
    -- with repositoryPrefix are returned.
    repositoryPrefix: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListRepositoriesOutput
    -- The returned list of RepositorySummary
    -- objects.
    repositories: {RepositorySummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListRepositoriesInDomainInput
    -- The name of the domain that contains the returned list of repositories.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- Filter the list of repositories to only include those that are managed by the Amazon Web Services account ID.
    administratorAccount: string
    -- A prefix used to filter returned repositories. Only repositories with names that start with
    -- repositoryPrefix are returned.
    repositoryPrefix: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListRepositoriesInDomainOutput
    -- The returned list of repositories.
    repositories: {RepositorySummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListSubPackageGroupsInput
    -- The name of the domain which contains the package group from which to list sub package groups.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The pattern of the package group from which to list sub package groups.
    packageGroup: string
    -- The maximum number of results to return per page.
    maxResults: number
    -- The token for the next set of results. Use the value returned in the previous response in the next request to retrieve the next set of results.
    nextToken: string
end

local record ListSubPackageGroupsOutput
    -- A list of sub package groups for the requested package group.
    packageGroups: {PackageGroupSummary}
    -- If there are additional results, this is the token for the next set of results.
    nextToken: string
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the resource to get tags for.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- A list of tag key and value pairs associated with the specified resource.
    tags: {Tag}
end

local record PublishPackageVersionInput
    -- The name of the domain that contains the repository that contains the package version to publish.
    domain: string
    -- The 12-digit account number of the AWS account that owns the domain. It does not include dashes or spaces.
    domainOwner: string
    -- The name of the repository that the package version will be published to.
    repository: string
    -- A format that specifies the type of the package version with the requested asset file.
    -- The only supported value is generic.
    format: string
    -- The namespace of the package version to publish.
    namespace: string
    -- The name of the package version to publish.
    package: string
    -- The package version to publish (for example, 3.5.2).
    packageVersion: string
    -- The content of the asset to publish.
    assetContent: function(): string, string
    -- The name of the asset to publish. Asset names can include Unicode letters and numbers, and
    -- the following special characters: ~ ! @ ^ & ( ) - ` _ + [ ] { } ; , .
    -- `
    assetName: string
    -- The SHA256 hash of the assetContent to publish. This value must be calculated
    -- by the caller and provided with the request (see Publishing a generic package in the CodeArtifact User
    -- Guide).
    -- This value is used as an integrity check to verify that the assetContent has
    -- not changed after it was originally sent.
    assetSHA256: string
    -- Specifies whether the package version should remain in the unfinished
    -- state. If omitted, the package version status will be set to Published (see
    -- Package version status in the CodeArtifact User Guide).
    -- Valid values: unfinished
    unfinished: boolean
end

local record PublishPackageVersionOutput
    -- The format of the package version.
    format: string
    -- The namespace of the package version.
    namespace: string
    -- The name of the package.
    package: string
    -- The version of the package.
    version: string
    -- The revision of the package version.
    versionRevision: string
    -- A string that contains the status of the package version. For more information, see Package version status in the CodeArtifact User Guide.
    status: string
    -- An AssetSummary for the published asset.
    asset: AssetSummary
end

local record PutDomainPermissionsPolicyInput
    -- The name of the domain on which to set the resource policy.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The current revision of the resource policy to be set. This revision is used for optimistic locking, which
    -- prevents others from overwriting your changes to the domain's resource policy.
    policyRevision: string
    -- A valid displayable JSON Aspen policy string to be set as the access control resource
    -- policy on the provided domain.
    policyDocument: string
end

local record PutDomainPermissionsPolicyOutput
    -- The resource policy that was set after processing the request.
    policy: ResourcePolicy
end

local record PutPackageOriginConfigurationInput
    -- The name of the domain that contains the repository that contains the package.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository that contains the package.
    repository: string
    -- A format that specifies the type of the package to be updated.
    format: string
    -- The namespace of the package to be updated. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package to be updated.
    package: string
    -- A PackageOriginRestrictions
    -- object that contains information about the upstream and publish package origin restrictions.
    -- The upstream restriction determines if new package versions can be ingested or retained from external connections or upstream repositories.
    -- The publish restriction determines if new package versions can be published directly to the repository.
    -- You must include both the desired upstream and publish restrictions.
    restrictions: PackageOriginRestrictions
end

local record PutPackageOriginConfigurationOutput
    -- A PackageOriginConfiguration
    -- object that describes the origin configuration set for the package. It contains a
    -- PackageOriginRestrictions
    -- object that describes how new versions of the package can be introduced to the repository.
    originConfiguration: PackageOriginConfiguration
end

local record PutRepositoryPermissionsPolicyInput
    -- The name of the domain containing the repository to set the resource policy on.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository to set the resource policy on.
    repository: string
    -- Sets the revision of the resource policy that specifies permissions to access the repository.
    -- This revision is used for optimistic locking, which prevents others from overwriting your
    -- changes to the repository's resource policy.
    policyRevision: string
    -- A valid displayable JSON Aspen policy string to be set as the access control resource
    -- policy on the provided repository.
    policyDocument: string
end

local record PutRepositoryPermissionsPolicyOutput
    -- The resource policy that was set after processing the request.
    policy: ResourcePolicy
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the resource that you want to add or update tags for.
    resourceArn: string
    -- The tags you want to modify or add to the resource.
    tags: {Tag}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the resource that you want to remove tags from.
    resourceArn: string
    -- The tag key for each tag that you want to remove from the resource.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdatePackageGroupInput
    -- The name of the domain which contains the package group to be updated.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The pattern of the package group to be updated.
    packageGroup: string
    -- Contact information which you want to update the requested package group with.
    contactInfo: string
    -- The description you want to update the requested package group with.
    description: string
end

local record UpdatePackageGroupOutput
    -- The package group and information about it after the request has been processed.
    packageGroup: PackageGroupDescription
end

local record UpdatePackageGroupOriginConfigurationInput
    -- The name of the domain which contains the package group for which to update the origin configuration.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The pattern of the package group for which to update the origin configuration.
    packageGroup: string
    -- The origin configuration settings that determine how package versions can enter repositories.
    restrictions: {string : string}
    -- The repository name and restrictions to add to the allowed repository list of the specified package group.
    addAllowedRepositories: {PackageGroupAllowedRepository}
    -- The repository name and restrictions to remove from the allowed repository list of the specified package group.
    removeAllowedRepositories: {PackageGroupAllowedRepository}
end

local record UpdatePackageGroupOriginConfigurationOutput
    -- The package group and information about it after processing the request.
    packageGroup: PackageGroupDescription
    -- Information about the updated allowed repositories after processing the request.
    allowedRepositoryUpdates: {string : {string : {string}}}
end

local record UpdatePackageVersionsStatusInput
    -- The name of the domain that contains the repository that contains the package versions with a status to be updated.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The repository that contains the package versions with the status you want to update.
    repository: string
    -- A format that specifies the type of the package with the statuses to update.
    format: string
    -- The namespace of the package version to be updated. The package component that specifies its
    -- namespace depends on its type. For example:
    -- The namespace of a Maven package version is its groupId.
    -- The namespace of an npm or Swift package version is its scope.
    -- The namespace of a generic package is its namespace.
    -- Python, NuGet, Ruby, and Cargo package versions do not contain a corresponding component, package versions
    -- of those formats do not have a namespace.
    namespace: string
    -- The name of the package with the version statuses to update.
    package: string
    -- An array of strings that specify the versions of the package with the statuses to update.
    versions: {string}
    -- A map of package versions and package version revisions. The map key is the
    -- package version (for example, 3.5.2), and the map value is the
    -- package version revision.
    versionRevisions: {string : string}
    -- The package version’s expected status before it is updated. If
    -- expectedStatus is provided, the package version's status is updated only if its
    -- status at the time UpdatePackageVersionsStatus is called matches
    -- expectedStatus.
    expectedStatus: string
    -- The status you want to change the package version status to.
    targetStatus: string
end

local record UpdatePackageVersionsStatusOutput
    -- A list of PackageVersionError objects, one for each package version with
    -- a status that failed to update.
    successfulVersions: {string : SuccessfulPackageVersionInfo}
    -- A list of SuccessfulPackageVersionInfo objects, one for each package version
    -- with a status that successfully updated.
    failedVersions: {string : PackageVersionError}
end

local record UpdateRepositoryInput
    -- The name of the domain associated with the repository to update.
    domain: string
    -- The 12-digit account number of the Amazon Web Services account that owns the domain. It does not include
    -- dashes or spaces.
    domainOwner: string
    -- The name of the repository to update.
    repository: string
    -- An updated repository description.
    description: string
    -- A list of upstream repositories to associate with the repository. The order of the upstream repositories
    -- in the list determines their priority order when CodeArtifact looks for a requested package version. For more
    -- information, see Working with upstream repositories.
    upstreams: {UpstreamRepository}
end

local record UpdateRepositoryOutput
    -- The updated repository.
    repository: RepositoryDescription
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

-- The operation did not succeed because of an unauthorized access attempt.
local record AccessDeniedException
    message: string
end

-- The operation did not succeed because prerequisites are not met.
local record ConflictException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because of an error that occurred inside CodeArtifact.
local record InternalServerException
    message: string
end

-- The operation did not succeed because the resource requested is not found in the service.
local record ResourceNotFoundException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because it would have exceeded a service limit for your account.
local record ServiceQuotaExceededException
    message: string
    -- The ID of the resource.
    resourceId: string
    -- The type of Amazon Web Services resource.
    resourceType: string
end

-- The operation did not succeed because too many requests are sent to the service.
local record ThrottlingException
    message: string
    -- The time period, in seconds, to wait before retrying the request.
    retryAfterSeconds: number
end

-- The operation did not succeed because a parameter in the request was sent with an invalid value.
local record ValidationException
    message: string
    reason: string
end

local type AllowPublish = "ALLOW" | "BLOCK"

local type AllowUpstream = "ALLOW" | "BLOCK"

local type EndpointType = "dualstack" | "ipv4"

local type PackageFormat = "npm" | "pypi" | "maven" | "nuget" | "generic" | "ruby" | "swift" | "cargo"

local type PackageGroupAssociationType = "STRONG" | "WEAK"

local type PackageGroupOriginRestrictionType = "EXTERNAL_UPSTREAM" | "INTERNAL_UPSTREAM" | "PUBLISH"

local type PackageVersionOriginType = "INTERNAL" | "EXTERNAL" | "UNKNOWN"

local type PackageVersionSortType = "PUBLISHED_TIME"

local type PackageVersionStatus = "Published" | "Unfinished" | "Unlisted" | "Archived" | "Disposed" | "Deleted"

local record M
    AssociateExternalConnectionInput: AssociateExternalConnectionInput
    AssociateExternalConnectionOutput: AssociateExternalConnectionOutput
    CopyPackageVersionsInput: CopyPackageVersionsInput
    CopyPackageVersionsOutput: CopyPackageVersionsOutput
    CreateDomainInput: CreateDomainInput
    CreateDomainOutput: CreateDomainOutput
    CreatePackageGroupInput: CreatePackageGroupInput
    CreatePackageGroupOutput: CreatePackageGroupOutput
    CreateRepositoryInput: CreateRepositoryInput
    CreateRepositoryOutput: CreateRepositoryOutput
    DeleteDomainInput: DeleteDomainInput
    DeleteDomainOutput: DeleteDomainOutput
    DeleteDomainPermissionsPolicyInput: DeleteDomainPermissionsPolicyInput
    DeleteDomainPermissionsPolicyOutput: DeleteDomainPermissionsPolicyOutput
    DeletePackageInput: DeletePackageInput
    DeletePackageOutput: DeletePackageOutput
    DeletePackageGroupInput: DeletePackageGroupInput
    DeletePackageGroupOutput: DeletePackageGroupOutput
    DeletePackageVersionsInput: DeletePackageVersionsInput
    DeletePackageVersionsOutput: DeletePackageVersionsOutput
    DeleteRepositoryInput: DeleteRepositoryInput
    DeleteRepositoryOutput: DeleteRepositoryOutput
    DeleteRepositoryPermissionsPolicyInput: DeleteRepositoryPermissionsPolicyInput
    DeleteRepositoryPermissionsPolicyOutput: DeleteRepositoryPermissionsPolicyOutput
    DescribeDomainInput: DescribeDomainInput
    DescribeDomainOutput: DescribeDomainOutput
    DescribePackageInput: DescribePackageInput
    DescribePackageOutput: DescribePackageOutput
    DescribePackageGroupInput: DescribePackageGroupInput
    DescribePackageGroupOutput: DescribePackageGroupOutput
    DescribePackageVersionInput: DescribePackageVersionInput
    DescribePackageVersionOutput: DescribePackageVersionOutput
    DescribeRepositoryInput: DescribeRepositoryInput
    DescribeRepositoryOutput: DescribeRepositoryOutput
    DisassociateExternalConnectionInput: DisassociateExternalConnectionInput
    DisassociateExternalConnectionOutput: DisassociateExternalConnectionOutput
    DisposePackageVersionsInput: DisposePackageVersionsInput
    DisposePackageVersionsOutput: DisposePackageVersionsOutput
    GetAssociatedPackageGroupInput: GetAssociatedPackageGroupInput
    GetAssociatedPackageGroupOutput: GetAssociatedPackageGroupOutput
    GetAuthorizationTokenInput: GetAuthorizationTokenInput
    GetAuthorizationTokenOutput: GetAuthorizationTokenOutput
    GetDomainPermissionsPolicyInput: GetDomainPermissionsPolicyInput
    GetDomainPermissionsPolicyOutput: GetDomainPermissionsPolicyOutput
    GetPackageVersionAssetInput: GetPackageVersionAssetInput
    GetPackageVersionAssetOutput: GetPackageVersionAssetOutput
    GetPackageVersionReadmeInput: GetPackageVersionReadmeInput
    GetPackageVersionReadmeOutput: GetPackageVersionReadmeOutput
    GetRepositoryEndpointInput: GetRepositoryEndpointInput
    GetRepositoryEndpointOutput: GetRepositoryEndpointOutput
    GetRepositoryPermissionsPolicyInput: GetRepositoryPermissionsPolicyInput
    GetRepositoryPermissionsPolicyOutput: GetRepositoryPermissionsPolicyOutput
    ListAllowedRepositoriesForGroupInput: ListAllowedRepositoriesForGroupInput
    ListAllowedRepositoriesForGroupOutput: ListAllowedRepositoriesForGroupOutput
    ListAssociatedPackagesInput: ListAssociatedPackagesInput
    ListAssociatedPackagesOutput: ListAssociatedPackagesOutput
    ListDomainsInput: ListDomainsInput
    ListDomainsOutput: ListDomainsOutput
    ListPackageGroupsInput: ListPackageGroupsInput
    ListPackageGroupsOutput: ListPackageGroupsOutput
    ListPackagesInput: ListPackagesInput
    ListPackagesOutput: ListPackagesOutput
    ListPackageVersionAssetsInput: ListPackageVersionAssetsInput
    ListPackageVersionAssetsOutput: ListPackageVersionAssetsOutput
    ListPackageVersionDependenciesInput: ListPackageVersionDependenciesInput
    ListPackageVersionDependenciesOutput: ListPackageVersionDependenciesOutput
    ListPackageVersionsInput: ListPackageVersionsInput
    ListPackageVersionsOutput: ListPackageVersionsOutput
    ListRepositoriesInput: ListRepositoriesInput
    ListRepositoriesOutput: ListRepositoriesOutput
    ListRepositoriesInDomainInput: ListRepositoriesInDomainInput
    ListRepositoriesInDomainOutput: ListRepositoriesInDomainOutput
    ListSubPackageGroupsInput: ListSubPackageGroupsInput
    ListSubPackageGroupsOutput: ListSubPackageGroupsOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    PublishPackageVersionInput: PublishPackageVersionInput
    PublishPackageVersionOutput: PublishPackageVersionOutput
    PutDomainPermissionsPolicyInput: PutDomainPermissionsPolicyInput
    PutDomainPermissionsPolicyOutput: PutDomainPermissionsPolicyOutput
    PutPackageOriginConfigurationInput: PutPackageOriginConfigurationInput
    PutPackageOriginConfigurationOutput: PutPackageOriginConfigurationOutput
    PutRepositoryPermissionsPolicyInput: PutRepositoryPermissionsPolicyInput
    PutRepositoryPermissionsPolicyOutput: PutRepositoryPermissionsPolicyOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdatePackageGroupInput: UpdatePackageGroupInput
    UpdatePackageGroupOutput: UpdatePackageGroupOutput
    UpdatePackageGroupOriginConfigurationInput: UpdatePackageGroupOriginConfigurationInput
    UpdatePackageGroupOriginConfigurationOutput: UpdatePackageGroupOriginConfigurationOutput
    UpdatePackageVersionsStatusInput: UpdatePackageVersionsStatusInput
    UpdatePackageVersionsStatusOutput: UpdatePackageVersionsStatusOutput
    UpdateRepositoryInput: UpdateRepositoryInput
    UpdateRepositoryOutput: UpdateRepositoryOutput
end

return M
