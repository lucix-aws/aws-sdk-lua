-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record CreateConnectorInput
        -- The identifier of the Active Directory.
        DirectoryId: string
        -- The Amazon Resource Name (ARN) of the certificate authority being used.
        CertificateAuthorityArn: string
        -- Information about your VPC and security groups used with the connector.
        VpcInformation: VpcInformation
        -- Idempotency token.
        ClientToken: string
        -- Metadata assigned to a connector consisting of a key-value pair.
        Tags: {string : string}
    end

    record CreateConnectorOutput
        -- If successful, the Amazon Resource Name (ARN) of the connector for Active Directory.
        ConnectorArn: string
    end

    record CreateDirectoryRegistrationInput
        -- The identifier of the Active Directory.
        DirectoryId: string
        -- Idempotency token.
        ClientToken: string
        -- Metadata assigned to a directory registration consisting of a key-value pair.
        Tags: {string : string}
    end

    record CreateDirectoryRegistrationOutput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration.
        DirectoryRegistrationArn: string
    end

    record CreateServicePrincipalNameInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration.
        DirectoryRegistrationArn: string
        -- The Amazon Resource Name (ARN) that was returned when you called CreateConnector.
        ConnectorArn: string
        -- Idempotency token.
        ClientToken: string
    end

    record CreateServicePrincipalNameOutput
    end

    record CreateTemplateInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateConnector.
        ConnectorArn: string
        -- Name of the template. The template name must be unique.
        Name: string
        -- Template configuration to define the information included in certificates. Define
        -- certificate validity and renewal periods, certificate request handling and enrollment
        -- options, key usage extensions, application policies, and cryptography settings.
        Definition: TemplateDefinition
        -- Idempotency token.
        ClientToken: string
        -- Metadata assigned to a template consisting of a key-value pair.
        Tags: {string : string}
    end

    record CreateTemplateOutput
        -- If successful, the Amazon Resource Name (ARN) of the template.
        TemplateArn: string
    end

    record CreateTemplateGroupAccessControlEntryInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateTemplate.
        TemplateArn: string
        -- Security identifier (SID) of the group object from Active Directory. The SID starts with
        -- "S-".
        GroupSecurityIdentifier: string
        -- Name of the Active Directory group. This name does not need to match the group name in Active Directory.
        GroupDisplayName: string
        -- Allow or deny permissions for an Active Directory group to enroll or autoenroll certificates for a
        -- template.
        AccessRights: AccessRights
        -- Idempotency token.
        ClientToken: string
    end

    record CreateTemplateGroupAccessControlEntryOutput
    end

    record DeleteConnectorInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateConnector.
        ConnectorArn: string
    end

    record DeleteConnectorOutput
    end

    record DeleteDirectoryRegistrationInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration.
        DirectoryRegistrationArn: string
    end

    record DeleteDirectoryRegistrationOutput
    end

    record DeleteServicePrincipalNameInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration.
        DirectoryRegistrationArn: string
        -- The Amazon Resource Name (ARN) that was returned when you called CreateConnector.
        ConnectorArn: string
    end

    record DeleteServicePrincipalNameOutput
    end

    record DeleteTemplateInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateTemplate.
        TemplateArn: string
    end

    record DeleteTemplateOutput
    end

    record DeleteTemplateGroupAccessControlEntryInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateTemplate.
        TemplateArn: string
        -- Security identifier (SID) of the group object from Active Directory. The SID starts with
        -- "S-".
        GroupSecurityIdentifier: string
    end

    record DeleteTemplateGroupAccessControlEntryOutput
    end

    record GetConnectorInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateConnector.
        ConnectorArn: string
    end

    record GetConnectorOutput
        -- A structure that contains information about your connector.
        Connector: Connector
    end

    record GetDirectoryRegistrationInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration.
        DirectoryRegistrationArn: string
    end

    record GetDirectoryRegistrationOutput
        -- The directory registration represents the authorization of the connector service with a
        -- directory.
        DirectoryRegistration: DirectoryRegistration
    end

    record GetServicePrincipalNameInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration.
        DirectoryRegistrationArn: string
        -- The Amazon Resource Name (ARN) that was returned when you called CreateConnector.
        ConnectorArn: string
    end

    record GetServicePrincipalNameOutput
        -- The service principal name that the connector uses to authenticate with Active Directory.
        ServicePrincipalName: ServicePrincipalName
    end

    record GetTemplateInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateTemplate.
        TemplateArn: string
    end

    record GetTemplateOutput
        -- A certificate template that the connector uses to issue certificates from a private
        -- CA.
        Template: Template
    end

    record GetTemplateGroupAccessControlEntryInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateTemplate.
        TemplateArn: string
        -- Security identifier (SID) of the group object from Active Directory. The SID starts with
        -- "S-".
        GroupSecurityIdentifier: string
    end

    record GetTemplateGroupAccessControlEntryOutput
        -- An access control entry allows or denies an Active Directory group from enrolling and/or
        -- autoenrolling with a template.
        AccessControlEntry: AccessControlEntry
    end

    record ListConnectorsInput
        -- Use this parameter when paginating results to specify the maximum number of items to
        -- return in the response on each page. If additional items exist beyond the number you
        -- specify, the NextToken element is sent in the response. Use this
        -- NextToken value in a subsequent request to retrieve additional
        -- items.
        MaxResults: number
        -- Use this parameter when paginating results in a subsequent request after you receive a
        -- response with truncated results. Set it to the value of the NextToken
        -- parameter from the response you just received.
        NextToken: string
    end

    record ListConnectorsOutput
        -- Summary information about each connector you have created.
        Connectors: {ConnectorSummary}
        -- Use this parameter when paginating results in a subsequent request after you receive a
        -- response with truncated results. Set it to the value of the NextToken parameter from the
        -- response you just received.
        NextToken: string
    end

    record ListDirectoryRegistrationsInput
        -- Use this parameter when paginating results to specify the maximum number of items to
        -- return in the response on each page. If additional items exist beyond the number you
        -- specify, the NextToken element is sent in the response. Use this
        -- NextToken value in a subsequent request to retrieve additional
        -- items.
        MaxResults: number
        -- Use this parameter when paginating results in a subsequent request after you receive a
        -- response with truncated results. Set it to the value of the NextToken
        -- parameter from the response you just received.
        NextToken: string
    end

    record ListDirectoryRegistrationsOutput
        -- Summary information about each directory registration you have created.
        DirectoryRegistrations: {DirectoryRegistrationSummary}
        -- Use this parameter when paginating results in a subsequent request after you receive a
        -- response with truncated results. Set it to the value of the NextToken
        -- parameter from the response you just received.
        NextToken: string
    end

    record ListServicePrincipalNamesInput
        -- Use this parameter when paginating results to specify the maximum number of items to
        -- return in the response on each page. If additional items exist beyond the number you
        -- specify, the NextToken element is sent in the response. Use this
        -- NextToken value in a subsequent request to retrieve additional
        -- items.
        MaxResults: number
        -- Use this parameter when paginating results in a subsequent request after you receive a
        -- response with truncated results. Set it to the value of the NextToken
        -- parameter from the response you just received.
        NextToken: string
        -- The Amazon Resource Name (ARN) that was returned when you called CreateDirectoryRegistration.
        DirectoryRegistrationArn: string
    end

    record ListServicePrincipalNamesOutput
        -- The service principal name, if any, that the connector uses to authenticate with
        -- Active Directory.
        ServicePrincipalNames: {ServicePrincipalNameSummary}
        -- Use this parameter when paginating results in a subsequent request after you receive a
        -- response with truncated results. Set it to the value of the NextToken
        -- parameter from the response you just received.
        NextToken: string
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) that was returned when you created the resource.
        ResourceArn: string
    end

    record ListTagsForResourceOutput
        -- The tags, if any, that are associated with your resource.
        Tags: {string : string}
    end

    record ListTemplateGroupAccessControlEntriesInput
        -- Use this parameter when paginating results to specify the maximum number of items to
        -- return in the response on each page. If additional items exist beyond the number you
        -- specify, the NextToken element is sent in the response. Use this
        -- NextToken value in a subsequent request to retrieve additional
        -- items.
        MaxResults: number
        -- Use this parameter when paginating results in a subsequent request after you receive a
        -- response with truncated results. Set it to the value of the NextToken
        -- parameter from the response you just received.
        NextToken: string
        -- The Amazon Resource Name (ARN) that was returned when you called CreateTemplate.
        TemplateArn: string
    end

    record ListTemplateGroupAccessControlEntriesOutput
        -- An access control entry grants or denies permission to an Active Directory group to enroll
        -- certificates for a template.
        AccessControlEntries: {AccessControlEntrySummary}
        -- Use this parameter when paginating results in a subsequent request after you receive a
        -- response with truncated results. Set it to the value of the NextToken
        -- parameter from the response you just received.
        NextToken: string
    end

    record ListTemplatesInput
        -- Use this parameter when paginating results to specify the maximum number of items to
        -- return in the response on each page. If additional items exist beyond the number you
        -- specify, the NextToken element is sent in the response. Use this
        -- NextToken value in a subsequent request to retrieve additional
        -- items.
        MaxResults: number
        -- Use this parameter when paginating results in a subsequent request after you receive a
        -- response with truncated results. Set it to the value of the NextToken
        -- parameter from the response you just received.
        NextToken: string
        -- The Amazon Resource Name (ARN) that was returned when you called CreateConnector.
        ConnectorArn: string
    end

    record ListTemplatesOutput
        -- Custom configuration templates used when issuing a certificate.
        Templates: {TemplateSummary}
        -- Use this parameter when paginating results in a subsequent request after you receive a
        -- response with truncated results. Set it to the value of the NextToken
        -- parameter from the response you just received.
        NextToken: string
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) that was returned when you created the resource.
        ResourceArn: string
        -- Metadata assigned to a directory registration consisting of a key-value pair.
        Tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) that was returned when you created the resource.
        ResourceArn: string
        -- Specifies a list of tag keys that you want to remove from the specified
        -- resources.
        TagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateTemplateInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateTemplate.
        TemplateArn: string
        -- Template configuration to define the information included in certificates. Define
        -- certificate validity and renewal periods, certificate request handling and enrollment
        -- options, key usage extensions, application policies, and cryptography settings.
        Definition: TemplateDefinition
        -- This setting allows the major version of a template to be increased automatically. All
        -- members of Active Directory groups that are allowed to enroll with a template will receive a new
        -- certificate issued using that template.
        ReenrollAllCertificateHolders: boolean
    end

    record UpdateTemplateOutput
    end

    record UpdateTemplateGroupAccessControlEntryInput
        -- The Amazon Resource Name (ARN) that was returned when you called CreateTemplate.
        TemplateArn: string
        -- Security identifier (SID) of the group object from Active Directory. The SID starts with
        -- "S-".
        GroupSecurityIdentifier: string
        -- Name of the Active Directory group. This name does not need to match the group name in Active Directory.
        GroupDisplayName: string
        -- Allow or deny permissions for an Active Directory group to enroll or autoenroll certificates for a
        -- template.
        AccessRights: AccessRights
    end

    record UpdateTemplateGroupAccessControlEntryOutput
    end

    -- You can receive this error if you attempt to create a resource share when you don't have
    -- the required permissions. This can be caused by insufficient permissions in policies
    -- attached to your Amazon Web Services Identity and Access Management (IAM) principal. It can also happen
    -- because of restrictions in place from an Amazon Web Services Organizations service control policy (SCP)
    -- that affects your Amazon Web Services account.
    record AccessDeniedException
        Message: string
    end

    -- This request cannot be completed for one of the following reasons because the requested
    -- resource was being concurrently modified by another request.
    record ConflictException
        Message: string
        -- The identifier of the Amazon Web Services resource.
        ResourceId: string
        -- The resource type, which can be one of Connector, Template, TemplateGroupAccessControlEntry, ServicePrincipalName, or DirectoryRegistration.
        ResourceType: string
    end

    -- The request processing has failed because of an unknown error, exception or failure with
    -- an internal server.
    record InternalServerException
        Message: string
    end

    -- The operation tried to access a nonexistent resource. The resource might not be
    -- specified correctly, or its status might not be ACTIVE.
    record ResourceNotFoundException
        Message: string
        -- The identifier of the Amazon Web Services resource.
        ResourceId: string
        -- The resource type, which can be one of Connector, Template, TemplateGroupAccessControlEntry, ServicePrincipalName, or DirectoryRegistration.
        ResourceType: string
    end

    -- Request would cause a service quota to be exceeded.
    record ServiceQuotaExceededException
        Message: string
        -- The identifier of the Amazon Web Services resource.
        ResourceId: string
        -- The resource type, which can be one of Connector, Template, TemplateGroupAccessControlEntry, ServicePrincipalName, or DirectoryRegistration.
        ResourceType: string
        -- Identifies the originating service.
        ServiceCode: string
        -- The code associated with the service quota.
        QuotaCode: string
    end

    -- The limit on the number of requests per second was exceeded.
    record ThrottlingException
        Message: string
        -- Identifies the originating service.
        ServiceCode: string
        -- The code associated with the quota.
        QuotaCode: string
    end

    -- An input validation error occurred. For example, invalid characters in a template name,
    -- or if a pagination token is invalid.
    record ValidationException
        Message: string
        -- The reason for the validation error. This won't be return for every
        -- validation exception.
        Reason: string
    end

end

return M
