-- Code generated by lua-client-codegen. DO NOT EDIT.

local record DeleteConnectionInput
    -- The unique identifier of the MQTT client to disconnect. The client ID can't start with a dollar sign ($).
    clientId: string
    -- Specifies whether to remove the client's session state when disconnecting. Set to TRUE to delete all session information, including subscriptions and queued messages. Set to FALSE to preserve the session state. By default, this is set to FALSE (preserves the session state).
    cleanSession: boolean
    -- Controls if Amazon Web Services IoT Core publishes the client's Last Will and Testament (LWT) message upon disconnection. Set to TRUE to prevent publishing the LWT message. Set to FALSE to allow publishing. By default, this is set to FALSE (allows publishing the LWT message).
    preventWillMessage: boolean
end

local record DeleteConnectionOutput
end

-- The input for the DeleteThingShadow operation.
local record DeleteThingShadowInput
    -- The name of the thing.
    thingName: string
    -- The name of the shadow.
    shadowName: string
end

-- The output from the DeleteThingShadow operation.
local record DeleteThingShadowOutput
    -- The state information, in JSON format.
    payload: string
end

-- The input for the GetRetainedMessage operation.
local record GetRetainedMessageInput
    -- The topic name of the retained message to retrieve.
    topic: string
end

-- The output from the GetRetainedMessage operation.
local record GetRetainedMessageOutput
    -- The topic name to which the retained message was published.
    topic: string
    -- The Base64-encoded message payload of the retained message body.
    payload: string
    -- The quality of service (QoS) level used to publish the retained message.
    qos: number
    -- The Epoch date and time, in milliseconds, when the retained message was stored by IoT.
    lastModifiedTime: number
    -- A base64-encoded JSON string that includes an array of JSON objects, or null if the
    -- retained message doesn't include any user properties.
    -- The following example userProperties parameter is a JSON string that
    -- represents two user properties. Note that it will be base64-encoded:
    -- [{"deviceName": "alpha"}, {"deviceCnt": "45"}]
    userProperties: string
end

-- The input for the GetThingShadow operation.
local record GetThingShadowInput
    -- The name of the thing.
    thingName: string
    -- The name of the shadow.
    shadowName: string
end

-- The output from the GetThingShadow operation.
local record GetThingShadowOutput
    -- The state information, in JSON format.
    payload: string
end

local record ListNamedShadowsForThingInput
    -- The name of the thing.
    thingName: string
    -- The token to retrieve the next set of results.
    nextToken: string
    -- The result page size.
    pageSize: number
end

local record ListNamedShadowsForThingOutput
    -- The list of shadows for the specified thing.
    results: {string}
    -- The token to use to get the next set of results, or null if there are no additional results.
    nextToken: string
    -- The Epoch date and time the response was generated by IoT.
    timestamp: number
end

local record ListRetainedMessagesInput
    -- To retrieve the next set of results, the nextToken
    -- value from a previous response; otherwise null to receive
    -- the first set of results.
    nextToken: string
    -- The maximum number of results to return at one time.
    maxResults: number
end

local record ListRetainedMessagesOutput
    -- A summary list the account's retained messages. The information returned doesn't include
    -- the message payloads of the retained messages.
    retainedTopics: {RetainedMessageSummary}
    -- The token for the next set of results, or null if there are no additional results.
    nextToken: string
end

-- The input for the Publish operation.
local record PublishInput
    -- The name of the MQTT topic.
    topic: string
    -- The Quality of Service (QoS) level. The default QoS level is 0.
    qos: number
    -- A Boolean value that determines whether to set the RETAIN flag when the message is published.
    -- Setting the RETAIN flag causes the message to be retained and sent to new subscribers to the topic.
    -- Valid values: true | false
    -- Default value: false
    retain: boolean
    -- The message body. MQTT accepts text, binary, and empty (null) message payloads.
    -- Publishing an empty (null) payload with retain =
    -- true deletes the retained message identified by topic from Amazon Web Services IoT Core.
    payload: string
    -- A JSON string that contains an array of JSON objects. If you don’t use Amazon Web Services SDK or CLI,
    -- you must encode the JSON string to base64 format before adding it to the HTTP header.
    -- userProperties is an HTTP header value in the API.
    -- The following example userProperties parameter is a JSON string which
    -- represents two User Properties. Note that it needs to be base64-encoded:
    -- [{"deviceName": "alpha"}, {"deviceCnt": "45"}]
    userProperties: string
    -- An Enum string value that indicates whether the payload is formatted as
    -- UTF-8. payloadFormatIndicator is an HTTP header value in the API.
    payloadFormatIndicator: string
    -- A UTF-8 encoded string that describes the content of the publishing message.
    contentType: string
    -- A UTF-8 encoded string that's used as the topic name for a response message. The response
    -- topic is used to describe the topic which the receiver should publish to as part of the
    -- request-response flow. The topic must not contain wildcard characters.
    responseTopic: string
    -- The base64-encoded binary data used by the sender of the request message to identify which
    -- request the response message is for when it's received. correlationData is an
    -- HTTP header value in the API.
    correlationData: string
    -- A user-defined integer value that represents the message expiry interval in seconds. If
    -- absent, the message doesn't expire. For more information about the limits of
    -- messageExpiry, see Amazon Web Services IoT Core message broker and
    -- protocol limits and quotas from the Amazon Web Services Reference Guide.
    messageExpiry: number
end

local record PublishOutput
end

-- The input for the UpdateThingShadow operation.
local record UpdateThingShadowInput
    -- The name of the thing.
    thingName: string
    -- The name of the shadow.
    shadowName: string
    -- The state information, in JSON format.
    payload: string
end

-- The output from the UpdateThingShadow operation.
local record UpdateThingShadowOutput
    -- The state information, in JSON format.
    payload: string
end

-- The caller isn't authorized to make the request.
local record ForbiddenException
    message: string
end

-- An unexpected error has occurred.
local record InternalFailureException
    -- The message for the exception.
    message: string
end

-- The request is not valid.
local record InvalidRequestException
    -- The message for the exception.
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    -- The message for the exception.
    message: string
end

-- The rate exceeds the limit.
local record ThrottlingException
    -- The message for the exception.
    message: string
end

-- An unexpected error has occurred.
local record InternalFailureException
    -- The message for the exception.
    message: string
end

-- The request is not valid.
local record InvalidRequestException
    -- The message for the exception.
    message: string
end

-- The specified combination of HTTP verb and URI is not supported.
local record MethodNotAllowedException
    -- The message for the exception.
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    -- The message for the exception.
    message: string
end

-- The service is temporarily unavailable.
local record ServiceUnavailableException
    -- The message for the exception.
    message: string
end

-- The rate exceeds the limit.
local record ThrottlingException
    -- The message for the exception.
    message: string
end

-- You are not authorized to perform this operation.
local record UnauthorizedException
    -- The message for the exception.
    message: string
end

-- The document encoding is not supported.
local record UnsupportedDocumentEncodingException
    -- The message for the exception.
    message: string
end

-- An unexpected error has occurred.
local record InternalFailureException
    -- The message for the exception.
    message: string
end

-- The request is not valid.
local record InvalidRequestException
    -- The message for the exception.
    message: string
end

-- The specified combination of HTTP verb and URI is not supported.
local record MethodNotAllowedException
    -- The message for the exception.
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    -- The message for the exception.
    message: string
end

-- The service is temporarily unavailable.
local record ServiceUnavailableException
    -- The message for the exception.
    message: string
end

-- The rate exceeds the limit.
local record ThrottlingException
    -- The message for the exception.
    message: string
end

-- You are not authorized to perform this operation.
local record UnauthorizedException
    -- The message for the exception.
    message: string
end

-- An unexpected error has occurred.
local record InternalFailureException
    -- The message for the exception.
    message: string
end

-- The request is not valid.
local record InvalidRequestException
    -- The message for the exception.
    message: string
end

-- The specified combination of HTTP verb and URI is not supported.
local record MethodNotAllowedException
    -- The message for the exception.
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    -- The message for the exception.
    message: string
end

-- The service is temporarily unavailable.
local record ServiceUnavailableException
    -- The message for the exception.
    message: string
end

-- The rate exceeds the limit.
local record ThrottlingException
    -- The message for the exception.
    message: string
end

-- You are not authorized to perform this operation.
local record UnauthorizedException
    -- The message for the exception.
    message: string
end

-- The document encoding is not supported.
local record UnsupportedDocumentEncodingException
    -- The message for the exception.
    message: string
end

-- An unexpected error has occurred.
local record InternalFailureException
    -- The message for the exception.
    message: string
end

-- The request is not valid.
local record InvalidRequestException
    -- The message for the exception.
    message: string
end

-- The specified combination of HTTP verb and URI is not supported.
local record MethodNotAllowedException
    -- The message for the exception.
    message: string
end

-- The specified resource does not exist.
local record ResourceNotFoundException
    -- The message for the exception.
    message: string
end

-- The service is temporarily unavailable.
local record ServiceUnavailableException
    -- The message for the exception.
    message: string
end

-- The rate exceeds the limit.
local record ThrottlingException
    -- The message for the exception.
    message: string
end

-- You are not authorized to perform this operation.
local record UnauthorizedException
    -- The message for the exception.
    message: string
end

-- An unexpected error has occurred.
local record InternalFailureException
    -- The message for the exception.
    message: string
end

-- The request is not valid.
local record InvalidRequestException
    -- The message for the exception.
    message: string
end

-- The specified combination of HTTP verb and URI is not supported.
local record MethodNotAllowedException
    -- The message for the exception.
    message: string
end

-- The service is temporarily unavailable.
local record ServiceUnavailableException
    -- The message for the exception.
    message: string
end

-- The rate exceeds the limit.
local record ThrottlingException
    -- The message for the exception.
    message: string
end

-- You are not authorized to perform this operation.
local record UnauthorizedException
    -- The message for the exception.
    message: string
end

-- An unexpected error has occurred.
local record InternalFailureException
    -- The message for the exception.
    message: string
end

-- The request is not valid.
local record InvalidRequestException
    -- The message for the exception.
    message: string
end

-- The specified combination of HTTP verb and URI is not supported.
local record MethodNotAllowedException
    -- The message for the exception.
    message: string
end

-- The rate exceeds the limit.
local record ThrottlingException
    -- The message for the exception.
    message: string
end

-- You are not authorized to perform this operation.
local record UnauthorizedException
    -- The message for the exception.
    message: string
end

-- The specified version does not match the version of the document.
local record ConflictException
    -- The message for the exception.
    message: string
end

-- An unexpected error has occurred.
local record InternalFailureException
    -- The message for the exception.
    message: string
end

-- The request is not valid.
local record InvalidRequestException
    -- The message for the exception.
    message: string
end

-- The specified combination of HTTP verb and URI is not supported.
local record MethodNotAllowedException
    -- The message for the exception.
    message: string
end

-- The payload exceeds the maximum size allowed.
local record RequestEntityTooLargeException
    -- The message for the exception.
    message: string
end

-- The service is temporarily unavailable.
local record ServiceUnavailableException
    -- The message for the exception.
    message: string
end

-- The rate exceeds the limit.
local record ThrottlingException
    -- The message for the exception.
    message: string
end

-- You are not authorized to perform this operation.
local record UnauthorizedException
    -- The message for the exception.
    message: string
end

-- The document encoding is not supported.
local record UnsupportedDocumentEncodingException
    -- The message for the exception.
    message: string
end

local type PayloadFormatIndicator = "UNSPECIFIED_BYTES" | "UTF8_DATA"

local record M
    DeleteConnectionInput: DeleteConnectionInput
    DeleteConnectionOutput: DeleteConnectionOutput
    DeleteThingShadowInput: DeleteThingShadowInput
    DeleteThingShadowOutput: DeleteThingShadowOutput
    GetRetainedMessageInput: GetRetainedMessageInput
    GetRetainedMessageOutput: GetRetainedMessageOutput
    GetThingShadowInput: GetThingShadowInput
    GetThingShadowOutput: GetThingShadowOutput
    ListNamedShadowsForThingInput: ListNamedShadowsForThingInput
    ListNamedShadowsForThingOutput: ListNamedShadowsForThingOutput
    ListRetainedMessagesInput: ListRetainedMessagesInput
    ListRetainedMessagesOutput: ListRetainedMessagesOutput
    PublishInput: PublishInput
    PublishOutput: PublishOutput
    UpdateThingShadowInput: UpdateThingShadowInput
    UpdateThingShadowOutput: UpdateThingShadowOutput
end

return M
