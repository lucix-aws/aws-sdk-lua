-- Code generated by lua-client-codegen. DO NOT EDIT.

-- Shared member fields for Associate inputs and {Resource}Member response structures.
-- principalId is excluded because it has @httpLabel on inputs but not on responses.
local record AssociateMemberToFarmInput
    -- The ID of the farm to associate with the member.
    farmId: string
    -- The principal type of the member to associate with the farm.
    principalType: string
    -- The identity store ID of the member to associate with the farm.
    identityStoreId: string
    -- The principal's membership level for the associated farm.
    membershipLevel: string
    -- The member's principal ID to associate with the farm.
    principalId: string
end

local record AssociateMemberToFarmOutput
end

-- Shared member fields for Associate inputs and {Resource}Member response structures.
-- principalId is excluded because it has @httpLabel on inputs but not on responses.
local record AssociateMemberToFleetInput
    -- The farm ID of the fleet to associate with the member.
    farmId: string
    -- The ID of the fleet to associate with a member.
    fleetId: string
    -- The member's principal type to associate with the fleet.
    principalType: string
    -- The member's identity store ID to associate with the fleet.
    identityStoreId: string
    -- The principal's membership level for the associated fleet.
    membershipLevel: string
    -- The member's principal ID to associate with a fleet.
    principalId: string
end

local record AssociateMemberToFleetOutput
end

-- Shared member fields for Associate inputs and {Resource}Member response structures.
-- principalId is excluded because it has @httpLabel on inputs but not on responses.
local record AssociateMemberToJobInput
    -- The farm ID of the job to associate with the member.
    farmId: string
    -- The queue ID to associate to the member.
    queueId: string
    -- The job ID to associate with the member.
    jobId: string
    -- The member's principal type to associate with the job.
    principalType: string
    -- The member's identity store ID to associate with the job.
    identityStoreId: string
    -- The principal's membership level for the associated job.
    membershipLevel: string
    -- The member's principal ID to associate with the job.
    principalId: string
end

local record AssociateMemberToJobOutput
end

-- Shared member fields for Associate inputs and {Resource}Member response structures.
-- principalId is excluded because it has @httpLabel on inputs but not on responses.
local record AssociateMemberToQueueInput
    -- The farm ID of the queue to associate with the member.
    farmId: string
    -- The ID of the queue to associate to the member.
    queueId: string
    -- The member's principal type to associate with the queue.
    principalType: string
    -- The member's identity store ID to associate with the queue.
    identityStoreId: string
    -- The principal's membership level for the associated queue.
    membershipLevel: string
    -- The member's principal ID to associate with the queue.
    principalId: string
end

local record AssociateMemberToQueueOutput
end

local record AssumeFleetRoleForReadInput
    -- The farm ID for the fleet's farm.
    farmId: string
    -- The fleet ID.
    fleetId: string
end

-- Shared response body for AssumeRole operations where credentials are required.
-- AssumeQueueRoleForWorkerResponse is excluded because credentials is optional there
-- because Queue.roleArn is optional, so the mixin's @required trait would be incorrect.
local record AssumeFleetRoleForReadOutput
    -- The credentials for the fleet role.
    credentials: AwsCredentials
end

local record AssumeFleetRoleForWorkerInput
    -- The farm ID for the fleet's farm.
    farmId: string
    -- The fleet ID that contains the worker.
    fleetId: string
    -- The ID of the worker assuming the fleet role.
    workerId: string
end

-- Shared response body for AssumeRole operations where credentials are required.
-- AssumeQueueRoleForWorkerResponse is excluded because credentials is optional there
-- because Queue.roleArn is optional, so the mixin's @required trait would be incorrect.
local record AssumeFleetRoleForWorkerOutput
    -- The credentials for the worker.
    credentials: AwsCredentials
end

local record AssumeQueueRoleForReadInput
    -- The farm ID of the farm containing the queue.
    farmId: string
    -- The queue ID.
    queueId: string
end

-- Shared response body for AssumeRole operations where credentials are required.
-- AssumeQueueRoleForWorkerResponse is excluded because credentials is optional there
-- because Queue.roleArn is optional, so the mixin's @required trait would be incorrect.
local record AssumeQueueRoleForReadOutput
    -- The credentials for the queue role.
    credentials: AwsCredentials
end

local record AssumeQueueRoleForUserInput
    -- The farm ID of the queue that the user assumes the role for.
    farmId: string
    -- The queue ID of the queue that the user assumes the role for.
    queueId: string
end

-- Shared response body for AssumeRole operations where credentials are required.
-- AssumeQueueRoleForWorkerResponse is excluded because credentials is optional there
-- because Queue.roleArn is optional, so the mixin's @required trait would be incorrect.
local record AssumeQueueRoleForUserOutput
    -- The credentials for the queue role that a user has access to.
    credentials: AwsCredentials
end

local record AssumeQueueRoleForWorkerInput
    -- The farm ID of the worker assuming the queue role.
    farmId: string
    -- The fleet ID of the worker assuming the queue role.
    fleetId: string
    -- The worker ID of the worker assuming the queue role.
    workerId: string
    -- The queue ID of the worker assuming the queue role.
    queueId: string
end

local record AssumeQueueRoleForWorkerOutput
    -- The Amazon Web Services credentials for the role that the worker is assuming.
    credentials: AwsCredentials
end

local record BatchGetJobInput
    -- The list of job identifiers to retrieve. You can specify up to 100 identifiers per request.
    identifiers: {BatchGetJobIdentifier}
end

local record BatchGetJobOutput
    -- A list of jobs that were successfully retrieved.
    jobs: {BatchGetJobItem}
    -- A list of errors for jobs that could not be retrieved.
    errors: {BatchGetJobError}
end

local record BatchGetJobEntityInput
    -- The farm ID of the worker that's fetching job details. The worker must have an assignment on a job to fetch job details.
    farmId: string
    -- The fleet ID of the worker that's fetching job details. The worker must have an assignment on a job to fetch job details.
    fleetId: string
    -- The worker ID of the worker containing the job details to get.
    workerId: string
    -- The job identifiers to include within the job entity batch details.
    identifiers: {JobEntityIdentifiersUnion}
end

local record BatchGetJobEntityOutput
    -- A list of the job entities, or details, in the batch.
    entities: {JobEntity}
    -- A list of errors from the job error logs for the batch.
    errors: {GetJobEntityError}
end

local record BatchGetSessionInput
    -- The list of session identifiers to retrieve. You can specify up to 100 identifiers per request.
    identifiers: {BatchGetSessionIdentifier}
end

local record BatchGetSessionOutput
    -- A list of sessions that were successfully retrieved.
    sessions: {BatchGetSessionItem}
    -- A list of errors for sessions that could not be retrieved.
    errors: {BatchGetSessionError}
end

local record BatchGetSessionActionInput
    -- The list of session action identifiers to retrieve. You can specify up to 100 identifiers per request.
    identifiers: {BatchGetSessionActionIdentifier}
end

local record BatchGetSessionActionOutput
    -- A list of session actions that were successfully retrieved.
    sessionActions: {BatchGetSessionActionItem}
    -- A list of errors for session actions that could not be retrieved.
    errors: {BatchGetSessionActionError}
end

local record BatchGetStepInput
    -- The list of step identifiers to retrieve. You can specify up to 100 identifiers per request.
    identifiers: {BatchGetStepIdentifier}
end

local record BatchGetStepOutput
    -- A list of steps that were successfully retrieved.
    steps: {BatchGetStepItem}
    -- A list of errors for steps that could not be retrieved.
    errors: {BatchGetStepError}
end

local record BatchGetTaskInput
    -- The list of task identifiers to retrieve. You can specify up to 100 identifiers per request.
    identifiers: {BatchGetTaskIdentifier}
end

local record BatchGetTaskOutput
    -- A list of tasks that were successfully retrieved.
    tasks: {BatchGetTaskItem}
    -- A list of errors for tasks that could not be retrieved.
    errors: {BatchGetTaskError}
end

local record BatchGetWorkerInput
    -- The list of worker identifiers to retrieve. You can specify up to 100 identifiers per request.
    identifiers: {BatchGetWorkerIdentifier}
end

local record BatchGetWorkerOutput
    -- A list of workers that were successfully retrieved.
    workers: {BatchGetWorkerItem}
    -- A list of errors for workers that could not be retrieved.
    errors: {BatchGetWorkerError}
end

local record BatchUpdateJobInput
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The list of jobs to update. You can specify up to 100 jobs per request.
    jobs: {BatchUpdateJobItem}
end

local record BatchUpdateJobOutput
    -- A list of errors for jobs that could not be updated.
    errors: {BatchUpdateJobError}
end

local record BatchUpdateTaskInput
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The list of tasks to update. You can specify up to 100 tasks per request.
    tasks: {BatchUpdateTaskItem}
end

local record BatchUpdateTaskOutput
    -- A list of errors for tasks that could not be updated.
    errors: {BatchUpdateTaskError}
end

local record CopyJobTemplateInput
    -- The farm ID to copy.
    farmId: string
    -- The queue ID to copy.
    queueId: string
    -- The job ID to copy.
    jobId: string
    -- The Amazon S3 bucket name and key where you would like to add a copy of the job template.
    targetS3Location: S3Location
end

local record CopyJobTemplateOutput
    -- The format of the job template, either JSON or YAML.
    templateType: string
end

-- Shared displayName + description for Create operations where both are present.
-- displayName is @required here - this mixin is Create-only by design (Update has optional displayName).
local record CreateBudgetInput
    -- The farm ID to include in this budget.
    farmId: string
    -- The display name of the budget. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The description of the budget. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The queue ID provided to this budget to track usage.
    usageTrackingResource: UsageTrackingResource
    -- The dollar limit based on consumed usage.
    approximateDollarLimit: number
    -- The budget actions to specify what happens when the budget runs out.
    actions: {BudgetActionToAdd}
    -- The schedule to associate with this budget.
    schedule: BudgetSchedule
    -- Each tag consists of a tag key and a tag value. Tag keys and values are both required, but tag values can be empty strings.
    tags: {string : string}
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record CreateBudgetOutput
    -- The budget ID.
    budgetId: string
end

-- Shared displayName + description for Create operations where both are present.
-- displayName is @required here - this mixin is Create-only by design (Update has optional displayName).
local record CreateFarmInput
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The display name of the farm. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The description of the farm. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The ARN of the KMS key to use on the farm.
    kmsKeyArn: string
    -- A multiplier applied to the farm's calculated costs for usage data and budget tracking. A value less than 1 represents a discount, a value greater than 1 represents a premium, and a value of 1 represents no adjustment. The default value is 1.
    costScaleFactor: number
    -- The tags to add to your farm. Each tag consists of a tag key and a tag value. Tag keys and values are both required, but tag values can be empty strings.
    tags: {string : string}
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record CreateFarmOutput
    -- The farm ID.
    farmId: string
end

-- Shared displayName + description for Create operations where both are present.
-- displayName is @required here - this mixin is Create-only by design (Update has optional displayName).
local record CreateFleetInput
    -- The farm ID of the farm to connect to the fleet.
    farmId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The display name of the fleet. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The description of the fleet. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The IAM role ARN for the role that the fleet's workers will use.
    roleArn: string
    -- The minimum number of workers for the fleet.
    minWorkerCount: number
    -- The maximum number of workers for the fleet. Deadline Cloud limits the number of workers to less than or equal to the fleet's maximum worker count. The service maintains eventual consistency for the worker count. If you make multiple rapid calls to CreateWorker before the field updates, you might exceed your fleet's maximum worker count. For example, if your maxWorkerCount is 10 and you currently have 9 workers, making two quick CreateWorker calls might successfully create 2 workers instead of 1, resulting in 11 total workers.
    maxWorkerCount: number
    -- The configuration settings for the fleet. Customer managed fleets are self-managed. Service managed Amazon EC2 fleets are managed by Deadline Cloud.
    configuration: FleetConfiguration
    -- Each tag consists of a tag key and a tag value. Tag keys and values are both required, but tag values can be empty strings.
    tags: {string : string}
    -- Provides a script that runs as a worker is starting up that you can use to provide additional configuration for workers in your fleet.
    hostConfiguration: HostConfiguration
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record CreateFleetOutput
    -- The fleet ID.
    fleetId: string
end

local record CreateJobInput
    -- The farm ID of the farm to connect to the job.
    farmId: string
    -- The ID of the queue that the job is submitted to.
    queueId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The job template to use for this job.
    template: string
    -- The file type for the job template.
    templateType: string
    -- The priority of the job. The highest priority (first scheduled) is 100. When two jobs have the same priority, the oldest job is scheduled first.
    priority: number
    -- The parameters for the job.
    parameters: {string : JobParameter}
    -- The attachments for the job. Attach files required for the job to run to a render job.
    attachments: Attachments
    -- The storage profile ID for the storage profile to connect to the job.
    storageProfileId: string
    -- The initial job status when it is created. Jobs that are created with a SUSPENDED status will not run until manually requeued.
    targetTaskRunStatus: string
    -- The number of task failures before the job stops running and is marked as FAILED.
    maxFailedTasksCount: number
    -- The maximum number of retries for each task.
    maxRetriesPerTask: number
    -- The maximum number of worker hosts that can concurrently process a job. When the maxWorkerCount is reached, no more workers will be assigned to process the job, even if the fleets assigned to the job's queue has available workers. You can't set the maxWorkerCount to 0. If you set it to -1, there is no maximum number of workers. If you don't specify the maxWorkerCount, Deadline Cloud won't throttle the number of workers used to process the job.
    maxWorkerCount: number
    -- The job ID for the source job.
    sourceJobId: string
    -- A custom name to override the job name derived from the job template.
    nameOverride: string
    -- A custom description to override the job description derived from the job template.
    descriptionOverride: string
    -- The tags to add to your job. Each tag consists of a tag key and a tag value. Tag keys and values are both required, but tag values can be empty strings.
    tags: {string : string}
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record CreateJobOutput
    -- The job ID.
    jobId: string
end

local record CreateLicenseEndpointInput
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The VPC (virtual private cloud) ID to use with the license endpoint.
    vpcId: string
    -- The subnet IDs.
    subnetIds: {string}
    -- The security group IDs.
    securityGroupIds: {string}
    -- Each tag consists of a tag key and a tag value. Tag keys and values are both required, but tag values can be empty strings.
    tags: {string : string}
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record CreateLicenseEndpointOutput
    -- The license endpoint ID.
    licenseEndpointId: string
end

local record CreateLimitInput
    -- The farm ID of the farm that contains the limit.
    farmId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The display name of the limit. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The value that you specify as the name in the amounts field of the hostRequirements in a step of a job template to declare the limit requirement.
    amountRequirementName: string
    -- The maximum number of resources constrained by this limit. When all of the resources are in use, steps that require the limit won't be scheduled until the resource is available. The maxCount must not be 0. If the value is -1, there is no restriction on the number of resources that can be acquired for this limit.
    maxCount: number
    -- A description of the limit. A description helps you identify the purpose of the limit. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
end

local record CreateLimitOutput
    -- A unique identifier for the limit. Use this identifier in other operations, such as CreateQueueLimitAssociation and DeleteLimit.
    limitId: string
end

local record CreateMonitorInput
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The name that you give the monitor that is displayed in the Deadline Cloud console. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The Amazon Resource Name of the IAM Identity Center instance that authenticates monitor users.
    identityCenterInstanceArn: string
    -- The AWS Region where IAM Identity Center is enabled. Required when IAM Identity Center is in a different Region than the monitor.
    identityCenterRegion: string
    -- The subdomain to use when creating the monitor URL. The full URL of the monitor is subdomain.Region.deadlinecloud.amazonaws.com.
    subdomain: string
    -- The Amazon Resource Name of the IAM role that the monitor uses to connect to Deadline Cloud. Every user that signs in to the monitor using IAM Identity Center uses this role to access Deadline Cloud resources.
    roleArn: string
    -- The tags to add to your monitor. Each tag consists of a tag key and a tag value. Tag keys and values are both required, but tag values can be empty strings.
    tags: {string : string}
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record CreateMonitorOutput
    -- The unique identifier of the monitor.
    monitorId: string
    -- The Amazon Resource Name that IAM Identity Center assigns to the monitor.
    identityCenterApplicationArn: string
end

-- Shared displayName + description for Create operations where both are present.
-- displayName is @required here - this mixin is Create-only by design (Update has optional displayName).
local record CreateQueueInput
    -- The farm ID of the farm to connect to the queue.
    farmId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The display name of the queue. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The description of the queue. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The default action to take on a queue if a budget isn't configured.
    defaultBudgetAction: string
    -- The job attachment settings for the queue. These are the Amazon S3 bucket name and the Amazon S3 prefix.
    jobAttachmentSettings: JobAttachmentSettings
    -- The IAM role ARN that workers will use while running jobs for this queue.
    roleArn: string
    -- The jobs in the queue run as the specified POSIX user.
    jobRunAsUser: JobRunAsUser
    -- The file system location name to include in the queue.
    requiredFileSystemLocationNames: {string}
    -- The storage profile IDs to include in the queue.
    allowedStorageProfileIds: {string}
    -- Each tag consists of a tag key and a tag value. Tag keys and values are both required, but tag values can be empty strings.
    tags: {string : string}
    -- The scheduling configuration for the queue. This configuration determines how workers are assigned to jobs in the queue. If not specified, the queue defaults to the priorityFifo scheduling configuration.
    schedulingConfiguration: SchedulingConfiguration
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record CreateQueueOutput
    -- The queue ID.
    queueId: string
end

local record CreateQueueEnvironmentInput
    -- The farm ID of the farm to connect to the environment.
    farmId: string
    -- The queue ID to connect the queue and environment.
    queueId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- Sets the priority of the environments in the queue from 0 to 10,000, where 0 is the highest priority (activated first and deactivated last). If two environments share the same priority value, the environment created first takes higher priority.
    priority: number
    -- The template's file type, JSON or YAML.
    templateType: string
    -- The environment template to use in the queue.
    template: string
end

local record CreateQueueEnvironmentOutput
    -- The queue environment ID.
    queueEnvironmentId: string
end

local record CreateQueueFleetAssociationInput
    -- The ID of the farm that the queue and fleet belong to.
    farmId: string
    -- The queue ID.
    queueId: string
    -- The fleet ID.
    fleetId: string
end

local record CreateQueueFleetAssociationOutput
end

local record CreateQueueLimitAssociationInput
    -- The unique identifier of the farm that contains the queue and limit to associate.
    farmId: string
    -- The unique identifier of the queue to associate with the limit.
    queueId: string
    -- The unique identifier of the limit to associate with the queue.
    limitId: string
end

local record CreateQueueLimitAssociationOutput
end

local record CreateStorageProfileInput
    -- The farm ID of the farm to connect to the storage profile.
    farmId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The display name of the storage profile. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The type of operating system (OS) for the storage profile.
    osFamily: string
    -- File system paths to include in the storage profile.
    fileSystemLocations: {FileSystemLocation}
end

local record CreateStorageProfileOutput
    -- The storage profile ID.
    storageProfileId: string
end

local record CreateWorkerInput
    -- The farm ID of the farm to connect to the worker.
    farmId: string
    -- The fleet ID to connect to the worker.
    fleetId: string
    -- The IP address and host name of the worker.
    hostProperties: HostPropertiesRequest
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- Each tag consists of a tag key and a tag value. Tag keys and values are both required, but tag values can be empty strings.
    tags: {string : string}
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record CreateWorkerOutput
    -- The worker ID.
    workerId: string
end

local record DeleteBudgetInput
    -- The farm ID of the farm to remove from the budget.
    farmId: string
    -- The budget ID of the budget to delete.
    budgetId: string
end

local record DeleteBudgetOutput
end

local record DeleteFarmInput
    -- The farm ID of the farm to delete.
    farmId: string
end

local record DeleteFarmOutput
end

local record DeleteFleetInput
    -- The farm ID of the farm to remove from the fleet.
    farmId: string
    -- The fleet ID of the fleet to delete.
    fleetId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
end

local record DeleteFleetOutput
end

local record DeleteLicenseEndpointInput
    -- The license endpoint ID of the license endpoint to delete.
    licenseEndpointId: string
end

local record DeleteLicenseEndpointOutput
end

local record DeleteLimitInput
    -- The unique identifier of the farm that contains the limit to delete.
    farmId: string
    -- The unique identifier of the limit to delete.
    limitId: string
end

local record DeleteLimitOutput
end

local record DeleteMeteredProductInput
    -- The ID of the license endpoint from which to remove the metered product.
    licenseEndpointId: string
    -- The product ID to remove from the license endpoint.
    productId: string
end

local record DeleteMeteredProductOutput
end

local record DeleteMonitorInput
    -- The unique identifier of the monitor to delete. This ID is returned by the CreateMonitor operation, and is included in the response to the GetMonitor operation.
    monitorId: string
end

local record DeleteMonitorOutput
end

local record DeleteQueueInput
    -- The ID of the farm from which to remove the queue.
    farmId: string
    -- The queue ID of the queue to delete.
    queueId: string
end

local record DeleteQueueOutput
end

local record DeleteQueueEnvironmentInput
    -- The farm ID of the farm from which to remove the queue environment.
    farmId: string
    -- The queue ID of the queue environment to delete.
    queueId: string
    -- The queue environment ID of the queue environment to delete.
    queueEnvironmentId: string
end

local record DeleteQueueEnvironmentOutput
end

-- Identifier mixin for queue-fleet association operations.
-- Composes QueueIdentifierMixin (farmId + queueId) and adds fleetId.
local record DeleteQueueFleetAssociationInput
    -- The farm ID of the farm that holds the queue-fleet association.
    farmId: string
    -- The queue ID of the queue-fleet association.
    queueId: string
    -- The fleet ID of the queue-fleet association.
    fleetId: string
end

local record DeleteQueueFleetAssociationOutput
end

local record DeleteQueueLimitAssociationInput
    -- The unique identifier of the farm that contains the queue and limit to disassociate.
    farmId: string
    -- The unique identifier of the queue to disassociate.
    queueId: string
    -- The unique identifier of the limit to disassociate.
    limitId: string
end

local record DeleteQueueLimitAssociationOutput
end

local record DeleteStorageProfileInput
    -- The farm ID of the farm from which to remove the storage profile.
    farmId: string
    -- The storage profile ID of the storage profile to delete.
    storageProfileId: string
end

local record DeleteStorageProfileOutput
end

local record DeleteWorkerInput
    -- The farm ID of the worker to delete.
    farmId: string
    -- The fleet ID of the worker to delete.
    fleetId: string
    -- The worker ID of the worker to delete.
    workerId: string
end

local record DeleteWorkerOutput
end

local record DisassociateMemberFromFarmInput
    -- The farm ID of the farm to disassociate from the member.
    farmId: string
    -- A member's principal ID to disassociate from a farm.
    principalId: string
end

local record DisassociateMemberFromFarmOutput
end

local record DisassociateMemberFromFleetInput
    -- The farm ID of the fleet to disassociate a member from.
    farmId: string
    -- The fleet ID of the fleet to from which to disassociate a member.
    fleetId: string
    -- A member's principal ID to disassociate from a fleet.
    principalId: string
end

local record DisassociateMemberFromFleetOutput
end

local record DisassociateMemberFromJobInput
    -- The farm ID for the job to disassociate from the member.
    farmId: string
    -- The queue ID connected to a job for which you're disassociating a member.
    queueId: string
    -- The job ID to disassociate from a member in a job.
    jobId: string
    -- A member's principal ID to disassociate from a job.
    principalId: string
end

local record DisassociateMemberFromJobOutput
end

local record DisassociateMemberFromQueueInput
    -- The farm ID for the queue to disassociate from a member.
    farmId: string
    -- The queue ID of the queue in which you're disassociating from a member.
    queueId: string
    -- A member's principal ID to disassociate from a queue.
    principalId: string
end

local record DisassociateMemberFromQueueOutput
end

local record GetBudgetInput
    -- The farm ID of the farm connected to the budget.
    farmId: string
    -- The budget ID.
    budgetId: string
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record GetBudgetOutput
    -- The budget ID.
    budgetId: string
    -- The resource that the budget is tracking usage for.
    usageTrackingResource: UsageTrackingResource
    -- The status of the budget. ACTIVE–Get a budget being evaluated. INACTIVE–Get an inactive budget. This can include expired, canceled, or deleted statuses.
    status: string
    -- The display name of the budget. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The consumed usage limit for the budget.
    approximateDollarLimit: number
    -- The usages of the budget.
    usages: ConsumedUsages
    -- The user or system that created this resource.
    createdBy: string
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that updated this resource.
    updatedBy: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The description of the budget. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The budget actions for the budget.
    actions: {ResponseBudgetAction}
    -- The budget schedule.
    schedule: BudgetSchedule
    -- The date and time the queue stopped.
    queueStoppedAt: number
end

local record GetFarmInput
    -- The farm ID of the farm.
    farmId: string
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record GetFarmOutput
    -- The farm ID of the farm to get.
    farmId: string
    -- The display name of the farm. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The ARN of the KMS key used on the farm.
    kmsKeyArn: string
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that created this resource.
    createdBy: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
    -- The description of the farm. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- A multiplier applied to the farm's calculated costs for usage data and budget tracking. A value less than 1 represents a discount, a value greater than 1 represents a premium, and a value of 1 represents no adjustment.
    costScaleFactor: number
end

local record GetFleetInput
    -- The farm ID of the farm in the fleet.
    farmId: string
    -- The fleet ID of the fleet to get.
    fleetId: string
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record GetFleetOutput
    -- The fleet ID.
    fleetId: string
    -- The farm ID of the farm in the fleet.
    farmId: string
    -- The display name of the fleet. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The status of the fleet.
    status: string
    -- A message that communicates a suspended status of the fleet.
    statusMessage: string
    -- The Auto Scaling status of the fleet. Either GROWING, STEADY, or SHRINKING.
    autoScalingStatus: string
    -- The number of target workers in the fleet.
    targetWorkerCount: number
    -- The number of workers in the fleet.
    workerCount: number
    -- The minimum number of workers specified in the fleet.
    minWorkerCount: number
    -- The maximum number of workers specified in the fleet.
    maxWorkerCount: number
    -- The configuration setting for the fleet.
    configuration: FleetConfiguration
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that created this resource.
    createdBy: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
    -- The description of the fleet. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The script that runs as a worker is starting up that you can use to provide additional configuration for workers in your fleet.
    hostConfiguration: HostConfiguration
    -- Outlines what the fleet is capable of for minimums, maximums, and naming, in addition to attribute names and values.
    capabilities: FleetCapabilities
    -- The IAM role ARN.
    roleArn: string
end

local record GetJobInput
    -- The farm ID of the farm in the job.
    farmId: string
    -- The queue ID associated with the job.
    queueId: string
    -- The job ID.
    jobId: string
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record GetJobOutput
    -- The job ID.
    jobId: string
    -- The name of the job.
    name: string
    -- The life cycle status for the job.
    lifecycleStatus: string
    -- A message that communicates the status of the life cycle for the job.
    lifecycleStatusMessage: string
    -- The job priority.
    priority: number
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that created this resource.
    createdBy: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
    -- The date and time the resource started running.
    startedAt: number
    -- The date and time the resource ended running.
    endedAt: number
    -- The task run status for the job.
    taskRunStatus: string
    -- The task status with which the job started.
    targetTaskRunStatus: string
    -- The number of tasks running on the job.
    taskRunStatusCounts: {string : number}
    -- The total number of times tasks from the job failed and were retried.
    taskFailureRetryCount: number
    -- The storage profile ID associated with the job.
    storageProfileId: string
    -- The number of task failures before the job stops running and is marked as FAILED.
    maxFailedTasksCount: number
    -- The maximum number of retries per failed tasks.
    maxRetriesPerTask: number
    -- The parameters for the job.
    parameters: {string : JobParameter}
    -- The attachments for the job.
    attachments: Attachments
    -- The description of the job. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The maximum number of worker hosts that can concurrently process a job. When the maxWorkerCount is reached, no more workers will be assigned to process the job, even if the fleets assigned to the job's queue has available workers. If you don't set the maxWorkerCount when you create a job, this value is not returned in the response.
    maxWorkerCount: number
    -- The job ID for the source job.
    sourceJobId: string
end

local record GetLicenseEndpointInput
    -- The license endpoint ID.
    licenseEndpointId: string
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record GetLicenseEndpointOutput
    -- The license endpoint ID.
    licenseEndpointId: string
    -- The status of the license endpoint.
    status: string
    -- The status message of the license endpoint.
    statusMessage: string
    -- The VPC (virtual private cloud) ID associated with the license endpoint.
    vpcId: string
    -- The DNS name.
    dnsName: string
    -- The subnet IDs.
    subnetIds: {string}
    -- The security group IDs for the license endpoint.
    securityGroupIds: {string}
end

local record GetLimitInput
    -- The unique identifier of the farm that contains the limit.
    farmId: string
    -- The unique identifier of the limit to return.
    limitId: string
end

-- Domain fields for Limit summary/response shapes, ordered before timestamps.
local record GetLimitOutput
    -- The unique identifier of the farm that contains the limit.
    farmId: string
    -- The unique identifier of the limit.
    limitId: string
    -- The number of resources from the limit that are being used by jobs. The result is delayed and may not be the count at the time that you called the operation.
    currentCount: number
    -- The Unix timestamp of the date and time that the limit was created.
    createdAt: number
    -- The user identifier of the person that created the limit.
    createdBy: string
    -- The Unix timestamp of the date and time that the limit was last updated.
    updatedAt: number
    -- The user identifier of the person that last updated the limit.
    updatedBy: string
    -- The display name of the limit. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The value that you specify as the name in the amounts field of the hostRequirements in a step of a job template to declare the limit requirement.
    amountRequirementName: string
    -- The maximum number of resources constrained by this limit. When all of the resources are in use, steps that require the limit won't be scheduled until the resource is available. The maxValue must not be 0. If the value is -1, there is no restriction on the number of resources that can be acquired for this limit.
    maxCount: number
    -- The description of the limit that helps identify what the limit is used for. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
end

local record GetMonitorInput
    -- The unique identifier for the monitor. This ID is returned by the CreateMonitor operation.
    monitorId: string
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record GetMonitorOutput
    -- The unique identifier for the monitor.
    monitorId: string
    -- The name used to identify the monitor on the Deadline Cloud console. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The subdomain used for the monitor URL. The full URL of the monitor is subdomain.Region.deadlinecloud.amazonaws.com.
    subdomain: string
    -- The complete URL of the monitor. The full URL of the monitor is subdomain.Region.deadlinecloud.amazonaws.com.
    url: string
    -- The Amazon Resource Name of the IAM role for the monitor. Users of the monitor use this role to access Deadline Cloud resources.
    roleArn: string
    -- The Amazon Resource Name of the IAM Identity Center instance responsible for authenticating monitor users.
    identityCenterInstanceArn: string
    -- The AWS Region where IAM Identity Center is enabled.
    identityCenterRegion: string
    -- The Amazon Resource Name that the IAM Identity Center assigned to the monitor when it was created.
    identityCenterApplicationArn: string
    -- The UNIX timestamp of the date and time that the monitor was created.
    createdAt: number
    -- The user name of the person that created the monitor.
    createdBy: string
    -- The UNIX timestamp of the last date and time that the monitor was updated.
    updatedAt: number
    -- The user name of the person that last updated the monitor.
    updatedBy: string
end

local record GetMonitorSettingsInput
    -- The unique identifier of the monitor. This ID is returned by the CreateMonitor operation, and is included in the response to the ListMonitors operation.
    monitorId: string
end

local record GetMonitorSettingsOutput
    -- The monitor settings as key-value pairs.
    settings: {string : string}
end

local record GetQueueInput
    -- The farm ID of the farm in the queue.
    farmId: string
    -- The queue ID for the queue to retrieve.
    queueId: string
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record GetQueueOutput
    -- The farm ID for the queue.
    farmId: string
    -- The queue ID.
    queueId: string
    -- The display name of the queue. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The status of the queue. ACTIVE–The queue is active. SCHEDULING–The queue is scheduling. SCHEDULING_BLOCKED–The queue scheduling is blocked. See the provided reason.
    status: string
    -- The default action taken on a queue if a budget wasn't configured.
    defaultBudgetAction: string
    -- The reason the queue was blocked.
    blockedReason: string
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that created this resource.
    createdBy: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
    -- The description of the queue. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The job attachment settings for the queue.
    jobAttachmentSettings: JobAttachmentSettings
    -- The IAM role ARN.
    roleArn: string
    -- A list of the required file system location names in the queue.
    requiredFileSystemLocationNames: {string}
    -- The storage profile IDs for the queue.
    allowedStorageProfileIds: {string}
    -- The jobs in the queue ran as this specified POSIX user.
    jobRunAsUser: JobRunAsUser
    -- The scheduling configuration for the queue. This configuration determines how workers are assigned to jobs in the queue.
    schedulingConfiguration: SchedulingConfiguration
end

local record GetQueueEnvironmentInput
    -- The farm ID for the queue environment.
    farmId: string
    -- The queue ID for the queue environment.
    queueId: string
    -- The queue environment ID.
    queueEnvironmentId: string
end

local record GetQueueEnvironmentOutput
    -- The queue environment ID.
    queueEnvironmentId: string
    -- The name of the queue environment.
    name: string
    -- The priority of the queue environment.
    priority: number
    -- The type of template for the queue environment.
    templateType: string
    -- The template for the queue environment.
    template: string
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that created this resource.>
    createdBy: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
end

-- Identifier mixin for queue-fleet association operations.
-- Composes QueueIdentifierMixin (farmId + queueId) and adds fleetId.
local record GetQueueFleetAssociationInput
    -- The farm ID of the farm that contains the queue-fleet association.
    farmId: string
    -- The queue ID for the queue-fleet association.
    queueId: string
    -- The fleet ID for the queue-fleet association.
    fleetId: string
end

-- Domain fields for QueueFleetAssociation summary/response shapes, ordered before timestamps.
local record GetQueueFleetAssociationOutput
    -- The queue ID for the queue-fleet association.
    queueId: string
    -- The fleet ID for the queue-fleet association.
    fleetId: string
    -- The status of the queue-fleet association.
    status: string
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that created this resource.
    createdBy: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
end

local record GetQueueLimitAssociationInput
    -- The unique identifier of the farm that contains the associated queue and limit.
    farmId: string
    -- The unique identifier of the queue associated with the limit.
    queueId: string
    -- The unique identifier of the limit associated with the queue.
    limitId: string
end

-- Domain fields for QueueLimitAssociation summary/response shapes, ordered before timestamps.
local record GetQueueLimitAssociationOutput
    -- The unique identifier of the queue associated with the limit.
    queueId: string
    -- The unique identifier of the limit associated with the queue.
    limitId: string
    -- The current status of the limit.
    status: string
    -- The Unix timestamp of the date and time that the association was created.
    createdAt: number
    -- The user identifier of the person that created the association.
    createdBy: string
    -- The Unix timestamp of the date and time that the association was last updated.
    updatedAt: number
    -- The user identifier of the person that last updated the association.
    updatedBy: string
end

local record GetSessionInput
    -- The farm ID for the session.
    farmId: string
    -- The queue ID for the session.
    queueId: string
    -- The job ID for the session.
    jobId: string
    -- The session ID.
    sessionId: string
end

-- Session lifecycle/status fields, ordered after IDs in session shapes.
local record GetSessionOutput
    -- The session ID.
    sessionId: string
    -- The fleet ID for the session.
    fleetId: string
    -- The worker ID for the session.
    workerId: string
    -- The date and time the resource started running.
    startedAt: number
    -- The life cycle status of the session.
    lifecycleStatus: string
    -- The date and time the resource ended running.
    endedAt: number
    -- The life cycle status with which the session started.
    targetLifecycleStatus: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
    -- The session log.
    log: LogConfiguration
    -- Provides the Amazon EC2 properties of the host.
    hostProperties: HostPropertiesResponse
    -- The worker log for the session.
    workerLog: LogConfiguration
end

local record GetSessionActionInput
    -- The farm ID for the session action.
    farmId: string
    -- The queue ID for the session action.
    queueId: string
    -- The job ID for the session.
    jobId: string
    -- The session action ID for the session.
    sessionActionId: string
end

local record GetSessionActionOutput
    -- The session action ID.
    sessionActionId: string
    -- The status of the session action.
    status: string
    -- The date and time the resource started running.
    startedAt: number
    -- The date and time the resource ended running.
    endedAt: number
    -- The Linux timestamp of the date and time the session action was last updated.
    workerUpdatedAt: number
    -- The percentage completed for a session action.
    progressPercent: number
    -- The list of manifest properties that describe file attachments for the task run.
    manifests: {TaskRunManifestPropertiesResponse}
    -- The session ID for the session action.
    sessionId: string
    -- The process exit code. The default Deadline Cloud worker agent converts unsigned 32-bit exit codes to signed 32-bit exit codes.
    processExitCode: number
    -- The message that communicates the progress of the session action.
    progressMessage: string
    -- The limits and their amounts acquired during a session action. If no limits were acquired during the session, this field isn't returned.
    acquiredLimits: {AcquiredLimit}
    -- The session action definition.
    definition: SessionActionDefinition
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record GetSessionsStatisticsAggregationInput
    -- The identifier of the farm to include in the statistics. This should be the same as the farm ID used in the call to the StartSessionsStatisticsAggregation operation.
    farmId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
    -- The identifier returned by the StartSessionsStatisticsAggregation operation that identifies the aggregated statistics.
    aggregationId: string
end

-- Shared pagination field for List operation outputs (nextToken).
local record GetSessionsStatisticsAggregationOutput
    -- The statistics for the specified fleets or queues.
    statistics: {Statistics}
    -- The status of the aggregated results. An aggregation may fail or time out if the results are too large. If this happens, you can call the StartSessionsStatisticsAggregation operation after you reduce the aggregation time frame, reduce the number of queues or fleets in the aggregation, or increase the period length. If you call the StartSessionsStatisticsAggregation operation when the status is IN_PROGRESS, you will receive a ThrottlingException.
    status: string
    -- A message that describes the status.
    statusMessage: string
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

local record GetStepInput
    -- The farm ID for the step.
    farmId: string
    -- The queue ID for the step.
    queueId: string
    -- The job ID for the step.
    jobId: string
    -- The step ID.
    stepId: string
end

local record GetStepOutput
    -- The step ID.
    stepId: string
    -- The name of the step.
    name: string
    -- The life cycle status of the step.
    lifecycleStatus: string
    -- A message that describes the lifecycle status of the step.
    lifecycleStatusMessage: string
    -- The task run status for the job.
    taskRunStatus: string
    -- The number of tasks running on the job.
    taskRunStatusCounts: {string : number}
    -- The total number of times tasks from the step failed and were retried.
    taskFailureRetryCount: number
    -- The task status with which the job started.
    targetTaskRunStatus: string
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that created this resource.
    createdBy: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
    -- The date and time the resource started running.
    startedAt: number
    -- The date and time the resource ended running.
    endedAt: number
    -- The number of dependencies in the step.
    dependencyCounts: DependencyCounts
    -- The required capabilities of the step.
    requiredCapabilities: StepRequiredCapabilities
    -- A list of step parameters and the combination expression for the step.
    parameterSpace: ParameterSpace
    -- The description of the step. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
end

local record GetStorageProfileInput
    -- The farm ID for the storage profile.
    farmId: string
    -- The storage profile ID.
    storageProfileId: string
end

local record GetStorageProfileOutput
    -- The storage profile ID.
    storageProfileId: string
    -- The display name of the storage profile. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The operating system (OS) for the storage profile.
    osFamily: string
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that created this resource.
    createdBy: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
    -- The location of the files for the storage profile.
    fileSystemLocations: {FileSystemLocation}
end

local record GetStorageProfileForQueueInput
    -- The farm ID for the queue in storage profile.
    farmId: string
    -- The queue ID the queue in the storage profile.
    queueId: string
    -- The storage profile ID for the storage profile in the queue.
    storageProfileId: string
end

local record GetStorageProfileForQueueOutput
    -- The storage profile ID.
    storageProfileId: string
    -- The display name of the storage profile connected to a queue. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The operating system of the storage profile in the queue.
    osFamily: string
    -- The location of the files for the storage profile within the queue.
    fileSystemLocations: {FileSystemLocation}
end

local record GetTaskInput
    -- The farm ID of the farm connected to the task.
    farmId: string
    -- The queue ID for the queue connected to the task.
    queueId: string
    -- The job ID of the job connected to the task.
    jobId: string
    -- The step ID for the step connected to the task.
    stepId: string
    -- The task ID.
    taskId: string
end

local record GetTaskOutput
    -- The task ID.
    taskId: string
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that created this resource.
    createdBy: string
    -- The run status for the task.
    runStatus: string
    -- The run status with which to start the task.
    targetRunStatus: string
    -- The number of times that the task failed and was retried.
    failureRetryCount: number
    -- The date and time the resource started running.
    startedAt: number
    -- The date and time the resource ended running.
    endedAt: number
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
    -- The latest session action ID for the task.
    latestSessionActionId: string
    -- The parameters for the task.
    parameters: {string : TaskParameterValue}
end

local record GetWorkerInput
    -- The farm ID for the worker.
    farmId: string
    -- The fleet ID of the worker.
    fleetId: string
    -- The worker ID.
    workerId: string
end

-- Mixin that adds an optional ARN field to response structures.
-- Apply to SummaryMixins (flows into Get, Summary, and BatchGet) and Create outputs.
local record GetWorkerOutput
    -- The farm ID.
    farmId: string
    -- The fleet ID.
    fleetId: string
    -- The worker ID.
    workerId: string
    -- The host properties for the worker.
    hostProperties: HostPropertiesResponse
    -- The status of the worker.
    status: string
    -- The logs for the associated worker.
    log: LogConfiguration
    -- The date and time the resource was created.
    createdAt: number
    -- The user or system that created this resource.
    createdBy: string
    -- The date and time the resource was updated.
    updatedAt: number
    -- The user or system that updated this resource.
    updatedBy: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListAvailableMeteredProductsInput
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListAvailableMeteredProductsOutput
    -- The metered products.
    meteredProducts: {MeteredProductSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListBudgetsInput
    -- The farm ID associated with the budgets.
    farmId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
    -- The status to list for the budgets.
    status: string
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListBudgetsOutput
    -- The budgets to include on the list.
    budgets: {BudgetSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListFarmMembersInput
    -- The farm ID.
    farmId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListFarmMembersOutput
    -- The members on the list.
    members: {FarmMember}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListFarmsInput
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
    -- The principal ID of the member to list on the farm.
    principalId: string
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListFarmsOutput
    -- Farms on the list.
    farms: {FarmSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListFleetMembersInput
    -- The farm ID of the fleet.
    farmId: string
    -- The fleet ID to include on the list.
    fleetId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListFleetMembersOutput
    -- The members on the list.
    members: {FleetMember}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListFleetsInput
    -- The farm ID of the fleets.
    farmId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
    -- The principal ID of the members to include in the fleet.
    principalId: string
    -- The display names of a list of fleets. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The status of the fleet.
    status: string
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListFleetsOutput
    -- The fleets on the list.
    fleets: {FleetSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListJobMembersInput
    -- The farm ID of the job to list.
    farmId: string
    -- The queue ID to include on the list.
    queueId: string
    -- The job ID to include on the list.
    jobId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListJobMembersOutput
    -- The members on the list.
    members: {JobMember}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListJobParameterDefinitionsInput
    -- The farm ID of the job to list.
    farmId: string
    -- The queue ID to include on the list.
    queueId: string
    -- The job ID to include on the list.
    jobId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListJobParameterDefinitionsOutput
    -- Lists parameter definitions of a job.
    jobParameterDefinitions: {any}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListJobsInput
    -- The farm ID for the jobs.
    farmId: string
    -- The queue ID for the job.
    queueId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
    -- The principal ID of the members on the jobs.
    principalId: string
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListJobsOutput
    -- The jobs on the list.
    jobs: {JobSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListLicenseEndpointsInput
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListLicenseEndpointsOutput
    -- The license endpoints.
    licenseEndpoints: {LicenseEndpointSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListLimitsInput
    -- The unique identifier of the farm that contains the limits.
    farmId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of limits to return in each page of results.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListLimitsOutput
    -- A list of limits that the farm contains.
    limits: {LimitSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListMeteredProductsInput
    -- The license endpoint ID to include on the list of metered products.
    licenseEndpointId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListMeteredProductsOutput
    -- The metered products to list.
    meteredProducts: {MeteredProductSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListMonitorsInput
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListMonitorsOutput
    -- A list of MonitorSummary objects that describe your monitors in the Deadline Cloud.
    monitors: {MonitorSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListQueueEnvironmentsInput
    -- The farm ID for the queue environment list.
    farmId: string
    -- The queue ID for the queue environment list.
    queueId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListQueueEnvironmentsOutput
    -- The environments to include in the queue environments list.
    environments: {QueueEnvironmentSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListQueueFleetAssociationsInput
    -- The farm ID for the queue-fleet association list.
    farmId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
    -- The queue ID for the queue-fleet association list.
    queueId: string
    -- The fleet ID for the queue-fleet association list.
    fleetId: string
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListQueueFleetAssociationsOutput
    -- The queue-fleet associations on the list.
    queueFleetAssociations: {QueueFleetAssociationSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListQueueLimitAssociationsInput
    -- The unique identifier of the farm that contains the limits and associations.
    farmId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of associations to return in each page of results.
    maxResults: number
    -- Specifies that the operation should return only the queue limit associations for the specified queue. If you specify both the queueId and the limitId, only the specified limit is returned if it exists.
    queueId: string
    -- Specifies that the operation should return only the queue limit associations for the specified limit. If you specify both the queueId and the limitId, only the specified limit is returned if it exists.
    limitId: string
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListQueueLimitAssociationsOutput
    -- A list of associations between limits and queues in the farm specified in the request.
    queueLimitAssociations: {QueueLimitAssociationSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListQueueMembersInput
    -- The farm ID for the queue.
    farmId: string
    -- The queue ID to include on the list.
    queueId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListQueueMembersOutput
    -- The members on the list.
    members: {QueueMember}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListQueuesInput
    -- The farm ID of the queue.
    farmId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
    -- The principal IDs to include in the list of queues.
    principalId: string
    -- The status of the queues listed. ACTIVE–The queues are active. SCHEDULING–The queues are scheduling. SCHEDULING_BLOCKED–The queue scheduling is blocked for these queues.
    status: string
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListQueuesOutput
    -- The queues on the list.
    queues: {QueueSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListSessionActionsInput
    -- The farm ID for the session actions list.
    farmId: string
    -- The queue ID for the session actions list.
    queueId: string
    -- The job ID for the session actions list.
    jobId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
    -- The session ID to include on the sessions action list.
    sessionId: string
    -- The task ID for the session actions list.
    taskId: string
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListSessionActionsOutput
    -- The session actions.
    sessionActions: {SessionActionSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListSessionsInput
    -- The farm ID for the list of sessions.
    farmId: string
    -- The queue ID for the list of sessions
    queueId: string
    -- The job ID for the list of sessions.
    jobId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListSessionsOutput
    -- The sessions on the list.
    sessions: {SessionSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListSessionsForWorkerInput
    -- The farm ID for the session.
    farmId: string
    -- The fleet ID for the session.
    fleetId: string
    -- The worker ID for the session.
    workerId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListSessionsForWorkerOutput
    -- The sessions in the response.
    sessions: {WorkerSessionSummary}
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
end

local record ListStepConsumersInput
    -- The farm ID for the list of step consumers.
    farmId: string
    -- The queue ID for the step consumer.
    queueId: string
    -- The job ID for the step consumer.
    jobId: string
    -- The step ID to include on the list.
    stepId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListStepConsumersOutput
    -- The consumers on the list.
    consumers: {StepConsumer}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

local record ListStepDependenciesInput
    -- The farm ID for the step dependencies list.
    farmId: string
    -- The queue ID for the step dependencies list.
    queueId: string
    -- The job ID for the step dependencies list.
    jobId: string
    -- The step ID to include on the list.
    stepId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListStepDependenciesOutput
    -- The dependencies on the list.
    dependencies: {StepDependency}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListStepsInput
    -- The farm ID to include on the list of steps.
    farmId: string
    -- The queue ID to include on the list of steps.
    queueId: string
    -- The job ID to include on the list of steps.
    jobId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListStepsOutput
    -- The steps on the list.
    steps: {StepSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListStorageProfilesInput
    -- The farm ID of the storage profile.
    farmId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListStorageProfilesOutput
    -- The storage profiles.
    storageProfiles: {StorageProfileSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListStorageProfilesForQueueInput
    -- The farm ID of the queue's storage profile.
    farmId: string
    -- The queue ID for the storage profile.
    queueId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListStorageProfilesForQueueOutput
    -- The storage profiles in the queue.
    storageProfiles: {StorageProfileSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

local record ListTagsForResourceInput
    -- The resource ARN to list tags for.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- Each tag consists of a tag key and a tag value. Tag keys and values are both required, but tag values can be empty strings.
    tags: {string : string}
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListTasksInput
    -- The farm ID connected to the tasks.
    farmId: string
    -- The queue ID connected to the tasks.
    queueId: string
    -- The job ID for the tasks.
    jobId: string
    -- The step ID for the tasks.
    stepId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListTasksOutput
    -- Tasks for the job.
    tasks: {TaskSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

-- Shared pagination fields for List operation inputs (nextToken + maxResults).
local record ListWorkersInput
    -- The farm ID connected to the workers.
    farmId: string
    -- The fleet ID of the workers.
    fleetId: string
    -- The token for the next set of results, or null to start from the beginning.
    nextToken: string
    -- The maximum number of results to return. Use this parameter with NextToken to get results as a set of sequential pages.
    maxResults: number
end

-- Shared pagination field for List operation outputs (nextToken).
local record ListWorkersOutput
    -- The workers on the list.
    workers: {WorkerSummary}
    -- If Deadline Cloud returns nextToken, then there are more results available. The value of nextToken is a unique pagination token for each page. To retrieve the next page, call the operation again using the returned token. Keep all other arguments unchanged. If no results remain, then nextToken is set to null. Each pagination token expires after 24 hours. If you provide a token that isn't valid, then you receive an HTTP 400 ValidationException error.
    nextToken: string
end

local record PutMeteredProductInput
    -- The license endpoint ID to add to the metered product.
    licenseEndpointId: string
    -- The product ID to add to the metered product.
    productId: string
end

local record PutMeteredProductOutput
end

-- Shared input fields for all Search operations (filterExpressions, sortExpressions, itemOffset, pageSize).
local record SearchJobsInput
    -- The farm ID of the job.
    farmId: string
    -- The search terms for a resource.
    filterExpressions: SearchGroupedFilterExpressions
    -- The search terms for a resource.
    sortExpressions: {SearchSortExpression}
    -- The offset for the search results.
    itemOffset: number
    -- Specifies the number of results to return.
    pageSize: number
    -- The queue ID to use in the job search.
    queueIds: {string}
end

-- Shared output fields for all Search operations (nextItemOffset, totalResults).
local record SearchJobsOutput
    -- The jobs in the search.
    jobs: {JobSearchSummary}
    -- The next item offset for the search results.
    nextItemOffset: number
    -- The total number of results in the search.
    totalResults: number
end

-- Shared input fields for all Search operations (filterExpressions, sortExpressions, itemOffset, pageSize).
local record SearchStepsInput
    -- The farm ID to use for the step search.
    farmId: string
    -- The search terms for a resource.
    filterExpressions: SearchGroupedFilterExpressions
    -- The search terms for a resource.
    sortExpressions: {SearchSortExpression}
    -- The offset for the search results.
    itemOffset: number
    -- Specifies the number of results to return.
    pageSize: number
    -- The queue IDs in the step search.
    queueIds: {string}
    -- The job ID to use in the step search.
    jobId: string
end

-- Shared output fields for all Search operations (nextItemOffset, totalResults).
local record SearchStepsOutput
    -- The steps in the search.
    steps: {StepSearchSummary}
    -- The next item offset for the search results.
    nextItemOffset: number
    -- The total number of results in the search.
    totalResults: number
end

-- Shared input fields for all Search operations (filterExpressions, sortExpressions, itemOffset, pageSize).
local record SearchTasksInput
    -- The farm ID of the task.
    farmId: string
    -- The search terms for a resource.
    filterExpressions: SearchGroupedFilterExpressions
    -- The search terms for a resource.
    sortExpressions: {SearchSortExpression}
    -- The offset for the search results.
    itemOffset: number
    -- Specifies the number of results to return.
    pageSize: number
    -- The queue IDs to include in the search.
    queueIds: {string}
    -- The job ID for the task search.
    jobId: string
end

-- Shared output fields for all Search operations (nextItemOffset, totalResults).
local record SearchTasksOutput
    -- Tasks in the search.
    tasks: {TaskSearchSummary}
    -- The next item offset for the search results.
    nextItemOffset: number
    -- The total number of results in the search.
    totalResults: number
end

-- Shared input fields for all Search operations (filterExpressions, sortExpressions, itemOffset, pageSize).
local record SearchWorkersInput
    -- The farm ID in the workers search.
    farmId: string
    -- The search terms for a resource.
    filterExpressions: SearchGroupedFilterExpressions
    -- The search terms for a resource.
    sortExpressions: {SearchSortExpression}
    -- The offset for the search results.
    itemOffset: number
    -- Specifies the number of results to return.
    pageSize: number
    -- The fleet ID of the workers to search for.
    fleetIds: {string}
end

-- Shared output fields for all Search operations (nextItemOffset, totalResults).
local record SearchWorkersOutput
    -- The workers for the search.
    workers: {WorkerSearchSummary}
    -- The next item offset for the search results.
    nextItemOffset: number
    -- The total number of results in the search.
    totalResults: number
end

local record StartSessionsStatisticsAggregationInput
    -- The identifier of the farm that contains queues or fleets to return statistics for.
    farmId: string
    -- A list of fleet IDs or queue IDs to gather statistics for.
    resourceIds: SessionsStatisticsResources
    -- The Linux timestamp of the date and time that the statistics start.
    startTime: number
    -- The Linux timestamp of the date and time that the statistics end.
    endTime: number
    -- The timezone to use for the statistics. Use UTC notation such as "UTC+8."
    timezone: string
    -- The period to aggregate the statistics.
    period: string
    -- The field to use to group the statistics.
    groupBy: {string}
    -- One to four statistics to return.
    statistics: {string}
end

local record StartSessionsStatisticsAggregationOutput
    -- A unique identifier for the aggregated statistics. Use this identifier with the GetAggregatedStatisticsForSessions operation to return the statistics.
    aggregationId: string
end

local record TagResourceInput
    -- The ARN of the resource to apply tags to.
    resourceArn: string
    -- Each tag consists of a tag key and a tag value. Tag keys and values are both required, but tag values can be empty strings.
    tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The ARN of the resource to remove the tag from.
    resourceArn: string
    -- They keys of the tag.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateBudgetInput
    -- The farm ID of the budget to update.
    farmId: string
    -- The budget ID to update.
    budgetId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The display name of the budget to update. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The description of the budget to update. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- Updates the status of the budget. ACTIVE–The budget is being evaluated. INACTIVE–The budget is inactive. This can include Expired, Canceled, or deleted Deleted statuses.
    status: string
    -- The dollar limit to update on the budget. Based on consumed usage.
    approximateDollarLimit: number
    -- The budget actions to add. Budget actions specify what happens when the budget runs out.
    actionsToAdd: {BudgetActionToAdd}
    -- The budget actions to remove from the budget.
    actionsToRemove: {BudgetActionToRemove}
    -- The schedule to update.
    schedule: BudgetSchedule
end

local record UpdateBudgetOutput
end

local record UpdateFarmInput
    -- The farm ID to update.
    farmId: string
    -- The display name of the farm to update. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The description of the farm to update. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- A multiplier applied to the farm's calculated costs for usage data and budget tracking. A value less than 1 represents a discount, a value greater than 1 represents a premium, and a value of 1 represents no adjustment.
    costScaleFactor: number
end

local record UpdateFarmOutput
end

local record UpdateFleetInput
    -- The farm ID to update.
    farmId: string
    -- The fleet ID to update.
    fleetId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The display name of the fleet to update. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The description of the fleet to update. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The IAM role ARN that the fleet's workers assume while running jobs.
    roleArn: string
    -- The minimum number of workers in the fleet.
    minWorkerCount: number
    -- The maximum number of workers in the fleet. Deadline Cloud limits the number of workers to less than or equal to the fleet's maximum worker count. The service maintains eventual consistency for the worker count. If you make multiple rapid calls to CreateWorker before the field updates, you might exceed your fleet's maximum worker count. For example, if your maxWorkerCount is 10 and you currently have 9 workers, making two quick CreateWorker calls might successfully create 2 workers instead of 1, resulting in 11 total workers.
    maxWorkerCount: number
    -- The fleet configuration to update.
    configuration: FleetConfiguration
    -- Provides a script that runs as a worker is starting up that you can use to provide additional configuration for workers in your fleet.
    hostConfiguration: HostConfiguration
end

local record UpdateFleetOutput
end

local record UpdateJobInput
    -- The farm ID of the job to update.
    farmId: string
    -- The queue ID of the job to update.
    queueId: string
    -- The job ID to update.
    jobId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The task status to update the job's tasks to.
    targetTaskRunStatus: string
    -- The updated job priority.
    priority: number
    -- The number of task failures before the job stops running and is marked as FAILED.
    maxFailedTasksCount: number
    -- The maximum number of retries for a job.
    maxRetriesPerTask: number
    -- The status of a job in its lifecycle. When you change the status of the job to ARCHIVED, the job can't be scheduled or archived. An archived jobs and its steps and tasks are deleted after 120 days. The job can't be recovered.
    lifecycleStatus: string
    -- The maximum number of worker hosts that can concurrently process a job. When the maxWorkerCount is reached, no more workers will be assigned to process the job, even if the fleets assigned to the job's queue has available workers. You can't set the maxWorkerCount to 0. If you set it to -1, there is no maximum number of workers. If you don't specify the maxWorkerCount, the default is -1. The maximum number of workers that can process tasks in the job.
    maxWorkerCount: number
    -- The updated job name.
    name: string
    -- The updated job description.
    description: string
end

local record UpdateJobOutput
end

local record UpdateLimitInput
    -- The unique identifier of the farm that contains the limit.
    farmId: string
    -- The unique identifier of the limit to update.
    limitId: string
    -- The new display name of the limit. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The new description of the limit. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The maximum number of resources constrained by this limit. When all of the resources are in use, steps that require the limit won't be scheduled until the resource is available. If more than the new maximum number is currently in use, running jobs finish but no new jobs are started until the number of resources in use is below the new maximum number. The maxCount must not be 0. If the value is -1, there is no restriction on the number of resources that can be acquired for this limit.
    maxCount: number
end

local record UpdateLimitOutput
end

local record UpdateMonitorInput
    -- The unique identifier of the monitor to update.
    monitorId: string
    -- The new value of the subdomain to use when forming the monitor URL.
    subdomain: string
    -- The new value to use for the monitor's display name. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The Amazon Resource Name of the new IAM role to use with the monitor.
    roleArn: string
end

local record UpdateMonitorOutput
end

local record UpdateMonitorSettingsInput
    -- The unique identifier of the monitor to update settings for.
    monitorId: string
    -- The monitor settings to update as key-value pairs. Keys present in the request are upserted; keys absent are left unchanged. Send an empty string value to delete a key.
    settings: {string : string}
end

local record UpdateMonitorSettingsOutput
end

local record UpdateQueueInput
    -- The farm ID to update in the queue.
    farmId: string
    -- The queue ID to update.
    queueId: string
    -- The idempotency token to update in the queue.
    clientToken: string
    -- The display name of the queue to update. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The description of the queue to update. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    description: string
    -- The default action to take for a queue update if a budget isn't configured.
    defaultBudgetAction: string
    -- The job attachment settings to update for the queue.
    jobAttachmentSettings: JobAttachmentSettings
    -- The IAM role ARN that's used to run jobs from this queue.
    roleArn: string
    -- Update the jobs in the queue to run as a specified POSIX user.
    jobRunAsUser: JobRunAsUser
    -- The required file system location names to add to the queue.
    requiredFileSystemLocationNamesToAdd: {string}
    -- The required file system location names to remove from the queue.
    requiredFileSystemLocationNamesToRemove: {string}
    -- The storage profile IDs to add.
    allowedStorageProfileIdsToAdd: {string}
    -- The storage profile ID to remove.
    allowedStorageProfileIdsToRemove: {string}
    -- The scheduling configuration for the queue. This configuration determines how workers are assigned to jobs in the queue. When updating the scheduling configuration, the entire configuration is replaced. In-progress tasks run to completion before the new scheduling configuration takes effect.
    schedulingConfiguration: SchedulingConfiguration
end

local record UpdateQueueOutput
end

local record UpdateQueueEnvironmentInput
    -- The farm ID of the queue environment to update.
    farmId: string
    -- The queue ID of the queue environment to update.
    queueId: string
    -- The queue environment ID to update.
    queueEnvironmentId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The priority to update.
    priority: number
    -- The template type to update.
    templateType: string
    -- The template to update.
    template: string
end

local record UpdateQueueEnvironmentOutput
end

-- Identifier mixin for queue-fleet association operations.
-- Composes QueueIdentifierMixin (farmId + queueId) and adds fleetId.
local record UpdateQueueFleetAssociationInput
    -- The farm ID to update.
    farmId: string
    -- The queue ID to update.
    queueId: string
    -- The fleet ID to update.
    fleetId: string
    -- The status to update.
    status: string
end

local record UpdateQueueFleetAssociationOutput
end

local record UpdateQueueLimitAssociationInput
    -- The unique identifier of the farm that contains the associated queues and limits.
    farmId: string
    -- The unique identifier of the queue associated to the limit.
    queueId: string
    -- The unique identifier of the limit associated to the queue.
    limitId: string
    -- Sets the status of the limit. You can mark the limit active, or you can stop usage of the limit and either complete existing tasks or cancel any existing tasks immediately.
    status: string
end

local record UpdateQueueLimitAssociationOutput
end

local record UpdateSessionInput
    -- The farm ID to update in the session.
    farmId: string
    -- The queue ID to update in the session.
    queueId: string
    -- The job ID to update in the session.
    jobId: string
    -- The session ID to update.
    sessionId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The life cycle status to update in the session.
    targetLifecycleStatus: string
end

local record UpdateSessionOutput
end

local record UpdateStepInput
    -- The farm ID to update.
    farmId: string
    -- The queue ID to update.
    queueId: string
    -- The job ID to update.
    jobId: string
    -- The step ID to update.
    stepId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The task status to update the step's tasks to.
    targetTaskRunStatus: string
end

local record UpdateStepOutput
end

local record UpdateStorageProfileInput
    -- The farm ID to update.
    farmId: string
    -- The storage profile ID to update.
    storageProfileId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The display name of the storage profile to update. This field can store any content. Escape or encode this content before displaying it on a webpage or any other system that might interpret the content of this field.
    displayName: string
    -- The OS system to update.
    osFamily: string
    -- The file system location names to add.
    fileSystemLocationsToAdd: {FileSystemLocation}
    -- The file system location names to remove.
    fileSystemLocationsToRemove: {FileSystemLocation}
end

local record UpdateStorageProfileOutput
end

local record UpdateTaskInput
    -- The farm ID to update.
    farmId: string
    -- The queue ID to update.
    queueId: string
    -- The job ID to update.
    jobId: string
    -- The step ID to update.
    stepId: string
    -- The task ID to update.
    taskId: string
    -- The unique token which the server uses to recognize retries of the same request.
    clientToken: string
    -- The run status with which to start the task.
    targetRunStatus: string
end

local record UpdateTaskOutput
end

local record UpdateWorkerInput
    -- The farm ID to update.
    farmId: string
    -- The fleet ID to update.
    fleetId: string
    -- The worker ID to update.
    workerId: string
    -- The worker status to update.
    status: string
    -- The worker capabilities to update.
    capabilities: WorkerCapabilities
    -- The host properties to update.
    hostProperties: HostPropertiesRequest
end

local record UpdateWorkerOutput
    -- The worker log to update.
    log: LogConfiguration
    -- The script that runs as a worker is starting up that you can use to provide additional configuration for workers in your fleet.
    hostConfiguration: HostConfiguration
end

local record UpdateWorkerScheduleInput
    -- The farm ID to update.
    farmId: string
    -- The fleet ID to update.
    fleetId: string
    -- The worker ID to update.
    workerId: string
    -- The session actions associated with the worker schedule to update.
    updatedSessionActions: {string : UpdatedSessionActionInfo}
end

local record UpdateWorkerScheduleOutput
    -- The assigned sessions to update.
    assignedSessions: {string : AssignedSession}
    -- The session actions associated with the worker schedule to cancel.
    cancelSessionActions: {string : {string}}
    -- The status to update the worker to.
    desiredWorkerStatus: string
    -- Updates the time interval (in seconds) for the schedule.
    updateIntervalSeconds: number
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You exceeded your service quota. Service quotas, also referred to as limits, are the maximum number of service resources or operations for your Amazon Web Services account.
local record ServiceQuotaExceededException
    message: string
    -- A string that describes the reason the quota was exceeded.
    reason: string
    -- The type of the affected resource
    resourceType: string
    -- Identifies the service that exceeded the quota.
    serviceCode: string
    -- Identifies the quota that has been exceeded.
    quotaCode: string
    -- The identifier of the affected resource.
    resourceId: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- You don't have permission to perform the action.
local record AccessDeniedException
    message: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request has conflicting operations. This can occur if you're trying to perform more than one operation on the same resource at the same time.
local record ConflictException
    message: string
    -- A description of the error.
    reason: string
    -- The identifier of the resource in use.
    resourceId: string
    -- The type of the resource in use.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Deadline Cloud can't process your request right now. Try again later.
local record InternalServerErrorException
    message: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
end

-- The requested resource can't be found.
local record ResourceNotFoundException
    message: string
    -- The identifier of the resource that couldn't be found.
    resourceId: string
    -- The type of the resource that couldn't be found.
    resourceType: string
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- Your request exceeded a request rate quota.
local record ThrottlingException
    message: string
    -- Identifies the service that is being throttled.
    serviceCode: string
    -- Identifies the quota that is being throttled.
    quotaCode: string
    -- The number of seconds a client should wait before retrying the request.
    retryAfterSeconds: number
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

-- The request isn't valid. This can occur if your request contains malformed JSON or unsupported characters.
local record ValidationException
    message: string
    -- The reason that the request failed validation.
    reason: string
    -- A list of fields that failed validation.
    fieldList: {ValidationExceptionField}
    -- Information about the resources in use when the exception was thrown.
    context: {string : string}
end

local type AutoScalingStatus = "GROWING" | "STEADY" | "SHRINKING"

local type BudgetStatus = "ACTIVE" | "INACTIVE"

local type CreateJobTargetTaskRunStatus = "READY" | "SUSPENDED"

local type DeadlinePrincipalType = "USER" | "GROUP"

local type DefaultQueueBudgetAction = "NONE" | "STOP_SCHEDULING_AND_COMPLETE_TASKS" | "STOP_SCHEDULING_AND_CANCEL_TASKS"

local type DesiredWorkerStatus = "STOPPED"

local type EnvironmentTemplateType = "JSON" | "YAML"

local type FleetStatus = "ACTIVE" | "CREATE_IN_PROGRESS" | "UPDATE_IN_PROGRESS" | "CREATE_FAILED" | "UPDATE_FAILED" | "SUSPENDED"

local type JobLifecycleStatus = "CREATE_IN_PROGRESS" | "CREATE_FAILED" | "CREATE_COMPLETE" | "UPLOAD_IN_PROGRESS" | "UPLOAD_FAILED" | "UPDATE_IN_PROGRESS" | "UPDATE_FAILED" | "UPDATE_SUCCEEDED" | "ARCHIVED"

local type JobTargetTaskRunStatus = "READY" | "FAILED" | "SUCCEEDED" | "CANCELED" | "SUSPENDED" | "PENDING"

local type JobTemplateType = "JSON" | "YAML"

local type LicenseEndpointStatus = "CREATE_IN_PROGRESS" | "DELETE_IN_PROGRESS" | "READY" | "NOT_READY"

local type MembershipLevel = "VIEWER" | "CONTRIBUTOR" | "OWNER" | "MANAGER"

local type Period = "HOURLY" | "DAILY" | "WEEKLY" | "MONTHLY"

local type QueueBlockedReason = "NO_BUDGET_CONFIGURED" | "BUDGET_THRESHOLD_REACHED"

local type QueueFleetAssociationStatus = "ACTIVE" | "STOP_SCHEDULING_AND_COMPLETE_TASKS" | "STOP_SCHEDULING_AND_CANCEL_TASKS" | "STOPPED"

local type QueueLimitAssociationStatus = "ACTIVE" | "STOP_LIMIT_USAGE_AND_COMPLETE_TASKS" | "STOP_LIMIT_USAGE_AND_CANCEL_TASKS" | "STOPPED"

local type QueueStatus = "IDLE" | "SCHEDULING" | "SCHEDULING_BLOCKED"

local type SessionActionStatus = "ASSIGNED" | "RUNNING" | "CANCELING" | "SUCCEEDED" | "FAILED" | "INTERRUPTED" | "CANCELED" | "NEVER_ATTEMPTED" | "SCHEDULED" | "RECLAIMING" | "RECLAIMED"

local type SessionLifecycleStatus = "STARTED" | "UPDATE_IN_PROGRESS" | "UPDATE_SUCCEEDED" | "UPDATE_FAILED" | "ENDED"

local type SessionLifecycleTargetStatus = "ENDED"

local type SessionsStatisticsAggregationStatus = "IN_PROGRESS" | "TIMEOUT" | "FAILED" | "COMPLETED"

local type StepLifecycleStatus = "CREATE_COMPLETE" | "UPDATE_IN_PROGRESS" | "UPDATE_FAILED" | "UPDATE_SUCCEEDED"

local type StepTargetTaskRunStatus = "READY" | "FAILED" | "SUCCEEDED" | "CANCELED" | "SUSPENDED" | "PENDING"

local type StorageProfileOperatingSystemFamily = "WINDOWS" | "LINUX" | "MACOS"

local type TaskRunStatus = "PENDING" | "READY" | "ASSIGNED" | "STARTING" | "SCHEDULED" | "INTERRUPTING" | "RUNNING" | "SUSPENDED" | "CANCELED" | "FAILED" | "SUCCEEDED" | "NOT_COMPATIBLE"

local type TaskTargetRunStatus = "READY" | "FAILED" | "SUCCEEDED" | "CANCELED" | "SUSPENDED" | "PENDING"

local type UpdateJobLifecycleStatus = "ARCHIVED"

local type UpdateQueueFleetAssociationStatus = "ACTIVE" | "STOP_SCHEDULING_AND_COMPLETE_TASKS" | "STOP_SCHEDULING_AND_CANCEL_TASKS"

local type UpdateQueueLimitAssociationStatus = "ACTIVE" | "STOP_LIMIT_USAGE_AND_COMPLETE_TASKS" | "STOP_LIMIT_USAGE_AND_CANCEL_TASKS"

local type UpdatedWorkerStatus = "STARTED" | "STOPPING" | "STOPPED"

local type WorkerStatus = "CREATED" | "STARTED" | "STOPPING" | "STOPPED" | "NOT_RESPONDING" | "NOT_COMPATIBLE" | "RUNNING" | "IDLE"

local record M
    AssociateMemberToFarmInput: AssociateMemberToFarmInput
    AssociateMemberToFarmOutput: AssociateMemberToFarmOutput
    AssociateMemberToFleetInput: AssociateMemberToFleetInput
    AssociateMemberToFleetOutput: AssociateMemberToFleetOutput
    AssociateMemberToJobInput: AssociateMemberToJobInput
    AssociateMemberToJobOutput: AssociateMemberToJobOutput
    AssociateMemberToQueueInput: AssociateMemberToQueueInput
    AssociateMemberToQueueOutput: AssociateMemberToQueueOutput
    AssumeFleetRoleForReadInput: AssumeFleetRoleForReadInput
    AssumeFleetRoleForReadOutput: AssumeFleetRoleForReadOutput
    AssumeFleetRoleForWorkerInput: AssumeFleetRoleForWorkerInput
    AssumeFleetRoleForWorkerOutput: AssumeFleetRoleForWorkerOutput
    AssumeQueueRoleForReadInput: AssumeQueueRoleForReadInput
    AssumeQueueRoleForReadOutput: AssumeQueueRoleForReadOutput
    AssumeQueueRoleForUserInput: AssumeQueueRoleForUserInput
    AssumeQueueRoleForUserOutput: AssumeQueueRoleForUserOutput
    AssumeQueueRoleForWorkerInput: AssumeQueueRoleForWorkerInput
    AssumeQueueRoleForWorkerOutput: AssumeQueueRoleForWorkerOutput
    BatchGetJobInput: BatchGetJobInput
    BatchGetJobOutput: BatchGetJobOutput
    BatchGetJobEntityInput: BatchGetJobEntityInput
    BatchGetJobEntityOutput: BatchGetJobEntityOutput
    BatchGetSessionInput: BatchGetSessionInput
    BatchGetSessionOutput: BatchGetSessionOutput
    BatchGetSessionActionInput: BatchGetSessionActionInput
    BatchGetSessionActionOutput: BatchGetSessionActionOutput
    BatchGetStepInput: BatchGetStepInput
    BatchGetStepOutput: BatchGetStepOutput
    BatchGetTaskInput: BatchGetTaskInput
    BatchGetTaskOutput: BatchGetTaskOutput
    BatchGetWorkerInput: BatchGetWorkerInput
    BatchGetWorkerOutput: BatchGetWorkerOutput
    BatchUpdateJobInput: BatchUpdateJobInput
    BatchUpdateJobOutput: BatchUpdateJobOutput
    BatchUpdateTaskInput: BatchUpdateTaskInput
    BatchUpdateTaskOutput: BatchUpdateTaskOutput
    CopyJobTemplateInput: CopyJobTemplateInput
    CopyJobTemplateOutput: CopyJobTemplateOutput
    CreateBudgetInput: CreateBudgetInput
    CreateBudgetOutput: CreateBudgetOutput
    CreateFarmInput: CreateFarmInput
    CreateFarmOutput: CreateFarmOutput
    CreateFleetInput: CreateFleetInput
    CreateFleetOutput: CreateFleetOutput
    CreateJobInput: CreateJobInput
    CreateJobOutput: CreateJobOutput
    CreateLicenseEndpointInput: CreateLicenseEndpointInput
    CreateLicenseEndpointOutput: CreateLicenseEndpointOutput
    CreateLimitInput: CreateLimitInput
    CreateLimitOutput: CreateLimitOutput
    CreateMonitorInput: CreateMonitorInput
    CreateMonitorOutput: CreateMonitorOutput
    CreateQueueInput: CreateQueueInput
    CreateQueueOutput: CreateQueueOutput
    CreateQueueEnvironmentInput: CreateQueueEnvironmentInput
    CreateQueueEnvironmentOutput: CreateQueueEnvironmentOutput
    CreateQueueFleetAssociationInput: CreateQueueFleetAssociationInput
    CreateQueueFleetAssociationOutput: CreateQueueFleetAssociationOutput
    CreateQueueLimitAssociationInput: CreateQueueLimitAssociationInput
    CreateQueueLimitAssociationOutput: CreateQueueLimitAssociationOutput
    CreateStorageProfileInput: CreateStorageProfileInput
    CreateStorageProfileOutput: CreateStorageProfileOutput
    CreateWorkerInput: CreateWorkerInput
    CreateWorkerOutput: CreateWorkerOutput
    DeleteBudgetInput: DeleteBudgetInput
    DeleteBudgetOutput: DeleteBudgetOutput
    DeleteFarmInput: DeleteFarmInput
    DeleteFarmOutput: DeleteFarmOutput
    DeleteFleetInput: DeleteFleetInput
    DeleteFleetOutput: DeleteFleetOutput
    DeleteLicenseEndpointInput: DeleteLicenseEndpointInput
    DeleteLicenseEndpointOutput: DeleteLicenseEndpointOutput
    DeleteLimitInput: DeleteLimitInput
    DeleteLimitOutput: DeleteLimitOutput
    DeleteMeteredProductInput: DeleteMeteredProductInput
    DeleteMeteredProductOutput: DeleteMeteredProductOutput
    DeleteMonitorInput: DeleteMonitorInput
    DeleteMonitorOutput: DeleteMonitorOutput
    DeleteQueueInput: DeleteQueueInput
    DeleteQueueOutput: DeleteQueueOutput
    DeleteQueueEnvironmentInput: DeleteQueueEnvironmentInput
    DeleteQueueEnvironmentOutput: DeleteQueueEnvironmentOutput
    DeleteQueueFleetAssociationInput: DeleteQueueFleetAssociationInput
    DeleteQueueFleetAssociationOutput: DeleteQueueFleetAssociationOutput
    DeleteQueueLimitAssociationInput: DeleteQueueLimitAssociationInput
    DeleteQueueLimitAssociationOutput: DeleteQueueLimitAssociationOutput
    DeleteStorageProfileInput: DeleteStorageProfileInput
    DeleteStorageProfileOutput: DeleteStorageProfileOutput
    DeleteWorkerInput: DeleteWorkerInput
    DeleteWorkerOutput: DeleteWorkerOutput
    DisassociateMemberFromFarmInput: DisassociateMemberFromFarmInput
    DisassociateMemberFromFarmOutput: DisassociateMemberFromFarmOutput
    DisassociateMemberFromFleetInput: DisassociateMemberFromFleetInput
    DisassociateMemberFromFleetOutput: DisassociateMemberFromFleetOutput
    DisassociateMemberFromJobInput: DisassociateMemberFromJobInput
    DisassociateMemberFromJobOutput: DisassociateMemberFromJobOutput
    DisassociateMemberFromQueueInput: DisassociateMemberFromQueueInput
    DisassociateMemberFromQueueOutput: DisassociateMemberFromQueueOutput
    GetBudgetInput: GetBudgetInput
    GetBudgetOutput: GetBudgetOutput
    GetFarmInput: GetFarmInput
    GetFarmOutput: GetFarmOutput
    GetFleetInput: GetFleetInput
    GetFleetOutput: GetFleetOutput
    GetJobInput: GetJobInput
    GetJobOutput: GetJobOutput
    GetLicenseEndpointInput: GetLicenseEndpointInput
    GetLicenseEndpointOutput: GetLicenseEndpointOutput
    GetLimitInput: GetLimitInput
    GetLimitOutput: GetLimitOutput
    GetMonitorInput: GetMonitorInput
    GetMonitorOutput: GetMonitorOutput
    GetMonitorSettingsInput: GetMonitorSettingsInput
    GetMonitorSettingsOutput: GetMonitorSettingsOutput
    GetQueueInput: GetQueueInput
    GetQueueOutput: GetQueueOutput
    GetQueueEnvironmentInput: GetQueueEnvironmentInput
    GetQueueEnvironmentOutput: GetQueueEnvironmentOutput
    GetQueueFleetAssociationInput: GetQueueFleetAssociationInput
    GetQueueFleetAssociationOutput: GetQueueFleetAssociationOutput
    GetQueueLimitAssociationInput: GetQueueLimitAssociationInput
    GetQueueLimitAssociationOutput: GetQueueLimitAssociationOutput
    GetSessionInput: GetSessionInput
    GetSessionOutput: GetSessionOutput
    GetSessionActionInput: GetSessionActionInput
    GetSessionActionOutput: GetSessionActionOutput
    GetSessionsStatisticsAggregationInput: GetSessionsStatisticsAggregationInput
    GetSessionsStatisticsAggregationOutput: GetSessionsStatisticsAggregationOutput
    GetStepInput: GetStepInput
    GetStepOutput: GetStepOutput
    GetStorageProfileInput: GetStorageProfileInput
    GetStorageProfileOutput: GetStorageProfileOutput
    GetStorageProfileForQueueInput: GetStorageProfileForQueueInput
    GetStorageProfileForQueueOutput: GetStorageProfileForQueueOutput
    GetTaskInput: GetTaskInput
    GetTaskOutput: GetTaskOutput
    GetWorkerInput: GetWorkerInput
    GetWorkerOutput: GetWorkerOutput
    ListAvailableMeteredProductsInput: ListAvailableMeteredProductsInput
    ListAvailableMeteredProductsOutput: ListAvailableMeteredProductsOutput
    ListBudgetsInput: ListBudgetsInput
    ListBudgetsOutput: ListBudgetsOutput
    ListFarmMembersInput: ListFarmMembersInput
    ListFarmMembersOutput: ListFarmMembersOutput
    ListFarmsInput: ListFarmsInput
    ListFarmsOutput: ListFarmsOutput
    ListFleetMembersInput: ListFleetMembersInput
    ListFleetMembersOutput: ListFleetMembersOutput
    ListFleetsInput: ListFleetsInput
    ListFleetsOutput: ListFleetsOutput
    ListJobMembersInput: ListJobMembersInput
    ListJobMembersOutput: ListJobMembersOutput
    ListJobParameterDefinitionsInput: ListJobParameterDefinitionsInput
    ListJobParameterDefinitionsOutput: ListJobParameterDefinitionsOutput
    ListJobsInput: ListJobsInput
    ListJobsOutput: ListJobsOutput
    ListLicenseEndpointsInput: ListLicenseEndpointsInput
    ListLicenseEndpointsOutput: ListLicenseEndpointsOutput
    ListLimitsInput: ListLimitsInput
    ListLimitsOutput: ListLimitsOutput
    ListMeteredProductsInput: ListMeteredProductsInput
    ListMeteredProductsOutput: ListMeteredProductsOutput
    ListMonitorsInput: ListMonitorsInput
    ListMonitorsOutput: ListMonitorsOutput
    ListQueueEnvironmentsInput: ListQueueEnvironmentsInput
    ListQueueEnvironmentsOutput: ListQueueEnvironmentsOutput
    ListQueueFleetAssociationsInput: ListQueueFleetAssociationsInput
    ListQueueFleetAssociationsOutput: ListQueueFleetAssociationsOutput
    ListQueueLimitAssociationsInput: ListQueueLimitAssociationsInput
    ListQueueLimitAssociationsOutput: ListQueueLimitAssociationsOutput
    ListQueueMembersInput: ListQueueMembersInput
    ListQueueMembersOutput: ListQueueMembersOutput
    ListQueuesInput: ListQueuesInput
    ListQueuesOutput: ListQueuesOutput
    ListSessionActionsInput: ListSessionActionsInput
    ListSessionActionsOutput: ListSessionActionsOutput
    ListSessionsInput: ListSessionsInput
    ListSessionsOutput: ListSessionsOutput
    ListSessionsForWorkerInput: ListSessionsForWorkerInput
    ListSessionsForWorkerOutput: ListSessionsForWorkerOutput
    ListStepConsumersInput: ListStepConsumersInput
    ListStepConsumersOutput: ListStepConsumersOutput
    ListStepDependenciesInput: ListStepDependenciesInput
    ListStepDependenciesOutput: ListStepDependenciesOutput
    ListStepsInput: ListStepsInput
    ListStepsOutput: ListStepsOutput
    ListStorageProfilesInput: ListStorageProfilesInput
    ListStorageProfilesOutput: ListStorageProfilesOutput
    ListStorageProfilesForQueueInput: ListStorageProfilesForQueueInput
    ListStorageProfilesForQueueOutput: ListStorageProfilesForQueueOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    ListTasksInput: ListTasksInput
    ListTasksOutput: ListTasksOutput
    ListWorkersInput: ListWorkersInput
    ListWorkersOutput: ListWorkersOutput
    PutMeteredProductInput: PutMeteredProductInput
    PutMeteredProductOutput: PutMeteredProductOutput
    SearchJobsInput: SearchJobsInput
    SearchJobsOutput: SearchJobsOutput
    SearchStepsInput: SearchStepsInput
    SearchStepsOutput: SearchStepsOutput
    SearchTasksInput: SearchTasksInput
    SearchTasksOutput: SearchTasksOutput
    SearchWorkersInput: SearchWorkersInput
    SearchWorkersOutput: SearchWorkersOutput
    StartSessionsStatisticsAggregationInput: StartSessionsStatisticsAggregationInput
    StartSessionsStatisticsAggregationOutput: StartSessionsStatisticsAggregationOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateBudgetInput: UpdateBudgetInput
    UpdateBudgetOutput: UpdateBudgetOutput
    UpdateFarmInput: UpdateFarmInput
    UpdateFarmOutput: UpdateFarmOutput
    UpdateFleetInput: UpdateFleetInput
    UpdateFleetOutput: UpdateFleetOutput
    UpdateJobInput: UpdateJobInput
    UpdateJobOutput: UpdateJobOutput
    UpdateLimitInput: UpdateLimitInput
    UpdateLimitOutput: UpdateLimitOutput
    UpdateMonitorInput: UpdateMonitorInput
    UpdateMonitorOutput: UpdateMonitorOutput
    UpdateMonitorSettingsInput: UpdateMonitorSettingsInput
    UpdateMonitorSettingsOutput: UpdateMonitorSettingsOutput
    UpdateQueueInput: UpdateQueueInput
    UpdateQueueOutput: UpdateQueueOutput
    UpdateQueueEnvironmentInput: UpdateQueueEnvironmentInput
    UpdateQueueEnvironmentOutput: UpdateQueueEnvironmentOutput
    UpdateQueueFleetAssociationInput: UpdateQueueFleetAssociationInput
    UpdateQueueFleetAssociationOutput: UpdateQueueFleetAssociationOutput
    UpdateQueueLimitAssociationInput: UpdateQueueLimitAssociationInput
    UpdateQueueLimitAssociationOutput: UpdateQueueLimitAssociationOutput
    UpdateSessionInput: UpdateSessionInput
    UpdateSessionOutput: UpdateSessionOutput
    UpdateStepInput: UpdateStepInput
    UpdateStepOutput: UpdateStepOutput
    UpdateStorageProfileInput: UpdateStorageProfileInput
    UpdateStorageProfileOutput: UpdateStorageProfileOutput
    UpdateTaskInput: UpdateTaskInput
    UpdateTaskOutput: UpdateTaskOutput
    UpdateWorkerInput: UpdateWorkerInput
    UpdateWorkerOutput: UpdateWorkerOutput
    UpdateWorkerScheduleInput: UpdateWorkerScheduleInput
    UpdateWorkerScheduleOutput: UpdateWorkerScheduleOutput
end

return M
