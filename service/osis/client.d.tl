-- Code generated by lua-client-codegen. DO NOT EDIT.

local types = require("osis.types")

local record Operation<T>
    await: function(Operation<T>): T, table
end

local record Client
    -- Creates an OpenSearch Ingestion pipeline. For more information, see Creating Amazon OpenSearch
    -- Ingestion pipelines.
    createPipeline: function(Client, types.CreatePipelineInput): Operation<types.CreatePipelineOutput>
    -- Creates a VPC endpoint for an OpenSearch Ingestion pipeline. Pipeline endpoints allow you to
    -- ingest data from your VPC into pipelines that you have access to.
    createPipelineEndpoint: function(Client, types.CreatePipelineEndpointInput): Operation<types.CreatePipelineEndpointOutput>
    -- Deletes an OpenSearch Ingestion pipeline. For more information, see Deleting Amazon OpenSearch
    -- Ingestion pipelines.
    deletePipeline: function(Client, types.DeletePipelineInput): Operation<types.DeletePipelineOutput>
    -- Deletes a VPC endpoint for an OpenSearch Ingestion pipeline.
    deletePipelineEndpoint: function(Client, types.DeletePipelineEndpointInput): Operation<types.DeletePipelineEndpointOutput>
    -- Deletes a resource-based policy from an OpenSearch Ingestion resource.
    deleteResourcePolicy: function(Client, types.DeleteResourcePolicyInput): Operation<types.DeleteResourcePolicyOutput>
    -- Retrieves information about an OpenSearch Ingestion pipeline.
    getPipeline: function(Client, types.GetPipelineInput): Operation<types.GetPipelineOutput>
    -- Retrieves information about a specific blueprint for OpenSearch Ingestion. Blueprints are
    -- templates for the configuration needed for a CreatePipeline request. For more
    -- information, see Using
    -- blueprints to create a pipeline.
    getPipelineBlueprint: function(Client, types.GetPipelineBlueprintInput): Operation<types.GetPipelineBlueprintOutput>
    -- Returns progress information for the current change happening on an OpenSearch Ingestion
    -- pipeline. Currently, this operation only returns information when a pipeline is being
    -- created.
    -- For more information, see Tracking the status of pipeline creation.
    getPipelineChangeProgress: function(Client, types.GetPipelineChangeProgressInput): Operation<types.GetPipelineChangeProgressOutput>
    -- Retrieves the resource-based policy attached to an OpenSearch Ingestion resource.
    getResourcePolicy: function(Client, types.GetResourcePolicyInput): Operation<types.GetResourcePolicyOutput>
    -- Retrieves a list of all available blueprints for Data Prepper. For more information, see
    -- Using
    -- blueprints to create a pipeline.
    listPipelineBlueprints: function(Client, types.ListPipelineBlueprintsInput): Operation<types.ListPipelineBlueprintsOutput>
    -- Lists the pipeline endpoints connected to pipelines in your account.
    listPipelineEndpointConnections: function(Client, types.ListPipelineEndpointConnectionsInput): Operation<types.ListPipelineEndpointConnectionsOutput>
    -- Lists all pipeline endpoints in your account.
    listPipelineEndpoints: function(Client, types.ListPipelineEndpointsInput): Operation<types.ListPipelineEndpointsOutput>
    -- Lists all OpenSearch Ingestion pipelines in the current Amazon Web Services account and Region.
    -- For more information, see Viewing Amazon OpenSearch
    -- Ingestion pipelines.
    listPipelines: function(Client, types.ListPipelinesInput): Operation<types.ListPipelinesOutput>
    -- Lists all resource tags associated with an OpenSearch Ingestion pipeline. For more information,
    -- see Tagging Amazon OpenSearch Ingestion pipelines.
    listTagsForResource: function(Client, types.ListTagsForResourceInput): Operation<types.ListTagsForResourceOutput>
    -- Attaches a resource-based policy to an OpenSearch Ingestion resource. Resource-based
    -- policies grant permissions to principals to perform actions on the resource.
    putResourcePolicy: function(Client, types.PutResourcePolicyInput): Operation<types.PutResourcePolicyOutput>
    -- Revokes pipeline endpoints from specified endpoint IDs.
    revokePipelineEndpointConnections: function(Client, types.RevokePipelineEndpointConnectionsInput): Operation<types.RevokePipelineEndpointConnectionsOutput>
    -- Starts an OpenSearch Ingestion pipeline. For more information, see Starting an OpenSearch Ingestion pipeline.
    startPipeline: function(Client, types.StartPipelineInput): Operation<types.StartPipelineOutput>
    -- Stops an OpenSearch Ingestion pipeline. For more information, see Stopping
    -- an OpenSearch Ingestion pipeline.
    stopPipeline: function(Client, types.StopPipelineInput): Operation<types.StopPipelineOutput>
    -- Tags an OpenSearch Ingestion pipeline. For more information, see Tagging Amazon OpenSearch
    -- Ingestion pipelines.
    tagResource: function(Client, types.TagResourceInput): Operation<types.TagResourceOutput>
    -- Removes one or more tags from an OpenSearch Ingestion pipeline. For more information, see Tagging
    -- Amazon OpenSearch Ingestion pipelines.
    untagResource: function(Client, types.UntagResourceInput): Operation<types.UntagResourceOutput>
    -- Updates an OpenSearch Ingestion pipeline. For more information, see Updating Amazon OpenSearch
    -- Ingestion pipelines.
    updatePipeline: function(Client, types.UpdatePipelineInput): Operation<types.UpdatePipelineOutput>
    -- Checks whether an OpenSearch Ingestion pipeline configuration is valid prior to creation. For
    -- more information, see Creating Amazon OpenSearch
    -- Ingestion pipelines.
    validatePipeline: function(Client, types.ValidatePipelineInput): Operation<types.ValidatePipelineOutput>
end

local record M
    new: function(table): Client
end

return M
