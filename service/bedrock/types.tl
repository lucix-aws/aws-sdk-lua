-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record BatchDeleteEvaluationJobInput
        -- A list of one or more evaluation job Amazon Resource Names (ARNs) you want to delete.
        jobIdentifiers: {string}
    end

    record BatchDeleteEvaluationJobOutput
        -- A JSON object containing the HTTP status codes and the ARNs of evaluation jobs that failed to be deleted.
        errors: {BatchDeleteEvaluationJobError}
        -- The list of evaluation jobs for deletion.
        evaluationJobs: {BatchDeleteEvaluationJobItem}
    end

    record CancelAutomatedReasoningPolicyBuildWorkflowInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy whose build workflow you want to cancel.
        policyArn: string
        -- The unique identifier of the build workflow to cancel. You can get this ID from the StartAutomatedReasoningPolicyBuildWorkflow response or by listing build workflows.
        buildWorkflowId: string
    end

    record CancelAutomatedReasoningPolicyBuildWorkflowOutput
    end

    record CreateAutomatedReasoningPolicyInput
        -- A unique name for the Automated Reasoning policy. The name must be between 1 and 63 characters and can contain letters, numbers, hyphens, and underscores.
        name: string
        -- A description of the Automated Reasoning policy. Use this to provide context about the policy's purpose and the types of validations it performs.
        description: string
        -- A unique, case-sensitive identifier to ensure that the operation completes no more than once. If this token matches a previous request, Amazon Bedrock ignores the request but doesn't return an error.
        clientRequestToken: string
        -- The policy definition that contains the formal logic rules, variables, and custom variable types used to validate foundation model responses in your application.
        policyDefinition: AutomatedReasoningPolicyDefinition
        -- The identifier of the KMS key to use for encrypting the automated reasoning policy and its associated artifacts. If you don't specify a KMS key, Amazon Bedrock uses an KMS managed key for encryption. For enhanced security and control, you can specify a customer managed KMS key.
        kmsKeyId: string
        -- A list of tags to associate with the Automated Reasoning policy. Tags help you organize and manage your policies.
        tags: {Tag}
    end

    record CreateAutomatedReasoningPolicyOutput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy that you created.
        policyArn: string
        -- The version number of the newly created Automated Reasoning policy. The initial version is always DRAFT.
        version: string
        -- The name of the Automated Reasoning policy.
        name: string
        -- The description of the Automated Reasoning policy.
        description: string
        -- The hash of the policy definition. This is used as a concurrency token for creating policy versions that you can use in your application.
        definitionHash: string
        -- The timestamp when the policy was created.
        createdAt: number
        -- The timestamp when the policy was last updated.
        updatedAt: number
    end

    record CreateAutomatedReasoningPolicyTestCaseInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy for which to create the test.
        policyArn: string
        -- The output content that's validated by the Automated Reasoning policy. This represents the foundation model response that will be checked for accuracy.
        guardContent: string
        -- The input query or prompt that generated the content. This provides context for the validation.
        queryContent: string
        -- The expected result of the Automated Reasoning check. Valid values include: , TOO_COMPLEX, and NO_TRANSLATIONS. VALID - The claims are true. The claims are implied by the premises and the Automated Reasoning policy. Given the Automated Reasoning policy and premises, it is not possible for these claims to be false. In other words, there are no alternative answers that are true that contradict the claims. INVALID - The claims are false. The claims are not implied by the premises and Automated Reasoning policy. Furthermore, there exists different claims that are consistent with the premises and Automated Reasoning policy. SATISFIABLE - The claims can be true or false. It depends on what assumptions are made for the claim to be implied from the premises and Automated Reasoning policy rules. In this situation, different assumptions can make input claims false and alternative claims true. IMPOSSIBLE - Automated Reasoning can’t make a statement about the claims. This can happen if the premises are logically incorrect, or if there is a conflict within the Automated Reasoning policy itself. TRANSLATION_AMBIGUOUS - Detected an ambiguity in the translation meant it would be unsound to continue with validity checking. Additional context or follow-up questions might be needed to get translation to succeed. TOO_COMPLEX - The input contains too much information for Automated Reasoning to process within its latency limits. NO_TRANSLATIONS - Identifies that some or all of the input prompt wasn't translated into logic. This can happen if the input isn't relevant to the Automated Reasoning policy, or if the policy doesn't have variables to model relevant input. If Automated Reasoning can't translate anything, you get a single NO_TRANSLATIONS finding. You might also see a NO_TRANSLATIONS (along with other findings) if some part of the validation isn't translated.
        expectedAggregatedFindingsResult: string
        -- A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error.
        clientRequestToken: string
        -- The minimum confidence level for logic validation. Content that meets the threshold is considered a high-confidence finding that can be validated.
        confidenceThreshold: number
    end

    record CreateAutomatedReasoningPolicyTestCaseOutput
        -- The Amazon Resource Name (ARN) of the policy for which the test was created.
        policyArn: string
        -- The unique identifier of the created test.
        testCaseId: string
    end

    record CreateAutomatedReasoningPolicyVersionInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy for which to create a version.
        policyArn: string
        -- A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error.
        clientRequestToken: string
        -- The hash of the current policy definition used as a concurrency token to ensure the policy hasn't been modified since you last retrieved it.
        lastUpdatedDefinitionHash: string
        -- A list of tags to associate with the policy version.
        tags: {Tag}
    end

    record CreateAutomatedReasoningPolicyVersionOutput
        -- The versioned Amazon Resource Name (ARN) of the policy version.
        policyArn: string
        -- The version number of the policy version.
        version: string
        -- The name of the policy version.
        name: string
        -- The description of the policy version.
        description: string
        -- The hash of the policy definition for this version.
        definitionHash: string
        -- The timestamp when the policy version was created.
        createdAt: number
    end

    record CreateCustomModelInput
        -- A unique name for the custom model.
        modelName: string
        -- The data source for the model. The Amazon S3 URI in the model source must be for the Amazon-managed Amazon S3 bucket containing your model artifacts.
        modelSourceConfig: ModelDataSource
        -- The Amazon Resource Name (ARN) of the customer managed KMS key to encrypt the custom model. If you don't provide a KMS key, Amazon Bedrock uses an Amazon Web Services-managed KMS key to encrypt the model. If you provide a customer managed KMS key, your Amazon Bedrock service role must have permissions to use it. For more information see Encryption of imported models.
        modelKmsKeyArn: string
        -- The Amazon Resource Name (ARN) of an IAM service role that Amazon Bedrock assumes to perform tasks on your behalf. This role must have permissions to access the Amazon S3 bucket containing your model artifacts and the KMS key (if specified). For more information, see Setting up an IAM service role for importing models in the Amazon Bedrock User Guide.
        roleArn: string
        -- A list of key-value pairs to associate with the custom model resource. You can use these tags to organize and identify your resources. For more information, see Tagging resources in the Amazon Bedrock User Guide.
        modelTags: {Tag}
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency.
        clientRequestToken: string
    end

    record CreateCustomModelOutput
        -- The Amazon Resource Name (ARN) of the new custom model.
        modelArn: string
    end

    record CreateCustomModelDeploymentInput
        -- The name for the custom model deployment. The name must be unique within your Amazon Web Services account and Region.
        modelDeploymentName: string
        -- The Amazon Resource Name (ARN) of the custom model to deploy for on-demand inference. The custom model must be in the Active state.
        modelArn: string
        -- A description for the custom model deployment to help you identify its purpose.
        description: string
        -- Tags to assign to the custom model deployment. You can use tags to organize and track your Amazon Web Services resources for cost allocation and management purposes.
        tags: {Tag}
        -- A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency.
        clientRequestToken: string
    end

    record CreateCustomModelDeploymentOutput
        -- The Amazon Resource Name (ARN) of the custom model deployment. Use this ARN as the modelId parameter when invoking the model with the InvokeModel or Converse operations.
        customModelDeploymentArn: string
    end

    record CreateEvaluationJobInput
        -- A name for the evaluation job. Names must unique with your Amazon Web Services account, and your account's Amazon Web Services region.
        jobName: string
        -- A description of the evaluation job.
        jobDescription: string
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency.
        clientRequestToken: string
        -- The Amazon Resource Name (ARN) of an IAM service role that Amazon Bedrock can assume to perform tasks on your behalf. To learn more about the required permissions, see Required permissions for model evaluations.
        roleArn: string
        -- Specify your customer managed encryption key Amazon Resource Name (ARN) that will be used to encrypt your evaluation job.
        customerEncryptionKeyId: string
        -- Tags to attach to the model evaluation job.
        jobTags: {Tag}
        -- Specifies whether the evaluation job is for evaluating a model or evaluating a knowledge base (retrieval and response generation).
        applicationType: string
        -- Contains the configuration details of either an automated or human-based evaluation job.
        evaluationConfig: EvaluationConfig
        -- Contains the configuration details of the inference model for the evaluation job. For model evaluation jobs, automated jobs support a single model or inference profile, and jobs that use human workers support two models or inference profiles.
        inferenceConfig: EvaluationInferenceConfig
        -- Contains the configuration details of the Amazon S3 bucket for storing the results of the evaluation job.
        outputDataConfig: EvaluationOutputDataConfig
    end

    record CreateEvaluationJobOutput
        -- The Amazon Resource Name (ARN) of the evaluation job.
        jobArn: string
    end

    record CreateFoundationModelAgreementInput
        -- An offer token encapsulates the information for an offer.
        offerToken: string
        -- Model Id of the model for the access request.
        modelId: string
    end

    record CreateFoundationModelAgreementOutput
        -- Model Id of the model for the access request.
        modelId: string
    end

    record CreateGuardrailInput
        -- The name to give the guardrail.
        name: string
        -- A description of the guardrail.
        description: string
        -- The topic policies to configure for the guardrail.
        topicPolicyConfig: GuardrailTopicPolicyConfig
        -- The content filter policies to configure for the guardrail.
        contentPolicyConfig: GuardrailContentPolicyConfig
        -- The word policy you configure for the guardrail.
        wordPolicyConfig: GuardrailWordPolicyConfig
        -- The sensitive information policy to configure for the guardrail.
        sensitiveInformationPolicyConfig: GuardrailSensitiveInformationPolicyConfig
        -- The contextual grounding policy configuration used to create a guardrail.
        contextualGroundingPolicyConfig: GuardrailContextualGroundingPolicyConfig
        -- Optional configuration for integrating Automated Reasoning policies with the new guardrail.
        automatedReasoningPolicyConfig: GuardrailAutomatedReasoningPolicyConfig
        -- The system-defined guardrail profile that you're using with your guardrail. Guardrail profiles define the destination Amazon Web Services Regions where guardrail inference requests can be automatically routed. For more information, see the Amazon Bedrock User Guide.
        crossRegionConfig: GuardrailCrossRegionConfig
        -- The message to return when the guardrail blocks a prompt.
        blockedInputMessaging: string
        -- The message to return when the guardrail blocks a model response.
        blockedOutputsMessaging: string
        -- The ARN of the KMS key that you use to encrypt the guardrail.
        kmsKeyId: string
        -- The tags that you want to attach to the guardrail.
        tags: {Tag}
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than once. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency in the Amazon S3 User Guide.
        clientRequestToken: string
    end

    record CreateGuardrailOutput
        -- The unique identifier of the guardrail that was created.
        guardrailId: string
        -- The ARN of the guardrail.
        guardrailArn: string
        -- The version of the guardrail that was created. This value will always be DRAFT.
        version: string
        -- The time at which the guardrail was created.
        createdAt: number
    end

    record CreateGuardrailVersionInput
        -- The unique identifier of the guardrail. This can be an ID or the ARN.
        guardrailIdentifier: string
        -- A description of the guardrail version.
        description: string
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than once. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency in the Amazon S3 User Guide.
        clientRequestToken: string
    end

    record CreateGuardrailVersionOutput
        -- The unique identifier of the guardrail.
        guardrailId: string
        -- The number of the version of the guardrail.
        version: string
    end

    record CreateInferenceProfileInput
        -- A name for the inference profile.
        inferenceProfileName: string
        -- A description for the inference profile.
        description: string
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency.
        clientRequestToken: string
        -- The foundation model or system-defined inference profile that the inference profile will track metrics and costs for.
        modelSource: InferenceProfileModelSource
        -- An array of objects, each of which contains a tag and its value. For more information, see Tagging resources in the Amazon Bedrock User Guide.
        tags: {Tag}
    end

    record CreateInferenceProfileOutput
        -- The ARN of the inference profile that you created.
        inferenceProfileArn: string
        -- The status of the inference profile. ACTIVE means that the inference profile is ready to be used.
        status: string
    end

    record CreateMarketplaceModelEndpointInput
        -- The ARN of the model from Amazon Bedrock Marketplace that you want to deploy to the endpoint.
        modelSourceIdentifier: string
        -- The configuration for the endpoint, including the number and type of instances to use.
        endpointConfig: EndpointConfig
        -- Indicates whether you accept the end-user license agreement (EULA) for the model. Set to true to accept the EULA.
        acceptEula: boolean
        -- The name of the endpoint. This name must be unique within your Amazon Web Services account and region.
        endpointName: string
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. This token is listed as not required because Amazon Web Services SDKs automatically generate it for you and set this parameter. If you're not using the Amazon Web Services SDK or the CLI, you must provide this token or the action will fail.
        clientRequestToken: string
        -- An array of key-value pairs to apply to the underlying Amazon SageMaker endpoint. You can use these tags to organize and identify your Amazon Web Services resources.
        tags: {Tag}
    end

    record CreateMarketplaceModelEndpointOutput
        -- Details about the created endpoint.
        marketplaceModelEndpoint: MarketplaceModelEndpoint
    end

    record CreateModelCopyJobInput
        -- The Amazon Resource Name (ARN) of the model to be copied.
        sourceModelArn: string
        -- A name for the copied model.
        targetModelName: string
        -- The ARN of the KMS key that you use to encrypt the model copy.
        modelKmsKeyId: string
        -- Tags to associate with the target model. For more information, see Tag resources in the Amazon Bedrock User Guide.
        targetModelTags: {Tag}
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency.
        clientRequestToken: string
    end

    record CreateModelCopyJobOutput
        -- The Amazon Resource Name (ARN) of the model copy job.
        jobArn: string
    end

    record CreateModelCustomizationJobInput
        -- A name for the fine-tuning job.
        jobName: string
        -- A name for the resulting custom model.
        customModelName: string
        -- The Amazon Resource Name (ARN) of an IAM service role that Amazon Bedrock can assume to perform tasks on your behalf. For example, during model training, Amazon Bedrock needs your permission to read input data from an S3 bucket, write model artifacts to an S3 bucket. To pass this role to Amazon Bedrock, the caller of this API must have the iam:PassRole permission.
        roleArn: string
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency.
        clientRequestToken: string
        -- Name of the base model.
        baseModelIdentifier: string
        -- The customization type.
        customizationType: string
        -- The custom model is encrypted at rest using this key.
        customModelKmsKeyId: string
        -- Tags to attach to the job.
        jobTags: {Tag}
        -- Tags to attach to the resulting custom model.
        customModelTags: {Tag}
        -- Information about the training dataset.
        trainingDataConfig: TrainingDataConfig
        -- Information about the validation dataset.
        validationDataConfig: ValidationDataConfig
        -- S3 location for the output data.
        outputDataConfig: OutputDataConfig
        -- Parameters related to tuning the model. For details on the format for different models, see Custom model hyperparameters.
        hyperParameters: {string : string}
        -- The configuration of the Virtual Private Cloud (VPC) that contains the resources that you're using for this job. For more information, see Protect your model customization jobs using a VPC.
        vpcConfig: VpcConfig
        -- The customization configuration for the model customization job.
        customizationConfig: CustomizationConfig
    end

    record CreateModelCustomizationJobOutput
        -- Amazon Resource Name (ARN) of the fine tuning job
        jobArn: string
    end

    record CreateModelImportJobInput
        -- The name of the import job.
        jobName: string
        -- The name of the imported model.
        importedModelName: string
        -- The Amazon Resource Name (ARN) of the model import job.
        roleArn: string
        -- The data source for the imported model.
        modelDataSource: ModelDataSource
        -- Tags to attach to this import job.
        jobTags: {Tag}
        -- Tags to attach to the imported model.
        importedModelTags: {Tag}
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency.
        clientRequestToken: string
        -- VPC configuration parameters for the private Virtual Private Cloud (VPC) that contains the resources you are using for the import job.
        vpcConfig: VpcConfig
        -- The imported model is encrypted at rest using this key.
        importedModelKmsKeyId: string
    end

    record CreateModelImportJobOutput
        -- The Amazon Resource Name (ARN) of the model import job.
        jobArn: string
    end

    record CreateModelInvocationJobInput
        -- A name to give the batch inference job.
        jobName: string
        -- The Amazon Resource Name (ARN) of the service role with permissions to carry out and manage batch inference. You can use the console to create a default service role or follow the steps at Create a service role for batch inference.
        roleArn: string
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency.
        clientRequestToken: string
        -- The unique identifier of the foundation model to use for the batch inference job.
        modelId: string
        -- Details about the location of the input to the batch inference job.
        inputDataConfig: ModelInvocationJobInputDataConfig
        -- Details about the location of the output of the batch inference job.
        outputDataConfig: ModelInvocationJobOutputDataConfig
        -- The configuration of the Virtual Private Cloud (VPC) for the data in the batch inference job. For more information, see Protect batch inference jobs using a VPC.
        vpcConfig: VpcConfig
        -- The number of hours after which to force the batch inference job to time out.
        timeoutDurationInHours: number
        -- Any tags to associate with the batch inference job. For more information, see Tagging Amazon Bedrock resources.
        tags: {Tag}
        -- The invocation endpoint for ModelInvocationJob
        modelInvocationType: string
    end

    record CreateModelInvocationJobOutput
        -- The Amazon Resource Name (ARN) of the batch inference job.
        jobArn: string
    end

    record CreatePromptRouterInput
        -- A unique, case-sensitive identifier that you provide to ensure idempotency of your requests. If not specified, the Amazon Web Services SDK automatically generates one for you.
        clientRequestToken: string
        -- The name of the prompt router. The name must be unique within your Amazon Web Services account in the current region.
        promptRouterName: string
        -- A list of foundation models that the prompt router can route requests to. At least one model must be specified.
        models: {PromptRouterTargetModel}
        -- An optional description of the prompt router to help identify its purpose.
        description: string
        -- The criteria, which is the response quality difference, used to determine how incoming requests are routed to different models.
        routingCriteria: RoutingCriteria
        -- The default model to use when the routing criteria is not met.
        fallbackModel: PromptRouterTargetModel
        -- An array of key-value pairs to apply to this resource as tags. You can use tags to categorize and manage your Amazon Web Services resources.
        tags: {Tag}
    end

    record CreatePromptRouterOutput
        -- The Amazon Resource Name (ARN) that uniquely identifies the prompt router.
        promptRouterArn: string
    end

    record CreateProvisionedModelThroughputInput
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency in the Amazon S3 User Guide.
        clientRequestToken: string
        -- Number of model units to allocate. A model unit delivers a specific throughput level for the specified model. The throughput level of a model unit specifies the total number of input and output tokens that it can process and generate within a span of one minute. By default, your account has no model units for purchasing Provisioned Throughputs with commitment. You must first visit the Amazon Web Services support center to request MUs. For model unit quotas, see Provisioned Throughput quotas in the Amazon Bedrock User Guide. For more information about what an MU specifies, contact your Amazon Web Services account manager.
        modelUnits: number
        -- The name for this Provisioned Throughput.
        provisionedModelName: string
        -- The Amazon Resource Name (ARN) or name of the model to associate with this Provisioned Throughput. For a list of models for which you can purchase Provisioned Throughput, see Amazon Bedrock model IDs for purchasing Provisioned Throughput in the Amazon Bedrock User Guide.
        modelId: string
        -- The commitment duration requested for the Provisioned Throughput. Billing occurs hourly and is discounted for longer commitment terms. To request a no-commit Provisioned Throughput, omit this field. Custom models support all levels of commitment. To see which base models support no commitment, see Supported regions and models for Provisioned Throughput in the Amazon Bedrock User Guide
        commitmentDuration: string
        -- Tags to associate with this Provisioned Throughput.
        tags: {Tag}
    end

    record CreateProvisionedModelThroughputOutput
        -- The Amazon Resource Name (ARN) for this Provisioned Throughput.
        provisionedModelArn: string
    end

    record DeleteAutomatedReasoningPolicyInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy to delete.
        policyArn: string
        -- Specifies whether to force delete the automated reasoning policy even if it has active resources. When false, Amazon Bedrock validates if all artifacts have been deleted (e.g. policy version, test case, test result) for a policy before deletion. When true, Amazon Bedrock will delete the policy and all its artifacts without validation. Default is false.
        force: boolean
    end

    record DeleteAutomatedReasoningPolicyOutput
    end

    record DeleteAutomatedReasoningPolicyBuildWorkflowInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy whose build workflow you want to delete.
        policyArn: string
        -- The unique identifier of the build workflow to delete.
        buildWorkflowId: string
        -- The timestamp when the build workflow was last updated. This is used for optimistic concurrency control to prevent accidental deletion of workflows that have been modified.
        lastUpdatedAt: number
    end

    record DeleteAutomatedReasoningPolicyBuildWorkflowOutput
    end

    record DeleteAutomatedReasoningPolicyTestCaseInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy that contains the test.
        policyArn: string
        -- The unique identifier of the test to delete.
        testCaseId: string
        -- The timestamp when the test was last updated. This is used as a concurrency token to prevent conflicting modifications.
        lastUpdatedAt: number
    end

    record DeleteAutomatedReasoningPolicyTestCaseOutput
    end

    record DeleteCustomModelInput
        -- Name of the model to delete.
        modelIdentifier: string
    end

    record DeleteCustomModelOutput
    end

    record DeleteCustomModelDeploymentInput
        -- The Amazon Resource Name (ARN) or name of the custom model deployment to delete.
        customModelDeploymentIdentifier: string
    end

    record DeleteCustomModelDeploymentOutput
    end

    record DeleteEnforcedGuardrailConfigurationInput
        -- Unique ID for the account enforced configuration.
        configId: string
    end

    record DeleteEnforcedGuardrailConfigurationOutput
    end

    record DeleteFoundationModelAgreementInput
        -- Model Id of the model access to delete.
        modelId: string
    end

    record DeleteFoundationModelAgreementOutput
    end

    record DeleteGuardrailInput
        -- The unique identifier of the guardrail. This can be an ID or the ARN.
        guardrailIdentifier: string
        -- The version of the guardrail.
        guardrailVersion: string
    end

    record DeleteGuardrailOutput
    end

    record DeleteImportedModelInput
        -- Name of the imported model to delete.
        modelIdentifier: string
    end

    record DeleteImportedModelOutput
    end

    record DeleteInferenceProfileInput
        -- The Amazon Resource Name (ARN) or ID of the application inference profile to delete.
        inferenceProfileIdentifier: string
    end

    record DeleteInferenceProfileOutput
    end

    record DeleteMarketplaceModelEndpointInput
        -- The Amazon Resource Name (ARN) of the endpoint you want to delete.
        endpointArn: string
    end

    record DeleteMarketplaceModelEndpointOutput
    end

    record DeleteModelInvocationLoggingConfigurationInput
    end

    record DeleteModelInvocationLoggingConfigurationOutput
    end

    record DeletePromptRouterInput
        -- The Amazon Resource Name (ARN) of the prompt router to delete.
        promptRouterArn: string
    end

    record DeletePromptRouterOutput
    end

    record DeleteProvisionedModelThroughputInput
        -- The Amazon Resource Name (ARN) or name of the Provisioned Throughput.
        provisionedModelId: string
    end

    record DeleteProvisionedModelThroughputOutput
    end

    record DeleteResourcePolicyInput
        -- The ARN of the Bedrock resource to which this resource policy applies.
        resourceArn: string
    end

    record DeleteResourcePolicyOutput
    end

    record DeregisterMarketplaceModelEndpointInput
        -- The Amazon Resource Name (ARN) of the endpoint you want to deregister.
        endpointArn: string
    end

    record DeregisterMarketplaceModelEndpointOutput
    end

    record ExportAutomatedReasoningPolicyVersionInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy to export. Can be either the unversioned ARN for the draft policy or a versioned ARN for a specific policy version.
        policyArn: string
    end

    record ExportAutomatedReasoningPolicyVersionOutput
        -- The exported policy definition containing the formal logic rules, variables, and custom variable types.
        policyDefinition: AutomatedReasoningPolicyDefinition
    end

    record GetAutomatedReasoningPolicyInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy to retrieve. Can be either the unversioned ARN for the draft policy or an ARN for a specific policy version.
        policyArn: string
    end

    record GetAutomatedReasoningPolicyOutput
        -- The Amazon Resource Name (ARN) of the policy.
        policyArn: string
        -- The name of the policy.
        name: string
        -- The version of the policy.
        version: string
        -- The unique identifier of the policy.
        policyId: string
        -- The description of the policy.
        description: string
        -- The hash of the policy definition used as a concurrency token.
        definitionHash: string
        -- The Amazon Resource Name (ARN) of the KMS key used to encrypt the automated reasoning policy and its associated artifacts. If a KMS key is not provided during the initial CreateAutomatedReasoningPolicyRequest, the kmsKeyArn won't be included in the GetAutomatedReasoningPolicyResponse.
        kmsKeyArn: string
        -- The timestamp when the policy was created.
        createdAt: number
        -- The timestamp when the policy was last updated.
        updatedAt: number
    end

    record GetAutomatedReasoningPolicyAnnotationsInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy whose annotations you want to retrieve.
        policyArn: string
        -- The unique identifier of the build workflow whose annotations you want to retrieve.
        buildWorkflowId: string
    end

    record GetAutomatedReasoningPolicyAnnotationsOutput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy.
        policyArn: string
        -- The name of the Automated Reasoning policy.
        name: string
        -- The unique identifier of the build workflow.
        buildWorkflowId: string
        -- The current set of annotations containing rules, variables, and types extracted from the source documents. These can be modified before finalizing the policy.
        annotations: {AutomatedReasoningPolicyAnnotation}
        -- A hash value representing the current state of the annotations. This is used for optimistic concurrency control when updating annotations.
        annotationSetHash: string
        -- The timestamp when the annotations were last updated.
        updatedAt: number
    end

    record GetAutomatedReasoningPolicyBuildWorkflowInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy whose build workflow you want to retrieve.
        policyArn: string
        -- The unique identifier of the build workflow to retrieve.
        buildWorkflowId: string
    end

    record GetAutomatedReasoningPolicyBuildWorkflowOutput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy.
        policyArn: string
        -- The unique identifier of the build workflow.
        buildWorkflowId: string
        -- The current status of the build workflow (e.g., RUNNING, COMPLETED, FAILED, CANCELLED).
        status: string
        -- The type of build workflow being executed (e.g., DOCUMENT_INGESTION, POLICY_REPAIR).
        buildWorkflowType: string
        -- The name of the source document used in the build workflow.
        documentName: string
        -- The content type of the source document (e.g., text/plain, application/pdf).
        documentContentType: string
        -- A detailed description of the document's content and how it should be used in the policy generation process.
        documentDescription: string
        -- The timestamp when the build workflow was created.
        createdAt: number
        -- The timestamp when the build workflow was last updated.
        updatedAt: number
    end

    record GetAutomatedReasoningPolicyBuildWorkflowResultAssetsInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy whose build workflow assets you want to retrieve.
        policyArn: string
        -- The unique identifier of the build workflow whose result assets you want to retrieve.
        buildWorkflowId: string
        -- The type of asset to retrieve (e.g., BUILD_LOG, QUALITY_REPORT, POLICY_DEFINITION, GENERATED_TEST_CASES, POLICY_SCENARIOS, FIDELITY_REPORT, ASSET_MANIFEST, SOURCE_DOCUMENT).
        assetType: string
        -- The unique identifier of the specific asset to retrieve when multiple assets of the same type exist. This is required when retrieving SOURCE_DOCUMENT assets, as multiple source documents may have been used in the workflow. The asset ID can be obtained from the asset manifest.
        assetId: string
    end

    record GetAutomatedReasoningPolicyBuildWorkflowResultAssetsOutput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy.
        policyArn: string
        -- The unique identifier of the build workflow.
        buildWorkflowId: string
        -- The requested build workflow asset. This is a union type that returns only one of the available asset types (logs, reports, or generated artifacts) based on the specific asset type requested in the API call.
        buildWorkflowAssets: AutomatedReasoningPolicyBuildResultAssets
    end

    record GetAutomatedReasoningPolicyNextScenarioInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy for which you want to get the next test scenario.
        policyArn: string
        -- The unique identifier of the build workflow associated with the test scenarios.
        buildWorkflowId: string
    end

    record GetAutomatedReasoningPolicyNextScenarioOutput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy.
        policyArn: string
        -- The next test scenario to validate, including the test expression and expected results.
        scenario: AutomatedReasoningPolicyScenario
    end

    record GetAutomatedReasoningPolicyTestCaseInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy that contains the test.
        policyArn: string
        -- The unique identifier of the test to retrieve.
        testCaseId: string
    end

    record GetAutomatedReasoningPolicyTestCaseOutput
        -- The Amazon Resource Name (ARN) of the policy that contains the test.
        policyArn: string
        -- The test details including the content, query, expected result, and metadata.
        testCase: AutomatedReasoningPolicyTestCase
    end

    record GetAutomatedReasoningPolicyTestResultInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy.
        policyArn: string
        -- The build workflow identifier. The build workflow must display a COMPLETED status to get results.
        buildWorkflowId: string
        -- The unique identifier of the test for which to retrieve results.
        testCaseId: string
    end

    record GetAutomatedReasoningPolicyTestResultOutput
        -- The test result containing validation findings, execution status, and detailed analysis.
        testResult: AutomatedReasoningPolicyTestResult
    end

    record GetCustomModelInput
        -- Name or Amazon Resource Name (ARN) of the custom model.
        modelIdentifier: string
    end

    record GetCustomModelOutput
        -- Amazon Resource Name (ARN) associated with this model.
        modelArn: string
        -- Model name associated with this model.
        modelName: string
        -- Job name associated with this model.
        jobName: string
        -- Job Amazon Resource Name (ARN) associated with this model. For models that you create with the CreateCustomModel API operation, this is NULL.
        jobArn: string
        -- Amazon Resource Name (ARN) of the base model.
        baseModelArn: string
        -- The type of model customization.
        customizationType: string
        -- The custom model is encrypted at rest using this key.
        modelKmsKeyArn: string
        -- Hyperparameter values associated with this model. For details on the format for different models, see Custom model hyperparameters.
        hyperParameters: {string : string}
        -- Contains information about the training dataset.
        trainingDataConfig: TrainingDataConfig
        -- Contains information about the validation dataset.
        validationDataConfig: ValidationDataConfig
        -- Output data configuration associated with this custom model.
        outputDataConfig: OutputDataConfig
        -- Contains training metrics from the job creation.
        trainingMetrics: TrainingMetrics
        -- The validation metrics from the job creation.
        validationMetrics: {ValidatorMetric}
        -- Creation time of the model.
        creationTime: number
        -- The customization configuration for the custom model.
        customizationConfig: CustomizationConfig
        -- The current status of the custom model. Possible values include: Creating - The model is being created and validated. Active - The model has been successfully created and is ready for use. Failed - The model creation process failed. Check the failureMessage field for details.
        modelStatus: string
        -- A failure message for any issues that occurred when creating the custom model. This is included for only a failed CreateCustomModel operation.
        failureMessage: string
    end

    record GetCustomModelDeploymentInput
        -- The Amazon Resource Name (ARN) or name of the custom model deployment to retrieve information about.
        customModelDeploymentIdentifier: string
    end

    record GetCustomModelDeploymentOutput
        -- The Amazon Resource Name (ARN) of the custom model deployment.
        customModelDeploymentArn: string
        -- The name of the custom model deployment.
        modelDeploymentName: string
        -- The Amazon Resource Name (ARN) of the custom model associated with this deployment.
        modelArn: string
        -- The date and time when the custom model deployment was created.
        createdAt: number
        -- The status of the custom model deployment. Possible values are: CREATING - The deployment is being set up and prepared for inference. ACTIVE - The deployment is ready and available for inference requests. FAILED - The deployment failed to be created or became unavailable.
        status: string
        -- The description of the custom model deployment.
        description: string
        -- Details about any pending or completed updates to the custom model deployment, including the new model ARN and update status.
        updateDetails: CustomModelDeploymentUpdateDetails
        -- If the deployment status is FAILED, this field contains a message describing the failure reason.
        failureMessage: string
        -- The date and time when the custom model deployment was last updated.
        lastUpdatedAt: number
    end

    record GetEvaluationJobInput
        -- The Amazon Resource Name (ARN) of the evaluation job you want get information on.
        jobIdentifier: string
    end

    record GetEvaluationJobOutput
        -- The name for the evaluation job.
        jobName: string
        -- The current status of the evaluation job.
        status: string
        -- The Amazon Resource Name (ARN) of the evaluation job.
        jobArn: string
        -- The description of the evaluation job.
        jobDescription: string
        -- The Amazon Resource Name (ARN) of the IAM service role used in the evaluation job.
        roleArn: string
        -- The Amazon Resource Name (ARN) of the customer managed encryption key specified when the evaluation job was created.
        customerEncryptionKeyId: string
        -- Specifies whether the evaluation job is automated or human-based.
        jobType: string
        -- Specifies whether the evaluation job is for evaluating a model or evaluating a knowledge base (retrieval and response generation).
        applicationType: string
        -- Contains the configuration details of either an automated or human-based evaluation job.
        evaluationConfig: EvaluationConfig
        -- Contains the configuration details of the inference model used for the evaluation job.
        inferenceConfig: EvaluationInferenceConfig
        -- Contains the configuration details of the Amazon S3 bucket for storing the results of the evaluation job.
        outputDataConfig: EvaluationOutputDataConfig
        -- The time the evaluation job was created.
        creationTime: number
        -- The time the evaluation job was last modified.
        lastModifiedTime: number
        -- A list of strings that specify why the evaluation job failed to create.
        failureMessages: {string}
    end

    record GetFoundationModelInput
        -- The model identifier.
        modelIdentifier: string
    end

    record GetFoundationModelOutput
        -- Information about the foundation model.
        modelDetails: FoundationModelDetails
    end

    record GetFoundationModelAvailabilityInput
        -- The model Id of the foundation model.
        modelId: string
    end

    record GetFoundationModelAvailabilityOutput
        -- The model Id of the foundation model.
        modelId: string
        -- Agreement availability.
        agreementAvailability: AgreementAvailability
        -- Authorization status.
        authorizationStatus: string
        -- Entitlement availability.
        entitlementAvailability: string
        -- Region availability.
        regionAvailability: string
    end

    record GetGuardrailInput
        -- The unique identifier of the guardrail for which to get details. This can be an ID or the ARN.
        guardrailIdentifier: string
        -- The version of the guardrail for which to get details. If you don't specify a version, the response returns details for the DRAFT version.
        guardrailVersion: string
    end

    record GetGuardrailOutput
        -- The name of the guardrail.
        name: string
        -- The description of the guardrail.
        description: string
        -- The unique identifier of the guardrail.
        guardrailId: string
        -- The ARN of the guardrail.
        guardrailArn: string
        -- The version of the guardrail.
        version: string
        -- The status of the guardrail.
        status: string
        -- The topic policy that was configured for the guardrail.
        topicPolicy: GuardrailTopicPolicy
        -- The content policy that was configured for the guardrail.
        contentPolicy: GuardrailContentPolicy
        -- The word policy that was configured for the guardrail.
        wordPolicy: GuardrailWordPolicy
        -- The sensitive information policy that was configured for the guardrail.
        sensitiveInformationPolicy: GuardrailSensitiveInformationPolicy
        -- The contextual grounding policy used in the guardrail.
        contextualGroundingPolicy: GuardrailContextualGroundingPolicy
        -- The current Automated Reasoning policy configuration for the guardrail, if any is configured.
        automatedReasoningPolicy: GuardrailAutomatedReasoningPolicy
        -- Details about the system-defined guardrail profile that you're using with your guardrail, including the guardrail profile ID and Amazon Resource Name (ARN).
        crossRegionDetails: GuardrailCrossRegionDetails
        -- The date and time at which the guardrail was created.
        createdAt: number
        -- The date and time at which the guardrail was updated.
        updatedAt: number
        -- Appears if the status is FAILED. A list of reasons for why the guardrail failed to be created, updated, versioned, or deleted.
        statusReasons: {string}
        -- Appears if the status of the guardrail is FAILED. A list of recommendations to carry out before retrying the request.
        failureRecommendations: {string}
        -- The message that the guardrail returns when it blocks a prompt.
        blockedInputMessaging: string
        -- The message that the guardrail returns when it blocks a model response.
        blockedOutputsMessaging: string
        -- The ARN of the KMS key that encrypts the guardrail.
        kmsKeyArn: string
    end

    record GetImportedModelInput
        -- Name or Amazon Resource Name (ARN) of the imported model.
        modelIdentifier: string
    end

    record GetImportedModelOutput
        -- The Amazon Resource Name (ARN) associated with this imported model.
        modelArn: string
        -- The name of the imported model.
        modelName: string
        -- Job name associated with the imported model.
        jobName: string
        -- Job Amazon Resource Name (ARN) associated with the imported model.
        jobArn: string
        -- The data source for this imported model.
        modelDataSource: ModelDataSource
        -- Creation time of the imported model.
        creationTime: number
        -- The architecture of the imported model.
        modelArchitecture: string
        -- The imported model is encrypted at rest using this key.
        modelKmsKeyArn: string
        -- Specifies if the imported model supports converse.
        instructSupported: boolean
        -- Information about the hardware utilization for a single copy of the model.
        customModelUnits: CustomModelUnits
    end

    record GetInferenceProfileInput
        -- The ID or Amazon Resource Name (ARN) of the inference profile.
        inferenceProfileIdentifier: string
    end

    record GetInferenceProfileOutput
        -- The name of the inference profile.
        inferenceProfileName: string
        -- The description of the inference profile.
        description: string
        -- The time at which the inference profile was created.
        createdAt: number
        -- The time at which the inference profile was last updated.
        updatedAt: number
        -- The Amazon Resource Name (ARN) of the inference profile.
        inferenceProfileArn: string
        -- A list of information about each model in the inference profile.
        models: {InferenceProfileModel}
        -- The unique identifier of the inference profile.
        inferenceProfileId: string
        -- The status of the inference profile. ACTIVE means that the inference profile is ready to be used.
        status: string
    end

    record GetMarketplaceModelEndpointInput
        -- The Amazon Resource Name (ARN) of the endpoint you want to get information about.
        endpointArn: string
    end

    record GetMarketplaceModelEndpointOutput
        -- Details about the requested endpoint.
        marketplaceModelEndpoint: MarketplaceModelEndpoint
    end

    record GetModelCopyJobInput
        -- The Amazon Resource Name (ARN) of the model copy job.
        jobArn: string
    end

    record GetModelCopyJobOutput
        -- The Amazon Resource Name (ARN) of the model copy job.
        jobArn: string
        -- The status of the model copy job.
        status: string
        -- The time at which the model copy job was created.
        creationTime: number
        -- The Amazon Resource Name (ARN) of the copied model.
        targetModelArn: string
        -- The name of the copied model.
        targetModelName: string
        -- The unique identifier of the account that the model being copied originated from.
        sourceAccountId: string
        -- The Amazon Resource Name (ARN) of the original model being copied.
        sourceModelArn: string
        -- The Amazon Resource Name (ARN) of the KMS key encrypting the copied model.
        targetModelKmsKeyArn: string
        -- The tags associated with the copied model.
        targetModelTags: {Tag}
        -- An error message for why the model copy job failed.
        failureMessage: string
        -- The name of the original model being copied.
        sourceModelName: string
    end

    record GetModelCustomizationJobInput
        -- Identifier for the customization job.
        jobIdentifier: string
    end

    record GetModelCustomizationJobOutput
        -- The Amazon Resource Name (ARN) of the customization job.
        jobArn: string
        -- The name of the customization job.
        jobName: string
        -- The name of the output model.
        outputModelName: string
        -- The Amazon Resource Name (ARN) of the output model.
        outputModelArn: string
        -- The token that you specified in the CreateCustomizationJob request.
        clientRequestToken: string
        -- The Amazon Resource Name (ARN) of the IAM role.
        roleArn: string
        -- The status of the job. A successful job transitions from in-progress to completed when the output model is ready to use. If the job failed, the failure message contains information about why the job failed.
        status: string
        -- For a Distillation job, the details about the statuses of the sub-tasks of the customization job.
        statusDetails: StatusDetails
        -- Information about why the job failed.
        failureMessage: string
        -- Time that the resource was created.
        creationTime: number
        -- Time that the resource was last modified.
        lastModifiedTime: number
        -- Time that the resource transitioned to terminal state.
        endTime: number
        -- Amazon Resource Name (ARN) of the base model.
        baseModelArn: string
        -- The hyperparameter values for the job. For details on the format for different models, see Custom model hyperparameters.
        hyperParameters: {string : string}
        -- Contains information about the training dataset.
        trainingDataConfig: TrainingDataConfig
        -- Contains information about the validation dataset.
        validationDataConfig: ValidationDataConfig
        -- Output data configuration
        outputDataConfig: OutputDataConfig
        -- The type of model customization.
        customizationType: string
        -- The custom model is encrypted at rest using this key.
        outputModelKmsKeyArn: string
        -- Contains training metrics from the job creation.
        trainingMetrics: TrainingMetrics
        -- The loss metric for each validator that you provided in the createjob request.
        validationMetrics: {ValidatorMetric}
        -- VPC configuration for the custom model job.
        vpcConfig: VpcConfig
        -- The customization configuration for the model customization job.
        customizationConfig: CustomizationConfig
    end

    record GetModelImportJobInput
        -- The identifier of the import job.
        jobIdentifier: string
    end

    record GetModelImportJobOutput
        -- The Amazon Resource Name (ARN) of the import job.
        jobArn: string
        -- The name of the import job.
        jobName: string
        -- The name of the imported model.
        importedModelName: string
        -- The Amazon Resource Name (ARN) of the imported model.
        importedModelArn: string
        -- The Amazon Resource Name (ARN) of the IAM role associated with this job.
        roleArn: string
        -- The data source for the imported model.
        modelDataSource: ModelDataSource
        -- The status of the job. A successful job transitions from in-progress to completed when the imported model is ready to use. If the job failed, the failure message contains information about why the job failed.
        status: string
        -- Information about why the import job failed.
        failureMessage: string
        -- The time the resource was created.
        creationTime: number
        -- Time the resource was last modified.
        lastModifiedTime: number
        -- Time that the resource transitioned to terminal state.
        endTime: number
        -- The Virtual Private Cloud (VPC) configuration of the import model job.
        vpcConfig: VpcConfig
        -- The imported model is encrypted at rest using this key.
        importedModelKmsKeyArn: string
    end

    record GetModelInvocationJobInput
        -- The Amazon Resource Name (ARN) of the batch inference job.
        jobIdentifier: string
    end

    record GetModelInvocationJobOutput
        -- The Amazon Resource Name (ARN) of the batch inference job.
        jobArn: string
        -- The name of the batch inference job.
        jobName: string
        -- The unique identifier of the foundation model used for model inference.
        modelId: string
        -- A unique, case-sensitive identifier to ensure that the API request completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error. For more information, see Ensuring idempotency.
        clientRequestToken: string
        -- The Amazon Resource Name (ARN) of the service role with permissions to carry out and manage batch inference. You can use the console to create a default service role or follow the steps at Create a service role for batch inference.
        roleArn: string
        -- The status of the batch inference job. The following statuses are possible: Submitted – This job has been submitted to a queue for validation. Validating – This job is being validated for the requirements described in Format and upload your batch inference data. The criteria include the following: Your IAM service role has access to the Amazon S3 buckets containing your files. Your files are .jsonl files and each individual record is a JSON object in the correct format. Note that validation doesn't check if the modelInput value matches the request body for the model. Your files fulfill the requirements for file size and number of records. For more information, see Quotas for Amazon Bedrock. Scheduled – This job has been validated and is now in a queue. The job will automatically start when it reaches its turn. Expired – This job timed out because it was scheduled but didn't begin before the set timeout duration. Submit a new job request. InProgress – This job has begun. You can start viewing the results in the output S3 location. Completed – This job has successfully completed. View the output files in the output S3 location. PartiallyCompleted – This job has partially completed. Not all of your records could be processed in time. View the output files in the output S3 location. Failed – This job has failed. Check the failure message for any further details. For further assistance, reach out to the Amazon Web Services Support Center. Stopped – This job was stopped by a user. Stopping – This job is being stopped by a user.
        status: string
        -- If the batch inference job failed, this field contains a message describing why the job failed.
        message: string
        -- The time at which the batch inference job was submitted.
        submitTime: number
        -- The time at which the batch inference job was last modified.
        lastModifiedTime: number
        -- The time at which the batch inference job ended.
        endTime: number
        -- Details about the location of the input to the batch inference job.
        inputDataConfig: ModelInvocationJobInputDataConfig
        -- Details about the location of the output of the batch inference job.
        outputDataConfig: ModelInvocationJobOutputDataConfig
        -- The configuration of the Virtual Private Cloud (VPC) for the data in the batch inference job. For more information, see Protect batch inference jobs using a VPC.
        vpcConfig: VpcConfig
        -- The number of hours after which batch inference job was set to time out.
        timeoutDurationInHours: number
        -- The time at which the batch inference job times or timed out.
        jobExpirationTime: number
        -- The invocation endpoint for ModelInvocationJob
        modelInvocationType: string
        -- The total number of records in the batch inference job.
        totalRecordCount: number
        -- The number of records that have been processed in the batch inference job.
        processedRecordCount: number
        -- The number of records that were successfully processed in the batch inference job.
        successRecordCount: number
        -- The number of records that failed to process in the batch inference job.
        errorRecordCount: number
    end

    record GetModelInvocationLoggingConfigurationInput
    end

    record GetModelInvocationLoggingConfigurationOutput
        -- The current configuration values.
        loggingConfig: LoggingConfig
    end

    record GetPromptRouterInput
        -- The prompt router's ARN
        promptRouterArn: string
    end

    record GetPromptRouterOutput
        -- The router's name.
        promptRouterName: string
        -- The router's routing criteria.
        routingCriteria: RoutingCriteria
        -- The router's description.
        description: string
        -- When the router was created.
        createdAt: number
        -- When the router was updated.
        updatedAt: number
        -- The prompt router's ARN
        promptRouterArn: string
        -- The router's models.
        models: {PromptRouterTargetModel}
        -- The router's fallback model.
        fallbackModel: PromptRouterTargetModel
        -- The router's status.
        status: string
    end

    record GetProvisionedModelThroughputInput
        -- The Amazon Resource Name (ARN) or name of the Provisioned Throughput.
        provisionedModelId: string
    end

    record GetProvisionedModelThroughputOutput
        -- The number of model units allocated to this Provisioned Throughput.
        modelUnits: number
        -- The number of model units that was requested for this Provisioned Throughput.
        desiredModelUnits: number
        -- The name of the Provisioned Throughput.
        provisionedModelName: string
        -- The Amazon Resource Name (ARN) of the Provisioned Throughput.
        provisionedModelArn: string
        -- The Amazon Resource Name (ARN) of the model associated with this Provisioned Throughput.
        modelArn: string
        -- The Amazon Resource Name (ARN) of the model requested to be associated to this Provisioned Throughput. This value differs from the modelArn if updating hasn't completed.
        desiredModelArn: string
        -- The Amazon Resource Name (ARN) of the base model for which the Provisioned Throughput was created, or of the base model that the custom model for which the Provisioned Throughput was created was customized.
        foundationModelArn: string
        -- The status of the Provisioned Throughput.
        status: string
        -- The timestamp of the creation time for this Provisioned Throughput.
        creationTime: number
        -- The timestamp of the last time that this Provisioned Throughput was modified.
        lastModifiedTime: number
        -- A failure message for any issues that occurred during creation, updating, or deletion of the Provisioned Throughput.
        failureMessage: string
        -- Commitment duration of the Provisioned Throughput.
        commitmentDuration: string
        -- The timestamp for when the commitment term for the Provisioned Throughput expires.
        commitmentExpirationTime: number
    end

    record GetResourcePolicyInput
        -- The ARN of the Bedrock resource to which this resource policy applies.
        resourceArn: string
    end

    record GetResourcePolicyOutput
        -- The JSON string representing the Bedrock resource policy.
        resourcePolicy: string
    end

    record GetUseCaseForModelAccessInput
    end

    record GetUseCaseForModelAccessOutput
        -- Get customer profile Response.
        formData: string
    end

    record ListAutomatedReasoningPoliciesInput
        -- Optional filter to list only the policy versions with the specified Amazon Resource Name (ARN). If not provided, the DRAFT versions for all policies are listed.
        policyArn: string
        -- The pagination token from a previous request to retrieve the next page of results.
        nextToken: string
        -- The maximum number of policies to return in a single call.
        maxResults: number
    end

    record ListAutomatedReasoningPoliciesOutput
        -- A list of Automated Reasoning policy summaries.
        automatedReasoningPolicySummaries: {AutomatedReasoningPolicySummary}
        -- The pagination token to use in a subsequent request to retrieve the next page of results.
        nextToken: string
    end

    record ListAutomatedReasoningPolicyBuildWorkflowsInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy whose build workflows you want to list.
        policyArn: string
        -- A pagination token from a previous request to continue listing build workflows from where the previous request left off.
        nextToken: string
        -- The maximum number of build workflows to return in a single response. Valid range is 1-100.
        maxResults: number
    end

    record ListAutomatedReasoningPolicyBuildWorkflowsOutput
        -- A list of build workflow summaries, each containing key information about a build workflow including its status and timestamps.
        automatedReasoningPolicyBuildWorkflowSummaries: {AutomatedReasoningPolicyBuildWorkflowSummary}
        -- A pagination token to use in subsequent requests to retrieve additional build workflows.
        nextToken: string
    end

    record ListAutomatedReasoningPolicyTestCasesInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy for which to list tests.
        policyArn: string
        -- The pagination token from a previous request to retrieve the next page of results.
        nextToken: string
        -- The maximum number of tests to return in a single call.
        maxResults: number
    end

    record ListAutomatedReasoningPolicyTestCasesOutput
        -- A list of tests for the specified policy.
        testCases: {AutomatedReasoningPolicyTestCase}
        -- The pagination token to use in a subsequent request to retrieve the next page of results.
        nextToken: string
    end

    record ListAutomatedReasoningPolicyTestResultsInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy whose test results you want to list.
        policyArn: string
        -- The unique identifier of the build workflow whose test results you want to list.
        buildWorkflowId: string
        -- A pagination token from a previous request to continue listing test results from where the previous request left off.
        nextToken: string
        -- The maximum number of test results to return in a single response. Valid range is 1-100.
        maxResults: number
    end

    record ListAutomatedReasoningPolicyTestResultsOutput
        -- A list of test results, each containing information about how the policy performed on specific test scenarios.
        testResults: {AutomatedReasoningPolicyTestResult}
        -- A pagination token to use in subsequent requests to retrieve additional test results.
        nextToken: string
    end

    record ListCustomModelDeploymentsInput
        -- Filters deployments created before the specified date and time.
        createdBefore: number
        -- Filters deployments created after the specified date and time.
        createdAfter: number
        -- Filters deployments whose names contain the specified string.
        nameContains: string
        -- The maximum number of results to return in a single call.
        maxResults: number
        -- The token for the next set of results. Use this token to retrieve additional results when the response is truncated.
        nextToken: string
        -- The field to sort the results by. The only supported value is CreationTime.
        sortBy: string
        -- The sort order for the results. Valid values are Ascending and Descending. Default is Descending.
        sortOrder: string
        -- Filters deployments by status. Valid values are CREATING, ACTIVE, and FAILED.
        statusEquals: string
        -- Filters deployments by the Amazon Resource Name (ARN) of the associated custom model.
        modelArnEquals: string
    end

    record ListCustomModelDeploymentsOutput
        -- The token for the next set of results. This value is null when there are no more results to return.
        nextToken: string
        -- A list of custom model deployment summaries.
        modelDeploymentSummaries: {CustomModelDeploymentSummary}
    end

    record ListCustomModelsInput
        -- Return custom models created before the specified time.
        creationTimeBefore: number
        -- Return custom models created after the specified time.
        creationTimeAfter: number
        -- Return custom models only if the job name contains these characters.
        nameContains: string
        -- Return custom models only if the base model Amazon Resource Name (ARN) matches this parameter.
        baseModelArnEquals: string
        -- Return custom models only if the foundation model Amazon Resource Name (ARN) matches this parameter.
        foundationModelArnEquals: string
        -- The maximum number of results to return in the response. If the total number of results is greater than this value, use the token returned in the response in the nextToken field when making another request to return the next batch of results.
        maxResults: number
        -- If the total number of results is greater than the maxResults value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results.
        nextToken: string
        -- The field to sort by in the returned list of models.
        sortBy: string
        -- The sort order of the results.
        sortOrder: string
        -- Return custom models depending on if the current account owns them (true) or if they were shared with the current account (false).
        isOwned: boolean
        -- The status of them model to filter results by. Possible values include: Creating - Include only models that are currently being created and validated. Active - Include only models that have been successfully created and are ready for use. Failed - Include only models where the creation process failed. If you don't specify a status, the API returns models in all states.
        modelStatus: string
    end

    record ListCustomModelsOutput
        -- If the total number of results is greater than the maxResults value provided in the request, use this token when making another request in the nextToken field to return the next batch of results.
        nextToken: string
        -- Model summaries.
        modelSummaries: {CustomModelSummary}
    end

    record ListEnforcedGuardrailsConfigurationInput
        -- Opaque continuation token of previous paginated response.
        nextToken: string
    end

    record ListEnforcedGuardrailsConfigurationOutput
        -- Array of AccountEnforcedGuardrailOutputConfiguration objects.
        guardrailsConfig: {AccountEnforcedGuardrailOutputConfiguration}
        -- Opaque continuation token of previous paginated response.
        nextToken: string
    end

    record ListEvaluationJobsInput
        -- A filter to only list evaluation jobs created after a specified time.
        creationTimeAfter: number
        -- A filter to only list evaluation jobs created before a specified time.
        creationTimeBefore: number
        -- A filter to only list evaluation jobs that are of a certain status.
        statusEquals: string
        -- A filter to only list evaluation jobs that are either model evaluations or knowledge base evaluations.
        applicationTypeEquals: string
        -- A filter to only list evaluation jobs that contain a specified string in the job name.
        nameContains: string
        -- The maximum number of results to return.
        maxResults: number
        -- Continuation token from the previous response, for Amazon Bedrock to list the next set of results.
        nextToken: string
        -- Specifies a creation time to sort the list of evaluation jobs by when they were created.
        sortBy: string
        -- Specifies whether to sort the list of evaluation jobs by either ascending or descending order.
        sortOrder: string
    end

    record ListEvaluationJobsOutput
        -- Continuation token from the previous response, for Amazon Bedrock to list the next set of results.
        nextToken: string
        -- A list of summaries of the evaluation jobs.
        jobSummaries: {EvaluationSummary}
    end

    record ListFoundationModelAgreementOffersInput
        -- Model Id of the foundation model.
        modelId: string
        -- Type of offer associated with the model.
        offerType: string
    end

    record ListFoundationModelAgreementOffersOutput
        -- Model Id of the foundation model.
        modelId: string
        -- List of the offers associated with the specified model.
        offers: {Offer}
    end

    record ListFoundationModelsInput
        -- Return models belonging to the model provider that you specify.
        byProvider: string
        -- Return models that support the customization type that you specify. For more information, see Custom models in the Amazon Bedrock User Guide.
        byCustomizationType: string
        -- Return models that support the output modality that you specify.
        byOutputModality: string
        -- Return models that support the inference type that you specify. For more information, see Provisioned Throughput in the Amazon Bedrock User Guide.
        byInferenceType: string
    end

    record ListFoundationModelsOutput
        -- A list of Amazon Bedrock foundation models.
        modelSummaries: {FoundationModelSummary}
    end

    record ListGuardrailsInput
        -- The unique identifier of the guardrail. This can be an ID or the ARN.
        guardrailIdentifier: string
        -- The maximum number of results to return in the response.
        maxResults: number
        -- If there are more results than were returned in the response, the response returns a nextToken that you can send in another ListGuardrails request to see the next batch of results.
        nextToken: string
    end

    record ListGuardrailsOutput
        -- A list of objects, each of which contains details about a guardrail.
        guardrails: {GuardrailSummary}
        -- If there are more results than were returned in the response, the response returns a nextToken that you can send in another ListGuardrails request to see the next batch of results.
        nextToken: string
    end

    record ListImportedModelsInput
        -- Return imported models that created before the specified time.
        creationTimeBefore: number
        -- Return imported models that were created after the specified time.
        creationTimeAfter: number
        -- Return imported models only if the model name contains these characters.
        nameContains: string
        -- The maximum number of results to return in the response. If the total number of results is greater than this value, use the token returned in the response in the nextToken field when making another request to return the next batch of results.
        maxResults: number
        -- If the total number of results is greater than the maxResults value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results.
        nextToken: string
        -- The field to sort by in the returned list of imported models.
        sortBy: string
        -- Specifies whetehr to sort the results in ascending or descending order.
        sortOrder: string
    end

    record ListImportedModelsOutput
        -- If the total number of results is greater than the maxResults value provided in the request, use this token when making another request in the nextToken field to return the next batch of results.
        nextToken: string
        -- Model summaries.
        modelSummaries: {ImportedModelSummary}
    end

    record ListInferenceProfilesInput
        -- The maximum number of results to return in the response. If the total number of results is greater than this value, use the token returned in the response in the nextToken field when making another request to return the next batch of results.
        maxResults: number
        -- If the total number of results is greater than the maxResults value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results.
        nextToken: string
        -- Filters for inference profiles that match the type you specify. SYSTEM_DEFINED – The inference profile is defined by Amazon Bedrock. You can route inference requests across regions with these inference profiles. APPLICATION – The inference profile was created by a user. This type of inference profile can track metrics and costs when invoking the model in it. The inference profile may route requests to one or multiple regions.
        typeEquals: string
    end

    record ListInferenceProfilesOutput
        -- A list of information about each inference profile that you can use.
        inferenceProfileSummaries: {InferenceProfileSummary}
        -- If the total number of results is greater than the maxResults value provided in the request, use this token when making another request in the nextToken field to return the next batch of results.
        nextToken: string
    end

    record ListMarketplaceModelEndpointsInput
        -- The maximum number of results to return in a single call. If more results are available, the operation returns a NextToken value.
        maxResults: number
        -- The token for the next set of results. You receive this token from a previous ListMarketplaceModelEndpoints call.
        nextToken: string
        -- If specified, only endpoints for the given model source identifier are returned.
        modelSourceEquals: string
    end

    record ListMarketplaceModelEndpointsOutput
        -- An array of endpoint summaries.
        marketplaceModelEndpoints: {MarketplaceModelEndpointSummary}
        -- The token for the next set of results. Use this token to get the next set of results.
        nextToken: string
    end

    record ListModelCopyJobsInput
        -- Filters for model copy jobs created after the specified time.
        creationTimeAfter: number
        -- Filters for model copy jobs created before the specified time.
        creationTimeBefore: number
        -- Filters for model copy jobs whose status matches the value that you specify.
        statusEquals: string
        -- Filters for model copy jobs in which the account that the source model belongs to is equal to the value that you specify.
        sourceAccountEquals: string
        -- Filters for model copy jobs in which the Amazon Resource Name (ARN) of the source model to is equal to the value that you specify.
        sourceModelArnEquals: string
        -- Filters for model copy jobs in which the name of the copied model contains the string that you specify.
        targetModelNameContains: string
        -- The maximum number of results to return in the response. If the total number of results is greater than this value, use the token returned in the response in the nextToken field when making another request to return the next batch of results.
        maxResults: number
        -- If the total number of results is greater than the maxResults value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results.
        nextToken: string
        -- The field to sort by in the returned list of model copy jobs.
        sortBy: string
        -- Specifies whether to sort the results in ascending or descending order.
        sortOrder: string
    end

    record ListModelCopyJobsOutput
        -- If the total number of results is greater than the maxResults value provided in the request, use this token when making another request in the nextToken field to return the next batch of results.
        nextToken: string
        -- A list of information about each model copy job.
        modelCopyJobSummaries: {ModelCopyJobSummary}
    end

    record ListModelCustomizationJobsInput
        -- Return customization jobs created after the specified time.
        creationTimeAfter: number
        -- Return customization jobs created before the specified time.
        creationTimeBefore: number
        -- Return customization jobs with the specified status.
        statusEquals: string
        -- Return customization jobs only if the job name contains these characters.
        nameContains: string
        -- The maximum number of results to return in the response. If the total number of results is greater than this value, use the token returned in the response in the nextToken field when making another request to return the next batch of results.
        maxResults: number
        -- If the total number of results is greater than the maxResults value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results.
        nextToken: string
        -- The field to sort by in the returned list of jobs.
        sortBy: string
        -- The sort order of the results.
        sortOrder: string
    end

    record ListModelCustomizationJobsOutput
        -- If the total number of results is greater than the maxResults value provided in the request, use this token when making another request in the nextToken field to return the next batch of results.
        nextToken: string
        -- Job summaries.
        modelCustomizationJobSummaries: {ModelCustomizationJobSummary}
    end

    record ListModelImportJobsInput
        -- Return import jobs that were created after the specified time.
        creationTimeAfter: number
        -- Return import jobs that were created before the specified time.
        creationTimeBefore: number
        -- Return imported jobs with the specified status.
        statusEquals: string
        -- Return imported jobs only if the job name contains these characters.
        nameContains: string
        -- The maximum number of results to return in the response. If the total number of results is greater than this value, use the token returned in the response in the nextToken field when making another request to return the next batch of results.
        maxResults: number
        -- If the total number of results is greater than the maxResults value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results.
        nextToken: string
        -- The field to sort by in the returned list of imported jobs.
        sortBy: string
        -- Specifies whether to sort the results in ascending or descending order.
        sortOrder: string
    end

    record ListModelImportJobsOutput
        -- If the total number of results is greater than the maxResults value provided in the request, enter the token returned in the nextToken field in the response in this field to return the next batch of results.
        nextToken: string
        -- Import job summaries.
        modelImportJobSummaries: {ModelImportJobSummary}
    end

    record ListModelInvocationJobsInput
        -- Specify a time to filter for batch inference jobs that were submitted after the time you specify.
        submitTimeAfter: number
        -- Specify a time to filter for batch inference jobs that were submitted before the time you specify.
        submitTimeBefore: number
        -- Specify a status to filter for batch inference jobs whose statuses match the string you specify. The following statuses are possible: Submitted – This job has been submitted to a queue for validation. Validating – This job is being validated for the requirements described in Format and upload your batch inference data. The criteria include the following: Your IAM service role has access to the Amazon S3 buckets containing your files. Your files are .jsonl files and each individual record is a JSON object in the correct format. Note that validation doesn't check if the modelInput value matches the request body for the model. Your files fulfill the requirements for file size and number of records. For more information, see Quotas for Amazon Bedrock. Scheduled – This job has been validated and is now in a queue. The job will automatically start when it reaches its turn. Expired – This job timed out because it was scheduled but didn't begin before the set timeout duration. Submit a new job request. InProgress – This job has begun. You can start viewing the results in the output S3 location. Completed – This job has successfully completed. View the output files in the output S3 location. PartiallyCompleted – This job has partially completed. Not all of your records could be processed in time. View the output files in the output S3 location. Failed – This job has failed. Check the failure message for any further details. For further assistance, reach out to the Amazon Web Services Support Center. Stopped – This job was stopped by a user. Stopping – This job is being stopped by a user.
        statusEquals: string
        -- Specify a string to filter for batch inference jobs whose names contain the string.
        nameContains: string
        -- The maximum number of results to return. If there are more results than the number that you specify, a nextToken value is returned. Use the nextToken in a request to return the next batch of results.
        maxResults: number
        -- If there were more results than the value you specified in the maxResults field in a previous ListModelInvocationJobs request, the response would have returned a nextToken value. To see the next batch of results, send the nextToken value in another request.
        nextToken: string
        -- An attribute by which to sort the results.
        sortBy: string
        -- Specifies whether to sort the results by ascending or descending order.
        sortOrder: string
    end

    record ListModelInvocationJobsOutput
        -- If there are more results than can fit in the response, a nextToken is returned. Use the nextToken in a request to return the next batch of results.
        nextToken: string
        -- A list of items, each of which contains a summary about a batch inference job.
        invocationJobSummaries: {ModelInvocationJobSummary}
    end

    record ListPromptRoutersInput
        -- The maximum number of prompt routers to return in one page of results.
        maxResults: number
        -- Specify the pagination token from a previous request to retrieve the next page of results.
        nextToken: string
    end

    record ListPromptRoutersOutput
        -- A list of prompt router summaries.
        promptRouterSummaries: {PromptRouterSummary}
        -- Specify the pagination token from a previous request to retrieve the next page of results.
        nextToken: string
    end

    record ListProvisionedModelThroughputsInput
        -- A filter that returns Provisioned Throughputs created after the specified time.
        creationTimeAfter: number
        -- A filter that returns Provisioned Throughputs created before the specified time.
        creationTimeBefore: number
        -- A filter that returns Provisioned Throughputs if their statuses matches the value that you specify.
        statusEquals: string
        -- A filter that returns Provisioned Throughputs whose model Amazon Resource Name (ARN) is equal to the value that you specify.
        modelArnEquals: string
        -- A filter that returns Provisioned Throughputs if their name contains the expression that you specify.
        nameContains: string
        -- THe maximum number of results to return in the response. If there are more results than the number you specified, the response returns a nextToken value. To see the next batch of results, send the nextToken value in another list request.
        maxResults: number
        -- If there are more results than the number you specified in the maxResults field, the response returns a nextToken value. To see the next batch of results, specify the nextToken value in this field.
        nextToken: string
        -- The field by which to sort the returned list of Provisioned Throughputs.
        sortBy: string
        -- The sort order of the results.
        sortOrder: string
    end

    record ListProvisionedModelThroughputsOutput
        -- If there are more results than the number you specified in the maxResults field, this value is returned. To see the next batch of results, include this value in the nextToken field in another list request.
        nextToken: string
        -- A list of summaries, one for each Provisioned Throughput in the response.
        provisionedModelSummaries: {ProvisionedModelSummary}
    end

    record ListTagsForResourceInput
        -- The Amazon Resource Name (ARN) of the resource.
        resourceARN: string
    end

    record ListTagsForResourceOutput
        -- An array of the tags associated with this resource.
        tags: {Tag}
    end

    record PutEnforcedGuardrailConfigurationInput
        -- Unique ID for the account enforced configuration.
        configId: string
        -- Account-level enforced guardrail input configuration.
        guardrailInferenceConfig: AccountEnforcedGuardrailInferenceInputConfiguration
    end

    record PutEnforcedGuardrailConfigurationOutput
        -- Unique ID for the account enforced configuration.
        configId: string
        -- Timestamp.
        updatedAt: number
        -- The ARN of the role used to update the configuration.
        updatedBy: string
    end

    record PutModelInvocationLoggingConfigurationInput
        -- The logging configuration values to set.
        loggingConfig: LoggingConfig
    end

    record PutModelInvocationLoggingConfigurationOutput
    end

    record PutResourcePolicyInput
        -- The ARN of the Bedrock resource to which this resource policy applies.
        resourceArn: string
        -- The JSON string representing the Bedrock resource policy.
        resourcePolicy: string
    end

    record PutResourcePolicyOutput
        -- The ARN of the Bedrock resource to which this resource policy applies.
        resourceArn: string
    end

    record PutUseCaseForModelAccessInput
        -- Put customer profile Request.
        formData: string
    end

    record PutUseCaseForModelAccessOutput
    end

    record RegisterMarketplaceModelEndpointInput
        -- The ARN of the Amazon SageMaker endpoint you want to register with Amazon Bedrock Marketplace.
        endpointIdentifier: string
        -- The ARN of the model from Amazon Bedrock Marketplace that is deployed on the endpoint.
        modelSourceIdentifier: string
    end

    record RegisterMarketplaceModelEndpointOutput
        -- Details about the registered endpoint.
        marketplaceModelEndpoint: MarketplaceModelEndpoint
    end

    record StartAutomatedReasoningPolicyBuildWorkflowInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy for which to start the build workflow.
        policyArn: string
        -- The type of build workflow to start (e.g., DOCUMENT_INGESTION for processing new documents, POLICY_REPAIR for fixing existing policies).
        buildWorkflowType: string
        -- A unique, case-sensitive identifier to ensure that the operation completes no more than once. If this token matches a previous request, Amazon Bedrock ignores the request but doesn't return an error.
        clientRequestToken: string
        -- The source content for the build workflow, such as documents to analyze or repair instructions for existing policies.
        sourceContent: AutomatedReasoningPolicyBuildWorkflowSource
    end

    record StartAutomatedReasoningPolicyBuildWorkflowOutput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy.
        policyArn: string
        -- The unique identifier of the newly started build workflow. Use this ID to track the workflow's progress and retrieve its results.
        buildWorkflowId: string
    end

    record StartAutomatedReasoningPolicyTestWorkflowInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy to test.
        policyArn: string
        -- The build workflow identifier. The build workflow must show a COMPLETED status before running tests.
        buildWorkflowId: string
        -- The list of test identifiers to run. If not provided, all tests for the policy are run.
        testCaseIds: {string}
        -- A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request but doesn't return an error.
        clientRequestToken: string
    end

    record StartAutomatedReasoningPolicyTestWorkflowOutput
        -- The Amazon Resource Name (ARN) of the policy for which the test workflow was started.
        policyArn: string
    end

    record StopEvaluationJobInput
        -- The Amazon Resource Name (ARN) of the evaluation job you want to stop.
        jobIdentifier: string
    end

    record StopEvaluationJobOutput
    end

    record StopModelCustomizationJobInput
        -- Job identifier of the job to stop.
        jobIdentifier: string
    end

    record StopModelCustomizationJobOutput
    end

    record StopModelInvocationJobInput
        -- The Amazon Resource Name (ARN) of the batch inference job to stop.
        jobIdentifier: string
    end

    record StopModelInvocationJobOutput
    end

    record TagResourceInput
        -- The Amazon Resource Name (ARN) of the resource to tag.
        resourceARN: string
        -- Tags to associate with the resource.
        tags: {Tag}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The Amazon Resource Name (ARN) of the resource to untag.
        resourceARN: string
        -- Tag keys of the tags to remove from the resource.
        tagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateAutomatedReasoningPolicyInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy to update. This must be the ARN of a draft policy.
        policyArn: string
        -- The updated policy definition containing the formal logic rules, variables, and types.
        policyDefinition: AutomatedReasoningPolicyDefinition
        -- The updated name for the Automated Reasoning policy.
        name: string
        -- The updated description for the Automated Reasoning policy.
        description: string
    end

    record UpdateAutomatedReasoningPolicyOutput
        -- The Amazon Resource Name (ARN) of the updated policy.
        policyArn: string
        -- The updated name of the policy.
        name: string
        -- The hash of the updated policy definition.
        definitionHash: string
        -- The timestamp when the policy was last updated.
        updatedAt: number
    end

    record UpdateAutomatedReasoningPolicyAnnotationsInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy whose annotations you want to update.
        policyArn: string
        -- The unique identifier of the build workflow whose annotations you want to update.
        buildWorkflowId: string
        -- The updated annotations containing modified rules, variables, and types for the policy.
        annotations: {AutomatedReasoningPolicyAnnotation}
        -- The hash value of the annotation set that you're updating. This is used for optimistic concurrency control to prevent conflicting updates.
        lastUpdatedAnnotationSetHash: string
    end

    record UpdateAutomatedReasoningPolicyAnnotationsOutput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy.
        policyArn: string
        -- The unique identifier of the build workflow.
        buildWorkflowId: string
        -- The new hash value representing the updated state of the annotations.
        annotationSetHash: string
        -- The timestamp when the annotations were updated.
        updatedAt: number
    end

    record UpdateAutomatedReasoningPolicyTestCaseInput
        -- The Amazon Resource Name (ARN) of the Automated Reasoning policy that contains the test.
        policyArn: string
        -- The unique identifier of the test to update.
        testCaseId: string
        -- The updated content to be validated by the Automated Reasoning policy.
        guardContent: string
        -- The updated input query or prompt that generated the content.
        queryContent: string
        -- The timestamp when the test was last updated. This is used as a concurrency token to prevent conflicting modifications.
        lastUpdatedAt: number
        -- The updated expected result of the Automated Reasoning check.
        expectedAggregatedFindingsResult: string
        -- The updated minimum confidence level for logic validation. If null is provided, the threshold will be removed.
        confidenceThreshold: number
        -- A unique, case-sensitive identifier to ensure that the operation completes no more than one time. If this token matches a previous request, Amazon Bedrock ignores the request, but does not return an error.
        clientRequestToken: string
    end

    record UpdateAutomatedReasoningPolicyTestCaseOutput
        -- The Amazon Resource Name (ARN) of the policy that contains the updated test.
        policyArn: string
        -- The unique identifier of the updated test.
        testCaseId: string
    end

    record UpdateCustomModelDeploymentInput
        -- ARN of the new custom model to deploy. This replaces the currently deployed model.
        modelArn: string
        -- Identifier of the custom model deployment to update with the new custom model.
        customModelDeploymentIdentifier: string
    end

    record UpdateCustomModelDeploymentOutput
        -- ARN of the custom model deployment being updated.
        customModelDeploymentArn: string
    end

    record UpdateGuardrailInput
        -- The unique identifier of the guardrail. This can be an ID or the ARN.
        guardrailIdentifier: string
        -- A name for the guardrail.
        name: string
        -- A description of the guardrail.
        description: string
        -- The topic policy to configure for the guardrail.
        topicPolicyConfig: GuardrailTopicPolicyConfig
        -- The content policy to configure for the guardrail.
        contentPolicyConfig: GuardrailContentPolicyConfig
        -- The word policy to configure for the guardrail.
        wordPolicyConfig: GuardrailWordPolicyConfig
        -- The sensitive information policy to configure for the guardrail.
        sensitiveInformationPolicyConfig: GuardrailSensitiveInformationPolicyConfig
        -- The contextual grounding policy configuration used to update a guardrail.
        contextualGroundingPolicyConfig: GuardrailContextualGroundingPolicyConfig
        -- Updated configuration for Automated Reasoning policies associated with the guardrail.
        automatedReasoningPolicyConfig: GuardrailAutomatedReasoningPolicyConfig
        -- The system-defined guardrail profile that you're using with your guardrail. Guardrail profiles define the destination Amazon Web Services Regions where guardrail inference requests can be automatically routed. For more information, see the Amazon Bedrock User Guide.
        crossRegionConfig: GuardrailCrossRegionConfig
        -- The message to return when the guardrail blocks a prompt.
        blockedInputMessaging: string
        -- The message to return when the guardrail blocks a model response.
        blockedOutputsMessaging: string
        -- The ARN of the KMS key with which to encrypt the guardrail.
        kmsKeyId: string
    end

    record UpdateGuardrailOutput
        -- The unique identifier of the guardrail
        guardrailId: string
        -- The ARN of the guardrail.
        guardrailArn: string
        -- The version of the guardrail.
        version: string
        -- The date and time at which the guardrail was updated.
        updatedAt: number
    end

    record UpdateMarketplaceModelEndpointInput
        -- The Amazon Resource Name (ARN) of the endpoint you want to update.
        endpointArn: string
        -- The new configuration for the endpoint, including the number and type of instances to use.
        endpointConfig: EndpointConfig
        -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. This token is listed as not required because Amazon Web Services SDKs automatically generate it for you and set this parameter. If you're not using the Amazon Web Services SDK or the CLI, you must provide this token or the action will fail.
        clientRequestToken: string
    end

    record UpdateMarketplaceModelEndpointOutput
        -- Details about the updated endpoint.
        marketplaceModelEndpoint: MarketplaceModelEndpoint
    end

    record UpdateProvisionedModelThroughputInput
        -- The Amazon Resource Name (ARN) or name of the Provisioned Throughput to update.
        provisionedModelId: string
        -- The new name for this Provisioned Throughput.
        desiredProvisionedModelName: string
        -- The Amazon Resource Name (ARN) of the new model to associate with this Provisioned Throughput. You can't specify this field if this Provisioned Throughput is associated with a base model. If this Provisioned Throughput is associated with a custom model, you can specify one of the following options: The base model from which the custom model was customized. Another custom model that was customized from the same base model as the custom model.
        desiredModelId: string
    end

    record UpdateProvisionedModelThroughputOutput
    end

    -- The request is denied because of missing access permissions.
    record AccessDeniedException
        message: string
    end

    -- Error occurred because of a conflict while performing an operation.
    record ConflictException
        message: string
    end

    -- An internal server error occurred. Retry your request.
    record InternalServerException
        message: string
    end

    -- The specified resource Amazon Resource Name (ARN) was not found. Check the Amazon Resource Name (ARN) and try your request again.
    record ResourceNotFoundException
        message: string
    end

    -- The number of requests exceeds the limit. Resubmit your request later.
    record ThrottlingException
        message: string
    end

    -- Input validation failed. Check your request parameters and retry the request.
    record ValidationException
        message: string
    end

    -- The number of requests exceeds the service quota. Resubmit your request later.
    record ServiceQuotaExceededException
        message: string
    end

    -- The request contains more tags than can be associated with a resource (50 tags per resource). The maximum number of tags includes both existing tags and those included in your current request.
    record TooManyTagsException
        message: string
        -- The name of the resource with too many tags.
        resourceName: string
    end

    -- Thrown when attempting to delete or modify a resource that is currently being used by other resources or operations. For example, trying to delete an Automated Reasoning policy that is referenced by an active guardrail.
    record ResourceInUseException
        message: string
    end

    -- Returned if the service cannot complete the request.
    record ServiceUnavailableException
        message: string
    end

    enum ApplicationType
        "ModelEvaluation"
        "RagEvaluation"
    end

    enum AuthorizationStatus
        "AUTHORIZED"
        "NOT_AUTHORIZED"
    end

    enum AutomatedReasoningCheckResult
        "VALID"
        "INVALID"
        "SATISFIABLE"
        "IMPOSSIBLE"
        "TRANSLATION_AMBIGUOUS"
        "TOO_COMPLEX"
        "NO_TRANSLATION"
    end

    enum AutomatedReasoningPolicyBuildDocumentContentType
        "pdf"
        "txt"
    end

    enum AutomatedReasoningPolicyBuildResultAssetType
        "BUILD_LOG"
        "QUALITY_REPORT"
        "POLICY_DEFINITION"
        "GENERATED_TEST_CASES"
        "POLICY_SCENARIOS"
        "FIDELITY_REPORT"
        "ASSET_MANIFEST"
        "SOURCE_DOCUMENT"
    end

    enum AutomatedReasoningPolicyBuildWorkflowStatus
        "SCHEDULED"
        "CANCEL_REQUESTED"
        "PREPROCESSING"
        "BUILDING"
        "TESTING"
        "COMPLETED"
        "FAILED"
        "CANCELLED"
    end

    enum AutomatedReasoningPolicyBuildWorkflowType
        "INGEST_CONTENT"
        "REFINE_POLICY"
        "IMPORT_POLICY"
        "GENERATE_FIDELITY_REPORT"
        "GENERATE_POLICY_SCENARIOS"
    end

    enum CommitmentDuration
        "OneMonth"
        "SixMonths"
    end

    enum CustomModelDeploymentStatus
        "Creating"
        "Active"
        "Failed"
    end

    enum CustomizationType
        "FINE_TUNING"
        "CONTINUED_PRE_TRAINING"
        "DISTILLATION"
        "REINFORCEMENT_FINE_TUNING"
        "IMPORTED"
    end

    enum EntitlementAvailability
        "AVAILABLE"
        "NOT_AVAILABLE"
    end

    enum EvaluationJobStatus
        "InProgress"
        "Completed"
        "Failed"
        "Stopping"
        "Stopped"
        "Deleting"
    end

    enum EvaluationJobType
        "Human"
        "Automated"
    end

    enum FineTuningJobStatus
        "InProgress"
        "Completed"
        "Failed"
        "Stopping"
        "Stopped"
    end

    enum GuardrailStatus
        "CREATING"
        "UPDATING"
        "VERSIONING"
        "READY"
        "FAILED"
        "DELETING"
    end

    enum InferenceProfileStatus
        "ACTIVE"
    end

    enum InferenceProfileType
        "SYSTEM_DEFINED"
        "APPLICATION"
    end

    enum InferenceType
        "ON_DEMAND"
        "PROVISIONED"
    end

    enum ModelCopyJobStatus
        "InProgress"
        "Completed"
        "Failed"
    end

    enum ModelCustomization
        "FINE_TUNING"
        "CONTINUED_PRE_TRAINING"
        "DISTILLATION"
    end

    enum ModelCustomizationJobStatus
        "InProgress"
        "Completed"
        "Failed"
        "Stopping"
        "Stopped"
    end

    enum ModelImportJobStatus
        "InProgress"
        "Completed"
        "Failed"
    end

    enum ModelInvocationJobStatus
        "Submitted"
        "InProgress"
        "Completed"
        "Failed"
        "Stopping"
        "Stopped"
        "PartiallyCompleted"
        "Expired"
        "Validating"
        "Scheduled"
    end

    enum ModelInvocationType
        "InvokeModel"
        "Converse"
    end

    enum ModelModality
        "TEXT"
        "IMAGE"
        "EMBEDDING"
    end

    enum ModelStatus
        "Active"
        "Creating"
        "Failed"
    end

    enum OfferType
        "ALL"
        "PUBLIC"
    end

    enum PromptRouterStatus
        "AVAILABLE"
    end

    enum PromptRouterType
        "custom"
        "default"
    end

    enum ProvisionedModelStatus
        "Creating"
        "InService"
        "Updating"
        "Failed"
    end

    enum RegionAvailability
        "AVAILABLE"
        "NOT_AVAILABLE"
    end

    enum SortByProvisionedModels
        "CreationTime"
    end

    enum SortJobsBy
        "CreationTime"
    end

    enum SortModelsBy
        "CreationTime"
    end

    enum SortOrder
        "Ascending"
        "Descending"
    end

end

return M
