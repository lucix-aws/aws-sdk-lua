-- Code generated by lua-client-codegen. DO NOT EDIT.

local record CreateLandingZoneInput
    -- The landing zone version, for example, 3.0.
    version: string
    -- Specifies the types of remediation actions to apply when creating the landing zone, such as automatic drift correction or compliance enforcement.
    remediationTypes: {string}
    -- Tags to be applied to the landing zone.
    tags: {string : string}
    -- The manifest JSON file is a text file that describes your Amazon Web Services resources. For examples, review Launch your landing zone.
    manifest: any
end

local record CreateLandingZoneOutput
    -- The ARN of the landing zone resource.
    arn: string
    -- A unique identifier assigned to a CreateLandingZone operation. You can use this identifier as an input of GetLandingZoneOperation to check the operation's status.
    operationIdentifier: string
end

local record DeleteLandingZoneInput
    -- The unique identifier of the landing zone.
    landingZoneIdentifier: string
end

local record DeleteLandingZoneOutput
    -- >A unique identifier assigned to a DeleteLandingZone operation. You can use this identifier as an input parameter of GetLandingZoneOperation to check the operation's status.
    operationIdentifier: string
end

local record DisableBaselineInput
    -- Identifier of the EnabledBaseline resource to be deactivated, in ARN format.
    enabledBaselineIdentifier: string
end

local record DisableBaselineOutput
    -- The ID (in UUID format) of the asynchronous DisableBaseline operation. This operationIdentifier is used to track status through calls to the GetBaselineOperation API.
    operationIdentifier: string
end

local record DisableControlInput
    -- The ARN of the control. Only Strongly recommended and Elective controls are permitted, with the exception of the Region deny control. For information on how to find the controlIdentifier, see the overview page.
    controlIdentifier: string
    -- The ARN of the organizational unit. For information on how to find the targetIdentifier, see the overview page.
    targetIdentifier: string
    -- The ARN of the enabled control to be disabled, which uniquely identifies the control instance on the target organizational unit.
    enabledControlIdentifier: string
end

local record DisableControlOutput
    -- The ID of the asynchronous operation, which is used to track status. The operation is available for 90 days.
    operationIdentifier: string
end

local record EnableBaselineInput
    -- The specific version to be enabled of the specified baseline.
    baselineVersion: string
    -- A list of key-value objects that specify enablement parameters, where key is a string and value is a document of any type.
    parameters: {EnabledBaselineParameter}
    -- The ARN of the baseline to be enabled.
    baselineIdentifier: string
    -- The ARN of the target on which the baseline will be enabled. Only OUs are supported as targets.
    targetIdentifier: string
    -- Tags associated with input to EnableBaseline.
    tags: {string : string}
end

local record EnableBaselineOutput
    -- The ID (in UUID format) of the asynchronous EnableBaseline operation. This operationIdentifier is used to track status through calls to the GetBaselineOperation API.
    operationIdentifier: string
    -- The ARN of the EnabledBaseline resource.
    arn: string
end

local record EnableControlInput
    -- The ARN of the control. Only Strongly recommended and Elective controls are permitted, with the exception of the Region deny control. For information on how to find the controlIdentifier, see the overview page.
    controlIdentifier: string
    -- The ARN of the organizational unit. For information on how to find the targetIdentifier, see the overview page.
    targetIdentifier: string
    -- Tags to be applied to the EnabledControl resource.
    tags: {string : string}
    -- A list of input parameter values, which are specified to configure the control when you enable it.
    parameters: {EnabledControlParameter}
end

local record EnableControlOutput
    -- The ID of the asynchronous operation, which is used to track status. The operation is available for 90 days.
    operationIdentifier: string
    -- The ARN of the EnabledControl resource.
    arn: string
end

local record GetBaselineInput
    -- The ARN of the Baseline resource to be retrieved.
    baselineIdentifier: string
end

local record GetBaselineOutput
    -- The baseline ARN.
    arn: string
    -- A user-friendly name for the baseline.
    name: string
    -- A description of the baseline.
    description: string
end

local record GetBaselineOperationInput
    -- The operation ID returned from mutating asynchronous APIs (Enable, Disable, Update, Reset).
    operationIdentifier: string
end

local record GetBaselineOperationOutput
    -- A baselineOperation object that shows information about the specified operation ID.
    baselineOperation: BaselineOperation
end

local record GetControlOperationInput
    -- The ID of the asynchronous operation, which is used to track status. The operation is available for 90 days.
    operationIdentifier: string
end

local record GetControlOperationOutput
    -- An operation performed by the control.
    controlOperation: ControlOperation
end

local record GetEnabledBaselineInput
    -- Identifier of the EnabledBaseline resource to be retrieved, in ARN format.
    enabledBaselineIdentifier: string
end

local record GetEnabledBaselineOutput
    -- Details of the EnabledBaseline resource.
    enabledBaselineDetails: EnabledBaselineDetails
end

local record GetEnabledControlInput
    -- The controlIdentifier of the enabled control.
    enabledControlIdentifier: string
end

local record GetEnabledControlOutput
    -- Information about the enabled control.
    enabledControlDetails: EnabledControlDetails
end

local record GetLandingZoneInput
    -- The unique identifier of the landing zone.
    landingZoneIdentifier: string
end

local record GetLandingZoneOutput
    -- Information about the landing zone.
    landingZone: LandingZoneDetail
end

local record GetLandingZoneOperationInput
    -- A unique identifier assigned to a landing zone operation.
    operationIdentifier: string
end

local record GetLandingZoneOperationOutput
    -- Details about a landing zone operation.
    operationDetails: LandingZoneOperationDetail
end

local record ListBaselinesInput
    -- A pagination token.
    nextToken: string
    -- The maximum number of results to be shown.
    maxResults: number
end

local record ListBaselinesOutput
    -- A list of Baseline object details.
    baselines: {BaselineSummary}
    -- A pagination token.
    nextToken: string
end

local record ListControlOperationsInput
    -- An input filter for the ListControlOperations API that lets you select the types of control operations to view.
    filter: ControlOperationFilter
    -- A pagination token.
    nextToken: string
    -- The maximum number of results to be shown.
    maxResults: number
end

local record ListControlOperationsOutput
    -- Returns a list of output from control operations.
    controlOperations: {ControlOperationSummary}
    -- A pagination token.
    nextToken: string
end

local record ListEnabledBaselinesInput
    -- A filter applied on the ListEnabledBaseline operation. Allowed filters are baselineIdentifiers and targetIdentifiers. The filter can be applied for either, or both.
    filter: EnabledBaselineFilter
    -- A pagination token.
    nextToken: string
    -- The maximum number of results to be shown.
    maxResults: number
    -- A value that can be set to include the child enabled baselines in responses. The default value is false.
    includeChildren: boolean
end

local record ListEnabledBaselinesOutput
    -- Retuens a list of summaries of EnabledBaseline resources.
    enabledBaselines: {EnabledBaselineSummary}
    -- A pagination token.
    nextToken: string
end

local record ListEnabledControlsInput
    -- The ARN of the organizational unit. For information on how to find the targetIdentifier, see the overview page.
    targetIdentifier: string
    -- The token to continue the list from a previous API call with the same parameters.
    nextToken: string
    -- How many results to return per API call.
    maxResults: number
    -- An input filter for the ListEnabledControls API that lets you select the types of control operations to view.
    filter: EnabledControlFilter
    -- A boolean value that determines whether to include enabled controls from child organizational units in the response.
    includeChildren: boolean
end

local record ListEnabledControlsOutput
    -- Lists the controls enabled by Amazon Web Services Control Tower on the specified organizational unit and the accounts it contains.
    enabledControls: {EnabledControlSummary}
    -- Retrieves the next page of results. If the string is empty, the response is the end of the results.
    nextToken: string
end

local record ListLandingZoneOperationsInput
    -- An input filter for the ListLandingZoneOperations API that lets you select the types of landing zone operations to view.
    filter: LandingZoneOperationFilter
    -- The token to continue the list from a previous API call with the same parameters.
    nextToken: string
    -- How many results to return per API call.
    maxResults: number
end

local record ListLandingZoneOperationsOutput
    -- Lists landing zone operations.
    landingZoneOperations: {LandingZoneOperationSummary}
    -- Retrieves the next page of results. If the string is empty, the response is the end of the results.
    nextToken: string
end

local record ListLandingZonesInput
    -- The token to continue the list from a previous API call with the same parameters.
    nextToken: string
    -- The maximum number of returned landing zone ARNs, which is one.
    maxResults: number
end

local record ListLandingZonesOutput
    -- The ARN of the landing zone.
    landingZones: {LandingZoneSummary}
    -- Retrieves the next page of results. If the string is empty, the response is the end of the results.
    nextToken: string
end

local record ListTagsForResourceInput
    -- The ARN of the resource.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- A list of tags, as key:value strings.
    tags: {string : string}
end

local record ResetEnabledBaselineInput
    -- Specifies the ID of the EnabledBaseline resource to be re-enabled, in ARN format.
    enabledBaselineIdentifier: string
end

local record ResetEnabledBaselineOutput
    -- The ID (in UUID format) of the asynchronous ResetEnabledBaseline operation. This operationIdentifier is used to track status through calls to the GetBaselineOperation API.
    operationIdentifier: string
end

local record ResetEnabledControlInput
    -- The ARN of the enabled control to be reset.
    enabledControlIdentifier: string
end

local record ResetEnabledControlOutput
    -- The operation identifier for this ResetEnabledControl operation.
    operationIdentifier: string
end

local record ResetLandingZoneInput
    -- The unique identifier of the landing zone.
    landingZoneIdentifier: string
end

local record ResetLandingZoneOutput
    -- A unique identifier assigned to a ResetLandingZone operation. You can use this identifier as an input parameter of GetLandingZoneOperation to check the operation's status.
    operationIdentifier: string
end

local record TagResourceInput
    -- The ARN of the resource to be tagged.
    resourceArn: string
    -- Tags to be applied to the resource.
    tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The ARN of the resource.
    resourceArn: string
    -- Tag keys to be removed from the resource.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateEnabledBaselineInput
    -- Specifies the new Baseline version, to which the EnabledBaseline should be updated.
    baselineVersion: string
    -- Parameters to apply when making an update.
    parameters: {EnabledBaselineParameter}
    -- Specifies the EnabledBaseline resource to be updated.
    enabledBaselineIdentifier: string
end

local record UpdateEnabledBaselineOutput
    -- The ID (in UUID format) of the asynchronous UpdateEnabledBaseline operation. This operationIdentifier is used to track status through calls to the GetBaselineOperation API.
    operationIdentifier: string
end

local record UpdateEnabledControlInput
    -- A key/value pair, where Key is of type String and Value is of type Document.
    parameters: {EnabledControlParameter}
    -- The ARN of the enabled control that will be updated.
    enabledControlIdentifier: string
end

local record UpdateEnabledControlOutput
    -- The operation identifier for this UpdateEnabledControl operation.
    operationIdentifier: string
end

local record UpdateLandingZoneInput
    -- The landing zone version, for example, 3.2.
    version: string
    -- Specifies the types of remediation actions to apply when updating the landing zone configuration.
    remediationTypes: {string}
    -- The unique identifier of the landing zone.
    landingZoneIdentifier: string
    -- The manifest file (JSON) is a text file that describes your Amazon Web Services resources. For an example, review Launch your landing zone. The example manifest file contains each of the available parameters. The schema for the landing zone's JSON manifest file is not published, by design.
    manifest: any
end

local record UpdateLandingZoneOutput
    -- A unique identifier assigned to a UpdateLandingZone operation. You can use this identifier as an input of GetLandingZoneOperation to check the operation's status.
    operationIdentifier: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request would cause a service quota to be exceeded. See Service quotas.
local record ServiceQuotaExceededException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request would cause a service quota to be exceeded. See Service quotas.
local record ServiceQuotaExceededException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request would cause a service quota to be exceeded. See Service quotas.
local record ServiceQuotaExceededException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request would cause a service quota to be exceeded. See Service quotas.
local record ServiceQuotaExceededException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request would cause a service quota to be exceeded. See Service quotas.
local record ServiceQuotaExceededException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request would cause a service quota to be exceeded. See Service quotas.
local record ServiceQuotaExceededException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request would cause a service quota to be exceeded. See Service quotas.
local record ServiceQuotaExceededException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request would cause a service quota to be exceeded. See Service quotas.
local record ServiceQuotaExceededException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- Updating or deleting the resource can cause an inconsistent state.
local record ConflictException
    message: string
end

-- An unexpected error occurred during processing of a request.
local record InternalServerException
    message: string
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
    -- The ID of the service that is associated with the error.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds the caller should wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
end

local record M
    CreateLandingZoneInput: CreateLandingZoneInput
    CreateLandingZoneOutput: CreateLandingZoneOutput
    DeleteLandingZoneInput: DeleteLandingZoneInput
    DeleteLandingZoneOutput: DeleteLandingZoneOutput
    DisableBaselineInput: DisableBaselineInput
    DisableBaselineOutput: DisableBaselineOutput
    DisableControlInput: DisableControlInput
    DisableControlOutput: DisableControlOutput
    EnableBaselineInput: EnableBaselineInput
    EnableBaselineOutput: EnableBaselineOutput
    EnableControlInput: EnableControlInput
    EnableControlOutput: EnableControlOutput
    GetBaselineInput: GetBaselineInput
    GetBaselineOutput: GetBaselineOutput
    GetBaselineOperationInput: GetBaselineOperationInput
    GetBaselineOperationOutput: GetBaselineOperationOutput
    GetControlOperationInput: GetControlOperationInput
    GetControlOperationOutput: GetControlOperationOutput
    GetEnabledBaselineInput: GetEnabledBaselineInput
    GetEnabledBaselineOutput: GetEnabledBaselineOutput
    GetEnabledControlInput: GetEnabledControlInput
    GetEnabledControlOutput: GetEnabledControlOutput
    GetLandingZoneInput: GetLandingZoneInput
    GetLandingZoneOutput: GetLandingZoneOutput
    GetLandingZoneOperationInput: GetLandingZoneOperationInput
    GetLandingZoneOperationOutput: GetLandingZoneOperationOutput
    ListBaselinesInput: ListBaselinesInput
    ListBaselinesOutput: ListBaselinesOutput
    ListControlOperationsInput: ListControlOperationsInput
    ListControlOperationsOutput: ListControlOperationsOutput
    ListEnabledBaselinesInput: ListEnabledBaselinesInput
    ListEnabledBaselinesOutput: ListEnabledBaselinesOutput
    ListEnabledControlsInput: ListEnabledControlsInput
    ListEnabledControlsOutput: ListEnabledControlsOutput
    ListLandingZoneOperationsInput: ListLandingZoneOperationsInput
    ListLandingZoneOperationsOutput: ListLandingZoneOperationsOutput
    ListLandingZonesInput: ListLandingZonesInput
    ListLandingZonesOutput: ListLandingZonesOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    ResetEnabledBaselineInput: ResetEnabledBaselineInput
    ResetEnabledBaselineOutput: ResetEnabledBaselineOutput
    ResetEnabledControlInput: ResetEnabledControlInput
    ResetEnabledControlOutput: ResetEnabledControlOutput
    ResetLandingZoneInput: ResetLandingZoneInput
    ResetLandingZoneOutput: ResetLandingZoneOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateEnabledBaselineInput: UpdateEnabledBaselineInput
    UpdateEnabledBaselineOutput: UpdateEnabledBaselineOutput
    UpdateEnabledControlInput: UpdateEnabledControlInput
    UpdateEnabledControlOutput: UpdateEnabledControlOutput
    UpdateLandingZoneInput: UpdateLandingZoneInput
    UpdateLandingZoneOutput: UpdateLandingZoneOutput
end

return M
