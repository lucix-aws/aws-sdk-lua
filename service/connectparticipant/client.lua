-- Code generated by lua-client-codegen. DO NOT EDIT.

local base_client = require("smithy.client")
local defaults = require("smithy.defaults")
local endpoint = require("smithy.endpoint")
local endpoint_rules = require("connectparticipant.endpoint_rules")
local restjson_protocol = require("smithy.protocol.restjson")
local schemas = require("connectparticipant.schemas")
local sdk_defaults = require("aws.sdk_defaults")
local traits = require("smithy.traits")

local M = {}

local Client = {}
Client.__index = Client

Client.invokeOperation = base_client.invokeOperation

function M.new(cfg)
    cfg = cfg or {}
    cfg.service_id = "AmazonConnectParticipantServiceLambda"
    if not cfg.protocol then
        cfg.protocol = restjson_protocol.new()
    end
    if not cfg.endpoint_provider then
        cfg.endpoint_provider = function(params)
            return endpoint.resolve(endpoint_rules, params)
        end
    end
    if not cfg.auth_scheme_resolver then
        cfg.auth_scheme_resolver = function(service, operation)
            local auth_trait = operation:trait(traits.AUTH) or service:trait(traits.AUTH)
            local options = {}
            for _, scheme in ipairs(auth_trait or {}) do
                local scheme_id = scheme.scheme_id or scheme
                if scheme_id == "aws.auth#sigv4" or scheme_id == "aws.auth#sigv4a" then
                    options[#options + 1] = { scheme_id = scheme_id, signer_properties = { signing_name = "execute-api", signing_region = cfg.region } }
                else
                    options[#options + 1] = { scheme_id = scheme_id }
                end
            end
            return options
        end
    end
    defaults.resolve_auth_schemes(cfg)
    defaults.resolve_identity_resolvers(cfg)
    defaults.resolve_http_client(cfg)
    defaults.resolve_retry_strategy(cfg)
    sdk_defaults.resolve_identity_resolver(cfg)
    local self = setmetatable(base_client.new(cfg), Client)
    return self
end

function Client:cancelParticipantAuthentication(input, options)
    return self:invokeOperation(schemas.Service, schemas.CancelParticipantAuthentication, input, options)
end

function Client:completeAttachmentUpload(input, options)
    return self:invokeOperation(schemas.Service, schemas.CompleteAttachmentUpload, input, options)
end

function Client:createParticipantConnection(input, options)
    return self:invokeOperation(schemas.Service, schemas.CreateParticipantConnection, input, options)
end

function Client:describeView(input, options)
    return self:invokeOperation(schemas.Service, schemas.DescribeView, input, options)
end

function Client:disconnectParticipant(input, options)
    return self:invokeOperation(schemas.Service, schemas.DisconnectParticipant, input, options)
end

function Client:getAttachment(input, options)
    return self:invokeOperation(schemas.Service, schemas.GetAttachment, input, options)
end

function Client:getAuthenticationUrl(input, options)
    return self:invokeOperation(schemas.Service, schemas.GetAuthenticationUrl, input, options)
end

function Client:getTranscript(input, options)
    return self:invokeOperation(schemas.Service, schemas.GetTranscript, input, options)
end

function Client:sendEvent(input, options)
    return self:invokeOperation(schemas.Service, schemas.SendEvent, input, options)
end

function Client:sendMessage(input, options)
    return self:invokeOperation(schemas.Service, schemas.SendMessage, input, options)
end

function Client:startAttachmentUpload(input, options)
    return self:invokeOperation(schemas.Service, schemas.StartAttachmentUpload, input, options)
end

return M
