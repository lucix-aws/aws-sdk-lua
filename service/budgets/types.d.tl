-- Code generated by lua-client-codegen. DO NOT EDIT.

-- Request of CreateBudget
local record CreateBudgetInput
    -- The accountId that is associated with the budget.
    AccountId: string
    -- The budget object that you want to create.
    Budget: Budget
    -- A notification that you want to associate with a budget. A budget can have up to five notifications, and each notification can have one SNS subscriber and up to 10 email subscribers. If you include notifications and subscribers in your CreateBudget call, Amazon Web Services creates the notifications and subscribers for you.
    NotificationsWithSubscribers: {NotificationWithSubscribers}
    -- An optional list of tags to associate with the specified budget. Each tag consists of a
    -- key and a value, and each key must be unique for the resource.
    ResourceTags: {ResourceTag}
end

-- Response of CreateBudget
local record CreateBudgetOutput
end

local record CreateBudgetActionInput
    AccountId: string
    BudgetName: string
    NotificationType: string
    -- The type of action. This defines the type of tasks that can be carried out by this action. This field also determines the format for definition.
    ActionType: string
    ActionThreshold: ActionThreshold
    Definition: Definition
    -- The role passed for action execution and reversion. Roles and actions must be in the same account.
    ExecutionRoleArn: string
    -- This specifies if the action needs manual or automatic approval.
    ApprovalModel: string
    Subscribers: {Subscriber}
    -- An optional list of tags to associate with the specified budget action. Each tag consists of a
    -- key and a value, and each key must be unique for the resource.
    ResourceTags: {ResourceTag}
end

local record CreateBudgetActionOutput
    AccountId: string
    BudgetName: string
    -- A system-generated universally unique identifier (UUID) for the action.
    ActionId: string
end

-- Request of CreateNotification
local record CreateNotificationInput
    -- The accountId that is associated with the budget that you want to create a notification for.
    AccountId: string
    -- The name of the budget that you want Amazon Web Services to notify you about. Budget names must be unique within an account.
    BudgetName: string
    -- The notification that you want to create.
    Notification: Notification
    -- A list of subscribers that you want to associate with the notification. Each notification can have one SNS subscriber and up to 10 email subscribers.
    Subscribers: {Subscriber}
end

-- Response of CreateNotification
local record CreateNotificationOutput
end

-- Request of CreateSubscriber
local record CreateSubscriberInput
    -- The accountId that is associated with the budget that you want to create a subscriber for.
    AccountId: string
    -- The name of the budget that you want to subscribe to. Budget names must be unique within an account.
    BudgetName: string
    -- The notification that you want to create a subscriber for.
    Notification: Notification
    -- The subscriber that you want to associate with a budget notification.
    Subscriber: Subscriber
end

-- Response of CreateSubscriber
local record CreateSubscriberOutput
end

-- Request of DeleteBudget
local record DeleteBudgetInput
    -- The accountId that is associated with the budget that you want to delete.
    AccountId: string
    -- The name of the budget that you want to delete.
    BudgetName: string
end

-- Response of DeleteBudget
local record DeleteBudgetOutput
end

local record DeleteBudgetActionInput
    AccountId: string
    BudgetName: string
    -- A system-generated universally unique identifier (UUID) for the action.
    ActionId: string
end

local record DeleteBudgetActionOutput
    AccountId: string
    BudgetName: string
    Action: Action
end

-- Request of DeleteNotification
local record DeleteNotificationInput
    -- The accountId that is associated with the budget whose notification you want to delete.
    AccountId: string
    -- The name of the budget whose notification you want to delete.
    BudgetName: string
    -- The notification that you want to delete.
    Notification: Notification
end

-- Response of DeleteNotification
local record DeleteNotificationOutput
end

-- Request of DeleteSubscriber
local record DeleteSubscriberInput
    -- The accountId that is associated with the budget whose subscriber you want to delete.
    AccountId: string
    -- The name of the budget whose subscriber you want to delete.
    BudgetName: string
    -- The notification whose subscriber you want to delete.
    Notification: Notification
    -- The subscriber that you want to delete.
    Subscriber: Subscriber
end

-- Response of DeleteSubscriber
local record DeleteSubscriberOutput
end

-- Request of DescribeBudget
local record DescribeBudgetInput
    -- The accountId that is associated with the budget that you want a description of.
    AccountId: string
    -- The name of the budget that you want a description of.
    BudgetName: string
    -- Specifies whether the response includes the filter expression associated with the
    -- budget. By showing the filter expression, you can see detailed filtering logic applied to
    -- the budget, such as Amazon Web Services services or tags that are being tracked.
    ShowFilterExpression: boolean
end

-- Response of DescribeBudget
local record DescribeBudgetOutput
    -- The description of the budget.
    Budget: Budget
end

local record DescribeBudgetActionInput
    AccountId: string
    BudgetName: string
    -- A system-generated universally unique identifier (UUID) for the action.
    ActionId: string
end

local record DescribeBudgetActionOutput
    AccountId: string
    BudgetName: string
    -- A budget action resource.
    Action: Action
end

local record DescribeBudgetActionHistoriesInput
    AccountId: string
    BudgetName: string
    -- A system-generated universally unique identifier (UUID) for the action.
    ActionId: string
    TimePeriod: TimePeriod
    MaxResults: number
    NextToken: string
end

local record DescribeBudgetActionHistoriesOutput
    -- The historical record of the budget action resource.
    ActionHistories: {ActionHistory}
    NextToken: string
end

local record DescribeBudgetActionsForAccountInput
    AccountId: string
    MaxResults: number
    NextToken: string
end

local record DescribeBudgetActionsForAccountOutput
    -- A list of the budget action resources information.
    Actions: {Action}
    NextToken: string
end

local record DescribeBudgetActionsForBudgetInput
    AccountId: string
    BudgetName: string
    MaxResults: number
    NextToken: string
end

local record DescribeBudgetActionsForBudgetOutput
    -- A list of the budget action resources information.
    Actions: {Action}
    NextToken: string
end

local record DescribeBudgetNotificationsForAccountInput
    AccountId: string
    -- An integer that represents how many budgets a paginated response contains. The
    -- default is 50.
    MaxResults: number
    NextToken: string
end

local record DescribeBudgetNotificationsForAccountOutput
    -- A list of budget names and associated notifications for an account.
    BudgetNotificationsForAccount: {BudgetNotificationsForAccount}
    NextToken: string
end

local record DescribeBudgetPerformanceHistoryInput
    AccountId: string
    BudgetName: string
    -- Retrieves how often the budget went into an ALARM state for the specified time period.
    TimePeriod: TimePeriod
    MaxResults: number
    NextToken: string
end

local record DescribeBudgetPerformanceHistoryOutput
    -- The history of how often the budget has gone into an ALARM state.
    -- For DAILY budgets, the history saves the state of the budget for the last 60 days. For MONTHLY budgets, the history saves the state of the budget for the current month plus the last 12 months. For QUARTERLY budgets, the history saves the state of the budget for the last four quarters.
    BudgetPerformanceHistory: BudgetPerformanceHistory
    NextToken: string
end

-- Request of DescribeBudgets
local record DescribeBudgetsInput
    -- The accountId that is associated with the budgets that you want to
    -- describe.
    AccountId: string
    -- An integer that represents how many budgets a paginated response contains. The default is
    -- 100.
    MaxResults: number
    -- The pagination token that you include in your request to indicate the next set of results that you want to retrieve.
    NextToken: string
    -- Specifies whether the response includes the filter expression associated with the
    -- budgets. By showing the filter expression, you can see detailed filtering logic applied to
    -- the budgets, such as Amazon Web Services services or tags that are being tracked.
    ShowFilterExpression: boolean
end

-- Response of DescribeBudgets
local record DescribeBudgetsOutput
    -- A list of budgets.
    Budgets: {Budget}
    -- The pagination token in the service response that indicates the next set of results that you can retrieve.
    NextToken: string
end

-- Request of DescribeNotificationsForBudget
local record DescribeNotificationsForBudgetInput
    -- The accountId that is associated with the budget whose notifications you want
    -- descriptions of.
    AccountId: string
    -- The name of the budget whose notifications you want descriptions of.
    BudgetName: string
    -- An optional integer that represents how many entries a paginated response contains.
    MaxResults: number
    -- The pagination token that you include in your request to indicate the next set of results that you want to retrieve.
    NextToken: string
end

-- Response of GetNotificationsForBudget
local record DescribeNotificationsForBudgetOutput
    -- A list of notifications that are associated with a budget.
    Notifications: {Notification}
    -- The pagination token in the service response that indicates the next set of results that you can retrieve.
    NextToken: string
end

-- Request of DescribeSubscribersForNotification
local record DescribeSubscribersForNotificationInput
    -- The accountId that is associated with the budget whose subscribers you want descriptions of.
    AccountId: string
    -- The name of the budget whose subscribers you want descriptions of.
    BudgetName: string
    -- The notification whose subscribers you want to list.
    Notification: Notification
    -- An optional integer that represents how many entries a paginated response contains.
    MaxResults: number
    -- The pagination token that you include in your request to indicate the next set of results that you want to retrieve.
    NextToken: string
end

-- Response of DescribeSubscribersForNotification
local record DescribeSubscribersForNotificationOutput
    -- A list of subscribers that are associated with a notification.
    Subscribers: {Subscriber}
    -- The pagination token in the service response that indicates the next set of results that you can retrieve.
    NextToken: string
end

local record ExecuteBudgetActionInput
    AccountId: string
    BudgetName: string
    -- A system-generated universally unique identifier (UUID) for the action.
    ActionId: string
    -- The type of execution.
    ExecutionType: string
end

local record ExecuteBudgetActionOutput
    AccountId: string
    BudgetName: string
    -- A system-generated universally unique identifier (UUID) for the action.
    ActionId: string
    -- The type of execution.
    ExecutionType: string
end

local record ListTagsForResourceInput
    -- The unique identifier for the resource.
    ResourceARN: string
end

local record ListTagsForResourceOutput
    -- The tags associated with the resource.
    ResourceTags: {ResourceTag}
end

local record TagResourceInput
    -- The unique identifier for the resource.
    ResourceARN: string
    -- The tags associated with the resource.
    ResourceTags: {ResourceTag}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The unique identifier for the resource.
    ResourceARN: string
    -- The key that's associated with the tag.
    ResourceTagKeys: {string}
end

local record UntagResourceOutput
end

-- Request of UpdateBudget
local record UpdateBudgetInput
    -- The accountId that is associated with the budget that you want to update.
    AccountId: string
    -- The budget that you want to update your budget to.
    NewBudget: Budget
end

-- Response of UpdateBudget
local record UpdateBudgetOutput
end

local record UpdateBudgetActionInput
    AccountId: string
    BudgetName: string
    -- A system-generated universally unique identifier (UUID) for the action.
    ActionId: string
    NotificationType: string
    ActionThreshold: ActionThreshold
    Definition: Definition
    -- The role passed for action execution and reversion. Roles and actions must be in the same account.
    ExecutionRoleArn: string
    -- This specifies if the action needs manual or automatic approval.
    ApprovalModel: string
    Subscribers: {Subscriber}
end

local record UpdateBudgetActionOutput
    AccountId: string
    BudgetName: string
    -- The previous action resource information.
    OldAction: Action
    -- The updated action resource information.
    NewAction: Action
end

-- Request of UpdateNotification
local record UpdateNotificationInput
    -- The accountId that is associated with the budget whose notification you want to update.
    AccountId: string
    -- The name of the budget whose notification you want to update.
    BudgetName: string
    -- The previous notification that is associated with a budget.
    OldNotification: Notification
    -- The updated notification to be associated with a budget.
    NewNotification: Notification
end

-- Response of UpdateNotification
local record UpdateNotificationOutput
end

-- Request of UpdateSubscriber
local record UpdateSubscriberInput
    -- The accountId that is associated with the budget whose subscriber you want to update.
    AccountId: string
    -- The name of the budget whose subscriber you want to update.
    BudgetName: string
    -- The notification whose subscriber you want to update.
    Notification: Notification
    -- The previous subscriber that is associated with a budget notification.
    OldSubscriber: Subscriber
    -- The updated subscriber that is associated with a budget notification.
    NewSubscriber: Subscriber
end

-- Response of UpdateSubscriber
local record UpdateSubscriberOutput
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- The billing view status must be HEALTHY to perform this action. Try again when the status is HEALTHY.
local record BillingViewHealthStatusException
    Message: string
end

-- You've exceeded the notification or subscriber limit.
local record CreationLimitExceededException
    Message: string
end

-- The budget name already exists. Budget names must be unique within an account.
local record DuplicateRecordException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- You've reached a Service Quota limit on this resource.
local record ServiceQuotaExceededException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- You've exceeded the notification or subscriber limit.
local record CreationLimitExceededException
    Message: string
end

-- The budget name already exists. Budget names must be unique within an account.
local record DuplicateRecordException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- You've reached a Service Quota limit on this resource.
local record ServiceQuotaExceededException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- You've exceeded the notification or subscriber limit.
local record CreationLimitExceededException
    Message: string
end

-- The budget name already exists. Budget names must be unique within an account.
local record DuplicateRecordException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- You've exceeded the notification or subscriber limit.
local record CreationLimitExceededException
    Message: string
end

-- The budget name already exists. Budget names must be unique within an account.
local record DuplicateRecordException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The request was received and recognized by the server, but the server rejected that
-- particular method for the requested resource.
local record ResourceLockedException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid.
local record InvalidNextTokenException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid.
local record InvalidNextTokenException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid.
local record InvalidNextTokenException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- The pagination token expired.
local record ExpiredNextTokenException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid.
local record InvalidNextTokenException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- The billing view status must be HEALTHY to perform this action. Try again when the status is HEALTHY.
local record BillingViewHealthStatusException
    Message: string
end

-- The pagination token expired.
local record ExpiredNextTokenException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid.
local record InvalidNextTokenException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- The pagination token expired.
local record ExpiredNextTokenException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid.
local record InvalidNextTokenException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- The pagination token expired.
local record ExpiredNextTokenException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid.
local record InvalidNextTokenException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- The pagination token expired.
local record ExpiredNextTokenException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- The pagination token is invalid.
local record InvalidNextTokenException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The request was received and recognized by the server, but the server rejected that
-- particular method for the requested resource.
local record ResourceLockedException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- You've reached a Service Quota limit on this resource.
local record ServiceQuotaExceededException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- The billing view status must be HEALTHY to perform this action. Try again when the status is HEALTHY.
local record BillingViewHealthStatusException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- You've reached a Service Quota limit on this resource.
local record ServiceQuotaExceededException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The request was received and recognized by the server, but the server rejected that
-- particular method for the requested resource.
local record ResourceLockedException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- The budget name already exists. Budget names must be unique within an account.
local record DuplicateRecordException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

-- You are not authorized to use this operation with the given parameters.
local record AccessDeniedException
    Message: string
end

-- The budget name already exists. Budget names must be unique within an account.
local record DuplicateRecordException
    Message: string
end

-- An error on the server occurred during the processing of your request. Try again later.
local record InternalErrorException
    Message: string
end

-- An error on the client occurred. Typically, the cause is an invalid input value.
local record InvalidParameterException
    Message: string
end

-- We can’t locate the resource that you specified.
local record NotFoundException
    Message: string
end

-- The number of API requests has exceeded the maximum allowed API request throttling limit
-- for the account.
local record ThrottlingException
    Message: string
end

local type ActionType = "APPLY_IAM_POLICY" | "APPLY_SCP_POLICY" | "RUN_SSM_DOCUMENTS"

local type ApprovalModel = "AUTOMATIC" | "MANUAL"

local type ExecutionType = "APPROVE_BUDGET_ACTION" | "RETRY_BUDGET_ACTION" | "REVERSE_BUDGET_ACTION" | "RESET_BUDGET_ACTION"

local type NotificationType = "ACTUAL" | "FORECASTED"

local record M
    CreateBudgetInput: CreateBudgetInput
    CreateBudgetOutput: CreateBudgetOutput
    CreateBudgetActionInput: CreateBudgetActionInput
    CreateBudgetActionOutput: CreateBudgetActionOutput
    CreateNotificationInput: CreateNotificationInput
    CreateNotificationOutput: CreateNotificationOutput
    CreateSubscriberInput: CreateSubscriberInput
    CreateSubscriberOutput: CreateSubscriberOutput
    DeleteBudgetInput: DeleteBudgetInput
    DeleteBudgetOutput: DeleteBudgetOutput
    DeleteBudgetActionInput: DeleteBudgetActionInput
    DeleteBudgetActionOutput: DeleteBudgetActionOutput
    DeleteNotificationInput: DeleteNotificationInput
    DeleteNotificationOutput: DeleteNotificationOutput
    DeleteSubscriberInput: DeleteSubscriberInput
    DeleteSubscriberOutput: DeleteSubscriberOutput
    DescribeBudgetInput: DescribeBudgetInput
    DescribeBudgetOutput: DescribeBudgetOutput
    DescribeBudgetActionInput: DescribeBudgetActionInput
    DescribeBudgetActionOutput: DescribeBudgetActionOutput
    DescribeBudgetActionHistoriesInput: DescribeBudgetActionHistoriesInput
    DescribeBudgetActionHistoriesOutput: DescribeBudgetActionHistoriesOutput
    DescribeBudgetActionsForAccountInput: DescribeBudgetActionsForAccountInput
    DescribeBudgetActionsForAccountOutput: DescribeBudgetActionsForAccountOutput
    DescribeBudgetActionsForBudgetInput: DescribeBudgetActionsForBudgetInput
    DescribeBudgetActionsForBudgetOutput: DescribeBudgetActionsForBudgetOutput
    DescribeBudgetNotificationsForAccountInput: DescribeBudgetNotificationsForAccountInput
    DescribeBudgetNotificationsForAccountOutput: DescribeBudgetNotificationsForAccountOutput
    DescribeBudgetPerformanceHistoryInput: DescribeBudgetPerformanceHistoryInput
    DescribeBudgetPerformanceHistoryOutput: DescribeBudgetPerformanceHistoryOutput
    DescribeBudgetsInput: DescribeBudgetsInput
    DescribeBudgetsOutput: DescribeBudgetsOutput
    DescribeNotificationsForBudgetInput: DescribeNotificationsForBudgetInput
    DescribeNotificationsForBudgetOutput: DescribeNotificationsForBudgetOutput
    DescribeSubscribersForNotificationInput: DescribeSubscribersForNotificationInput
    DescribeSubscribersForNotificationOutput: DescribeSubscribersForNotificationOutput
    ExecuteBudgetActionInput: ExecuteBudgetActionInput
    ExecuteBudgetActionOutput: ExecuteBudgetActionOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateBudgetInput: UpdateBudgetInput
    UpdateBudgetOutput: UpdateBudgetOutput
    UpdateBudgetActionInput: UpdateBudgetActionInput
    UpdateBudgetActionOutput: UpdateBudgetActionOutput
    UpdateNotificationInput: UpdateNotificationInput
    UpdateNotificationOutput: UpdateNotificationOutput
    UpdateSubscriberInput: UpdateSubscriberInput
    UpdateSubscriberOutput: UpdateSubscriberOutput
end

return M
