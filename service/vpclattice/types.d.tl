-- Code generated by lua-client-codegen. DO NOT EDIT.

local record BatchUpdateRuleInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The ID or ARN of the listener.
    listenerIdentifier: string
    -- The rules for the specified listener.
    rules: {RuleUpdate}
end

local record BatchUpdateRuleOutput
    -- The rules that were successfully updated.
    successful: {RuleUpdateSuccess}
    -- The rules that the operation couldn't update.
    unsuccessful: {RuleUpdateFailure}
end

local record CreateAccessLogSubscriptionInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The ID or ARN of the service network or service.
    resourceIdentifier: string
    -- The Amazon Resource Name (ARN) of the destination. The supported destination types are CloudWatch Log groups, Kinesis Data Firehose delivery streams, and Amazon S3 buckets.
    destinationArn: string
    -- The type of log that monitors your Amazon VPC Lattice service networks.
    serviceNetworkLogType: string
    -- The tags for the access log subscription.
    tags: {string : string}
end

local record CreateAccessLogSubscriptionOutput
    -- The ID of the access log subscription.
    id: string
    -- The Amazon Resource Name (ARN) of the access log subscription.
    arn: string
    -- The ID of the service network or service.
    resourceId: string
    -- The Amazon Resource Name (ARN) of the service network or service.
    resourceArn: string
    -- The type of log that monitors your Amazon VPC Lattice service networks.
    serviceNetworkLogType: string
    -- The Amazon Resource Name (ARN) of the log destination.
    destinationArn: string
end

local record CreateListenerInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The name of the listener. A listener name must be unique within a service. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
    name: string
    -- The listener protocol.
    protocol: string
    -- The listener port. You can specify a value from 1 to 65535. For HTTP, the default is 80. For HTTPS, the default is 443.
    port: number
    -- The action for the default rule. Each listener has a default rule. The default rule is used if no other rules match.
    defaultAction: RuleAction
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The tags for the listener.
    tags: {string : string}
end

local record CreateListenerOutput
    -- The Amazon Resource Name (ARN) of the listener.
    arn: string
    -- The ID of the listener.
    id: string
    -- The name of the listener.
    name: string
    -- The protocol of the listener.
    protocol: string
    -- The port number of the listener.
    port: number
    -- The Amazon Resource Name (ARN) of the service.
    serviceArn: string
    -- The ID of the service.
    serviceId: string
    -- The action for the default rule.
    defaultAction: RuleAction
end

local record CreateResourceConfigurationInput
    -- The name of the resource configuration. The name must be unique within the account. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
    name: string
    -- The type of resource configuration. A resource configuration can be one of the following types: SINGLE - A single resource. GROUP - A group of resources. You must create a group resource configuration before you create a child resource configuration. CHILD - A single resource that is part of a group resource configuration. ARN - An Amazon Web Services resource.
    type: string
    -- (SINGLE, GROUP, CHILD) The TCP port ranges that a consumer can use to access a resource configuration (for example: 1-65535). You can separate port ranges using commas (for example: 1,2,22-30).
    portRanges: {string}
    -- (SINGLE, GROUP) The protocol accepted by the resource configuration.
    protocol: string
    -- (SINGLE, GROUP, ARN) The ID or ARN of the resource gateway used to connect to the resource configuration. For a child resource configuration, this value is inherited from the parent resource configuration.
    resourceGatewayIdentifier: string
    -- (CHILD) The ID or ARN of the parent resource configuration of type GROUP. This is used to associate a child resource configuration with a group resource configuration.
    resourceConfigurationGroupIdentifier: string
    -- Identifies the resource configuration in one of the following ways: Amazon Resource Name (ARN) - Supported resource-types that are provisioned by Amazon Web Services services, such as RDS databases, can be identified by their ARN. Domain name - Any domain name that is publicly resolvable. IP address - For IPv4 and IPv6, only IP addresses in the VPC are supported.
    resourceConfigurationDefinition: ResourceConfigurationDefinition
    -- (SINGLE, GROUP, ARN) Specifies whether the resource configuration can be associated with a sharable service network. The default is false.
    allowAssociationToShareableServiceNetwork: boolean
    -- A custom domain name for your resource configuration. Additionally, provide a DomainVerificationID to prove your ownership of a domain.
    customDomainName: string
    -- (GROUP) The group domain for a group resource configuration. Any domains that you create for the child resource are subdomains of the group domain. Child resources inherit the verification status of the domain.
    groupDomain: string
    -- The domain verification ID of your verified custom domain name. If you don't provide an ID, you must configure the DNS settings yourself.
    domainVerificationIdentifier: string
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The tags for the resource configuration.
    tags: {string : string}
end

local record CreateResourceConfigurationOutput
    -- The ID of the resource configuration.
    id: string
    -- The name of the resource configuration.
    name: string
    -- The Amazon Resource Name (ARN) of the resource configuration.
    arn: string
    -- The ID of the resource gateway associated with the resource configuration.
    resourceGatewayId: string
    -- The ID of the parent resource configuration of type GROUP.
    resourceConfigurationGroupId: string
    -- The type of resource configuration. A resource configuration can be one of the following types: SINGLE - A single resource. GROUP - A group of resources. You must create a group resource configuration before you create a child resource configuration. CHILD - A single resource that is part of a group resource configuration. ARN - An Amazon Web Services resource.
    type: string
    -- The port range.
    portRanges: {string}
    -- The protocol.
    protocol: string
    -- The current status of the resource configuration.
    status: string
    -- Identifies the resource configuration in one of the following ways: Amazon Resource Name (ARN) - Supported resource-types that are provisioned by Amazon Web Services services, such as RDS databases, can be identified by their ARN. Domain name - Any domain name that is publicly resolvable. IP address - For IPv4 and IPv6, only IP addresses in the VPC are supported.
    resourceConfigurationDefinition: ResourceConfigurationDefinition
    -- Specifies whether the resource configuration can be associated with a sharable service network.
    allowAssociationToShareableServiceNetwork: boolean
    -- The date and time that the resource configuration was created, in ISO-8601 format.
    createdAt: number
    -- The reason that the request failed.
    failureReason: string
    -- The custom domain name for your resource configuration.
    customDomainName: string
    -- The domain name verification ID.
    domainVerificationId: string
    -- (GROUP) The group domain for a group resource configuration. Any domains that you create for the child resource are subdomains of the group domain. Child resources inherit the verification status of the domain.
    groupDomain: string
    -- The verification ID ARN
    domainVerificationArn: string
end

local record CreateResourceGatewayInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The name of the resource gateway.
    name: string
    -- The ID of the VPC for the resource gateway.
    vpcIdentifier: string
    -- The IDs of the VPC subnets in which to create the resource gateway.
    subnetIds: {string}
    -- The IDs of the security groups to apply to the resource gateway. The security groups must be in the same VPC.
    securityGroupIds: {string}
    -- A resource gateway can have IPv4, IPv6 or dualstack addresses. The IP address type of a resource gateway must be compatible with the subnets of the resource gateway and the IP address type of the resource, as described here: IPv4Assign IPv4 addresses to your resource gateway network interfaces. This option is supported only if all selected subnets have IPv4 address ranges, and the resource also has an IPv4 address. IPv6Assign IPv6 addresses to your resource gateway network interfaces. This option is supported only if all selected subnets are IPv6 only subnets, and the resource also has an IPv6 address. DualstackAssign both IPv4 and IPv6 addresses to your resource gateway network interfaces. This option is supported only if all selected subnets have both IPv4 and IPv6 address ranges, and the resource either has an IPv4 or IPv6 address. The IP address type of the resource gateway is independent of the IP address type of the client or the VPC endpoint through which the resource is accessed.
    ipAddressType: string
    -- The number of IPv4 addresses in each ENI for the resource gateway.
    ipv4AddressesPerEni: number
    -- Indicates how DNS is resolved for resource configurations associated to this resource gateway. ResourceConfigDnsResolution is set at creation time and cannot be changed. IN_VPC - DNS resolution occurs privately within the resource gateway's VPC. DNS queries for resources behind this resource gateway resolve using the DNS resolvers defined in the VPC's DHCP option sets. Use this when your resource domain names are hosted in private Route 53 hosted zones or on-premises DNS servers reachable from the VPC. PUBLIC - DNS resolution occurs against public DNS resolvers. DNS queries for resources behind this resource gateway resolve using standard public DNS. Use this when your resource domain names are publicly resolvable.
    resourceConfigDnsResolution: string
    -- The tags for the resource gateway.
    tags: {string : string}
end

local record CreateResourceGatewayOutput
    -- The name of the resource gateway.
    name: string
    -- The ID of the resource gateway.
    id: string
    -- The Amazon Resource Name (ARN) of the resource gateway.
    arn: string
    -- The status of the resource gateway.
    status: string
    -- The ID of the VPC.
    vpcIdentifier: string
    -- The IDs of the resource gateway subnets.
    subnetIds: {string}
    -- The IDs of the security groups for the resource gateway.
    securityGroupIds: {string}
    -- The type of IP address for the resource gateway.
    ipAddressType: string
    -- The number of IPv4 addresses in each ENI for the resource gateway.
    ipv4AddressesPerEni: number
    -- The DNS resolution type for resource configurations that are associated with this resource gateway.
    resourceConfigDnsResolution: string
end

local record CreateRuleInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The ID or ARN of the listener.
    listenerIdentifier: string
    -- The name of the rule. The name must be unique within the listener. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
    name: string
    -- The rule match.
    match: RuleMatch
    -- The priority assigned to the rule. Each rule for a specific listener must have a unique priority. The lower the priority number the higher the priority.
    priority: number
    -- The action for the default rule.
    action: RuleAction
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The tags for the rule.
    tags: {string : string}
end

local record CreateRuleOutput
    -- The Amazon Resource Name (ARN) of the rule.
    arn: string
    -- The ID of the rule.
    id: string
    -- The name of the rule.
    name: string
    -- The rule match. The RuleMatch must be an HttpMatch. This means that the rule should be an exact match on HTTP constraints which are made up of the HTTP method, path, and header.
    match: RuleMatch
    -- The priority assigned to the rule. The lower the priority number the higher the priority.
    priority: number
    -- The rule action.
    action: RuleAction
end

local record CreateServiceInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The name of the service. The name must be unique within the account. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
    name: string
    -- The tags for the service.
    tags: {string : string}
    -- The custom domain name of the service.
    customDomainName: string
    -- The Amazon Resource Name (ARN) of the certificate.
    certificateArn: string
    -- The type of IAM policy. NONE: The resource does not use an IAM policy. This is the default. AWS_IAM: The resource uses an IAM policy. When this type is used, auth is enabled and an auth policy is required.
    authType: string
end

local record CreateServiceOutput
    -- The ID of the service.
    id: string
    -- The Amazon Resource Name (ARN) of the service.
    arn: string
    -- The name of the service.
    name: string
    -- The custom domain name of the service.
    customDomainName: string
    -- The Amazon Resource Name (ARN) of the certificate.
    certificateArn: string
    -- The status. If the status is CREATE_FAILED, you must delete and recreate the service.
    status: string
    -- The type of IAM policy.
    authType: string
    -- The public DNS name of the service.
    dnsEntry: DnsEntry
end

local record CreateServiceNetworkInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The name of the service network. The name must be unique to the account. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
    name: string
    -- The type of IAM policy. NONE: The resource does not use an IAM policy. This is the default. AWS_IAM: The resource uses an IAM policy. When this type is used, auth is enabled and an auth policy is required.
    authType: string
    -- The tags for the service network.
    tags: {string : string}
    -- Specify if the service network should be enabled for sharing.
    sharingConfig: SharingConfig
end

local record CreateServiceNetworkOutput
    -- The ID of the service network.
    id: string
    -- The name of the service network.
    name: string
    -- The Amazon Resource Name (ARN) of the service network.
    arn: string
    -- Specifies if the service network is enabled for sharing.
    sharingConfig: SharingConfig
    -- The type of IAM policy.
    authType: string
end

local record CreateServiceNetworkResourceAssociationInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The ID of the resource configuration to associate with the service network.
    resourceConfigurationIdentifier: string
    -- The ID of the service network to associate with the resource configuration.
    serviceNetworkIdentifier: string
    -- Indicates if private DNS is enabled for the service network resource association.
    privateDnsEnabled: boolean
    -- A key-value pair to associate with a resource.
    tags: {string : string}
end

local record CreateServiceNetworkResourceAssociationOutput
    -- The ID of the association.
    id: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
    -- The status of the association.
    status: string
    -- The ID of the account that created the association.
    createdBy: string
    -- Indicates if private DNS is is enabled for the service network resource association.
    privateDnsEnabled: boolean
end

local record CreateServiceNetworkServiceAssociationInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The ID or ARN of the service network. You must use an ARN if the resources are in different accounts.
    serviceNetworkIdentifier: string
    -- The tags for the association.
    tags: {string : string}
end

local record CreateServiceNetworkServiceAssociationOutput
    -- The ID of the association.
    id: string
    -- The association status.
    status: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
    -- The account that created the association.
    createdBy: string
    -- The custom domain name of the service.
    customDomainName: string
    -- The DNS name of the service.
    dnsEntry: DnsEntry
end

local record CreateServiceNetworkVpcAssociationInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The ID or ARN of the service network. You must use an ARN if the resources are in different accounts.
    serviceNetworkIdentifier: string
    -- The ID of the VPC.
    vpcIdentifier: string
    -- Indicates if private DNS is enabled for the VPC association.
    privateDnsEnabled: boolean
    -- The IDs of the security groups. Security groups aren't added by default. You can add a security group to apply network level controls to control which resources in a VPC are allowed to access the service network and its services. For more information, see Control traffic to resources using security groups in the Amazon VPC User Guide.
    securityGroupIds: {string}
    -- The tags for the association.
    tags: {string : string}
    -- DNS options for the service network VPC association.
    dnsOptions: DnsOptions
end

local record CreateServiceNetworkVpcAssociationOutput
    -- The ID of the association.
    id: string
    -- The association status.
    status: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
    -- The account that created the association.
    createdBy: string
    -- The IDs of the security groups.
    securityGroupIds: {string}
    -- Indicates if private DNS is enabled for the VPC association.
    privateDnsEnabled: boolean
    dnsOptions: DnsOptions
end

local record CreateTargetGroupInput
    -- The name of the target group. The name must be unique within the account. The valid characters are a-z, 0-9, and hyphens (-). You can't use a hyphen as the first or last character, or immediately after another hyphen.
    name: string
    -- The type of target group.
    type: string
    -- The target group configuration.
    config: TargetGroupConfig
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The tags for the target group.
    tags: {string : string}
end

local record CreateTargetGroupOutput
    -- The ID of the target group.
    id: string
    -- The Amazon Resource Name (ARN) of the target group.
    arn: string
    -- The name of the target group.
    name: string
    -- The type of target group.
    type: string
    -- The target group configuration.
    config: TargetGroupConfig
    -- The status. You can retry the operation if the status is CREATE_FAILED. However, if you retry it while the status is CREATE_IN_PROGRESS, there is no change in the status.
    status: string
end

local record DeleteAccessLogSubscriptionInput
    -- The ID or ARN of the access log subscription.
    accessLogSubscriptionIdentifier: string
end

local record DeleteAccessLogSubscriptionOutput
end

local record DeleteAuthPolicyInput
    -- The ID or ARN of the resource.
    resourceIdentifier: string
end

local record DeleteAuthPolicyOutput
end

local record DeleteDomainVerificationInput
    -- The ID of the domain verification to delete.
    domainVerificationIdentifier: string
end

local record DeleteDomainVerificationOutput
end

local record DeleteListenerInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The ID or ARN of the listener.
    listenerIdentifier: string
end

local record DeleteListenerOutput
end

local record DeleteResourceConfigurationInput
    -- The ID or ARN of the resource configuration.
    resourceConfigurationIdentifier: string
end

local record DeleteResourceConfigurationOutput
end

local record DeleteResourceEndpointAssociationInput
    -- The ID or ARN of the association.
    resourceEndpointAssociationIdentifier: string
end

local record DeleteResourceEndpointAssociationOutput
    -- The ID of the association.
    id: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
    -- The ID of the resource configuration.
    resourceConfigurationId: string
    -- The Amazon Resource Name (ARN) of the resource configuration associated with the VPC endpoint of type resource.
    resourceConfigurationArn: string
    -- The ID of the resource VPC endpoint that is associated with the resource configuration.
    vpcEndpointId: string
end

local record DeleteResourceGatewayInput
    -- The ID or ARN of the resource gateway.
    resourceGatewayIdentifier: string
end

local record DeleteResourceGatewayOutput
    -- The ID of the resource gateway.
    id: string
    -- The Amazon Resource Name (ARN) of the resource gateway.
    arn: string
    -- The name of the resource gateway.
    name: string
    -- The status of the resource gateway.
    status: string
end

local record DeleteResourcePolicyInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
end

local record DeleteResourcePolicyOutput
end

local record DeleteRuleInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The ID or ARN of the listener.
    listenerIdentifier: string
    -- The ID or ARN of the rule.
    ruleIdentifier: string
end

local record DeleteRuleOutput
end

local record DeleteServiceInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
end

local record DeleteServiceOutput
    -- The ID of the service.
    id: string
    -- The Amazon Resource Name (ARN) of the service.
    arn: string
    -- The name of the service.
    name: string
    -- The status. You can retry the operation if the status is DELETE_FAILED. However, if you retry it while the status is DELETE_IN_PROGRESS, the status doesn't change.
    status: string
end

local record DeleteServiceNetworkInput
    -- The ID or ARN of the service network.
    serviceNetworkIdentifier: string
end

local record DeleteServiceNetworkOutput
end

local record DeleteServiceNetworkResourceAssociationInput
    -- The ID of the association.
    serviceNetworkResourceAssociationIdentifier: string
end

local record DeleteServiceNetworkResourceAssociationOutput
    -- The ID of the association.
    id: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
    -- The status of the association.
    status: string
end

local record DeleteServiceNetworkServiceAssociationInput
    -- The ID or ARN of the association.
    serviceNetworkServiceAssociationIdentifier: string
end

local record DeleteServiceNetworkServiceAssociationOutput
    -- The ID of the association.
    id: string
    -- The status. You can retry the operation if the status is DELETE_FAILED. However, if you retry it when the status is DELETE_IN_PROGRESS, there is no change in the status.
    status: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
end

local record DeleteServiceNetworkVpcAssociationInput
    -- The ID or ARN of the association.
    serviceNetworkVpcAssociationIdentifier: string
end

local record DeleteServiceNetworkVpcAssociationOutput
    -- The ID of the association.
    id: string
    -- The status. You can retry the operation if the status is DELETE_FAILED. However, if you retry it while the status is DELETE_IN_PROGRESS, there is no change in the status.
    status: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
end

local record DeleteTargetGroupInput
    -- The ID or ARN of the target group.
    targetGroupIdentifier: string
end

local record DeleteTargetGroupOutput
    -- The ID of the target group.
    id: string
    -- The Amazon Resource Name (ARN) of the target group.
    arn: string
    -- The status. You can retry the operation if the status is DELETE_FAILED. However, if you retry it while the status is DELETE_IN_PROGRESS, the status doesn't change.
    status: string
end

local record DeregisterTargetsInput
    -- The ID or ARN of the target group.
    targetGroupIdentifier: string
    -- The targets to deregister.
    targets: {Target}
end

local record DeregisterTargetsOutput
    -- The targets that were successfully deregistered.
    successful: {Target}
    -- The targets that the operation couldn't deregister.
    unsuccessful: {TargetFailure}
end

local record GetAccessLogSubscriptionInput
    -- The ID or ARN of the access log subscription.
    accessLogSubscriptionIdentifier: string
end

local record GetAccessLogSubscriptionOutput
    -- The ID of the access log subscription.
    id: string
    -- The Amazon Resource Name (ARN) of the access log subscription.
    arn: string
    -- The ID of the service network or service.
    resourceId: string
    -- The Amazon Resource Name (ARN) of the service network or service.
    resourceArn: string
    -- The Amazon Resource Name (ARN) of the access log destination.
    destinationArn: string
    -- The log type for the service network.
    serviceNetworkLogType: string
    -- The date and time that the access log subscription was created, in ISO-8601 format.
    createdAt: number
    -- The date and time that the access log subscription was last updated, in ISO-8601 format.
    lastUpdatedAt: number
end

local record GetAuthPolicyInput
    -- The ID or ARN of the service network or service.
    resourceIdentifier: string
end

local record GetAuthPolicyOutput
    -- The auth policy.
    policy: string
    -- The state of the auth policy. The auth policy is only active when the auth type is set to AWS_IAM. If you provide a policy, then authentication and authorization decisions are made based on this policy and the client's IAM policy. If the auth type is NONE, then any auth policy that you provide remains inactive. For more information, see Create a service network in the Amazon VPC Lattice User Guide.
    state: string
    -- The date and time that the auth policy was created, in ISO-8601 format.
    createdAt: number
    -- The date and time that the auth policy was last updated, in ISO-8601 format.
    lastUpdatedAt: number
end

local record GetDomainVerificationInput
    -- The ID or ARN of the domain verification to retrieve.
    domainVerificationIdentifier: string
end

local record GetDomainVerificationOutput
    -- The ID of the domain verification.
    id: string
    -- The Amazon Resource Name (ARN) of the domain verification.
    arn: string
    -- The domain name being verified.
    domainName: string
    -- The current status of the domain verification process.
    status: string
    -- The TXT record configuration used for domain verification.
    txtMethodConfig: TxtMethodConfig
    -- The date and time that the domain verification was created, in ISO-8601 format.
    createdAt: number
    -- The date and time that the domain was last successfully verified, in ISO-8601 format.
    lastVerifiedTime: number
    -- The tags associated with the domain verification.
    tags: {string : string}
end

local record GetListenerInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The ID or ARN of the listener.
    listenerIdentifier: string
end

local record GetListenerOutput
    -- The Amazon Resource Name (ARN) of the listener.
    arn: string
    -- The ID of the listener.
    id: string
    -- The name of the listener.
    name: string
    -- The listener protocol.
    protocol: string
    -- The listener port.
    port: number
    -- The Amazon Resource Name (ARN) of the service.
    serviceArn: string
    -- The ID of the service.
    serviceId: string
    -- The actions for the default listener rule.
    defaultAction: RuleAction
    -- The date and time that the listener was created, in ISO-8601 format.
    createdAt: number
    -- The date and time that the listener was last updated, in ISO-8601 format.
    lastUpdatedAt: number
end

local record GetResourceConfigurationInput
    -- The ID of the resource configuration.
    resourceConfigurationIdentifier: string
end

local record GetResourceConfigurationOutput
    -- The ID of the resource configuration.
    id: string
    -- The name of the resource configuration.
    name: string
    -- The Amazon Resource Name (ARN) of the resource configuration.
    arn: string
    -- The ID of the resource gateway used to connect to the resource configuration in a given VPC. You can specify the resource gateway identifier only for resource configurations with type SINGLE, GROUP, or ARN.
    resourceGatewayId: string
    -- The ID of the group resource configuration.
    resourceConfigurationGroupId: string
    -- The type of resource configuration. SINGLE - A single resource. GROUP - A group of resources. CHILD - A single resource that is part of a group resource configuration. ARN - An Amazon Web Services resource.
    type: string
    -- Specifies whether the resource configuration is associated with a sharable service network.
    allowAssociationToShareableServiceNetwork: boolean
    -- The TCP port ranges that a consumer can use to access a resource configuration. You can separate port ranges with a comma. Example: 1-65535 or 1,2,22-30
    portRanges: {string}
    -- The TCP protocol accepted by the specified resource configuration.
    protocol: string
    -- The custom domain name of the resource configuration.
    customDomainName: string
    -- The status of the resource configuration.
    status: string
    -- The resource configuration.
    resourceConfigurationDefinition: ResourceConfigurationDefinition
    -- The date and time that the resource configuration was created, in ISO-8601 format.
    createdAt: number
    -- Indicates whether the resource configuration was created and is managed by Amazon.
    amazonManaged: boolean
    -- The reason the create-resource-configuration request failed.
    failureReason: string
    -- The most recent date and time that the resource configuration was updated, in ISO-8601 format.
    lastUpdatedAt: number
    -- The domain verification ID.
    domainVerificationId: string
    -- The ARN of the domain verification.
    domainVerificationArn: string
    -- The domain verification status.
    domainVerificationStatus: string
    -- (GROUP) The group domain for a group resource configuration. Any domains that you create for the child resource are subdomains of the group domain. Child resources inherit the verification status of the domain.
    groupDomain: string
end

local record GetResourceGatewayInput
    -- The ID of the resource gateway.
    resourceGatewayIdentifier: string
end

local record GetResourceGatewayOutput
    -- The name of the resource gateway.
    name: string
    -- The ID of the resource gateway.
    id: string
    -- The Amazon Resource Name (ARN) of the resource gateway.
    arn: string
    -- The status for the resource gateway.
    status: string
    -- The ID of the VPC for the resource gateway.
    vpcId: string
    -- The IDs of the VPC subnets for resource gateway.
    subnetIds: {string}
    -- Indicates whether the resource gateway is managed by an AWS service.
    serviceManaged: boolean
    -- The AWS service that manages the resource gateway.
    managedBy: string
    -- The security group IDs associated with the resource gateway.
    securityGroupIds: {string}
    -- The type of IP address for the resource gateway.
    ipAddressType: string
    -- The number of IPv4 addresses in each ENI for the resource gateway.
    ipv4AddressesPerEni: number
    -- The DNS resolution type for resource configurations that are associated with this resource gateway.
    resourceConfigDnsResolution: string
    -- The date and time that the resource gateway was created, in ISO-8601 format.
    createdAt: number
    -- The date and time that the resource gateway was last updated, in ISO-8601 format.
    lastUpdatedAt: number
end

local record GetResourcePolicyInput
    -- The Amazon Resource Name (ARN) of the service network or service.
    resourceArn: string
end

local record GetResourcePolicyOutput
    -- An IAM policy.
    policy: string
end

local record GetRuleInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The ID or ARN of the listener.
    listenerIdentifier: string
    -- The ID or ARN of the listener rule.
    ruleIdentifier: string
end

local record GetRuleOutput
    -- The Amazon Resource Name (ARN) of the listener.
    arn: string
    -- The ID of the listener.
    id: string
    -- The name of the listener.
    name: string
    -- Indicates whether this is the default rule.
    isDefault: boolean
    -- The rule match.
    match: RuleMatch
    -- The priority level for the specified rule.
    priority: number
    -- The action for the default rule.
    action: RuleAction
    -- The date and time that the listener rule was created, in ISO-8601 format.
    createdAt: number
    -- The date and time that the listener rule was last updated, in ISO-8601 format.
    lastUpdatedAt: number
end

local record GetServiceInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
end

local record GetServiceOutput
    -- The ID of the service.
    id: string
    -- The name of the service.
    name: string
    -- The Amazon Resource Name (ARN) of the service.
    arn: string
    -- The date and time that the service was created, in ISO-8601 format.
    createdAt: number
    -- The date and time that the service was last updated, in ISO-8601 format.
    lastUpdatedAt: number
    -- The DNS name of the service.
    dnsEntry: DnsEntry
    -- The custom domain name of the service.
    customDomainName: string
    -- The Amazon Resource Name (ARN) of the certificate.
    certificateArn: string
    -- The status of the service.
    status: string
    -- The type of IAM policy.
    authType: string
    -- The failure code.
    failureCode: string
    -- The failure message.
    failureMessage: string
end

local record GetServiceNetworkInput
    -- The ID or ARN of the service network.
    serviceNetworkIdentifier: string
end

local record GetServiceNetworkOutput
    -- The ID of the service network.
    id: string
    -- The name of the service network.
    name: string
    -- The date and time that the service network was created, in ISO-8601 format.
    createdAt: number
    -- The date and time of the last update, in ISO-8601 format.
    lastUpdatedAt: number
    -- The Amazon Resource Name (ARN) of the service network.
    arn: string
    -- The type of IAM policy.
    authType: string
    -- Specifies if the service network is enabled for sharing.
    sharingConfig: SharingConfig
    -- The number of VPCs associated with the service network.
    numberOfAssociatedVPCs: number
    -- The number of services associated with the service network.
    numberOfAssociatedServices: number
end

local record GetServiceNetworkResourceAssociationInput
    -- The ID of the association.
    serviceNetworkResourceAssociationIdentifier: string
end

local record GetServiceNetworkResourceAssociationOutput
    -- The ID of the association.
    id: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
    -- The status of the association.
    status: string
    -- The account that created the association.
    createdBy: string
    -- The date and time that the association was created, in ISO-8601 format.
    createdAt: number
    -- The ID of the resource configuration that is associated with the service network.
    resourceConfigurationId: string
    -- The Amazon Resource Name (ARN) of the association.
    resourceConfigurationArn: string
    -- The name of the resource configuration that is associated with the service network.
    resourceConfigurationName: string
    -- The ID of the service network that is associated with the resource configuration.
    serviceNetworkId: string
    -- The Amazon Resource Name (ARN) of the service network that is associated with the resource configuration.
    serviceNetworkArn: string
    -- The name of the service network that is associated with the resource configuration.
    serviceNetworkName: string
    -- The reason the association request failed.
    failureReason: string
    -- The failure code.
    failureCode: string
    -- The most recent date and time that the association was updated, in ISO-8601 format.
    lastUpdatedAt: number
    -- The private DNS entry for the service.
    privateDnsEntry: DnsEntry
    -- Indicates if private DNS is enabled in the service network resource association.
    privateDnsEnabled: boolean
    -- The DNS entry for the service.
    dnsEntry: DnsEntry
    -- Indicates whether the association is managed by Amazon.
    isManagedAssociation: boolean
    -- The domain verification status in the service network resource association.
    domainVerificationStatus: string
end

local record GetServiceNetworkServiceAssociationInput
    -- The ID or ARN of the association.
    serviceNetworkServiceAssociationIdentifier: string
end

local record GetServiceNetworkServiceAssociationOutput
    -- The ID of the service network and service association.
    id: string
    -- The status of the association.
    status: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
    -- The account that created the association.
    createdBy: string
    -- The date and time that the association was created, in ISO-8601 format.
    createdAt: number
    -- The ID of the service.
    serviceId: string
    -- The name of the service.
    serviceName: string
    -- The Amazon Resource Name (ARN) of the service.
    serviceArn: string
    -- The ID of the service network.
    serviceNetworkId: string
    -- The name of the service network.
    serviceNetworkName: string
    -- The Amazon Resource Name (ARN) of the service network.
    serviceNetworkArn: string
    -- The DNS name of the service.
    dnsEntry: DnsEntry
    -- The custom domain name of the service.
    customDomainName: string
    -- The failure message.
    failureMessage: string
    -- The failure code.
    failureCode: string
end

local record GetServiceNetworkVpcAssociationInput
    -- The ID or ARN of the association.
    serviceNetworkVpcAssociationIdentifier: string
end

local record GetServiceNetworkVpcAssociationOutput
    -- The ID of the association.
    id: string
    -- The status of the association.
    status: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
    -- The account that created the association.
    createdBy: string
    -- The date and time that the association was created, in ISO-8601 format.
    createdAt: number
    -- The ID of the service network.
    serviceNetworkId: string
    -- The name of the service network.
    serviceNetworkName: string
    -- The Amazon Resource Name (ARN) of the service network.
    serviceNetworkArn: string
    -- The ID of the VPC.
    vpcId: string
    -- The IDs of the security groups.
    securityGroupIds: {string}
    -- Indicates if private DNS is enabled in the VPC association.
    privateDnsEnabled: boolean
    -- The failure message.
    failureMessage: string
    -- The failure code.
    failureCode: string
    -- The date and time that the association was last updated, in ISO-8601 format.
    lastUpdatedAt: number
    -- DNS options for the service network VPC association.
    dnsOptions: DnsOptions
end

local record GetTargetGroupInput
    -- The ID or ARN of the target group.
    targetGroupIdentifier: string
end

local record GetTargetGroupOutput
    -- The ID of the target group.
    id: string
    -- The Amazon Resource Name (ARN) of the target group.
    arn: string
    -- The name of the target group.
    name: string
    -- The target group type.
    type: string
    -- The target group configuration.
    config: TargetGroupConfig
    -- The date and time that the target group was created, in ISO-8601 format.
    createdAt: number
    -- The date and time that the target group was last updated, in ISO-8601 format.
    lastUpdatedAt: number
    -- The status.
    status: string
    -- The Amazon Resource Names (ARNs) of the service.
    serviceArns: {string}
    -- The failure message.
    failureMessage: string
    -- The failure code.
    failureCode: string
end

local record ListAccessLogSubscriptionsInput
    -- The ID or ARN of the service network or service.
    resourceIdentifier: string
    -- The maximum number of results to return.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListAccessLogSubscriptionsOutput
    -- Information about the access log subscriptions.
    items: {AccessLogSubscriptionSummary}
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListDomainVerificationsInput
    -- The maximum number of results to return.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListDomainVerificationsOutput
    -- Information about the domain verifications.
    items: {DomainVerificationSummary}
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListListenersInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The maximum number of results to return.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListListenersOutput
    -- Information about the listeners.
    items: {ListenerSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListResourceConfigurationsInput
    -- The ID of the resource gateway for the resource configuration.
    resourceGatewayIdentifier: string
    -- The ID of the resource configuration of type Group.
    resourceConfigurationGroupIdentifier: string
    -- The domain verification ID.
    domainVerificationIdentifier: string
    -- The maximum page size.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListResourceConfigurationsOutput
    -- Information about the resource configurations.
    items: {ResourceConfigurationSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListResourceEndpointAssociationsInput
    -- The ID for the resource configuration associated with the VPC endpoint.
    resourceConfigurationIdentifier: string
    -- The ID of the association.
    resourceEndpointAssociationIdentifier: string
    -- The ID of the VPC endpoint in the association.
    vpcEndpointId: string
    -- The owner of the VPC endpoint in the association.
    vpcEndpointOwner: string
    -- The maximum page size.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListResourceEndpointAssociationsOutput
    -- Information about the VPC endpoint associations.
    items: {ResourceEndpointAssociationSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListResourceGatewaysInput
    -- The maximum page size.
    maxResults: number
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListResourceGatewaysOutput
    -- Information about the resource gateways.
    items: {ResourceGatewaySummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListRulesInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The ID or ARN of the listener.
    listenerIdentifier: string
    -- The maximum number of results to return.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListRulesOutput
    -- Information about the rules.
    items: {RuleSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListServiceNetworkResourceAssociationsInput
    -- The ID of the service network.
    serviceNetworkIdentifier: string
    -- The ID of the resource configuration.
    resourceConfigurationIdentifier: string
    -- The maximum page size.
    maxResults: number
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
    -- Include service network resource associations of the child resource configuration with the grouped resource configuration. The type is boolean and the default value is false.
    includeChildren: boolean
end

local record ListServiceNetworkResourceAssociationsOutput
    -- Information about the associations.
    items: {ServiceNetworkResourceAssociationSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListServiceNetworksInput
    -- The maximum number of results to return.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListServiceNetworksOutput
    -- Information about the service networks.
    items: {ServiceNetworkSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListServiceNetworkServiceAssociationsInput
    -- The ID or ARN of the service network.
    serviceNetworkIdentifier: string
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The maximum number of results to return.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListServiceNetworkServiceAssociationsOutput
    -- Information about the associations.
    items: {ServiceNetworkServiceAssociationSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListServiceNetworkVpcAssociationsInput
    -- The ID or ARN of the service network.
    serviceNetworkIdentifier: string
    -- The ID or ARN of the VPC.
    vpcIdentifier: string
    -- The maximum number of results to return.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListServiceNetworkVpcAssociationsOutput
    -- Information about the associations.
    items: {ServiceNetworkVpcAssociationSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListServiceNetworkVpcEndpointAssociationsInput
    -- The ID of the service network associated with the VPC endpoint.
    serviceNetworkIdentifier: string
    -- The maximum page size.
    maxResults: number
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListServiceNetworkVpcEndpointAssociationsOutput
    -- Information about the association between the VPC endpoint and service network.
    items: {ServiceNetworkEndpointAssociation}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListServicesInput
    -- The maximum number of results to return.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
end

local record ListServicesOutput
    -- Information about the services.
    items: {ServiceSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- Information about the tags.
    tags: {string : string}
end

local record ListTargetGroupsInput
    -- The maximum number of results to return.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
    -- The ID or ARN of the VPC.
    vpcIdentifier: string
    -- The target group type.
    targetGroupType: string
end

local record ListTargetGroupsOutput
    -- Information about the target groups.
    items: {TargetGroupSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record ListTargetsInput
    -- The ID or ARN of the target group.
    targetGroupIdentifier: string
    -- The maximum number of results to return.
    maxResults: number
    -- A pagination token for the next page of results.
    nextToken: string
    -- The targets.
    targets: {Target}
end

local record ListTargetsOutput
    -- Information about the targets.
    items: {TargetSummary}
    -- If there are additional results, a pagination token for the next page of results.
    nextToken: string
end

local record PutAuthPolicyInput
    -- The ID or ARN of the service network or service for which the policy is created.
    resourceIdentifier: string
    -- The auth policy. The policy string in JSON must not contain newlines or blank lines.
    policy: string
end

local record PutAuthPolicyOutput
    -- The auth policy. The policy string in JSON must not contain newlines or blank lines.
    policy: string
    -- The state of the auth policy. The auth policy is only active when the auth type is set to AWS_IAM. If you provide a policy, then authentication and authorization decisions are made based on this policy and the client's IAM policy. If the Auth type is NONE, then, any auth policy that you provide remains inactive. For more information, see Create a service network in the Amazon VPC Lattice User Guide.
    state: string
end

local record PutResourcePolicyInput
    -- The ID or ARN of the service network or service for which the policy is created.
    resourceArn: string
    -- An IAM policy. The policy string in JSON must not contain newlines or blank lines.
    policy: string
end

local record PutResourcePolicyOutput
end

local record RegisterTargetsInput
    -- The ID or ARN of the target group.
    targetGroupIdentifier: string
    -- The targets.
    targets: {Target}
end

local record RegisterTargetsOutput
    -- The targets that were successfully registered.
    successful: {Target}
    -- The targets that were not registered.
    unsuccessful: {TargetFailure}
end

local record StartDomainVerificationInput
    -- A unique, case-sensitive identifier that you provide to ensure the idempotency of the request. If you retry a request that completed successfully using the same client token and parameters, the retry succeeds without performing any actions. If the parameters aren't identical, the retry fails.
    clientToken: string
    -- The domain name to verify ownership for.
    domainName: string
    -- The tags for the domain verification.
    tags: {string : string}
end

local record StartDomainVerificationOutput
    -- The ID of the domain verification.
    id: string
    -- The Amazon Resource Name (ARN) of the domain verification.
    arn: string
    -- The domain name being verified.
    domainName: string
    -- The current status of the domain verification process.
    status: string
    -- The TXT record configuration used for domain verification.
    txtMethodConfig: TxtMethodConfig
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
    -- The tags for the resource.
    tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
    -- The tag keys of the tags to remove.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateAccessLogSubscriptionInput
    -- The ID or ARN of the access log subscription.
    accessLogSubscriptionIdentifier: string
    -- The Amazon Resource Name (ARN) of the access log destination.
    destinationArn: string
end

local record UpdateAccessLogSubscriptionOutput
    -- The ID of the access log subscription.
    id: string
    -- The Amazon Resource Name (ARN) of the access log subscription.
    arn: string
    -- The ID of the resource.
    resourceId: string
    -- The Amazon Resource Name (ARN) of the access log subscription.
    resourceArn: string
    -- The Amazon Resource Name (ARN) of the access log destination.
    destinationArn: string
end

local record UpdateListenerInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The ID or ARN of the listener.
    listenerIdentifier: string
    -- The action for the default rule.
    defaultAction: RuleAction
end

local record UpdateListenerOutput
    -- The Amazon Resource Name (ARN) of the listener.
    arn: string
    -- The ID of the listener.
    id: string
    -- The name of the listener.
    name: string
    -- The protocol of the listener.
    protocol: string
    -- The listener port.
    port: number
    -- The Amazon Resource Name (ARN) of the service.
    serviceArn: string
    -- The ID of the service.
    serviceId: string
    -- The action for the default rule.
    defaultAction: RuleAction
end

local record UpdateResourceConfigurationInput
    -- The ID of the resource configuration.
    resourceConfigurationIdentifier: string
    -- Identifies the resource configuration in one of the following ways: Amazon Resource Name (ARN) - Supported resource-types that are provisioned by Amazon Web Services services, such as RDS databases, can be identified by their ARN. Domain name - Any domain name that is publicly resolvable. IP address - For IPv4 and IPv6, only IP addresses in the VPC are supported.
    resourceConfigurationDefinition: ResourceConfigurationDefinition
    -- Indicates whether to add the resource configuration to service networks that are shared with other accounts.
    allowAssociationToShareableServiceNetwork: boolean
    -- The TCP port ranges that a consumer can use to access a resource configuration. You can separate port ranges with a comma. Example: 1-65535 or 1,2,22-30
    portRanges: {string}
end

local record UpdateResourceConfigurationOutput
    -- The ID of the resource configuration.
    id: string
    -- The name of the resource configuration.
    name: string
    -- The Amazon Resource Name (ARN) of the resource configuration.
    arn: string
    -- The ID of the resource gateway associated with the resource configuration.
    resourceGatewayId: string
    -- The ID of the group resource configuration.
    resourceConfigurationGroupId: string
    -- The type of resource configuration. SINGLE - A single resource. GROUP - A group of resources. CHILD - A single resource that is part of a group resource configuration. ARN - An Amazon Web Services resource.
    type: string
    -- The TCP port ranges that a consumer can use to access a resource configuration. You can separate port ranges with a comma. Example: 1-65535 or 1,2,22-30
    portRanges: {string}
    -- Indicates whether to add the resource configuration to service networks that are shared with other accounts.
    allowAssociationToShareableServiceNetwork: boolean
    -- The TCP protocol accepted by the specified resource configuration.
    protocol: string
    -- The status of the resource configuration.
    status: string
    -- The resource configuration.
    resourceConfigurationDefinition: ResourceConfigurationDefinition
end

local record UpdateResourceGatewayInput
    -- The ID or ARN of the resource gateway.
    resourceGatewayIdentifier: string
    -- The IDs of the security groups associated with the resource gateway.
    securityGroupIds: {string}
end

local record UpdateResourceGatewayOutput
    -- The name of the resource gateway.
    name: string
    -- The ID of the resource gateway.
    id: string
    -- The Amazon Resource Name (ARN) of the resource gateway.
    arn: string
    -- The status of the resource gateway.
    status: string
    -- The ID of the VPC for the resource gateway.
    vpcId: string
    -- The IDs of the VPC subnets for the resource gateway.
    subnetIds: {string}
    -- The IDs of the security groups associated with the resource gateway.
    securityGroupIds: {string}
    -- The type of IP address used by the resource gateway.
    ipAddressType: string
end

local record UpdateRuleInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The ID or ARN of the listener.
    listenerIdentifier: string
    -- The ID or ARN of the rule.
    ruleIdentifier: string
    -- The rule match.
    match: RuleMatch
    -- The rule priority. A listener can't have multiple rules with the same priority.
    priority: number
    -- Information about the action for the specified listener rule.
    action: RuleAction
end

local record UpdateRuleOutput
    -- The Amazon Resource Name (ARN) of the listener.
    arn: string
    -- The ID of the listener.
    id: string
    -- The name of the listener.
    name: string
    -- Indicates whether this is the default rule.
    isDefault: boolean
    -- The rule match.
    match: RuleMatch
    -- The rule priority.
    priority: number
    -- Information about the action for the specified listener rule.
    action: RuleAction
end

local record UpdateServiceInput
    -- The ID or ARN of the service.
    serviceIdentifier: string
    -- The Amazon Resource Name (ARN) of the certificate.
    certificateArn: string
    -- The type of IAM policy. NONE: The resource does not use an IAM policy. This is the default. AWS_IAM: The resource uses an IAM policy. When this type is used, auth is enabled and an auth policy is required.
    authType: string
end

local record UpdateServiceOutput
    -- The ID of the service.
    id: string
    -- The Amazon Resource Name (ARN) of the service.
    arn: string
    -- The name of the service.
    name: string
    -- The custom domain name of the service.
    customDomainName: string
    -- The Amazon Resource Name (ARN) of the certificate.
    certificateArn: string
    -- The type of IAM policy.
    authType: string
end

local record UpdateServiceNetworkInput
    -- The ID or ARN of the service network.
    serviceNetworkIdentifier: string
    -- The type of IAM policy. NONE: The resource does not use an IAM policy. This is the default. AWS_IAM: The resource uses an IAM policy. When this type is used, auth is enabled and an auth policy is required.
    authType: string
end

local record UpdateServiceNetworkOutput
    -- The ID of the service network.
    id: string
    -- The name of the service network.
    name: string
    -- The Amazon Resource Name (ARN) of the service network.
    arn: string
    -- The type of IAM policy.
    authType: string
end

local record UpdateServiceNetworkVpcAssociationInput
    -- The ID or ARN of the association.
    serviceNetworkVpcAssociationIdentifier: string
    -- The IDs of the security groups.
    securityGroupIds: {string}
end

local record UpdateServiceNetworkVpcAssociationOutput
    -- The ID of the association.
    id: string
    -- The Amazon Resource Name (ARN) of the association.
    arn: string
    -- The status. You can retry the operation if the status is DELETE_FAILED. However, if you retry it while the status is DELETE_IN_PROGRESS, there is no change in the status.
    status: string
    -- The account that created the association.
    createdBy: string
    -- The IDs of the security groups.
    securityGroupIds: {string}
end

local record UpdateTargetGroupInput
    -- The ID or ARN of the target group.
    targetGroupIdentifier: string
    -- The health check configuration.
    healthCheck: HealthCheckConfig
end

local record UpdateTargetGroupOutput
    -- The ID of the target group.
    id: string
    -- The Amazon Resource Name (ARN) of the target group.
    arn: string
    -- The name of the target group.
    name: string
    -- The target group type.
    type: string
    -- The target group configuration.
    config: TargetGroupConfig
    -- The status.
    status: string
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

-- The user does not have sufficient access to perform this action.
local record AccessDeniedException
    message: string
end

-- The request conflicts with the current state of the resource. Updating or deleting a resource can cause an inconsistent state.
local record ConflictException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- An unexpected error occurred while processing the request.
local record InternalServerException
    message: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The request references a resource that does not exist.
local record ResourceNotFoundException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
end

-- The request would cause a service quota to be exceeded.
local record ServiceQuotaExceededException
    message: string
    -- The resource ID.
    resourceId: string
    -- The resource type.
    resourceType: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
end

-- The limit on the number of requests per second was exceeded.
local record ThrottlingException
    message: string
    -- The service code.
    serviceCode: string
    -- The ID of the service quota that was exceeded.
    quotaCode: string
    -- The number of seconds to wait before retrying.
    retryAfterSeconds: number
end

-- The input does not satisfy the constraints specified by an Amazon Web Services service.
local record ValidationException
    message: string
    -- The reason.
    reason: string
    -- The fields that failed validation.
    fieldList: {ValidationExceptionField}
end

local type ResourceConfigurationType = "GROUP" | "CHILD" | "SINGLE" | "ARN"

local record M
    BatchUpdateRuleInput: BatchUpdateRuleInput
    BatchUpdateRuleOutput: BatchUpdateRuleOutput
    CreateAccessLogSubscriptionInput: CreateAccessLogSubscriptionInput
    CreateAccessLogSubscriptionOutput: CreateAccessLogSubscriptionOutput
    CreateListenerInput: CreateListenerInput
    CreateListenerOutput: CreateListenerOutput
    CreateResourceConfigurationInput: CreateResourceConfigurationInput
    CreateResourceConfigurationOutput: CreateResourceConfigurationOutput
    CreateResourceGatewayInput: CreateResourceGatewayInput
    CreateResourceGatewayOutput: CreateResourceGatewayOutput
    CreateRuleInput: CreateRuleInput
    CreateRuleOutput: CreateRuleOutput
    CreateServiceInput: CreateServiceInput
    CreateServiceOutput: CreateServiceOutput
    CreateServiceNetworkInput: CreateServiceNetworkInput
    CreateServiceNetworkOutput: CreateServiceNetworkOutput
    CreateServiceNetworkResourceAssociationInput: CreateServiceNetworkResourceAssociationInput
    CreateServiceNetworkResourceAssociationOutput: CreateServiceNetworkResourceAssociationOutput
    CreateServiceNetworkServiceAssociationInput: CreateServiceNetworkServiceAssociationInput
    CreateServiceNetworkServiceAssociationOutput: CreateServiceNetworkServiceAssociationOutput
    CreateServiceNetworkVpcAssociationInput: CreateServiceNetworkVpcAssociationInput
    CreateServiceNetworkVpcAssociationOutput: CreateServiceNetworkVpcAssociationOutput
    CreateTargetGroupInput: CreateTargetGroupInput
    CreateTargetGroupOutput: CreateTargetGroupOutput
    DeleteAccessLogSubscriptionInput: DeleteAccessLogSubscriptionInput
    DeleteAccessLogSubscriptionOutput: DeleteAccessLogSubscriptionOutput
    DeleteAuthPolicyInput: DeleteAuthPolicyInput
    DeleteAuthPolicyOutput: DeleteAuthPolicyOutput
    DeleteDomainVerificationInput: DeleteDomainVerificationInput
    DeleteDomainVerificationOutput: DeleteDomainVerificationOutput
    DeleteListenerInput: DeleteListenerInput
    DeleteListenerOutput: DeleteListenerOutput
    DeleteResourceConfigurationInput: DeleteResourceConfigurationInput
    DeleteResourceConfigurationOutput: DeleteResourceConfigurationOutput
    DeleteResourceEndpointAssociationInput: DeleteResourceEndpointAssociationInput
    DeleteResourceEndpointAssociationOutput: DeleteResourceEndpointAssociationOutput
    DeleteResourceGatewayInput: DeleteResourceGatewayInput
    DeleteResourceGatewayOutput: DeleteResourceGatewayOutput
    DeleteResourcePolicyInput: DeleteResourcePolicyInput
    DeleteResourcePolicyOutput: DeleteResourcePolicyOutput
    DeleteRuleInput: DeleteRuleInput
    DeleteRuleOutput: DeleteRuleOutput
    DeleteServiceInput: DeleteServiceInput
    DeleteServiceOutput: DeleteServiceOutput
    DeleteServiceNetworkInput: DeleteServiceNetworkInput
    DeleteServiceNetworkOutput: DeleteServiceNetworkOutput
    DeleteServiceNetworkResourceAssociationInput: DeleteServiceNetworkResourceAssociationInput
    DeleteServiceNetworkResourceAssociationOutput: DeleteServiceNetworkResourceAssociationOutput
    DeleteServiceNetworkServiceAssociationInput: DeleteServiceNetworkServiceAssociationInput
    DeleteServiceNetworkServiceAssociationOutput: DeleteServiceNetworkServiceAssociationOutput
    DeleteServiceNetworkVpcAssociationInput: DeleteServiceNetworkVpcAssociationInput
    DeleteServiceNetworkVpcAssociationOutput: DeleteServiceNetworkVpcAssociationOutput
    DeleteTargetGroupInput: DeleteTargetGroupInput
    DeleteTargetGroupOutput: DeleteTargetGroupOutput
    DeregisterTargetsInput: DeregisterTargetsInput
    DeregisterTargetsOutput: DeregisterTargetsOutput
    GetAccessLogSubscriptionInput: GetAccessLogSubscriptionInput
    GetAccessLogSubscriptionOutput: GetAccessLogSubscriptionOutput
    GetAuthPolicyInput: GetAuthPolicyInput
    GetAuthPolicyOutput: GetAuthPolicyOutput
    GetDomainVerificationInput: GetDomainVerificationInput
    GetDomainVerificationOutput: GetDomainVerificationOutput
    GetListenerInput: GetListenerInput
    GetListenerOutput: GetListenerOutput
    GetResourceConfigurationInput: GetResourceConfigurationInput
    GetResourceConfigurationOutput: GetResourceConfigurationOutput
    GetResourceGatewayInput: GetResourceGatewayInput
    GetResourceGatewayOutput: GetResourceGatewayOutput
    GetResourcePolicyInput: GetResourcePolicyInput
    GetResourcePolicyOutput: GetResourcePolicyOutput
    GetRuleInput: GetRuleInput
    GetRuleOutput: GetRuleOutput
    GetServiceInput: GetServiceInput
    GetServiceOutput: GetServiceOutput
    GetServiceNetworkInput: GetServiceNetworkInput
    GetServiceNetworkOutput: GetServiceNetworkOutput
    GetServiceNetworkResourceAssociationInput: GetServiceNetworkResourceAssociationInput
    GetServiceNetworkResourceAssociationOutput: GetServiceNetworkResourceAssociationOutput
    GetServiceNetworkServiceAssociationInput: GetServiceNetworkServiceAssociationInput
    GetServiceNetworkServiceAssociationOutput: GetServiceNetworkServiceAssociationOutput
    GetServiceNetworkVpcAssociationInput: GetServiceNetworkVpcAssociationInput
    GetServiceNetworkVpcAssociationOutput: GetServiceNetworkVpcAssociationOutput
    GetTargetGroupInput: GetTargetGroupInput
    GetTargetGroupOutput: GetTargetGroupOutput
    ListAccessLogSubscriptionsInput: ListAccessLogSubscriptionsInput
    ListAccessLogSubscriptionsOutput: ListAccessLogSubscriptionsOutput
    ListDomainVerificationsInput: ListDomainVerificationsInput
    ListDomainVerificationsOutput: ListDomainVerificationsOutput
    ListListenersInput: ListListenersInput
    ListListenersOutput: ListListenersOutput
    ListResourceConfigurationsInput: ListResourceConfigurationsInput
    ListResourceConfigurationsOutput: ListResourceConfigurationsOutput
    ListResourceEndpointAssociationsInput: ListResourceEndpointAssociationsInput
    ListResourceEndpointAssociationsOutput: ListResourceEndpointAssociationsOutput
    ListResourceGatewaysInput: ListResourceGatewaysInput
    ListResourceGatewaysOutput: ListResourceGatewaysOutput
    ListRulesInput: ListRulesInput
    ListRulesOutput: ListRulesOutput
    ListServiceNetworkResourceAssociationsInput: ListServiceNetworkResourceAssociationsInput
    ListServiceNetworkResourceAssociationsOutput: ListServiceNetworkResourceAssociationsOutput
    ListServiceNetworksInput: ListServiceNetworksInput
    ListServiceNetworksOutput: ListServiceNetworksOutput
    ListServiceNetworkServiceAssociationsInput: ListServiceNetworkServiceAssociationsInput
    ListServiceNetworkServiceAssociationsOutput: ListServiceNetworkServiceAssociationsOutput
    ListServiceNetworkVpcAssociationsInput: ListServiceNetworkVpcAssociationsInput
    ListServiceNetworkVpcAssociationsOutput: ListServiceNetworkVpcAssociationsOutput
    ListServiceNetworkVpcEndpointAssociationsInput: ListServiceNetworkVpcEndpointAssociationsInput
    ListServiceNetworkVpcEndpointAssociationsOutput: ListServiceNetworkVpcEndpointAssociationsOutput
    ListServicesInput: ListServicesInput
    ListServicesOutput: ListServicesOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    ListTargetGroupsInput: ListTargetGroupsInput
    ListTargetGroupsOutput: ListTargetGroupsOutput
    ListTargetsInput: ListTargetsInput
    ListTargetsOutput: ListTargetsOutput
    PutAuthPolicyInput: PutAuthPolicyInput
    PutAuthPolicyOutput: PutAuthPolicyOutput
    PutResourcePolicyInput: PutResourcePolicyInput
    PutResourcePolicyOutput: PutResourcePolicyOutput
    RegisterTargetsInput: RegisterTargetsInput
    RegisterTargetsOutput: RegisterTargetsOutput
    StartDomainVerificationInput: StartDomainVerificationInput
    StartDomainVerificationOutput: StartDomainVerificationOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateAccessLogSubscriptionInput: UpdateAccessLogSubscriptionInput
    UpdateAccessLogSubscriptionOutput: UpdateAccessLogSubscriptionOutput
    UpdateListenerInput: UpdateListenerInput
    UpdateListenerOutput: UpdateListenerOutput
    UpdateResourceConfigurationInput: UpdateResourceConfigurationInput
    UpdateResourceConfigurationOutput: UpdateResourceConfigurationOutput
    UpdateResourceGatewayInput: UpdateResourceGatewayInput
    UpdateResourceGatewayOutput: UpdateResourceGatewayOutput
    UpdateRuleInput: UpdateRuleInput
    UpdateRuleOutput: UpdateRuleOutput
    UpdateServiceInput: UpdateServiceInput
    UpdateServiceOutput: UpdateServiceOutput
    UpdateServiceNetworkInput: UpdateServiceNetworkInput
    UpdateServiceNetworkOutput: UpdateServiceNetworkOutput
    UpdateServiceNetworkVpcAssociationInput: UpdateServiceNetworkVpcAssociationInput
    UpdateServiceNetworkVpcAssociationOutput: UpdateServiceNetworkVpcAssociationOutput
    UpdateTargetGroupInput: UpdateTargetGroupInput
    UpdateTargetGroupOutput: UpdateTargetGroupOutput
end

return M
