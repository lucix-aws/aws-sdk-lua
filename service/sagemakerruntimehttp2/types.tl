-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M

    -- The input fails to satisfy the constraints specified by an AWS service.
    record InputValidationError
        -- Error message.
        Message: string
        -- Error code.
        ErrorCode: string
    end

    -- The request processing has failed because of an unknown error, exception or failure.
    record InternalServerError
        -- Error message.
        Message: string
        -- Error code.
        ErrorCode: string
    end

    -- Internal stream failure that occurs during streaming.
    record InternalStreamFailure
        -- Error message.
        Message: string
    end

    -- Request payload part structure.
    record RequestPayloadPart
        -- The payload bytes.
        Bytes: string
        -- Data type header. Can be one of these possible values: "UTF8", "BINARY".
        DataType: string
        -- Completion state header. Can be one of these possible values: "PARTIAL", "COMPLETE".
        CompletionState: string
        -- Padding string for alignment.
        P: string
    end

    -- Request stream event union.
    record RequestStreamEvent
        -- Payload part event.
        PayloadPart: RequestPayloadPart
    end

    record InvokeEndpointWithBidirectionalStreamInput
        -- The name of the endpoint to invoke.
        EndpointName: string
        -- The request payload stream.
        Body: function(): string, string
        -- Target variant for the request.
        TargetVariant: string
        -- Model invocation path.
        ModelInvocationPath: string
        -- Model query string.
        ModelQueryString: string
    end

    -- Model stream error that occurs during streaming.
    record ModelStreamError
        -- Error message.
        Message: string
        -- Error code.
        ErrorCode: string
    end

    -- Response payload part structure.
    record ResponsePayloadPart
        -- The payload bytes.
        Bytes: string
        -- Data type header. Can be one of these possible values: "UTF8", "BINARY".
        DataType: string
        -- Completion state header. Can be one of these possible values: "PARTIAL", "COMPLETE".
        CompletionState: string
        -- Padding string for alignment.
        P: string
    end

    -- Response stream event union.
    record ResponseStreamEvent
        -- Payload part event.
        PayloadPart: ResponsePayloadPart
        -- Model stream error event.
        ModelStreamError: ModelStreamError
        -- Internal stream failure event.
        InternalStreamFailure: InternalStreamFailure
    end

    record InvokeEndpointWithBidirectionalStreamOutput
        -- The response payload stream.
        Body: function(): string, string
        -- The invoked production variant.
        InvokedProductionVariant: string
    end

    -- An error occurred while processing the model.
    record ModelError
        -- Error message.
        Message: string
        -- HTTP status code returned by model.
        OriginalStatusCode: number
        -- Original error message from the model.
        OriginalMessage: string
        -- CloudWatch log stream ARN.
        LogStreamArn: string
        -- Error code.
        ErrorCode: string
    end

    -- The request has failed due to a temporary failure of the server.
    record ServiceUnavailableError
        -- Error message.
        Message: string
        -- Error code.
        ErrorCode: string
    end

end

return M
