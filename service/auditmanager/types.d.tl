-- Code generated by lua-client-codegen. DO NOT EDIT.

local record AssociateAssessmentReportEvidenceFolderInput
    -- The identifier for the assessment.
    assessmentId: string
    -- The identifier for the folder that the evidence is stored in.
    evidenceFolderId: string
end

local record AssociateAssessmentReportEvidenceFolderOutput
end

local record BatchAssociateAssessmentReportEvidenceInput
    -- The identifier for the assessment.
    assessmentId: string
    -- The identifier for the folder that the evidence is stored in.
    evidenceFolderId: string
    -- The list of evidence identifiers.
    evidenceIds: {string}
end

local record BatchAssociateAssessmentReportEvidenceOutput
    -- The list of evidence identifiers.
    evidenceIds: {string}
    -- A list of errors that the BatchAssociateAssessmentReportEvidence API
    -- returned.
    errors: {AssessmentReportEvidenceError}
end

local record BatchCreateDelegationByAssessmentInput
    -- The API request to batch create delegations in Audit Manager.
    createDelegationRequests: {CreateDelegationRequest}
    -- The identifier for the assessment.
    assessmentId: string
end

local record BatchCreateDelegationByAssessmentOutput
    -- The delegations that are associated with the assessment.
    delegations: {Delegation}
    -- A list of errors that the BatchCreateDelegationByAssessment API returned.
    errors: {BatchCreateDelegationByAssessmentError}
end

local record BatchDeleteDelegationByAssessmentInput
    -- The identifiers for the delegations.
    delegationIds: {string}
    -- The identifier for the assessment.
    assessmentId: string
end

local record BatchDeleteDelegationByAssessmentOutput
    -- A list of errors that the BatchDeleteDelegationByAssessment API returned.
    errors: {BatchDeleteDelegationByAssessmentError}
end

local record BatchDisassociateAssessmentReportEvidenceInput
    -- The identifier for the assessment.
    assessmentId: string
    -- The identifier for the folder that the evidence is stored in.
    evidenceFolderId: string
    -- The list of evidence identifiers.
    evidenceIds: {string}
end

local record BatchDisassociateAssessmentReportEvidenceOutput
    -- The identifier for the evidence.
    evidenceIds: {string}
    -- A list of errors that the BatchDisassociateAssessmentReportEvidence API
    -- returned.
    errors: {AssessmentReportEvidenceError}
end

local record BatchImportEvidenceToAssessmentControlInput
    -- The identifier for the assessment.
    assessmentId: string
    -- The identifier for the control set.
    controlSetId: string
    -- The identifier for the control.
    controlId: string
    -- The list of manual evidence objects.
    manualEvidence: {ManualEvidence}
end

local record BatchImportEvidenceToAssessmentControlOutput
    -- A list of errors that the BatchImportEvidenceToAssessmentControl API
    -- returned.
    errors: {BatchImportEvidenceToAssessmentControlError}
end

local record CreateAssessmentInput
    -- The name of the assessment to be created.
    name: string
    -- The optional description of the assessment to be created.
    description: string
    -- The assessment report storage destination for the assessment that's being created.
    assessmentReportsDestination: AssessmentReportsDestination
    scope: Scope
    -- The list of roles for the assessment.
    roles: {Role}
    -- The identifier for the framework that the assessment will be created from.
    frameworkId: string
    -- The tags that are associated with the assessment.
    tags: {string : string}
end

local record CreateAssessmentOutput
    assessment: Assessment
end

local record CreateAssessmentFrameworkInput
    -- The name of the new custom framework.
    name: string
    -- An optional description for the new custom framework.
    description: string
    -- The compliance type that the new custom framework supports, such as CIS or HIPAA.
    complianceType: string
    -- The control sets that are associated with the framework.
    -- The Controls object returns a partial response when called through Framework
    -- APIs. For a complete Controls object, use GetControl.
    controlSets: {CreateAssessmentFrameworkControlSet}
    -- The tags that are associated with the framework.
    tags: {string : string}
end

local record CreateAssessmentFrameworkOutput
    -- The new framework object that the CreateAssessmentFramework API returned.
    framework: Framework
end

local record CreateAssessmentReportInput
    -- The name of the new assessment report.
    name: string
    -- The description of the assessment report.
    description: string
    -- The identifier for the assessment.
    assessmentId: string
    -- A SQL statement that represents an evidence finder query.
    -- Provide this parameter when you want to generate an assessment report from the results
    -- of an evidence finder search query. When you use this parameter, Audit Manager
    -- generates a one-time report using only the evidence from the query output. This report does
    -- not include any assessment evidence that was manually added to a report using the console, or associated with a report using the API.
    -- To use this parameter, the enablementStatus of evidence finder must be ENABLED.
    -- For examples and help resolving queryStatement validation exceptions, see
    -- Troubleshooting evidence finder issues in the
    -- Audit Manager User Guide.
    queryStatement: string
end

local record CreateAssessmentReportOutput
    -- The new assessment report that the CreateAssessmentReport API returned.
    assessmentReport: AssessmentReport
end

local record CreateControlInput
    -- The name of the control.
    name: string
    -- The description of the control.
    description: string
    -- The steps to follow to determine if the control is satisfied.
    testingInformation: string
    -- The title of the action plan for remediating the control.
    actionPlanTitle: string
    -- The recommended actions to carry out if the control isn't fulfilled.
    actionPlanInstructions: string
    -- The data mapping sources for the control.
    controlMappingSources: {CreateControlMappingSource}
    -- The tags that are associated with the control.
    tags: {string : string}
end

local record CreateControlOutput
    -- The new control that the CreateControl API returned.
    control: Control
end

local record DeleteAssessmentInput
    -- The identifier for the assessment.
    assessmentId: string
end

local record DeleteAssessmentOutput
end

local record DeleteAssessmentFrameworkInput
    -- The identifier for the custom framework.
    frameworkId: string
end

local record DeleteAssessmentFrameworkOutput
end

local record DeleteAssessmentFrameworkShareInput
    -- The unique identifier for the share request to be deleted.
    requestId: string
    -- Specifies whether the share request is a sent request or a received request.
    requestType: string
end

local record DeleteAssessmentFrameworkShareOutput
end

local record DeleteAssessmentReportInput
    -- The unique identifier for the assessment.
    assessmentId: string
    -- The unique identifier for the assessment report.
    assessmentReportId: string
end

local record DeleteAssessmentReportOutput
end

local record DeleteControlInput
    -- The unique identifier for the control.
    controlId: string
end

local record DeleteControlOutput
end

local record DeregisterAccountInput
end

local record DeregisterAccountOutput
    -- The registration status of the account.
    status: string
end

local record DeregisterOrganizationAdminAccountInput
    -- The identifier for the administrator account.
    adminAccountId: string
end

local record DeregisterOrganizationAdminAccountOutput
end

local record DisassociateAssessmentReportEvidenceFolderInput
    -- The unique identifier for the assessment.
    assessmentId: string
    -- The unique identifier for the folder that the evidence is stored in.
    evidenceFolderId: string
end

local record DisassociateAssessmentReportEvidenceFolderOutput
end

local record GetAccountStatusInput
end

local record GetAccountStatusOutput
    -- The status of the Amazon Web Services account.
    status: string
end

local record GetAssessmentInput
    -- The unique identifier for the assessment.
    assessmentId: string
end

local record GetAssessmentOutput
    assessment: Assessment
    userRole: Role
end

local record GetAssessmentFrameworkInput
    -- The identifier for the framework.
    frameworkId: string
end

local record GetAssessmentFrameworkOutput
    -- The framework that the GetAssessmentFramework API returned.
    -- The Controls object returns a partial response when called through
    -- Framework APIs. For a complete Controls object, use
    -- GetControl.
    framework: Framework
end

local record GetAssessmentReportUrlInput
    -- The unique identifier for the assessment report.
    assessmentReportId: string
    -- The unique identifier for the assessment.
    assessmentId: string
end

local record GetAssessmentReportUrlOutput
    preSignedUrl: URL
end

local record GetChangeLogsInput
    -- The unique identifier for the assessment.
    assessmentId: string
    -- The unique identifier for the control set.
    controlSetId: string
    -- The unique identifier for the control.
    controlId: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record GetChangeLogsOutput
    -- The list of user activity for the control.
    changeLogs: {ChangeLog}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record GetControlInput
    -- The identifier for the control.
    controlId: string
end

local record GetControlOutput
    -- The details of the control that the GetControl API returned.
    control: Control
end

local record GetDelegationsInput
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record GetDelegationsOutput
    -- The list of delegations that the GetDelegations API returned.
    delegations: {DelegationMetadata}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record GetEvidenceInput
    -- The unique identifier for the assessment.
    assessmentId: string
    -- The unique identifier for the control set.
    controlSetId: string
    -- The unique identifier for the folder that the evidence is stored in.
    evidenceFolderId: string
    -- The unique identifier for the evidence.
    evidenceId: string
end

local record GetEvidenceOutput
    -- The evidence that the GetEvidence API returned.
    evidence: Evidence
end

local record GetEvidenceByEvidenceFolderInput
    -- The identifier for the assessment.
    assessmentId: string
    -- The identifier for the control set.
    controlSetId: string
    -- The unique identifier for the folder that the evidence is stored in.
    evidenceFolderId: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record GetEvidenceByEvidenceFolderOutput
    -- The list of evidence that the GetEvidenceByEvidenceFolder API returned.
    evidence: {Evidence}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record GetEvidenceFileUploadUrlInput
    -- The file that you want to upload. For a list of supported file formats, see Supported file types for manual evidence in the Audit Manager
    -- User Guide.
    fileName: string
end

local record GetEvidenceFileUploadUrlOutput
    -- The name of the uploaded manual evidence file that the presigned URL was generated
    -- for.
    evidenceFileName: string
    -- The presigned URL that was generated.
    uploadUrl: string
end

local record GetEvidenceFolderInput
    -- The unique identifier for the assessment.
    assessmentId: string
    -- The unique identifier for the control set.
    controlSetId: string
    -- The unique identifier for the folder that the evidence is stored in.
    evidenceFolderId: string
end

local record GetEvidenceFolderOutput
    -- The folder that the evidence is stored in.
    evidenceFolder: AssessmentEvidenceFolder
end

local record GetEvidenceFoldersByAssessmentInput
    -- The unique identifier for the assessment.
    assessmentId: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record GetEvidenceFoldersByAssessmentOutput
    -- The list of evidence folders that the GetEvidenceFoldersByAssessment API
    -- returned.
    evidenceFolders: {AssessmentEvidenceFolder}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record GetEvidenceFoldersByAssessmentControlInput
    -- The identifier for the assessment.
    assessmentId: string
    -- The identifier for the control set.
    controlSetId: string
    -- The identifier for the control.
    controlId: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record GetEvidenceFoldersByAssessmentControlOutput
    -- The list of evidence folders that the
    -- GetEvidenceFoldersByAssessmentControl API returned.
    evidenceFolders: {AssessmentEvidenceFolder}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record GetInsightsInput
end

local record GetInsightsOutput
    -- The analytics data that the GetInsights API returned.
    insights: Insights
end

local record GetInsightsByAssessmentInput
    -- The unique identifier for the assessment.
    assessmentId: string
end

local record GetInsightsByAssessmentOutput
    -- The assessment analytics data that the GetInsightsByAssessment API
    -- returned.
    insights: InsightsByAssessment
end

local record GetOrganizationAdminAccountInput
end

local record GetOrganizationAdminAccountOutput
    -- The identifier for the administrator account.
    adminAccountId: string
    -- The identifier for the organization.
    organizationId: string
end

local record GetServicesInScopeInput
end

local record GetServicesInScopeOutput
    -- The metadata that's associated with the Amazon Web Services service.
    serviceMetadata: {ServiceMetadata}
end

local record GetSettingsInput
    -- The list of setting attribute enum values.
    attribute: string
end

local record GetSettingsOutput
    -- The settings object that holds all supported Audit Manager settings.
    settings: Settings
end

local record ListAssessmentControlInsightsByControlDomainInput
    -- The unique identifier for the control domain.
    -- Audit Manager supports the control domains that are provided by Amazon Web Services
    -- Control Catalog. For information about how to find a list of available control domains, see
    -- ListDomains
    -- in the Amazon Web Services Control
    -- Catalog API Reference.
    controlDomainId: string
    -- The unique identifier for the active assessment.
    assessmentId: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record ListAssessmentControlInsightsByControlDomainOutput
    -- The assessment control analytics data that the
    -- ListAssessmentControlInsightsByControlDomain API returned.
    controlInsightsByAssessment: {ControlInsightsMetadataByAssessmentItem}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListAssessmentFrameworksInput
    -- The type of framework, such as a standard framework or a custom framework.
    frameworkType: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record ListAssessmentFrameworksOutput
    -- A list of metadata that the ListAssessmentFrameworks API returns for each
    -- framework.
    frameworkMetadataList: {AssessmentFrameworkMetadata}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListAssessmentFrameworkShareRequestsInput
    -- Specifies whether the share request is a sent request or a received request.
    requestType: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record ListAssessmentFrameworkShareRequestsOutput
    -- The list of share requests that the ListAssessmentFrameworkShareRequests
    -- API returned.
    assessmentFrameworkShareRequests: {AssessmentFrameworkShareRequest}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListAssessmentReportsInput
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record ListAssessmentReportsOutput
    -- The list of assessment reports that the ListAssessmentReports API
    -- returned.
    assessmentReports: {AssessmentReportMetadata}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListAssessmentsInput
    -- The current status of the assessment.
    status: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record ListAssessmentsOutput
    -- The metadata that the ListAssessments API returns for each
    -- assessment.
    assessmentMetadata: {AssessmentMetadataItem}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListControlDomainInsightsInput
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record ListControlDomainInsightsOutput
    -- The control domain analytics data that the ListControlDomainInsights API
    -- returned.
    controlDomainInsights: {ControlDomainInsights}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListControlDomainInsightsByAssessmentInput
    -- The unique identifier for the active assessment.
    assessmentId: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record ListControlDomainInsightsByAssessmentOutput
    -- The control domain analytics data that the
    -- ListControlDomainInsightsByAssessment API returned.
    controlDomainInsights: {ControlDomainInsights}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListControlInsightsByControlDomainInput
    -- The unique identifier for the control domain.
    -- Audit Manager supports the control domains that are provided by Amazon Web Services
    -- Control Catalog. For information about how to find a list of available control domains, see
    -- ListDomains
    -- in the Amazon Web Services Control
    -- Catalog API Reference.
    controlDomainId: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record ListControlInsightsByControlDomainOutput
    -- The control analytics data that the ListControlInsightsByControlDomain API
    -- returned.
    controlInsightsMetadata: {ControlInsightsMetadataItem}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListControlsInput
    -- A filter that narrows the list of controls to a specific type.
    controlType: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- The maximum number of results on a page or for an API request call.
    maxResults: number
    -- A filter that narrows the list of controls to a specific resource from the Amazon Web Services
    -- Control Catalog.
    -- To use this parameter, specify the ARN of the Control Catalog resource. You can specify either
    -- a control domain, a control objective, or a common control. For information about how to find the ARNs
    -- for these resources, see
    -- ListDomains
    -- ,
    -- ListObjectives
    -- , and
    -- ListCommonControls
    -- .
    -- You can only filter by one Control Catalog resource at a time.
    -- Specifying multiple resource ARNs isn’t currently supported. If you want to filter by more
    -- than one ARN, we recommend that you run the ListControls operation separately
    -- for each ARN.
    -- Alternatively, specify UNCATEGORIZED to list controls that aren't
    -- mapped to a Control Catalog resource. For example, this operation might return a list of
    -- custom controls that don't belong to any control domain or control objective.
    controlCatalogId: string
end

local record ListControlsOutput
    -- A list of metadata that the ListControls API returns for each
    -- control.
    controlMetadataList: {ControlMetadata}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListKeywordsForDataSourceInput
    -- The control mapping data source that the keywords apply to.
    source: string
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record ListKeywordsForDataSourceOutput
    -- The list of keywords for the control mapping source.
    keywords: {string}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListNotificationsInput
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
    -- Represents the maximum number of results on a page or for an API request call.
    maxResults: number
end

local record ListNotificationsOutput
    -- The returned list of notifications.
    notifications: {Notification}
    -- The pagination token that's used to fetch the next set of results.
    nextToken: string
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
end

local record ListTagsForResourceOutput
    -- The list of tags that the ListTagsForResource API returned.
    tags: {string : string}
end

local record RegisterAccountInput
    -- The KMS key details.
    kmsKey: string
    -- The delegated administrator account for Audit Manager.
    delegatedAdminAccount: string
end

local record RegisterAccountOutput
    -- The status of the account registration request.
    status: string
end

local record RegisterOrganizationAdminAccountInput
    -- The identifier for the delegated administrator account.
    adminAccountId: string
end

local record RegisterOrganizationAdminAccountOutput
    -- The identifier for the delegated administrator account.
    adminAccountId: string
    -- The identifier for the organization.
    organizationId: string
end

local record StartAssessmentFrameworkShareInput
    -- The unique identifier for the custom framework to be shared.
    frameworkId: string
    -- The Amazon Web Services account of the recipient.
    destinationAccount: string
    -- The Amazon Web Services Region of the recipient.
    destinationRegion: string
    -- An optional comment from the sender about the share request.
    comment: string
end

local record StartAssessmentFrameworkShareOutput
    -- The share request that's created by the StartAssessmentFrameworkShare API.
    assessmentFrameworkShareRequest: AssessmentFrameworkShareRequest
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the resource.
    resourceArn: string
    -- The tags that are associated with the resource.
    tags: {string : string}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the specified resource.
    resourceArn: string
    -- The name or key of the tag.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateAssessmentInput
    -- The unique identifier for the assessment.
    assessmentId: string
    -- The name of the assessment to be updated.
    assessmentName: string
    -- The description of the assessment.
    assessmentDescription: string
    -- The scope of the assessment.
    scope: Scope
    -- The assessment report storage destination for the assessment that's being updated.
    assessmentReportsDestination: AssessmentReportsDestination
    -- The list of roles for the assessment.
    roles: {Role}
end

local record UpdateAssessmentOutput
    -- The response object for the UpdateAssessment API. This is the name of the
    -- updated assessment.
    assessment: Assessment
end

local record UpdateAssessmentControlInput
    -- The unique identifier for the assessment.
    assessmentId: string
    -- The unique identifier for the control set.
    controlSetId: string
    -- The unique identifier for the control.
    controlId: string
    -- The status of the control.
    controlStatus: string
    -- The comment body text for the control.
    commentBody: string
end

local record UpdateAssessmentControlOutput
    -- The name of the updated control set that the UpdateAssessmentControl API
    -- returned.
    control: AssessmentControl
end

local record UpdateAssessmentControlSetStatusInput
    -- The unique identifier for the assessment.
    assessmentId: string
    -- The unique identifier for the control set.
    controlSetId: string
    -- The status of the control set that's being updated.
    status: string
    -- The comment that's related to the status update.
    comment: string
end

local record UpdateAssessmentControlSetStatusOutput
    -- The name of the updated control set that the
    -- UpdateAssessmentControlSetStatus API returned.
    controlSet: AssessmentControlSet
end

local record UpdateAssessmentFrameworkInput
    -- The unique identifier for the framework.
    frameworkId: string
    -- The name of the framework to be updated.
    name: string
    -- The description of the updated framework.
    description: string
    -- The compliance type that the new custom framework supports, such as CIS or HIPAA.
    complianceType: string
    -- The control sets that are associated with the framework.
    -- The Controls object returns a partial response when called through Framework
    -- APIs. For a complete Controls object, use GetControl.
    controlSets: {UpdateAssessmentFrameworkControlSet}
end

local record UpdateAssessmentFrameworkOutput
    -- The framework object.
    framework: Framework
end

local record UpdateAssessmentFrameworkShareInput
    -- The unique identifier for the share request.
    requestId: string
    -- Specifies whether the share request is a sent request or a received request.
    requestType: string
    -- Specifies the update action for the share request.
    action: string
end

local record UpdateAssessmentFrameworkShareOutput
    -- The updated share request that's returned by the
    -- UpdateAssessmentFrameworkShare operation.
    assessmentFrameworkShareRequest: AssessmentFrameworkShareRequest
end

local record UpdateAssessmentStatusInput
    -- The unique identifier for the assessment.
    assessmentId: string
    -- The current status of the assessment.
    status: string
end

local record UpdateAssessmentStatusOutput
    -- The name of the updated assessment that the UpdateAssessmentStatus API
    -- returned.
    assessment: Assessment
end

local record UpdateControlInput
    -- The identifier for the control.
    controlId: string
    -- The name of the updated control.
    name: string
    -- The optional description of the control.
    description: string
    -- The steps that you should follow to determine if the control is met.
    testingInformation: string
    -- The title of the action plan for remediating the control.
    actionPlanTitle: string
    -- The recommended actions to carry out if the control isn't fulfilled.
    actionPlanInstructions: string
    -- The data mapping sources for the control.
    controlMappingSources: {ControlMappingSource}
end

local record UpdateControlOutput
    -- The name of the updated control set that the UpdateControl API returned.
    control: Control
end

local record UpdateSettingsInput
    -- The Amazon Simple Notification Service (Amazon SNS) topic that Audit Manager sends
    -- notifications to.
    snsTopic: string
    -- The default S3 destination bucket for storing assessment reports.
    defaultAssessmentReportsDestination: AssessmentReportsDestination
    -- A list of the default audit owners.
    defaultProcessOwners: {Role}
    -- The KMS key details.
    kmsKey: string
    -- Specifies whether the evidence finder feature is enabled. Change this attribute to
    -- enable or disable evidence finder.
    -- When you use this attribute to disable evidence finder, Audit Manager deletes the
    -- event data store that’s used to query your evidence data. As a result, you can’t
    -- re-enable evidence finder and use the feature again. Your only alternative is to deregister and then re-register
    -- Audit Manager.
    evidenceFinderEnabled: boolean
    -- The deregistration policy for your Audit Manager data. You can
    -- use this attribute to determine how your data is handled when you deregister Audit Manager.
    deregistrationPolicy: DeregistrationPolicy
    -- The default S3 destination bucket for storing evidence finder exports.
    defaultExportDestination: DefaultExportDestination
end

local record UpdateSettingsOutput
    -- The current list of settings.
    settings: Settings
end

local record ValidateAssessmentReportIntegrityInput
    -- The relative path of the Amazon S3 bucket that the assessment report is stored
    -- in.
    s3RelativePath: string
end

local record ValidateAssessmentReportIntegrityOutput
    -- Specifies whether the signature key is valid.
    signatureValid: boolean
    -- The signature algorithm that's used to code sign the assessment report file.
    signatureAlgorithm: string
    -- The date and time signature that specifies when the assessment report was created.
    signatureDateTime: string
    -- The unique identifier for the validation signature key.
    signatureKeyId: string
    -- Represents any errors that occurred when validating the assessment report.
    validationErrors: {string}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- You've reached your account quota for this resource type. To perform the requested
-- action, delete some existing resources or request a quota increase from
-- the Service Quotas console. For a list of Audit Manager service quotas, see Quotas and
-- restrictions for Audit Manager.
local record ServiceQuotaExceededException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- You've reached your account quota for this resource type. To perform the requested
-- action, delete some existing resources or request a quota increase from
-- the Service Quotas console. For a list of Audit Manager service quotas, see Quotas and
-- restrictions for Audit Manager.
local record ServiceQuotaExceededException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- You've reached your account quota for this resource type. To perform the requested
-- action, delete some existing resources or request a quota increase from
-- the Service Quotas console. For a list of Audit Manager service quotas, see Quotas and
-- restrictions for Audit Manager.
local record ServiceQuotaExceededException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- You've reached your account quota for this resource type. To perform the requested
-- action, delete some existing resources or request a quota increase from
-- the Service Quotas console. For a list of Audit Manager service quotas, see Quotas and
-- restrictions for Audit Manager.
local record ServiceQuotaExceededException
    message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- You've reached your account quota for this resource type. To perform the requested
-- action, delete some existing resources or request a quota increase from
-- the Service Quotas console. For a list of Audit Manager service quotas, see Quotas and
-- restrictions for Audit Manager.
local record ServiceQuotaExceededException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- You've reached your account quota for this resource type. To perform the requested
-- action, delete some existing resources or request a quota increase from
-- the Service Quotas console. For a list of Audit Manager service quotas, see Quotas and
-- restrictions for Audit Manager.
local record ServiceQuotaExceededException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- You've reached your account quota for this resource type. To perform the requested
-- action, delete some existing resources or request a quota increase from
-- the Service Quotas console. For a list of Audit Manager service quotas, see Quotas and
-- restrictions for Audit Manager.
local record ServiceQuotaExceededException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

-- Your account isn't registered with Audit Manager. Check the delegated
-- administrator setup on the Audit Manager settings page, and try again.
local record AccessDeniedException
    message: string
end

-- An internal service error occurred during the processing of your request. Try again
-- later.
local record InternalServerException
    message: string
end

-- The resource that's specified in the request can't be found.
local record ResourceNotFoundException
    message: string
    -- The unique identifier for the resource.
    resourceId: string
    -- The type of resource that's affected by the error.
    resourceType: string
end

-- The request has invalid or missing parameters.
local record ValidationException
    message: string
    -- The reason the request failed validation.
    reason: string
    -- The fields that caused the error, if applicable.
    fields: {ValidationExceptionField}
end

local type AccountStatus = "ACTIVE" | "INACTIVE" | "PENDING_ACTIVATION"

local type AssessmentStatus = "ACTIVE" | "INACTIVE"

local type ControlSetStatus = "ACTIVE" | "UNDER_REVIEW" | "REVIEWED"

local type ControlStatus = "UNDER_REVIEW" | "REVIEWED" | "INACTIVE"

local type ControlType = "Standard" | "Custom" | "Core"

local type DataSourceType = "AWS_Cloudtrail" | "AWS_Config" | "AWS_Security_Hub" | "AWS_API_Call" | "MANUAL"

local type FrameworkType = "Standard" | "Custom"

local type SettingAttribute = "ALL" | "IS_AWS_ORG_ENABLED" | "SNS_TOPIC" | "DEFAULT_ASSESSMENT_REPORTS_DESTINATION" | "DEFAULT_PROCESS_OWNERS" | "EVIDENCE_FINDER_ENABLEMENT" | "DEREGISTRATION_POLICY" | "DEFAULT_EXPORT_DESTINATION"

local type ShareRequestAction = "ACCEPT" | "DECLINE" | "REVOKE"

local type ShareRequestType = "SENT" | "RECEIVED"

local record M
    AssociateAssessmentReportEvidenceFolderInput: AssociateAssessmentReportEvidenceFolderInput
    AssociateAssessmentReportEvidenceFolderOutput: AssociateAssessmentReportEvidenceFolderOutput
    BatchAssociateAssessmentReportEvidenceInput: BatchAssociateAssessmentReportEvidenceInput
    BatchAssociateAssessmentReportEvidenceOutput: BatchAssociateAssessmentReportEvidenceOutput
    BatchCreateDelegationByAssessmentInput: BatchCreateDelegationByAssessmentInput
    BatchCreateDelegationByAssessmentOutput: BatchCreateDelegationByAssessmentOutput
    BatchDeleteDelegationByAssessmentInput: BatchDeleteDelegationByAssessmentInput
    BatchDeleteDelegationByAssessmentOutput: BatchDeleteDelegationByAssessmentOutput
    BatchDisassociateAssessmentReportEvidenceInput: BatchDisassociateAssessmentReportEvidenceInput
    BatchDisassociateAssessmentReportEvidenceOutput: BatchDisassociateAssessmentReportEvidenceOutput
    BatchImportEvidenceToAssessmentControlInput: BatchImportEvidenceToAssessmentControlInput
    BatchImportEvidenceToAssessmentControlOutput: BatchImportEvidenceToAssessmentControlOutput
    CreateAssessmentInput: CreateAssessmentInput
    CreateAssessmentOutput: CreateAssessmentOutput
    CreateAssessmentFrameworkInput: CreateAssessmentFrameworkInput
    CreateAssessmentFrameworkOutput: CreateAssessmentFrameworkOutput
    CreateAssessmentReportInput: CreateAssessmentReportInput
    CreateAssessmentReportOutput: CreateAssessmentReportOutput
    CreateControlInput: CreateControlInput
    CreateControlOutput: CreateControlOutput
    DeleteAssessmentInput: DeleteAssessmentInput
    DeleteAssessmentOutput: DeleteAssessmentOutput
    DeleteAssessmentFrameworkInput: DeleteAssessmentFrameworkInput
    DeleteAssessmentFrameworkOutput: DeleteAssessmentFrameworkOutput
    DeleteAssessmentFrameworkShareInput: DeleteAssessmentFrameworkShareInput
    DeleteAssessmentFrameworkShareOutput: DeleteAssessmentFrameworkShareOutput
    DeleteAssessmentReportInput: DeleteAssessmentReportInput
    DeleteAssessmentReportOutput: DeleteAssessmentReportOutput
    DeleteControlInput: DeleteControlInput
    DeleteControlOutput: DeleteControlOutput
    DeregisterAccountInput: DeregisterAccountInput
    DeregisterAccountOutput: DeregisterAccountOutput
    DeregisterOrganizationAdminAccountInput: DeregisterOrganizationAdminAccountInput
    DeregisterOrganizationAdminAccountOutput: DeregisterOrganizationAdminAccountOutput
    DisassociateAssessmentReportEvidenceFolderInput: DisassociateAssessmentReportEvidenceFolderInput
    DisassociateAssessmentReportEvidenceFolderOutput: DisassociateAssessmentReportEvidenceFolderOutput
    GetAccountStatusInput: GetAccountStatusInput
    GetAccountStatusOutput: GetAccountStatusOutput
    GetAssessmentInput: GetAssessmentInput
    GetAssessmentOutput: GetAssessmentOutput
    GetAssessmentFrameworkInput: GetAssessmentFrameworkInput
    GetAssessmentFrameworkOutput: GetAssessmentFrameworkOutput
    GetAssessmentReportUrlInput: GetAssessmentReportUrlInput
    GetAssessmentReportUrlOutput: GetAssessmentReportUrlOutput
    GetChangeLogsInput: GetChangeLogsInput
    GetChangeLogsOutput: GetChangeLogsOutput
    GetControlInput: GetControlInput
    GetControlOutput: GetControlOutput
    GetDelegationsInput: GetDelegationsInput
    GetDelegationsOutput: GetDelegationsOutput
    GetEvidenceInput: GetEvidenceInput
    GetEvidenceOutput: GetEvidenceOutput
    GetEvidenceByEvidenceFolderInput: GetEvidenceByEvidenceFolderInput
    GetEvidenceByEvidenceFolderOutput: GetEvidenceByEvidenceFolderOutput
    GetEvidenceFileUploadUrlInput: GetEvidenceFileUploadUrlInput
    GetEvidenceFileUploadUrlOutput: GetEvidenceFileUploadUrlOutput
    GetEvidenceFolderInput: GetEvidenceFolderInput
    GetEvidenceFolderOutput: GetEvidenceFolderOutput
    GetEvidenceFoldersByAssessmentInput: GetEvidenceFoldersByAssessmentInput
    GetEvidenceFoldersByAssessmentOutput: GetEvidenceFoldersByAssessmentOutput
    GetEvidenceFoldersByAssessmentControlInput: GetEvidenceFoldersByAssessmentControlInput
    GetEvidenceFoldersByAssessmentControlOutput: GetEvidenceFoldersByAssessmentControlOutput
    GetInsightsInput: GetInsightsInput
    GetInsightsOutput: GetInsightsOutput
    GetInsightsByAssessmentInput: GetInsightsByAssessmentInput
    GetInsightsByAssessmentOutput: GetInsightsByAssessmentOutput
    GetOrganizationAdminAccountInput: GetOrganizationAdminAccountInput
    GetOrganizationAdminAccountOutput: GetOrganizationAdminAccountOutput
    GetServicesInScopeInput: GetServicesInScopeInput
    GetServicesInScopeOutput: GetServicesInScopeOutput
    GetSettingsInput: GetSettingsInput
    GetSettingsOutput: GetSettingsOutput
    ListAssessmentControlInsightsByControlDomainInput: ListAssessmentControlInsightsByControlDomainInput
    ListAssessmentControlInsightsByControlDomainOutput: ListAssessmentControlInsightsByControlDomainOutput
    ListAssessmentFrameworksInput: ListAssessmentFrameworksInput
    ListAssessmentFrameworksOutput: ListAssessmentFrameworksOutput
    ListAssessmentFrameworkShareRequestsInput: ListAssessmentFrameworkShareRequestsInput
    ListAssessmentFrameworkShareRequestsOutput: ListAssessmentFrameworkShareRequestsOutput
    ListAssessmentReportsInput: ListAssessmentReportsInput
    ListAssessmentReportsOutput: ListAssessmentReportsOutput
    ListAssessmentsInput: ListAssessmentsInput
    ListAssessmentsOutput: ListAssessmentsOutput
    ListControlDomainInsightsInput: ListControlDomainInsightsInput
    ListControlDomainInsightsOutput: ListControlDomainInsightsOutput
    ListControlDomainInsightsByAssessmentInput: ListControlDomainInsightsByAssessmentInput
    ListControlDomainInsightsByAssessmentOutput: ListControlDomainInsightsByAssessmentOutput
    ListControlInsightsByControlDomainInput: ListControlInsightsByControlDomainInput
    ListControlInsightsByControlDomainOutput: ListControlInsightsByControlDomainOutput
    ListControlsInput: ListControlsInput
    ListControlsOutput: ListControlsOutput
    ListKeywordsForDataSourceInput: ListKeywordsForDataSourceInput
    ListKeywordsForDataSourceOutput: ListKeywordsForDataSourceOutput
    ListNotificationsInput: ListNotificationsInput
    ListNotificationsOutput: ListNotificationsOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    RegisterAccountInput: RegisterAccountInput
    RegisterAccountOutput: RegisterAccountOutput
    RegisterOrganizationAdminAccountInput: RegisterOrganizationAdminAccountInput
    RegisterOrganizationAdminAccountOutput: RegisterOrganizationAdminAccountOutput
    StartAssessmentFrameworkShareInput: StartAssessmentFrameworkShareInput
    StartAssessmentFrameworkShareOutput: StartAssessmentFrameworkShareOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateAssessmentInput: UpdateAssessmentInput
    UpdateAssessmentOutput: UpdateAssessmentOutput
    UpdateAssessmentControlInput: UpdateAssessmentControlInput
    UpdateAssessmentControlOutput: UpdateAssessmentControlOutput
    UpdateAssessmentControlSetStatusInput: UpdateAssessmentControlSetStatusInput
    UpdateAssessmentControlSetStatusOutput: UpdateAssessmentControlSetStatusOutput
    UpdateAssessmentFrameworkInput: UpdateAssessmentFrameworkInput
    UpdateAssessmentFrameworkOutput: UpdateAssessmentFrameworkOutput
    UpdateAssessmentFrameworkShareInput: UpdateAssessmentFrameworkShareInput
    UpdateAssessmentFrameworkShareOutput: UpdateAssessmentFrameworkShareOutput
    UpdateAssessmentStatusInput: UpdateAssessmentStatusInput
    UpdateAssessmentStatusOutput: UpdateAssessmentStatusOutput
    UpdateControlInput: UpdateControlInput
    UpdateControlOutput: UpdateControlOutput
    UpdateSettingsInput: UpdateSettingsInput
    UpdateSettingsOutput: UpdateSettingsOutput
    ValidateAssessmentReportIntegrityInput: ValidateAssessmentReportIntegrityInput
    ValidateAssessmentReportIntegrityOutput: ValidateAssessmentReportIntegrityOutput
end

return M
