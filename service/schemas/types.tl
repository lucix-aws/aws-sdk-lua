-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record CreateDiscovererInput
        -- A description for the discoverer.
        Description: string
        -- The ARN of the event bus.
        SourceArn: string
        -- Support discovery of schemas in events sent to the bus from another account. (default: true).
        CrossAccount: boolean
        -- Tags associated with the resource.
        Tags: {string : string}
    end

    record CreateDiscovererOutput
        -- The description of the discoverer.
        Description: string
        -- The ARN of the discoverer.
        DiscovererArn: string
        -- The ID of the discoverer.
        DiscovererId: string
        -- The ARN of the event bus.
        SourceArn: string
        -- The state of the discoverer.
        State: string
        -- The Status if the discoverer will discover schemas from events sent from another account.
        CrossAccount: boolean
        -- Tags associated with the resource.
        Tags: {string : string}
    end

    record CreateRegistryInput
        -- A description of the registry to be created.
        Description: string
        -- The name of the registry.
        RegistryName: string
        -- Tags to associate with the registry.
        Tags: {string : string}
    end

    record CreateRegistryOutput
        -- The description of the registry.
        Description: string
        -- The ARN of the registry.
        RegistryArn: string
        -- The name of the registry.
        RegistryName: string
        -- Tags associated with the registry.
        Tags: {string : string}
    end

    record CreateSchemaInput
        -- The source of the schema definition.
        Content: string
        -- A description of the schema.
        Description: string
        -- The name of the registry.
        RegistryName: string
        -- The name of the schema.
        SchemaName: string
        -- Tags associated with the schema.
        Tags: {string : string}
        -- The type of schema.
        Type: string
    end

    record CreateSchemaOutput
        -- The description of the schema.
        Description: string
        -- The date and time that schema was modified.
        LastModified: number
        -- The ARN of the schema.
        SchemaArn: string
        -- The name of the schema.
        SchemaName: string
        -- The version number of the schema
        SchemaVersion: string
        Tags: {string : string}
        -- The type of the schema.
        Type: string
        -- The date the schema version was created.
        VersionCreatedDate: number
    end

    record DeleteDiscovererInput
        -- The ID of the discoverer.
        DiscovererId: string
    end

    record DeleteDiscovererOutput
    end

    record DeleteRegistryInput
        -- The name of the registry.
        RegistryName: string
    end

    record DeleteRegistryOutput
    end

    record DeleteResourcePolicyInput
        -- The name of the registry.
        RegistryName: string
    end

    record DeleteResourcePolicyOutput
    end

    record DeleteSchemaInput
        -- The name of the registry.
        RegistryName: string
        -- The name of the schema.
        SchemaName: string
    end

    record DeleteSchemaOutput
    end

    record DeleteSchemaVersionInput
        -- The name of the registry.
        RegistryName: string
        -- The name of the schema.
        SchemaName: string
        -- The version number of the schema
        SchemaVersion: string
    end

    record DeleteSchemaVersionOutput
    end

    record DescribeCodeBindingInput
        -- The language of the code binding.
        Language: string
        -- The name of the registry.
        RegistryName: string
        -- The name of the schema.
        SchemaName: string
        -- Specifying this limits the results to only this schema version.
        SchemaVersion: string
    end

    record DescribeCodeBindingOutput
        -- The time and date that the code binding was created.
        CreationDate: number
        -- The date and time that code bindings were modified.
        LastModified: number
        -- The version number of the schema.
        SchemaVersion: string
        -- The current status of code binding generation.
        Status: string
    end

    record DescribeDiscovererInput
        -- The ID of the discoverer.
        DiscovererId: string
    end

    record DescribeDiscovererOutput
        -- The description of the discoverer.
        Description: string
        -- The ARN of the discoverer.
        DiscovererArn: string
        -- The ID of the discoverer.
        DiscovererId: string
        -- The ARN of the event bus.
        SourceArn: string
        -- The state of the discoverer.
        State: string
        -- The Status if the discoverer will discover schemas from events sent from another account.
        CrossAccount: boolean
        -- Tags associated with the resource.
        Tags: {string : string}
    end

    record DescribeRegistryInput
        -- The name of the registry.
        RegistryName: string
    end

    record DescribeRegistryOutput
        -- The description of the registry.
        Description: string
        -- The ARN of the registry.
        RegistryArn: string
        -- The name of the registry.
        RegistryName: string
        -- Tags associated with the registry.
        Tags: {string : string}
    end

    record DescribeSchemaInput
        -- The name of the registry.
        RegistryName: string
        -- The name of the schema.
        SchemaName: string
        -- Specifying this limits the results to only this schema version.
        SchemaVersion: string
    end

    record DescribeSchemaOutput
        -- The source of the schema definition.
        Content: string
        -- The description of the schema.
        Description: string
        -- The date and time that schema was modified.
        LastModified: number
        -- The ARN of the schema.
        SchemaArn: string
        -- The name of the schema.
        SchemaName: string
        -- The version number of the schema
        SchemaVersion: string
        -- Tags associated with the resource.
        Tags: {string : string}
        -- The type of the schema.
        Type: string
        -- The date the schema version was created.
        VersionCreatedDate: number
    end

    record ExportSchemaInput
        -- The name of the registry.
        RegistryName: string
        -- The name of the schema.
        SchemaName: string
        -- Specifying this limits the results to only this schema version.
        SchemaVersion: string
        Type: string
    end

    record ExportSchemaOutput
        Content: string
        SchemaArn: string
        SchemaName: string
        SchemaVersion: string
        Type: string
    end

    record GetCodeBindingSourceInput
        -- The language of the code binding.
        Language: string
        -- The name of the registry.
        RegistryName: string
        -- The name of the schema.
        SchemaName: string
        -- Specifying this limits the results to only this schema version.
        SchemaVersion: string
    end

    record GetCodeBindingSourceOutput
        Body: string
    end

    record GetDiscoveredSchemaInput
        -- An array of strings where each string is a JSON event. These are the events that were used to generate the schema. The array includes a single type of event and has a maximum size of 10 events.
        Events: {string}
        -- The type of event.
        Type: string
    end

    record GetDiscoveredSchemaOutput
        -- The source of the schema definition.
        Content: string
    end

    record GetResourcePolicyInput
        -- The name of the registry.
        RegistryName: string
    end

    record GetResourcePolicyOutput
        -- The resource-based policy.
        Policy: string
        -- The revision ID.
        RevisionId: string
    end

    record ListDiscoverersInput
        -- Specifying this limits the results to only those discoverer IDs that start with the specified prefix.
        DiscovererIdPrefix: string
        Limit: number
        -- The token that specifies the next page of results to return. To request the first page, leave NextToken empty. The token will expire in 24 hours, and cannot be shared with other accounts.
        NextToken: string
        -- Specifying this limits the results to only those ARNs that start with the specified prefix.
        SourceArnPrefix: string
    end

    record ListDiscoverersOutput
        -- An array of DiscovererSummary information.
        Discoverers: {DiscovererSummary}
        -- The token that specifies the next page of results to return. To request the first page, leave NextToken empty. The token will expire in 24 hours, and cannot be shared with other accounts.
        NextToken: string
    end

    record ListRegistriesInput
        Limit: number
        -- The token that specifies the next page of results to return. To request the first page, leave NextToken empty. The token will expire in 24 hours, and cannot be shared with other accounts.
        NextToken: string
        -- Specifying this limits the results to only those registry names that start with the specified prefix.
        RegistryNamePrefix: string
        -- Can be set to Local or AWS to limit responses to your custom registries, or the ones provided by AWS.
        Scope: string
    end

    record ListRegistriesOutput
        -- The token that specifies the next page of results to return. To request the first page, leave NextToken empty. The token will expire in 24 hours, and cannot be shared with other accounts.
        NextToken: string
        -- An array of registry summaries.
        Registries: {RegistrySummary}
    end

    record ListSchemasInput
        Limit: number
        -- The token that specifies the next page of results to return. To request the first page, leave NextToken empty. The token will expire in 24 hours, and cannot be shared with other accounts.
        NextToken: string
        -- The name of the registry.
        RegistryName: string
        -- Specifying this limits the results to only those schema names that start with the specified prefix.
        SchemaNamePrefix: string
    end

    record ListSchemasOutput
        -- The token that specifies the next page of results to return. To request the first page, leave NextToken empty. The token will expire in 24 hours, and cannot be shared with other accounts.
        NextToken: string
        -- An array of schema summaries.
        Schemas: {SchemaSummary}
    end

    record ListSchemaVersionsInput
        Limit: number
        -- The token that specifies the next page of results to return. To request the first page, leave NextToken empty. The token will expire in 24 hours, and cannot be shared with other accounts.
        NextToken: string
        -- The name of the registry.
        RegistryName: string
        -- The name of the schema.
        SchemaName: string
    end

    record ListSchemaVersionsOutput
        -- The token that specifies the next page of results to return. To request the first page, leave NextToken empty. The token will expire in 24 hours, and cannot be shared with other accounts.
        NextToken: string
        -- An array of schema version summaries.
        SchemaVersions: {SchemaVersionSummary}
    end

    record ListTagsForResourceInput
        -- The ARN of the resource.
        ResourceArn: string
    end

    record ListTagsForResourceOutput
        Tags: {string : string}
    end

    record PutCodeBindingInput
        -- The language of the code binding.
        Language: string
        -- The name of the registry.
        RegistryName: string
        -- The name of the schema.
        SchemaName: string
        -- Specifying this limits the results to only this schema version.
        SchemaVersion: string
    end

    record PutCodeBindingOutput
        -- The time and date that the code binding was created.
        CreationDate: number
        -- The date and time that code bindings were modified.
        LastModified: number
        -- The version number of the schema.
        SchemaVersion: string
        -- The current status of code binding generation.
        Status: string
    end

    -- The name of the policy.
    record PutResourcePolicyInput
        -- The resource-based policy.
        Policy: string
        -- The name of the registry.
        RegistryName: string
        -- The revision ID of the policy.
        RevisionId: string
    end

    record PutResourcePolicyOutput
        -- The resource-based policy.
        Policy: string
        -- The revision ID of the policy.
        RevisionId: string
    end

    record SearchSchemasInput
        -- Specifying this limits the results to only schemas that include the provided keywords.
        Keywords: string
        Limit: number
        -- The token that specifies the next page of results to return. To request the first page, leave NextToken empty. The token will expire in 24 hours, and cannot be shared with other accounts.
        NextToken: string
        -- The name of the registry.
        RegistryName: string
    end

    record SearchSchemasOutput
        -- The token that specifies the next page of results to return. To request the first page, leave NextToken empty. The token will expire in 24 hours, and cannot be shared with other accounts.
        NextToken: string
        -- An array of SearchSchemaSummary information.
        Schemas: {SearchSchemaSummary}
    end

    record StartDiscovererInput
        -- The ID of the discoverer.
        DiscovererId: string
    end

    record StartDiscovererOutput
        -- The ID of the discoverer.
        DiscovererId: string
        -- The state of the discoverer.
        State: string
    end

    record StopDiscovererInput
        -- The ID of the discoverer.
        DiscovererId: string
    end

    record StopDiscovererOutput
        -- The ID of the discoverer.
        DiscovererId: string
        -- The state of the discoverer.
        State: string
    end

    record TagResourceInput
        -- The ARN of the resource.
        ResourceArn: string
        -- Tags associated with the resource.
        Tags: {string : string}
    end

    record TagResourceOutput
    end

    record UntagResourceInput
        -- The ARN of the resource.
        ResourceArn: string
        -- Keys of key-value pairs.
        TagKeys: {string}
    end

    record UntagResourceOutput
    end

    record UpdateDiscovererInput
        -- The description of the discoverer to update.
        Description: string
        -- The ID of the discoverer.
        DiscovererId: string
        -- Support discovery of schemas in events sent to the bus from another account. (default: true)
        CrossAccount: boolean
    end

    record UpdateDiscovererOutput
        -- The description of the discoverer.
        Description: string
        -- The ARN of the discoverer.
        DiscovererArn: string
        -- The ID of the discoverer.
        DiscovererId: string
        -- The ARN of the event bus.
        SourceArn: string
        -- The state of the discoverer.
        State: string
        -- The Status if the discoverer will discover schemas from events sent from another account.
        CrossAccount: boolean
        -- Tags associated with the resource.
        Tags: {string : string}
    end

    -- Updates the registry.
    record UpdateRegistryInput
        -- The description of the registry to update.
        Description: string
        -- The name of the registry.
        RegistryName: string
    end

    record UpdateRegistryOutput
        -- The description of the registry.
        Description: string
        -- The ARN of the registry.
        RegistryArn: string
        -- The name of the registry.
        RegistryName: string
        -- Tags associated with the registry.
        Tags: {string : string}
    end

    record UpdateSchemaInput
        -- The ID of the client token.
        ClientTokenId: string
        -- The source of the schema definition.
        Content: string
        -- The description of the schema.
        Description: string
        -- The name of the registry.
        RegistryName: string
        -- The name of the schema.
        SchemaName: string
        -- The schema type for the events schema.
        Type: string
    end

    record UpdateSchemaOutput
        -- The description of the schema.
        Description: string
        -- The date and time that schema was modified.
        LastModified: number
        -- The ARN of the schema.
        SchemaArn: string
        -- The name of the schema.
        SchemaName: string
        -- The version number of the schema
        SchemaVersion: string
        Tags: {string : string}
        -- The type of the schema.
        Type: string
        -- The date the schema version was created.
        VersionCreatedDate: number
    end

    record BadRequestException
        -- The error code.
        Code: string
        -- The message string of the error output.
        Message: string
    end

    record ConflictException
        -- The error code.
        Code: string
        -- The message string of the error output.
        Message: string
    end

    record ForbiddenException
        -- The error code.
        Code: string
        -- The message string of the error output.
        Message: string
    end

    record InternalServerErrorException
        -- The error code.
        Code: string
        -- The message string of the error output.
        Message: string
    end

    record ServiceUnavailableException
        -- The error code.
        Code: string
        -- The message string of the error output.
        Message: string
    end

    record UnauthorizedException
        -- The error code.
        Code: string
        -- The message string of the error output.
        Message: string
    end

    record NotFoundException
        -- The error code.
        Code: string
        -- The message string of the error output.
        Message: string
    end

    record TooManyRequestsException
        -- The error code.
        Code: string
        -- The message string of the error output.
        Message: string
    end

    record GoneException
        -- The error code.
        Code: string
        -- The message string of the error output.
        Message: string
    end

    record PreconditionFailedException
        -- The error code.
        Code: string
        -- The message string of the error output.
        Message: string
    end

    enum CodeGenerationStatus
        "CREATE_IN_PROGRESS"
        "CREATE_COMPLETE"
        "CREATE_FAILED"
    end

    enum DiscovererState
        "STARTED"
        "STOPPED"
    end

    enum Type
        "OpenApi3"
        "JSONSchemaDraft4"
    end

end

return M
