-- Code generated by lua-client-codegen. DO NOT EDIT.

local record AssociateCreatedArtifactInput
    -- The name of the ProgressUpdateStream.
    ProgressUpdateStream: string
    -- Unique identifier that references the migration task. Do not store personal
    -- data in this field.
    MigrationTaskName: string
    -- An ARN of the AWS resource related to the migration (e.g., AMI, EC2 instance, RDS
    -- instance, etc.)
    CreatedArtifact: CreatedArtifact
    -- Optional boolean flag to indicate whether any effect should take place. Used to test if
    -- the caller has permission to make the call.
    DryRun: boolean
end

local record AssociateCreatedArtifactOutput
end

local record AssociateDiscoveredResourceInput
    -- The name of the ProgressUpdateStream.
    ProgressUpdateStream: string
    -- The identifier given to the MigrationTask. Do not store personal data in this
    -- field.
    MigrationTaskName: string
    -- Object representing a Resource.
    DiscoveredResource: DiscoveredResource
    -- Optional boolean flag to indicate whether any effect should take place. Used to test if
    -- the caller has permission to make the call.
    DryRun: boolean
end

local record AssociateDiscoveredResourceOutput
end

local record AssociateSourceResourceInput
    -- The name of the progress-update stream, which is used for access control as well as a
    -- namespace for migration-task names that is implicitly linked to your AWS account. The
    -- progress-update stream must uniquely identify the migration tool as it is used for all
    -- updates made by the tool; however, it does not need to be unique for each AWS account
    -- because it is scoped to the AWS account.
    ProgressUpdateStream: string
    -- A unique identifier that references the migration task. Do not include
    -- sensitive data in this field.
    MigrationTaskName: string
    -- The source resource that you want to associate.
    SourceResource: SourceResource
    -- This is an optional parameter that you can use to test whether the call will succeed.
    -- Set this parameter to true to verify that you have the permissions that are
    -- required to make the call, and that you have specified the other parameters in the call
    -- correctly.
    DryRun: boolean
end

local record AssociateSourceResourceOutput
end

local record CreateProgressUpdateStreamInput
    -- The name of the ProgressUpdateStream. Do not store personal data in this
    -- field.
    ProgressUpdateStreamName: string
    -- Optional boolean flag to indicate whether any effect should take place. Used to test if
    -- the caller has permission to make the call.
    DryRun: boolean
end

local record CreateProgressUpdateStreamOutput
end

local record DeleteProgressUpdateStreamInput
    -- The name of the ProgressUpdateStream. Do not store personal data in this
    -- field.
    ProgressUpdateStreamName: string
    -- Optional boolean flag to indicate whether any effect should take place. Used to test if
    -- the caller has permission to make the call.
    DryRun: boolean
end

local record DeleteProgressUpdateStreamOutput
end

local record DescribeApplicationStateInput
    -- The configurationId in Application Discovery Service that uniquely identifies the
    -- grouped application.
    ApplicationId: string
end

local record DescribeApplicationStateOutput
    -- Status of the application - Not Started, In-Progress, Complete.
    ApplicationStatus: string
    -- The timestamp when the application status was last updated.
    LastUpdatedTime: number
end

local record DescribeMigrationTaskInput
    -- The name of the ProgressUpdateStream.
    ProgressUpdateStream: string
    -- The identifier given to the MigrationTask. Do not store personal data in this
    -- field.
    MigrationTaskName: string
end

local record DescribeMigrationTaskOutput
    -- Object encapsulating information about the migration task.
    MigrationTask: MigrationTask
end

local record DisassociateCreatedArtifactInput
    -- The name of the ProgressUpdateStream.
    ProgressUpdateStream: string
    -- Unique identifier that references the migration task to be disassociated with the
    -- artifact. Do not store personal data in this field.
    MigrationTaskName: string
    -- An ARN of the AWS resource related to the migration (e.g., AMI, EC2 instance, RDS
    -- instance, etc.)
    CreatedArtifactName: string
    -- Optional boolean flag to indicate whether any effect should take place. Used to test if
    -- the caller has permission to make the call.
    DryRun: boolean
end

local record DisassociateCreatedArtifactOutput
end

local record DisassociateDiscoveredResourceInput
    -- The name of the ProgressUpdateStream.
    ProgressUpdateStream: string
    -- The identifier given to the MigrationTask. Do not store personal data in this
    -- field.
    MigrationTaskName: string
    -- ConfigurationId of the Application Discovery Service resource to be
    -- disassociated.
    ConfigurationId: string
    -- Optional boolean flag to indicate whether any effect should take place. Used to test if
    -- the caller has permission to make the call.
    DryRun: boolean
end

local record DisassociateDiscoveredResourceOutput
end

local record DisassociateSourceResourceInput
    -- The name of the progress-update stream, which is used for access control as well as a
    -- namespace for migration-task names that is implicitly linked to your AWS account. The
    -- progress-update stream must uniquely identify the migration tool as it is used for all
    -- updates made by the tool; however, it does not need to be unique for each AWS account
    -- because it is scoped to the AWS account.
    ProgressUpdateStream: string
    -- A unique identifier that references the migration task. Do not include
    -- sensitive data in this field.
    MigrationTaskName: string
    -- The name that was specified for the source resource.
    SourceResourceName: string
    -- This is an optional parameter that you can use to test whether the call will succeed.
    -- Set this parameter to true to verify that you have the permissions that are
    -- required to make the call, and that you have specified the other parameters in the call
    -- correctly.
    DryRun: boolean
end

local record DisassociateSourceResourceOutput
end

local record ImportMigrationTaskInput
    -- The name of the ProgressUpdateStream. >
    ProgressUpdateStream: string
    -- Unique identifier that references the migration task. Do not store personal
    -- data in this field.
    MigrationTaskName: string
    -- Optional boolean flag to indicate whether any effect should take place. Used to test if
    -- the caller has permission to make the call.
    DryRun: boolean
end

local record ImportMigrationTaskOutput
end

local record ListApplicationStatesInput
    -- The configurationIds from the Application Discovery Service that uniquely identifies
    -- your applications.
    ApplicationIds: {string}
    -- If a NextToken was returned by a previous call, there are more results
    -- available. To retrieve the next page of results, make the call again using the returned
    -- token in NextToken.
    NextToken: string
    -- Maximum number of results to be returned per page.
    MaxResults: number
end

local record ListApplicationStatesOutput
    -- A list of Applications that exist in Application Discovery Service.
    ApplicationStateList: {ApplicationState}
    -- If a NextToken was returned by a previous call, there are more results
    -- available. To retrieve the next page of results, make the call again using the returned
    -- token in NextToken.
    NextToken: string
end

local record ListCreatedArtifactsInput
    -- The name of the ProgressUpdateStream.
    ProgressUpdateStream: string
    -- Unique identifier that references the migration task. Do not store personal
    -- data in this field.
    MigrationTaskName: string
    -- If a NextToken was returned by a previous call, there are more results
    -- available. To retrieve the next page of results, make the call again using the returned
    -- token in NextToken.
    NextToken: string
    -- Maximum number of results to be returned per page.
    MaxResults: number
end

local record ListCreatedArtifactsOutput
    -- If there are more created artifacts than the max result, return the next token to be
    -- passed to the next call as a bookmark of where to start from.
    NextToken: string
    -- List of created artifacts up to the maximum number of results specified in the
    -- request.
    CreatedArtifactList: {CreatedArtifact}
end

local record ListDiscoveredResourcesInput
    -- The name of the ProgressUpdateStream.
    ProgressUpdateStream: string
    -- The name of the MigrationTask. Do not store personal data in this
    -- field.
    MigrationTaskName: string
    -- If a NextToken was returned by a previous call, there are more results
    -- available. To retrieve the next page of results, make the call again using the returned
    -- token in NextToken.
    NextToken: string
    -- The maximum number of results returned per page.
    MaxResults: number
end

local record ListDiscoveredResourcesOutput
    -- If there are more discovered resources than the max result, return the next token to be
    -- passed to the next call as a bookmark of where to start from.
    NextToken: string
    -- Returned list of discovered resources associated with the given MigrationTask.
    DiscoveredResourceList: {DiscoveredResource}
end

local record ListMigrationTasksInput
    -- If a NextToken was returned by a previous call, there are more results
    -- available. To retrieve the next page of results, make the call again using the returned
    -- token in NextToken.
    NextToken: string
    -- Value to specify how many results are returned per page.
    MaxResults: number
    -- Filter migration tasks by discovered resource name.
    ResourceName: string
end

local record ListMigrationTasksOutput
    -- If there are more migration tasks than the max result, return the next token to be
    -- passed to the next call as a bookmark of where to start from.
    NextToken: string
    -- Lists the migration task's summary which includes: MigrationTaskName,
    -- ProgressPercent, ProgressUpdateStream, Status,
    -- and the UpdateDateTime for each task.
    MigrationTaskSummaryList: {MigrationTaskSummary}
end

local record ListMigrationTaskUpdatesInput
    -- The name of the progress-update stream, which is used for access control as well as a
    -- namespace for migration-task names that is implicitly linked to your AWS account. The
    -- progress-update stream must uniquely identify the migration tool as it is used for all
    -- updates made by the tool; however, it does not need to be unique for each AWS account
    -- because it is scoped to the AWS account.
    ProgressUpdateStream: string
    -- A unique identifier that references the migration task. Do not include
    -- sensitive data in this field.
    MigrationTaskName: string
    -- If NextToken was returned by a previous call, there are more results
    -- available. The value of NextToken is a unique pagination token for each page.
    -- To retrieve the next page of results, specify the NextToken value that the
    -- previous call returned. Keep all other arguments unchanged. Each pagination token expires
    -- after 24 hours. Using an expired pagination token will return an HTTP 400 InvalidToken
    -- error.
    NextToken: string
    -- The maximum number of results to include in the response. If more results exist than the
    -- value that you specify here for MaxResults, the response will include a token
    -- that you can use to retrieve the next set of results.
    MaxResults: number
end

local record ListMigrationTaskUpdatesOutput
    -- If the response includes a NextToken value, that means that there are more
    -- results available. The value of NextToken is a unique pagination token for
    -- each page. To retrieve the next page of results, call this API again and specify this
    -- NextToken value in the request. Keep all other arguments unchanged. Each
    -- pagination token expires after 24 hours. Using an expired pagination token will return an
    -- HTTP 400 InvalidToken error.
    NextToken: string
    -- The list of migration-task updates.
    MigrationTaskUpdateList: {MigrationTaskUpdate}
end

local record ListProgressUpdateStreamsInput
    -- If a NextToken was returned by a previous call, there are more results
    -- available. To retrieve the next page of results, make the call again using the returned
    -- token in NextToken.
    NextToken: string
    -- Filter to limit the maximum number of results to list per page.
    MaxResults: number
end

local record ListProgressUpdateStreamsOutput
    -- List of progress update streams up to the max number of results passed in the
    -- input.
    ProgressUpdateStreamSummaryList: {ProgressUpdateStreamSummary}
    -- If there are more streams created than the max result, return the next token to be
    -- passed to the next call as a bookmark of where to start from.
    NextToken: string
end

local record ListSourceResourcesInput
    -- The name of the progress-update stream, which is used for access control as well as a
    -- namespace for migration-task names that is implicitly linked to your AWS account. The
    -- progress-update stream must uniquely identify the migration tool as it is used for all
    -- updates made by the tool; however, it does not need to be unique for each AWS account
    -- because it is scoped to the AWS account.
    ProgressUpdateStream: string
    -- A unique identifier that references the migration task. Do not store
    -- confidential data in this field.
    MigrationTaskName: string
    -- If NextToken was returned by a previous call, there are more results
    -- available. The value of NextToken is a unique pagination token for each page.
    -- To retrieve the next page of results, specify the NextToken value that the
    -- previous call returned. Keep all other arguments unchanged. Each pagination token expires
    -- after 24 hours. Using an expired pagination token will return an HTTP 400 InvalidToken
    -- error.
    NextToken: string
    -- The maximum number of results to include in the response. If more results exist than the
    -- value that you specify here for MaxResults, the response will include a token
    -- that you can use to retrieve the next set of results.
    MaxResults: number
end

local record ListSourceResourcesOutput
    -- If the response includes a NextToken value, that means that there are more
    -- results available. The value of NextToken is a unique pagination token for
    -- each page. To retrieve the next page of results, call this API again and specify this
    -- NextToken value in the request. Keep all other arguments unchanged. Each
    -- pagination token expires after 24 hours. Using an expired pagination token will return an
    -- HTTP 400 InvalidToken error.
    NextToken: string
    -- The list of source resources.
    SourceResourceList: {SourceResource}
end

local record NotifyApplicationStateInput
    -- The configurationId in Application Discovery Service that uniquely identifies the
    -- grouped application.
    ApplicationId: string
    -- Status of the application - Not Started, In-Progress, Complete.
    Status: string
    -- The timestamp when the application state changed.
    UpdateDateTime: number
    -- Optional boolean flag to indicate whether any effect should take place. Used to test if
    -- the caller has permission to make the call.
    DryRun: boolean
end

local record NotifyApplicationStateOutput
end

local record NotifyMigrationTaskStateInput
    -- The name of the ProgressUpdateStream.
    ProgressUpdateStream: string
    -- Unique identifier that references the migration task. Do not store personal
    -- data in this field.
    MigrationTaskName: string
    -- Information about the task's progress and status.
    Task: Task
    -- The timestamp when the task was gathered.
    UpdateDateTime: number
    -- Number of seconds after the UpdateDateTime within which the Migration Hub can expect an
    -- update. If Migration Hub does not receive an update within the specified interval, then the
    -- migration task will be considered stale.
    NextUpdateSeconds: number
    -- Optional boolean flag to indicate whether any effect should take place. Used to test if
    -- the caller has permission to make the call.
    DryRun: boolean
end

local record NotifyMigrationTaskStateOutput
end

local record PutResourceAttributesInput
    -- The name of the ProgressUpdateStream.
    ProgressUpdateStream: string
    -- Unique identifier that references the migration task. Do not store personal
    -- data in this field.
    MigrationTaskName: string
    -- Information about the resource that is being migrated. This data will be used to map the
    -- task to a resource in the Application Discovery Service repository.
    -- Takes the object array of ResourceAttribute where the Type
    -- field is reserved for the following values: IPV4_ADDRESS | IPV6_ADDRESS |
    -- MAC_ADDRESS | FQDN | VM_MANAGER_ID | VM_MANAGED_OBJECT_REFERENCE | VM_NAME | VM_PATH
    -- | BIOS_ID | MOTHERBOARD_SERIAL_NUMBER where the identifying value can be a
    -- string up to 256 characters.
    -- If any "VM" related value is set for a ResourceAttribute object,
    -- it is required that VM_MANAGER_ID, as a minimum, is always set. If
    -- VM_MANAGER_ID is not set, then all "VM" fields will be discarded
    -- and "VM" fields will not be used for matching the migration task to a server in
    -- Application Discovery Service repository. See the Example section below for a use case of specifying "VM" related
    -- values.
    -- If a server you are trying to match has multiple IP or MAC addresses, you
    -- should provide as many as you know in separate type/value pairs passed to the
    -- ResourceAttributeList parameter to maximize the chances of
    -- matching.
    ResourceAttributeList: {ResourceAttribute}
    -- Optional boolean flag to indicate whether any effect should take place. Used to test if
    -- the caller has permission to make the call.
    DryRun: boolean
end

local record PutResourceAttributesOutput
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when there are problems accessing Application Discovery Service
-- (Application Discovery Service); most likely due to a misconfigured policy or the
-- migrationhub-discovery role is missing or not configured correctly.
local record PolicyErrorException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when there are problems accessing Application Discovery Service
-- (Application Discovery Service); most likely due to a misconfigured policy or the
-- migrationhub-discovery role is missing or not configured correctly.
local record PolicyErrorException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when there are problems accessing Application Discovery Service
-- (Application Discovery Service); most likely due to a misconfigured policy or the
-- migrationhub-discovery role is missing or not configured correctly.
local record PolicyErrorException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when there are problems accessing Application Discovery Service
-- (Application Discovery Service); most likely due to a misconfigured policy or the
-- migrationhub-discovery role is missing or not configured correctly.
local record PolicyErrorException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

-- You do not have sufficient access to perform this action.
local record AccessDeniedException
    Message: string
end

-- Exception raised to indicate a successfully authorized action when the
-- DryRun flag is set to "true".
local record DryRunOperation
    Message: string
end

-- The home region is not set. Set the home region to continue.
local record HomeRegionNotSetException
    Message: string
end

-- Exception raised when an internal, configuration, or dependency error is
-- encountered.
local record InternalServerError
    Message: string
end

-- Exception raised when the provided input violates a policy constraint or is entered in
-- the wrong format or data type.
local record InvalidInputException
    Message: string
end

-- Exception raised when the request references a resource (Application Discovery Service
-- configuration, update stream, migration task, etc.) that does not exist in Application
-- Discovery Service (Application Discovery Service) or in Migration Hub's repository.
local record ResourceNotFoundException
    Message: string
end

-- Exception raised when there is an internal, configuration, or dependency error
-- encountered.
local record ServiceUnavailableException
    Message: string
end

-- The request was denied due to request throttling.
local record ThrottlingException
    -- A message that provides information about the exception.
    Message: string
    -- The number of seconds the caller should wait before retrying.
    RetryAfterSeconds: number
end

-- Exception raised to indicate a request was not authorized when the DryRun
-- flag is set to "true".
local record UnauthorizedOperation
    Message: string
end

local type ApplicationStatus = "NOT_STARTED" | "IN_PROGRESS" | "COMPLETED"

local record M
    AssociateCreatedArtifactInput: AssociateCreatedArtifactInput
    AssociateCreatedArtifactOutput: AssociateCreatedArtifactOutput
    AssociateDiscoveredResourceInput: AssociateDiscoveredResourceInput
    AssociateDiscoveredResourceOutput: AssociateDiscoveredResourceOutput
    AssociateSourceResourceInput: AssociateSourceResourceInput
    AssociateSourceResourceOutput: AssociateSourceResourceOutput
    CreateProgressUpdateStreamInput: CreateProgressUpdateStreamInput
    CreateProgressUpdateStreamOutput: CreateProgressUpdateStreamOutput
    DeleteProgressUpdateStreamInput: DeleteProgressUpdateStreamInput
    DeleteProgressUpdateStreamOutput: DeleteProgressUpdateStreamOutput
    DescribeApplicationStateInput: DescribeApplicationStateInput
    DescribeApplicationStateOutput: DescribeApplicationStateOutput
    DescribeMigrationTaskInput: DescribeMigrationTaskInput
    DescribeMigrationTaskOutput: DescribeMigrationTaskOutput
    DisassociateCreatedArtifactInput: DisassociateCreatedArtifactInput
    DisassociateCreatedArtifactOutput: DisassociateCreatedArtifactOutput
    DisassociateDiscoveredResourceInput: DisassociateDiscoveredResourceInput
    DisassociateDiscoveredResourceOutput: DisassociateDiscoveredResourceOutput
    DisassociateSourceResourceInput: DisassociateSourceResourceInput
    DisassociateSourceResourceOutput: DisassociateSourceResourceOutput
    ImportMigrationTaskInput: ImportMigrationTaskInput
    ImportMigrationTaskOutput: ImportMigrationTaskOutput
    ListApplicationStatesInput: ListApplicationStatesInput
    ListApplicationStatesOutput: ListApplicationStatesOutput
    ListCreatedArtifactsInput: ListCreatedArtifactsInput
    ListCreatedArtifactsOutput: ListCreatedArtifactsOutput
    ListDiscoveredResourcesInput: ListDiscoveredResourcesInput
    ListDiscoveredResourcesOutput: ListDiscoveredResourcesOutput
    ListMigrationTasksInput: ListMigrationTasksInput
    ListMigrationTasksOutput: ListMigrationTasksOutput
    ListMigrationTaskUpdatesInput: ListMigrationTaskUpdatesInput
    ListMigrationTaskUpdatesOutput: ListMigrationTaskUpdatesOutput
    ListProgressUpdateStreamsInput: ListProgressUpdateStreamsInput
    ListProgressUpdateStreamsOutput: ListProgressUpdateStreamsOutput
    ListSourceResourcesInput: ListSourceResourcesInput
    ListSourceResourcesOutput: ListSourceResourcesOutput
    NotifyApplicationStateInput: NotifyApplicationStateInput
    NotifyApplicationStateOutput: NotifyApplicationStateOutput
    NotifyMigrationTaskStateInput: NotifyMigrationTaskStateInput
    NotifyMigrationTaskStateOutput: NotifyMigrationTaskStateOutput
    PutResourceAttributesInput: PutResourceAttributesInput
    PutResourceAttributesOutput: PutResourceAttributesOutput
end

return M
