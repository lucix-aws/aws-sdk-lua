-- Code generated by lua-client-codegen. DO NOT EDIT.

local record M
    record GetRoutingControlStateInput
        -- The Amazon Resource Name (ARN) for the routing control that you want to get the state for.
        RoutingControlArn: string
    end

    record GetRoutingControlStateOutput
        -- The Amazon Resource Name (ARN) of the response.
        RoutingControlArn: string
        -- The state of the routing control.
        RoutingControlState: string
        -- The routing control name.
        RoutingControlName: string
    end

    record ListRoutingControlsInput
        -- The Amazon Resource Name (ARN) of the control panel of the routing controls to list.
        ControlPanelArn: string
        -- The token for the next set of results. You receive this token from a previous call.
        NextToken: string
        -- The number of routing controls objects that you want to return with this call. The default value is 500.
        MaxResults: number
    end

    record ListRoutingControlsOutput
        -- The list of routing controls.
        RoutingControls: {RoutingControl}
        -- The token for the next set of results. You receive this token from a previous call.
        NextToken: string
    end

    record UpdateRoutingControlStateInput
        -- The Amazon Resource Name (ARN) for the routing control that you want to update the state for.
        RoutingControlArn: string
        -- The state of the routing control. You can set the value to ON or OFF.
        RoutingControlState: string
        -- The Amazon Resource Names (ARNs) for the safety rules that you want to override when you're updating the state of
        -- a routing control. You can override one safety rule or multiple safety rules by including one or more ARNs, separated
        -- by commas.
        -- For more information, see
        -- Override safety rules to reroute traffic in the Amazon Route 53 Application Recovery Controller Developer Guide.
        SafetyRulesToOverride: {string}
    end

    record UpdateRoutingControlStateOutput
    end

    record UpdateRoutingControlStatesInput
        -- A set of routing control entries that you want to update.
        UpdateRoutingControlStateEntries: {UpdateRoutingControlStateEntry}
        -- The Amazon Resource Names (ARNs) for the safety rules that you want to override when you're updating routing
        -- control states. You can override one safety rule or multiple safety rules by including one or more ARNs, separated
        -- by commas.
        -- For more information, see
        -- Override safety rules to reroute traffic in the Amazon Route 53 Application Recovery Controller Developer Guide.
        SafetyRulesToOverride: {string}
    end

    record UpdateRoutingControlStatesOutput
    end

    -- You don't have sufficient permissions to perform this action.
    record AccessDeniedException
        message: string
    end

    -- The cluster endpoint isn't available. Try another cluster endpoint.
    record EndpointTemporarilyUnavailableException
        message: string
    end

    -- There was an unexpected error during processing of the request.
    record InternalServerException
        message: string
        retryAfterSeconds: number
    end

    -- The request references a routing control or control panel that was not found.
    record ResourceNotFoundException
        message: string
        -- Hypothetical resource identifier that was not found
        resourceId: string
        -- Hypothetical resource type that was not found
        resourceType: string
    end

    -- The request was denied because of request throttling.
    record ThrottlingException
        message: string
        retryAfterSeconds: number
    end

    -- There was a validation error on the request.
    record ValidationException
        message: string
        reason: string
        fields: {ValidationExceptionField}
    end

    -- There was a conflict with this request. Try again.
    record ConflictException
        -- Description of the ConflictException error
        message: string
        -- Identifier of the resource in use
        resourceId: string
        -- Type of the resource in use
        resourceType: string
    end

    -- The request can't update that many routing control states at the same time. Try again with fewer routing control states.
    record ServiceLimitExceededException
        message: string
        -- The resource identifier of the limit that was exceeded.
        resourceId: string
        -- The resource type of the limit that was exceeded.
        resourceType: string
        -- The code of the limit that was exceeded.
        limitCode: string
        -- The service code of the limit that was exceeded.
        serviceCode: string
    end

    enum RoutingControlState
        "On"
        "Off"
    end

end

return M
