-- Code generated by lua-client-codegen. DO NOT EDIT.

-- Generated endpoint ruleset tests — do not edit

local endpoint = require("smithy.endpoint")
local ruleset = require("cloudfrontkeyvaluestore.endpoint_rules")

describe("endpoint rules", function()
    it("FIPS should error", function()
        local params = {
            UseFIPS = true,
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("Invalid Configuration: FIPS is not supported with CloudFront-KeyValueStore.", err)
    end)

    it("KVS ARN must be provided to use this service", function()
        local params = {}
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("KVS ARN must be provided to use this service", err)
    end)

    it("KVS ARN must be a valid ARN", function()
        local params = {
            KvsARN = "not-a-valid-arn",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("KVS ARN must be a valid ARN", err)
    end)

    it("Provided ARN was not a valid CloudFront Service ARN. Found: `notcloudfront`", function()
        local params = {
            KvsARN = "arn:aws:notcloudfront::123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("Provided ARN is not a valid CloudFront Service ARN. Found: `notcloudfront`", err)
    end)

    it("Provided ARN must be a global resource ARN. Found: `us-west-2`", function()
        local params = {
            KvsARN = "arn:aws:cloudfront:us-west-2:123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("Provided ARN must be a global resource ARN. Found: `us-west-2`", err)
    end)

    it("ARN resource type is invalid. Expected `key-value-store`, found: `some-other-resource-type`", function()
        local params = {
            KvsARN = "arn:aws:cloudfront::123456789012:some-other-resource-type/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("ARN resource type is invalid. Expected `key-value-store`, found: `some-other-resource-type`", err)
    end)

    it("CloudFront-KeyValueStore is not supported in partition `aws-cn`", function()
        local params = {
            KvsARN = "arn:aws-cn:cloudfront::123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("CloudFront-KeyValueStore is not supported in partition `aws-cn`", err)
    end)

    it("CloudFront-KeyValueStore is not supported in partition `aws-us-gov`", function()
        local params = {
            KvsARN = "arn:aws-us-gov:cloudfront::123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("CloudFront-KeyValueStore is not supported in partition `aws-us-gov`", err)
    end)

    it("Valid account based endpoint", function()
        local params = {
            KvsARN = "arn:aws:cloudfront::123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_not_nil(result, "expected endpoint but got error: " .. tostring(err))
        assert.are.equal("https://123456789012.cloudfront-kvs.global.api.aws", result.url)
        assert.is_not_nil(result.properties, "missing properties")
        local expected_props = {
            authSchemes = {
            {
            signingName = "cloudfront-keyvaluestore",
            name = "sigv4a",
            signingRegionSet = {
            "*",
        },
        },
        },
        }
        local function deep_eq(a, b)
            if type(a) ~= type(b) then return false end
            if type(a) ~= "table" then return a == b end
            for k, v in pairs(a) do if not deep_eq(v, b[k]) then return false end end
            for k, _ in pairs(b) do if a[k] == nil then return false end end
            return true
        end
        assert.is_true(deep_eq(result.properties, expected_props), "properties mismatch")
    end)

    it("Valid account based endpoint, with SDK region", function()
        local params = {
            KvsARN = "arn:aws:cloudfront::123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
            Region = "us-west-2",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_not_nil(result, "expected endpoint but got error: " .. tostring(err))
        assert.are.equal("https://123456789012.cloudfront-kvs.global.api.aws", result.url)
        assert.is_not_nil(result.properties, "missing properties")
        local expected_props = {
            authSchemes = {
            {
            signingName = "cloudfront-keyvaluestore",
            name = "sigv4a",
            signingRegionSet = {
            "*",
        },
        },
        },
        }
        local function deep_eq(a, b)
            if type(a) ~= type(b) then return false end
            if type(a) ~= "table" then return a == b end
            for k, v in pairs(a) do if not deep_eq(v, b[k]) then return false end end
            for k, _ in pairs(b) do if a[k] == nil then return false end end
            return true
        end
        assert.is_true(deep_eq(result.properties, expected_props), "properties mismatch")
    end)

    it("Valid ARN, different partition, should error", function()
        local params = {
            KvsARN = "arn:aws:cloudfront::123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
            Region = "cn-north-1",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("Client was configured for partition `aws-cn` but Kvs ARN has `aws`", err)
    end)

    it("Valid account based endpoint with FIPS, should error", function()
        local params = {
            KvsARN = "arn:aws:cloudfront::123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
            Region = "us-east-1",
            UseFIPS = true,
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("Invalid Configuration: FIPS is not supported with CloudFront-KeyValueStore.", err)
    end)

    it("Custom sdk endpoint override", function()
        local params = {
            KvsARN = "arn:aws:cloudfront::123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
            Region = "us-east-1",
            Endpoint = "https://my-override.example.com",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_not_nil(result, "expected endpoint but got error: " .. tostring(err))
        assert.are.equal("https://123456789012.my-override.example.com", result.url)
        assert.is_not_nil(result.properties, "missing properties")
        local expected_props = {
            authSchemes = {
            {
            signingName = "cloudfront-keyvaluestore",
            name = "sigv4a",
            signingRegionSet = {
            "*",
        },
        },
        },
        }
        local function deep_eq(a, b)
            if type(a) ~= type(b) then return false end
            if type(a) ~= "table" then return a == b end
            for k, v in pairs(a) do if not deep_eq(v, b[k]) then return false end end
            for k, _ in pairs(b) do if a[k] == nil then return false end end
            return true
        end
        assert.is_true(deep_eq(result.properties, expected_props), "properties mismatch")
    end)

    it("Custom sdk endpoint override with path and http", function()
        local params = {
            KvsARN = "arn:aws:cloudfront::123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
            Endpoint = "http://my-override.example.com/custom-path",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_not_nil(result, "expected endpoint but got error: " .. tostring(err))
        assert.are.equal("http://123456789012.my-override.example.com/custom-path", result.url)
        assert.is_not_nil(result.properties, "missing properties")
        local expected_props = {
            authSchemes = {
            {
            signingName = "cloudfront-keyvaluestore",
            name = "sigv4a",
            signingRegionSet = {
            "*",
        },
        },
        },
        }
        local function deep_eq(a, b)
            if type(a) ~= type(b) then return false end
            if type(a) ~= "table" then return a == b end
            for k, v in pairs(a) do if not deep_eq(v, b[k]) then return false end end
            for k, _ in pairs(b) do if a[k] == nil then return false end end
            return true
        end
        assert.is_true(deep_eq(result.properties, expected_props), "properties mismatch")
    end)

    it("Custom override with different partition should error", function()
        local params = {
            KvsARN = "arn:aws:cloudfront::123456789012:key-value-store/my-first-kvs-e10b1dce4f394248811e77167e0451ba",
            Region = "us-gov-east-1",
            Endpoint = "https://my-override.example.com",
        }
        local result, err = endpoint.resolve(ruleset, params)
        assert.is_nil(result, "expected error but got result")
        assert.is_not_nil(err, "expected error but got nil")
        assert.are.equal("Client was configured for partition `aws-us-gov` but Kvs ARN has `aws`", err)
    end)

end)
