-- Code generated by lua-client-codegen. DO NOT EDIT.

local types = require("backup.types")

local record Operation<T>
    await: function(Operation<T>): T, table
end

local record Client
    -- Associates an MPA approval team with a backup vault.
    associateBackupVaultMpaApprovalTeam: function(Client, types.AssociateBackupVaultMpaApprovalTeamInput): Operation<types.AssociateBackupVaultMpaApprovalTeamOutput>
    -- Removes the specified legal hold on a recovery point. This action can only be performed
    -- by a user with sufficient permissions.
    cancelLegalHold: function(Client, types.CancelLegalHoldInput): Operation<types.CancelLegalHoldOutput>
    -- Creates a backup plan using a backup plan name and backup rules. A backup plan is a
    -- document that contains information that Backup uses to schedule tasks that
    -- create recovery points for resources.
    -- If you call CreateBackupPlan with a plan that already exists, you receive
    -- an AlreadyExistsException exception.
    createBackupPlan: function(Client, types.CreateBackupPlanInput): Operation<types.CreateBackupPlanOutput>
    -- Creates a JSON document that specifies a set of resources to assign to a backup plan.
    -- For examples, see Assigning resources programmatically.
    createBackupSelection: function(Client, types.CreateBackupSelectionInput): Operation<types.CreateBackupSelectionOutput>
    -- Creates a logical container where backups are stored. A CreateBackupVault
    -- request includes a name, optionally one or more resource tags, an encryption key, and a
    -- request ID.
    -- Do not include sensitive data, such as passport numbers, in the name of a backup
    -- vault.
    createBackupVault: function(Client, types.CreateBackupVaultInput): Operation<types.CreateBackupVaultOutput>
    -- Creates a framework with one or more controls. A framework is a collection of controls
    -- that you can use to evaluate your backup practices. By using pre-built customizable
    -- controls to define your policies, you can evaluate whether your backup practices comply
    -- with your policies and which resources are not yet in compliance.
    createFramework: function(Client, types.CreateFrameworkInput): Operation<types.CreateFrameworkOutput>
    -- Creates a legal hold on a recovery point (backup). A legal hold is a restraint on
    -- altering or deleting a backup until an authorized user cancels the legal hold. Any actions
    -- to delete or disassociate a recovery point will fail with an error if one or more active
    -- legal holds are on the recovery point.
    createLegalHold: function(Client, types.CreateLegalHoldInput): Operation<types.CreateLegalHoldOutput>
    -- Creates a logical container to where backups may be copied.
    -- This request includes a name, the Region, the maximum number of retention days, the
    -- minimum number of retention days, and optionally can include tags and a creator request
    -- ID.
    -- Do not include sensitive data, such as passport numbers, in the name of a backup
    -- vault.
    createLogicallyAirGappedBackupVault: function(Client, types.CreateLogicallyAirGappedBackupVaultInput): Operation<types.CreateLogicallyAirGappedBackupVaultOutput>
    -- Creates a report plan. A report plan is a document that contains information about the
    -- contents of the report and where Backup will deliver it.
    -- If you call CreateReportPlan with a plan that already exists, you receive
    -- an AlreadyExistsException exception.
    createReportPlan: function(Client, types.CreateReportPlanInput): Operation<types.CreateReportPlanOutput>
    -- Creates a restore access backup vault that provides temporary access to recovery points in a logically air-gapped backup vault, subject to MPA approval.
    createRestoreAccessBackupVault: function(Client, types.CreateRestoreAccessBackupVaultInput): Operation<types.CreateRestoreAccessBackupVaultOutput>
    -- Creates a restore testing plan.
    -- The first of two steps to create a restore testing
    -- plan. After this request is successful, finish the procedure using
    -- CreateRestoreTestingSelection.
    createRestoreTestingPlan: function(Client, types.CreateRestoreTestingPlanInput): Operation<types.CreateRestoreTestingPlanOutput>
    -- This request can be sent after CreateRestoreTestingPlan request
    -- returns successfully. This is the second part of creating a resource testing
    -- plan, and it must be completed sequentially.
    -- This consists of RestoreTestingSelectionName,
    -- ProtectedResourceType, and one of the following:
    -- ProtectedResourceArns
    -- ProtectedResourceConditions
    -- Each protected resource type can have one single value.
    -- A restore testing selection can include a wildcard value ("*") for
    -- ProtectedResourceArns along with ProtectedResourceConditions.
    -- Alternatively, you can include up to 30 specific protected resource ARNs in
    -- ProtectedResourceArns.
    -- Cannot select by both protected resource types AND specific ARNs.
    -- Request will fail if both are included.
    createRestoreTestingSelection: function(Client, types.CreateRestoreTestingSelectionInput): Operation<types.CreateRestoreTestingSelectionOutput>
    -- Creates a tiering configuration.
    -- A tiering configuration enables automatic movement of backup data to a lower-cost storage tier based on the age of backed-up objects in the backup vault.
    -- Each vault can only have one vault-specific tiering configuration, in addition to any global configuration that applies to all vaults.
    createTieringConfiguration: function(Client, types.CreateTieringConfigurationInput): Operation<types.CreateTieringConfigurationOutput>
    -- Deletes a backup plan. A backup plan can only be deleted after all associated selections
    -- of resources have been deleted. Deleting a backup plan deletes the current version of a
    -- backup plan. Previous versions, if any, will still exist.
    deleteBackupPlan: function(Client, types.DeleteBackupPlanInput): Operation<types.DeleteBackupPlanOutput>
    -- Deletes the resource selection associated with a backup plan that is specified by the
    -- SelectionId.
    deleteBackupSelection: function(Client, types.DeleteBackupSelectionInput): Operation<types.DeleteBackupSelectionOutput>
    -- Deletes the backup vault identified by its name. A vault can be deleted only if it is
    -- empty.
    deleteBackupVault: function(Client, types.DeleteBackupVaultInput): Operation<types.DeleteBackupVaultOutput>
    -- Deletes the policy document that manages permissions on a backup vault.
    deleteBackupVaultAccessPolicy: function(Client, types.DeleteBackupVaultAccessPolicyInput): Operation<types.DeleteBackupVaultAccessPolicyOutput>
    -- Deletes Backup Vault Lock from a backup vault specified by a backup vault
    -- name.
    -- If the Vault Lock configuration is immutable, then you cannot delete Vault Lock using
    -- API operations, and you will receive an InvalidRequestException if you attempt
    -- to do so. For more information, see Vault Lock in the
    -- Backup Developer Guide.
    deleteBackupVaultLockConfiguration: function(Client, types.DeleteBackupVaultLockConfigurationInput): Operation<types.DeleteBackupVaultLockConfigurationOutput>
    -- Deletes event notifications for the specified backup vault.
    deleteBackupVaultNotifications: function(Client, types.DeleteBackupVaultNotificationsInput): Operation<types.DeleteBackupVaultNotificationsOutput>
    -- Deletes the framework specified by a framework name.
    deleteFramework: function(Client, types.DeleteFrameworkInput): Operation<types.DeleteFrameworkOutput>
    -- Deletes the recovery point specified by a recovery point ID.
    -- If the recovery point ID belongs to a continuous backup, calling this endpoint deletes
    -- the existing continuous backup and stops future continuous backup.
    -- When an IAM role's permissions are insufficient to call this API, the service sends back
    -- an HTTP 200 response with an empty HTTP body, but the recovery point is not deleted.
    -- Instead, it enters an EXPIRED state.
    -- EXPIRED recovery points can be deleted with this API once the IAM role
    -- has the iam:CreateServiceLinkedRole action. To learn more about adding this role, see
    -- Troubleshooting manual deletions.
    -- If the user or role is deleted or the permission within the role is removed,
    -- the deletion will not be successful and will enter an EXPIRED state.
    deleteRecoveryPoint: function(Client, types.DeleteRecoveryPointInput): Operation<types.DeleteRecoveryPointOutput>
    -- Deletes the report plan specified by a report plan name.
    deleteReportPlan: function(Client, types.DeleteReportPlanInput): Operation<types.DeleteReportPlanOutput>
    -- This request deletes the specified restore testing plan.
    -- Deletion can only successfully occur if all associated
    -- restore testing selections are deleted first.
    deleteRestoreTestingPlan: function(Client, types.DeleteRestoreTestingPlanInput): Operation<types.DeleteRestoreTestingPlanOutput>
    -- Input the Restore Testing Plan name and Restore Testing Selection
    -- name.
    -- All testing selections associated with a restore testing plan must
    -- be deleted before the restore testing plan can be deleted.
    deleteRestoreTestingSelection: function(Client, types.DeleteRestoreTestingSelectionInput): Operation<types.DeleteRestoreTestingSelectionOutput>
    -- Deletes the tiering configuration specified by a tiering configuration name.
    deleteTieringConfiguration: function(Client, types.DeleteTieringConfigurationInput): Operation<types.DeleteTieringConfigurationOutput>
    -- Returns backup job details for the specified BackupJobId.
    describeBackupJob: function(Client, types.DescribeBackupJobInput): Operation<types.DescribeBackupJobOutput>
    -- Returns metadata about a backup vault specified by its name.
    describeBackupVault: function(Client, types.DescribeBackupVaultInput): Operation<types.DescribeBackupVaultOutput>
    -- Returns metadata associated with creating a copy of a resource.
    describeCopyJob: function(Client, types.DescribeCopyJobInput): Operation<types.DescribeCopyJobOutput>
    -- Returns the framework details for the specified FrameworkName.
    describeFramework: function(Client, types.DescribeFrameworkInput): Operation<types.DescribeFrameworkOutput>
    -- Describes whether the Amazon Web Services account has enabled different cross-account management options, including cross-account backup, multi-party approval, and delegated administrator. Returns an error if the account is not a member of an Organizations organization. Example: describe-global-settings --region us-west-2
    describeGlobalSettings: function(Client, types.DescribeGlobalSettingsInput): Operation<types.DescribeGlobalSettingsOutput>
    -- Returns information about a saved resource, including the last time it was backed up,
    -- its Amazon Resource Name (ARN), and the Amazon Web Services service type of the saved
    -- resource.
    describeProtectedResource: function(Client, types.DescribeProtectedResourceInput): Operation<types.DescribeProtectedResourceOutput>
    -- Returns metadata associated with a recovery point, including ID, status, encryption, and
    -- lifecycle.
    describeRecoveryPoint: function(Client, types.DescribeRecoveryPointInput): Operation<types.DescribeRecoveryPointOutput>
    -- Returns the current service opt-in settings for the Region. If service opt-in is enabled
    -- for a service, Backup tries to protect that service's resources in this Region,
    -- when the resource is included in an on-demand backup or scheduled backup plan. Otherwise,
    -- Backup does not try to protect that service's resources in this
    -- Region.
    describeRegionSettings: function(Client, types.DescribeRegionSettingsInput): Operation<types.DescribeRegionSettingsOutput>
    -- Returns the details associated with creating a report as specified by its
    -- ReportJobId.
    describeReportJob: function(Client, types.DescribeReportJobInput): Operation<types.DescribeReportJobOutput>
    -- Returns a list of all report plans for an Amazon Web Services account and Amazon Web Services Region.
    describeReportPlan: function(Client, types.DescribeReportPlanInput): Operation<types.DescribeReportPlanOutput>
    -- Returns metadata associated with a restore job that is specified by a job ID.
    describeRestoreJob: function(Client, types.DescribeRestoreJobInput): Operation<types.DescribeRestoreJobOutput>
    -- Returns scan job details for the specified ScanJobID.
    describeScanJob: function(Client, types.DescribeScanJobInput): Operation<types.DescribeScanJobOutput>
    -- Removes the association between an MPA approval team and a backup vault, disabling the MPA approval workflow for restore operations.
    disassociateBackupVaultMpaApprovalTeam: function(Client, types.DisassociateBackupVaultMpaApprovalTeamInput): Operation<types.DisassociateBackupVaultMpaApprovalTeamOutput>
    -- Deletes the specified continuous backup recovery point from Backup and
    -- releases control of that continuous backup to the source service, such as Amazon RDS. The source service will continue to create and retain continuous backups using the
    -- lifecycle that you specified in your original backup plan.
    -- Does not support snapshot backup recovery points.
    disassociateRecoveryPoint: function(Client, types.DisassociateRecoveryPointInput): Operation<types.DisassociateRecoveryPointOutput>
    -- This action to a specific child (nested) recovery point removes the relationship
    -- between the specified recovery point and its parent (composite) recovery point.
    disassociateRecoveryPointFromParent: function(Client, types.DisassociateRecoveryPointFromParentInput): Operation<types.DisassociateRecoveryPointFromParentOutput>
    -- Returns the backup plan that is specified by the plan ID as a backup template.
    exportBackupPlanTemplate: function(Client, types.ExportBackupPlanTemplateInput): Operation<types.ExportBackupPlanTemplateOutput>
    -- Returns BackupPlan details for the specified BackupPlanId. The
    -- details are the body of a backup plan in JSON format, in addition to plan metadata.
    getBackupPlan: function(Client, types.GetBackupPlanInput): Operation<types.GetBackupPlanOutput>
    -- Returns a valid JSON document specifying a backup plan or an error.
    getBackupPlanFromJSON: function(Client, types.GetBackupPlanFromJSONInput): Operation<types.GetBackupPlanFromJSONOutput>
    -- Returns the template specified by its templateId as a backup plan.
    getBackupPlanFromTemplate: function(Client, types.GetBackupPlanFromTemplateInput): Operation<types.GetBackupPlanFromTemplateOutput>
    -- Returns selection metadata and a document in JSON format that specifies a list of
    -- resources that are associated with a backup plan.
    getBackupSelection: function(Client, types.GetBackupSelectionInput): Operation<types.GetBackupSelectionOutput>
    -- Returns the access policy document that is associated with the named backup
    -- vault.
    getBackupVaultAccessPolicy: function(Client, types.GetBackupVaultAccessPolicyInput): Operation<types.GetBackupVaultAccessPolicyOutput>
    -- Returns event notifications for the specified backup vault.
    getBackupVaultNotifications: function(Client, types.GetBackupVaultNotificationsInput): Operation<types.GetBackupVaultNotificationsOutput>
    -- This action returns details for a specified legal hold. The details are the
    -- body of a legal hold in JSON format, in addition to metadata.
    getLegalHold: function(Client, types.GetLegalHoldInput): Operation<types.GetLegalHoldOutput>
    -- This operation returns the metadata and details specific to
    -- the backup index associated with the specified recovery point.
    getRecoveryPointIndexDetails: function(Client, types.GetRecoveryPointIndexDetailsInput): Operation<types.GetRecoveryPointIndexDetailsOutput>
    -- Returns a set of metadata key-value pairs that were used to create the backup.
    getRecoveryPointRestoreMetadata: function(Client, types.GetRecoveryPointRestoreMetadataInput): Operation<types.GetRecoveryPointRestoreMetadataOutput>
    -- This request returns the metadata for the specified restore job.
    getRestoreJobMetadata: function(Client, types.GetRestoreJobMetadataInput): Operation<types.GetRestoreJobMetadataOutput>
    -- This request returns the minimal required set of metadata needed to
    -- start a restore job with secure default settings. BackupVaultName
    -- and RecoveryPointArn are required parameters.
    -- BackupVaultAccountId is an optional parameter.
    getRestoreTestingInferredMetadata: function(Client, types.GetRestoreTestingInferredMetadataInput): Operation<types.GetRestoreTestingInferredMetadataOutput>
    -- Returns RestoreTestingPlan details for the specified
    -- RestoreTestingPlanName. The details are the body of a restore testing plan
    -- in JSON format, in addition to plan metadata.
    getRestoreTestingPlan: function(Client, types.GetRestoreTestingPlanInput): Operation<types.GetRestoreTestingPlanOutput>
    -- Returns RestoreTestingSelection, which displays resources
    -- and elements of the restore testing plan.
    getRestoreTestingSelection: function(Client, types.GetRestoreTestingSelectionInput): Operation<types.GetRestoreTestingSelectionOutput>
    -- Returns the Amazon Web Services resource types supported by Backup.
    getSupportedResourceTypes: function(Client, types.GetSupportedResourceTypesInput): Operation<types.GetSupportedResourceTypesOutput>
    -- Returns TieringConfiguration details for the specified
    -- TieringConfigurationName. The details are the body of a tiering configuration
    -- in JSON format, in addition to configuration metadata.
    getTieringConfiguration: function(Client, types.GetTieringConfigurationInput): Operation<types.GetTieringConfigurationOutput>
    -- Returns a list of existing backup jobs for an authenticated account for the last 30
    -- days. For a longer period of time, consider using these monitoring tools.
    listBackupJobs: function(Client, types.ListBackupJobsInput): Operation<types.ListBackupJobsOutput>
    -- This is a request for a summary of backup jobs created
    -- or running within the most recent 30 days. You can
    -- include parameters AccountID, State, ResourceType, MessageCategory,
    -- AggregationPeriod, MaxResults, or NextToken to filter
    -- results.
    -- This request returns a summary that contains
    -- Region, Account, State, ResourceType, MessageCategory,
    -- StartTime, EndTime, and Count of included jobs.
    listBackupJobSummaries: function(Client, types.ListBackupJobSummariesInput): Operation<types.ListBackupJobSummariesOutput>
    -- Lists the active backup plans for the account.
    listBackupPlans: function(Client, types.ListBackupPlansInput): Operation<types.ListBackupPlansOutput>
    -- Lists the backup plan templates.
    listBackupPlanTemplates: function(Client, types.ListBackupPlanTemplatesInput): Operation<types.ListBackupPlanTemplatesOutput>
    -- Returns version metadata of your backup plans, including Amazon Resource Names (ARNs),
    -- backup plan IDs, creation and deletion dates, plan names, and version IDs.
    listBackupPlanVersions: function(Client, types.ListBackupPlanVersionsInput): Operation<types.ListBackupPlanVersionsOutput>
    -- Returns an array containing metadata of the resources associated with the target backup
    -- plan.
    listBackupSelections: function(Client, types.ListBackupSelectionsInput): Operation<types.ListBackupSelectionsOutput>
    -- Returns a list of recovery point storage containers along with information about
    -- them.
    listBackupVaults: function(Client, types.ListBackupVaultsInput): Operation<types.ListBackupVaultsOutput>
    -- Returns metadata about your copy jobs.
    listCopyJobs: function(Client, types.ListCopyJobsInput): Operation<types.ListCopyJobsOutput>
    -- This request obtains a list of copy jobs created
    -- or running within the the most recent 30 days. You can
    -- include parameters AccountID, State, ResourceType, MessageCategory,
    -- AggregationPeriod, MaxResults, or NextToken to filter
    -- results.
    -- This request returns a summary that contains
    -- Region, Account, State, RestourceType, MessageCategory,
    -- StartTime, EndTime, and Count of included jobs.
    listCopyJobSummaries: function(Client, types.ListCopyJobSummariesInput): Operation<types.ListCopyJobSummariesOutput>
    -- Returns a list of all frameworks for an Amazon Web Services account and Amazon Web Services Region.
    listFrameworks: function(Client, types.ListFrameworksInput): Operation<types.ListFrameworksOutput>
    -- This operation returns a list of recovery points that have an
    -- associated index, belonging to the specified account.
    -- Optional parameters you can include are: MaxResults;
    -- NextToken; SourceResourceArns; CreatedBefore; CreatedAfter;
    -- and ResourceType.
    listIndexedRecoveryPoints: function(Client, types.ListIndexedRecoveryPointsInput): Operation<types.ListIndexedRecoveryPointsOutput>
    -- This action returns metadata about active and previous legal holds.
    listLegalHolds: function(Client, types.ListLegalHoldsInput): Operation<types.ListLegalHoldsOutput>
    -- Returns an array of resources successfully backed up by Backup, including
    -- the time the resource was saved, an Amazon Resource Name (ARN) of the resource, and a
    -- resource type.
    listProtectedResources: function(Client, types.ListProtectedResourcesInput): Operation<types.ListProtectedResourcesOutput>
    -- This request lists the protected resources corresponding to each backup vault.
    listProtectedResourcesByBackupVault: function(Client, types.ListProtectedResourcesByBackupVaultInput): Operation<types.ListProtectedResourcesByBackupVaultOutput>
    -- Returns detailed information about the recovery points stored in a backup vault.
    listRecoveryPointsByBackupVault: function(Client, types.ListRecoveryPointsByBackupVaultInput): Operation<types.ListRecoveryPointsByBackupVaultOutput>
    -- This action returns recovery point ARNs (Amazon Resource Names) of the
    -- specified legal hold.
    listRecoveryPointsByLegalHold: function(Client, types.ListRecoveryPointsByLegalHoldInput): Operation<types.ListRecoveryPointsByLegalHoldOutput>
    -- The information about the recovery points of the type specified by a
    -- resource Amazon Resource Name (ARN).
    -- For Amazon EFS and Amazon EC2, this action only lists recovery points
    -- created by Backup.
    listRecoveryPointsByResource: function(Client, types.ListRecoveryPointsByResourceInput): Operation<types.ListRecoveryPointsByResourceOutput>
    -- Returns details about your report jobs.
    listReportJobs: function(Client, types.ListReportJobsInput): Operation<types.ListReportJobsOutput>
    -- Returns a list of your report plans. For detailed information about a single report
    -- plan, use DescribeReportPlan.
    listReportPlans: function(Client, types.ListReportPlansInput): Operation<types.ListReportPlansOutput>
    -- Returns a list of restore access backup vaults associated with a specified backup vault.
    listRestoreAccessBackupVaults: function(Client, types.ListRestoreAccessBackupVaultsInput): Operation<types.ListRestoreAccessBackupVaultsOutput>
    -- Returns a list of jobs that Backup initiated to restore a saved resource,
    -- including details about the recovery process.
    listRestoreJobs: function(Client, types.ListRestoreJobsInput): Operation<types.ListRestoreJobsOutput>
    -- This returns restore jobs that contain the specified protected resource.
    -- You must include ResourceArn. You can optionally include
    -- NextToken, ByStatus, MaxResults,
    -- ByRecoveryPointCreationDateAfter , and
    -- ByRecoveryPointCreationDateBefore.
    listRestoreJobsByProtectedResource: function(Client, types.ListRestoreJobsByProtectedResourceInput): Operation<types.ListRestoreJobsByProtectedResourceOutput>
    -- This request obtains a summary of restore jobs created
    -- or running within the the most recent 30 days. You can
    -- include parameters AccountID, State, ResourceType,
    -- AggregationPeriod, MaxResults, or NextToken to filter
    -- results.
    -- This request returns a summary that contains
    -- Region, Account, State, RestourceType, MessageCategory,
    -- StartTime, EndTime, and Count of included jobs.
    listRestoreJobSummaries: function(Client, types.ListRestoreJobSummariesInput): Operation<types.ListRestoreJobSummariesOutput>
    -- Returns a list of restore testing plans.
    listRestoreTestingPlans: function(Client, types.ListRestoreTestingPlansInput): Operation<types.ListRestoreTestingPlansOutput>
    -- Returns a list of restore testing selections. Can be filtered
    -- by MaxResults and RestoreTestingPlanName.
    listRestoreTestingSelections: function(Client, types.ListRestoreTestingSelectionsInput): Operation<types.ListRestoreTestingSelectionsOutput>
    -- Returns a list of existing scan jobs for an authenticated account for the last 30 days.
    listScanJobs: function(Client, types.ListScanJobsInput): Operation<types.ListScanJobsOutput>
    -- This is a request for a summary of scan jobs created or running within the most recent 30 days.
    listScanJobSummaries: function(Client, types.ListScanJobSummariesInput): Operation<types.ListScanJobSummariesOutput>
    -- Returns the tags assigned to the resource, such as a target recovery point, backup plan,
    -- or backup vault.
    -- This operation returns results depending on the resource type used in the value for
    -- resourceArn. For example, recovery points of Amazon DynamoDB with
    -- Advanced Settings have an ARN (Amazon Resource Name) that begins with
    -- arn:aws:backup. Recovery points (backups) of DynamoDB without
    -- Advanced Settings enabled have an ARN that begins with
    -- arn:aws:dynamodb.
    -- When this operation is called and when you include values of resourceArn
    -- that have an ARN other than arn:aws:backup, it may return one of the
    -- exceptions listed below. To prevent this exception, include only values representing
    -- resource types that are fully managed by Backup. These have an ARN that begins
    -- arn:aws:backup and they are noted in the Feature availability by resource table.
    listTags: function(Client, types.ListTagsInput): Operation<types.ListTagsOutput>
    -- Returns a list of tiering configurations.
    listTieringConfigurations: function(Client, types.ListTieringConfigurationsInput): Operation<types.ListTieringConfigurationsOutput>
    -- Sets a resource-based policy that is used to manage access permissions on the target
    -- backup vault. Requires a backup vault name and an access policy document in JSON
    -- format.
    putBackupVaultAccessPolicy: function(Client, types.PutBackupVaultAccessPolicyInput): Operation<types.PutBackupVaultAccessPolicyOutput>
    -- Applies Backup Vault Lock to a backup vault, preventing attempts to delete
    -- any recovery point stored in or created in a backup vault. Vault Lock also prevents
    -- attempts to update the lifecycle policy that controls the retention period of any recovery
    -- point currently stored in a backup vault. If specified, Vault Lock enforces a minimum and
    -- maximum retention period for future backup and copy jobs that target a backup vault.
    -- Backup Vault Lock has been assessed by Cohasset Associates for use in environments
    -- that are subject to SEC 17a-4, CFTC, and FINRA regulations. For more information about
    -- how Backup Vault Lock relates to these regulations, see the
    -- Cohasset Associates
    -- Compliance Assessment.
    -- For more information, see Backup Vault Lock.
    putBackupVaultLockConfiguration: function(Client, types.PutBackupVaultLockConfigurationInput): Operation<types.PutBackupVaultLockConfigurationOutput>
    -- Turns on notifications on a backup vault for the specified topic and events.
    putBackupVaultNotifications: function(Client, types.PutBackupVaultNotificationsInput): Operation<types.PutBackupVaultNotificationsOutput>
    -- This request allows you to send your independent self-run
    -- restore test validation results.
    -- RestoreJobId and ValidationStatus
    -- are required. Optionally, you can input a
    -- ValidationStatusMessage.
    putRestoreValidationResult: function(Client, types.PutRestoreValidationResultInput): Operation<types.PutRestoreValidationResultOutput>
    -- Revokes access to a restore access backup vault, removing the ability to restore from its recovery points and permanently deleting the vault.
    revokeRestoreAccessBackupVault: function(Client, types.RevokeRestoreAccessBackupVaultInput): Operation<types.RevokeRestoreAccessBackupVaultOutput>
    -- Starts an on-demand backup job for the specified resource.
    startBackupJob: function(Client, types.StartBackupJobInput): Operation<types.StartBackupJobOutput>
    -- Starts a job to create a one-time copy of the specified resource.
    -- Does not support continuous backups.
    -- See Copy
    -- job retry for information on how Backup retries copy job
    -- operations.
    startCopyJob: function(Client, types.StartCopyJobInput): Operation<types.StartCopyJobOutput>
    -- Starts an on-demand report job for the specified report plan.
    startReportJob: function(Client, types.StartReportJobInput): Operation<types.StartReportJobOutput>
    -- Recovers the saved resource identified by an Amazon Resource Name (ARN).
    startRestoreJob: function(Client, types.StartRestoreJobInput): Operation<types.StartRestoreJobOutput>
    -- Starts scanning jobs for specific resources.
    startScanJob: function(Client, types.StartScanJobInput): Operation<types.StartScanJobOutput>
    -- Attempts to cancel a job to create a one-time backup of a resource.
    -- This action is not supported for the following services:
    -- Amazon Aurora
    -- Amazon DocumentDB (with MongoDB compatibility)
    -- Amazon FSx for Lustre
    -- Amazon FSx for NetApp ONTAP
    -- Amazon FSx for OpenZFS
    -- Amazon FSx for Windows File Server
    -- Amazon Neptune
    -- SAP HANA databases on Amazon EC2 instances
    -- Amazon RDS
    stopBackupJob: function(Client, types.StopBackupJobInput): Operation<types.StopBackupJobOutput>
    -- Assigns a set of key-value pairs to a resource.
    tagResource: function(Client, types.TagResourceInput): Operation<types.TagResourceOutput>
    -- Removes a set of key-value pairs from a recovery point, backup plan, or backup vault
    -- identified by an Amazon Resource Name (ARN)
    -- This API is not supported for recovery points for resource types
    -- including Aurora, Amazon DocumentDB. Amazon EBS,
    -- Amazon FSx, Neptune, and Amazon RDS.
    untagResource: function(Client, types.UntagResourceInput): Operation<types.UntagResourceOutput>
    -- Updates the specified backup plan. The new version is uniquely identified by its ID.
    updateBackupPlan: function(Client, types.UpdateBackupPlanInput): Operation<types.UpdateBackupPlanOutput>
    -- Updates the specified framework.
    updateFramework: function(Client, types.UpdateFrameworkInput): Operation<types.UpdateFrameworkOutput>
    -- Updates whether the Amazon Web Services account has enabled different cross-account management options, including cross-account backup, multi-party approval, and delegated administrator. Returns an error if the account is not an Organizations management account. Use the DescribeGlobalSettings API to determine the current settings.
    updateGlobalSettings: function(Client, types.UpdateGlobalSettingsInput): Operation<types.UpdateGlobalSettingsOutput>
    -- This operation updates the settings of a recovery point index.
    -- Required: BackupVaultName, RecoveryPointArn, and IAMRoleArn
    updateRecoveryPointIndexSettings: function(Client, types.UpdateRecoveryPointIndexSettingsInput): Operation<types.UpdateRecoveryPointIndexSettingsOutput>
    -- Sets the transition lifecycle of a recovery point.
    -- The lifecycle defines when a protected resource is transitioned to cold storage and when
    -- it expires. Backup transitions and expires backups automatically according to
    -- the lifecycle that you define.
    -- Resource types that can transition to cold storage are listed in the Feature availability by resource table. Backup ignores this expression for
    -- other resource types.
    -- Backups transitioned to cold storage must be stored in cold storage for a minimum of 90
    -- days. Therefore, the “retention” setting must be 90 days greater than the “transition to
    -- cold after days” setting. The “transition to cold after days” setting cannot be changed
    -- after a backup has been transitioned to cold.
    -- If your lifecycle currently uses the parameters DeleteAfterDays and
    -- MoveToColdStorageAfterDays, include these parameters and their values when you call
    -- this operation. Not including them may result in your plan updating with null values.
    -- This operation does not support continuous backups.
    updateRecoveryPointLifecycle: function(Client, types.UpdateRecoveryPointLifecycleInput): Operation<types.UpdateRecoveryPointLifecycleOutput>
    -- Updates the current service opt-in settings for the Region.
    -- Use
    -- the DescribeRegionSettings API to determine the resource types that are
    -- supported.
    updateRegionSettings: function(Client, types.UpdateRegionSettingsInput): Operation<types.UpdateRegionSettingsOutput>
    -- Updates the specified report plan.
    updateReportPlan: function(Client, types.UpdateReportPlanInput): Operation<types.UpdateReportPlanOutput>
    -- This request will send changes to your specified restore testing
    -- plan. RestoreTestingPlanName
    -- cannot be updated after it is created.
    -- RecoveryPointSelection can contain:
    -- Algorithm
    -- ExcludeVaults
    -- IncludeVaults
    -- RecoveryPointTypes
    -- SelectionWindowDays
    updateRestoreTestingPlan: function(Client, types.UpdateRestoreTestingPlanInput): Operation<types.UpdateRestoreTestingPlanOutput>
    -- Updates the specified restore testing selection.
    -- Most elements except the RestoreTestingSelectionName
    -- can be updated with this request.
    -- You can use either protected resource ARNs or conditions, but not both.
    updateRestoreTestingSelection: function(Client, types.UpdateRestoreTestingSelectionInput): Operation<types.UpdateRestoreTestingSelectionOutput>
    -- This request will send changes to your specified tiering
    -- configuration. TieringConfigurationName
    -- cannot be updated after it is created.
    -- ResourceSelection can contain:
    -- Resources
    -- TieringDownSettingsInDays
    -- ResourceType
    updateTieringConfiguration: function(Client, types.UpdateTieringConfigurationInput): Operation<types.UpdateTieringConfigurationOutput>
end

local record M
    new: function(table): Client
end

return M
