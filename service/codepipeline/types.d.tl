-- Code generated by lua-client-codegen. DO NOT EDIT.

-- Represents the input of an AcknowledgeJob action.
local record AcknowledgeJobInput
    -- The unique system-generated ID of the job for which you want to confirm
    -- receipt.
    jobId: string
    -- A system-generated random number that CodePipeline uses to ensure that the
    -- job is being worked on by only one job worker. Get this number from the response of the
    -- PollForJobs request that returned this job.
    nonce: string
end

-- Represents the output of an AcknowledgeJob action.
local record AcknowledgeJobOutput
    -- Whether the job worker has received the specified job.
    status: string
end

-- Represents the input of an AcknowledgeThirdPartyJob action.
local record AcknowledgeThirdPartyJobInput
    -- The unique system-generated ID of the job.
    jobId: string
    -- A system-generated random number that CodePipeline uses to ensure that the
    -- job is being worked on by only one job worker. Get this number from the response to a
    -- GetThirdPartyJobDetails request.
    nonce: string
    -- The clientToken portion of the clientId and clientToken pair used to verify that
    -- the calling entity is allowed access to the job and its details.
    clientToken: string
end

-- Represents the output of an AcknowledgeThirdPartyJob action.
local record AcknowledgeThirdPartyJobOutput
    -- The status information for the third party job, if any.
    status: string
end

-- Represents the input of a CreateCustomActionType operation.
local record CreateCustomActionTypeInput
    -- The category of the custom action, such as a build action or a test
    -- action.
    category: string
    -- The provider of the service used in the custom action, such as
    -- CodeDeploy.
    provider: string
    -- The version identifier of the custom action.
    version: string
    -- URLs that provide users information about this custom action.
    settings: ActionTypeSettings
    -- The configuration properties for the custom action.
    -- You can refer to a name in the configuration properties of the custom action
    -- within the URL templates by following the format of {Config:name}, as long as the
    -- configuration property is both required and not secret. For more information, see
    -- Create a
    -- Custom Action for a Pipeline.
    configurationProperties: {ActionConfigurationProperty}
    -- The details of the input artifact for the action, such as its commit ID.
    inputArtifactDetails: ArtifactDetails
    -- The details of the output artifact of the action, such as its commit ID.
    outputArtifactDetails: ArtifactDetails
    -- The tags for the custom action.
    tags: {Tag}
end

-- Represents the output of a CreateCustomActionType operation.
local record CreateCustomActionTypeOutput
    -- Returns information about the details of an action type.
    actionType: ActionType
    -- Specifies the tags applied to the custom action.
    tags: {Tag}
end

-- Represents the input of a CreatePipeline action.
local record CreatePipelineInput
    -- Represents the structure of actions and stages to be performed in the pipeline.
    pipeline: PipelineDeclaration
    -- The tags for the pipeline.
    tags: {Tag}
end

-- Represents the output of a CreatePipeline action.
local record CreatePipelineOutput
    -- Represents the structure of actions and stages to be performed in the pipeline.
    pipeline: PipelineDeclaration
    -- Specifies the tags applied to the pipeline.
    tags: {Tag}
end

-- Represents the input of a DeleteCustomActionType operation. The custom
-- action will be marked as deleted.
local record DeleteCustomActionTypeInput
    -- The category of the custom action that you want to delete, such as source or
    -- deploy.
    category: string
    -- The provider of the service used in the custom action, such as
    -- CodeDeploy.
    provider: string
    -- The version of the custom action to delete.
    version: string
end

local record DeleteCustomActionTypeOutput
end

-- Represents the input of a DeletePipeline action.
local record DeletePipelineInput
    -- The name of the pipeline to be deleted.
    name: string
end

local record DeletePipelineOutput
end

local record DeleteWebhookInput
    -- The name of the webhook you want to delete.
    name: string
end

local record DeleteWebhookOutput
end

local record DeregisterWebhookWithThirdPartyInput
    -- The name of the webhook you want to deregister.
    webhookName: string
end

local record DeregisterWebhookWithThirdPartyOutput
end

-- Represents the input of a DisableStageTransition action.
local record DisableStageTransitionInput
    -- The name of the pipeline in which you want to disable the flow of artifacts from
    -- one stage to another.
    pipelineName: string
    -- The name of the stage where you want to disable the inbound or outbound transition
    -- of artifacts.
    stageName: string
    -- Specifies whether artifacts are prevented from transitioning into the stage and
    -- being processed by the actions in that stage (inbound), or prevented from transitioning
    -- from the stage after they have been processed by the actions in that stage
    -- (outbound).
    transitionType: string
    -- The reason given to the user that a stage is disabled, such as waiting for manual
    -- approval or manual tests. This message is displayed in the pipeline console
    -- UI.
    reason: string
end

local record DisableStageTransitionOutput
end

-- Represents the input of an EnableStageTransition action.
local record EnableStageTransitionInput
    -- The name of the pipeline in which you want to enable the flow of artifacts from one
    -- stage to another.
    pipelineName: string
    -- The name of the stage where you want to enable the transition of artifacts, either
    -- into the stage (inbound) or from that stage to the next stage (outbound).
    stageName: string
    -- Specifies whether artifacts are allowed to enter the stage and be processed by the
    -- actions in that stage (inbound) or whether already processed artifacts are allowed to
    -- transition to the next stage (outbound).
    transitionType: string
end

local record EnableStageTransitionOutput
end

local record GetActionTypeInput
    -- Defines what kind of action can be taken in the stage. The following are the valid
    -- values:
    -- Source
    -- Build
    -- Test
    -- Deploy
    -- Approval
    -- Invoke
    -- Compute
    category: string
    -- The creator of an action type that was created with any supported integration model.
    -- There are two valid values: AWS and ThirdParty.
    owner: string
    -- The provider of the action type being called. The provider name is specified when the
    -- action type is created.
    provider: string
    -- A string that describes the action type version.
    version: string
end

local record GetActionTypeOutput
    -- The action type information for the requested action type, such as the action type
    -- ID.
    actionType: ActionTypeDeclaration
end

-- Represents the input of a GetJobDetails action.
local record GetJobDetailsInput
    -- The unique system-generated ID for the job.
    jobId: string
end

-- Represents the output of a GetJobDetails action.
local record GetJobDetailsOutput
    -- The details of the job.
    -- If AWSSessionCredentials is used, a long-running job can call
    -- GetJobDetails again to obtain new credentials.
    jobDetails: JobDetails
end

-- Represents the input of a GetPipeline action.
local record GetPipelineInput
    -- The name of the pipeline for which you want to get information. Pipeline names must
    -- be unique in an Amazon Web Services account.
    name: string
    -- The version number of the pipeline. If you do not specify a version, defaults to
    -- the current version.
    version: number
end

-- Represents the output of a GetPipeline action.
local record GetPipelineOutput
    -- Represents the structure of actions and stages to be performed in the pipeline.
    pipeline: PipelineDeclaration
    -- Represents the pipeline metadata information returned as part of the output of a
    -- GetPipeline action.
    metadata: PipelineMetadata
end

-- Represents the input of a GetPipelineExecution action.
local record GetPipelineExecutionInput
    -- The name of the pipeline about which you want to get execution details.
    pipelineName: string
    -- The ID of the pipeline execution about which you want to get execution
    -- details.
    pipelineExecutionId: string
end

-- Represents the output of a GetPipelineExecution action.
local record GetPipelineExecutionOutput
    -- Represents information about the execution of a pipeline.
    pipelineExecution: PipelineExecution
end

-- Represents the input of a GetPipelineState action.
local record GetPipelineStateInput
    -- The name of the pipeline about which you want to get information.
    name: string
end

-- Represents the output of a GetPipelineState action.
local record GetPipelineStateOutput
    -- The name of the pipeline for which you want to get the state.
    pipelineName: string
    -- The version number of the pipeline.
    -- A newly created pipeline is always assigned a version number of
    -- 1.
    pipelineVersion: number
    -- A list of the pipeline stage output information, including stage name, state, most
    -- recent run details, whether the stage is disabled, and other data.
    stageStates: {StageState}
    -- The date and time the pipeline was created, in timestamp format.
    created: number
    -- The date and time the pipeline was last updated, in timestamp format.
    updated: number
end

-- Represents the input of a GetThirdPartyJobDetails action.
local record GetThirdPartyJobDetailsInput
    -- The unique system-generated ID used for identifying the job.
    jobId: string
    -- The clientToken portion of the clientId and clientToken pair used to verify that
    -- the calling entity is allowed access to the job and its details.
    clientToken: string
end

-- Represents the output of a GetThirdPartyJobDetails action.
local record GetThirdPartyJobDetailsOutput
    -- The details of the job, including any protected values defined for the
    -- job.
    jobDetails: ThirdPartyJobDetails
end

local record ListActionExecutionsInput
    -- The name of the pipeline for which you want to list action execution history.
    pipelineName: string
    -- Input information used to filter action execution history.
    filter: ActionExecutionFilter
    -- The maximum number of results to return in a single call. To retrieve the remaining
    -- results, make another call with the returned nextToken value. Action execution history
    -- is retained for up to 12 months, based on action execution start times. Default value is
    -- 100.
    maxResults: number
    -- The token that was returned from the previous ListActionExecutions call,
    -- which can be used to return the next set of action executions in the list.
    nextToken: string
end

local record ListActionExecutionsOutput
    -- The details for a list of recent executions, such as action execution ID.
    actionExecutionDetails: {ActionExecutionDetail}
    -- If the amount of returned information is significantly large, an identifier is also
    -- returned and can be used in a subsequent ListActionExecutions call to
    -- return the next set of action executions in the list.
    nextToken: string
end

-- Represents the input of a ListActionTypes action.
local record ListActionTypesInput
    -- Filters the list of action types to those created by a specified entity.
    actionOwnerFilter: string
    -- An identifier that was returned from the previous list action types call, which can
    -- be used to return the next set of action types in the list.
    nextToken: string
    -- The Region to filter on for the list of action types.
    regionFilter: string
end

-- Represents the output of a ListActionTypes action.
local record ListActionTypesOutput
    -- Provides details of the action types.
    actionTypes: {ActionType}
    -- If the amount of returned information is significantly large, an identifier is also
    -- returned. It can be used in a subsequent list action types call to return the next set
    -- of action types in the list.
    nextToken: string
end

local record ListDeployActionExecutionTargetsInput
    -- The name of the pipeline with the deploy action.
    pipelineName: string
    -- The execution ID for the deploy action.
    actionExecutionId: string
    -- Filters the targets for a specified deploy action.
    filters: {TargetFilter}
    -- The maximum number of results to return in a single call. To retrieve the remaining
    -- results, make another call with the returned nextToken value.
    maxResults: number
    -- An identifier that was returned from the previous list action types call, which can be
    -- used to return the next set of action types in the list.
    nextToken: string
end

local record ListDeployActionExecutionTargetsOutput
    -- The targets for the deploy action.
    targets: {DeployActionExecutionTarget}
    -- An identifier that was returned from the previous list action types call, which can be
    -- used to return the next set of action types in the list.
    nextToken: string
end

-- Represents the input of a ListPipelineExecutions action.
local record ListPipelineExecutionsInput
    -- The name of the pipeline for which you want to get execution summary
    -- information.
    pipelineName: string
    -- The maximum number of results to return in a single call. To retrieve the remaining
    -- results, make another call with the returned nextToken value. Pipeline history is
    -- limited to the most recent 12 months, based on pipeline execution start times. Default
    -- value is 100.
    maxResults: number
    -- The pipeline execution to filter on.
    filter: PipelineExecutionFilter
    -- The token that was returned from the previous ListPipelineExecutions
    -- call, which can be used to return the next set of pipeline executions in the
    -- list.
    nextToken: string
end

-- Represents the output of a ListPipelineExecutions action.
local record ListPipelineExecutionsOutput
    -- A list of executions in the history of a pipeline.
    pipelineExecutionSummaries: {PipelineExecutionSummary}
    -- A token that can be used in the next ListPipelineExecutions call. To
    -- view all items in the list, continue to call this operation with each subsequent token
    -- until no more nextToken values are returned.
    nextToken: string
end

-- Represents the input of a ListPipelines action.
local record ListPipelinesInput
    -- An identifier that was returned from the previous list pipelines call. It can be
    -- used to return the next set of pipelines in the list.
    nextToken: string
    -- The maximum number of pipelines to return in a single call. To retrieve the remaining
    -- pipelines, make another call with the returned nextToken value. The minimum value you
    -- can specify is 1. The maximum accepted value is 1000.
    maxResults: number
end

-- Represents the output of a ListPipelines action.
local record ListPipelinesOutput
    -- The list of pipelines.
    pipelines: {PipelineSummary}
    -- If the amount of returned information is significantly large, an identifier is also
    -- returned. It can be used in a subsequent list pipelines call to return the next set of
    -- pipelines in the list.
    nextToken: string
end

local record ListRuleExecutionsInput
    -- The name of the pipeline for which you want to get execution summary
    -- information.
    pipelineName: string
    -- Input information used to filter rule execution history.
    filter: RuleExecutionFilter
    -- The maximum number of results to return in a single call. To retrieve the remaining
    -- results, make another call with the returned nextToken value. Pipeline history is
    -- limited to the most recent 12 months, based on pipeline execution start times. Default
    -- value is 100.
    maxResults: number
    -- The token that was returned from the previous ListRuleExecutions call,
    -- which can be used to return the next set of rule executions in the list.
    nextToken: string
end

local record ListRuleExecutionsOutput
    -- Details about the output for listing rule executions.
    ruleExecutionDetails: {RuleExecutionDetail}
    -- A token that can be used in the next ListRuleExecutions call. To view all
    -- items in the list, continue to call this operation with each subsequent token until no
    -- more nextToken values are returned.
    nextToken: string
end

local record ListRuleTypesInput
    -- The rule owner to filter on.
    ruleOwnerFilter: string
    -- The rule Region to filter on.
    regionFilter: string
end

local record ListRuleTypesOutput
    -- Lists the rules that are configured for the condition.
    ruleTypes: {RuleType}
end

local record ListTagsForResourceInput
    -- The Amazon Resource Name (ARN) of the resource to get tags for.
    resourceArn: string
    -- The token that was returned from the previous API call, which would be used to return
    -- the next page of the list. The ListTagsforResource call lists all available tags in one
    -- call and does not use pagination.
    nextToken: string
    -- The maximum number of results to return in a single call.
    maxResults: number
end

local record ListTagsForResourceOutput
    -- The tags for the resource.
    tags: {Tag}
    -- If the amount of returned information is significantly large, an identifier is also
    -- returned and can be used in a subsequent API call to return the next page of the list.
    -- The ListTagsforResource call lists all available tags in one call and does not use
    -- pagination.
    nextToken: string
end

local record ListWebhooksInput
    -- The token that was returned from the previous ListWebhooks call, which can be used
    -- to return the next set of webhooks in the list.
    NextToken: string
    -- The maximum number of results to return in a single call. To retrieve the remaining
    -- results, make another call with the returned nextToken value.
    MaxResults: number
end

local record ListWebhooksOutput
    -- The JSON detail returned for each webhook in the list output for the ListWebhooks
    -- call.
    webhooks: {ListWebhookItem}
    -- If the amount of returned information is significantly large, an identifier is also
    -- returned and can be used in a subsequent ListWebhooks call to return the next set of
    -- webhooks in the list.
    NextToken: string
end

local record OverrideStageConditionInput
    -- The name of the pipeline with the stage that will override the condition.
    pipelineName: string
    -- The name of the stage for the override.
    stageName: string
    -- The ID of the pipeline execution for the override.
    pipelineExecutionId: string
    -- The type of condition to override for the stage, such as entry conditions, failure
    -- conditions, or success conditions.
    conditionType: string
end

local record OverrideStageConditionOutput
end

-- Represents the input of a PollForJobs action.
local record PollForJobsInput
    -- Represents information about an action type.
    actionTypeId: ActionTypeId
    -- The maximum number of jobs to return in a poll for jobs call.
    maxBatchSize: number
    -- A map of property names and values. For an action type with no queryable
    -- properties, this value must be null or an empty map. For an action type with a queryable
    -- property, you must supply that property as a key in the map. Only jobs whose action
    -- configuration matches the mapped value are returned.
    queryParam: {string : string}
end

-- Represents the output of a PollForJobs action.
local record PollForJobsOutput
    -- Information about the jobs to take action on.
    jobs: {Job}
end

-- Represents the input of a PollForThirdPartyJobs action.
local record PollForThirdPartyJobsInput
    -- Represents information about an action type.
    actionTypeId: ActionTypeId
    -- The maximum number of jobs to return in a poll for jobs call.
    maxBatchSize: number
end

-- Represents the output of a PollForThirdPartyJobs action.
local record PollForThirdPartyJobsOutput
    -- Information about the jobs to take action on.
    jobs: {ThirdPartyJob}
end

-- Represents the input of a PutActionRevision action.
local record PutActionRevisionInput
    -- The name of the pipeline that starts processing the revision to the
    -- source.
    pipelineName: string
    -- The name of the stage that contains the action that acts on the revision.
    stageName: string
    -- The name of the action that processes the revision.
    actionName: string
    -- Represents information about the version (or revision) of an action.
    actionRevision: ActionRevision
end

-- Represents the output of a PutActionRevision action.
local record PutActionRevisionOutput
    -- Indicates whether the artifact revision was previously used in an execution of the
    -- specified pipeline.
    newRevision: boolean
    -- The ID of the current workflow state of the pipeline.
    pipelineExecutionId: string
end

-- Represents the input of a PutApprovalResult action.
local record PutApprovalResultInput
    -- The name of the pipeline that contains the action.
    pipelineName: string
    -- The name of the stage that contains the action.
    stageName: string
    -- The name of the action for which approval is requested.
    actionName: string
    -- Represents information about the result of the approval request.
    result: ApprovalResult
    -- The system-generated token used to identify a unique approval request. The token
    -- for each open approval request can be obtained using the GetPipelineState action. It is used to validate that the approval
    -- request corresponding to this token is still valid.
    -- For a pipeline where the execution mode is set to PARALLEL, the token required to
    -- approve/reject an approval request as detailed above is not available. Instead, use
    -- the externalExecutionId in the response output from the ListActionExecutions action as the token in the approval
    -- request.
    token: string
end

-- Represents the output of a PutApprovalResult action.
local record PutApprovalResultOutput
    -- The timestamp showing when the approval or rejection was submitted.
    approvedAt: number
end

-- Represents the input of a PutJobFailureResult action.
local record PutJobFailureResultInput
    -- The unique system-generated ID of the job that failed. This is the same ID returned
    -- from PollForJobs.
    jobId: string
    -- The details about the failure of a job.
    failureDetails: FailureDetails
end

local record PutJobFailureResultOutput
end

-- Represents the input of a PutJobSuccessResult action.
local record PutJobSuccessResultInput
    -- The unique system-generated ID of the job that succeeded. This is the same ID
    -- returned from PollForJobs.
    jobId: string
    -- The ID of the current revision of the artifact successfully worked on by the
    -- job.
    currentRevision: CurrentRevision
    -- A token generated by a job worker, such as a CodeDeploy deployment ID, that a
    -- successful job provides to identify a custom action in progress. Future jobs use this
    -- token to identify the running instance of the action. It can be reused to return more
    -- information about the progress of the custom action. When the action is complete, no
    -- continuation token should be supplied.
    continuationToken: string
    -- The execution details of the successful job, such as the actions taken by the job
    -- worker.
    executionDetails: ExecutionDetails
    -- Key-value pairs produced as output by a job worker that can be made available to a
    -- downstream action configuration. outputVariables can be included only when
    -- there is no continuation token on the request.
    outputVariables: {string : string}
end

local record PutJobSuccessResultOutput
end

-- Represents the input of a PutThirdPartyJobFailureResult
-- action.
local record PutThirdPartyJobFailureResultInput
    -- The ID of the job that failed. This is the same ID returned from
    -- PollForThirdPartyJobs.
    jobId: string
    -- The clientToken portion of the clientId and clientToken pair used to verify that
    -- the calling entity is allowed access to the job and its details.
    clientToken: string
    -- Represents information about failure details.
    failureDetails: FailureDetails
end

local record PutThirdPartyJobFailureResultOutput
end

-- Represents the input of a PutThirdPartyJobSuccessResult
-- action.
local record PutThirdPartyJobSuccessResultInput
    -- The ID of the job that successfully completed. This is the same ID returned from
    -- PollForThirdPartyJobs.
    jobId: string
    -- The clientToken portion of the clientId and clientToken pair used to verify that
    -- the calling entity is allowed access to the job and its details.
    clientToken: string
    -- Represents information about a current revision.
    currentRevision: CurrentRevision
    -- A token generated by a job worker, such as a CodeDeploy deployment ID, that a
    -- successful job provides to identify a partner action in progress. Future jobs use this
    -- token to identify the running instance of the action. It can be reused to return more
    -- information about the progress of the partner action. When the action is complete, no
    -- continuation token should be supplied.
    continuationToken: string
    -- The details of the actions taken and results produced on an artifact as it passes
    -- through stages in the pipeline.
    executionDetails: ExecutionDetails
end

local record PutThirdPartyJobSuccessResultOutput
end

local record PutWebhookInput
    -- The detail provided in an input file to create the webhook, such as the webhook
    -- name, the pipeline name, and the action name. Give the webhook a unique name that helps
    -- you identify it. You might name the webhook after the pipeline and action it targets so
    -- that you can easily recognize what it's used for later.
    webhook: WebhookDefinition
    -- The tags for the webhook.
    tags: {Tag}
end

local record PutWebhookOutput
    -- The detail returned from creating the webhook, such as the webhook name, webhook
    -- URL, and webhook ARN.
    webhook: ListWebhookItem
end

local record RegisterWebhookWithThirdPartyInput
    -- The name of an existing webhook created with PutWebhook to register with a
    -- supported third party.
    webhookName: string
end

local record RegisterWebhookWithThirdPartyOutput
end

-- Represents the input of a RetryStageExecution action.
local record RetryStageExecutionInput
    -- The name of the pipeline that contains the failed stage.
    pipelineName: string
    -- The name of the failed stage to be retried.
    stageName: string
    -- The ID of the pipeline execution in the failed stage to be retried. Use the GetPipelineState action to retrieve the current pipelineExecutionId of
    -- the failed stage
    pipelineExecutionId: string
    -- The scope of the retry attempt.
    retryMode: string
end

-- Represents the output of a RetryStageExecution action.
local record RetryStageExecutionOutput
    -- The ID of the current workflow execution in the failed stage.
    pipelineExecutionId: string
end

local record RollbackStageInput
    -- The name of the pipeline for which the stage will be rolled back.
    pipelineName: string
    -- The name of the stage in the pipeline to be rolled back.
    stageName: string
    -- The pipeline execution ID for the stage to be rolled back to.
    targetPipelineExecutionId: string
end

local record RollbackStageOutput
    -- The execution ID of the pipeline execution for the stage that has been rolled
    -- back.
    pipelineExecutionId: string
end

-- Represents the input of a StartPipelineExecution action.
local record StartPipelineExecutionInput
    -- The name of the pipeline to start.
    name: string
    -- A list that overrides pipeline variables for a pipeline execution that's being
    -- started. Variable names must match [A-Za-z0-9@\-_]+, and the values can be
    -- anything except an empty string.
    variables: {PipelineVariable}
    -- The system-generated unique ID used to identify a unique execution
    -- request.
    clientRequestToken: string
    -- A list that allows you to specify, or override, the source revision for a pipeline
    -- execution that's being started. A source revision is the version with all the changes to
    -- your application code, or source artifact, for the pipeline execution.
    sourceRevisions: {SourceRevisionOverride}
end

-- Represents the output of a StartPipelineExecution action.
local record StartPipelineExecutionOutput
    -- The unique system-generated ID of the pipeline execution that was
    -- started.
    pipelineExecutionId: string
end

local record StopPipelineExecutionInput
    -- The name of the pipeline to stop.
    pipelineName: string
    -- The ID of the pipeline execution to be stopped in the current stage. Use the
    -- GetPipelineState action to retrieve the current
    -- pipelineExecutionId.
    pipelineExecutionId: string
    -- Use this option to stop the pipeline execution by abandoning, rather than finishing,
    -- in-progress actions.
    -- This option can lead to failed or out-of-sequence tasks.
    abandon: boolean
    -- Use this option to enter comments, such as the reason the pipeline was stopped.
    reason: string
end

local record StopPipelineExecutionOutput
    -- The unique system-generated ID of the pipeline execution that was stopped.
    pipelineExecutionId: string
end

local record TagResourceInput
    -- The Amazon Resource Name (ARN) of the resource you want to add tags to.
    resourceArn: string
    -- The tags you want to modify or add to the resource.
    tags: {Tag}
end

local record TagResourceOutput
end

local record UntagResourceInput
    -- The Amazon Resource Name (ARN) of the resource to remove tags from.
    resourceArn: string
    -- The list of keys for the tags to be removed from the resource.
    tagKeys: {string}
end

local record UntagResourceOutput
end

local record UpdateActionTypeInput
    -- The action type definition for the action type to be updated.
    actionType: ActionTypeDeclaration
end

local record UpdateActionTypeOutput
end

-- Represents the input of an UpdatePipeline action.
local record UpdatePipelineInput
    -- The name of the pipeline to be updated.
    pipeline: PipelineDeclaration
end

-- Represents the output of an UpdatePipeline action.
local record UpdatePipelineOutput
    -- The structure of the updated pipeline.
    pipeline: PipelineDeclaration
end

-- The nonce was specified in an invalid format.
local record InvalidNonceException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job was specified in an invalid format or cannot be found.
local record JobNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The client token was specified in an invalid format
local record InvalidClientTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The nonce was specified in an invalid format.
local record InvalidNonceException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job was specified in an invalid format or cannot be found.
local record JobNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Unable to modify the tag due to a simultaneous update request.
local record ConcurrentModificationException
    message: string
end

-- The specified resource tags are invalid.
local record InvalidTagsException
    message: string
end

-- The number of pipelines associated with the Amazon Web Services account has exceeded
-- the limit allowed for the account.
local record LimitExceededException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The tags limit for a resource has been exceeded.
local record TooManyTagsException
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Unable to modify the tag due to a simultaneous update request.
local record ConcurrentModificationException
    message: string
end

-- The action declaration was specified in an invalid format.
local record InvalidActionDeclarationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Reserved for future use.
local record InvalidBlockerDeclarationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The stage declaration was specified in an invalid format.
local record InvalidStageDeclarationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The structure was specified in an invalid format.
local record InvalidStructureException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified resource tags are invalid.
local record InvalidTagsException
    message: string
end

-- The number of pipelines associated with the Amazon Web Services account has exceeded
-- the limit allowed for the account.
local record LimitExceededException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified pipeline name is already in use.
local record PipelineNameInUseException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The tags limit for a resource has been exceeded.
local record TooManyTagsException
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Unable to modify the tag due to a simultaneous update request.
local record ConcurrentModificationException
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Unable to modify the tag due to a simultaneous update request.
local record ConcurrentModificationException
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Unable to modify the tag due to a simultaneous update request.
local record ConcurrentModificationException
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified webhook was entered in an invalid format or cannot be
-- found.
local record WebhookNotFoundException
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The stage was specified in an invalid format or cannot be found.
local record StageNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The stage was specified in an invalid format or cannot be found.
local record StageNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified action type cannot be found.
local record ActionTypeNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job was specified in an invalid format or cannot be found.
local record JobNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline version was specified in an invalid format or cannot be
-- found.
local record PipelineVersionNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline execution was specified in an invalid format or cannot be found, or an
-- execution ID does not belong to the specified pipeline.
local record PipelineExecutionNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The client token was specified in an invalid format
local record InvalidClientTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job was specified in an invalid format or cannot be found.
local record InvalidJobException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job was specified in an invalid format or cannot be found.
local record JobNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The next token was specified in an invalid format. Make sure that the next token
-- you provide is the token returned by a previous call.
local record InvalidNextTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline execution was specified in an invalid format or cannot be found, or an
-- execution ID does not belong to the specified pipeline.
local record PipelineExecutionNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The next token was specified in an invalid format. Make sure that the next token
-- you provide is the token returned by a previous call.
local record InvalidNextTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The action execution was not found.
local record ActionExecutionNotFoundException
    message: string
end

-- The next token was specified in an invalid format. Make sure that the next token
-- you provide is the token returned by a previous call.
local record InvalidNextTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The next token was specified in an invalid format. Make sure that the next token
-- you provide is the token returned by a previous call.
local record InvalidNextTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The next token was specified in an invalid format. Make sure that the next token
-- you provide is the token returned by a previous call.
local record InvalidNextTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The next token was specified in an invalid format. Make sure that the next token
-- you provide is the token returned by a previous call.
local record InvalidNextTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline execution was specified in an invalid format or cannot be found, or an
-- execution ID does not belong to the specified pipeline.
local record PipelineExecutionNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The next token was specified in an invalid format. Make sure that the next token
-- you provide is the token returned by a previous call.
local record InvalidNextTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified resource ARN is invalid.
local record InvalidArnException
    message: string
end

-- The next token was specified in an invalid format. Make sure that the next token
-- you provide is the token returned by a previous call.
local record InvalidNextTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The resource was specified in an invalid format.
local record ResourceNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The next token was specified in an invalid format. Make sure that the next token
-- you provide is the token returned by a previous call.
local record InvalidNextTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline has reached the limit for concurrent pipeline executions.
local record ConcurrentPipelineExecutionsLimitExceededException
    message: string
end

-- Unable to override because the condition does not allow overrides.
local record ConditionNotOverridableException
    message: string
end

-- Your request cannot be handled because the pipeline is busy handling ongoing
-- activities. Try again later.
local record ConflictException
    message: string
end

-- The stage has failed in a later run of the pipeline and the
-- pipelineExecutionId associated with the request is out of
-- date.
local record NotLatestPipelineExecutionException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The stage was specified in an invalid format or cannot be found.
local record StageNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified action type cannot be found.
local record ActionTypeNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified action type cannot be found.
local record ActionTypeNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified action cannot be found.
local record ActionNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline has reached the limit for concurrent pipeline executions.
local record ConcurrentPipelineExecutionsLimitExceededException
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The stage was specified in an invalid format or cannot be found.
local record StageNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified action cannot be found.
local record ActionNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The approval action has already been approved or rejected.
local record ApprovalAlreadyCompletedException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The approval request already received a response or has expired.
local record InvalidApprovalTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The stage was specified in an invalid format or cannot be found.
local record StageNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job state was specified in an invalid format.
local record InvalidJobStateException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job was specified in an invalid format or cannot be found.
local record JobNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job state was specified in an invalid format.
local record InvalidJobStateException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job was specified in an invalid format or cannot be found.
local record JobNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Exceeded the total size limit for all variables in the pipeline.
local record OutputVariablesSizeExceededException
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The client token was specified in an invalid format
local record InvalidClientTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job state was specified in an invalid format.
local record InvalidJobStateException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job was specified in an invalid format or cannot be found.
local record JobNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The client token was specified in an invalid format
local record InvalidClientTokenException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job state was specified in an invalid format.
local record InvalidJobStateException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The job was specified in an invalid format or cannot be found.
local record JobNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Unable to modify the tag due to a simultaneous update request.
local record ConcurrentModificationException
    message: string
end

-- The specified resource tags are invalid.
local record InvalidTagsException
    message: string
end

-- The specified authentication type is in an invalid format.
local record InvalidWebhookAuthenticationParametersException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified event filter rule is in an invalid format.
local record InvalidWebhookFilterPatternException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The number of pipelines associated with the Amazon Web Services account has exceeded
-- the limit allowed for the account.
local record LimitExceededException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The tags limit for a resource has been exceeded.
local record TooManyTagsException
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified webhook was entered in an invalid format or cannot be
-- found.
local record WebhookNotFoundException
end

-- The pipeline has reached the limit for concurrent pipeline executions.
local record ConcurrentPipelineExecutionsLimitExceededException
    message: string
end

-- Your request cannot be handled because the pipeline is busy handling ongoing
-- activities. Try again later.
local record ConflictException
    message: string
end

-- The stage has failed in a later run of the pipeline and the
-- pipelineExecutionId associated with the request is out of
-- date.
local record NotLatestPipelineExecutionException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The stage was specified in an invalid format or cannot be found.
local record StageNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Unable to retry. The pipeline structure or stage state might have changed while
-- actions awaited retry, or the stage contains no failed
-- actions.
local record StageNotRetryableException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Your request cannot be handled because the pipeline is busy handling ongoing
-- activities. Try again later.
local record ConflictException
    message: string
end

-- The pipeline execution was specified in an invalid format or cannot be found, or an
-- execution ID does not belong to the specified pipeline.
local record PipelineExecutionNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified pipeline execution is outdated and cannot be used as a target pipeline
-- execution for rollback.
local record PipelineExecutionOutdatedException
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The stage was specified in an invalid format or cannot be found.
local record StageNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Unable to roll back the stage. The cause might be if the pipeline version has changed
-- since the target pipeline execution was deployed, the stage is currently running, or an
-- incorrect target pipeline execution ID was provided.
local record UnableToRollbackStageException
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The pipeline has reached the limit for concurrent pipeline executions.
local record ConcurrentPipelineExecutionsLimitExceededException
    message: string
end

-- Your request cannot be handled because the pipeline is busy handling ongoing
-- activities. Try again later.
local record ConflictException
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Your request cannot be handled because the pipeline is busy handling ongoing
-- activities. Try again later.
local record ConflictException
    message: string
end

-- The pipeline execution is already in a Stopping state. If you already
-- chose to stop and wait, you cannot make that request again. You can choose to stop and
-- abandon now, but be aware that this option can lead to failed tasks or out of sequence
-- tasks. If you already chose to stop and abandon, you cannot make that request
-- again.
local record DuplicatedStopRequestException
    message: string
end

-- Unable to stop the pipeline execution. The execution might already be in a
-- Stopped state, or it might no longer be in progress.
local record PipelineExecutionNotStoppableException
    message: string
end

-- The pipeline was specified in an invalid format or cannot be found.
local record PipelineNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Unable to modify the tag due to a simultaneous update request.
local record ConcurrentModificationException
    message: string
end

-- The specified resource ARN is invalid.
local record InvalidArnException
    message: string
end

-- The specified resource tags are invalid.
local record InvalidTagsException
    message: string
end

-- The resource was specified in an invalid format.
local record ResourceNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The tags limit for a resource has been exceeded.
local record TooManyTagsException
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Unable to modify the tag due to a simultaneous update request.
local record ConcurrentModificationException
    message: string
end

-- The specified resource ARN is invalid.
local record InvalidArnException
    message: string
end

-- The specified resource tags are invalid.
local record InvalidTagsException
    message: string
end

-- The resource was specified in an invalid format.
local record ResourceNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The specified action type cannot be found.
local record ActionTypeNotFoundException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The request failed because of an unknown error, exception, or failure.
local record RequestFailedException
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The action declaration was specified in an invalid format.
local record InvalidActionDeclarationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- Reserved for future use.
local record InvalidBlockerDeclarationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The stage declaration was specified in an invalid format.
local record InvalidStageDeclarationException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The structure was specified in an invalid format.
local record InvalidStructureException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The number of pipelines associated with the Amazon Web Services account has exceeded
-- the limit allowed for the account.
local record LimitExceededException
    -- The message provided to the user in the event of an exception.
    message: string
end

-- The validation was specified in an invalid format.
local record ValidationException
    -- The message provided to the user in the event of an exception.
    message: string
end

local type ActionCategory = "Source" | "Build" | "Deploy" | "Test" | "Invoke" | "Approval" | "Compute"

local type ActionOwner = "AWS" | "ThirdParty" | "Custom"

local type ConditionType = "BEFORE_ENTRY" | "ON_SUCCESS"

local type JobStatus = "Created" | "Queued" | "Dispatched" | "InProgress" | "TimedOut" | "Succeeded" | "Failed"

local type RuleOwner = "AWS"

local type StageRetryMode = "FAILED_ACTIONS" | "ALL_ACTIONS"

local type StageTransitionType = "Inbound" | "Outbound"

local record M
    AcknowledgeJobInput: AcknowledgeJobInput
    AcknowledgeJobOutput: AcknowledgeJobOutput
    AcknowledgeThirdPartyJobInput: AcknowledgeThirdPartyJobInput
    AcknowledgeThirdPartyJobOutput: AcknowledgeThirdPartyJobOutput
    CreateCustomActionTypeInput: CreateCustomActionTypeInput
    CreateCustomActionTypeOutput: CreateCustomActionTypeOutput
    CreatePipelineInput: CreatePipelineInput
    CreatePipelineOutput: CreatePipelineOutput
    DeleteCustomActionTypeInput: DeleteCustomActionTypeInput
    DeleteCustomActionTypeOutput: DeleteCustomActionTypeOutput
    DeletePipelineInput: DeletePipelineInput
    DeletePipelineOutput: DeletePipelineOutput
    DeleteWebhookInput: DeleteWebhookInput
    DeleteWebhookOutput: DeleteWebhookOutput
    DeregisterWebhookWithThirdPartyInput: DeregisterWebhookWithThirdPartyInput
    DeregisterWebhookWithThirdPartyOutput: DeregisterWebhookWithThirdPartyOutput
    DisableStageTransitionInput: DisableStageTransitionInput
    DisableStageTransitionOutput: DisableStageTransitionOutput
    EnableStageTransitionInput: EnableStageTransitionInput
    EnableStageTransitionOutput: EnableStageTransitionOutput
    GetActionTypeInput: GetActionTypeInput
    GetActionTypeOutput: GetActionTypeOutput
    GetJobDetailsInput: GetJobDetailsInput
    GetJobDetailsOutput: GetJobDetailsOutput
    GetPipelineInput: GetPipelineInput
    GetPipelineOutput: GetPipelineOutput
    GetPipelineExecutionInput: GetPipelineExecutionInput
    GetPipelineExecutionOutput: GetPipelineExecutionOutput
    GetPipelineStateInput: GetPipelineStateInput
    GetPipelineStateOutput: GetPipelineStateOutput
    GetThirdPartyJobDetailsInput: GetThirdPartyJobDetailsInput
    GetThirdPartyJobDetailsOutput: GetThirdPartyJobDetailsOutput
    ListActionExecutionsInput: ListActionExecutionsInput
    ListActionExecutionsOutput: ListActionExecutionsOutput
    ListActionTypesInput: ListActionTypesInput
    ListActionTypesOutput: ListActionTypesOutput
    ListDeployActionExecutionTargetsInput: ListDeployActionExecutionTargetsInput
    ListDeployActionExecutionTargetsOutput: ListDeployActionExecutionTargetsOutput
    ListPipelineExecutionsInput: ListPipelineExecutionsInput
    ListPipelineExecutionsOutput: ListPipelineExecutionsOutput
    ListPipelinesInput: ListPipelinesInput
    ListPipelinesOutput: ListPipelinesOutput
    ListRuleExecutionsInput: ListRuleExecutionsInput
    ListRuleExecutionsOutput: ListRuleExecutionsOutput
    ListRuleTypesInput: ListRuleTypesInput
    ListRuleTypesOutput: ListRuleTypesOutput
    ListTagsForResourceInput: ListTagsForResourceInput
    ListTagsForResourceOutput: ListTagsForResourceOutput
    ListWebhooksInput: ListWebhooksInput
    ListWebhooksOutput: ListWebhooksOutput
    OverrideStageConditionInput: OverrideStageConditionInput
    OverrideStageConditionOutput: OverrideStageConditionOutput
    PollForJobsInput: PollForJobsInput
    PollForJobsOutput: PollForJobsOutput
    PollForThirdPartyJobsInput: PollForThirdPartyJobsInput
    PollForThirdPartyJobsOutput: PollForThirdPartyJobsOutput
    PutActionRevisionInput: PutActionRevisionInput
    PutActionRevisionOutput: PutActionRevisionOutput
    PutApprovalResultInput: PutApprovalResultInput
    PutApprovalResultOutput: PutApprovalResultOutput
    PutJobFailureResultInput: PutJobFailureResultInput
    PutJobFailureResultOutput: PutJobFailureResultOutput
    PutJobSuccessResultInput: PutJobSuccessResultInput
    PutJobSuccessResultOutput: PutJobSuccessResultOutput
    PutThirdPartyJobFailureResultInput: PutThirdPartyJobFailureResultInput
    PutThirdPartyJobFailureResultOutput: PutThirdPartyJobFailureResultOutput
    PutThirdPartyJobSuccessResultInput: PutThirdPartyJobSuccessResultInput
    PutThirdPartyJobSuccessResultOutput: PutThirdPartyJobSuccessResultOutput
    PutWebhookInput: PutWebhookInput
    PutWebhookOutput: PutWebhookOutput
    RegisterWebhookWithThirdPartyInput: RegisterWebhookWithThirdPartyInput
    RegisterWebhookWithThirdPartyOutput: RegisterWebhookWithThirdPartyOutput
    RetryStageExecutionInput: RetryStageExecutionInput
    RetryStageExecutionOutput: RetryStageExecutionOutput
    RollbackStageInput: RollbackStageInput
    RollbackStageOutput: RollbackStageOutput
    StartPipelineExecutionInput: StartPipelineExecutionInput
    StartPipelineExecutionOutput: StartPipelineExecutionOutput
    StopPipelineExecutionInput: StopPipelineExecutionInput
    StopPipelineExecutionOutput: StopPipelineExecutionOutput
    TagResourceInput: TagResourceInput
    TagResourceOutput: TagResourceOutput
    UntagResourceInput: UntagResourceInput
    UntagResourceOutput: UntagResourceOutput
    UpdateActionTypeInput: UpdateActionTypeInput
    UpdateActionTypeOutput: UpdateActionTypeOutput
    UpdatePipelineInput: UpdatePipelineInput
    UpdatePipelineOutput: UpdatePipelineOutput
end

return M
